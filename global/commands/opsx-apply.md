---
description: Implement tasks from an OpenSpec change (Experimental)
---

Load the `openspec-apply-change` skill and follow its complete workflow for this command.

Treat `$ARGUMENTS` as the optional change name or apply request. If the skill is unavailable, report that exact missing capability and do not recreate a partial apply workflow in this command.
