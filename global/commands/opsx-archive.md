---
description: Deterministically validate, synchronize, and archive a completed OpenSpec change
---

Load the `openspec-archive-change` skill and follow its complete workflow for this command.

Treat `$ARGUMENTS` as the optional change name or archive request. If the skill is unavailable, report that exact missing capability and do not recreate a partial archive workflow in this command.
