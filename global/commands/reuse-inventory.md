---
description: Inventory or refresh explicitly configured projects in a private reuse registry from a plain-language request.
agent: build
---

Load the `reuse-discovery` skill and execute its **Inventory Command** workflow for this entire free-form request:

$ARGUMENTS

The text above is user intent, not positional or flag syntax. Resolve only named projects/groups and exact bindings already present in the explicit private reuse config. Show the concise resolved preview, then continue autonomously when it is exact and safe. Ask one precise question only for a material identity ambiguity or when a registry URL has no existing local checkout. Never clone, fetch, pull, commit, push, install, publish, infer an unconfigured root, enumerate unrelated projects, or expose private roots in shared output.
