---
description: Propose a new change - create it and generate all artifacts in one step
---

Load the `openspec-propose` skill and follow its complete workflow for this command.

Treat `$ARGUMENTS` as the requested change name or description. If the skill is unavailable, report that exact missing capability and do not recreate a partial proposal workflow in this command.
