{
  "schemaVersion": 1,
  "id": "config-missing-deterministic-gate",
  "taskType": "config",
  "status": "Review",
  "priority": "medium",
  "dependencies": [],
  "scope": {
    "read": ["config/**", "fixtures/config/**"],
    "write": ["config/**", "fixtures/config/**"],
    "forbidden": ["openspec/changes/*/automation/**", ".autopilot/**"]
  },
  "autonomy": {
    "allowCommit": true,
    "allowPush": true,
    "allowCreateMr": true,
    "allowMerge": false
  },
  "validation": {
    "commands": [{ "name": "config validation", "command": "<validation-command>", "status": "passed" }]
  },
  "phaseProfile": {
    "analyze": { "required": true, "depth": "focused" },
    "implementation": { "required": true, "mode": "schema-first" },
    "review": { "required": true, "mode": "config-review" },
    "acceptance": { "required": true, "mr": "required" }
  },
  "phaseEvidence": {
    "analyze": {
      "planSummary": "Update one configuration surface.",
      "slices": ["Add config fixture", "Update defaults"],
      "scope": "config and fixtures/config only",
      "testStrategy": "Schema-backed fixture validation."
    }
  },
  "testDecision": {
    "decision": "required",
    "reason": "Config changes require deterministic schema or fixture evidence.",
    "evidence": ["fixtures/config/minimal.json"]
  },
  "plan": {
    "summary": "Update one configuration surface.",
    "slices": ["Add config fixture", "Update defaults", "Review"],
    "scope": "config/** and fixtures/config/**",
    "testStrategy": "Schema-backed fixture validation."
  },
  "reviewPolicy": {
    "required": [{ "reviewer": "deployment-config-reviewer", "status": "pending", "reason": "Configuration semantics changed." }],
    "skipped": []
  },
  "mr": {
    "required": true,
    "status": "waiting-review",
    "url": "https://example.invalid/mr/config-missing-deterministic-gate"
  },
  "blockers": [],
  "feedback": [],
  "history": [
    { "from": "Ready", "to": "Analyze", "at": "2026-06-10T00:00:00.000Z", "by": "plugin", "source": "autopilot_run_next", "evidence": { "reason": "Config task selected." } },
    { "from": "Analyze", "to": "Implementation", "at": "2026-06-10T00:01:00.000Z", "by": "plugin", "source": "autopilot_run_next", "evidence": { "planSummary": "Update one configuration surface.", "slices": ["Add config fixture", "Update defaults"], "scope": "config and fixtures/config only", "testStrategy": "Schema-backed fixture validation." } },
    { "from": "Implementation", "to": "Review", "at": "2026-06-10T00:02:00.000Z", "by": "plugin", "source": "autopilot_collect", "evidence": { "changedFiles": ["config/defaults.json", "fixtures/config/minimal.json"], "validation": { "status": "passed", "commands": ["<validation-command>"] }, "secretScan": { "status": "placeholder", "reason": "Scanner integration deferred." } } }
  ],
  "revision": {
    "number": 3,
    "contentHash": "placeholder",
    "updatedBy": "plugin",
    "updatedAt": "2026-06-10T00:02:00.000Z"
  }
}
