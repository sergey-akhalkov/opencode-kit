{
  "schemaVersion": 1,
  "id": "tooling-deterministic-gate",
  "taskType": "tooling",
  "status": "Review",
  "priority": "medium",
  "dependencies": [],
  "scope": {
    "read": ["tools/**", "fixtures/tooling/**"],
    "write": ["tools/**", "fixtures/tooling/**"],
    "forbidden": ["openspec/changes/*/automation/**", ".autopilot/**"]
  },
  "autonomy": {
    "allowCommit": true,
    "allowPush": true,
    "allowCreateMr": true,
    "allowMerge": false
  },
  "validation": {
    "commands": [{ "name": "tooling tests", "command": "<validation-command>", "status": "passed" }]
  },
  "phaseProfile": {
    "analyze": { "required": true, "depth": "focused" },
    "implementation": { "required": true, "mode": "test-first" },
    "review": { "required": true, "mode": "tooling-review" },
    "acceptance": { "required": true, "mr": "required" }
  },
  "phaseEvidence": {
    "analyze": {
      "planSummary": "Update one deterministic tooling path.",
      "slices": ["Add fixture", "Update tool"],
      "scope": "tools and fixtures/tooling only",
      "testStrategy": "Fixture-backed tooling validation."
    }
  },
  "testDecision": {
    "decision": "required",
    "reason": "Tooling changes require deterministic fixture coverage.",
    "evidence": ["fixtures/tooling/example.json"]
  },
  "plan": {
    "summary": "Update one deterministic tooling path.",
    "slices": ["Add fixture", "Update tool", "Review"],
    "scope": "tools/** and fixtures/tooling/**",
    "testStrategy": "Fixture-backed tooling validation."
  },
  "reviewPolicy": {
    "required": [
      { "reviewer": "code-quality-reviewer", "status": "pending", "reason": "Tool implementation changed." },
      { "reviewer": "test-coverage-reviewer", "status": "pending", "reason": "Tooling fixture coverage must be reviewed." }
    ],
    "skipped": []
  },
  "mr": {
    "required": true,
    "status": "waiting-review",
    "url": "https://example.invalid/mr/tooling-deterministic-gate"
  },
  "blockers": [],
  "feedback": [],
  "history": [
    { "from": "Ready", "to": "Analyze", "at": "2026-06-10T00:00:00.000Z", "by": "plugin", "source": "autopilot_run_next", "evidence": { "reason": "Tooling task selected." } },
    { "from": "Analyze", "to": "Implementation", "at": "2026-06-10T00:01:00.000Z", "by": "plugin", "source": "autopilot_run_next", "evidence": { "planSummary": "Update one deterministic tooling path.", "slices": ["Add fixture", "Update tool"], "scope": "tools and fixtures/tooling only", "testStrategy": "Fixture-backed tooling validation." } },
    { "from": "Implementation", "to": "Review", "at": "2026-06-10T00:02:00.000Z", "by": "plugin", "source": "autopilot_collect", "evidence": { "changedFiles": ["tools/example.ts", "fixtures/tooling/example.json"], "validation": { "status": "passed", "commands": ["<validation-command>"] }, "secretScan": { "status": "placeholder", "reason": "Scanner integration deferred." }, "toolingGate": { "summary": "Fixture validates the tooling output contract.", "command": "<validation-command>", "fixture": "fixtures/tooling/example.json" } } }
  ],
  "revision": {
    "number": 3,
    "contentHash": "placeholder",
    "updatedBy": "plugin",
    "updatedAt": "2026-06-10T00:02:00.000Z"
  }
}
