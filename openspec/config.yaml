schema: spec-driven

context: |
  Apply the working philosophy: high quality without proxy substitution, the
  shortest verified path, autonomy until a real owner boundary, maximum token
  economy, and evidence-backed continuous improvement. Fix, narrow, or remove
  concrete impediments at the smallest authorized layer without weakening safety,
  accepted scope, protected boundaries, or unrelated work. Define the next useful
  working increment, not the imagined final system.
  Every behavior-changing increment needs an Outcome, technically enforced
  Operating Envelope, Non-Goals, Non-Deferrable Invariants, Observable Proof,
  Material Residual Risks, and Stop Line. Resolve only decisions that can change
  the current increment or a protected boundary. Keep unreachable future behavior
  outside current acceptance. Main owns runtime proof and focused validation;
  fresh critical-only SDET follows proof and accepted-scope completion only for
  a reachable named critical consequence or explicit requirement. Minimize time-to-first-real-signal for every
  behavior dependency chain without bypassing owner authorization, safety,
  restoration, cleanup, or live-attempt gates. Plans, artifacts, task inventories,
  attempt limits, and stop lines remain autonomous implementation controls when
  accepted semantics stay unchanged; their update never authorizes the underlying
  protected action. Record Claim And Evidence Scope once at change level. One
  Ordinary Small exact-case line names the exact claim and matching proof boundary.
  A declared broad class instead names its claim id/class, population, coverage
  basis, production/comparison paths, environment, real oracle, unresolved
  observations, and maximum claim before mutation; deterministic helpers validate
  only those reviewed fields and never infer breadth or semantics from prose.
  Before representing decision-material planning as semantically implementation-ready,
  main obtains one bounded original-request-grounded challenge from the existing
  outcome-readiness owner. Ordinary Small exact work may use a reviewed exemption.
  Deterministic checks establish structural artifact readiness only and never supply
  semantic task-fit evidence.
  Edits alone never complete a task.

rules:
  proposal:
    - Include an Outcome Capsule with all seven current-increment fields from project context.
    - Include one Claim And Evidence Scope owner. Use one concise exact-claim/proof-boundary line for Ordinary Small exact work; for a declared broad class use explicit Claim ID, Claim Class, Population, Coverage Basis, Production Path, Comparison Paths, Environment, Real Oracle, Unresolved Observations, and Maximum Claim fields.
    - Include exactly one Bounded Falsification Review declaration: required with the decision surface for decision-material work, or exempt with a reviewed Ordinary Small reason. Keep structural artifact readiness separate from semantic implementation readiness.
    - State user value and why this bounded increment is needed now.
    - List exact new and modified capabilities; do not hide behavior changes as implementation detail.
  design:
    - Resolve only current-increment architecture, ownership, failure boundaries, and protected decisions.
    - Record alternatives and why a simpler remove, narrow, reuse, or local-guard option is insufficient.
    - Keep future scale, variants, and compatibility out of the current design unless reachable in the enforced envelope.
    - Define the current fidelity rung, next real boundary, and exact authorization, safeguards, restoration, cleanup, and evidence needed to reach it.
    - Treat agent-authored attempt ceilings and process stop lines as revisable controls, not owner scope; keep authority for the underlying protected action explicit.
    - Reference the change-level claim record and add only architecture or proof-boundary decisions that change it; do not repeat the complete record.
  specs:
    - Give every current-increment requirement at least one observable acceptance scenario.
    - Do not use TBD, as appropriate, handle errors, or another vague placeholder in actionable requirements.
    - Keep non-applicable and future behavior explicit rather than silently omitting or requiring it.
  tasks:
    - Group work by meaningful behavior, evidence, or gate outcome rather than one checkbox per file.
    - Put the smallest complete happy path and observable runtime proof before broad testing or optional review.
    - Put the earliest safely reachable real check before dependent behavior; if deferred, name the exact blocker, earliest unblocking task, and dependency-chain stop condition.
    - State the exact observable proof and applicable focused validation for each behavior task; keep it unchecked until both pass.
    - Represent corrections required for the accepted outcome as ordinary tasks; keep optional retrospective, process, token, or tooling observations outside the product task graph.
    - Main may author the smallest focused regression after current proof; triggered independent critical test artifacts belong to fresh test-only SDET.
    - When evidence invalidates an agent-authored plan or attempt limit without changing accepted semantics, update the affected artifacts/tasks/history and continue without an owner process-approval question.
    - Reference claim ids and state only each task's changed row, next proof, or blocker; do not duplicate unchanged Claim And Evidence Scope fields in every task.
