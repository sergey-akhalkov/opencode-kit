## 1. Ownership And Earliest Characterization

- [ ] 1.1 Re-read the dirty worktree and reconcile exact ownership for the planned instruction, validator, package, config, proof, CI, and OpenSpec paths before mutation; verify `make-team-advice-evidence-triggered` archived first and `add-prospective-consequence-rehearsal` archived next with terminal writers; reread the current mutation-disabled `ownership.json`, record exact transfers, and enable mutation only after `npm run openspec:change-inventory -- --mode ownership`, current `git status --short`, package scripts, runtime/plugin entrypoints, and direct overlapping artifacts prove every overlap has a current dependency/transfer or a non-overlapping write boundary. **Current Rung:** planning-only current source with explicit predecessor dependencies. **Next Real Boundary:** pinned analyzer installation and provider-free characterization in tasks 1.2-1.3. **Blocker/Unblocker:** either predecessor still active, any live or unresolved overlapping writer, or stale transfer evidence blocks production mutation until terminal closure and exact readback; do not infer availability from HEAD or stale Codebase Memory. **Observable Proof:** current ownership output and source readback identify one semantic owner per planned behavior, exact archived-predecessor transfers, and no implementation path relying on an unresolved overlap. **Validation:** focused OpenSpec change-inventory test, strict selected-change validation, propose/apply ownership checks, and current inventory JSON/readback.

- [ ] 1.2 Pin `knip`, `typescript`, and `@types/node` as development-only dependencies, update the package lock without changing runtime dependencies or scripts beyond the later planned focused entry, and verify exact installed versions, Node 24 compatibility, license metadata, package/lock agreement, and import/CLI resolution from the repository root. **Current Rung:** source-verified external dependency with runtime fit unknown. **Next Real Boundary:** direct production-mode analyzer invocation over disposable/current reviewed roots in task 1.3. **Blocker/Unblocker:** task 1.1 ownership closure; dependency resolution, unsupported engine, unexpected lifecycle script, runtime dependency movement, or lock drift stops analyzer-dependent work. **Observable Proof:** package manager exits zero, package and lock read back the same exact development versions, local CLI/help is effect-free, and no configured provider, install profile, remote repository, or consumer project is touched. **Validation:** package/lock readback and the existing package-script validation suite.

- [ ] 1.3 Characterize `CRVD-001` before gate wiring or loaded instruction edits: enumerate exact project patterns, package-script/CLI/plugin/extension/public/generated-profile/dynamic roots and source references, exclusions, unsupported edges, production-versus-test behavior, current normalized Knip findings, baseline need, analyzer timeout/terminal cleanup, and TypeScript `noEmit`/`noUnusedLocals`/`noUnusedParameters` feasibility in process-owned disposable configuration. **Current Rung:** direct provider-free analyzer boundary. **Next Real Boundary:** maintained read-only validator owner in task 2.1. **Blocker/Unblocker:** tasks 1.1-1.2; any failed, unsupported, or incomplete row blocks dependent gate wiring and loaded reachability claims until the mechanism is corrected and re-characterized or proposal/design/spec maximum claim is narrowed before further mutation. **Observable Proof:** exact argv, version, status, stdout/stderr, normalized findings, roots, test-only red control, temporary effects, terminal child state, and cleanup are inspected; a second unchanged run is stable. **Validation:** direct positive and red disposable probes plus source/package/config readback. **Dependency-Chain Stop Condition:** no task 2 or 3 mutation until analyzer fit, current roots, findings/baseline need, and process cleanup are known and the planning claim matches them.

## 2. Production-Reachability Capability

- [ ] 2.1 [automation-dividend] Implement one cohesive `tools/validators/production-reachability.ts` owner plus a thin explicit `tools/production-reachability.ts` CLI adapter and reviewed `config/production-reachability.json` seed. The owner SHALL provide a read-only check API, shell-free pinned production-mode analyzer invocation, stable project-relative finding normalization, exact optional-baseline comparison, bounded timeout, terminal child closure, `finally` cleanup, and cause-preserving diagnostics; only the explicit CLI materialization mode may write an explicitly named baseline target, and strict/pre-push/CI/default check paths cannot reach it. **Current Rung:** green characterization from task 1.3. **Next Real Boundary:** direct focused fixture matrix in task 2.2. **Blocker/Unblocker:** task 1.3 and any required planning-claim correction; unsupported analyzer shape or unsafe process ownership returns to characterization rather than adding custom AST/regex analysis. **Observable Proof:** effect-free `--help` and check modes exit zero for a reachable disposable capability; malformed seed, false/test root, analyzer failure, timeout, nonterminal child, unsupported finding, and write-attempt controls fail for exact causes; source/config/package bytes remain unchanged in check mode and temporary state is removed. **Authorization/Safeguards/Cleanup:** local repository implementation/test/config writes and process-owned temporary projects only; deterministic code validates explicit facts and never infers value, demand, deletion, documentation truth, or portability. **Validation:** focused owner/CLI/schema/process tests, two stable check runs, package-lock readback, and source-before/after manifests.

- [ ] 2.2 Add one provider-free `tools/test-production-reachability.ts` owner that creates and removes disposable TypeScript projects for reachable first use, second production consumer, unused file/export, test-only use, transitively unreachable chain, last-consumer removal, every reviewed root kind, unsupported dynamic edge, malformed configuration, analyzer failure/timeout, new exact finding, stale baseline identity, stale baseline row, and cleanup failure; generate `config/production-reachability-baseline.json` through the guarded path only if task 1.3 found exact unrelated current debt, with one reviewed reason/evidence reference per row. **Current Rung:** direct validator happy path from task 2.1. **Next Real Boundary:** current repository orchestration in task 2.3. **Blocker/Unblocker:** task 2.1; no baseline is permitted for a new/current-change finding, count budget, wildcard, blank reason, false root, test-only use, or speculative consumer. **Observable Proof:** the complete finite fixture set reports stable ordered rows; each red control fails its intended oracle; last-consumer removal changes reachable to unused despite retained tests; baseline materialization/readback is byte-stable when applicable; all child processes and temp roots are terminal and removed. **Validation:** focused reachability suite twice plus malformed/schema/privacy/path-escape and cleanup controls.

- [ ] 2.3 Integrate the read-only reachability check into `tools/validate-library.ts`, focused package scripts, full repository validation, pre-push, and CI through their existing owners, adding no duplicate invocation/parser policy; run the actual gate against the current repository and verify any compiler-local-declaration phase only when task 1.3 proved its fit. **Current Rung:** directly proved reachability capability. **Next Real Boundary:** provider-free installed-behavior seed and unchanged loaded baseline in tasks 3.1-3.2. **Blocker/Unblocker:** tasks 2.1-2.2; a current finding not covered by the accepted claim, stale baseline, false root, timeout, cleanup failure, or write effect blocks integration until corrected at its owner. **Observable Proof:** focused command, `npm run validate:strict`, `npm run prepush:validate`, and the CI command plan invoke the same read-only owner once; current normalized findings and unknown edges match task 1.3; no baseline or source file changes during validation. **Validation:** reachability, library-validation, pre-push-plan, Windows-envelope, and applicable CI workflow tests plus source-before/after readback.

## 3. Loaded Value Behavior And Knowledge Freshness

- [ ] 3.1 Extend the smallest existing consumer-outcome, composable-capability, and reuse-discovery seed/evaluator owners with the exact `CRVD-001` installed population and deliberate red controls, keeping semantic expectations in reviewed data and adding no second generic runner or mixed proof owner. Include first-consumer readiness, second-consumer reuse, last-consumer retirement, cohesive direct implementation, trivial-fix proportionality, current/stale knowledge, native-analyzer use, unsupported-analyzer `unknown`, output-as-value, test-only liveness, and progress-fact-versus-`Delivery Checkpoint State` cases. **Current Rung:** green provider-free repository gate. **Next Real Boundary:** unchanged-source configured baseline in task 3.2. **Blocker/Unblocker:** task 2.3 and exact proof-owner mapping; if the current runners cannot represent a row without mixing responsibilities or weakening permissions/evidence/cleanup, update design/tasks before adding only the smallest owner-local extension. **Observable Proof:** effect-free help and provider-free preflight enumerate the complete finite population, two evaluations are byte-stable, every red control fails its intended explicit oracle, model/provider calls are zero, product source is unchanged, and disposable state is removed. **Validation:** focused consumer-outcome, reuse-discovery, composable-capability/contract, seed/schema/readback, privacy, permission, and cleanup tests.

- [ ] 3.2 Capture the unchanged loaded-instruction baseline for the complete configured `CRVD-001` behavior population through actual proof-owned OpenCode entrypoints before editing canonical or loaded instructions; preserve identical reviewed prompts, model/profile/variant, permissions, environment, initial fixtures, real oracles, effects, and cleanup for later candidate comparison. **Current Rung:** provider-free proof preflight. **Next Real Boundary:** canonical/loaded instruction implementation in task 3.3. **Blocker/Unblocker:** task 3.1; no configured call starts until provider-free schema, permission, path, terminal-process, and cleanup controls pass. A finalized failed call is inspected and not repeated unchanged. **Observable Proof:** every baseline scenario has current source/model/environment identity, terminal session/process state, exact outputs/tool events/file/effect manifest, direct and parent observations when applicable, structural-versus-semantic freshness facts, value-progress-fact presence/absence, and removed disposable roots; no repository product mutation or remote/protected effect occurs. **Validation:** current evaluator over preserved baseline bundles and provider-free replay/readback.

- [ ] 3.3 Update `global/principles-of-work.md` as the complete continuous-value owner and apply only compact role-specific deltas to `global/AGENTS.md`, `instructions/universal-development-loop.md`, `instructions/reusable-project-agent-instructions.md`, README routing, and exact maintained contract/mirror owners. Preserve existing reuse triggers, direct/parent proof, completion, and `Delivery Checkpoint State`; add first-consumer Value Ready semantics, observed second reuse, production reachability/retirement, federated discovery, bounded knowledge freshness, portable native-analyzer-or-`unknown`, and ordinary value progress facts without a new registry, skill, process gate, lifecycle, or checkpoint identity. **Current Rung:** current unchanged loaded baseline. **Next Real Boundary:** actual candidate configured behavior in task 3.4. **Blocker/Unblocker:** tasks 2.3 and 3.2; any baseline identity gap, unsupported gate claim, or overlapping active writer blocks only the affected instruction/proof path. **Observable Proof:** canonical-owner and compact-mirror checks pass; exact-duplication and forbidden registry/checklist/value-metric/progress-state controls pass; focused positive/negative instruction fixtures distinguish structural markers from semantic behavior. **Validation:** focused contracts, library, reuse-discovery, instruction-context, Markdown/mirror, and strict instruction validation.

- [ ] 3.4 Run matched candidate configured scenarios through the same actual OpenCode entrypoints and evaluate them against task 3.2 without changing model/profile/permissions/environment or fixture contracts. Verify first-consumer readiness includes direct proof, one production consumer, parent integration, reachability, discoverability, and retirement; second use reuses the owner; last-consumer removal retires the orphan; cohesive/trivial controls add no mechanism or progress ceremony; knowledge claims stop at executable/source evidence; unsupported portable analysis reports `unknown`; and no result creates `Delivery Checkpoint State` or completes an open parent. **Current Rung:** loaded candidate source. **Next Real Boundary:** complete repository integration in task 4.1. **Blocker/Unblocker:** task 3.3 and a clear configured proof path; failed scenarios are independently dispositioned, only invalidated rows rerun after a causal correction, and no reviewer/model output authorizes mutation. **Observable Proof:** current candidate/source/model/environment identities, exact scenario outcomes, tool/file/effect manifests, reachability/freshness facts, direct and parent observations, value progress facts, diagnostics, terminal writer/session/process closure, cleanup, and finite maximum claim all evaluate current. **Validation:** configured candidate evaluator plus provider-free replay of preserved bundles.

## 4. Integration And Completion Evidence

- [ ] 4.1 Exercise the integrated candidate at the nearest actual shipped boundaries: focused production-reachability CLI, `npm run validate:strict`, `npm run prepush:validate`, configured OpenCode value scenarios, and source-linked knowledge controls; inspect status, stdout/stderr, normalized findings, files/effects, process/session closure, and cleanup, and correct any accepted-outcome defect at its owning leaf or parent boundary. **Current Rung:** direct capability and configured scenario proof. **Next Real Boundary:** complete project-native validation in task 4.2. **Blocker/Unblocker:** tasks 2.3 and 3.4; an integration-only defect stays at this parent boundary and does not reopen unrelated passing fixture/scenario leaves unless their identity changed. **Observable Proof:** the same current candidate passes each actual boundary without validation writes, leaked processes/temp roots, registry/checkpoint artifacts, installation, restart, consumer-project mutation, configured-provider expansion, or remote effect. **Validation:** command outputs and current source/config/package/proof readback.

- [ ] 4.2 Run a changed-code maintainability reduction review over the reachability owner, thin CLI, fixtures, orchestration edits, and instruction deltas; remove or narrow confirmed duplication, wrapper-only behavior, mixed responsibilities, blanket roots/ignores, and unnecessary concepts while retaining every unique `CRVD-001` oracle, then rerun only invalidated direct and parent boundaries. **Current Rung:** working integrated candidate. **Next Real Boundary:** frozen complete validation in task 4.3. **Blocker/Unblocker:** task 4.1; reviewer or inventory observations do not authorize scope expansion, and optional unrelated debt remains parked. **Observable Proof:** one semantic reachability owner remains, validation and materialization paths stay separated, no second proof runner or value owner exists, and any accepted reduction preserves focused and installed behavior. **Validation:** focused reachability/contracts/proof tests plus affected strict and configured evaluations.

- [ ] 4.3 Freeze the current planning/implementation candidate and run `npm test`, `npm run validate:strict`, `npm run prepush:validate`, `npm run openspec:validate`, strict selected-change validation, the portable OpenSpec apply gate, `git diff --check`, package/lock/config schema readback, current ownership inventory, and secret/privacy-safe diff inspection; verify all `CRVD-001` population members, exact unknowns, cleanup, and maximum claim are current before checking implementation tasks complete. **Current Rung:** reviewed integrated candidate. **Next Real Boundary:** owner handoff or separately authorized archive. **Blocker/Unblocker:** tasks 4.1-4.2; any mutation invalidates only affected evidence and requires replay before completion. **Observable Proof:** all applicable project-native commands exit zero, current candidate identities correlate across direct/parent/configured evidence, no unchecked accepted task or reachable non-deferrable defect remains, and no install activation, restart, commit, push, archive, release, deployment, publication, consumer-project mutation, or remote effect occurred. **Validation:** exact command statuses/stdout/stderr, OpenSpec status/readback, diff/status inspection, and terminal cleanup.
