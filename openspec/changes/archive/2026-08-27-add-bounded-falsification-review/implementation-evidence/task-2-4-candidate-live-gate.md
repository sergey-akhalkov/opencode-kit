# Coherent-Wrong-Outcome Candidate Evidence Capture Gate

- Governed lane: `bounded-falsification-review-v1/coherent-wrong-outcome` configured candidate.
- Live-Attempt Gate: blocked after the finalized selected candidate r1 capture. This gate does not clear another member, a full-pack invocation, or the product claim.
- Candidate/source identity: current loaded instruction digest `d6d7dcfba687a51f38b5df249de4a32e1be1ba11379cd6f7c9629d17ec8cb80f` after tasks 2.1-2.3.
- Selected pack identity: `bounded-falsification-r1` / `coherent-wrong-outcome` at scenario digest `9603d433cd65e24fd69292d1c7ebb5914eb46158b58f76c8e871bed751c21afb` with a configured provider-request bound of two.
- Failure chain: the complete twelve-member pre-product baseline is frozen across eleven preserved r1 members and the selected `material-inline-plan` r2 member. The finalized full-pack r1 invocation is non-reusable. Candidate r1 made one primary configured request and one readiness-child launch; the reviewer correctly identified the represented CSV-versus-JSON defect, but the root wrote a non-terminal record outside the fixture's hidden exact schema and had no mutable candidate artifact on which to apply the required correction. The evaluator also compared the full shared `cases.json` digest even though only the unrelated `material-inline-plan` case changed after baseline capture.
- Preserved raw bundles: `baseline-sessions/bounded-falsification-baseline-r1/bundle.json`, `baseline-sessions/bounded-falsification-baseline-material-inline-r2/bundle.json`, and `candidate-sessions/bounded-falsification-candidate-coherent-wrong-outcome-r1/bundle.json`.
- Offline replay coverage: selected zero-call preflight passed with `modelCalls: 0`, the current source digest above, the selected scenario digest above, and the exact permission/model/profile/environment/request controls. Provider-free projection of the preserved r1 bundle passed `baseline-established` with no live calls.
- Terminal replay result: the selected baseline observation remains `bounded-contract-unavailable` with semantic readiness `unknown`; replay digest `5fdc4bb8c3b99bfe428c3c582208bdafe65848c85c3e2d52ec1a1150ab1045e7`. Candidate r1 provider-free replay reproduced `blocked` with terminal digest `a03590f070accece27c3e583b4994bce41d623930f58645113fef4230124c8fe` and reasons `environment:initialFixtureDigest` plus `malformed-observation`; cleanup remained complete and replay made zero live calls.
- Causal reachability: tasks 2.1-2.3 changed the governed loaded routing and record contract while the selected runner/evaluator can now represent the exact candidate observation without changing the frozen baseline identity.
- Unlock condition: not satisfied. Before another configured call, the existing harness must make the selected member's compared initial state independent of unrelated case rows and expose a real correction surface plus discoverable record contract through the loaded propose path. Focused provider-free tests and complete replay of every preserved bundle must then reach the new terminal evaluator/finalization chain, and a successor gate must name the exact remaining raw observation.
- Finalized r1 bound: one primary configured request, one completed `implementation-readiness-reviewer` child, one local fixture write, and complete fixture/process/session cleanup. Do not repeat this invocation, run another member, or increase only timeout/output bounds.
- Candidate r1 evidence root: `candidate-sessions/bounded-falsification-candidate-coherent-wrong-outcome-r1/`; it is immutable input to offline diagnosis and replay.
- External effects: no consumer repository, credentials, install, activation, commit, remote, destructive, or protected action is authorized or expected.
