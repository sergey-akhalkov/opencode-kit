# Task 2.1 Deterministic Contracts

- Outcome: the existing OpenSpec operation gate and consumer-outcome pack now own one bounded-falsification declaration and compact `falsification-review.md` structural contract without a new runner, dependency, reviewer, or semantic scorer.
- Reuse disposition: `extend`. `global/bin/openspec-operation-gate.ts` remains the operation owner; `global/bin/openspec-change/bounded-falsification.ts` isolates the cohesive declaration/record parser under the existing operation-gate seam; the consumer-outcome pack retains an independent reviewed attack-class oracle table.
- Cross-project disposition: `not-applicable`; this is repository-local kit behavior with no consumer-project mutation.
- Explicit contract: required/exempt declaration; original and reviewed request refs; accepted-outcome, candidate, inspected-candidate, reviewer/session/model identities; six ordered attack-class rows; challenge count zero through two; material finding/main disposition correlation; correction and invalidated-surface refs; terminal reason/state; unresolved evidence.
- Semantic boundary: unsupported fields fail with the exact message that semantic materiality and task fit remain `unknown`; a structurally valid record also reports semantic readiness `unknown`.
- Privacy boundary: request values must be privacy-safe references. Raw request/reviewer prose, absolute paths, transcripts, and hidden reasoning are not accepted record fields.
- Operation behavior: propose rejects missing, duplicate, and malformed declarations; a required missing record warns without making structural artifacts failed; archive fails absent required evidence; an exempt declaration rejects a synthetic record.
- Runtime Proof: `npm run test:focused:openspec-gate` exited zero with `OK: OpenSpec operation gate tests=21`; valid records round-tripped and stale candidate, challenge count 3, missing terminal state, raw private request, unsupported semantic inference, malformed/duplicate declarations, required-missing, and exempt-with-record cases produced their exact expected results. Fixture roots are removed in `finally`.
- Effect-free proof: `node global/bin/openspec-operation-gate.ts --help` exited zero without project or provider state. `node tools/proofs/consumer-outcome-regression.ts --mode preflight --pack bounded-falsification --source-ref working-tree` exited zero with `modelCalls: 0`, twelve exact members, source digest `af9e8a56a6cca47fe071f486e79ff3c50b11b96bb917fc56f17db0c304730c6a`, and scenario digest `54eb3332099b94d636b9629c0ccbeb4d4a81e43a6b3f6bc27319f094d64de109`.
- Replay: selected provider-free replay of `baseline-sessions/bounded-falsification-baseline-material-inline-r2/bundle.json` exited zero with `liveCalls: 0`, status `baseline-established`, and digest `2e8813256865caa1c78efd6baa66349a4fc69c9bf2eb3a42c17c677fb4a7ba85`.
- Focused validation: `npm run test:focused:consumer-outcome` exited zero with 32 tests; `openspec validate add-bounded-falsification-review --strict --no-interactive` reported valid; the current apply gate exited zero with the expected development warnings for the not-yet-authored required record and unpopulated candidate claim; scoped `git diff --check` found no whitespace error (Git emitted only existing line-ending conversion warnings).
- Code quality: inventory places the 471-line operation gate in `attention`, not `split-candidate`; parsing/validation is extracted into one cohesive module. Fresh `code-quality-reviewer` session `ses_fbf1ae454ffeyNDhq3JJbBvAJ6` (`xai/grok-4.6`) proposed three reductions. Main applied the unreachable negative-count branch and unused gate re-export removals; it retained the independent proof-harness class table so candidate-side source cannot define its own oracle vocabulary. Focused tests remained green after reduction.
- Diagnostics: the new parser has no Serena diagnostics. Existing repository TypeScript language-service gaps for Node built-in types remain unrelated; executable Node validation is green.
- External Operations: none. No install, activation, archive, commit, push, remote, credential, consumer-project, destructive, or protected action occurred.
- Known Non-Critical Limitations: semantic readiness correctly remains `unknown` until tasks 2.2-2.4 implement and exercise the loaded candidate route.
