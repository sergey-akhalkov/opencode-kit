## 1. Current-Host Preflight And Controller Contract

- [x] 1.1 Re-verify and freeze the current machine baseline before implementation: effective elevated token; Windows build/user SID; Alacritty, stable `pwsh.exe`, and OpenCode paths/versions; user and process `OPENCODE_CONFIG_DIR`; Desktop known-folder path; exact four Git worktrees; Alacritty config bytes/absence; task/shortcut/protected-root collisions; and every listener/process on `127.0.0.1:4096`. Update the smallest affected proposal/design/task control if current evidence changed without changing accepted semantics. **Current Rung:** proposal-time read-only inventory. **Next Real Boundary:** fresh read-only host identity and collision inventory through the same Windows/OpenCode entry points implementation will use. **Blocker/Unblocker:** none; unexplained existing config, task, shortcut, protected-root, or port ownership blocks only dependent mutation until positively classified. **Observable Proof:** one privacy-safe baseline identifies every required executable, path, version, SID, repository, config, process, task, shortcut, and port state without changing them. **Authorization/Safeguards:** read-only local inspection; no process stop, task/ACL/registry/config/Desktop write, credential, provider/model call, remote action, or target-repository mutation. **Restoration/Cleanup:** none required; terminate only diagnostic commands started by this task. **Immutable Evidence:** exact commands/exits/stdout/stderr, normalized identities and hashes where applicable, process/listener snapshot, and zero-mutation readback. **Validation:** compare the evidence to the proposal Operating Envelope and `git diff -- openspec/changes/configure-local-opencode-workstation`.

- [x] 1.2 Add the smallest cohesive repository TypeScript workstation controller with effect-free `--help`, read-only `preflight` and `status`, strict mode/repository allowlist parsing, environment and elevation checks, manifest/identity schemas, cause-preserving errors, and redacted structured output. Use Node standard APIs and bounded PowerShell commands only as Windows adapters; add no package dependency or maintained PowerShell source. `--help` SHALL list install, preflight, status, start, restart, launch, and rollback syntax before any effect. **Current Rung:** verified current-host baseline from 1.1, no reusable repository task/shortcut owner, and a preserved validator rejection of the non-TypeScript source attempt. **Next Real Boundary:** actual `node tools/windows/opencode-workstation.ts --help` and `preflight` against the current host with zero mutation. **Blocker/Unblocker:** 1.1; stop this dependency chain if installed Node cannot execute the source or a collision prevents a complete read-only plan. **Observable Proof:** help exits `0` and changes no file/task/process/endpoint; preflight emits the exact intended protected root, task, endpoint, config, six shortcut names, executable identities, global source, and four repository mappings with no secret. **Authorization/Safeguards:** local repository source edit and read-only host queries only; no installation or elevation request from help/preflight. **Restoration/Cleanup:** remove any proof-owned temporary parse/manifest fixture and prove no host artifact was created. **Immutable Evidence:** source hash, parsed CLI inventory, redacted preflight JSON, before/after host manifest, exits/stdout/stderr, and cleanup. **Validation:** Node source check and actual help execution, read-only `preflight`, unknown-mode/unknown-repository/surplus-argument rejection, and `npm run validate:strict` diagnostics limited to the new owner and unrelated current work left intact.

## 2. Shared Server Happy Path

- [x] 2.1 Implement the first server slice in the controller: create-before-switch installation into an administrator-only `%ProgramData%` root; exact source-copy hash; protected manifest and RNG-generated credential; no-trigger highest-privilege one-instance Scheduled Task; configured `OPENCODE_CONFIG_DIR`; explicit `127.0.0.1:4096`; in-memory `OPENCODE_SERVER_PASSWORD`; supervisor/OpenCode process-state capture; bounded secret-free lifecycle diagnostics; idempotent authenticated Start; read-only Status; drift-aware rollback metadata; and the Desktop Start entry point. **Current Rung:** green effect-free controller preflight from 1.2. **Next Real Boundary:** disposable install-plan/ACL/task-definition evaluation, then actual current-machine protected install without starting the server. **Blocker/Unblocker:** 1.2 and a freshly verified elevated token; if elevation is absent and Windows requires manual UAC, preserve the complete plan and stop at that owner interaction, otherwise proceed under standing local authorization. **Observable Proof:** installed task has no triggers and highest run level, references only the protected controller, the protected root denies unelevated modification, the manifest contains no credential, command lines contain no credential, the Start shortcut targets only the fixed protected mode, and Status reports stopped/installed. **Authorization/Safeguards:** explicit current-machine setup request plus standing local admin authorization; one serialized writer; exact managed namespace; no server start, provider/model call, firewall/remote change, software install, or target-repository write. **Restoration/Cleanup:** on any partial failure, remove only current-invocation artifacts and restore captured bytes/ACLs; retain exact pre-change evidence. **Immutable Evidence:** candidate/source/installed hashes, task XML/properties, ACLs, secret-presence checks without secret value, shortcut metadata, install exits/stdout/stderr, and post-install manifest. **Validation:** controller AST/help/preflight, install readback, Status, unauthenticated secret-leak scan over repository/Desktop/argv/diagnostics, and rollback dry-run identity evaluation.

- [x] 2.2 Invoke the actual Desktop Start entry point and run-observe-correct the shared-server slice until one elevated authenticated OpenCode server is healthy, an unauthenticated probe is rejected, a second Start preserves the same process/listener identity, and no second server exists. **Current Rung:** protected stopped installation from 2.1. **Next Real Boundary:** actual highest-privilege Scheduled Task, installed OpenCode server, loopback authentication middleware, and Desktop Start workflow. **Blocker/Unblocker:** 2.1; any unknown listener/process identity blocks destructive action, while an early server failure follows its native/controller logs and is corrected locally. **Observable Proof:** Start launches one elevated managed process tree on `127.0.0.1:4096`; authenticated `/global/health` reports the installed version; unauthenticated access fails; `OPENCODE_CONFIG_DIR` resolves to the accepted source; repeated Start leaves the same supervisor/server/listener PIDs and task instance. **Authorization/Safeguards:** current-machine local task/process/loopback effects only, no model/provider request, no `--auto`, no remote binding, one server, finite readiness timeout, and no existing TUI/process termination. **Restoration/Cleanup:** if proof cannot finish, stop only the positively identified managed tree or leave the exact safe stopped/running state documented; never kill an ambiguous process. **Immutable Evidence:** actual shortcut invocation, candidate/environment identities, task/token/process-tree/listener facts, authenticated and unauthenticated response status without credentials, exact exits/stdout/stderr, bounded log references, and cleanup/state. **Validation:** installed Status plus independent process/task/endpoint inventory; this partial server slice remains `Development-Stage: development` until one project client works.

## 3. Stable Terminal And First Attached Client

- [x] 3.1 Extend the controller with the ordinary Alacritty stable-shell configuration, exact pre-change backup/hash handling, a protected minimal elevated Alacritty config, fixed self-elevation, generic allowlisted `launch` behavior, authenticated server preflight, explicit `pwsh.exe`, explicit OpenCode `attach --dir`, no implicit-server fallback, and the `opencode-kit` Desktop launcher. **Current Rung:** current green shared-server behavior from 2.2. **Next Real Boundary:** repaired protected installation followed by an ordinary Alacritty launch and the actual `opencode-kit` Desktop shortcut. **Blocker/Unblocker:** 2.2; a newly existing or incompatible user Alacritty config blocks only terminal mutation until its bytes and a non-destructive merge/restore route are proven. **Observable Proof:** ordinary config selects stable `pwsh.exe` without unrelated drift; elevated launcher uses the protected config and only protected repository ID mapping; absent/wrong server, unknown ID/path, missing worktree, failed auth, or failed elevation exits before OpenCode launch with actionable secret-free diagnostics. **Authorization/Safeguards:** scoped reversible config/shortcut/protected-copy update; no target-repository content edit, provider/model call, arbitrary directory, password argv, or global `RUNASADMIN` setting. **Restoration/Cleanup:** preserve original Alacritty bytes/ACLs, remove proof-owned temporary windows/processes, and roll back only the failed update if install repair fails. **Immutable Evidence:** source/installed/config/shortcut hashes, before/after config bytes, allowlist and argv projections without credential, exits/stdout/stderr, and process cleanup. **Validation:** AST/help/preflight, config parse through actual Alacritty, negative mode/ID/path/server/auth cases, protected-copy and shortcut readback, and current shared-server Status.

- [x] 3.2 Invoke ordinary Alacritty and the actual `opencode-kit` Desktop launcher, then run-observe-correct until the ordinary child shell is stable PowerShell Core and the elevated OpenCode client attaches to the existing server with the mapped `opencode-kit` directory and no local server child. **Current Rung:** installed terminal/client slice from 3.1 and current shared server from 2.2. **Next Real Boundary:** actual Alacritty process tree, stable shell alias, elevated attached TUI, and server-side directory identity. **Blocker/Unblocker:** 3.1 and healthy authenticated Status; an unavailable graphical desktop or unavoidable UAC interaction blocks only this lane and must be reported with the prepared exact invocation, while safe process/API observations continue. **Observable Proof:** ordinary Alacritty's initial child is non-Preview PowerShell Core from `pwsh.exe`; the project shortcut creates elevated Alacritty/pwsh/OpenCode client processes; server observation reports the exact `opencode-kit` worktree; the client command is `attach` with explicit URL and `--dir`; no new listening OpenCode server appears. **Authorization/Safeguards:** local interactive GUI and authenticated loopback only; no prompt/model/provider call, repository mutation, command approval, or remote effect. **Restoration/Cleanup:** close only proof-launched terminal/client processes, leave the shared server healthy, and preserve user windows. **Immutable Evidence:** shortcut and process-tree identities, token elevation, shell edition/version/path, attach argv without credential, server directory/health response, listener count, exits/stdout/stderr where observable, and cleanup. **Validation:** controller Status and independent process/listener inventory after cleanup. This is the first real happy-path proof and promotes the candidate to `Development-Stage: MVP` if green.

## 4. Complete Launchers, Restart, And Rollback

- [x] 4.1 Complete accepted production scope: add the `pmac-emulator`, `controller-gateway-service`, and `windows-ui-automation` allowlist mappings and Desktop launchers; implement ownership-safe Restart and its Desktop entry point; persist and validate supervisor/server/listener creation identities; enforce post-stop descendant/listener absence; complete bounded diagnostics; make rollback remove or restore only matching managed artifacts while preserving drift; and add strict repository-owned machine configuration plus operator instructions that reproduce the install on another compatible workstation. **Current Rung:** current MVP server plus one attached client from 3.2. **Next Real Boundary:** effect-free configured preflight, actual remaining shortcuts, managed Restart, collision control, and complete rollback/reinstall cycle from repository source. **Blocker/Unblocker:** 3.2; invalid machine configuration, process identity ambiguity, or an unexplained artifact collision blocks only its dependent mutation until current ownership is proven, never authorizes broader process or file cleanup. **Observable Proof:** schema-valid configuration resolves exactly four Git roots; invalid/unknown/missing configuration fields fail before mutation; protected manifest records its identity and exactly four IDs; Desktop has exactly six managed entry points; Restart requires matching task/process/listener state; rollback has an exact pre-change action for every managed artifact and refuses drift; repository instructions contain every non-secret command needed on another compatible host; diagnostics retain original causes without secrets. **Authorization/Safeguards:** serialized local reversible edits; fixed task/root/shortcut/config namespace; no service, auto-start, Stop shortcut, arbitrary repo, external effect, provider/model call, or unrelated cleanup. **Restoration/Cleanup:** repair or remove only current-candidate artifacts on implementation failure and leave the last proven shared server state explicit. **Immutable Evidence:** reviewed diff, source/config/installed hashes, manifest/shortcut/task identities, process ownership rules, accepted/rejected config matrix, rollback decision table, diagnostics samples, exits/stdout/stderr, and state. **Validation:** AST/help/configured preflight/status, missing/unknown/schema/path/allowlist/stale-state negatives, secret scans, docs/config/task/shortcut readback, focused current-server health, and strict repository validation.

- [x] 4.2 Exercise every remaining project shortcut through the actual operator path and prove each elevated client attaches to the same server with its exact mapped directory, stable PowerShell, and no additional listener/server. Then invoke the actual Restart shortcut and prove old-tree absence, new elevated identity, restored authentication/health, and successful post-restart reattachment. **Current Rung:** complete installed production scope from 4.1. **Next Real Boundary:** four real Alacritty/attach workflows and one real managed server replacement on the current workstation. **Blocker/Unblocker:** 4.1; a graphical/UAC interaction follows the same bounded lane rule as 3.2, while any cleanup or identity failure blocks replacement until resolved. **Observable Proof:** all four mapped server directory identities are correct; every client uses one endpoint; no password appears; Restart changes the managed identity only after every old recorded PID/listener is absent; a fresh launcher works afterward; existing attached clients are not claimed to auto-reconnect. **Authorization/Safeguards:** local loopback/GUI/task/process effects only, no model/provider call, no target-repository mutation, finite waits, and one client proof process at a time. **Restoration/Cleanup:** close only proof-launched clients, retain one healthy managed server, and prove no orphaned proof or old-server process remains. **Immutable Evidence:** per-launch shortcut/process/token/shell/argv/directory/listener facts, pre/post-Restart tree and task identities, endpoint responses, exits/stdout/stderr, bounded logs, and cleanup. **Validation:** installed Status, independent process/listener/task inventory, and authenticated health after final cleanup.

- [x] 4.3 Challenge fail-closed and restoration behavior on the current candidate: with the managed server safely stopped, place a proof-owned unrelated listener on `4096` and prove Start/Restart preserve it; remove only that listener; perform a complete identity-matched rollback; prove the exact pre-change workstation state; reinstall the unchanged candidate directly from the documented repository source/configuration; and regain the current 2.2, 3.2, and 4.2 proof lanes. **Current Rung:** complete green accepted behavior from 4.2. **Next Real Boundary:** actual collision guard, destructive rollback boundary, repository-driven same-candidate reinstall, and reproof. **Blocker/Unblocker:** 4.2 and a clear managed-process cleanup state; if any artifact has real user drift, preserve it and stop only the dependent rollback action rather than forcing deletion. **Observable Proof:** unrelated listener survives controller actions; rollback removes/restores only recorded matching artifacts; unrelated Desktop/config/process/repository state is unchanged; documented repository preflight/install recreates the same protected source/config identities and all happy paths return green without an original-host-only artifact. **Authorization/Safeguards:** proof-owned loopback listener with known PID; exact before/after manifests; no arbitrary process kill, remote effect, software uninstall, target-repository write, or provider/model call. **Restoration/Cleanup:** terminate the proof listener by its captured identity, restore the final installed candidate and one healthy managed server, close proof clients, and leave no disposable task/root/shortcut/config/process. **Immutable Evidence:** collision PID/creation/path and survival facts, rollback decisions and pre-state comparison, documented reinstall commands/hashes, affected reproof bundles, exits/stdout/stderr, and final cleanup/state. **Validation:** full controller help/configured preflight/status, independent task/ACL/config/shortcut/process/listener inventory, and provider-free replay/evaluation of every preserved bundle. Accepted production scope is complete only when this task is current and green.

- [x] 4.4 Prove concurrent shared-server clients through actual Desktop entry points: keep a real `pmac-emulator` attached client alive, launch `opencode-kit` through its `.lnk`, and verify both exact client trees remain active against one unchanged managed listener; then close only the newly attributed `opencode-kit` proof tree and prove the existing `pmac-emulator` client/server remain unchanged. Capture any launcher error in the protected controller error log. **Current Rung:** individual four-launcher and Restart proof from 4.2/4.3 plus an owner-observed earlier second-launch failure. **Next Real Boundary:** two simultaneous real project clients on one actual server. **Blocker/Unblocker:** a missing first client requires launching one proof-owned `pmac-emulator` client; an owner-existing client must be preserved. **Observable Proof:** both process trees expose exact distinct `--dir` arguments, one listener PID remains unchanged, no second serve process appears, and closing the second tree leaves the first alive. **Authorization/Safeguards:** no provider/model call, no target-repository mutation, no server Restart, and only the newly created proof client may be terminated. **Restoration/Cleanup:** preserve owner windows; remove only proof-owned clients; retain one healthy server. **Immutable Evidence:** pre/post process trees, shortcut identities, exact argv/directories, listener/server PIDs, controller result/error log, and cleanup. **Validation:** actual `.lnk`, installed Status, listener/server inventory, and zero new orphan proof clients. Accepted production scope is complete only when this task is current and green.

## 5. Material Critical Risk Disposition

- [x] 5.1 Freeze the readable Product Candidate Reference and complete raw host/runtime bundles, then dispatch one fresh test-only Material SDET to challenge only reachable critical incidents: unauthenticated or wrong-credential control of the elevated server; unelevated mutation of privileged runtime/task/credential material; credential disclosure; stale identity killing an unrelated process; orphaned elevated descendants; arbitrary repository launch; wrong-directory attachment; rollback loss of unrelated config/data; and an implicit unprotected server fallback. **Current Rung:** current MVP with complete accepted scope and rollback reproof from 4.3. **Next Real Boundary:** fresh-context critical tests against the actual installed controller/task/shortcuts/endpoint and preserved bundles, preferring real access/process boundaries over mocks. **Blocker/Unblocker:** 4.3; unavailable or unusable SDET is recorded as `blocked` and leaves the candidate at MVP rather than being replaced by production-authored automated tests. **Observable Proof:** SDET returns exactly `critical-risks-reported`, `no-critical-risk`, or `blocked`, includes Effective Model and exact Candidate Reference, and authors at most the smallest test-only critical reproducer/oracle. **Authorization/Safeguards:** test-only write scope, no production/config/controller mutation, no nested agent, no provider/model product prompt, no remote/destructive unrelated action, and no lifecycle verdict. **Restoration/Cleanup:** reuse candidate cleanup and restore one healthy managed server with no test process/task/file outside the declared test scope. **Immutable Evidence:** SDET report/matrix, exact candidate attribution, test-only changed paths, commands/results, access/process/endpoint observations, model provenance, and cleanup. **Validation:** main inspects every row and independently reproduces every plausible critical/non-deferrable claim.

- [x] 5.2 Main-disposition the SDET evidence: reproduce, disprove, or show unreachable every plausible critical/non-deferrable claim; correct only confirmed current-outcome defects; then regain every affected install/server/client/restart/rollback proof lane and use a new fresh SDET only when the canonical materially changed-candidate continuation predicate is met. Park non-critical findings without extending scope. **Current Rung:** fresh SDET evidence from 5.1. **Next Real Boundary:** exact reported real-host reproducer and the affected actual operator path after any correction. **Blocker/Unblocker:** 5.1; reviewer/SDET severity never authorizes mutation without main reproduction, and unknown protected-boundary claims cannot be parked. **Observable Proof:** every SDET row has a main evidence disposition; corrected candidates have current runtime proof and no known confirmed reachable critical/non-deferrable defect; non-critical limitations are recorded only. **Authorization/Safeguards:** same current-machine local envelope and one production writer; no optional hardening, feature expansion, remote action, or unrelated cleanup. **Restoration/Cleanup:** close each reproducer, preserve pre/post-correction bundles, and restore final managed server/client state. **Immutable Evidence:** main reproduction/disposition, causal diff, scoped invalidation/reproof, SDET terminal history, parked limitations, exits/stdout/stderr, and cleanup. **Validation:** every affected command and runtime lane from sections 1-4 plus focused checks for any corrected controller behavior.

## 6. Complete Validation And Local Handoff

- [x] 6.1 Run complete validation on one current candidate: Node source checking and actual controller execution; controller `--help`, preflight, status, negative parser/allowlist/identity/secret controls; provider-free replay of preserved runtime bundles; `npm test`; `npm run validate:strict`; `npm run openspec:validate`; and `openspec.cmd validate configure-local-opencode-workstation --strict`. Inspect final repository diff and actual installed source/config/task/ACL/shortcut/process/endpoint identities, then record rollback instructions, SDET disposition, known non-critical limitations, and external-operation state. Freeze the next monotonic RC only when all accepted scope and validation are green and no known reachable critical/non-deferrable defect remains; promote that same RC to stable only with the complete local handoff. **Current Rung:** terminal main SDET disposition and current complete runtime proof from 5.2. **Next Real Boundary:** complete project-native validation plus final current-host operator readback; no deployment or release action follows. **Blocker/Unblocker:** 5.2; red validation or stale runtime proof keeps the candidate at MVP/development as applicable and returns only the affected lane to correction. **Observable Proof:** every named command exits `0`; strict OpenSpec validation passes; installed and repository controller hashes correlate; one authenticated elevated server is healthy; all six shortcuts and four mappings match; no credential or orphan exists; rollback remains identity-safe. **Authorization/Safeguards:** local validation/readback only; no commit, push, install of third-party software, release, deployment, remote binding, or target-repository mutation. **Restoration/Cleanup:** stop validation/proof processes, close proof clients, remove disposable state, retain the requested final workstation installation and one explicitly reported server running/stopped state, and preserve unrelated work. **Immutable Evidence:** exact commands/exits/stdout/stderr, validation report, final diff and Candidate Reference, runtime/environment identities, proof/SDET refs, cleanup, RC history, and handoff. **Validation:** the commands and actual-host checks named in this task; no optional retrospective or process-improvement work is added to the product task graph.
