# Task 4.3 Evidence-Sufficiency Challenge

- Inspected candidate: `continuous-complexity-management-5634611b`; no RC.
- Environment: `openai-gpt-5.6-sol-xhigh-opencode-1.18.23-windows-node-24.18.1`.
- Reviewer role: fresh read-only `evidence-sufficiency-reviewer` (`claim-evidence`).
- Effective model: `xai/grok-4.6`.
- Runtime task identity: `ses_fbc978564ffeQ3qA85lPBYJk2B`.
- Review effects: read-only; no source, test, config, evidence, remote, or lifecycle mutation.

## Checked Dimensions

- The proposal, fixture pack, and index name the same twelve unique reviewed partitions.
- The r4 baseline and candidate use the same OpenAI model, `xhigh` variant, Windows,
  OpenCode `1.18.23`, Node `24.18.1`, permission digest, scenario digest, and proof
  contract. Every baseline and candidate partition oracle is terminal and passed.
- Candidate behavior remains unchanged for all twelve representative fixture scenarios;
  useful-facade, wrapper-removal, and speculative-abstraction members reduce their
  reviewed context or public surface under their exact `*-only` ceilings.
- Ten candidate members retain one to three denied shell probes in exact error fact-diffs.
  Those calls are diagnostics, not oracle failures and not evidence of a clean session.
- Provider-free r4 replay reports `passed-no-regression` and `liveCalls=0`.
- PMAC remains a separately identified read-only diagnostic and is not bound to any
  generic observation row.

## Risk Matrix And Main Disposition

| Risk ID | Requirement and reachable scenario | Evidence | Consequence | Likelihood / confidence | Smallest mitigation | Main disposition |
| --- | --- | --- | --- | --- | --- | --- |
| `CCM-CE-001` | Evidence bounds claims. The original conjunctive statement could be read as requiring every partition to run facts, map/rehearsal, abstraction comparison, and lower-context refactor proof. | `evidence-index.json` statement versus r4 member-specific `ownerFacts`, `pathFacts`, and `maximumClaim` values in `task-3-3-configured-candidate-r4/evaluation.json`. | Cohesive stay-quiet, review-only, unreadable, and unsupported controls could be overrepresented as refactor evidence. | high / high | State the aggregate exact outcomes and apply lower-context claims only to represented refactor members. | Confirmed claim-record wording defect. The statement and maximum claim are narrowed to the exact singleton outcomes; no product or runtime recapture is required. |
| `CCM-CE-002` | Current population support must not compose historical or separately identified evidence. | Claim-level refs included `implementation-records` and `profile-readback`; per-member rows correctly cite only the r4 baseline/candidate. PMAC has its own candidate and lane. | A reader could mistake earlier facade/profile/PMAC evidence for r4 generic-member support. | medium / high | Bind the broad claim only to r4 population lanes and this challenge. | Confirmed claim-binding defect. Historical/profile/PMAC lanes remain available for their narrower tasks but are removed from broad claim refs. |
| `CCM-CE-003` | Failed tool calls remain visible and must not become a clean-session claim. | Ten error-dimension rows in the candidate r4 evaluation retain `tool:N:bash:error`; all behavior and semantic oracles still pass and duplicate failed calls are zero. | `12/12 supported` could be misread as error-free execution. | medium / high | Add an explicit clean-session exclusion and retain exact error fact-diffs. | Confirmed diagnostic caveat. No member is marked unresolved because the denied probes are fully observed and do not change its reviewed behavior oracle. |

## Maximum Supported Claim

Under the recorded OpenAI `gpt-5.6-sol` `xhigh` configured route, Windows, OpenCode
`1.18.23`, Node `24.18.1`, source, scenario, permission, proof, and cleanup identities,
the twelve reviewed disposable partitions support only their exact reviewed outcomes:
cohesive stay-quiet delta; modular focused retain; noisy scope narrowing; mixed-owner
owner-then-defer; useful-facade admission; frozen/current path classification; redundant
wrapper removal; speculative factory removal; review-only project routing; default-core
focused availability with project mode unavailable; unreadable-root unknown; and
unsupported-ecosystem unknown. Representative behavior is preserved, and lower consumer
context is supported only where the exact fact-diffs show it. Denied shell probes remain
diagnostic facts. PMAC, other providers/models/hosts/repositories/ecosystems, universal
architecture quality, optimal abstraction, and long-term productivity or defect effects
remain outside the claim.

The reviewer supplied evidence only. Main reproduced and dispositioned every row; the
review did not authorize mutation or lifecycle state.
