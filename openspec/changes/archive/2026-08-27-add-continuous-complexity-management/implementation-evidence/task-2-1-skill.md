# Task 2.1 Focused Skill Contract Evidence

## Outcome And Ownership

- Candidate: `continuous-complexity-management-skill-r1`.
- Environment: repository instruction source, Windows, Node `24.18.1`; no install, generated profile, configured model call, or active-session reload.
- Reuse disposition: `build-minimal` for the previously unowned focused map/rehearsal workflow; `extend` for the adjacent discovery descriptions and README catalog. Current-repository search confirmed `code-quality-audit` owns post-change maintainability, `service-architecture-design` owns new-service design, and `codebase-audit-loop` owns exhaustive coverage. Cross-project search was not applicable to a portable repository-owned instruction contract already resolved by the reviewed design.
- Changed source: new `global/skills/complexity-management/SKILL.md`; narrowed frontmatter/body stay-quiet routing in `global/skills/code-quality-audit/SKILL.md` and `global/skills/service-architecture-design/SKILL.md`; one focused route and three catalog descriptions in `README.md`.
- Deliberately unchanged: profile manifests/generation, always-loaded `global/AGENTS.md` routing, Practice Owner roster, exhaustive audit outputs, validators, package scripts, and installed state. Those are dependency-ordered later tasks.

## Contract Readback

- Focused discovery metadata begins `Use ONLY` and names existing-project comprehension/current-pressure/useful-abstraction/refactoring-locality triggers.
- Reverse stay-quiet metadata names cohesive deltas, changed-code review, new service design, seam-only Practice Owner routing, and exhaustive audits.
- The body contains one main-owned focused workflow with Fact Foraging, Architecture Comprehension Map, Change Rehearsal, Abstraction-Value Ladder, Same-Scenario Recheck, admission classes, and a bounded output contract.
- Exact active-source resolution consumes `bin/complexity-foraging-inventory.ts` when present and otherwise degrades to bounded manual foraging; it does not guess a repository parent or target package script.
- The adjacent skills point focused pre-expansion work back to `complexity-management` while retaining their existing output and lifecycle positions.
- README routing and the skill catalog identify the same focused owner without transferring exhaustive coverage or new-service design.

## Focused Proof And Validation

- Mutual-exclusion readback command: exit `0`; stdout `OK: complexity-management-discovery focused=1 adjacent=2 outputs=6 routing=1`; stderr empty.
- Direct maintained skill validator: exit `0`; stdout `OK: skill-contracts skills=33 complexity-management=1`; stderr empty.
- `npm run instruction:inventory -- --format markdown`: exit `0`; artifacts `74`, skill artifacts `34` including the project-local OpenSpec skill, context quality `passed`, deterministic errors `0`, review-only `0`.
- `npm run instruction:canonicalize -- --check .`: exit `0`; changed files `0`, safe fixes `0`, deterministic errors `0`, review-only `0`.
- `openspec validate add-continuous-complexity-management --strict --no-interactive`: valid.
- First `npm run validate:strict` identified missing exact trigger wording and happy-path-first guidance in the new skill. Both were corrected without changing scope; the focused skill validator and context-quality checks then passed.
- Current full `npm run validate:strict` reports only `profiles/all.json must match repository skills. Missing: complexity-management`. This is the explicit task-2.2 profile-availability dependency; task 2.1 intentionally does not mutate profiles or advertise loaded availability.

## Claim Ceiling

The repository source now defines one structurally valid, mutually exclusive focused skill contract and matching catalog route. No selected profile, generated/installed source, always-loaded route, configured OpenCode behavior, semantic map, refactor decision, or population member has been proved.
