## 1. Establish The Unchanged-Source Baseline

- [x] 1.1 Materialize and validate the change's reviewed `ownership.json` and `evidence-index.json` with the existing deterministic inventory owners, declaring `EXT-001`, exact capability/requirement/write ownership, baseline and candidate evidence lanes, mutation disabled, and task digest without inferring reuse quality; run the current apply operation gate and keep mutation disabled until both records parse, ownership is conflict-free, and pre-baseline lanes are explicitly incomplete rather than absent. **Current Rung:** planning artifacts only. **Next Real Boundary:** apply-gate readback of the claim/ownership records. **Blocker/Unblocker:** none. **Observable Proof:** gate exits 0; `EXT-001` is present with incomplete baseline/candidate observations. **Authorization:** local planning files only. **Safeguards:** no instruction mutation, provider call, install, or remote effect. **Restoration/Cleanup:** planning files only. **Validation:** apply operation gate. **Dependency-Chain Stop Condition:** stop if the claim record is missing or the gate infers claim class from prose.
- [x] 1.2 [automation-dividend] Map responsibilities in `tools/proofs/reuse-discovery.ts`, then extend that existing owner with reviewed `extend-existing-owner` fixture, prompt, help text, and evaluator facts for naming the current owner, recording `extend`, and forbidding a sibling module; keep or apply the design's scenario-data extraction fallback. Verify `npm run proof:reuse-discovery -- --help` is effect-free and provider-free preflight over `local-owner,trivial-fix,extend-existing-owner` exits zero without changing loaded global instructions. **Current Rung:** current two-scenario runner. **Next Real Boundary:** provider-free preflight including the new scenario. **Blocker/Unblocker:** task 1.1. **Observable Proof:** help lists the third scenario; preflight creates no product mutation and exits 0. **Authorization:** local proof-runner/fixture edits only. **Safeguards:** no provider call, no global instruction edit, no `tools/proofs/lib` collapse. **Restoration/Cleanup:** disposable preflight roots deleted. **Validation:** help plus preflight. **Dependency-Chain Stop Condition:** stop if adding the scenario requires a second runner or a new proof framework.
- [x] 1.3 Capture the unchanged global source through the installed OpenCode authoring entry point for every `EXT-001` scenario, preserving complete baseline bundles whose commands, tool calls, dispositions, fixture hashes, effective model/profile/permissions, and cleanup are inspectable before any target instruction mutation. **Current Rung:** provider-free preflight. **Next Real Boundary:** installed authoring captures on the current source. **Blocker/Unblocker:** task 1.2 and standing bounded synthetic provider authorization. **Observable Proof:** three baseline bundles exist; trivial-fix loads no reuse skill; local-owner and extend scenarios are recorded even if baseline chooses a sibling. **Authorization:** three configured-provider calls in disposable workspaces. **Safeguards:** same model/profile/prompts reserved for candidate; denied edit/remote/credential tools; session and root deletion. **Restoration/Cleanup:** delete sessions and disposable roots after immutable capture. **Validation:** provider-free evaluate/readback of baseline bundles. **Dependency-Chain Stop Condition:** do not mutate loaded instructions until all three baseline bundles are preserved.
- [x] 1.4 After the baseline happy path is observed, add the smallest focused automated coverage for the new scenario schema, stable ordering, source/environment mismatch rejection, extend-oracle facts, replay, privacy bounds, and cleanup failure; run the focused test directly and confirm injected invalid evidence fails with cause-preserving diagnostics. **Current Rung:** preserved baseline bundles. **Next Real Boundary:** focused proof-runner tests. **Blocker/Unblocker:** task 1.3. **Observable Proof:** focused tests pass; invalid evidence fails closed. **Authorization:** test-only edits after proof. **Safeguards:** no production instruction mutation. **Validation:** focused proof-runner test.

## 2. Implement Compact Default And Role Routing

- [x] 2.1 Replace the overlapping always-loaded reuse paragraph in `global/AGENTS.md` with the compact name-current-owner / default-`extend` / lazy-skill rule; update `reuse-discovery` so `extend` means reshape the current owner; verify a targeted read retains existing mechanism-trigger, trivial-opt-out, disposition, degraded cross-project, and no-registry protections and that `npm run instruction:budget -- --format json` stays at or below 13,279 combined startup token proxy. **Current Rung:** frozen baseline source. **Next Real Boundary:** instruction-budget and marker readback. **Blocker/Unblocker:** tasks 1.3-1.4. **Observable Proof:** budget does not grow; no second reuse owner or appended policy block. **Authorization:** local instruction edits. **Safeguards:** replace, do not append; do not edit `principles-of-work.md` unless an exact existing marker would break. **Validation:** instruction budget plus targeted marker read. **Dependency-Chain Stop Condition:** revert the paragraph if budget grows or a protected reuse/safety marker disappears.
- [x] 2.2 Add the production-role reshape-versus-unrelated-refactor delta to `implementation-worker` and the sibling/uncertainty launch-when delta to `code-quality-reviewer` plus the compact Practice Owner roster; keep architecture in its separate practice; verify inventory shows no new agent, no owner mutation/decision authority, and no owner launch required for punctuation-only work. **Current Rung:** compact always-loaded rule. **Next Real Boundary:** instruction/agent inventory and contract markers. **Blocker/Unblocker:** task 2.1. **Observable Proof:** launch-when text names sibling and same-versus-new uncertainty only; worker text says reshape is in-scope. **Authorization:** local role/roster edits. **Safeguards:** no new Practice Owner; no merge with architecture. **Validation:** instruction/agent inventory plus focused contract markers.
- [x] 2.3 Extend exact engineering-quality or instruction-contract markers and focused fixtures for name-current-owner, default-`extend`, reshape-versus-unrelated-refactor, sibling/uncertainty owner routing, and forbidden new-reuse-owner language; run `npm run test:focused:contracts` and prove each removed-marker fixture fails with its exact surface while no validator infers same-versus-new responsibility or scores reuse quality. **Current Rung:** role deltas present. **Next Real Boundary:** focused contract tests. **Blocker/Unblocker:** task 2.2. **Observable Proof:** focused contracts pass; missing-marker fixtures fail closed. **Authorization:** contract/fixture edits. **Safeguards:** no semantic scoring. **Validation:** `npm run test:focused:contracts`.
- [x] 2.4 Run provider-free reuse-discovery preflight and `npm run instruction:budget -- --format json`, then verify candidate source, fixture, permission, and command identities match the preserved baseline comparison envelope and startup token proxy did not increase. **Current Rung:** candidate instructions and markers. **Next Real Boundary:** provider-free candidate preflight. **Blocker/Unblocker:** task 2.3. **Observable Proof:** preflight exit 0; budget <= baseline and <= 13,279. **Authorization:** local read-only proof/budget. **Safeguards:** no provider call. **Validation:** preflight plus budget.

## 3. Prove Candidate Authoring Behavior

- [x] 3.1 Capture the candidate through the same installed authoring path, prompts, fixtures, model, variant, permissions, and environment as the preserved `EXT-001` baseline; verify `extend-existing-owner` names the current owner and records `extend` without a sibling module, `local-owner` still loads reuse detail, `trivial-fix` loads no reuse skill and launches no Practice Owner, and cleanup is terminal. **Current Rung:** candidate instructions. **Next Real Boundary:** matched installed authoring captures. **Blocker/Unblocker:** task 2.4 and standing bounded synthetic provider authorization. **Observable Proof:** three candidate bundles satisfy the scenario oracles. **Authorization:** three configured-provider calls in disposable workspaces. **Safeguards:** identical comparison envelope; denied mutation/remote/credential tools; session deletion. **Restoration/Cleanup:** delete sessions and disposable roots after immutable capture. **Validation:** provider-free evaluate. **Dependency-Chain Stop Condition:** do not claim `EXT-001` supported if the extend scenario adds a sibling or the trivial scenario loads the skill or an owner.
- [x] 3.2 Replay and evaluate the complete baseline/candidate bundles provider-free, then record main's scenario-by-scenario semantic disposition for `EXT-001` without using file counts or deterministic scoring as acceptance; obtain a fresh read-only `evidence-sufficiency-reviewer` challenge before representing the broad claim as complete; narrow or reject candidate wording for any failed oracle and recapture only evidence invalidated by a corrected candidate. **Current Rung:** candidate captures. **Next Real Boundary:** provider-free evaluate plus claim-evidence challenge. **Blocker/Unblocker:** task 3.1. **Observable Proof:** evaluate exits 0; claim challenge is current; maximum claim remains the three-scenario population. **Authorization:** local replay plus one read-only owner launch. **Safeguards:** missing challenge keeps only `EXT-001` blocked/unknown; it does not authorize mutation. **Validation:** evaluate plus owner report. **Dependency-Chain Stop Condition:** do not raise the claim ceiling or invent a fourth population.
- [x] 3.3 Run the candidate's nearest loaded-source canary in a fresh disposable OpenCode session and verify main names the current owner and defaults to `extend` on the known-owner case, the Practice Owner stays silent on trivial work, no new agent appears, and no runtime loader collision or private instruction content appears in the evidence. **Current Rung:** evaluated candidate. **Next Real Boundary:** fresh loaded-source canary. **Blocker/Unblocker:** task 3.2. **Observable Proof:** canary matches the compact default and zero-trigger silence. **Authorization:** one disposable local session. **Safeguards:** no product repo mutation. **Validation:** canary transcript plus cleanup.

## 4. Validate The Complete Planning Outcome

- [x] 4.1 Run the focused proof-runner test, `npm run test:focused:contracts`, `npm run validate:strict`, and `npm test`; preserve exit status, stdout/stderr, relevant failures, and artifact paths, and correct only candidate-owned defects before rerunning affected checks. **Current Rung:** proved candidate. **Next Real Boundary:** project-native validation. **Blocker/Unblocker:** task 3.3. **Observable Proof:** all named commands exit 0. **Authorization:** local validation. **Safeguards:** no install, activation, commit, push, or remote effect. **Validation:** the named commands.
- [x] 4.2 Run `openspec validate prefer-extend-existing-owner --strict` and the current apply operation gate, verify every delta scenario maps to current proof or focused validation including known-owner extend, triggered mechanism, trivial negative control, and budget non-growth, and record the `EXT-001` maximum claim plus any known non-critical limitation without claiming universal reuse quality. **Current Rung:** green validation. **Next Real Boundary:** strict OpenSpec plus apply gate. **Blocker/Unblocker:** task 4.1. **Observable Proof:** both commands exit 0; claim ceiling is explicit. **Authorization:** local validation. **Validation:** strict OpenSpec and apply gate.
- [x] 4.3 Confirm no critical-risk SDET trigger is reachable, no installation/activation/commit/push/release/remote effect occurred, running sessions require a fresh load before using changed instructions, and hand off `Outcome: working | blocked | unknown` with exact baseline/candidate evidence and validation paths. **Current Rung:** gated candidate. **Next Real Boundary:** local handoff. **Blocker/Unblocker:** task 4.2. **Observable Proof:** handoff names `EXT-001`, evidence paths, budget result, and external-operation state `none`. **Authorization:** local handoff only. **Safeguards:** no RC/stable claim from planning or from this apply increment beyond ordinary Outcome.
