## 1. Baseline And Authority

- [x] 1.1 Capture the current deterministic contract and fresh-session behavior for the fixed-image pre-COM scenario, determining whether current authority needs the process-versus-owner classification supplied explicitly. **Current Rung**: deterministic source/contracts. **Next Real Boundary**: fresh configured OpenCode primary session. **Blocker/Unblocker**: none; use a read-only synthetic scenario with all product actions prohibited. **Observable Proof**: baseline transcript identifies whether `question`/owner approval is selected and preserves the loaded source/model identity. **Authorization**: standing bounded configured-provider inference. **Safeguards**: no product repository mutation, controller, credentials, hardware, remote/destructive, or external operation; read-only scenario. **Restoration/Cleanup**: delete disposable session/root after immutable capture. **Expected Immutable Evidence**: baseline command, candidate/config identity, exit, event/tool/question order, response, cleanup. **Observed**: current authority continued autonomously only after the prompt explicitly classified `no R5` as agent-authored process bookkeeping; the user's incident shows that loaded instructions do not reliably derive this classification from owner-handoff-shaped wording. Session and raw observations are recorded in `history.md`.
- [x] 1.2 Confirm the implementation surface and exact authority distinction from proposal/design/specs: autonomous process-control mutation versus separately authorized protected action; preserve archived changes as history. **Current Rung**: source/spec inspection. **Next Real Boundary**: task 2.2 fresh session. **Blocker/Unblocker**: none. **Observable Proof**: traceability list covers global authority, Material skill, OpenSpec config/propose/apply/archive, project templates, completion arbiter, normative specs, and deterministic markers without historical rewrites. **Authorization**: local reads only. **Safeguards**: no semantics beyond accepted owner direction. **Restoration/Cleanup**: N/A - read-only inventory. **Expected Immutable Evidence**: task record and focused diff scope. **Observed**: the candidate touches only the listed active/runtime/normative surfaces plus exact contract markers; `openspec/changes/archive/**` is unchanged.

## 2. Implement And Prove MVP

- [x] 2.1 Update the smallest coherent loaded and portable instruction surfaces plus exact deterministic contract markers so plans, specs, revisions, attempt limits, and process stop lines adapt autonomously while underlying protected actions retain separate authority and all live-attempt gates. **Current Rung**: source mutation. **Next Real Boundary**: task 2.2 fresh configured OpenCode primary session. **Blocker/Unblocker**: task 1.2 scope confirmed. **Observable Proof**: focused contracts and strict validation require both autonomy and protected-action separation; no active surface asks for process-only approval. **Authorization**: local repository edits only. **Safeguards**: preserve accepted outcome/protected boundaries, no new mechanism, no archived history edits, no external action. **Restoration/Cleanup**: source-only rollback of this candidate. **Expected Immutable Evidence**: focused diff, contract output, instruction inventory. **Observed**: candidate reference `4b688e9c87ea05a2ee6c9e0e42dc7bf353cf9303`; focused contracts `65/65`, strict validation `skills=26 agents=18 markdown=299 warnings=0`, strict change validation and apply gate pass; inventory `55` artifacts and `91,959` token proxy.
- [x] 2.2 Invoke the actual loaded OpenCode entry point with paired synthetic scenarios: corrected pre-COM failure with existing endpoint authority and green replay, and the same plan update without authority for the underlying protected action. **Current Rung**: fresh configured model session. **Next Real Boundary**: none for product hardware; this change proves instruction behavior only. **Blocker/Unblocker**: task 2.1 candidate plus provider/loader preflight. **Observable Proof**: first scenario updates the attempt/spec boundary and continues without an owner question; second autonomously prepares artifacts but stops only at the exact protected action. **Authorization**: standing bounded configured-provider inference. **Safeguards**: disposable empty root, edit/bash/task/external tools denied, no real project or hardware actions, raw event capture, privacy-safe output. **Restoration/Cleanup**: delete proof sessions/root and verify no mutation. **Expected Immutable Evidence**: invocation, config/model/candidate identity, exit, stdout/stderr, event/tool/question order, observed response, cleanup. **Observed**: session `ses_009fbb340ffe8YJaIpXcX1br4I`, candidate `4b688e9c87ea05a2ee6c9e0e42dc7bf353cf9303`, exit `0`; authorized case continued autonomously, unauthorized protected-action case updated artifacts autonomously and stopped only before controller/manual access; no `question` or mutation event; session/root cleanup green. Runtime Proof promotes the candidate to `MVP`; full bundle is recorded in `history.md`.

## 3. Independent Critical Test Evidence

- [x] 3.1 After current MVP proof and accepted-scope completion, dispatch one fresh test-only SDET to assess critical regression risk and author only the smallest missing automated oracle in existing test paths. **Current Rung**: deterministic regression boundary. **Next Real Boundary**: existing focused contract/test runner. **Blocker/Unblocker**: tasks 2.1-2.2 green and candidate frozen. **Observable Proof**: SDET returns `critical-risks-reported | no-critical-risk | blocked`; any authored test distinguishes process autonomy from protected-action authority and fails on the baseline wording. **Authorization**: local test-only writes in explicitly supplied existing paths. **Safeguards**: no production/OpenSpec edits, no nested agents, no external operations. **Restoration/Cleanup**: close writer attempt and preserve exact diff/report. **Expected Immutable Evidence**: fresh task identity, Effective Model, risk matrix, changed test paths, focused test output. **Observed**: fresh SDET task `ses_009f96df8ffeAvpvbglxdfFTk6`, Effective Model `xai/grok-4.5`, returned `no-critical-risk`; changed only `tools/test-contracts-change-ready.ts` with one 129-line critical oracle covering canonical marker pairs and six negative mutations. Main inspection accepted the test-only diff and independently replayed `npm run test:focused:contracts` at `66/66`.

## 4. Validation And Handoff

- [x] 4.1 Run applicable focused contracts, strict library validation, full tests, strict OpenSpec validation, the repository-native `npm run prepush:validate`, instruction inventory, and replay the paired fresh-session proof if production/runner/environment changes affect its verdict. **Current Rung**: complete local validation plus preserved fresh-session proof. **Next Real Boundary**: none inside this instruction-only envelope. **Blocker/Unblocker**: terminal task 3.1 and closed writers. **Observable Proof**: all applicable commands exit zero, the active change validates strictly, loader behavior remains green, no known reachable critical/non-deferrable defect remains, and the final diff contains no unrelated or historical mutation. **Authorization**: local validation and standing bounded inference only. **Safeguards**: preserve unrelated work; no commit, push, archive, install, activation, hardware, remote, or destructive action. **Restoration/Cleanup**: proof session/root cleanup confirmed; source remains reviewable. **Expected Immutable Evidence**: commands, exits, stdout/stderr summaries, candidate hash/diff, runtime bundle, limitations. **Observed**: focused contracts `66/66`; strict validation `skills=26 agents=18 markdown=299 warnings=0`; full 11-suite `npm test` exit `0`; OpenSpec `12/12`; `prepush:validate` exit `0`; inventory `55` artifacts/`91,959` token proxy; `git diff --check` exit `0`. The unsupported `--operation prepush` attempt is recorded in `history.md` and was replaced by the repository-native entry point without production or proof mutation.
- [x] 4.2 Produce the local stable handoff with outcome, changed artifacts, Runtime Proof, Candidate Reference, SDET terminal reason, validation, known non-critical limitations, restart requirement, and external-operation state. **Current Rung**: local handoff. **Next Real Boundary**: future sessions loading the updated config-time artifacts after restart. **Blocker/Unblocker**: task 4.1 green. **Observable Proof**: one complete handoff reports `Development-Stage: stable` and `Stable Candidate: RC1` only if every prior gate is current. **Authorization**: local reporting only. **Safeguards**: no archive/commit/push/release/install. **Restoration/Cleanup**: N/A - handoff only. **Expected Immutable Evidence**: final response and active change task state. **Observed**: `implementation-evidence/stable-handoff.md` records complete local evidence and `External Operations: not performed`; current stage is stable, candidate RC1.
