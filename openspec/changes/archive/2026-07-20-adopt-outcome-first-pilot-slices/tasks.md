## 1. Freeze One Working Policy Slice

The frozen acceptance criteria for this change are:

- `AC-1`: canonical loaded authority requires remove -> narrow -> reuse -> local guard before new mechanisms or abstractions and classifies risk against technically reachable behavior;
- `AC-2`: `Pilot-Ready: yes | no | not requested` is separate from `Change-Ready`, keeps exactly `Ordinary Small | Material` profiles, satisfies the pilot safety floor, and never authorizes an external operation;
- `AC-3`: only material reachable pilot risks require one explicit user acceptance bundle, while uncontrolled authorization/privacy/data-integrity/irreversible/envelope-escape risks cannot be waived;
- `AC-4`: reviewers keep valid findings but block Pilot-Ready only for reachable current-outcome, safety-floor, invariant, accepted-risk-limit, or trusted-validation impact, and recommend the smallest sufficient remedy;
- `AC-5`: OpenSpec stops at next-increment sufficiency and does not require unreachable future design or split mechanical mirror edits into separate tasks;
- `AC-6`: existing deterministic contracts and focused tests enforce explicit policy markers and duplication boundaries without fuzzy risk classification, new runtime artifacts, or runtime-corpus growth.

The operating envelope is local repository instruction, validator, and test artifacts only. No active-config installation, reload, deployment, release, commit, push, merge, credential use, or remote mutation is allowed. The stop line is the exact instruction, production-support, and SDET test path sets in `design.md`; one initial fresh SDET; at most one eligible in-scope correction wave; one fresh final review; and one Material delivery review. Any new runtime artifact, new support/test file, third lifecycle profile, remote-operation authority, fuzzy risk classifier, or rewrite of `integrate-continuous-sdlc-learning` requires a separate owner decision.

- [x] 1.1 Main session: capture baseline outputs for `npm run instruction:inventory -- --format json`, `npm run validate:strict`, `npm test`, and `npm run openspec:validate`; record current changed-file scope; freeze `AC-1` through `AC-6`, the exact design write sets, one correction wave, mandatory gates, and the stop line before any loaded-instruction mutation. Pass condition: every command outcome and baseline failure, if any, is recorded without modifying candidate artifacts.

### Frozen Scope Capsule And Baseline

- Frozen on 2026-07-20 before any loaded-instruction mutation. Profile: `Material`. Primary outcome: implement the earliest safe outcome-first pilot-slice policy while preserving the existing `Ordinary Small | Material` lifecycle and full Change-Ready qualification.
- Frozen acceptance criteria: `AC-1` through `AC-6` exactly as stated above. Frozen task IDs: `1.1`, `2.1`, `2.2`, `2.3`, `2.4`, `3.1`, `3.2`, `4.1`, `4.2`, `5.1`, `5.2`, `6.1`, `6.2`, and `6.3`.
- Production write set: the 17 instruction paths in `design.md` lines 138-154 and the six existing support paths in `design.md` lines 179-184. Test write set: only existing files selected from the five paths in `design.md` lines 188-192. Main-session status and evidence-reference updates in this `tasks.md` remain allowed.
- Forbidden artifacts and non-goals: no new runtime, production-support, test, validator, evidence, service, role, agent, skill, plugin, MCP, state-store, deployment, or remote-operation artifact; no third lifecycle profile; no fuzzy risk classifier; no rewrite of `integrate-continuous-sdlc-learning`; no install, activation, reload, credential use, commit, push, merge, deployment, release, or remote mutation.
- Mandatory gates: post-production inventory and strict validation; one isolated fresh-session six-observation proof before SDET and one replay after SDET; one initial fresh SDET; parent contract and library suites; one complete-candidate inventory; one exact complete-candidate run of strict validation, `npm test`, and OpenSpec validation; one instruction-artifact review; one final-candidate review; and one Material delivery review.
- Finite correction policy: one eligible in-scope correction wave; at most one fresh corrected-candidate SDET if that wave is consumed; final and delivery rejection are terminal for this attempt.
- Stop line: the exact frozen production and test paths, gates, and review waves above. Any additional acceptance criterion, task, path, gate, evidence tool, or artifact requires a new owner-approved revision or separate change.
- Baseline changed-file scope: `git status --short` reported only `?? openspec/changes/adopt-outcome-first-pilot-slices/`.
- Baseline instruction inventory: pass, 55 artifacts, 95,087 total token proxy; `global/AGENTS.md` token proxy 13,891; combined token proxy for the 17 frozen changed runtime instruction paths 50,385.
- Baseline `npm run validate:strict`: pass, `skills=24 agents=18 markdown=153 warnings=0 infos=1`; the existing top-level allow permission was informational only.
- Baseline `npm test`: pass with exit code 0; no failing suite. Existing Node module-type performance warnings were non-failing.
- Baseline `npm run openspec:validate`: pass, 9 passed and 0 failed.

### Revision 2 Scope Capsule

- Owner decision: on 2026-07-20, after the first revision exhausted its correction wave, the user explicitly selected a new revision rather than leaving the change paused.
- Primary outcome and frozen acceptance criteria remain unchanged: `AC-1` through `AC-6` exactly as stated above. Existing task IDs remain unchanged; the only added task ID is `2.5` below.
- Revision baseline: instruction inventory passes with `global/AGENTS.md` token proxy 11,706 and combined frozen runtime-path token proxy 47,411; `npm run validate:strict` fails only because `REPO_AGENTS.md` no longer contains the exact required markers `repetitive, evidence-heavy`, `explicit outputs`, and `model-like summarization`.
- Revision production mutation is restricted to `REPO_AGENTS.md` and only the smallest wording needed to restore those three existing deterministic-helper contract markers without weakening `AC-1` through `AC-6` or exceeding the original inventory baselines. The complete existing 23-file production candidate and original five-path test scope remain the candidate boundary.
- Forbidden artifacts and non-goals remain unchanged. No new runtime/support/test file, criterion, gate, evidence tool, profile, external-operation authority, fuzzy classifier, or change to `integrate-continuous-sdlc-learning` is allowed.
- All prior post-production qualification evidence is invalidated. Mandatory gates remain tasks `3.1` through `6.3`: post-production inventory/strict validation; fresh-session proof; one initial fresh SDET; parent test suites; proof/inventory replay; one complete-candidate validation run; instruction review; final review; and Material delivery review.
- Revision 2 allows one eligible correction wave after task `2.5`, at most one fresh corrected-candidate SDET if consumed, one final review, and one delivery review. Final or delivery rejection remains terminal.
- Revision 2 stop line is task `2.5`, the unchanged production/test path boundaries, and the unchanged mandatory gates. Any additional task, path, criterion, gate, artifact, or evidence tool requires another explicit owner-approved revision or separate change.

### Revision 3 Scope Capsule

- Owner decision: on 2026-07-20, after the first SDET parent-suite run exposed an out-of-scope doctor authority fixture, the user explicitly approved Revision 3 and only one additional existing test path: `tools/test-library/doctor.ts`.
- Primary outcome, `AC-1` through `AC-6`, existing task IDs, production candidate paths, mandatory gates, non-goals, and external-operation prohibitions remain unchanged. No new runtime, production-support, or evidence artifact is allowed.
- Revision baseline failure batch: `node tools/test-contracts.ts` reports six failures. Five are candidate-attributable production contract omissions from instruction compression: the exact P2/note residual marker, corrected-candidate SDET second-correction prohibition, `destructive operations` user-owned boundary, complete missing-conforming-capability delivery boundary, and exact unrelated-work rollback protection. One is a SDET oracle defect that incorrectly treats parenthesized workflow stages as matching the existing step-name parser. `node tools/test-library.ts` reports 12 doctor failures because hardcoded conforming authority in `tools/test-library/doctor.ts` lacks the new Pilot-Ready/enforced-envelope markers.
- Revision production correction is restricted to `global/AGENTS.md`, `global/skills/change-ready-sdlc/SKILL.md`, and `instructions/universal-development-loop.md`, and only the five exact lost contract semantics above. Original inventory non-growth baselines remain mandatory.
- Revision SDET scope is the original five existing test paths plus `tools/test-library/doctor.ts`. The fresh corrected-candidate SDET may repair the workflow expectation, doctor conforming authority, and any directly related frozen `AC-1` through `AC-6` oracle from the captured failure batch; no other test path or support mechanism is allowed.
- The previous task `3.1`, `3.2`, and `4.1` evidence becomes historical when production mutation begins and must be replayed on the corrected candidate. Task `4.2` remains incomplete. Revision 3 permits one eligible correction wave after this approved production/SDET batch; a serious failure after that wave terminates.
- Revision 3 stop line is the three-path production correction, six-path test scope, existing gates `3.1` through `6.3`, one fresh corrected-candidate SDET, one final review, and one Material delivery review. Any further task, criterion, path, gate, artifact, or evidence tool requires another explicit owner-approved revision or separate change.
- Revision 3 correction-wave eligibility: after the approved SDET batch, `node tools/test-library.ts` passed all 262 cases and `node tools/test-contracts.ts` exposed only two further sequential baseline tokens in `global/skills/change-ready-sdlc/SKILL.md`: `A second serious failure after the correction wave terminates the attempt` and `does not count as full change rollback`. Baseline tests passed; the current candidate shortened both; exact reproducers are recorded; the complete fix fits the frozen skill path without new infrastructure; the wave was unused. Dispatching this two-token production correction consumes the single Revision 3 correction wave and requires replay plus one new fresh corrected-candidate SDET. Any later serious failure terminates the attempt.

### Revision 4 Scope Capsule

- Owner decision: on 2026-07-20, after Revision 3 terminated on one final sequential contract token, the user explicitly approved Revision 4 for that token and a complete qualification replay.
- Primary outcome, `AC-1` through `AC-6`, existing task IDs, candidate paths, test scope, mandatory gates, non-goals, and external-operation prohibitions remain unchanged. No path, artifact, criterion, task, gate, evidence tool, or runtime mechanism is added.
- Revision baseline: current inventory and strict validation pass; current fresh-session proof and fresh SDET assessment pass; pre-correction library suite passed 262/262. Post-correction contracts suite has exactly one failure: `global/skills/change-ready-sdlc/SKILL.md` contains the shortened `Final and delivery review are accept-or-reject only and never initiate autonomous correction or replay` instead of the final exact baseline token `Final and delivery review are accept-or-reject gates and never initiate autonomous correction or replay`. The existing test list confirms this is its last sequential token.
- Revision production mutation is restricted to `global/skills/change-ready-sdlc/SKILL.md` and only that exact phrase replacement. Original instruction non-growth baselines remain mandatory.
- Revision test scope remains the six existing paths approved in Revision 3. One new fresh SDET must assess the Revision 4 candidate; no test change is expected because the existing parent test directly asserts the restored token.
- Production mutation invalidates prior task `3.1`, `3.2`, and `4.1` evidence. Replay all gates `3.1` through `6.3`. Revision 4 allows one eligible correction wave after the approved one-token production/SDET batch; a serious failure after that wave terminates.
- Revision 4 stop line is the one-phrase skill mutation, unchanged six-path test scope, existing mandatory gates, one initial fresh SDET, at most one fresh corrected-candidate SDET if the correction wave is consumed, one final review, and one Material delivery review. Any expansion requires another explicit owner-approved revision or separate change.

### Revision 5 Scope Capsule

- Owner decision: on 2026-07-20, after the Revision 4 instruction-artifact review returned four P1 Blocking Evidence items, the user explicitly approved Revision 5, one new task `2.6`, one added existing production path `README.md`, and a full qualification replay.
- Primary outcome and `AC-1` through `AC-6` remain unchanged. Revision 5 directly resolves the four verified review blockers: complete always-loaded Pilot safety-floor authority for Ordinary Small; Material/qualification-only fresh SDET wording in planning skills; fence-aware operative-text Pilot marker validation; and README reviewer-field synchronization.
- Production write scope is restricted to existing paths: `global/AGENTS.md`, `global/skills/change-ready-sdlc/SKILL.md`, `global/skills/deep-task-planning/SKILL.md`, `global/skills/next-step/SKILL.md`, `global/skills/service-architecture-design/SKILL.md`, `global/skills/openspec-consistency-review/SKILL.md`, `tools/contracts/skills.ts`, `tools/validators/active-authority.ts`, `tools/validators/devkit-contract.ts`, `tools/validators/routing.ts`, and newly approved `README.md`. Use fewer paths when sufficient.
- Exact production outcomes: always-loaded authority must require bounded outcome/non-goals, enforced envelope, real-boundary proof, focused validation, critical-invariant protection, material failure visibility, proportional disable/rollback/containment, accepted material reachable risk bundle, and no uncontrolled critical/envelope-escape risk before `Pilot-Ready: yes`; the skill retains qualification-specific procedure without a second complete block. Planning skills require fresh SDET only for Material/explicit qualification and reuse the Ordinary Small proof/focused-validation/optional-regression route. Whole-file Pilot markers must ignore closed and unclosed fenced examples using deterministic existing parsing/masking, not fuzzy inference. README replaces `Actionable Continuation Items` with non-authorizing `Follow-up Candidates`, and existing deterministic validation/tests enforce synchronization.
- Test write scope remains the six existing approved paths: `tools/test-contracts-change-ready.ts`, `tools/test-contracts-change-ready-delivery.ts`, `tools/test-contracts.ts`, `tools/test-helpers/library.ts`, `tools/test-library/validator-change-ready.ts`, and `tools/test-library/doctor.ts`. No new test/support file or framework is allowed.
- Revision baseline: inventory 55 artifacts, total token proxy 92,157, `global/AGENTS.md` 11,708, combined runtime-path proxy 47,455; inventory/strict/proof/SDET/parent/full validation passed, but instruction review `ses_08144f977ffeHiQ4Q5DB7uE4o3` returned the four verified blockers. Its P2 `Portable Small` alias remains residual and is outside Revision 5.
- Production mutation invalidates prior tasks `3.1` through `5.2`. Revision 5 requires one initial fresh SDET after new proof, one eligible correction wave, at most one fresh corrected-candidate SDET if consumed, one new instruction review, one fresh final review, and one Material delivery review. Review rejection never authorizes mutation.
- Forbidden artifacts and non-goals remain unchanged: no new runtime/test/support artifact, profile, role, fuzzy classifier, external-operation authority, deployment/activation/commit/push/merge, or rewrite of `integrate-continuous-sdlc-learning`.
- Revision 5 stop line is task `2.6`, the exact production/test paths above, gates `3.1` through `6.3`, one correction wave, one final review, and one Material delivery review. Any further task, criterion, path, gate, artifact, or evidence tool requires another explicit owner-approved revision or separate change.
- Revision 5 correction-wave eligibility: initial parent replay produced `node tools/test-library.ts` pass `271/271` and one `node tools/test-contracts.ts` failure in the newly authored planning-profile test. The test uses case-sensitive `fresh-context` matching while `global/skills/next-step/SKILL.md` correctly begins its profile-conditioned sentence with `Fresh-context`; all other required markers are present. The finding maps to AC-1/AC-5, has a concrete reproducer, was introduced by the current SDET test, and fits entirely in `tools/test-contracts-change-ready.ts` without new infrastructure. Dispatching a new fresh SDET correction consumes the single Revision 5 correction wave; any later serious failure terminates the attempt.

### Revision 6 Scope Capsule

- Owner decision: on 2026-07-20, after the Revision 5 instruction review returned two remaining P1 blockers, the user explicitly approved Revision 6 without new paths, added task `2.7`, and authorized a complete qualification replay.
- Primary outcome and `AC-1` through `AC-6` remain unchanged. Revision 6 makes `global/AGENTS.md` the sole complete Pilot safety-floor authority; `global/skills/change-ready-sdlc/SKILL.md` retains only qualification-specific Pilot disposition reference, Pilot/Change coexistence, terminal Change-Ready gate interaction, and external-operation separation. It also conditions `global/agents/openspec-architecture-reviewer.md` fresh-context testing on Material/explicit qualification and gives Ordinary Small the proportional focused route.
- Production write scope is restricted to existing approved paths: `global/skills/change-ready-sdlc/SKILL.md`, `global/agents/openspec-architecture-reviewer.md`, `tools/contracts/skills.ts`, `tools/validators/active-authority.ts`, and `tools/validators/routing.ts`. Use fewer when sufficient. No complete floor marker or test may remain normative in the skill.
- Test write scope remains the six existing approved paths. Required test outcomes: skill markers/assertions cover only qualification-specific detail and explicitly bind complete floor authority to `global/AGENTS.md`; the existing profile-conditioned planning oracle includes `openspec-architecture-reviewer` and proves its Material/Ordinary split; all prior fence/no-fuzzy/README/doctor/AC coverage remains.
- Revision baseline: inventory 55/92,493, `global/AGENTS.md` 11,859, combined runtime 47,787; proof/SDET/parents/full validation passed; instruction review `ses_08119b06effeDO32BxOVF14Y0b` accepted prior P1 closures but rejected duplicate floor authority and unconditional OpenSpec reviewer testing. P2 `Portable Small` remains deferred.
- Production mutation invalidates tasks `3.1` through `5.2`. Revision 6 requires one initial fresh SDET, one eligible correction wave, at most one corrected fresh SDET, one new instruction review, one fresh final review, and one Material delivery review. Review rejection remains terminal and non-authorizing.
- Forbidden artifacts and non-goals remain unchanged: no new path/file/framework/profile/fuzzy classifier/external authority/deployment/activation/commit/push/merge or unrelated cleanup.
- Revision 6 stop line is task `2.7`, the exact production/test paths above, gates `3.1` through `6.3`, one correction wave, one final review, and one delivery review. Any expansion requires a new explicit owner-approved revision or separate change.

### Revision 7 Scope Capsule

- Owner decision: on 2026-07-20, after the Revision 6 instruction review found one new P1 outside its frozen paths, the user explicitly selected Revision 7 to continue the current change with the smallest approved expansion.
- Primary outcome and `AC-1` through `AC-6` remain unchanged. Revision 7 closes only the verified Ordinary Small/Material testing-route contradiction in three default-installed, Ordinary-reachable surfaces.
- Production write scope is restricted to existing paths: `global/skills/code-quality-audit/SKILL.md`, `global/agents/instruction-artifact-reviewer.md`, and `README.md`. Exact outcome: independent fresh-context systematic risk testing is required only for Material/explicit qualification; Ordinary Small keeps observable proof, focused validation, and optional smallest post-proof regression. No other policy or output-contract cleanup is authorized.
- SDET write scope is restricted to the existing `tools/test-contracts-change-ready.ts`. The required oracle extends the existing parent-reachable profile-conditioned route across all three Revision 7 surfaces and verifies that every operative fresh-context SDET/testing rule is Material/explicit-qualification qualified while Ordinary Small retains the focused route.
- Revision baseline: tracked Candidate Reference `5306c45bb9a690591b6b2c40a48b62f3faa3fd9d`; inventory 55/92,326, `global/AGENTS.md` 11,859, combined runtime 47,620; proof/SDET/parents/full validation passed. Instruction review `ses_080ff8eb3ffeQSHWYs2n1KqylA` verified both Revision 5 closures but returned P1 `BE-1` against the three paths above. Main readback confirmed the cited operative text and default-profile reachability.
- Production mutation invalidates tasks `3.1` through `5.2`. Revision 7 requires one initial fresh SDET after new proof, one eligible correction wave, at most one corrected fresh SDET if consumed, one new instruction review, one fresh final review, and one Material delivery review. Review rejection remains terminal and non-authorizing.
- Forbidden artifacts and non-goals remain unchanged: no new path/file/framework/profile/fuzzy classifier/external authority/deployment/activation/commit/push/merge, no production support change, no unrelated `code-quality-audit` output-contract cleanup, and no rewrite of `integrate-continuous-sdlc-learning`.
- Revision 7 stop line is task `2.8`, the exact three production paths and one test path above, gates `3.1` through `6.3`, one correction wave, one final review, and one delivery review. Any expansion requires a new explicit owner-approved revision or separate change.

### Revision 8 Scope Capsule

- Owner decision: on 2026-07-20, after the Revision 7 instruction review rejected a path-by-path oracle and found additional default-installed contradictions, the user explicitly selected Revision 8 for a catalog-complete closure in the current change.
- Primary outcome and `AC-1` through `AC-6` remain unchanged. Revision 8 closes the verified Ordinary Small/Material testing-route class across every confirmed default-profile mixed-trigger surface and adds one deterministic catalog-completeness oracle. Surfaces whose exact trigger itself guarantees public protocol/compatibility, concurrency, deployment, or broad high-risk/exhaustive Material work remain unchanged but must be explicitly classified by the test oracle; role-ownership-only references remain unchanged.
- Production write scope is restricted to nine existing paths: `global/skills/rust-workspace-bootstrap/SKILL.md`, `global/skills/config-schema-validation/SKILL.md`, `global/skills/external-service-simulator-harness/SKILL.md`, `global/skills/latency-benchmark-pack/SKILL.md`, `global/skills/documentation-hardening-loop/SKILL.md`, `global/skills/instruction-artifact-tuning/SKILL.md`, `global/agents/deployment-config-reviewer.md`, `global/agents/performance-reliability-reviewer.md`, and `global/agents/session-delivery-reviewer.md`. Exact outcome: every mixed-trigger fresh-context testing mandate is Material/explicit-qualification-only; Ordinary Small keeps observable proof, focused validation, and optional smallest post-proof regression; the delivery diagnostic scale cannot override the portable profile.
- SDET write scope is restricted to existing `tools/test-contracts-change-ready.ts`. Required oracle: derive installed skill/agent paths deterministically from read-only `profiles/all.json`; require every installed surface with fresh-context/fresh-SDET testing language to be explicitly classified as profile-conditioned, exact-trigger Material-only, or role-ownership-only; reject an unclassified installed surface; retain line-level Material qualification and Ordinary Small route checks for every profile-conditioned surface. No fuzzy trigger/risk inference is allowed.
- Revision baseline: tracked Candidate Reference `d9569b25b9d284f3af74ab12402c91a6a038f819`; inventory 55/92,449, `global/AGENTS.md` 11,859, combined 19 changed runtime paths 51,745 against corresponding baseline 54,416; proof/SDET/parents/full validation passed. Instruction review `ses_080e8d1feffeWtYYuQCYbDhQNy` returned `BE-1` for eight mixed-trigger paths and `BE-2` for the non-catalog-complete oracle. Main deterministic inventory confirmed those and the ninth delivery diagnostic-scale contradiction.
- Production mutation invalidates tasks `3.1` through `5.2`. Revision 8 requires one initial fresh SDET after new proof, one eligible correction wave, at most one corrected fresh SDET if consumed, one new instruction review, one fresh final review, and one Material delivery review. Review rejection remains terminal and non-authorizing.
- Forbidden artifacts and non-goals remain unchanged: no new path/file/framework/profile/fuzzy classifier/external authority/deployment/activation/commit/push/merge, no production support/validator/profile mutation, no unrelated output-contract or prose cleanup, and no rewrite of `integrate-continuous-sdlc-learning`.
- Revision 8 stop line is task `2.9`, the exact nine production paths and one test path above, gates `3.1` through `6.3`, one correction wave, one final review, and one delivery review. Any expansion requires a new explicit owner-approved revision or separate change.

## 2. Implement Canonical Policy

- [x] 2.1 Production author: replace the conflicting maximal-planning and broad complexity language in `global/AGENTS.md` and add qualification-specific Pilot-Ready behavior to `global/skills/change-ready-sdlc/SKILL.md`; preserve Ordinary Small/Material routing, closed-world scope, Change-Ready qualification, unrelated work, and external-operation safety. Pass condition: bounded readback maps the two files to `AC-1` through `AC-5` with no third profile or duplicated complete policy block.
- [x] 2.2 Production author: align only the role-specific decisions in `global/agents/implementation-worker.md`, `global/agents/sdet-quality-engineer.md`, `global/agents/implementation-readiness-reviewer.md`, `global/agents/openspec-architecture-reviewer.md`, `global/agents/final-candidate-reviewer.md`, `global/agents/session-delivery-reviewer.md`, and `instructions/leaf-reviewer-agent-contract.md`. Pass condition: implementation uses the enforced envelope, SDET selects representative reachable risks, readiness stops at the next slice, reviewers retain valid findings with minimum remedies, and Change-Ready rejection does not automatically erase independent Pilot-Ready evidence.
- [x] 2.3 Production author: align next-slice planning and project-facing mirrors in `global/skills/deep-task-planning/SKILL.md`, `global/skills/next-step/SKILL.md`, `global/skills/service-architecture-design/SKILL.md`, `global/skills/openspec-consistency-review/SKILL.md`, `instructions/universal-development-loop.md`, `instructions/reusable-project-agent-instructions.md`, `templates/project/AGENTS.md`, and `REPO_AGENTS.md`. Pass condition: current-envelope decisions remain actionable, future-scope design is non-blocking, and no file copies the complete canonical policy.
- [x] 2.4 Production author: extend only `tools/contracts/agents.ts`, `tools/contracts/reviewer-binding.ts`, `tools/contracts/skills.ts`, `tools/validators/active-authority.ts`, `tools/validators/devkit-contract.ts`, and `tools/validators/routing.ts` to enforce `AC-1` through `AC-6` through explicit fields, markers, configured paths, and forbidden phrases. Pass condition: no helper infers reachability, severity, materiality, containment, or risk acceptance from natural-language text and no new support file exists.
- [x] 2.5 Revision 2 production author: restore the existing deterministic-helper contract markers `repetitive, evidence-heavy`, `explicit outputs`, and `model-like summarization` in `REPO_AGENTS.md` with the smallest wording change. Pass condition: `npm run validate:strict` passes, original instruction non-growth baselines remain satisfied, and no other candidate artifact changes.
- [x] 2.6 Revision 5 production author: close the four verified instruction-review blockers within the Revision 5 scope while preserving inventory non-growth, canonical placement, Ordinary Small/Material routing, and all prior contract semantics. Pass condition: bounded readback shows a complete always-loaded Pilot floor, profile-conditioned planning SDET, fence-aware operative marker checks, synchronized README guidance, no fuzzy classification, and no path outside the Revision 5 production set.
- [x] 2.7 Revision 6 production author: remove the complete Pilot floor from the conditional skill while retaining qualification-specific Pilot/Change semantics, and profile-condition the OpenSpec architecture reviewer within the existing deterministic contract surfaces. Pass condition: no duplicate complete floor authority remains; the reviewer cannot require qualification SDET for Ordinary Small; validators remain explicit/fence-aware/non-fuzzy; no path outside Revision 6 changes.
- [x] 2.8 Revision 7 production author: profile-condition fresh-context testing in `global/skills/code-quality-audit/SKILL.md`, `global/agents/instruction-artifact-reviewer.md`, and `README.md` without changing other behavior. Pass condition: Material/explicit qualification retains independent fresh-context SDET/testing; Ordinary Small retains observable proof, focused validation, and optional smallest post-proof regression; no other candidate path changes.
- [x] 2.9 Revision 8 production author: profile-condition fresh-context testing in the nine exact Revision 8 production paths without changing exact-trigger Material-only or role-ownership surfaces. Pass condition: mixed-trigger surfaces preserve proof first, qualification testing only for Material/explicit qualification, and the Ordinary Small focused route; delivery diagnostic detail cannot override portable profile; no other candidate path changes.

## 3. Prove The Working Instruction Path

Fresh-session proof uses a new OpenCode session with `OPENCODE_CONFIG_DIR` set to the candidate `global/` directory, an explicitly selected authenticated model, JSON output, an isolated fixture directory, and read-only prompts. Retain the session ids and bounded outputs in the handoff; do not add a persistent evidence subsystem.

- [x] 3.1 Main session: run `npm run instruction:inventory -- --format json` and `npm run validate:strict` after production integration. Pass condition: strict validation passes, `global/AGENTS.md` token proxy does not exceed baseline, the combined changed runtime instruction corpus does not exceed baseline, no runtime instruction path was added, and the candidate diff is limited to the frozen production paths.
- [x] 3.2 Main session: run one fresh-session read-only proof covering these exact observations: an enforced single-user/read-only slice is preferred over future concurrency/mutation mechanisms; a prose-only limit is rejected as containment; a reachable authorization or uncontrolled data-loss risk yields `Pilot-Ready: no`; a contained material risk requires one user decision; future-scale and evidence-format polish remain visible but do not block Pilot-Ready; and neither readiness disposition authorizes deployment. Pass condition: the fresh session demonstrates all six observations without proposing a third profile or editing the fixture.

Fresh-session proof evidence: session `ses_0817461cbffeOC5cnXnwrJgADm`, model `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r2-20260720`. The six numbered observations preferred enforced one-user/read-only containment, rejected prose-only containment, returned `Pilot-Ready: no` for reachable authorization/data-loss risk, required one explicit user decision for contained material risk, kept future-load and formatting-only findings non-blocking while visible, and denied deployment authority to both readiness dispositions. It named only `Ordinary Small` and `Material`; fixture readback contained zero entries.

Revision 3 replay evidence: inventory passed with `global/AGENTS.md` token proxy 11,708 and combined frozen runtime-path token proxy 47,449; strict validation passed with zero warnings. Fresh session `ses_0816354e7ffeeHXFZRDHJNEUAr` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r3-20260720`; it repeated all six required observations, named only `Ordinary Small | Material`, and left the zero-entry fixture unchanged.

Post-correction replay evidence: inventory passed with `global/AGENTS.md` token proxy 11,708 and combined frozen runtime-path token proxy 47,454; strict validation passed with zero warnings. Fresh session `ses_0815853b2ffesaebVZm0O4iKAW` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r3-corrected-20260720`; it repeated all six required observations, named only `Ordinary Small` and `Material`, and left the zero-entry fixture unchanged.

Revision 4 replay evidence: inventory passed with `global/AGENTS.md` token proxy 11,708 and combined frozen runtime-path token proxy 47,455; strict validation passed with zero warnings. Fresh session `ses_0814ebc96ffe5t19jqcGbgoH1y` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r4-20260720`; it repeated all six required observations, named only `Ordinary Small` and `Material`, and left the zero-entry fixture unchanged.

Revision 5 production replay evidence: inventory passed with 55 artifacts, `global/AGENTS.md` token proxy 11,859, and combined frozen runtime-path token proxy 47,787; strict validation passed with zero warnings. Fresh session `ses_0812e1d7cffeHEKNQ6lYXMTZUA` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r5-20260720`; it repeated all six required observations, named only `Ordinary Small` and `Material`, and left the zero-entry fixture unchanged.

Revision 6 production replay evidence: inventory passed with 55 artifacts, total token proxy 92,326, `global/AGENTS.md` token proxy 11,859, and combined frozen runtime-path token proxy 47,620; strict validation passed with zero warnings. Fresh session `ses_0810c76f6ffeLWZhL5tmcRjAJK` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r6-20260720`; it repeated all six required observations, named only `Ordinary Small` and `Material`, and left the zero-entry fixture unchanged.

Revision 7 production replay evidence: tracked Candidate Reference `af3a109e161e514f2ee89de8157f8f7c53f6ec83`; inventory passed with 55 artifacts, total token proxy 92,449, `global/AGENTS.md` token proxy 11,859, and combined 19 changed runtime-path token proxy 51,745 against the corresponding 54,416 baseline; strict validation passed with zero warnings. Fresh session `ses_080f40e5effe36k5vKEux8xcTJ` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r7-20260720`; it repeated all six required observations, named only `Ordinary Small` and `Material`, and left the zero-entry fixture unchanged.

Revision 8 production replay evidence: tracked Candidate Reference `f3270630103409ff6f4f618d4ca94da52fc98228`; inventory passed with 55 artifacts, total token proxy 92,755, `global/AGENTS.md` token proxy 11,859, and combined 27 changed runtime-path token proxy 57,754 against the corresponding 60,119 baseline; strict validation passed with zero warnings. Fresh session `ses_080da6e94ffewIihXxp6qrsP7f` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r8-20260720`; it repeated all six required observations, named only `Ordinary Small` and `Material`, and left the zero-entry fixture unchanged.

## 4. Add Independent Risk Evidence

- [x] 4.1 Fresh SDET: independently assess `AC-1` through `AC-6` and author or strengthen only the smallest required tests within `tools/test-contracts-change-ready.ts`, `tools/test-contracts-change-ready-delivery.ts`, `tools/test-contracts.ts`, `tools/test-helpers/library.ts`, and `tools/test-library/validator-change-ready.ts`; use fewer files when sufficient and do not edit production artifacts. Required scenarios are enforced versus prose-only containment, non-waivable critical risk, one material-risk bundle, future-scope residual routing, minimum-remedy order, next-increment OpenSpec readiness, semantic versus formatting evidence gaps, Pilot-Ready/Change-Ready separation, no third profile, no external-operation authority, canonical placement, and no fuzzy helper classification.

Fresh SDET evidence: session `ses_081729ff5ffeHcd3UJmb5wTbtr`, action `authored-tests`, effective model `openai/gpt-5.6-sol`, `Blocking Evidence: none`. Test-only ownership covered exactly the five frozen paths; both parent suites reach the authored cases. No mock exception or production mutation was reported.

Revision 3 fresh SDET evidence: session `ses_08161d066ffedo4R18e6ATEM1D`, action `authored-tests`, effective model `openai/gpt-5.6-sol`, `Blocking Evidence: none`. It retained the focused AC-1 through AC-6 coverage, restored the baseline 12-name workflow parser oracle, and updated the newly approved `tools/test-library/doctor.ts` conforming authority fixture without production mutation. Final changed test scope is five original test paths plus the approved doctor fixture; `tools/test-contracts.ts` returned byte-equivalent to baseline.

Post-correction fresh SDET evidence: session `ses_081574fa3ffebNzsEoXhEwCLsl`, action `assessed-existing-tests`, effective model `openai/gpt-5.6-sol`, no file changes, `Blocking Evidence: none`. It confirmed direct parent-reachable coverage for both corrected tokens and all AC-1 through AC-6 scenarios.

Revision 4 fresh SDET evidence: session `ses_0814de535ffen4sBpkRnfefQzj`, action `assessed-existing-tests`, effective model `openai/gpt-5.6-sol`, no file changes, `Blocking Evidence: none`. It confirmed that the final exact phrase and all AC-1 through AC-6 scenarios are directly parent-reachable without duplicate tests.

Revision 5 fresh SDET evidence: session `ses_0812c7778ffeirGlGP4GOP003x`, action `authored-tests`, effective model `openai/gpt-5.6-sol`, `Blocking Evidence: none`. It changed only five approved test paths and added direct parent-reachable oracles for the complete always-loaded floor, four profile-conditioned planning surfaces, closed/tilde/unclosed fenced-only markers, role-duplication fence masking, and README current/stale fields. No production mutation or mock exception was reported.

Revision 5 corrected-candidate SDET evidence: session `ses_08122b94dffex5AonyDCGjK3hE`, action `authored-tests`, effective model `openai/gpt-5.6-sol`, `Blocking Evidence: none`. It changed only `tools/test-contracts-change-ready.ts` to make `fresh-context` lexical matching case-insensitive while preserving exact profile and per-line Material qualifier checks. This consumed the single Revision 5 correction wave; a later serious failure terminates the attempt.

Revision 6 fresh SDET evidence: session `ses_0810b20aeffem6KQQiYZ4GjJi0`, action `authored-tests`, effective model `openai/gpt-5.6-sol`, `Blocking Evidence: none`. It changed only four approved test paths: `tools/test-contracts-change-ready.ts`, `tools/test-contracts-change-ready-delivery.ts`, `tools/test-library/validator-change-ready.ts`, and `tools/test-library/doctor.ts`. Direct parent-reachable oracles now require the complete floor only in global AGENTS, require the skill's global-floor authority reference while keeping its complete-policy marker count below threshold, include `openspec-architecture-reviewer` in the Material/Ordinary profile split, and preserve prior fence/no-fuzzy/README/doctor coverage. No production mutation or mock exception was reported; same-model correlation remains a residual risk. Post-SDET tracked Candidate Reference: `5306c45bb9a690591b6b2c40a48b62f3faa3fd9d`.

Revision 7 fresh SDET evidence: session `ses_080f2bfd4ffe19nskJXfjobYe0`, action `authored-tests`, effective model `openai/gpt-5.6-sol`, `Blocking Evidence: none`. It changed only the single approved `tools/test-contracts-change-ready.ts`, extending the existing profile-conditioned case rather than adding a new case. The parent-reachable oracle now covers `code-quality-audit`, `instruction-artifact-reviewer`, and README; checks both code-quality fresh-context rules; requires proof before fresh-context testing, exact Material/explicit qualification, and the Ordinary Small focused route; and preserves prior AC-1 through AC-6 coverage. No production mutation or mock exception was reported; same-model correlation remains residual. Post-SDET tracked Candidate Reference: `d9569b25b9d284f3af74ab12402c91a6a038f819`.

Revision 8 fresh SDET evidence: session `ses_080d8da2cffeBy7pdUMAbmKRUU`, action `authored-tests`, effective model `openai/gpt-5.6-sol`, `Blocking Evidence: none`. It changed only `tools/test-contracts-change-ready.ts`, extending the existing case without adding a new case/file. The parent-reachable oracle derives installed paths from `profiles/all.json`, detects operative exact fresh-context/fresh-SDET language, and requires exact equality with one explicit 31-surface table: 18 profile-conditioned, 10 exact-trigger Material, and 3 role-ownership. Every class has direct static path/marker assertions; README remains a direct non-profile curation oracle; no fuzzy inference, production mutation, or mock exception was added. Same-model correlation remains residual. Post-SDET tracked Candidate Reference: `42c89b7400ec286375842fb96e417ede36ecfbc1`.
- [x] 4.2 Main session: inspect the SDET diff for exact test-only ownership, recapture the candidate, then run the existing executable parent suites `node tools/test-contracts.ts` and `node tools/test-library.ts`. Pass condition: both suites pass and their output includes the focused Change-Ready contract and validator cases; extracted modules are not invoked as standalone scripts.

Revision 3 terminal blocker: pre-correction `node tools/test-library.ts` passed `262/262`. Post-correction `node tools/test-contracts.ts` has one remaining failure because `global/skills/change-ready-sdlc/SKILL.md` lacks the exact final sequential baseline token `Final and delivery review are accept-or-reject gates and never initiate autonomous correction or replay`. This candidate-attributable token was revealed only after the two-token correction, but the single Revision 3 correction wave is exhausted. Per the frozen stop line, no further mutation or gate replay is authorized in this attempt; task `4.2` remains incomplete.

Revision 4 parent evidence supersedes the Revision 3 terminal result for the new owner-approved revision: `node tools/test-contracts.ts` passed all 78 cases and `node tools/test-library.ts` passed all 262 cases. Output included the focused outcome-first/Pilot-Ready contracts, validator marker/duplication/no-fuzzy cases, and doctor authority topology. No extracted module was run standalone.

Revision 5 parent evidence: after corrected-candidate SDET, `node tools/test-contracts.ts` passed all 79 cases and `node tools/test-library.ts` passed all 271 cases. Output included complete floor, planning profile, fence-aware operative marker, role-duplication fence, README synchronization, no-fuzzy, and doctor topology cases. No extracted module was run standalone.

Revision 6 parent evidence: `node tools/test-contracts.ts` passed all 79 cases and `node tools/test-library.ts` passed all 271 cases. Output included the qualification-only skill/global-floor binding, role/planning complete-policy threshold, `openspec-architecture-reviewer` Material/Ordinary split, fence-aware markers, no-fuzzy, README, and doctor cases. No extracted module was run standalone.

Revision 7 parent evidence: `node tools/test-contracts.ts` passed all 79 cases and `node tools/test-library.ts` passed all 271 cases. Output included the renamed planning/review/audit/README Material-only fresh-testing contract plus all prior qualification-only skill/global-floor, duplication, fence, no-fuzzy, README, and doctor cases. No extracted module was run standalone.

Revision 8 parent evidence: `node tools/test-contracts.ts` passed all 79 cases and `node tools/test-library.ts` passed all 271 cases. Output included the catalog-classified installed fresh-testing surface contract plus all prior qualification-only skill/global-floor, duplication, fence, no-fuzzy, README, and doctor cases. No extracted module was run standalone.

## 5. Validate The Complete Candidate

- [x] 5.1 Main session: rerun the fresh-session proof from task 3.2 against the complete post-SDET candidate and rerun `npm run instruction:inventory -- --format json`. Pass condition: all six behavioral observations still hold, candidate scope is unchanged outside frozen production/test paths, and both instruction non-growth conditions still pass.

Revision 4 post-SDET evidence: inventory remained `global/AGENTS.md` token proxy 11,708 and combined frozen runtime-path token proxy 47,455. Fresh session `ses_0814877dbffexSnFB2zCMi59ZO` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r4-post-sdet-20260720`; all six observations passed, only `Ordinary Small` and `Material` were named, and fixture readback had zero entries. Final changed scope is the frozen 23 production paths plus five approved changed test paths; no additional runtime path exists.

Revision 5 post-SDET evidence: inventory remained 55 artifacts, `global/AGENTS.md` token proxy 11,859, and combined frozen runtime-path token proxy 47,787. Fresh session `ses_0811d10eaffewat5G3h3tqOnEL` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r5-post-sdet-20260720`; all six observations passed, only `Ordinary Small` and `Material` were named, and fixture readback had zero entries. Final scope is 29 approved production/test paths plus the OpenSpec source directory; no runtime path was added.

Revision 6 post-SDET evidence: inventory remained 55 artifacts, total token proxy 92,326, `global/AGENTS.md` token proxy 11,859, and combined frozen runtime-path token proxy 47,620. Fresh session `ses_08102e9f1ffeHhci0S9XMYU4km` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r6-post-sdet-20260720`; all six observations passed, only `Ordinary Small` and `Material` were named, and fixture readback had zero entries. Final scope remains 29 approved production/test paths plus the OpenSpec source directory; no runtime path was added.

Revision 7 post-SDET evidence: inventory remained 55 artifacts, total token proxy 92,449, `global/AGENTS.md` token proxy 11,859, and combined 19 changed runtime-path token proxy 51,745 against the corresponding 54,416 baseline. Fresh session `ses_080ec0706ffelnlthbCroFbnBF` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r7-post-sdet-20260720`; all six observations passed, only `Ordinary Small` and `Material` were named, and fixture readback had zero entries. Final scope is 31 approved production/test paths plus the OpenSpec source directory; no runtime path was added.

Revision 8 post-SDET evidence: inventory remained 55 artifacts, total token proxy 92,755, `global/AGENTS.md` token proxy 11,859, and combined 27 changed runtime-path token proxy 57,754 against the corresponding 60,119 baseline. Fresh session `ses_080ced56affe2ogf0FlKQvowUA` used `openai/gpt-5.5`, JSON output, candidate `OPENCODE_CONFIG_DIR`, and isolated fixture `pilot-proof-r8-post-sdet-20260720`; all six observations passed, only `Ordinary Small` and `Material` were named, and fixture readback had zero entries. Final scope is 39 approved production/test paths plus the OpenSpec source directory; no runtime path was added.
- [x] 5.2 Main session: run `npm run validate:strict`, `npm test`, and `npm run openspec:validate` exactly once against the complete current candidate. Pass condition: all three commands pass; any candidate-attributable frozen-criterion failure is routed only through the single allowed correction wave, while baseline or out-of-scope findings remain blockers/follow-ups without scope expansion.

Revision 4 complete validation: `npm run validate:strict` passed with zero warnings; `npm test` passed every suite, including library 262 and contracts 78; `npm run openspec:validate` passed 9/9. Existing Node module-type performance warnings were non-failing. No correction was required and the Revision 4 correction wave remains unused.

Revision 5 complete validation: `npm run validate:strict` passed with zero warnings; `npm test` passed every suite, including library 271 and contracts 79; `npm run openspec:validate` passed 9/9. Existing Node module-type performance warnings were non-failing. The only correction was the recorded SDET case-sensitivity fix; the Revision 5 correction wave is exhausted.

Revision 6 complete validation: `npm run validate:strict` passed with zero warnings; `npm test` exited 0 and passed every suite, including library 271 and contracts 79; `npm run openspec:validate` passed 9/9. Existing Node module-type performance warnings were non-failing. No correction was required and the Revision 6 correction wave remains unused.

Revision 7 complete validation: `npm run validate:strict` passed with zero warnings; `npm test` exited 0 and passed every suite, including library 271 and contracts 79; `npm run openspec:validate` passed 9/9. Existing Node module-type performance warnings were non-failing. No correction was required and the Revision 7 correction wave remains unused.

Revision 8 complete validation: `npm run validate:strict` passed with zero warnings; `npm test` exited 0 and passed every suite, including library 271 and contracts 79; `npm run openspec:validate` passed 9/9. Existing Node module-type performance warnings were non-failing. No correction was required and the Revision 8 correction wave remains unused.

## 6. Review And Handoff

- [x] 6.1 Dispatch one fresh read-only `instruction-artifact-reviewer` over the complete candidate, `AC-1` through `AC-6`, source-versus-runtime authority, canonical placement, instruction-inventory comparison, focused SDET report, fresh-session proof, and full validation. Pass condition: no unresolved finding proves duplicated authority, runtime growth, unsafe pilot semantics, or a current-envelope contradiction; P2/note polish remains residual.

Revision 4 instruction review evidence: fresh session `ses_08144f977ffeHiQ4Q5DB7uE4o3` returned `material findings` with four P1 Blocking Evidence items and one P2 residual. Verified blockers are: the complete Pilot safety floor exists only in the conditionally loaded skill while Ordinary Small may report Pilot-Ready; three planning skills and `next-step` retain unqualified fresh-SDET behavior; whole-file Pilot authority markers can be satisfied inside fenced examples; and `README.md` still prescribes forbidden `Actionable Continuation Items`. Inventory, proof, SDET, and validation evidence were accepted, but task `6.1` remains incomplete. The review authorizes no mutation or replay.

Revision 5 instruction review evidence: fresh session `ses_08119b06effeDO32BxOVF14Y0b` accepted the always-loaded floor semantics, four planning-skill fixes, fence-aware checks, README synchronization, inventory, proof, SDET, and validation, but returned two P1 Blocking Evidence items. The complete Pilot floor remains duplicated in `global/skills/change-ready-sdlc/SKILL.md` and is required there by current marker/tests, contrary to Revision 5 canonical placement. `global/agents/openspec-architecture-reviewer.md` also retains an unconditional fresh-context testing rule and is absent from the planning profile oracle. Revision 5 correction wave is exhausted; task `6.1` remains incomplete and the review authorizes no mutation or replay.

Revision 6 instruction review evidence: fresh session `ses_080ff8eb3ffeQSHWYs2n1KqylA` verified both Revision 5 P1 closures: `global/AGENTS.md` is now the sole complete Pilot floor and `openspec-architecture-reviewer` now has the required Material/Ordinary split with a parent-reachable oracle. It also accepted inventory non-growth, Pilot semantics, deterministic test reachability, and no duplicated authority. The review nevertheless returned one new P1 `Blocking Evidence` item against AC-2: default-installed, Ordinary-reachable `global/skills/code-quality-audit/SKILL.md` lines 20/107, `global/agents/instruction-artifact-reviewer.md` line 48, and `README.md` line 433 still impose unconditional qualification-style fresh-context testing despite the always-loaded Ordinary Small focused route. Main-session readback confirmed the cited operative text and default-profile reachability. Task `6.1` remains incomplete; Revision 6 is terminal and the finding authorizes no mutation or replay. A new owner-approved revision or separate change is required to expand production scope.

Revision 7 instruction review evidence: fresh session `ses_080e8d1feffeWtYYuQCYbDhQNy` verified the sole always-loaded floor, conditional-skill delta, planning/OpenSpec reviewer split, all three Revision 7 profile-conditioned surfaces, fence-aware authority, README field synchronization, inventory non-growth, and supplied proof/SDET/validation. It nevertheless returned P1 `BE-1` against AC-2 for eight additional default-installed Ordinary-reachable surfaces with unconditional fresh-context testing: `rust-workspace-bootstrap`, `config-schema-validation`, `external-service-simulator-harness`, `latency-benchmark-pack`, `documentation-hardening-loop`, `instruction-artifact-tuning`, `deployment-config-reviewer`, and `performance-reliability-reviewer`. P1 `BE-2` against AC-6 showed the existing eight-path oracle is not catalog-complete against `profiles/all.json`. Main-session deterministic inventory confirmed those paths and additionally found `session-delivery-reviewer` diagnostic `material` line 101 can impose qualification SDET without first binding the portable profile to Material; other default-profile fresh-testing surfaces are already profile-conditioned, role-ownership-only, or have exact protocol/compatibility/concurrency/deployment/broad-exhaustive triggers that exclude Ordinary Small. Task `6.1` remains incomplete; Revision 7 is terminal and the review/inventory authorize no mutation or replay. A new explicit owner decision is required.

Revision 8 instruction review evidence: fresh session `ses_080cb7961ffehe2pxI41zoI2Y0` returned `clean`, high confidence, `Blocking for acceptance: no`, and `Blocking Evidence: none`. It verified all prior P1 generations, the nine Revision 8 profile/diagnostic closures, the exact 31-surface installed catalog classification, parent reachability, canonical floor placement, deterministic no-fuzzy/fence behavior, inventory non-growth, and the supplied proof/SDET/full-validation chain. Three P2 residuals remain non-authorizing: unqualified fresh-testing prose in two non-installed manual templates, proposal impact-text drift for `code-quality-audit`, and `Portable Small/Material` terminology noise. Task `6.1` passes; no candidate mutation or replay was authorized or performed.
- [x] 6.2 After accepted SDET and complete validation, dispatch one fresh `final-candidate-reviewer` with the complete readable candidate and unsummarized required evidence. Pass condition: verdict is `approved` or `approved_with_notes`, `Blocking Evidence` is empty, and any Pilot-Ready distinction is evaluated without weakening terminal Change-Ready review authority.

Revision 8 final-candidate review evidence: fresh session `ses_080b9e111ffeyAnFv6Nx4DtFh6` returned `approved_with_notes`, high confidence, `Blocking: no`, and `Blocking Evidence: none`. It accepted candidate scope/identity, AC-1 through AC-6, proof, fresh SDET, parent/full validation, instruction review, safety, and external-operation separation. It found only P2/note residuals already routed outside the current candidate and assessed `Pilot-Ready: yes` only for the local limited envelope without installation/activation authority. Final-candidate gate passes; Material delivery review remains required before main may decide Change-Ready.
- [ ] 6.3 Dispatch one Material `session-delivery-reviewer` with the original owner decisions, frozen scope, complete candidate, proof, SDET, validation, instruction review, final review, inventory comparison, and residual risks. Pass condition: delivery is accepted under the current governing process, external operations remain `not performed`, and the handoff reports both `Pilot-Ready` and `Change-Ready` without claiming installation or activation.

Revision 8 delivery review evidence: fresh session `ses_080b439b7ffeSVvK0sh3Mo639L` returned `Verdict: not enough evidence`, `Blocking for Acceptance: yes`, `Pilot-Ready: no`, `Change-Ready: no`, and two P0 `Blocking Evidence` items. The optional `session_delivery_context` source resolved a root session with only two user messages, empty question/permission replies, and therefore could not verify the recorded explicit owner approvals for Revisions 2 through 8 from primary session evidence. It also reported 13 historical todo entries as unresolved after later todo snapshots replaced rather than explicitly completed/cancelled them. Technical gates, current candidate implementation, SDET, validation, instruction review, and final review were accepted, but task `6.3` self-gate was rejected. This delivery rejection is terminal for Revision 8 and authorizes no candidate mutation, administrative correction, or gate replay.
