# Local Qualification Handoff

## Outcome

- Profile: `Material`
- Candidate: `candidate-contract-green-r4`
- Development-Stage: `stable`
- Stable Candidate: `RC1`
- Outcome: later agents author repeated-use repository CLIs with effect-free,
  complete exit-zero `--help` / `-h`, and materialize mechanically derived durable
  structured artifacts from reviewed seed data outside helper code.
- Existing maintained-source placement and tiny one-off semantic records remain
  no-regression controls rather than new policy.

## Proof And Identity

- Exact Product Candidate, Runner, Evaluator, Environment, bundle, token, permission,
  and unrelated-diff identities are in `candidate-reference.md`.
- Frozen baseline: `evidence/baseline-capture-r2/`.
- Current candidate capture: `evidence/candidate-capture-r4/`.
- Current provider-free preflight: `evidence/preflight-candidate-r8/`.
- Current paired evaluator/replay: `evidence/paired-evaluate-r7/` and
  `evidence/paired-replay-r7/`, both `baselineComplete:true` and
  `candidateComplete:true`.
- Current permission readback: `evidence/permissions-r3/`, with permissive main,
  preserved specialist restrictions, and all-false hidden-arbiter tools.
- Instruction token proxies are `16,599` / `100,852`, below the frozen unrelated
  baseline `16,600` / `100,853`. The inherited normative `13,279` / `84,513`
  breach is outside this candidate and remains explicit.

## Observed Behavior

- Baseline repeated-use help printed usage but exited `1`; candidate `--help` and
  `-h` exit `0`, list commands/arguments, make no declared effect, and preserve the
  exact product scan result.
- Baseline embedded semantic variant rows in helper code without a reviewed seed;
  candidate keeps exact semantic rows in one seed JSON outside helper code and
  regenerates exact hash/length/order output stably.
- Baseline and candidate controls preserve reusable generator source outside
  ignored output and leave the tiny semantic one-off manual.
- Every configured session and disposable project was removed; no target repository
  or product CLI was changed.

## Critical Challenge And Validation

- Fresh SDET session `ses_0015246b6ffexEp7Izx59vHN7e`, Effective Model
  `xai/grok-4.6`, returned terminal `no-critical-risk` with no test edits. It
  inspected the earlier proven r2 hash; the root's terminal no-confirmed-critical
  rule forbids another SDET attempt. Current static-marker/consolidation changes are
  covered by current same-model proof and deterministic validation.
- `npm test`, `npm run validate:strict`, instruction inventory, selected/all strict
  OpenSpec validation, apply operation gate, permission proof, and
  `git diff --check` pass. Scoped secret review found no credential-like value.

## Limits, Migration, And Rollback

- This change does not repair existing `pmac-emulator` CLIs, migrate its structured
  plan, or move its ignored historical harnesses. Those remain separate deferred
  product changes.
- Model/version sensitivity and repository-local contrary instructions can still
  limit adherence. Helper-internal schema/readback strength and substring-level
  help completeness remain parked non-critical evaluator limitations.
- No installation, activation, provider configuration, credential, commit, push,
  archive, release, publication, target mutation, or remote state change occurred.
- To activate changed loaded instructions, an owner must separately authorize any
  installation and start a new OpenCode process; the current session cannot prove
  future process activation by source mutation alone.
- Rollback restores only this change's two `global/AGENTS.md` clauses, proof runner/
  inventory/package entry, shared process-boundary correction, and OpenSpec files,
  while preserving unrelated working-tree changes and immutable evidence.
- Archive is a separate owner-requested operation and has not run.
