# Strategy History

## 2026-08-14 - Treat the observed failures as project-only defects

- **Objective:** Avoid changing portable instructions when the concrete failures
  occurred in one target repository.
- **Approach:** Repair only the target CLI help handlers, proof README, structured
  plan, and ignored harness placement.
- **Evidence:** The product fixes are real and remain necessary, but current
  `global/AGENTS.md` requires proof-tool placement only in production-role terms,
  admits a deterministic helper only after repeated work, and contains no
  repeated-use CLI self-help or unsupported-help caller contract. The same gaps
  are reachable in unrelated repositories and in both Ordinary Small and Material
  work.
- **Outcome:** Rejected as the complete response. Target repairs remain explicit
  future scope while this change owns only the portable recurrence path.
- **Reason:** Product-only correction would leave future agents free to create the
  same no-help, hand-materialized, or ignored-source interfaces elsewhere.
- **Do-Not-Repeat Condition:** Do not broaden this change into target-project edits
  or claim that portable policy repairs existing binaries.
- **Evidence-Based Retry Condition:** Reconsider project-only scope only if same-
  model baseline proves the current portable instructions already enforce every
  proposed behavior and the observed recurrence is caused solely by contrary
  repository-local policy.

## 2026-08-14 - Put the policy only in change-ready-sdlc

- **Objective:** Keep always-loaded instruction context unchanged by routing the
  new controls through the Material qualification skill.
- **Approach:** Add CLI, structured-artifact, and helper-placement rules only to
  `global/skills/change-ready-sdlc/SKILL.md`.
- **Evidence:** Repeated-use CLIs and structured artifacts are authored during
  Ordinary Small work and before a task's Material profile is known. The concrete
  caller `--help` decision also occurs during discovery, when the skill may be
  correctly unloaded.
- **Outcome:** Rejected.
- **Reason:** A lazily loaded Material-only owner cannot prevent a high-frequency
  authoring defect in ordinary work.
- **Do-Not-Repeat Condition:** Do not place the canonical rule only in a lazily
  loaded skill or project template.
- **Evidence-Based Retry Condition:** Reconsider lazy routing only if the accepted
  trigger is narrowed to a behavior reachable exclusively after Material skill
  load, which is not the current outcome.

## 2026-08-14 - Add a new tooling-ergonomics policy chapter

- **Objective:** Make the complete new policy prominent and self-contained.
- **Approach:** Append a dedicated broad section to `global/AGENTS.md` and copy it
  into production/SDET instruction artifacts.
- **Evidence:** The existing reusable-proof-tooling, deterministic-helper, and
  Universal Task Briefing sections already own the relevant decisions. The
  canonical spec caps `global/AGENTS.md` at token proxy `13,279` and the complete
  instruction inventory at `84,513`; it also requires one complete runtime source
  plus role deltas rather than policy duplication.
- **Outcome:** Rejected in favor of replacing/consolidating existing clauses and
  adding terse specialist/review deltas.
- **Reason:** A new chapter increases retrieval and reconciliation cost and risks
  conflicting authority without creating a new owner.
- **Do-Not-Repeat Condition:** Do not add another complete policy block or duplicate
  it across agents.
- **Evidence-Based Retry Condition:** A new section is reconsidered only if source
  mapping proves no existing canonical clause can own the behavior without mixing
  unrelated responsibilities and token ceilings can still be met.

## 2026-08-14 - Use universal framework and size thresholds

- **Objective:** Make enforcement simple and mechanically obvious.
- **Approach:** Require every CLI to adopt one parser/help framework and every JSON
  above a line/byte threshold to be generated.
- **Evidence:** The accepted behavior needs no package dependency; a one-shot script
  does not need operator help, a short helper can own the only reproducer, a large
  cohesive semantic table can remain reviewable data, and generated raw evidence
  is legitimate. File size does not identify mechanical derivation or maintained
  knowledge.
- **Outcome:** Rejected.
- **Reason:** Framework and threshold rules overfit the observed files, widen
  implementation scope, and replace outcome-first judgment with poor proxies.
- **Do-Not-Repeat Condition:** Do not mandate a CLI library, generated command
  registry, blanket JSON generation, or line-count gate.
- **Evidence-Based Retry Condition:** Reconsider a shared framework only after at
  least two independent repository consumers demonstrate a compatible contract
  and lower total lifecycle cost than narrow behavior requirements.

## 2026-08-14 - Canonical global clauses plus role deltas and exact-fact proof

- **Objective:** Prevent the recurrence at the first authoring boundary without
  adding ceremony or weakening role/safety contracts.
- **Approach:** Extend the existing canonical global clauses; add concise worker,
  SDET, briefing, and instruction-review deltas; trigger generation from mechanical
  derivation/variant facts; classify maintained source by owned reusable knowledge;
  and compare identical same-model baseline/candidate workflows through an exact-
  fact evaluator.
- **Evidence:** Local proof infrastructure already owns isolated OpenCode sessions,
  same-model capture, candidate/source identity, evaluator replay, and cleanup.
  Existing static contracts can enforce markers and token/permission invariants but
  cannot prove model behavior. No current proof runner owns this exact three-
  scenario behavior.
- **Outcome:** Selected for proposal, design, delta spec, and tasks.
- **Reason:** This is the smallest portable contract that reaches Ordinary Small
  and Material authoring, preserves proportional one-offs, and supplies observable
  behavior evidence rather than prose confidence.
- **Do-Not-Repeat Condition:** Do not retain a candidate from structural markers or
  plausible wording alone; do not generalize beyond reproduced baseline gaps and
  paired candidate results.
- **Evidence-Based Retry Condition:** Select a materially different mechanism only
  if provider-free preflight or same-model baseline/candidate proof shows the
  canonical-clause approach cannot produce observable behavior without violating
  token, permission, role, or cleanup invariants.

## 2026-08-14 - Provider-free preflight attempt 1

- **Objective:** Prove rejected/compliant fixture discrimination, the installed
  loader route, zero-call boundary, cleanup, and create-new evidence contract
  before any configured-model call.
- **Approach:** Ran `npm run proof:tooling-ergonomics -- --mode preflight
  --evidence-root openspec/changes/improve-agent-tooling-ergonomics/evidence/
  preflight-baseline-r1 --capture-kind baseline --candidate-id
  baseline-800a61e9-dirty-c2623211`.
- **Evidence:** `evidence/preflight-baseline-r1/preflight.json` and
  `manifest.json`; model call count `0`; cleanup `removed`; repeated-CLI and
  mechanical-artifact oracle pairs passed.
- **Outcome:** Blocked at `source-placement` before baseline capture.
- **Reason:** The compliant fixture used the pretty-printing `writeJson` helper
  for `config/one-off.json`, while the accepted prompt and oracle require the
  exact compact record `{"message":"ready"}` plus a newline.
- **Do-Not-Repeat Condition:** Do not rerun this fixture implementation or
  overwrite `preflight-baseline-r1`.
- **Evidence-Based Retry Condition:** Write the one-off compliant fixture in the
  exact required compact form, retain the strict oracle, and run the complete
  provider-free preflight into a new immutable evidence root.

## 2026-08-14 - Provider-free preflight attempt 2 and baseline freeze

- **Objective:** Close tasks `1.1` and `1.2` without configured-model calls or
  loaded-instruction mutation.
- **Approach:** Corrected only the compliant one-off fixture, retained the exact
  oracle, exposed the runner through `proof:tooling-ergonomics`, inventoried it in
  `tools/proofs/README.md`, and reran all three scenarios into a new evidence root.
- **Evidence:** `evidence/preflight-baseline-r2/{identity.json,preflight.json,
  manifest.json}` records OpenCode `1.18.18`, profile `quality-independent`, route
  `openai/gpt-5.6-sol` / `xhigh`, prompt hashes, bounded tool policy, source hashes,
  schema versions, token inventory `16,600` / `100,853`, `modelCalls: 0`, all six
  rejected/accepted oracle outcomes, and cleanup `removed`. The frozen loaded
  `global/AGENTS.md` SHA-256 is
  `c2623211e3f432564bd3a3011fdbc7130948a5194656a12a11a79b6f42e24ddb`.
  The pre-change unrelated tracked diff identity retained from task entry is
  `7f8973b1ebb485ba95bc54108928585fb7e94abb`; preflight records the later full
  tracked working-diff identity separately and neither is a candidate identity.
- **Outcome:** Complete. Three of three oracle pairs discriminated correctly;
  installed loader preflight passed; no model call occurred; disposable state was
  removed.
- **Reason:** The exact compact fixture now matches both the scenario prompt and
  strict evaluator.
- **Do-Not-Repeat Condition:** Do not overwrite either preflight root or mutate the
  frozen baseline instruction sources before baseline capture.
- **Evidence-Based Retry Condition:** Rerun preflight only after a Proof Runner,
  evaluator, model profile, prompt, loader, or relevant environment mutation that
  invalidates its facts, always into a new root.

## 2026-08-14 - Baseline capture attempt 1

- **Objective:** Execute exactly one configured-model baseline call for each of the
  three frozen scenarios after green provider-free preflight.
- **Approach:** Started one `capture --scenarios all` invocation against the frozen
  source/model/prompt identities.
- **Evidence:** `evidence/baseline-capture-r1/{failure.json,manifest.json}` records
  the terminal error. No scenario command reached `spawnSync`, no event/session id
  was produced, and therefore no configured-model call occurred. The scenario
  `finally` path removed its disposable root.
- **Outcome:** Blocked before the first provider call.
- **Reason:** `runPortableCommand` built and validated a Windows shell command line
  before choosing its native-executable branch, so the frozen multiline prompt was
  rejected even though `opencode.exe` would be spawned directly without a shell.
- **Do-Not-Repeat Condition:** Do not overwrite `baseline-capture-r1`, flatten or
  weaken the frozen prompts merely to bypass the shared native-argv defect, or
  count this pre-spawn failure as a model attempt.
- **Evidence-Based Retry Condition:** Make shell quoting conditional on the shell
  fallback, prove multiline/metacharacter native argv provider-free through the
  shared helper, rerun the complete preflight into a new root, and only then retry
  baseline capture into a new immutable root.

## 2026-08-14 - Baseline capture attempt 2 and scope correction

- **Objective:** Capture the three frozen same-model scenarios once, evaluate them
  offline, and admit only reproduced instruction gaps.
- **Approach:** After `preflight-baseline-r3` proved the corrected native argument
  boundary, ran `baseline-capture-r2` through installed OpenCode `1.18.18` on
  `openai/gpt-5.6-sol` / `xhigh`, then evaluated and replayed its immutable bundles
  without further provider calls.
- **Evidence:** All three scenario commands exited `0`; each session deletion exited
  `0`; every disposable root was removed. `baseline-evaluate-r2` and
  `baseline-replay-r2` are terminal green after evaluator correction. Repeated CLI
  product behavior was exact while `--help` and `-h` both exited `1`. Mechanical
  output and regeneration were exact, but semantic variants remained embedded in
  `tools/materialize.mjs` and no reviewable seed existed. Source placement already
  passed an exact control: `scripts/generate-cases.js` remained outside `target/`,
  `npm run generate:cases` completed, generated cases were exact, and
  `config/one-off.json` remained manual.
- **Outcome:** Task `2.1` complete. CLI self-help and semantic-seed/materializer
  separation are reproduced gaps. Maintained-source placement and tiny-one-off
  proportionality are no-regression controls, not authorized new policy.
- **Reason:** The first evaluator encoded one preferred generator path and coupled
  mechanical output validation to seed existence. Preserved raw files/tool calls
  showed the actual source-placement behavior was compliant and the mechanical
  output itself was exact. The evaluator now distinguishes improvement gaps from
  controls and the live oracle accepts an exact package-owned generator path.
- **Do-Not-Repeat Condition:** Do not make another baseline model call, claim a
  placement gap, or add specialist/brief/reviewer placement policy from the target
  repository evidence alone.
- **Evidence-Based Retry Condition:** A new baseline call is allowed only if a
  protected paired identity changes or preserved raw evidence is proven
  insufficient for an accepted claimed improvement; evaluator-only corrections
  must continue to replay `baseline-capture-r2`.

## 2026-08-14 - Candidate capture attempt 1

- **Objective:** Prove both reproduced gaps and preserve the placement/one-off
  controls with exactly three candidate calls.
- **Approach:** Replaced/consolidated only the two canonical `global/AGENTS.md`
  clauses, held token proxies at the frozen `16,600` / `100,853`, passed
  `preflight-candidate-r1`, and captured all three frozen scenarios into
  `candidate-capture-r1`.
- **Evidence:** Repeated-use `--help` and `-h` both exited `0`, listed commands and
  arguments, caused no effect, and product scan stayed exact. Source placement and
  one-off controls passed. Mechanical output failed: the agent explicitly called
  the in-code rows a semantic registry, created no seed, and wrote root
  `artifacts.json` instead of named `data/artifacts.json`. `paired-evaluate-r1` and
  `paired-replay-r1` both terminate with `candidateComplete:false`. All three
  sessions and disposable roots were removed.
- **Outcome:** No MVP. One reachable accepted-outcome defect is confirmed in the
  mechanical scenario; no additional candidate call is yet authorized.
- **Reason:** The operative phrase `compact semantic seed` did not say that the
  seed remains reviewed data outside helper implementation, allowing the exact
  baseline anti-pattern to survive under a new name.
- **Do-Not-Repeat Condition:** Do not rerun the unchanged clause, alter prompts,
  overwrite candidate r1, or treat the green help/control rows as complete proof.
- **Evidence-Based Retry Condition:** Causally require compact reviewed seed data
  rather than helper-code rows, keep explicit named I/O and all controls, hold
  token proxies non-increasing, pass fresh zero-call preflight, and record a new
  candidate identity before one successor three-scenario attempt.

## 2026-08-14 - Candidate capture attempt 2 and evaluator replay

- **Objective:** Correct the mechanical behavior defect, regain proof without
  extra baseline calls, and freeze the complete production candidate.
- **Approach:** Clarified that reviewed seed data stays outside helper code, reduced
  token proxies below baseline, passed `preflight-candidate-r2`, and used the one
  authorized successor capture. When the first paired verdict stayed red, inspected
  the complete raw bundle before any live retry.
- **Evidence:** Candidate r2 created `data/artifacts.sources.json`, generated exact
  `data/artifacts.json`, and reran the generator stably. The red r2 evaluator had
  imposed unpromised names `data/seed.json` and `source`; raw evidence used the
  equivalent closed names `data/artifacts.sources.json` and `sourcePath`.
  Evaluator-only correction accepts exactly one semantic seed JSON plus the closed
  `source|sourcePath` schema and re-derives output from recorded input hashes/bytes.
  `preflight-candidate-r3`, `paired-evaluate-r3`, and `paired-replay-r3` are green
  with zero additional model calls. `permissions-r1` confirms installed-loader
  permissions are unchanged.
- **Outcome:** `candidate-reviewed-seed-r2` reached `MVP` and is frozen in
  `candidate-reference.md`; tasks `3.1`, `3.2`, and `4.1` are complete.
- **Reason:** Production wording fixed the actual in-code-seed ambiguity. The
  remaining failure was evaluator specificity, not product behavior, and immutable
  raw facts were sufficient for terminal replay.
- **Do-Not-Repeat Condition:** Do not make another configured-model call, mutate
  the frozen Product Candidate before SDET, or require a seed filename/field absent
  from the accepted contract.
- **Evidence-Based Retry Condition:** Another live attempt requires a newly
  reproduced critical/non-deferrable production defect, causal production fix,
  invalidated-lane preflight, and the canonical SDET continuation predicate.

## 2026-08-14 - Fresh critical-only SDET

- **Objective:** Independently challenge reachable critical failures in the frozen
  `candidate-reviewed-seed-r2` without production or proof mutation.
- **Approach:** Fresh test-only `sdet-quality-engineer` session
  `ses_0015246b6ffexEp7Izx59vHN7e` inspected the Candidate Reference, raw bundles,
  paired replay, current source hashes, and permission evidence. Its exact write
  scope was limited to one optional test file; it wrote none.
- **Evidence:** Terminal action `no-critical-risk`; Effective Model
  `xai/grok-4.6`; critical risk matrix `none`; test changes and execution request
  `none`. Main independently confirmed the frozen Product/Runner/process hashes,
  paired terminal verdict, and installed permission result.
- **Outcome:** Task `5.1` complete. This is the first precondition-valid no-confirmed-
  critical attempt, so SDET permanently stops for this root.
- **Reason:** No reachable critical or non-deferrable incident remained in the
  bounded local synthetic envelope.
- **Do-Not-Repeat Condition:** Do not launch another SDET attempt or turn parked
  non-critical observations into qualification work.
- **Evidence-Based Retry Condition:** None for this root under the terminal SDET
  rule. Parked limitations are helper-internal schema/readback enforcement,
  substring-level help completeness, unused hardcoded-row source inspection,
  inherited token-ceiling breach, unrepaired product CLIs, and model sensitivity.

## 2026-08-14 - Concurrent mutation, owner restoration, and validation correction

- **Objective:** Resume qualification without overwriting parallel owner work and
  satisfy the existing static instruction contract.
- **Approach:** Validation discovered that `global/AGENTS.md` changed after freeze
  from `3df1bdd1...` to unrecognized `b94fb8dd...`. The owner explicitly selected
  restoration of the frozen two-line candidate. `validate:strict` then exposed the
  candidate-caused missing marker `New instructions are a last resort`; the marker
  was restored with token-neutral consolidation and current-hash runtime capture
  was repeated.
- **Evidence:** Owner selection: `Restore frozen candidate`. Restored hash matched
  `3df1bdd1135a23dcffab978fb4a7ca75502747146ec894f31d54ae95ba67dc7f`.
  Static candidate `10d88015...` passed `validate:strict` and inventory
  `16,591` / `100,844`, but `candidate-capture-r3` failed the mechanical oracle:
  raw source again hardcoded semantic rows in `tools/materialize.mjs`, despite exact
  output, readback, stable regeneration, and green help/placement controls.
- **Outcome:** Current candidate remains `development`; prior MVP/SDET attribution
  is retained only for its inspected hash. No current-hash RC exists.
- **Reason:** The token-shortened structured-data sentence removed the proven
  `Before ... check` / `If present` decision sequence; the model explicitly treated
  helper-code rows as the reviewable source.
- **Do-Not-Repeat Condition:** Do not rerun hash `10d88015...`, use the stale
  unrecognized preflight r4, shorten the structured-data decision sequence, or
  perform wording-only retries after the final restored-clause attempt.
- **Evidence-Based Retry Condition:** Restore the exact r2 proven structured-data
  sentence, keep the required static marker in the adjacent behavior-workflow
  sentence, hold token proxies below baseline, pass fresh preflight, and allow one
  final current-hash capture. Any red terminal verdict blocks further live calls.

## 2026-08-14 - Final current-hash proof and validation blocker

- **Objective:** Prove the static-contract-complete Product Candidate and finish
  all applicable project validation without absorbing unrelated work.
- **Approach:** Kept the exact r2-proven structured-data decision sequence, restored
  `New instructions are a last resort`, and paid for it by consolidating only proof-
  tooling wording. Ran `preflight-candidate-r6`, `candidate-capture-r4`, paired
  evaluate/replay r5, current permission readback, strict validation, and full
  tests. After full tests exposed a process-safety regression, restored default
  fail-closed shell-metacharacter behavior and limited direct native arguments to
  the explicit Proof Runner option; provider-free r7 and paired replay r6 proved
  the capture boundary remained equivalent.
- **Evidence:** Current Product Candidate hash
  `4b5b79af042fd2688dbfca747373ad42a5dc0e131469a9570397fb219d5cc183`;
  runner `0b071aa8767171cf4bd43be105c0f553c983434ca1cafce3eca4b9851e21d51a`;
  process boundary `c2fa31b500d39dc5c49a964872bde1350f544472f3aa52856e11f9bc9cdea289`.
  `paired-evaluate-r6` and `paired-replay-r6` are terminal green. Inventory is
  `16,599` / `100,852`. `validate:strict`, focused 147 library tests, both OpenSpec
  validations, apply gate, permission proof, diff check, and scoped secret scan are
  green.
- **Outcome:** Current candidate is `MVP`, not RC. `npm test` remains red only in
  `tools/test-install-opencode-global.ts` from the concurrent installer change.
- **Reason:** The modified installer now requires
  `openspec-operation-gate.ts`, `roadmap-mission.ts`,
  `roadmap-mission/contracts.ts`, and `roadmap-mission/preflight.ts`, while its
  copied-test fixture still provisions only the pre-existing three portable files.
  Those production/test/files are unrecognized parallel work outside this change.
- **Do-Not-Repeat Condition:** Do not rerun full `npm test` until the installer
  fixture/source set changes, stage unrelated files, weaken the installer check, or
  fix another active change without owner authorization and writer coordination.
- **Evidence-Based Retry Condition:** The parallel installer owner completes and
  closes its writer attempt so copied fixtures include the required portable tools,
  or the owner explicitly authorizes coordinated takeover of that separate scope;
  then rerun `npm test` and the invalidated final validation/handoff lanes only.

## 2026-08-14 - Coordinated installer takeover readiness check

- **Objective:** Use the owner's coordinated-takeover authorization to close only
  the unrelated copied-installer fixture blocker.
- **Approach:** Read the owning
  `add-unattended-roadmap-orchestration/{history.md,tasks.md}`, installer required-
  file contract, copied fixture, and current portable source inventory before any
  mutation.
- **Evidence:** The exact fixture migration is open task `I1`, explicitly test-only,
  with prerequisite `accepted-scope Runtime Proof is complete; execute under the
  fresh test-only SDET lane`. Roadmap tasks `6.1` integrated configured-provider
  proof and `6.2` fresh SDET remain unchecked. Production currently requires seven
  portable paths while `writeFixturePortableTools` creates three.
- **Outcome:** No installer/test/roadmap file changed. Narrow takeover is blocked by
  Material test authorship and ordering, not by file ownership.
- **Reason:** Owner authorization can resolve overlapping-writer scope but cannot
  turn main into the pre-MVP automated-test author or waive the owning change's
  proof-before-SDET invariant.
- **Do-Not-Repeat Condition:** Do not edit the installer test as main, weaken the
  production required-file list, dispatch SDET before roadmap task `6.1`, or call
  the four new files optional.
- **Evidence-Based Retry Condition:** Either the roadmap owner completes task `6.1`
  and fresh SDET owns `I1`, or the owner explicitly expands this session to complete
  the full roadmap qualification dependency chain before that SDET test edit.

## 2026-08-14 - Roadmap dependency resolution and complete validation

- **Objective:** Resolve the separated installer/native test blocker without
  violating Material test ownership, then complete this candidate's current
  validation on exact source identities.
- **Approach:** With explicit owner scope, completed the roadmap change's integrated
  configured-provider proof, fresh SDET-owned I1/I3 migrations, full validation,
  handoff, and retrospective. Restored the owner-selected direct-native process
  contract, then replayed this change's current preflight/evaluator/permission lanes.
- **Evidence:** Roadmap `roadmap-provider-r4` is terminal complete; its fresh SDET
  returned `no-critical-risk`; full `npm test` is green. This change's current
  source hashes are Product `4b5b79af...`, process `6b4bd770...`, runner/evaluator
  `e6dd2cd9...`. `preflight-candidate-r8`, paired evaluate/replay r7, and
  `permissions-r3` are terminal green; inventory is `16,599` / `100,852`;
  strict repository/OpenSpec, apply gate, diff check, and secret review pass.
- **Outcome:** Task `6.1` is complete. Candidate is frozen as `RC1` for local
  handoff; only documentation synchronization and the one-time retrospective remain.
- **Reason:** The original red full test was correctly owned by the roadmap test-
  only lane, and current shared process semantics are now identical across both
  qualified changes.
- **Do-Not-Repeat Condition:** Do not reopen the resolved installer blocker, switch
  native argv semantics, rerun configured model captures, or launch another SDET
  without a Product Candidate mutation or reproduced critical defect.
- **Evidence-Based Retry Condition:** Only an exact invalidating source/environment
  mutation or current red validation result reopens the affected lane.

## 2026-08-14 - Final complete-history retrospective

Original User Goal: make later OpenCode sessions self-serve repeated-use repository
CLIs and mechanically derived structured artifacts without losing maintained source
or adding ceremony to one-off semantic data.

| | Working Repository | opencode-kit |
| --- | --- | --- |
| Quality | Live product evidence still contains unsupported documented help, a hand-maintained derived plan, and sole historical harness source under ignored output -> preserve three product-owned deferred migrations -> fixes existing consumers without claiming the portable contract retrofits them -> separate repository scope and fresh proof required. | Same-model baseline/candidate proved help and reviewed-seed decisions while placement/one-off remained controls -> no additional current-consumer policy -> avoids unsupported specialist/placement generalization -> none. |
| Cycle Speed | Agents lost cycles to help-as-error and manual derived-plan reconstruction -> deferred CLI/materializer migrations move discovery/regeneration to deterministic entrypoints -> faster first correct invocation/update -> product changes invalidate their own evidence. | Provider-free preflight, immutable bundles, exact evaluator replay, and create-new roots now retire runner/evaluator failures before another model call -> already implemented and proven -> no remaining improvement. |
| Token Economy | Large generated guard JSON is legitimate output; no measured current-consumer token defect -> none. | Candidate holds instruction proxies at `16,599` / `100,852`, below frozen unrelated baseline; absolute ceiling breach is inherited -> no safe current-change reduction without unrelated scope -> none. |

- **Retrospective Result:** No improvement has an exact remaining current-change
  consumer. No generated task is admitted.
- **Retrospective Execution:** Completed once. Do not rerun after this record.
- **Generated Tasks:** none.
- **Deferred Records:** `DIC-PMAC-CLI-HELP-001`,
  `DIC-PMAC-PLAN-MATERIALIZER-001`, and
  `DIC-PMAC-HARNESS-PLACEMENT-001`.

### Deferred Improvement Candidate DIC-PMAC-CLI-HELP-001

- **Impact Horizon:** Working Repository
- **Concrete Consumers:** `pmac-qualification.exe`,
  `pmac-qualification-stand.exe`, `g-s1-open-action-proof-runner.exe`,
  `mnc-resume.exe`, and `tools/proofs/README.md:619-621` in `pmac-emulator`.
- **Execution Class:** separate-change
- **Earliest Safe Point:** An authorized pmac-owned CLI change after this portable
  contract is current.
- **Invalidated Evidence:** Installed CLI help captures and matching proof-inventory
  interface claims.
- **Observable Payback:** One `--help` / `-h` probe exits `0`, prints complete usage,
  and creates no product/evidence effect.
- **Trigger/Evidence:** On pmac commit `0f4c056c...`, four installed help probes
  returned unknown/unsupported/positional usage failures while the inventory
  recommended help.
- **Why:** The documented discovery path is not executable; this kit change cannot
  repair another repository's binaries.
- **Prerequisites:** Authorized pmac change and current installed-entrypoint build;
  no controller/live action.
- **Scope/Non-Goals:** Parse help before validation and synchronize inventory only;
  no CLI framework or unrelated command behavior change.
- **Implementation:** Add effect-free exit-zero `--help` / `-h` to the four current
  owners and document only the proven modes/arguments.
- **Observable Proof:** Both help forms pass on all four installed binaries and
  ordinary product outputs remain byte/semantically unchanged.
- **Validation:** Focused installed CLI invocations plus inventory/source readback.
- **Why Not Admitted:** `pmac-emulator` mutation is an explicit non-goal and no
  current kit task consumes it.
- **Re-Evaluation Condition:** A later authorized pmac change owns those binaries.

### Deferred Improvement Candidate DIC-PMAC-PLAN-MATERIALIZER-001

- **Impact Horizon:** Working Repository
- **Concrete Consumers:** `tools/proofs/minimum-board-envelope/r1-plan.json`,
  `run.ps1`, and `source-inventory.json` in `pmac-emulator`.
- **Execution Class:** separate-change
- **Earliest Safe Point:** A pmac proof-tooling change after semantic seed ownership
  and closed schema are named.
- **Invalidated Evidence:** The authoritative plan and every guard/source identity
  derived from its order, lengths, and hashes.
- **Observable Payback:** Regeneration deterministically matches frozen identities;
  reviewed seed data contains no derived hash/length fields.
- **Trigger/Evidence:** The 698-line, 30,783-byte plan carries derived identities and
  consumers but no materializer was found.
- **Why:** Mechanical fields were model-authored despite first-artifact derivation
  triggers.
- **Prerequisites:** Closed schema, reviewed semantic seed, and local deterministic
  helper boundary.
- **Scope/Non-Goals:** Materialize/validate derived fields only; never infer policy,
  classification, or prohibit generated evidence.
- **Implementation:** Add the smallest seed/materializer/validator with stable order,
  schema/readback, and drift check.
- **Observable Proof:** Two regenerations are identical and match named input facts.
- **Validation:** Deterministic rerun, schema/readback, and current consumer proof.
- **Why Not Admitted:** The artifact belongs to another repository and this change
  proves only the portable rule.
- **Re-Evaluation Condition:** An authorized pmac envelope/tooling change owns the
  plan or its inputs.

### Deferred Improvement Candidate DIC-PMAC-HARNESS-PLACEMENT-001

- **Impact Horizon:** Working Repository
- **Concrete Consumers:** `target/d2b-sdet-r12-challenge.py` and
  `target/g-s1-restore-failure-runner/src/main.rs` in `pmac-emulator`.
- **Execution Class:** separate-change
- **Earliest Safe Point:** An authorized pmac test/proof placement change.
- **Invalidated Evidence:** Any capture whose sole reusable challenge/restore logic
  is moved or declared disposable.
- **Observable Payback:** Later sessions find the only reusable source in maintained
  scope, or an explicit disposal decision proves it is not required.
- **Trigger/Evidence:** `git check-ignore` confirms `target/` exclusion; no maintained
  duplicate of the 483-line Python generator was found.
- **Why:** The only source of reusable reproducer knowledge is currently disposable.
- **Prerequisites:** Owner classification as maintained test/proof source or truly
  one-off scratch.
- **Scope/Non-Goals:** Move or deliberately dispose the sole source; do not widen
  SDET production authority or retain generated outputs as source.
- **Implementation:** Place reusable logic under the project-native test/proof tree,
  or record and execute a safe one-off disposal decision.
- **Observable Proof:** The only reusable source is no longer ignored, or a reviewed
  disposal record proves no accepted evidence depends on it.
- **Validation:** Path inventory, ignore check, and affected proof replay.
- **Why Not Admitted:** Another-repository mutation and explicit non-goal; current
  candidate already preserves maintained placement in its synthetic control.
- **Re-Evaluation Condition:** An authorized pmac test/proof change owns these
  sources or current evidence depends on them again.
