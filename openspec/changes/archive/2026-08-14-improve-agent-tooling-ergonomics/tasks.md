## Fidelity Ladder

`observed installed-CLI/artifact evidence -> current source/spec/instruction
inventory -> provider-free synthetic fixture and structural preflight -> same-model
baseline capture on frozen current instructions -> smallest canonical instruction
and role-delta mutation -> same-model candidate capture with identical prompts and
environment -> offline exact-fact evaluator/replay plus token/role validation ->
isolated installed global-loader readback -> future target-project migrations`.

Profile: `Material` because the candidate changes loaded instruction behavior and
specialist safety-adjacent policy. Main owns production instruction, contract,
validator, and Proof Runner mutation. Production work creates no automated test
artifacts; after current runtime proof and accepted-scope production completion,
one fresh test-only SDET owns any automated test/fixture changes. No task authorizes
installation, activation, provider configuration, credentials, target-project
mutation, remote state, publication, or release.

## 1. Freeze Current Authority And Build Provider-Free Preflight

- [x] 1.1 Freeze the current working-tree identities for `global/AGENTS.md`, `global/agents/implementation-worker.md`, `global/agents/sdet-quality-engineer.md`, instruction-artifact tuning/review, relevant contracts/validators, README/spec surfaces, installed OpenCode/model profile, existing unrelated diffs, normative instruction token proxies, exact three scenario prompts, and non-existing baseline/candidate evidence roots. **Current Rung:** observed external evidence plus current source/spec inspection. **Next Real Boundary:** provider-free execution of the scenario fixture and exact evaluator. **Blocker/Unblocker:** none; unreadable or changing unrelated instruction content blocks mutation until it is frozen without modification. **Observable Proof:** one privacy-safe identity record distinguishes Product Candidate, Proof Runner, Evaluator, Environment, baseline source, candidate source, unrelated diff, prompts, and cleanup contract, and current token proxies are known. **Owner Authorization:** local read/hash/inventory only. **Safeguards:** do not copy private prompts, credentials, personal paths, or unrelated file content into shared evidence; do not reset, stage, or modify unrelated work. **Restoration/Cleanup:** no mutation or external state; disposable identity scratch is removed and the bounded evidence record is retained. **Expected Immutable Evidence:** exact command/exits/stdout/stderr, source hashes, redacted diff identity, model/profile identity, scenario IDs, token inventory, and evidence-root plan. **Dependency-Chain Stop Condition:** stop all instruction mutation if baseline and candidate cannot be compared against the same frozen unrelated working-tree state.
- [x] 1.2 Add `tools/proofs/agent-tooling-ergonomics.ts` as a thin scenario-specific adapter over the existing OpenCode proof client and baseline/candidate/evaluator conventions, with `preflight`, `capture`, `evaluate`, `replay`, and deterministic cleanup; add the exact inventory and package invocation without implementing any instruction change. Preflight SHALL build disposable generic repositories for repeated-use CLI, hash/length/order variant materialization, maintained harness placement, and tiny-one-off proportionality; validate allowed tools/effects, exact prompts, output schemas, command execution oracles, redaction, and create-new roots; and run zero model calls. **Current Rung:** frozen source/environment identity. **Next Real Boundary:** installed OpenCode same-model baseline capture on current instructions. **Blocker/Unblocker:** task 1.1; reuse `tools/proofs/lib/opencode-proof-client.ts` and existing capture/evaluator utilities where contracts fit, with no dependency or generic framework. **Observable Proof:** provider-free preflight exits `0`, proves every fixture/oracle can reach a terminal exact-fact verdict, rejects existing/aliased roots and malformed facts before session creation, and leaves no process/session/fixture owner. **Owner Authorization:** local disposable TypeScript/project/session-fixture work only; no provider call in this task. **Safeguards:** explicit three-scenario inventory, no fuzzy scoring, no target repository, no external directory, no remote/destructive/install tool, no secrets, bounded bytes/time, create-new output, and `finally` cleanup. **Restoration/Cleanup:** delete all disposable config/data/cache/state/project/session fixtures; retain only the immutable preflight bundle and maintained runner source. **Expected Immutable Evidence:** runner/evaluator hashes, exact argv, exit/stdout/stderr, scenario manifest, redacted fixture identities, rejection facts, and cleanup result. **Dependency-Chain Stop Condition:** do not consume configured-model calls until every provider-free preflight and cleanup oracle is terminal green.

## 2. Capture The Current Instruction Baseline

- [x] 2.1 Run the provider-free preflight, then execute exactly one configured-model baseline capture per scenario against an isolated copy of the task-1.1 current global source through the actual installed OpenCode entry point; preserve tool calls, produced file tree, source, command exits/stdout/stderr, output/effect facts, session/route/model identity, and cleanup. Evaluate and replay the immutable baseline offline. **Current Rung:** terminal provider-free fixture/evaluator preflight. **Next Real Boundary:** same-model candidate capture after the smallest loaded instruction mutation. **Blocker/Unblocker:** task 1.2 and standing bounded non-sensitive configured-provider authorization; any route/model/source/prompt/tool drift blocks comparison and requires a fresh paired baseline rather than normalization. **Observable Proof:** claimed improvements are bound to reproduced missing/failed repeated-use help and model-authored semantic variants in helper code; maintained source placement and tiny-one-off proportionality remain controls. A claim not reproduced is removed or retained only as a control before instruction mutation. **Owner Authorization:** exactly three configured-provider calls in disposable local repositories under standing kit authorization. **Safeguards:** same model/profile and prompts reserved for candidate, local non-sensitive inputs, denied remote/external/credential/install/destructive tools, no target repo, per-session deadline, no retries with wording-only changes, privacy-safe evidence, and terminal session deletion. **Restoration/Cleanup:** delete every baseline session and disposable root after immutable fact capture; no working-repository mutation. **Expected Immutable Evidence:** exact invocation, Effective Model/profile, baseline source identity, scenario raw facts, evaluator/replay verdict, exits/stdout/stderr, tool/file manifest, and cleanup. **Dependency-Chain Stop Condition:** do not author a rule whose claimed decision gap is absent or unobservable in the frozen baseline.

## 3. Implement And Prove The Loaded Happy Path

- [x] 3.1 Replace/consolidate the existing `global/AGENTS.md` reusable-tooling, deterministic-helper, and Universal Task Briefing wording with the narrow repeated-use CLI help/caller fallback, mechanical structured-artifact materialization; update only the proof inventory and directly required OpenSpec surfaces. Preserve all permissions, production/test ownership, protected-action gates, unrelated working-tree edits, project neutrality, explicit helper I/O/failure rules, and both instruction token ceilings. Do not create automated tests or fixtures. **Current Rung:** baseline-reproduced instruction decision gaps. **Next Real Boundary:** identical same-model candidate scenarios through the installed OpenCode loader. **Blocker/Unblocker:** task 2.1; each operative clause must map to a reproduced baseline row and fit by replacing/consolidating superseded wording. **Observable Proof:** provider-free loader/source readback sees exactly one canonical global owner and no new specialist policy copy; deterministic validation sees unchanged permissions/role boundaries, token proxies no higher than the frozen `16,600` and `100,853`, and the inherited normative breach; no unrelated diff byte changes. **Owner Authorization:** local instruction/TypeScript/README/OpenSpec production mutation only. **Safeguards:** no new policy chapter, dependency, CLI framework, line threshold, blanket JSON generator, third-party retrofit, SDET authority widening, installation, or activation. **Restoration/Cleanup:** source rollback is path-scoped to this change while raw baseline remains immutable; preserve unrelated owner edits. **Expected Immutable Evidence:** scoped diff, source/contract identities, token inventory, provider-free loader output, permissions/role comparison, diagnostics, and proof-runner identity. **Dependency-Chain Stop Condition:** stop candidate capture if structural readback cannot distinguish canonical policy from duplicated or permission-widening text.
- [x] 3.2 Run provider-free preflight against the changed candidate, then execute one configured-model candidate capture per scenario with the task-2.1 model/profile, prompts, tools, and synthetic environment; one successor is permitted only after a reproduced candidate defect, causal production correction, terminal preserved-bundle replay, and fresh preflight; evaluate/replay baseline and candidate without model calls. Inspect actual files and invoke generated CLI help/materializer paths until the current instruction candidate reaches terminal exact-fact verdict or an evidenced blocker. **Current Rung:** loaded candidate source plus structural preflight. **Next Real Boundary:** isolated installed global-loader behavior and accepted-scope specialist challenge. **Blocker/Unblocker:** task 3.1; source/prompt/model/environment drift requires a fresh paired baseline, while evaluator-only corrections replay preserved bundles first. **Observable Proof:** repeated-use `--help` and `-h` exit `0`, enumerate modes/arguments, and produce no declared effect; mechanical variants regenerate deterministically from compact reviewed seed data outside helper code with validated hash/length/order facts; the sole reusable harness source remains outside ignored output; tiny one-off semantic data remains manual; safety, role, failure, and cleanup oracles are unchanged. Completion establishes `Development-Stage: MVP` for this bounded loaded-instruction behavior. **Owner Authorization:** three configured-provider calls per candidate attempt in disposable local repositories under standing kit authorization; a successor requires the preceding evidenced defect and correction gates. **Safeguards:** no target project, provider/config mutation, credentials output, remote/destructive/install action, fuzzy evaluator, unbounded retry, or stronger generalization than observed scenarios. **Restoration/Cleanup:** delete candidate sessions and disposable roots after immutable capture; retain paired raw/evaluation/replay bundles and leave the working candidate readable. **Expected Immutable Evidence:** candidate/model/source/prompt identities, exact invocations, tool/file manifests, CLI/materializer exits/stdout/stderr, paired evaluator/replay results, token facts, and terminal cleanup. **Dependency-Chain Stop Condition:** a red happy path returns the candidate to `development`; do not expand test/review work until all accepted primary scenarios are current and green.

## 4. Complete Production Scope And Freeze The Candidate

- [x] 4.1 Complete any remaining production-only contract, validator, inventory, README, and spec synchronization required by the candidate proof; run the exact isolated installed global-source loader and permission readback, rerun affected provider-free and paired evaluator lanes, and freeze one readable Candidate Reference with Product Candidate, Proof Runner, Evaluator, Environment, baseline/candidate bundles, token inventory, and unrelated-diff identity. Do not add automated tests or alter the scenario semantics after the paired capture without recapturing both sides. **Current Rung:** same-model instruction MVP. **Next Real Boundary:** fresh critical-only test challenge of the complete Material candidate. **Blocker/Unblocker:** task 3.2; all accepted production scope must be complete and every mutation-dependent proof lane current. **Observable Proof:** the installed isolated loader selects the changed global/agent/skill sources, permissions and authorship remain exact, structural markers and token ceilings are green, paired behavior replay is terminal, and no installed/user/target state changed. **Owner Authorization:** local provider-free loader, source, and evidence work only. **Safeguards:** no global installation/activation, no target repository, no hidden instruction copy, no model call in evaluator/readback, no docs claim beyond paired scenarios. **Restoration/Cleanup:** isolated config/data/cache/state roots and loader processes are removed; immutable evidence and readable Candidate Reference remain. **Expected Immutable Evidence:** installed loader/version/source output, permission matrix, final scoped diff, source/token hashes, paired bundle/replay IDs, cleanup, and Candidate Reference. **Dependency-Chain Stop Condition:** do not dispatch SDET while accepted production artifacts or affected runtime proof are incomplete or mutable.

## 5. Challenge Critical Instruction Risk

- [x] 5.1 After task 4.1, dispatch one fresh test-only SDET against the exact Candidate Reference, original requirements, paired raw bundles, help effect/exit contract, semantic-seed boundary, ignored-source rule, tiny-one-off proportionality, Universal Brief propagation, unchanged permissions/authorship, token ceilings, and safe disposable runner. Supply exact project-native automated-test write scope under existing `tools/test-*.ts`/test-helper conventions and forbid production, proof-runner, instruction, contract, config, and OpenSpec mutation. Main independently reproduces every critical row, fixes only a confirmed reachable critical/non-deferrable production defect, and repeats invalidated proof before any rule-valid fresh successor. **Current Rung:** complete current Material candidate with paired runtime proof. **Next Real Boundary:** complete local project validation and handoff. **Blocker/Unblocker:** task 4.1; unavailable/unusable SDET leaves the candidate at MVP, while the first precondition-valid result without a main-confirmed critical defect permanently terminates SDET for this root. **Observable Proof:** terminal `no-critical-risk`, or a confirmed-critical production correction history followed by current paired proof and one permitted fresh successor result; any authored tests remain the smallest critical oracles and outside production paths. **Owner Authorization:** fresh local test-only specialist and local disposable execution only. **Safeguards:** SDET does not edit production/instructions/proof/OpenSpec, ask the user, use remote/credentials, widen scope, or claim lifecycle state; non-critical findings are parked. **Restoration/Cleanup:** disposable test/runtime state is removed; baseline/candidate raw bundles remain immutable. **Expected Immutable Evidence:** SDET identity/Effective Model, exact Candidate Reference, risk matrix, test-only diff or none, main reproduction/disposition, affected proof replay, and terminal action. **Dependency-Chain Stop Condition:** do not continue SDET after the terminal no-confirmed-critical condition; do not freeze RC with an unresolved plausible non-deferrable authority/data/effect claim.

## 6. Validate And Hand Off

- [x] 6.1 Run the current paired behavior evaluator/replay and installed-loader/permission readback, focused changed contract tests, `npm run instruction:inventory`, `npm run validate:strict`, `npm test`, `node global/bin/openspec-operation-gate.ts --root . --operation apply --change improve-agent-tooling-ergonomics`, `openspec validate improve-agent-tooling-ergonomics --strict`, `openspec validate --all --strict`, `git diff --check`, and a scoped source/diff/secret review on the exact Candidate Reference. Correct any applicable red check and replay only invalidated lanes. **Current Rung:** terminal critical SDET and current paired proof. **Next Real Boundary:** local candidate handoff. **Blocker/Unblocker:** task 5.1 terminal plus current proof; unresolved existing validation failures must be separated from candidate regressions without weakening mandatory policy. **Observable Proof:** every applicable command exits `0`, exact stdout/stderr and candidate identity are retained, token/permission/role/help/generator/placement oracles agree, and unrelated work remains untouched. **Owner Authorization:** local validation and provider-free replay only. **Safeguards:** no commit, push, install, activation, provider call, target mutation, archive, release, or remote action. **Restoration/Cleanup:** remove disposable validation/loader roots and processes; preserve immutable proof and test evidence. **Expected Immutable Evidence:** exact argv/exits/stdout/stderr, validation report, current diff/source/token identities, OpenSpec status/apply readback, and cleanup. **Dependency-Chain Stop Condition:** any current candidate regression or known critical/non-deferrable defect blocks RC until corrected and re-proven.
- [x] 6.2 Synchronize the authoritative `library-instruction-artifacts` spec, README/proof inventory, change `history.md`, and local handoff with the exact outcome, Candidate/Runner/Evaluator/Environment/bundle identities, observed baseline-to-candidate behavior, token cost, permissions/authorship result, limitations, rollback, external-operations statement, and installed-loader note; re-read every changed instruction and planning artifact from a cold-session perspective. **Current Rung:** green complete validation. **Next Real Boundary:** separately requested archive; target-project CLI/generator migrations remain separate changes. **Blocker/Unblocker:** task 6.1; no archive, install, or product migration is implied. **Observable Proof:** docs/spec/history/handoff agree with immutable evidence, no claim exceeds the three scenarios, no product repo is modified, and the same complete candidate can be designated the next monotonic RC and locally handed off as stable under the canonical lifecycle. **Owner Authorization:** local documentation/spec/handoff only. **Safeguards:** no retrofit claim, publication, commit, push, archive, installation, activation, or remote action. **Restoration/Cleanup:** documentation rollback only; retain proof bundles and unrelated work. **Expected Immutable Evidence:** final readback, scoped diff, status/apply output, Candidate Reference, lifecycle handoff, and residual-risk list. **Dependency-Chain Stop Condition:** do not claim stable or implementation completion if docs, proof, validation, or current candidate identity diverge.

## 7. Final History Retrospective

- [x] 7.1 After every other currently known task is complete, perform the one-time final history retrospective over the complete `history.md`. Apply the canonical six-cell `Quality`, `Cycle Speed`, and `Token Economy` by `Working Repository` and `opencode-kit` evidence matrix; classify every evidence-backed candidate with `Impact Horizon`, `Concrete Consumers`, `Execution Class`, `Earliest Safe Point`, `Invalidated Evidence`, and `Observable Payback`; append and immediately execute every admitted current-consumer improvement under `## Session-Derived Improvements`, preserve no-current-consumer evidence as non-checkbox deferred history records, or record `none` without inventing work. Apply, archive, compaction, and generated tasks MUST NOT create or rerun this retrospective or add another retrospective task. **Current Rung:** complete change history and handoff. **Next Real Boundary:** archive readiness. **Blocker/Unblocker:** tasks 1.1-6.2 and every generated admitted improvement complete. **Observable Proof:** one final retrospective record, canonical classification/persistence for every evidence-backed candidate, and no unresolved admitted improvement. **Owner Authorization:** local evidence/process work only. **Safeguards:** no scope expansion or protected action; deferred candidates do not block completion. **Restoration/Cleanup:** documentation rollback only. **Expected Immutable Evidence:** final `history.md`, task readback, and validation/proof replay affected by any admitted improvement.
