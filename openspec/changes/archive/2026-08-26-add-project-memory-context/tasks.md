## 1. Smallest Complete Happy Path

- [x] 1.1 Recheck active changes, `git status`, graph blast radius, and exact ownership for `global/plugin/session-env.ts`, shared redaction, package scripts, and planned tests; refresh `ownership.json` and the planning `PMC-001` row in `evidence-index.json`, record any overlap in `history.md`, and do not begin a conflicting mutation until the writer is closed or ownership is transferred. Verify the selected write set is non-overlapping and preserves every unrelated worktree change.
- [x] 1.2 Implement the minimal self-contained fixed-slot store, strict Markdown candidate/promotion events, exact platform data root, canonical worktree identity, startup enablement guard, project-root plus existing redaction, active-card fold, specified lexical formula, and total 8-KiB rendering under `global/plugin/project-memory/`. Current rung: no Product Candidate. Next boundary: invoke the production module against a disposable Git project and isolated `OPENCODE_DATA_DIR`; verify disabled mode writes nothing and one explicit candidate/promote/query returns one relevant redacted result without worktree or external-network effects.
- [x] 1.3 Compose `project_memory_recall`, `project_memory_manage`, bounded `chat.message` root lookup, and system-context hooks into the existing `session-env` owner without replacing its shell, Graphify, or session-delivery behavior or requiring changes to foreign-owned session-plugin tests. Current rung: direct module. Run copied-plugin support checks, then reach the real boundary by launching pinned OpenCode with the materialized plugin, disposable Git/data/config roots, and a deterministic loopback fake provider; record/promote one procedure, drive one root prompt, and observe actual message-before-transform order plus one advisory capsule in the captured provider request, bounded metadata, clean `git status --porcelain`, zero external egress, and preserved stdout/stderr/evidence paths.

## 2. Complete Accepted Behavior

- [x] 2.1 Complete append-only promote/invalidate folding, fixed-slot exclusive event creation, terminal invalidation, duplicate-action idempotence, unsupported transition errors, and capacity rejection. Verify concurrent provider-free processes never overwrite or exceed the final card/lifecycle slot, partial crash files remain bounded and quarantined, and every fold permutation makes invalidation terminal.
- [x] 2.2 Complete schema, field, 16-KiB candidate, 4-KiB lifecycle event, 2,000-card/10,000-event corpus, safe-selector, 180-day freshness, repository evidence hash, missing evidence, malformed-card quarantine, read-only over-limit handling, and incomplete-population guards. Verify each exclusion is visible through a privacy-safe explicit recall result and no excluded card reaches automatic injection.
- [x] 2.3 Add bounded read-only `.serena/memories/` discovery with regular-file/containment checks, 100-file/512-KiB limits, 2-KiB `core.md` context, current-file lexical ranking, independent source failure, and no MCP dependency. Verify absent, valid, over-limit, symlink, path-escape, and redacted-curated fixtures without changing any Serena file.
- [x] 2.4 Complete one-second verified-root selection, subagent/unknown/mismatched-root exclusion, required hook-order fail-closed behavior, stable system capsule composition, pre-transform and pre-compaction cross-process revalidation, cache invalidation after manage actions, session cleanup, and plugin disposal. Verify root, child, missing session id, timeout, reversed order, second-process invalidation, repeated transform, post-manage prompt, compaction, deletion, and disposal cases through the real hook objects with no transcript read or replacement compaction prompt.
- [x] 2.5 Complete cause-preserving bounded diagnostics, once-per-safe-ref warning behavior, 16-KiB recall/4-KiB manage output limits, and privacy handling for unreadable stores, unsupported versions, absolute/UNC/traversal selectors, canonical project-root variants, recognized credentials, home paths, session/project refs, local/curated partial failure, and over-budget rendering. Verify raw sensitive values and absolute project paths are absent from files, tool output, capsules, metadata, stdout, and stderr while unrelated plugin hooks remain usable.

## 3. Deterministic Proof Automation And Documentation

- [x] 3.1 [automation-dividend] Materialize the reviewed `PMC-001` synthetic corpus as stable seed data outside helper code and implement one provider-free runner that drives the production store, recall, renderer, tools, and hooks for the exact scoring/admission formula, hit, weak miss, selector boosts, all lifecycle states, stale/mismatched evidence, malformed data, project-root/secret redaction, all budgets, root separation, hook order/timeout, cross-process revalidation, compaction, disabled mode, concurrency, and no-side-effect cases. Verify schema/readback, stable ordering, regeneration drift, and expected outcomes without semantic inference in helper code.
- [x] 3.2 Exercise the direct-scan implementation at the 2,000-card/10,000-event operating boundary with maximum-sized representative event classes, preserve elapsed/resource observations and deterministic output hashes, and confirm enumeration and rendering remain bounded. If the direct scan cannot reach the next real boundary reliably, update design/history with the measured bottleneck before adding a derived index; do not add embeddings or a database.
- [x] 3.3 Expose effect-free `--help`, explicit fixture/data/project inputs, effect and cleanup descriptions, privacy-safe evidence output, and a package-script proof inventory entry for the maintained runner. Add focused project-memory tests to the project-native test inventory and verify help performs no writes or external-network access.
- [x] 3.4 Add project-memory operator documentation covering enablement, restart requirement, two tools, candidate/promotion/invalidation flow, local data location, Serena's separate curated role, precedence/staleness behavior, privacy limits, disable/rollback, and manual local-data cleanup. Verify every documented command and environment name against the implemented loaded surface.

## 4. Re-Proof And Validation

- [x] 4.1 Re-run the complete pinned-OpenCode loaded-entry-point boundary after all production mutations using a deterministic loopback fake provider, disposable Git project, and isolated data/config roots. Verify `PMC-001` across explicit tool lifecycle, exact scoring/admission, automatic relevant/irrelevant recall, root/subagent/hook-order/timeout, second-process invalidation before transform/compaction, current/stale/fingerprint state, curated Serena, malformed/over-limit failure, project-root/secret redaction, captured provider context, external-egress canary, worktree status, cleanup, and exact candidate/environment identity; preserve the raw evidence bundle before cleanup.
- [x] 4.2 After current accepted-scope proof, dispatch one fresh test-only critical SDET challenge for supported credential/project-path leakage, cross-project store collision, root/subagent isolation, and stale/invalidated context reaching the captured model request. Main SHALL independently reproduce and disposition every row, apply only confirmed in-scope corrections, and re-run affected proof before recording terminal `critical-risks-reported`, `no-critical-risk`, or `blocked` evidence.
- [x] 4.3 Freeze the corrected readable candidate, update all `PMC-001` observations and evidence lanes, and obtain one fresh read-only evidence-sufficiency challenge for the declared partitioned population. Verify every member has current candidate/environment/path/boundary evidence or narrow/block the maximum claim explicitly; do not mutate production or tests during the challenge.
- [x] 4.4 Run focused project-memory and session plugin tests, the maintained `proof:project-memory` command, `npm test`, `npm run validate:strict`, and strict validation of `add-project-memory-context`; inspect exact exit status/stdout/stderr and correct every in-scope failure without modifying unrelated dirty files.
- [x] 4.5 Run the configured runtime-source/profile and copied-plugin self-containment checks that cover `global/plugin/`, then inspect the final diff and graph blast radius for accidental plugin/config/installer coupling. Verify no new dependency, plugin config entry, MCP, hosted call, worktree storage, Serena mutation, transcript capture, or mandatory improvement task entered the candidate.
