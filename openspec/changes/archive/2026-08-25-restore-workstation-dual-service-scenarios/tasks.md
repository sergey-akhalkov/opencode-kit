## 1. Restore Dual-Service Scenarios

- [x] 1.1 Complete the three full `local-opencode-workstation` MODIFIED requirement blocks, verify every affected scenario matches its parent OpenCode-and-Graphify boundary, run `npx openspec validate restore-workstation-dual-service-scenarios --strict` and `npm test`, and retain no production or installed-runtime effect. **Current Rung:** merge-ready exact spec correction. **Next Real Boundary:** deterministic archive. **Blocker/Unblocker:** none. **Observable Proof:** all three complete blocks preserve the parent SHALL clauses; affected scenarios consistently require both services, either listener, the current steady tray state, and neither credential; strict delta validation and full `npm test` exit `0`. **Authorization/Safeguards:** repository planning/spec files only. **Restoration/Cleanup:** none. **Immutable Evidence:** current delta, checked evidence row, and command exits/stdout/stderr. **Validation:** named commands.
