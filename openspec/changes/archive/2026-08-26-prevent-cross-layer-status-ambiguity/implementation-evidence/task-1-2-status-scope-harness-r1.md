# Task 1.2 Status-Scope Harness

- Recorded at: `2026-08-26T03:24:57.8914752+03:00`.
- Outcome: the existing consumer-outcome runner now owns a finite `status-scope` lane. It records exact structured status dimensions for the main response and reconstruction, invokes the installed SDK `session.summarize` API through the shared proof client, retains privacy-redacted assistant/compaction evidence, and evaluates preserved bundles without a provider call.
- Call envelope: capture requires `--session-mode configured` and an explicit installed `--opencode`; each arm permits exactly one main prompt, one configured summarize request, and one reconstruction prompt. The exact evaluator rejects any per-arm count other than three and any matched total above six.
- Exact oracle: all three reviewed members and seven fields are compared without prose scoring. Provider-free tests reject resource-status broadening for `known-resource-path-unknown`, an accepted-outcome conflict after reconstruction for `compaction-roundtrip-mixed-status`, malformed/absent readback, and request-bound violations with field- and phase-attributed diagnostics.
- Compaction boundary: the fake SDK client exercised `session.create -> session.prompt -> session.summarize(auto=false) -> session.messages -> session.prompt -> session.messages -> session.delete`; the retained summary message and both response values were observed, and cleanup completed.
- Terminal replay: synthetic immutable baseline and candidate bundles replayed through the CLI twice with `liveCalls=0`, equal evaluation digests, and `passed-no-regression`. Baseline-only replay returned `baseline-established`; this caught and corrected the prior missing-candidate routing defect before live capture.
- Effect-free help: the maintained `--help` and `-h` test confirms no file or project-listing mutation and now documents the status-scope capture effects and six-call bound.
- Startup diagnostics: the first task 2.1 evidence capture exposed a proof-runner gap before any provider call: an exiting server handle and its captured streams were discarded. The shared client now retains the exact terminal child and streams on startup failure, the capture lane redacts and records them, and actual fixture/process/session cleanup values replace optimistic contract defaults. A provider-free exiting-child test proves terminal exit `1`, retained stderr, and fixture removal.
- Runtime prerequisites: preserved provider-free startup bundles additionally proved the database parent must be created, installed ripgrep must be first on PATH, and agent discovery must use bounded route readiness. Active route identity now comes from the selected `opencode.json`, not a model-profile assumption; provider-free tests cover delayed SDK v2 agent discovery and download-marker classification.
- Validation: `npm run test:focused:consumer-outcome` -> `OK: consumer outcome tests=24`; `node --check` passed for contracts, evaluator, status capture, runner, proof client, and focused tests; `openspec validate prevent-cross-layer-status-ambiguity --strict` passed.
- Type probe: a disposable `typescript@5.9.2` plus `@types/node@24.3.0` compiler run identified and led to correction of an `Array.map` callback/default-constraint defect and a duplicate test-helper property. The repository-wide import closure still reports pre-existing typing errors in OpenSpec evidence/ownership helpers, portable process helpers, and runtime-source code; this repository has no maintained TypeScript compiler dependency or clean project `tsconfig`, so native Node parsing plus project tests remain the applicable green gate.
- Code-quality review: `code-quality-reviewer` task `ses_fc49453a4fferyubsuXUAdc5Kf`, effective model `xai/grok-4.6`, inspected this working-tree slice read-only. Main applied all three high-confidence reductions: reused the contract-owned status key list, collapsed duplicate focused-pack guards, and narrowed the capture options type. Main retained every distinct test oracle. Split-or-justify: the large contracts and test files remain the established cohesive schema/test owners; extracting this finite pack would violate the explicit no-standalone-runner decision and add navigation rather than locality.
- Cleanup: synthetic test roots were deleted in `finally`; fake proof sessions were deleted; no OpenCode proof server, configured provider, live baseline, instruction mutation, remote operation, or foreign source mutation occurred.
- Claim ceiling: provider-free harness and evaluator behavior only. CSA-001 remains unknown with no configured baseline/candidate observation and no independent challenge.
