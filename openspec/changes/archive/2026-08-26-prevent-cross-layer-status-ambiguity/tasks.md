## 1. Freeze The Baseline And Extend Existing Proof Ownership

- [x] 1.1 Resolve the active kit global source, committed source/mirror ownership, current OpenCode/model/compaction identities, and overlapping active-change write ownership; freeze the baseline source and reviewed three-member CSA-001 scenario pack before mutation, and verify a provider-free preflight reports exact digests, a six-call total configured bound, no foreign source mutation, and no unresolved overlapping writer. **Current Rung:** direct source/spec/history inspection. **Next Real Boundary:** provider-free preflight against the actual installed config and proposed fixture. **Blocker/Unblocker:** any ambiguous active source, mirror owner, or overlapping writer blocks instruction mutation until exact source resolution or ownership transfer. **Observable Proof:** preflight JSON names one production source, baseline ref, candidate source plan, model routes, scenario members, permissions, call/byte bounds, and cleanup contract. **Validation:** effect-free preflight plus focused source-inventory and evidence-schema checks.
- [x] 1.2 [automation-dividend] Extend the existing consumer-outcome runner and shared proof client with the finite `status-scope` pack, exact structured status fields, actual SDK `session.summarize` compaction/reconstruction lane, privacy-safe text retention, terminal provider-free evaluation/replay, and cleanup; verify `--help` is effect-free and synthetic provider-free self-tests reject cross-dimension status mismatches without scoring prose. **Current Rung:** the existing matched-source consumer-outcome harness and installed SDK summarize API are verified owners. **Next Real Boundary:** provider-free fixture/self-test exercising all three expected decisions and one deliberately conflicting response. **Blocker/Unblocker:** 1.1 source and ownership preflight; stop this dependency chain if the existing runner cannot preserve actual compaction evidence without a new standalone mechanism, then update design/tasks with the smallest evidenced owner instead of adding one implicitly. **Observable Proof:** exact expected fields pass, a broadened status fails with cause-rich diagnostics, replay performs zero configured calls, and proof-owned state is removed. **Validation:** focused consumer-outcome contracts/tests, syntax/type checks for touched proof owners, and evaluator replay self-test.

## 2. Capture The Frozen Baseline

- [x] 2.1 Capture the frozen pre-change source for every CSA-001 member through one installed synthetic session arm, using at most one main response, one configured compaction, and one reconstruction call for the arm; preserve source/environment identities, structured decisions, assistant text, compaction context, exits/stdout/stderr, effects, and terminal cleanup, then run provider-free terminal evaluation without modifying loaded instructions. **Current Rung:** green provider-free status-scope runner from 1.2. **Next Real Boundary:** installed baseline main-response plus actual compaction/reconstruction path. **Blocker/Unblocker:** 1.2; any unknown identity, cleanup, or observation path blocks instruction correction until the preserved bundle receives terminal replay or names the exact missing observation. **Observable Proof:** immutable baseline bundle covers all three members and states only baseline observations, whether passing or failing, without manufacturing an improvement claim. **Validation:** baseline capture, provider-free evaluate, and two equal-digest replay runs.

## 3. Apply And Prove The Scoped-Status Contract

- [x] 3.1 Consolidate the universal subject/evidence-scope rule into the existing `Principle of Least Surprise`, add only the path-scoped Live-Attempt reporting delta at `global/AGENTS.md` and `change-ready-sdlc`, update the canonical compaction prompt source, materialize kit-owned mirrors through existing ownership, and add deterministic canonical-owner/mirror markers; verify no second communication framework, project-specific term, fuzzy scorer, or live-attempt semantic change is introduced. **Current Rung:** immutable baseline from 2.1. **Next Real Boundary:** deterministic installed-source readback before any candidate model call. **Blocker/Unblocker:** 2.1 and clear source/mirror ownership; stale or foreign runtime sources remain untouched and block candidate capture until the selected source is actually loadable. **Observable Proof:** source inventory identifies one canonical general rule and one exact Live-Attempt delta, all maintained mirrors match, and installed readback loads the candidate bytes. **Validation:** instruction/config contracts, source inventory, mirror/install checks, startup token budget, and selected strict validation for changed instruction artifacts.
- [x] 3.2 Capture the readable candidate with the same scenario bytes, model routes, permissions, OpenCode version, OS class, and call bounds as baseline and one terminal replay evaluator for both immutable arms; require every main-response and actual compaction/reconstruction member to preserve exact status dimensions, run terminal provider-free comparison/replay, and correct any reproduced in-scope contradiction before claiming the finite result. **Current Rung:** deterministically loaded candidate from 3.1. **Next Real Boundary:** installed candidate main-response plus actual SDK compaction/reconstruction path. **Blocker/Unblocker:** 3.1 and a clear configured-capture Live-Attempt Gate; a failed configured capture remains immutable and blocks equivalent repetition until provider-free terminal replay or an exact missing observation unlocks the lane. **Observable Proof:** all candidate members and cleanup oracles pass, no forbidden effect occurs, and the evaluator emits only CSA-001's recorded finite maximum claim. **Validation:** candidate capture, matched baseline/candidate evaluation, two equal-digest provider-free replays, and installed source/candidate identity readback.
- [x] 3.3 Update the maintained proof inventory, CSA-001 development record, and requirement/evidence references with exact commands, scenario population, identities, evidence roots, replay procedure, terminal verdict, unresolved observations, cleanup, and maximum supported claim; verify deterministic readback rejects stale or broadened records. **Current Rung:** terminal matched evidence from 3.2. **Next Real Boundary:** cold-reader and evidence-schema readback of the complete bounded claim. **Blocker/Unblocker:** 3.2; no supported disposition is allowed while a required member, real oracle, or cleanup fact is unknown. **Observable Proof:** one schema-valid claim row binds every member and evidence reference without claiming universal model or language compliance. **Validation:** evidence index validation, proof inventory checks, selected operation-gate readback, and strict selected-change validation.

## 4. Challenge And Validate The Complete Candidate

- [x] 4.1 Obtain one fresh read-only `evidence-sufficiency-reviewer` challenge for CSA-001 after current proof, main-disposition every row, and correct only a reproduced accepted-outcome or non-deferrable defect before replaying affected evidence; verify the claim record binds a usable challenge or remains explicitly unsupported. **Current Rung:** complete finite evidence and claim record from 3.3. **Next Real Boundary:** fresh independent challenge against the readable candidate and immutable bundles. **Blocker/Unblocker:** 3.3; missing or unusable challenge blocks only the broad finite claim and does not erase narrower direct observations. **Observable Proof:** challenge provenance, risk rows, main disposition, and any corrected-candidate replay are preserved with no reviewer mutation or authority expansion. **Validation:** evidence-schema closure and affected replay/contract checks.
- [x] 4.2 Run complete applicable project-native validation, strict OpenSpec consistency, installed loader/config readback, focused consumer-outcome tests, and operation-gate checks; verify the final handoff reports the bounded outcome, evidence, unresolved observations, rollback/reload note, no triggered critical SDET requirement, and no external operation. **Current Rung:** challenged current candidate from 4.1. **Next Real Boundary:** full local validation and final installed readback of the same source identity. **Blocker/Unblocker:** 4.1; any candidate mutation invalidates affected loaded behavior and must regain 3.2 evidence before final validation. **Observable Proof:** `npm test`, `npm run validate:strict`, `npm run openspec:validate`, applicable focused proof commands, installed readback, and `openspec validate prevent-cross-layer-status-ambiguity --strict` are green with no live proof-owned process or fixture. **Validation:** the commands named above plus final diff, cleanup, and claim-ceiling readback.
