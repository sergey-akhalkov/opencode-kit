## 1. Freeze The Current Boundary And Prove The Minimal Canonicalizer

Implementation uses only repository edits, dependency/lockfile updates, provider-free commands, and proof-owned disposable roots until a later task explicitly requires matched configured-model evidence. It does not install or activate the global kit, mutate consumer projects or remote state, use credentials, rewrite archives, commit, push, release, or deploy. Every fixture/temp root must be removed in `finally`; retained diagnostics remain privacy-safe. These process controls may be revised when evidence changes without authorizing any protected action.

- [x] 1.1 Capture the current repository candidate, active-change ownership/search results, `npm run instruction:inventory -- --format json`, and `npm run instruction:budget -- --format json` without materializing the seed; preserve exact source/package/Node identities and the current numeric output as historical before-evidence. **Current Rung:** reviewed source/spec/tool research. **Next Real Boundary:** actual package entry over a disposable Markdown fixture. **Observable Proof:** the candidate snapshot and both commands exit zero, the active search names every current ceiling-dependent control, and no source file changes. **Validation:** `node global/bin/repo-candidate-snapshot.ts --root . --summary`, `node tools/openspec-change-inventory.ts --root . --mode ownership`, both inventory commands, and `git diff --check`.

- [x] 1.2 [automation-dividend] Add the pinned `textlint` development dependency, reviewed `config/instruction-context-quality.json` seed, and one cohesive `tools/instruction-context-quality.ts` parser/evaluator/check/write/help owner with staged two-pass comparison, protected-value readback, atomic writes, and cause-preserving cleanup; add the package entry and focused disposable test owner. **Current Rung:** source-selected engine with no candidate runner. **Next Real Boundary:** actual `npm run instruction:canonicalize` happy path. **Observable Proof:** `--help` and `-h` exit zero without reading/writing source, malformed seed and protected-span fixtures fail without byte drift, and dependency installation/readback is green. **Validation:** focused canonicalizer tests plus package/lockfile readback. **Dependency-Chain Stop Condition:** do not extend repository inventory or remove budget enforcement until help, failure atomicity, and parser fixtures pass.

- [x] 1.3 Run the smallest complete canonicalization happy path through the actual package entry against a disposable maintained-shape Markdown corpus: check reports one approved fix, explicit write applies it, the immediate second check reports zero changed files, and preimage/protected values plus cleanup are exact. **Current Rung:** source-valid canonicalizer. **Next Real Boundary:** inventory-owned duplicate and exception evaluation. **Observable Proof:** exact exit/stdout/stderr, applied rule ID, before/after measurements, byte-identical second pass, protected hashes, and removed fixture root are preserved. **Validation:** `npm run instruction:canonicalize -- --check <fixture>`, `--write <fixture>`, second `--check`, and focused test replay.

## 2. Replace Budget Enforcement With Context-Quality Inventory

- [x] 2.1 Extend inventory classification to the explicit model-facing Markdown category set, import read-only output from the shared context-quality evaluator, delete `repeatedLines`, derive stable heading paths/locations/digests, detect every same-file and cross-file exact operative prose block, and validate one-owner independent-loader exceptions without weakening private-source redaction. **Current Rung:** proven fixed-point runner with line-only duplicate diagnostics. **Next Real Boundary:** actual inventory CLI over disposable duplicate fixtures. **Observable Proof:** agent/skill/command/template/OpenSpec-instruction coverage is explicit; intra-file and unexcepted inter-file fixtures fail with every location; one exact reviewed exception passes; stale, ambiguous, orphaned, and broader occurrences fail; similar-but-different semantics remain `unknown` review candidates with no fix. **Validation:** focused instruction-context tests and two byte-identical JSON reports.

- [x] 2.2 Add cycle, overlap, parse-failure, non-idempotence, frontmatter, code, URL, command/path/identifier, number, modal, negation, condition, exception, quoted-requirement, and privacy fixtures; keep automatic writes limited to reviewed exact rules and reject generic style presets or semantic scoring. **Current Rung:** duplicate-aware inventory and minimal safe fix. **Next Real Boundary:** strict validator integration over disposable kit copies. **Observable Proof:** every unsafe or semantic case is unchanged or review-only, every deterministic error preserves preimages and original cause, and repeated check output is stable ordered. **Validation:** focused canonicalizer/inventory suite including positive and negative fixtures.

- [x] 2.3 Integrate context-quality check mode into strict library validation while retaining separate startup/discovery/on-demand/unknown diagnostics and proving validation never mutates source or fails solely on measurement growth. **Current Rung:** actual package entries proven independently. **Next Real Boundary:** current repository inventory and duplicate disposition. **Observable Proof:** a disposable unique-growth candidate passes measurements and quality checks, duplicate/malformed candidates fail with cause-preserving diagnostics, and all fixture source hashes remain unchanged. **Validation:** `npm run test:focused:validation`, `npm run test:focused:library`, focused instruction-context tests, and disposable `npm run validate:strict`.

## 3. Migrate Current Source And Remove The Ceiling Owner

- [x] 3.1 Run the new inventory/check paths over the maintained model-facing category set, review every exact operative duplicate, remove only a proven redundant occurrence or add the smallest one-owner independent-loader exception, and retain all unique safety/routing/proof contracts. **Current Rung:** disposable strict-validation boundary. **Next Real Boundary:** repository candidate without the budget owner. **Observable Proof:** the actual repository check is green, each initial exact finding has an explicit retained disposition, near-duplicates remain review-only, and an immediate canonicalization second check reports zero changed files. **Validation:** actual `instruction:inventory`, `instruction:canonicalize -- --check`, scoped source review, and affected consumer/contract tests for any manually changed instruction.

- [x] 3.2 Delete `config/instruction-budget.json`, `tools/instruction-budget.ts`, `tools/proofs/instruction-inventory-budget.ts`, their package scripts, validator import, focused fixtures/helpers, and proof-inventory ownership only after task 3.1 is green; update maintained context documentation to describe diagnostics and quality gates without a compatibility alias or replacement maximum. **Current Rung:** replacement owner working on current source. **Next Real Boundary:** current plans/spec deltas reconciled with no budget code path. **Observable Proof:** package, validator, test/helper, proof-inventory, and maintained docs contain no budget seed, materializer, or `instruction:budget`/`proof:instruction-budget` owner, while inventory diagnostics remain available; live planning/spec wording is closed by task 3.3 rather than falsely claimed here. **Validation:** focused library/validation tests, package/proof inventory readback, bounded owner-path search, and strict validation.

- [x] 3.3 Reconcile current controls in all five current changes: update ceiling/budget contracts in `add-foundation-integrity-autorecovery`, `add-bounded-falsification-review`, `add-continuous-complexity-management`, and `add-specialist-team-advisor`, plus the removed instruction-budget preflight in `add-autonomous-campaign-orchestration`; preserve unrelated campaign/process budgets and accurate history, appending supersession only where needed. **Current Rung:** repository quality gate without budget owner. **Next Real Boundary:** all current OpenSpec changes evaluated under one contract. **Observable Proof:** a fresh active-change and canonical-delta search finds no executable numeric instruction ceiling or removed command, unrelated active-change semantics remain untouched, and overlap/dependency order requires this change to apply first or an explicit rebase. **Validation:** strict validation for this change and all active changes plus ownership inventory.

## 4. Prove The Loaded And Maintainer Boundaries

- [x] 4.1 Materialize the candidate `core` profile into a disposable root and run the existing runtime-surface loader/profile proof; verify required owners, source identity, permissions, collisions, separate context measurements, and cleanup without a numeric context verdict or active global installation. **Current Rung:** source/spec/active-plan candidate complete. **Next Real Boundary:** affected loaded consumer behavior when operative instructions changed. **Observable Proof:** actual disposable loader discovers every required core artifact exactly once, reports diagnostic categories, performs no active install/provider call, and removes the generated root. **Validation:** runtime-surface profile tests and `npm run proof:runtime-surface-loader` with a new candidate/evidence root.

- [x] 4.2 Freeze one candidate snapshot/hash shared by inventory, canonicalization check, profile proof, and this decision; if operative loaded instruction bytes changed from task 1.1, run only affected maintained matched baseline/candidate consumer scenarios under identical model/profile/prompt/permission/environment and require no regression in routing, authority, safety, proof, diagnostics, or cleanup; if no operative bytes changed, record exact source identity as the bounded no-behavior-change control and make no configured call. **Current Rung:** provider-free loaded profile. **Next Real Boundary:** complete local validation. **Observable Proof:** either terminal matched consumer rows with immutable privacy-safe evidence and cleanup, or a reproducible byte-identity control; no size reduction is claimed as behavior proof. **Validation:** affected consumer-outcome pack and provider-free replay, or exact source/profile hash readback.

## 5. Validate One Complete Candidate

- [x] 5.1 Run the portable apply gate, selected and all-current strict OpenSpec validation, focused instruction-context/library/validation/profile/consumer tests, full `npm test`, `npm run validate:strict`, actual inventory/canonicalization check, proof inventory, `git diff --check`, and scoped diff/search review on one candidate; correct only accepted-outcome defects and repeat invalidated proof lanes. **Current Rung:** repository and applicable loaded behavior proven. **Next Real Boundary:** local implementation handoff and later separately authorized archive/activation. **Observable Proof:** every applicable command exits zero, canonicalization pass two has zero diff, no active ceiling contract or removed command remains, no unknown writer/fixture/process is live, and diagnostics preserve exact failures if any. **Validation:** complete command/status/stdout/stderr/candidate matrix and final intended-path review.

- [x] 5.2 Publish the implementation handoff with Outcome, candidate identity, removed and replacement owners, actual-entrypoint runtime proof, loader/consumer evidence, validation, current context diagnostics, known semantic-duplication limitation, rollback/restart guidance, cleanup, and external-operation state; do not claim semantic equivalence, universal context safety, RC, stable, archive, or activation from local planning/implementation evidence. **Current Rung:** green complete local candidate. **Next Real Boundary:** owner-requested OpenSpec archive and optional global activation in a fresh process. **Observable Proof:** handoff references only retained evidence and names every unsupported or unknown lane without broadening the proposal claim. **Validation:** source/evidence link readback and selected change status.
