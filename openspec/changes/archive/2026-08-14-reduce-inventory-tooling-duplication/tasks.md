## 1. Production Walker Reduction And Proof

- [x] 1.1 Capture the current installed instruction-inventory JSON/Markdown output, replace the private Markdown walker with `walkMarkdownFiles`, and immediately rerun the actual package entry point against the same candidate before changing test infrastructure. **Current Rung:** source-proven equivalent walkers with no candidate mutation. **Next Real Boundary:** installed `npm run instruction:inventory` over the kit repository. **Blocker/Unblocker:** no concurrent writer may own `tools/instruction-artifacts-inventory.ts`; implement this change before `measure-loader-visible-instruction-budget` or rebase before proof. **Observable Proof:** normalized ordered artifacts, classifications, totals, repeated lines, root redaction, JSON version, exits, and errors match the baseline exactly. **Owner Authorization:** local read-only inventory execution and scoped source edit. **Safeguards:** no ignore, output, CLI, schema, or catalog behavior change; no provider, external repository, install, activation, or remote action. **Restoration/Cleanup:** restore only this scoped edit on mismatch and preserve baseline/candidate outputs. **Expected Immutable Evidence:** source identities, commands, exits, stdout/stderr, normalized comparison, and diff. **Validation:** actual instruction-inventory runtime proof. **Dependency-Chain Stop Condition:** any observable inventory drift blocks the test-harness reduction until corrected or the change is re-scoped.

## 2. Test Harness Reduction

- [x] 2.1 Replace the private fixture/process/assertion harness in `tools/test-code-quality-inventory.ts` with existing `tools/test-helpers/library.ts` owners, add at most one minimal code-quality invocation helper, retain all four unique scenarios, and run the focused package entry point. **Current Rung:** green production inventory proof and source-proven duplicate test harness. **Next Real Boundary:** `npm run test:focused:code-quality` through the maintained test entry point. **Blocker/Unblocker:** task 1.1 green. **Observable Proof:** attention filtering, split-candidate failure, default-root redaction, invalid-root redaction, argv/exits, fixture contents, and cleanup remain green with one shared harness owner. **Owner Authorization:** local temp files/processes and scoped test/helper edits. **Safeguards:** do not import a sibling executable, remove a test oracle, or change production inventory behavior. **Restoration/Cleanup:** shared helper and fixtures clean up only their owned temp state; preserve test output. **Expected Immutable Evidence:** focused test transcript and net line/concept reduction. **Validation:** focused code-quality inventory test. **Dependency-Chain Stop Condition:** helper side effects or lost unique oracle stop the reduction rather than broadening the helper framework.

## 3. Validation And Handoff

- [x] 3.1 Run focused inventory tests, complete project-native validation, strict OpenSpec validation, and one read-only code-quality reduction review; publish the local handoff with exact deletion/reuse targets, net delta, compatibility evidence, overlap ordering with `measure-loader-visible-instruction-budget`, rollback, and external-operation state. **Current Rung:** both reductions proven at their installed boundaries. **Next Real Boundary:** none; later loader-visible inventory work consumes the shared walker. **Blocker/Unblocker:** tasks 1.1-2.1 and no open mutator. **Observable Proof:** all applicable validation is green, only the two duplicate concepts were removed, and no public behavior or unique oracle changed. **Owner Authorization:** local validation/read-only review only. **Safeguards:** no install, activation, release, publication, commit, push, or remote mutation. **Restoration/Cleanup:** preserve validation evidence and remove only disposable state. **Expected Immutable Evidence:** Candidate Reference, validation transcript, reviewer matrix, and final diff. **Validation:** focused inventory suites, `npm run validate:strict`, full test suite, and `openspec validate reduce-inventory-tooling-duplication --strict`. **Dependency-Chain Stop Condition:** red validation or confirmed compatibility drift prevents completion; optional broader reductions remain parked.

## 4. Final History Retrospective

- [x] 4.1 After every other currently known task is complete, analyze the complete `history.md` once using the canonical six-cell `Quality`, `Cycle Speed`, and `Token Economy` matrix across `Working Repository` and `opencode-kit`; classify each evidence-backed candidate by Impact Horizon, Concrete Consumers, Execution Class, Earliest Safe Point, Invalidated Evidence, and Observable Payback; append and immediately execute every admitted exact-current-consumer improvement under `## Session-Derived Improvements`, preserve no-current-consumer candidates as non-checkbox deferred history records, or record `none` without inventing work. Apply, archive, compaction, and generated tasks MUST NOT create or rerun this final-history analysis.
