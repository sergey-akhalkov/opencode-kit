## Shared Execution Envelope

Inventory behavior is corrected before deletion or reuse. Deletion requires literal, graph, loader/config/package/dynamic-resolution, copied-source, and installed inventory evidence. No workstation, guard, doctor, or unrelated large-file behavior is changed.

## 1. Reproduce And Correct Inventory

- [x] 1.1 Add self-layout, conventional, mixed, evidence-heavy, empty, and unreadable fixtures; verify current source reproduces missing `tools/test*.ts` and source-owner classification before correction.
- [x] 1.2 Extend current project inventory classifiers and explicit reason/exclusion/unknown counts without changing its entrypoint; verify this repository reports production/tool/test owners, conventional fixtures remain stable, evidence/vendor/generated files are excluded, and unreadable roots fail with cause.
- [x] 1.3 Run corrected inventory against the current checkout and compare stable JSON/Markdown/redaction/output facts with the audit baseline; this direct CLI run is the first real behavior boundary.

## 2. Remove Only Proven Dead Modules

- [x] 2.1 Rebuild the zero-consumer matrix for audit candidates `index.ts`, `get-project-id.ts`, `log-warn.ts`, `mutex.ts`, `shell.ts`, `temp.ts`, and `terminal-detect.ts` using literal/graph/config/package/dynamic-loader/runtime-source evidence; explicitly retain notify/cmux-referenced modules, mark unknown candidates keep/block, and proceed only with the exact proven subset.
- [x] 2.2 Delete the proven zero-consumer set and run copied-global-source/OpenCode loader inventory plus focused/full tests; verify installed plugin/tool catalogs and behavior are unchanged.

## 3. Reuse Exact Helpers Without Oracle Loss

- [x] 3.1 Compare named `hashRef`, `dataOf`, `record`, `exactKeys`, stable/required/write-new helper clusters for signature, error, redaction, order, effects, and unique tests; record delete/reuse/keep decisions and net concept delta.
- [x] 3.2 Apply only high-confidence low-coupling reuse one cluster at a time; after each cluster verify all unique proof/compatibility oracles and revert no unrelated code.
- [x] 3.3 Re-run code-quality inventory and record `split-or-justify` for remaining large files; leave workstation/guard/doctor owners unchanged unless this change itself introduced a responsibility.

## 4. Complete Validation

- [x] 4.1 Run focused inventory/reduction tests, project/code-quality inventories, copied-source loader proof, `npm test`, `npm run validate:strict`, selected strict validation, and `openspec validate --all`; retain exact unresolved dynamic-load or unrelated active-change risks.
