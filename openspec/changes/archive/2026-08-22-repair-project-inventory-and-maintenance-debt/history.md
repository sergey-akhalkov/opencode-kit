# Strategy History

## 2026-08-21 - Select extension and evidence-bound reduction

- **Objective:** Correct the false repository map and reduce proven maintenance debt without creating another analysis framework.
- **Approach:** Consider a generic language/repository detector and broad clone extraction, then select explicit extensions to current inventory plus named zero-consumer and exact-contract reduction gates.
- **Evidence:** Existing inventory already owns traversal/output, while audit and graph evidence identify concrete missing root patterns and a bounded set of dead/duplicate candidates; line count and similarity alone do not prove safe refactor.
- **Outcome:** Existing-owner extension and bounded reduction selected; generic framework/broad refactor rejected.
- **Reason:** It gives the fastest real inventory correction and keeps behavior proof local.
- **Do-Not-Repeat Condition:** Do not auto-delete from graph absence, score semantic clones, or refactor unrelated large files by line count.
- **Evidence-Based Retry Condition:** Reconsider a broader classifier only after multiple maintained repositories expose distinct unsupported layouts that cannot be represented by explicit evidence rules.

## 2026-08-21 - Zero-consumer primitive subset

- **Objective:** Prove the exact kdco-primitives deletion set.
- **Approach:** Combine literal import/config/package search, graph callers, dynamic-load search, and `npm run opencode:sources` top-level plugin inventory.
- **Evidence:** Only `notify.ts` and `notify/cmux.ts` import `types.ts`, `cmux.ts`, and `with-timeout.ts`. The seven audit candidates are referenced only by the unused barrel. Plugin catalog is `graphify-project-context`, `notify`, `session-env`.
- **Outcome:** Delete the seven named candidates; retain notify/cmux helpers.
- **Reason:** Consumer proof is complete inside the supported envelope.
- **Do-Not-Repeat Condition:** Do not delete `types.ts`, `cmux.ts`, or `with-timeout.ts` from graph absence of the other files.
- **Evidence-Based Retry Condition:** Reopen a deleted file only if a later loader or import consumer appears.
