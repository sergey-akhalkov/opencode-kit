## 1. Establish Ownership And The Smallest Real Path

- [x] 1.1 Recheck active changes, current worktree state, graph blast radius, and exact ownership for `global/plugin/session-env.ts`, managed compaction prompt/template, `complain`, archive skill, runtime profiles, package/proof inventory, and planned tests; record the selected non-overlapping write set and any serialized dependency in `ownership.json` and `history.md`. Verify every unrelated or currently owned mutation remains untouched and no production edit begins while an overlapping writer is open.
- [x] 1.2 Prove the pinned OpenCode post-compaction identity before dependent prompt or capture implementation: use a disposable root, isolated config/data/database, current plugin SDK, and bounded synthetic compaction to correlate `session.compacted` with exactly one newest generated summary through the supported client surface. Preserve invocation, version, event/message identities, stdout/stderr, provider-call count, effects, cleanup, and the `KZN-001` observation. Stop the compaction dependency chain if identity is missing or ambiguous; do not substitute transcript scanning, prose inference, or another summarization call.
- [x] 1.3 Implement the minimal self-contained Kaizen record/store module with startup activation, canonical project hashing, platform data-root resolution, strict redacted signal/event schemas, exclusive fixed-slot append, idempotency, lifecycle fold, and fixed bounds. Current rung: source-verified design plus task 1.2 identity. Next real boundary: invoke the production module against two disposable Git roots and one isolated data root; verify one explicit signal from each is durable, bounded, privacy-safe, cross-project distinct, worktree-clean, and provider/network-free, while `OPENCODE_KAIZEN=0` writes nothing.
- [x] 1.4 Compose explicit report, status, decision, and checkpoint tools through the existing `session-env` owner without changing unrelated shell, Graphify, session-delivery, or project-memory behavior. Run the copied-plugin boundary and then pinned OpenCode with a disposable root; call explicit report/status through the loaded tools and observe matching immutable records, bounded metadata, preserved diagnostics, zero external egress, clean worktree, and complete process cleanup.

## 2. Complete The Capture And Triage Behavior

- [x] 2.1 Add the mandatory zero-to-three-signal envelope through the plugin-owned append-only compaction context without replacing the managed prompt; compose existing project-memory context; and retain the proven root-correlated post-compaction capture hook with bounded lookup, strict extraction, idempotency, `no-signal`, and visible capture-gap behavior. Drive context composition, valid, empty, missing, malformed, duplicate, wrong-root, child-session, timeout, and store-failure cases through real hooks, then repeat the loaded long-session boundary and observe only the redacted envelope persisted while existing goal/live-attempt/strategy continuation remains intact.
- [x] 2.2 Extend canonical agent-driven archive orchestration with idempotent `harvest-pending`, `captured`, `no-signal`, and `archive-failed` events plus derived `repair-gap` and non-persisted `unavailable` reporting while leaving the deterministic archive helper's completion authority intact. Through a disposable OpenSpec project, prove successful archive with findings, successful archive with no signal, interruption after archive, unavailable checkpoint, and failed archive; verify no `repair-gap` closure is written, archive movement is never repeated or relabeled, helper failure closes an opened checkpoint as `archive-failed`, and later repair closes only the open successful-archive checkpoint as `captured` or `no-signal`.
- [x] 2.3 Retarget `complain` to explicit Kaizen capture when loaded, retain bounded Markdown fallback when unavailable, and implement strict idempotent `FB-*` import without treating written legacy status as current truth. Verify one loaded complaint writes only the inbox, one disabled complaint writes only fallback Markdown, repeated import creates one signal, and a stale open fixture remains `needs-investigation` until current evidence exists.
- [x] 2.4 Complete bounded status and manual triage projection for stable age/ref ordering, exact totals/truncation, decisions, capacity, capture gaps, and archive repair gaps. Verify one invocation handles no more than 25 signals; every processed signal receives evidence, owner class, next boundary or terminal reason; unsupported ownership becomes `needs-investigation`; and deterministic code performs no fuzzy grouping, severity inference, or proposal authorization.
- [x] 2.5 Add the global status/triage command flow and configured proposal-owner-root containment. From a consumer disposable root, verify a reusable signal can become only `kit-candidate` or `needs-investigation` with zero target-worktree mutation; from a disposable configured `opencode-kit` owner root, run the canonical proposal workflow for one reviewed cohesive fixture and observe at most one valid change linked by privacy-safe signal refs. Preserve created/cleaned paths and prove no remote, release, install, or source-project effect.

## 3. Finish Safety, Portability, And Maintained Proof

- [x] 3.1 [automation-dividend] Materialize the reviewed `KZN-001` finite population as stable fixture data outside helper code and implement one maintained provider-free runner that drives the production store, schema, lifecycle, explicit tools, compaction-envelope parser/hook fixtures, archive checkpoints, legacy import, status/triage projection, capacity, concurrency, privacy, activation, and side-effect cases. Verify strict seed/readback schema, stable ordering, regeneration drift, exact totals, cause-preserving failures, and zero semantic inference in deterministic code; expose a loaded mode for the bounded synthetic compaction/archive path.
- [x] 3.2 Complete fixed-envelope, concurrent-process, malformed/partial-record quarantine, disk/store failure, root/session/credential/path redaction, output budget, once-per-safe-ref automatic warning, and explicit-tool cause-chain cases. Exercise the 2,000-signal/8,000-lifecycle boundary and maximum event sizes, preserve elapsed/resource observations, and confirm existing data remains readable while over-limit writes fail visibly and unrelated plugin hooks remain usable.
- [x] 3.3 Update core/all manifests, portable config ownership, command/skill contracts, doctor/runtime-source diagnostics, focused tests, proof inventory, and installed-copy validation so every referenced Kaizen artifact is included exactly once and no optional profile creates another owner. Verify profile resolution, fresh install preview/materialization, copied `global/plugin/` self-containment, active managed-prompt drift reporting, disabled rollback, and preservation of existing machine-local config bytes.
- [x] 3.4 Add operator documentation for default-on capture, `OPENCODE_KAIZEN=0`, restart requirement, explicit tools/commands, compaction and archive states, fallback/import, data location/bounds, privacy ceiling, proposal-root containment, inspection, inert rollback, exact manual cleanup, and maintained proof commands. Verify every documented field, command, path class, status, effect, and non-goal against the loaded source and `--help`/status output.

## 4. Re-Proof, Challenge, And Validate

- [x] 4.1 Re-run the complete installed `KZN-001` happy path after all production mutations using pinned OpenCode, two disposable consumer projects, one disposable configured proposal-owner root, isolated data/config/database, and bounded synthetic provider behavior. Observe explicit report, at least two long-session compactions, complete archive harvest, one interrupted repair gap and closure, legacy import, bounded triage, one canonical proposal, exact archive/proposal paths, privacy-safe state, zero unapproved egress, scoped clean worktrees after owned cleanup, and current candidate/environment identities; preserve the immutable raw bundle before cleanup.
- [x] 4.2 After current proof, add only the smallest focused main-owned regression needed for reproduced accepted-outcome behavior not already covered by the maintained runner, or record `none` with fixture/test evidence showing no distinct oracle gap. Re-run the affected production boundary before treating the test as current evidence.
- [x] 4.3 Dispatch one fresh test-only critical SDET challenge for supported credential or private path retention, cross-project payload disclosure, consumer-to-kit unauthorized mutation, and archive status corruption after harvest failure. Main SHALL independently reproduce and disposition every row, apply only confirmed in-scope corrections, and re-run affected `KZN-001` proof before recording terminal `critical-risks-reported`, `no-critical-risk`, or `blocked` evidence.
- [x] 4.4 Freeze the corrected readable candidate, update every `KZN-001` population observation and lane, and obtain one fresh read-only evidence-sufficiency challenge for the declared finite population and maximum claim. Verify each member has current candidate/environment/path/boundary evidence or explicitly narrow/block the claim; do not mutate production, tests, instructions, or fixtures during the challenge.
- [x] 4.5 Run focused Kaizen/session/archive/profile tests, the maintained provider-free and loaded proof modes, copied-plugin/config/schema checks, `npm test`, `npm run validate:strict`, and strict validation of `add-cross-project-kaizen-loop`; inspect exact exit status/stdout/stderr and correct every in-scope failure without modifying unrelated dirty files. Finish with scoped diff and graph blast-radius inspection proving no scheduler, campaign, transcript mining, project-memory write, remote client, multi-repository writer, generic event framework, or mandatory product retrospective entered the candidate.
