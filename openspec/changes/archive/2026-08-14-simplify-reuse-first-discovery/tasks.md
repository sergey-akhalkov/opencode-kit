## Fidelity Ladder

All behavior work uses one ladder: **Rung 1** provider-free source/catalog/loader and structural validation; **Rung 2** fresh installed OpenCode processes with matched non-sensitive triggered and trivial inputs under a no-product-mutation envelope; **Rung 3** future ordinary project use through its explicitly configured cross-project source. Rung 3 installation, configuration, refresh, broad project scan, dependency mutation, and remote effects are outside this change.

## 1. Freeze Current Behavior and Scope

- [x] 1.1 Freeze the Product Candidate path inventory, abandoned-change identity, clean-worktree baseline, current instruction/profile/catalog facts, root RC history (`none`), and exact removal ownership; capture the current installed loaded behavior for one triggered new-mechanism prompt and one matched trivial-fix prompt before product mutation. **Current Rung:** current installed loaded global source. **Next Real Boundary:** identical fresh candidate processes after task 2.1. **Blocker/Unblocker:** none; use current source and a bounded no-product-mutation permission envelope. **Observable Proof:** baseline evidence records OpenCode/model/profile/config identity, exact prompts, exit/stdout/stderr/raw events, skill/tool calls, registry behavior, disposition, side effects, session/root cleanup, and source hashes for both scenarios. **Owner Authorization:** two bounded non-sensitive configured-provider calls under standing kit authorization. **Safeguards:** no edit, dependency install, source copy, network mutation, private project inventory disclosure, commit, push, or remote effect; preserve all unrelated work. **Restoration/Cleanup:** delete attributable sessions and disposable roots after immutable capture; unknown cleanup blocks product mutation. **Expected Immutable Evidence:** `implementation-evidence/pre-change-baseline.md` and privacy-safe baseline bundles or exact transcripts. **Dependency-Chain Stop Condition:** do not mutate loaded behavior if the baseline cannot distinguish triggered registry ceremony from trivial-work opt-out or if cleanup/liveness is unknown.

## 2. Remove the Registry and Prove the Retained Workflow

- [x] 2.1 Remove the private registry/inventory command, CLI/modules/template, package/profile/README/proof-inventory references, and registry-specific proof runner without a compatibility shim; narrow `global/AGENTS.md` and `global/skills/reuse-discovery/SKILL.md` to the accepted tool-neutral trigger, bounded source order, current-source verification, degraded cross-project behavior, and compact disposition; synchronize current normative specs and deterministic instruction/catalog markers required for the same behavior. **Current Rung:** green task 1.1 baseline. **Next Real Boundary:** task 2.2 fresh installed candidate scenarios. **Blocker/Unblocker:** task 1.1; no external blocker. **Observable Proof:** provider-free loader/source/catalog readback contains one lazy skill, no `/reuse-inventory`, no registry package or runtime owner, no private/local Graphify details in committed portable text, and strict validation names no dangling active reference. **Owner Authorization:** local repository mutation only. **Safeguards:** preserve ignored machine-local instructions, abandoned historical evidence, unrelated changes, permissions, provider config, and all non-reuse behavior; no install/activation/remote action. **Restoration/Cleanup:** source rollback is limited to this candidate; no persisted registry data exists. **Expected Immutable Evidence:** scoped diff, source/path inventory, loader/catalog output, instruction-token facts, and current candidate hash. **Dependency-Chain Stop Condition:** do not run provider candidate proof while any active command/package/profile/docs surface still promises registry/inventory behavior or committed text leaks machine-local routing.

- [x] 2.2 Invoke the actual fresh installed OpenCode entry point with the task 1.1 model/profile, matched triggered/trivial prompts, and bounded no-product-mutation environment; inspect raw output and correct only accepted-outcome or non-deferrable defects until the current candidate reaches Runtime Proof. **Current Rung:** provider-free candidate readback. **Next Real Boundary:** future ordinary project use at Rung 3, outside this change. **Blocker/Unblocker:** task 2.1 and green zero-provider loader/permission preflight. **Observable Proof:** the triggered lane loads `reuse-discovery`, searches cheapest-first, inspects current-repository candidates, explicitly reports the unavailable cross-project layer as `degraded`, makes no registry call, and emits the required disposition; the trivial lane loads no reuse skill and makes no cross-project call; both exit successfully with no product mutation and complete cleanup. **Owner Authorization:** two bounded non-sensitive configured-provider calls per candidate attempt; a repeated attempt requires causal correction, preserved-bundle terminal replay, and fresh preflight. **Safeguards:** no real product edit, dependency install, source copy, unbounded project enumeration, credentials output, remote mutation, commit, push, or private root persistence. **Restoration/Cleanup:** delete sessions and disposable roots in `finally`; preserve failures before retry. **Expected Immutable Evidence:** `implementation-evidence/runtime-proof.md` with Candidate/Runner/Evaluator/Environment/Raw Bundle identity, exact invocation/input, exit/stdout/stderr, tool order, observed effects, cleanup, and `Live-Attempt Gate`. **Dependency-Chain Stop Condition:** red or unobservable happy path leaves `Development-Stage: development` and blocks SDET/qualification.

## 3. Complete Accepted Scope and Candidate Readback

- [x] 3.1 After current Runtime Proof, inspect every active reference and realistic requirement-linked edge: current-repository early stop, unavailable configured cross-project source, stale/unreadable peer source, trivial opt-out, ecosystem recommendation without installation, abandoned-history exclusion, and restart/loading notes; correct only accepted-scope gaps, replay affected proof, and freeze a readable Candidate Reference. **Current Rung:** current Rung 2 MVP. **Next Real Boundary:** fresh critical-only SDET against the same candidate. **Blocker/Unblocker:** task 2.2 current proof. **Observable Proof:** active docs/specs/catalog/source agree, no removed surface is reachable or advertised, all named edge cases have source or proof disposition, architecture remains one compact trigger plus one cohesive lazy skill, and diagnostics preserve blocked/degraded causes. **Owner Authorization:** local source/docs/spec mutation and provider-free replay only. **Safeguards:** no optional framework, new command, cross-project provider configuration, or historical-evidence rewrite. **Restoration/Cleanup:** source rollback only; replay only invalidated lanes. **Expected Immutable Evidence:** `implementation-evidence/candidate-reference.md`, active-reference inventory, edge-case matrix, and current proof references. **Dependency-Chain Stop Condition:** incomplete accepted scope or changed Product Candidate returns to `development` and must regain proof before SDET.

## 4. Fresh Material Critical SDET

- [x] 4.1 Dispatch one fresh `sdet-quality-engineer` after task 3.1 with the original requirements, current Candidate Reference/raw proof, exact test-only scope in existing `tools/test-*.ts` and fixture paths, and critical rubric covering accidental broad private-project disclosure, registry command remaining loaded, triggered work silently skipping all cross-project evidence, stale metadata authorizing source reuse, trivial fixes acquiring broad discovery, and removed artifact references breaking validation/install. Main SHALL independently reproduce and disposition every plausible row; another SDET attempt is allowed only after an immediately prior main-confirmed critical defect, production correction, and restored Runtime Proof. **Current Rung:** current complete Rung 2 candidate. **Next Real Boundary:** trusted project validation. **Blocker/Unblocker:** task 3.1 and closed mutation attempts. **Observable Proof:** terminal `critical-risks-reported | no-critical-risk | blocked`, Effective Model, any smallest test-only regression oracle, main reproduction/disposition, and current proof after any candidate/test mutation. **Owner Authorization:** local test-only writes and bounded configured inference. **Safeguards:** SDET edits no production/OpenSpec/instruction artifacts, performs no external action, and asks no user question. **Restoration/Cleanup:** remove disposable test state and close writer attempt. **Expected Immutable Evidence:** `implementation-evidence/critical-sdet.md`, test diff if any, commands/results, and terminal reason.

## 5. Validate the Complete Candidate

- [x] 5.1 Run focused contracts affected by task 4.1, current loader/catalog readback, `npm run validate:strict`, `npm test`, instruction and code-quality inventories, strict selected/all OpenSpec validation, apply/prepush operation gates, `npm run prepush:validate`, `git diff --check`, and scoped secret/path/reference review; rerun only proof lanes invalidated by candidate, runner, evaluator, or environment changes. **Current Rung:** terminal SDET candidate plus current Runtime Proof. **Next Real Boundary:** final history retrospective and local handoff. **Blocker/Unblocker:** terminal usable task 4.1 and no open writer. **Observable Proof:** every applicable command exits zero, no active removed-surface reference or machine-local path is committed, current loaded behavior evidence remains attributable, and no known confirmed reachable critical/non-deferrable defect remains. **Owner Authorization:** local validation only. **Safeguards:** no commit, push, archive, install, activation, deployment, release, or remote action. **Restoration/Cleanup:** delete disposable validation/loader state; preserve raw failures before correction. **Expected Immutable Evidence:** `implementation-evidence/final-validation.md` with exact argv/exits/stdout/stderr summaries, artifacts, source identities, and cleanup. **Dependency-Chain Stop Condition:** any candidate regression blocks RC until corrected, re-proven, and revalidated.

## 6. Final History Retrospective and Stable Handoff

- [x] 6.1 After every other currently known task is complete, perform the one-time final history retrospective over complete `history.md` using the canonical `Quality`, `Cycle Speed`, and `Token Economy` by `Working Repository` and `opencode-kit` matrix; classify every evidence-backed candidate with `Impact Horizon`, `Concrete Consumers`, `Execution Class`, `Earliest Safe Point`, `Invalidated Evidence`, and `Observable Payback`; append and immediately execute every admitted current-consumer improvement under `## Session-Derived Improvements`, preserve no-current-consumer evidence as non-checkbox deferred history records, or record `none` without inventing work. Apply, archive, compaction, and generated tasks MUST NOT create or rerun this analysis. After any admitted work, replay invalidated proof/validation, freeze the next monotonic RC, and write the complete local stable handoff with outcome/non-goals, architecture, diagnostics, Candidate/Runner/Evaluator/Environment/Raw Bundle identity, terminal SDET, validation, limitations, rollback, restart requirement, abandoned-change relationship, and `External Operations: not performed`. **Current Rung:** complete validated local candidate. **Next Real Boundary:** future restarted OpenCode process in ordinary project work. **Blocker/Unblocker:** tasks 1.1-5.1 and every generated admitted task complete. **Observable Proof:** one terminal retrospective record, admitted IDs or `none`, no unresolved accepted task, current proof/validation, `Development-Stage: stable`, and `Stable Candidate: RC<n>`. **Owner Authorization:** local evidence/spec/handoff work only. **Safeguards:** no scope expansion, protected action, archive, commit, push, install, activation, release, or remote mutation. **Restoration/Cleanup:** documentation/source rollback only; retain immutable proof. **Expected Immutable Evidence:** final `history.md`, tasks readback, `implementation-evidence/stable-handoff.md`, and final Candidate Reference/RC history.

## Session-Derived Improvements

- [x] SDI-1 Align the `library-instruction-artifacts` change delta with the proved explicit `degraded` cross-project disposition. **Impact Horizon:** Current Change. **Concrete Consumers:** task 4.1 SDET and task 5.1 selected/all OpenSpec validation. **Execution Class:** do-now. **Earliest Safe Point:** before SDET. **Invalidated Evidence:** none; normative wording only matched existing proof and Product Candidate. **Observable Payback:** delta, main spec, proposal, task 2.2, and Runtime Proof now agree. **Trigger/Evidence:** the change delta retained predecessor `not-applicable` wording after the simplified proof established `degraded`. **Why:** the contradiction made accepted behavior ambiguous. **Prerequisites:** `simplify-r2` proof. **Scope/Non-Goals:** no product, runner, provider, or live recapture change. **Implementation:** require current-source inspection and explicit `degraded` when the cross-project layer is unavailable. **Observable Proof:** strict selected/all OpenSpec validation exits zero. **Validation:** task 5.1.
- [x] SDI-2 Preserve the deterministic validation literals `Use this skill` and `reuse an existing platform/project mechanism` through any later instruction correction. **Impact Horizon:** Current Change. **Concrete Consumers:** any task 4.1-driven instruction correction and task 5.1 `npm run validate:strict`. **Execution Class:** before-task-5.1. **Earliest Safe Point:** before the next instruction mutation, otherwise immediately before task 5.1. **Invalidated Evidence:** only a later authorized instruction rewrite that deliberately changes matching validator markers. **Observable Payback:** strict validation avoids the already reproduced marker-loss failure. **Trigger/Evidence:** the first compact rewrite failed `validate:strict` until both exact phrases were restored. **Why:** these phrases are deterministic repository gates. **Prerequisites:** current restored instruction wording. **Scope/Non-Goals:** no new validator or policy expansion. **Implementation:** verify both literals before full validation. **Observable Proof:** `npm run validate:strict` prints `warnings=0`. **Validation:** task 5.1 plus `git diff --check`.
- [x] SDI-3 Make evaluator output creation transactional with respect to readable input bundles. **Impact Horizon:** Current Change. **Concrete Consumers:** restored task 2.2 offline replay and task 5.1 proof evidence readback. **Execution Class:** gate-closer. **Earliest Safe Point:** before another evaluator replay. **Invalidated Evidence:** evaluator-only derived verdict; raw baseline/candidate bundles remain valid. **Observable Payback:** a wrong input path fails without poisoning the requested immutable output root; corrected replay can use a fresh root and reach terminal verdict without model calls. **Trigger/Evidence:** the first r2 replay used the wrong baseline directory and created its output root before `ENOENT`; a second replay with the corrected input was blocked by that existing root. **Why:** fail-closed evidence roots should not be created before all immutable inputs are readable. **Prerequisites:** preserved baseline bundles and candidate-sessions-r2. **Scope/Non-Goals:** move output-root creation after reads; no capture behavior, product behavior, cleanup, or live call change. **Implementation:** read all four bundles before `createEvidenceRoot`. **Observable Proof:** `candidate-evaluation-r3/evaluation.json` is written once with `candidateComplete: true` and zero model calls. **Validation:** `node --check`, evaluator replay, and `git diff --check`.
