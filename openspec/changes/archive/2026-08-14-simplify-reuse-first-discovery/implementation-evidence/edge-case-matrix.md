# Edge-Case Matrix

| Case | Current owner | Evidence / disposition |
|---|---|---|
| Current repository satisfies the contract | `reuse-discovery` ordered early stop | Baseline and candidate local-owner lanes load the skill and inspect current sources. The current candidate rejects the defective apparent parser and extends the existing owner without bash or broader registry work. |
| Configured cross-project source unavailable | Skill degraded contract | Candidate local-owner lane reports `Cross-project: degraded` and does not claim complete peer search. |
| Stale or unreadable peer source | Skill source-verification rule plus normative spec | Index metadata cannot authorize reuse; selected current source and evidence must be readable and contract-checked. No compatibility claim is made from metadata alone. |
| Trivial local correction | Global trigger opt-out plus skill trigger | Candidate trivial lane loads no skill, makes no cross-project/registry call, changes no source, and returns the nearest proof. |
| Typical ecosystem package | Skill read-only ecosystem/mutation-authority rule | Research may recommend a package, but cannot install or add it; dependency mutation remains a separate product gate. |
| Removed slash command | Fresh loader config readback | Candidate preflight r3 command names exclude `reuse-inventory`; source hash is null. |
| Removed registry/package/template | Source/path inventory and strict validation | Global active references are absent. Six proof-only sentinels verify absence and zero calls; they expose no product entrypoint. |
| Abandoned predecessor appears active | OpenSpec status/list | `openspec list --json` returns only the replacement change. The predecessor has `ABANDONED.md`, 11 unchecked tasks, unsynchronized specs, and preserved evidence under the abandoned archive. |
| Machine-local provider details leak into portable authority | Source review and strict validation | Global AGENTS/skill use tool-neutral terms. Provider names, private projects, paths, and refresh commands remain in the ignored machine-local source; delta/current normative scenarios are provider-neutral. |
| Cross-project search authorizes mutation | Global/skill/spec protected-action language | Discovery never authorizes source copy, dependency install, publication, credentials, or remote mutation. |
| Running process retains old command/skill | OpenCode config-time behavior | Stable handoff must require restart/new process. The candidate proof uses fresh processes and therefore reflects retained source, not this already-running session's old catalog. |
