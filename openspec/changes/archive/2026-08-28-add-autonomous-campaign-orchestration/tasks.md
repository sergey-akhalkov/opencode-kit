## Fidelity Ladder

`current ownership/source inventory -> provider-free campaign schema/state/report ->
disposable real OpenSpec frozen-wave mission -> configured one-P1 discover-to-verify
happy path -> complete multi-wave semantic/recovery population -> protected Windows
supervisor re-entry -> replay/challenge/full validation`. Product Candidate is the
portable campaign core, mission correlation, readiness, and Windows supervisor adapter.
Proof Runner and Evaluator extend existing `tools/proofs/` owners. Environment Identity
includes kit/OpenCode/OpenSpec/Node/Git/model/profile/Windows/runtime/project/campaign/
mission digests. No task authorizes consumer mutation, remote action, deployment,
release, credential change, or another protected effect.

Profile: `Material`. Main owns production, integration, run-observe-correct, and focused
post-proof regressions. Fresh test-only SDET is required after current complete proof
because a duplicate/resumed writer or unsafe host recovery can corrupt project work or
cross the accepted authorization envelope.

## 1. Reconcile Ownership And Build The Proof Contract

- [x] 1.1 Re-read `openspec list/status`, active ownership manifests, repository candidate snapshot, loaded global source, instruction context diagnostics/quality, runtime and campaign budgets, current workstation diff, and the completed/active state of `add-foundation-integrity-autorecovery`; record an acyclic dependency or explicit ownership transfer for every overlapping OpenSpec/instruction/runtime/proof/workstation surface and leave all unrecognized files untouched. **Current Rung:** reviewed proposal/design/current source. **Next Real Boundary:** provider-free campaign fixture preflight. **Blocker/Unblocker:** implementation writes outside this change remain blocked until `replace-instruction-limits-with-context-quality` has applied or this change has explicitly rebased, and until the foundation change and workstation writers are archived/checkpointed, isolated, or explicitly transferred; read-only inventory and change-artifact correction may continue. **Observable Proof:** `openspec list --json`, `openspec status --change add-foundation-integrity-autorecovery --json`, `node tools/openspec-change-inventory.ts --root . --mode ownership`, `node global/bin/repo-candidate-snapshot.ts --root . --summary`, `npm run opencode:sources`, `npm run instruction:inventory -- --format markdown`, `npm run instruction:canonicalize -- --check .`, and relevant runtime/campaign budget and readiness commands exit zero with one current ownership record. **Authorization:** local read-only inspection and this change's planning/evidence rows only. **Safeguards:** no broad stage, restore, cleanup, archive, mutation of another active change, or replacement instruction-size ceiling; finite campaign/process/resource budgets remain unchanged. **Restoration/Cleanup:** none. **Expected Immutable Evidence:** current source/worktree/active-change/owner/context-quality/runtime-budget identities and exact unblock condition.

- [x] 1.1.1 Run one current bounded falsification episode against the original accepted campaign outcome and the complete planning candidate, attempt all six readiness attack classes, disposition every current material row, and correct only confirmed accepted-outcome or non-deferrable planning defects before production mutation. **Current Rung:** current ownership and reviewed planning. **Next Real Boundary:** provider-free campaign proof contract. **Blocker/Unblocker:** one fresh `implementation-readiness-reviewer`; at most one corrected-candidate re-review only if a confirmed correction changes the challenged decision surface. **Observable Proof:** original request and candidate are supplied separately, all six attack classes have explicit states, no protected owner decision is guessed, and the apply gate accepts the resulting record. **Authorization:** one read-only specialist observation plus local planning/evidence correction. **Safeguards:** no production mutation, reviewer authority, raw private prompt persistence, or optional scope. **Restoration/Cleanup:** no runtime state. **Expected Immutable Evidence:** attributed challenge, main disposition, and current operation-gate output.

- [x] 1.2 [automation-dividend] Extend the existing roadmap mission proof family and shared process/evidence/evaluator libraries with a `work-campaign` provider-free fixture pack, stable reviewed seed records, schema/readback/drift fixtures, effect-free `--help`/`preflight`/`replay` modes, and one proof-inventory row; reuse current temp-project, OpenSpec, Git, process, redaction, raw-bundle, evaluator, and cleanup owners rather than adding a second capture framework. **Current Rung:** current mission proof and campaign planning contract. **Next Real Boundary:** actual campaign schema/state/materializer CLI over disposable valid/invalid projects. **Blocker/Unblocker:** task 1.1 owner closure for touched proof files; if the existing proof family cannot express isolated campaign state and mission composition without weakening another lane, record the irreducible mismatch in design/history before adding the smallest new campaign pack owner. **Observable Proof:** help documents inputs/effects/evidence/cleanup; preflight exercises valid, extra/missing field, path escape, stale digest, P2-in-wave, and report-drift seeds with zero provider/source/host effect; replay is byte-stable and performs zero configured calls. **Authorization:** local disposable proof and fixture writes. **Safeguards:** helpers validate explicit facts only and do not infer severity, materiality, scope, grouping, or completion. **Restoration/Cleanup:** remove every disposable project/process; retain bounded immutable evidence. **Validation:** focused proof-contract/evaluator tests and two identical replays. **Dependency-Chain Stop Condition:** no semantic root, mission handoff, or host work until provider-free seed/evaluator contracts are green.

## 2. Build And Prove The Provider-Free Campaign Core

- [x] 2.1 Implement versioned portable campaign definition, adapter, inventory-block, partition-result, work-item, reconciliation, investigation, wave, closure, and result contracts with exact fields, containment, stable ids/order, finite budgets, allowed effects, one `audit-remediate` playbook, and cause-preserving schema errors; verify valid and each malformed/unsupported fixture through the production entrypoint. **Current Rung:** provider-free reviewed fixtures. **Next Real Boundary:** durable transition append/replay. **Blocker/Unblocker:** task 1.2; unsupported semantic fields remain explicit rather than guessed. **Observable Proof:** an unrelated disposable project receives one normalized definition digest/`inventory` phase while malformed playbook/path/budget/effect/argv records exit non-zero before provider/session/source/host effects. **Authorization:** repository source/tests plus disposable local files. **Safeguards:** no consumer identity, package manager, shell command string, provider credential, or semantic classifier in the core. **Restoration/Cleanup:** fixture deletion. **Validation:** new focused campaign-contract suite, syntax/type checks, and `git diff --check` for touched paths.

- [x] 2.2 Implement the separate append-only campaign transition chain, atomic projection, campaign-state lease, budget accounting/revision, stop intent, process/session/writer descriptors, deterministic replay/reconciliation, and state CLI operations; reuse mission state primitives only when contracts are identical and keep campaign and mission state roots/schemas separate. **Current Rung:** validated contracts. **Next Real Boundary:** kill/restart reconstruction in a disposable process. **Blocker/Unblocker:** task 2.1; an unknown prior writer/process blocks append/resume until terminal or isolated evidence. **Observable Proof:** production CLI persists preflight/phase/pause transitions, reconstructs the exact cursor after a proof-owned process kill, consumes no event twice, rejects corrupt/missing/reordered transitions and stale projection/definition, and leaves source bytes unchanged. **Authorization:** disposable local process/state writes and termination of proof-owned children only. **Safeguards:** append durable before projection, no PID-only liveness, no transcript/task/report inference. **Restoration/Cleanup:** terminate proof-owned children and delete runtime fixture; cleanup unknown is test failure. **Validation:** focused state/replay suite plus provider-free capture/evaluator.

- [x] 2.3 Implement deterministic inventory, seed indexes, ledger/closure projection, and atomic Markdown report materialization/readback for explicit reviewed records; include scope/exclusions, block coverage, P0-P3 dispositions, traceability, redundancy/test-gap/failure-mode matrices, waves/checkpoints, validation/proof, blockers, limitations, and maximum claim while preserving semantic text unchanged. **Current Rung:** restart-safe state core. **Next Real Boundary:** production materializer over a changing disposable ledger. **Blocker/Unblocker:** tasks 2.1-2.2; report drift or unreadable seeds block completion but not immutable evidence preservation. **Observable Proof:** changing one P1 seed from confirmed to fixed-and-verified updates every derived total/status/ref, removes no P2/P3 row, leaves no stale residual/backlog row, and a manually drifted report fails readback without changing state. **Authorization:** deterministic disposable evidence/report writes. **Safeguards:** one seed owner, stable order, privacy redaction, bounded bytes, no summary/severity inference. **Restoration/Cleanup:** remove disposable outputs. **Validation:** focused inventory/materializer tests and two regeneration/readback passes with identical digests.

- [x] 2.4 Compose contracts, state, inventory, ledger, and report into provider-free `preflight`, `status`, `stop`, `replay`, and fake-semantic phase execution through the actual portable entrypoint; inspect exit/status/stdout/stderr, transition chain, report, source manifest, processes, and cleanup. **Current Rung:** production provider-free components. **Next Real Boundary:** one actual frozen-wave mission in disposable OpenSpec. **Blocker/Unblocker:** tasks 2.1-2.3; any state/report/source side effect outside the fixture blocks mission integration. **Observable Proof:** a generic campaign advances deterministically through inventory/findings-freeze and pauses at wave execution with exact fake result refs, while invalid/dirty/active-change/unknown-writer fixtures fail before semantic or source work. **Authorization:** disposable local Git/evidence writes only. **Safeguards:** no provider, OpenCode session, host registration, source mutation, archive, or checkpoint. **Restoration/Cleanup:** fixture/process terminal and deleted. **Validation:** provider-free campaign proof evaluation and focused full-core suite. **Dependency-Chain Stop Condition:** do not add semantic provider or Windows behavior until this core boundary is terminal and green.

## 3. Hand One Frozen Wave To The Existing Mission

- [x] 3.1 Extend mission definition/slice/result contracts with optional parent campaign/wave/work-item correlation, update mission preflight and terminal handoff validation, and preserve every non-parent manual mission behavior, immutable 1..100 queue, certificate issuer, writer, archive, checkpoint, retry, status, stop, and no-successor invariant. **Current Rung:** provider-free campaign frozen-wave output. **Next Real Boundary:** actual mission preflight over an exact and mismatched parent wave. **Blocker/Unblocker:** task 2.4 plus ownership closure from 1.1 for roadmap source; stop if parent support requires dynamic queue mutation or a second certificate issuer. **Observable Proof:** exact parent/mission records pass ordinary preflight; mismatched ref/order/path/outcome/effect/digest fails before session/source effects; legacy mission fixtures and CLI remain byte/behavior compatible where no parent exists. **Authorization:** scoped mission contract/preflight/result edits and disposable fixtures. **Safeguards:** campaign cannot write mission state or generate successors; no semantic inference. **Restoration/Cleanup:** fixture deletion. **Validation:** existing mission preflight/state/controller/runtime/launcher suites plus focused parent-correlation tests.

- [x] 3.2 Implement campaign-to-mission launch/status/stop/terminal consumption with separate state roots and explicit campaign/source ownership handoff, then run one fake/no-model disposable OpenSpec wave through the actual mission controller, official propose/apply/archive/checkpoint/readback path, campaign verification, changed-block marking, and report regeneration. **Current Rung:** correlated schemas/preflight. **Next Real Boundary:** real one-wave production lifecycle without configured semantic discovery. **Blocker/Unblocker:** task 3.1; unknown mission writer/session/process/cleanup/checkpoint blocks parent consumption and replacement. **Observable Proof:** one seeded confirmed P1 becomes one mission slice, archives/checkpoints exactly once, campaign resumes at verification after injected parent restart, no concurrent source writer appears, current report links the archive/checkpoint/fixed evidence, and cleanup is terminal. **Authorization:** disposable local Git/OpenSpec mutation and local commit only when the proof definition explicitly authorizes it. **Safeguards:** one mutation writer, scoped staging, no push/remote/protected effects, parent observation paths non-overlapping. **Restoration/Cleanup:** delete sessions/processes/fixture repo after preserving raw bundle. **Validation:** campaign+mission evaluator, existing mission regression lanes, exact transition/archive/checkpoint counts. **Dependency-Chain Stop Condition:** no configured discovery expansion until this first real mutation boundary is green.

## 4. Add Bounded Semantic Discovery And Prove The Happy Path

- [x] 4.1 Implement the current-runtime semantic-root executor for discovery, reconciliation, investigation, synthesis, and final-challenge assignments with exact role permissions, bounded request/output/time/call/session evidence, isolated create-new result paths, terminal ownership/cleanup, and no mission certificate, nested task, question, OpenSpec/source write, or hidden server. **Current Rung:** proven fake-semantic campaign/mission path. **Next Real Boundary:** installed OpenCode root returning one schema-valid read-only partition result. **Blocker/Unblocker:** task 3.2 and current runtime/permission source identity; unavailable configured inference pauses without source mutation. **Observable Proof:** provider-free fake transport covers malformed/stale/oversize/timeout/cleanup-unknown results, then one installed bounded read-only root produces a correlated result while source/worktree bytes and tool permissions remain unchanged. **Authorization:** one non-sensitive configured model call in a disposable project after provider-free preflight. **Safeguards:** loopback verified runtime, denied mutation/children/questions/external paths/credentials, finite retention. **Restoration/Cleanup:** delete root/children and fixture; retain redacted bundle. **Validation:** focused executor/permission tests and actual runtime capture/readback.

- [x] 4.2 Implement the `audit-remediate` playbook orchestration: deterministic complete inventory/partitions, bounded isolated read-only fan-out, fresh reconciliation, explicit P0-P3 policy, optional-polish negative control, one bounded unknown-material investigation, findings freeze, synthesis candidate, and deterministic wave admission. **Current Rung:** one semantic assignment works at the real runtime. **Next Real Boundary:** configured one-P1 complete campaign. **Blocker/Unblocker:** task 4.1; no wave may launch while any block/result/unknown is non-terminal or P0/P1 assignment/effect/ownership is incomplete. **Observable Proof:** provider-free reviewed fixture pack produces stable candidate/reconciled/investigated/wave rows; P2/P3 and optional polish never enter a wave; credible unknown remains blocking until its explicit investigation result; two read-only partitions may overlap in time only with disjoint ids/result paths and terminal fan-in. **Authorization:** disposable evidence writes and bounded configured inference only in the next task. **Safeguards:** deterministic code does not classify semantics; source remains read-only until mission handoff. **Restoration/Cleanup:** all partition sessions/processes/fixtures terminal and removed. **Validation:** focused playbook/admission/concurrency tests and replay.

- [x] 4.3 Capture and run-observe-correct the smallest complete configured happy path: one generic disposable project, complete inventory, one confirmed material-quality P1 with an optional-polish/P2 negative control, one frozen mission slice, actual propose/apply/archive/checkpoint, aggregate validation, changed-block re-review, fixed-and-verified closure, generated report, and terminal campaign completion. **Current Rung:** provider-free complete path plus configured semantic assignment. **Next Real Boundary:** installed configured end-to-end campaign MVP. **Blocker/Unblocker:** tasks 4.1-4.2 and clear live-attempt lanes; a failed configured/evaluator attempt preserves its bundle and blocks unchanged recapture until complete offline replay is terminal. **Observable Proof:** exact operator invocation reaches `complete`; source behavior changes only through the mission writer; the P1 is fixed/verified, P2 remains report-only, no optional-polish slice exists, current block/report/claim totals match, sessions/processes/writers are terminal, and cleanup completes. **Authorization:** bounded synthetic configured calls, disposable local Git/OpenSpec/local commit, no remote/protected effect. **Safeguards:** non-sensitive prompts, exact tool maps, finite budgets, no consumer target. **Restoration/Cleanup:** delete sessions/processes/fixture; retain immutable raw/evaluation bundles. **Validation:** provider-free replay twice plus focused affected suites. **Dependency-Chain Stop Condition:** do not expand semantic partitions or host integration until this complete happy path is green.

## 5. Complete Multi-Wave, Re-Review, And Failure Semantics

- [x] 5.1 Add multi-wave convergence and changed/impact-block invalidation so a verified first wave can discover a second current P1, preserve stable item ids, freeze a new mission only after checkpoint/re-review, and reach completion only from current block/item/wave/proof facts; verify a two-wave disposable campaign archives/checkpoints each slice once and never mutates a frozen wave. **Current Rung:** configured one-wave MVP. **Next Real Boundary:** configured two-wave current-candidate closure. **Blocker/Unblocker:** task 4.3; if impact mapping cannot prove a block current, mark it `needs-rereview` rather than infer. **Observable Proof:** wave 1 changes one block, re-review creates one new P1, wave 2 fixes it, all changed blocks become current, and neither mission nor report/model prose selects a successor. **Authorization:** bounded configured calls and disposable local Git/OpenSpec mutations. **Safeguards:** one mission/source writer at a time, checkpoints between waves, no P2/P3. **Restoration/Cleanup:** terminal sessions/processes and deleted fixture. **Validation:** two-wave capture/evaluator/replay and mission regression suites.

- [x] 5.2 Complete the semantic/control partition pack for confirmed P0, confirmed P1, P2/P3 report-only, optional polish, duplicate/falsified rows, unknown-material confirmed/falsified/still-unknown/owner-required, protected effect, dirty/unlisted-active change, immutable input, transient retry, budget exhaustion, explicit stop, unknown writer/cleanup, report drift, and final challenge; preserve one terminal row per declared `autonomous-work-campaign-v1` population member. **Current Rung:** multi-wave happy path. **Next Real Boundary:** complete configured/replay population. **Blocker/Unblocker:** task 5.1; each live failure has its own preserved-bundle replay gate and cannot authorize unchanged repetition. **Observable Proof:** only authorized confirmed P0/P1 rows mutate; all other partitions stop/report exactly; budget pause never completes; protected/owner/unknown states preserve completed waves; report drift is detected; final challenge can block an overbroad claim without inventing work. **Authorization:** bounded non-sensitive configured calls and disposable local effects only. **Safeguards:** no remote/destructive/credential/product-semantic action, no semantic helper scoring, no equivalent verdict-seeking reruns. **Restoration/Cleanup:** all sessions/processes/fixtures terminal and deleted; raw bundles retained. **Validation:** complete evaluator composition, privacy scan, two provider-free replays, exact partition inventory.

- [x] 5.3 Add aggregate validation/proof correlation, P0 critical-trigger routing, closure matrix, current report readback, and campaign terminal handoff; author only the smallest focused main-owned regression for reproduced accepted-outcome defects after runtime proof and leave triggered critical test authorship to task 8.1. **Current Rung:** complete semantic/control population. **Next Real Boundary:** host-supervised installed campaign. **Blocker/Unblocker:** task 5.2; any stale candidate/environment/proof/report/ownership row keeps only its closure lane non-complete. **Observable Proof:** a current complete fixture emits one terminal handoff with exact maximum claim; one stale validation/proof/block/report row prevents completion; a P0 critical consequence records mandatory test-only SDET pending rather than silently closing. **Authorization:** provider-free production/test/evidence edits. **Safeguards:** no aggregate-count proxy, no self-issued SDET evidence, no report authority. **Restoration/Cleanup:** test fixtures removed. **Validation:** focused closure/operation-gate/evidence-index tests and replay.

## 6. Add Campaign Readiness And Safe Windows Auto-Resume

- [x] 6.1 Extend project bootstrap templates/guidance and doctor schemas/checks/output with independent `campaignStatus` and `--require campaign`, covering definition/adapter/paths/validation/checkpoint/provider-budget/runtime/workflow/active-change/worktree/writer/mission/supervisor prerequisites; verify ordinary, qualification, unattended, and campaign statuses remain independent and diagnostics perform no registration/resume/mutation. **Current Rung:** complete portable campaign behavior. **Next Real Boundary:** doctor against fresh, static-mission-ready, campaign-ready, and unsafe disposable projects. **Blocker/Unblocker:** task 5.3 plus ownership closure for installer/doctor files from 1.1. **Observable Proof:** static mission may pass while campaign blocks; fully configured disposable project passes `npm run doctor -- --project <fixture> --require campaign`; escaping paths, missing budget/route, active change, unknown writer, and missing supervisor each block with stable rows/exits. **Authorization:** local source/tests and disposable bootstrap writes. **Safeguards:** doctor/init never install/register/start/resume, enable providers, or copy canonical workflows. **Restoration/Cleanup:** fixtures/backups removed. **Validation:** focused doctor/init/readiness suites and strict schema checks.

- [x] 6.2 Implement the portable campaign supervisor process for explicit registry entries, OpenCode readiness wait, campaign preflight/status/resume/stop invocation, process identity, bounded restart/backoff/log rotation, signal/stop behavior, and privacy-safe diagnostics without embedding phase/severity/wave/mission/completion semantics. **Current Rung:** campaign core and readiness. **Next Real Boundary:** supervisor process restart over a disposable registered campaign. **Blocker/Unblocker:** task 6.1; unknown prior controller/writer/session state suppresses restart. **Observable Proof:** supervisor resumes one safe paused-transient campaign, does not resume explicit-stop/owner/protected/budget/drift/unknown cases, never starts duplicate controller/mission, preserves stdout/stderr generations, and performs no project write itself. **Authorization:** proof-owned local processes/disposable projects. **Safeguards:** exact fixed argv, no shell/arbitrary commands/credentials, finite timeout/backoff, PID+creation/executable/ownership identity. **Restoration/Cleanup:** terminate only proof-owned processes and delete fixtures. **Validation:** focused supervisor tests and provider-free restart capture/evaluator.

- [x] 6.3 Add cohesive Windows campaign-supervisor registry/install/preview/check/repair/rollback modules and the smallest integration seam with the existing protected workstation/task lifecycle; keep campaign policy outside `opencode-workstation.ts`, protect installed material from unelevated mutation, and preserve shared OpenCode/Graphify/tray/project-launcher behavior. **Current Rung:** portable supervisor proven. **Next Real Boundary:** non-mutating Windows preview/check then installed disposable registration. **Blocker/Unblocker:** task 6.2 and terminal ownership transfer for current workstation edits; stop if implementation would add campaign state logic to the workstation god file instead of one cohesive extraction. **Observable Proof:** preview derives exact protected files/task/registry/runtime/rollback with no host mutation; invalid/escaping/drifted roots fail; check distinguishes source/installed/runtime identities; rollback plans only attributable supervisor artifacts. **Authorization:** repository Windows source/tests, then separately authorized local protected installation in task 6.4. **Safeguards:** no secret in repo/args/logs, no arbitrary path/argv, no mutation of shared service credentials or unrelated tasks. **Restoration/Cleanup:** preview fixtures removed; no live install in this task. **Validation:** focused Windows campaign-supervisor/installer/config tests and existing workstation critical suites.

- [x] 6.4 Run the installed Windows supervisor boundary in an isolated protected registration: preview, install, check, invoke the actual owner-logon Scheduled Task action path without reboot, recover one interrupted campaign/controller against the existing authenticated loopback runtime, exercise status/graceful stop/explicit-stop suppression, then rollback and verify shared workstation/project/campaign evidence remains intact. **Current Rung:** source-valid Windows adapter. **Next Real Boundary:** installed host re-entry used by the supported workstation. **Blocker/Unblocker:** task 6.3, current effective elevation/managed runtime identity, and separate recorded install/activation authorization; unknown host/process/task identity blocks destructive cleanup and qualification. **Observable Proof:** one safe campaign resumes exactly once after task action, unknown writer does not resume, stop becomes safe only after closure, logs are secret-free, rollback removes only matching supervisor installation/process ownership, and shared OpenCode/Graphify/tray/listeners plus campaign/mission/project evidence remain current. **Authorization:** local protected Windows task/files/process installation and rollback only; no remote/provider call beyond the already captured disposable campaign path. **Safeguards:** exact process/task identities, backup/manifest, no reboot, credential values never captured, no consumer source mutation. **Restoration/Cleanup:** terminal controller/supervisor, removed registration/task/derived files, preserved raw bundle; unknown cleanup is blocking. **Validation:** installed evaluator, workstation status/readiness, before/after manifests/listeners/tasks/processes, existing critical workstation tests. **Dependency-Chain Stop Condition:** do not claim host auto-resume until the actual installed task action and rollback are terminal and green.

- [x] 6.5 Restore bounded schema-v2 evidence binding for the retained task 6.4 corpus by extending the existing evidence parser with explicit compact inheritance for row identities and identical required/observed boundaries, preserving expanded rows and mismatch behavior, then materialize all retained files without deletion, relabeling, semantic inference, or exceeding 65,536 bytes. **Current Rung:** installed host complete but index representation blocked. **Next Real Boundary:** current evidence inventory with zero unindexed files. **Blocker/Unblocker:** task 6.4 runtime proof and incident FI-CAMP-004; any missing hash, changed historical raw, stale task row, unindexed file, or oversized index remains blocking. **Observable Proof:** compact and expanded fixtures resolve identically; explicit identity/boundary mismatches remain visible; the unchanged 355-file corpus materializes and resolves current source/installed lanes; after 6.4 and 6.5 close, inventory reports 20 checked tasks, 20 matching rows, zero stale/mismatch/unknown/unindexed rows, and no retention excess. **Authorization:** repository schema/parser/tests/spec/evidence edits only. **Safeguards:** no evidence deletion/move, no new format version, no inferred semantics, no weakened hash/size/retention gate. **Restoration/Cleanup:** immutable evidence stays in place; only the deterministic index is regenerated. **Validation:** focused evidence-index/inventory/operation-gate tests, strict selected OpenSpec, materialize/readback repetition, and git diff check.

## 7. Complete Installed Campaign Proof And Evidence Closure

- [x] 7.1 Compose the current configured semantic, two-wave mission, interruption/replay, and installed Windows supervisor lanes into one installed disposable operator campaign with bounded calls/effects; preserve Product Candidate, Runner, Evaluator, Environment, campaign/mission/session/process/checkpoint/report/effect identities and verify every proposal partition or explicit unknown appears in the raw/evaluated corpus. **Current Rung:** separately proven semantic, mission, and host lanes. **Next Real Boundary:** full installed operator path and terminal evaluator. **Blocker/Unblocker:** tasks 5.2-6.5; all live-attempt gates and cleanup must be clear, compatible candidate/environment identities must compose, and failed evaluator-only lanes replay offline rather than repeat live effects. **Observable Proof:** one operator invocation reaches complete through discovery, at least two waves, re-review, report, process/OpenCode recovery, and host re-entry; one separate protected-stop fixture remains owner-required; no duplicate writer/archive/checkpoint/session, no P2/P3 mutation, complete cleanup, no remote/protected product effect. **Authorization:** bounded synthetic configured inference, disposable local Git/OpenSpec/local commits, and recorded reversible local Windows supervisor operation only. **Safeguards:** non-sensitive inputs, finite budgets, no consumer/remote/deployment/release/credential change. **Restoration/Cleanup:** all proof sessions/processes/fixtures and installed supervisor artifacts terminal/removed; immutable evidence retained. **Validation:** terminal evaluator and two complete provider-free replays.

- [x] 7.2 Populate `evidence-index.json` for `autonomous-work-campaign-v1` with actual candidate/environment, baseline/candidate/replay/host lanes, one terminal observation per declared population member, cleanup/effect states, challenge status, unresolved observations, and maximum claim; leave absent facts `unknown`/`missing` and verify operation-gate/evidence readers resolve every referenced lane without semantic inference. **Current Rung:** complete installed raw corpus. **Next Real Boundary:** fresh critical writer-safety SDET. **Blocker/Unblocker:** task 7.1; incomplete, stale, unreadable, mismatched, or cleanup-unknown lanes cannot support their members. **Observable Proof:** evidence-index schema and lane resolution are green; each claim population member maps to current immutable refs; host/provider/project limits remain explicit. **Authorization:** local change evidence/document edits only. **Safeguards:** privacy-safe refs, no raw prompts/credentials/private paths, honest claim ceiling. **Restoration/Cleanup:** none. **Validation:** evidence-index/operation-gate focused suites and current replay digests.

## 8. Challenge Critical Safety, Validate, And Hand Off

- [x] 8.1 After current complete proof and accepted-scope completion, launch one fresh test-only `sdet-quality-engineer` for duplicate/overlapping source writers, unsafe host resume after reboot/process loss, stale/corrupt transition acceptance, protected-effect escape, wrong-project/registry mutation, stop/rollback killing unrelated processes or deleting evidence, and credential disclosure; main independently reproduce every row, correct only confirmed critical defects, and re-prove affected lanes before one fresh successor when the candidate materially changes. **Current Rung:** complete installed candidate with schema-valid claim evidence. **Next Real Boundary:** terminal critical-risk evidence. **Blocker/Unblocker:** tasks 7.1-7.2; unavailable eligible fresh SDET or unreadable candidate/evidence leaves the critical gate blocked and cannot be impersonated by main. **Observable Proof:** SDET returns exactly `critical-risks-reported | no-critical-risk | blocked` with Effective Model and candidate attribution; any authored test is test-only and targets one reachable critical incident; main disposition and affected proof are current. **Authorization:** fresh local test-only SDET in disposable/installed-safe boundaries. **Safeguards:** no production edits, live consumer mutation, remote/destructive action, or reviewer authority. **Restoration/Cleanup:** SDET fixtures/processes removed; host remains in task 6.4 restored state. **Validation:** terminal SDET report, main reproducer results, and green affected regression/proof lanes.

- [x] 8.2 Launch one fresh read-only `evidence-sufficiency-reviewer` for the exact current candidate and `autonomous-work-campaign-v1` partitioned claim after terminal SDET; main disposition every coverage/environment/oracle/closure row by correction, falsification, narrowing, or explicit unknown, and rerun only invalidated lanes before retaining the challenge. **Current Rung:** critical-challenged complete candidate. **Next Real Boundary:** project-native freeze validation. **Blocker/Unblocker:** task 8.1 and current evidence index; a reproduced broad-claim defect requires correction/narrowing and a fresh challenge, while optional non-critical risk remains parked. **Observable Proof:** challenge names actual candidate/model/environment/raw bundles, covers every declared population partition, and maximum claim does not exceed installed Windows/generic-fixture observations. **Authorization:** one read-only specialist launch. **Safeguards:** no mutation/authority/nested orchestration/user question. **Restoration/Cleanup:** none. **Validation:** main disposition and evidence-index challenge fields read back current.

- [x] 8.3 Run the portable apply gate, selected/all strict OpenSpec validation, focused campaign/mission/doctor/Windows/evidence suites, existing mission/workstation critical regressions, provider-free final replays, `npm test`, `npm run validate:strict`, proof inventory/readiness checks, instruction/code-quality inventories, and `git diff --check`; inspect exact status/stdout/stderr and candidate/worktree/cleanup identities, correct only accepted-outcome defects, and rerun affected lanes. **Current Rung:** terminal SDET and broad-claim challenge. **Next Real Boundary:** local implementation handoff; installation/activation for ordinary operator use remains separate. **Blocker/Unblocker:** tasks 8.1-8.2; any non-zero command, stale evidence, unknown cleanup, active overlapping owner, or campaign/mission/source mismatch keeps the exact lane blocked/unknown. **Observable Proof:** all applicable commands exit zero, replay digests are stable, every runtime/config-time source is cataloged, no unrecognized file is modified, and the final handoff records outcome, candidate, proof, evidence, architecture, diagnostics, critical SDET, validation, limitations, rollback, cleanup, and external-operation state. **Authorization:** provider-free validation/readback only. **Safeguards:** no archive, commit, push, release, deployment, consumer campaign, permanent supervisor activation, or unrelated cleanup. **Restoration/Cleanup:** only proof-owned disposable outputs are removed; retained evidence remains bounded. **Validation:** complete command/result matrix and scoped diff review.
