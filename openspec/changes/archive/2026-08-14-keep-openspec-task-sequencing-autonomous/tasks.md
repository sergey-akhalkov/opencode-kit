## 1. Evidence And Scope

- [x] 1.1 Characterize the reported task-range owner menu against current loaded authority and identify the smallest active conflict. **Current Rung**: observed live incident plus fresh configured-session classification. **Next Real Boundary**: task 2.2 paired candidate session. **Blocker/Unblocker**: none. **Observable Proof**: the incident, source line ownership, and fresh current-source behavior distinguish an implementation-sequencing ambiguity from an accepted-outcome owner decision. **Authorization**: local reads and standing bounded configured-provider inference. **Safeguards**: no tools or target-project effects in proof prompts. **Restoration/Cleanup**: disposable sessions/root only. **Expected Immutable Evidence**: OpenCode/model/source identity, sessions, action outputs, and strategy record. **Observed**: `history.md` records OpenCode 1.18.18, two current-source sessions, autonomous focused-slice selections, the user-provided longer-session failure, and the conflicting generic `scope` ambiguity rule.
- [x] 1.2 Author the bounded proposal, design, delta requirements, strategy history, and dependency-ordered implementation controls. **Current Rung**: repository planning artifacts. **Next Real Boundary**: task 2.2. **Blocker/Unblocker**: task 1.1 evidence; satisfied. **Observable Proof**: artifacts preserve explicit user task limits and protected boundaries while requiring autonomous task sequencing. **Authorization**: local reversible planning writes. **Safeguards**: no archived history rewrite, permission change, install, activation, or external action. **Restoration/Cleanup**: source-only rollback. **Expected Immutable Evidence**: strict selected-change validation and proposal/apply gates.

## 2. Runtime Instruction Behavior

- [x] 2.1 Narrow the canonical ambiguity rule and align OpenSpec apply plus completion-arbiter surfaces so pending in-scope task order, task-range batching, optional review timing, and current-cycle size are orchestrator-owned; select the smallest dependency-valid slice reaching the earliest safe real boundary while preserving explicit user limits and exact owner boundaries. **Current Rung**: observed source conflict. **Next Real Boundary**: task 2.2 fresh configured primary session. **Blocker/Unblocker**: tasks 1.1-1.2 complete. **Observable Proof**: focused diff contains one canonical distinction and proportional apply/arbiter wording with no broad autonomy override. **Authorization**: local repository edits only. **Safeguards**: accepted semantics/protected actions remain owner-controlled; no provider/config/permission route change. **Restoration/Cleanup**: source-only rollback. **Expected Immutable Evidence**: candidate identity, changed-path inventory, focused source diagnostics, and contract-ready marker list. **Dependency-Chain Stop Condition**: any wording that authorizes work past an explicit user task limit or protected boundary returns the candidate to `development` before runtime proof. **Observed**: candidate `038e00a3edb7f7f2b9633bb33bd4ecbaf5d8569a`; four runtime artifacts changed; focused contracts passed `68/68` after the SDET-authored oracle; instruction budget passed without raising limits at catalog `100515/100519` and global authority `16646/16646`; strict selected-change validation and `git diff --check` passed.
- [x] 2.2 Invoke the actual fresh loaded OpenCode primary entry point with paired synthetic scenarios: the reported multi-batch task state without an owner boundary, and the same sequencing state where every advancing slice reaches an exact protected action. **Current Rung**: candidate source and deterministic markers. **Next Real Boundary**: none for target-project behavior; this change proves loaded instruction behavior only. **Blocker/Unblocker**: task 2.1 candidate. **Observable Proof**: the first scenario selects tasks 2.1-2.2 without an owner question; the second autonomously prepares safe work but stops only at the protected action. **Authorization**: standing bounded configured-provider inference. **Safeguards**: disposable empty root, no target project, credentials, remote/destructive action, install, activation, release, or protected effect; raw output retained. **Restoration/Cleanup**: delete disposable sessions/root and verify no target mutation. **Expected Immutable Evidence**: exact invocation, OpenCode/config/model/candidate identity, exit, stdout/stderr, action classification, and cleanup. **Dependency-Chain Stop Condition**: a red autonomous or owner-boundary oracle returns the candidate to `development` for one causal correction before SDET. **Observed**: installed OpenCode `1.18.18`, current candidate `038e00a3edb7f7f2b9633bb33bd4ecbaf5d8569a`, `openai/gpt-5.6-sol` `xhigh`, session `ses_ffe1fa6f2ffe09dQlY5XMrsTla`, exit `0`; `task-sequencing` returned `continue_next` with tasks 2.1-2.2 and no owner boundary, while `protected-action` returned `owner_required` only for controller credentials and physical contact. No tool or target-project effect occurred; the session and disposable root were deleted. Runtime Proof restores `MVP` after the Priority 2 correction.
- [x] 2.3 Bound hidden-arbiter route initialization and extend the existing installed question proof with a mixed protected-menu scenario. **Current Rung**: real question tool reached; guard failed before arbiter inference on transient fresh-instance route absence. **Next Real Boundary**: fresh installed OpenCode server, real question tool, configured hidden arbiter, and terminal owner-required state. **Blocker/Unblocker**: task 2.2 primary lane and SDET-confirmed mixed-menu invariant; route preflight now green but production settle is missing. **Observable Proof**: provider-free `--help` is effect-free; a fresh cold server resolves route within the finite settle, invokes the hidden arbiter, leaves the mixed question unanswered in owner-required state, and deletes all sessions/root. **Authorization**: local disposable processes plus standing bounded configured-provider inference. **Safeguards**: only question tool enabled; no credentials, target project, file/task/bash/external tool, protected effect, install, activation, remote, or release action; cancellation creates no child/model call. **Restoration/Cleanup**: stop proof-owned server, delete sessions/root and temporary directory, preserve privacy-safe stdout/stderr/log facts. **Expected Immutable Evidence**: candidate/runner/server/config/model identities, first failed bundle, provider-free exact resolver preflight, final result, server diagnostics, and cleanup. **Dependency-Chain Stop Condition**: another evidence-only live failure blocks repetition until complete preserved output and offline/provider-free terminal path inspection identify the next unlock condition. **Observed**: product candidate `fd38d010a742c3c3f9dbcc2669fd9b5cacecd083`, runner `6ff12554e99c78d75fe03cbcfe1e967ccd2a166a`, OpenCode `1.18.18`, server run `64d8c7e6`, root `ses_ffdf3f30affemMzRx9b9DH5J3m`, hidden child `ses_ffdf39d95ffeKc4Isk3EZyQl5y`; result had `auditStatuses:["owner-required"]`, `guardState:"owner-required"`, three offered labels, zero autonomous refs, zero human replies, no selected/projected answer, one running question, and cleanup complete. Focused contracts passed `68/68`, guard tests `33/33`, diagnostics and budget passed. Server, sessions, child, and empty disposable root were removed; Runtime Proof restores `MVP`.

## 3. Independent Critical Testing

- [x] 3.1 After current Runtime Proof and accepted-scope completion, dispatch one fresh test-only SDET to assess reachable critical regression risk and author only the smallest missing focused oracle in existing test paths. **Current Rung**: current MVP candidate. **Next Real Boundary**: project focused contract and guard test runners. **Blocker/Unblocker**: tasks 2.1-2.3 green. **Observable Proof**: SDET returns `critical-risks-reported | no-critical-risk | blocked`; test evidence requires autonomous sequencing, explicit-user/protected-action stops, every-advancing-option arbitration, and bounded route settling/cancellation. **Authorization**: local test-only writes in exact existing paths. **Safeguards**: no production/instruction/OpenSpec edits, nested agents, credentials, or external actions. **Restoration/Cleanup**: close the writer attempt and inspect its exact diff/report. **Expected Immutable Evidence**: child task identity, Effective Model, risk matrix, changed test paths, and focused test output. **Dependency-Chain Stop Condition**: only a main-confirmed reachable critical/non-deferrable defect earns a production correction and fresh proof; otherwise SDET terminates for this root. **Observed**: fresh task `ses_ffdf0baf1ffeV0uWofuh4SewNb`, Effective Model `xai/grok-4.6`, returned `no-critical-risk` for current candidate `fd38d010a742c3c3f9dbcc2669fd9b5cacecd083`; it added in-flight abort and hung-lookup deadline oracles only in `tools/test-session-completion-guard.ts`. Main replay passed guard tests `35/35` and contracts `68/68`. This first precondition-valid no-confirmed-critical result permanently terminates SDET for the root.

## 4. Qualification And Handoff

- [x] 4.1 Run focused contracts, strict library validation, full tests, strict OpenSpec validation, operation gates, instruction/runtime inventory, `git diff --check`, and replay only evidence lanes invalidated by later mutation. **Current Rung**: MVP plus terminal SDET. **Next Real Boundary**: local stable handoff. **Blocker/Unblocker**: task 3.1 terminal. **Observable Proof**: applicable commands exit zero, no unchecked accepted implementation task remains except handoff/retrospective controls, and no known reachable critical/non-deferrable defect remains. **Authorization**: local validation only. **Safeguards**: no commit, push, archive, install, activation, or release. **Restoration/Cleanup**: disposable proof resources removed; worktree remains reviewable. **Expected Immutable Evidence**: commands, exits, summaries, diagnostics, candidate identity, and residual limitations. **Observed**: focused contracts `68/68`, guard tests `35/35`, strict OpenSpec and repository validation, full tests, pre-push validation, permissions, instruction/source inventories, read-only operation gates, and `git diff --check` passed. Budgets remained catalog `100519/100519` and global authority `16646/16646` without changing maxima.
- [x] 4.2 Produce the local stable handoff with outcome, changed artifacts, Runtime Proof, Candidate Reference, SDET terminal reason, validation, known non-critical limitations, restart requirement, rollback, and external-operation state. **Current Rung**: fully validated RC candidate. **Next Real Boundary**: future new OpenCode sessions loading the changed config-time artifacts. **Blocker/Unblocker**: task 4.1 green. **Observable Proof**: one complete handoff reports `Development-Stage: stable` and `Stable Candidate: RC<n>` only if all prior gates are current. **Authorization**: local reporting only. **Safeguards**: no archive, commit, push, install, activation, or release. **Restoration/Cleanup**: N/A - reporting only. **Expected Immutable Evidence**: final response and task evidence. **Observed**: `implementation-evidence/stable-handoff.md` freezes and promotes `RC1` with current proof, validation, residual-risk, rollback, restart, and external-operation evidence.

## 5. Final History Retrospective

- [x] 5.1 After every other currently known task is complete, analyze the complete `history.md` once using the existing compaction matrix: rows `Quality`, `Cycle Speed`, and `Token Economy`; columns `Working Repository` and `opencode-kit`; each cell records evidence, smallest cheap improvement, expected benefit, and cost/risk, or `none`. Classify evidence-backed candidates with `Impact Horizon`, `Concrete Consumers`, `Execution Class`, `Earliest Safe Point`, `Invalidated Evidence`, and `Observable Payback`; apply the canonical admission/deferral, target ownership, authority, and record-field rules. Append and immediately execute every admitted current-consumer improvement under `## Session-Derived Improvements`, preserve no-current-consumer candidates as non-checkbox deferred history records, or record `none` without inventing work. Apply, archive, compaction, and generated tasks must not create or rerun this final analysis. **Current Rung**: complete change history. **Next Real Boundary**: any admitted improvement's stated proof, otherwise none. **Blocker/Unblocker**: tasks 1.1-4.2 complete. **Observable Proof**: persisted matrix result plus admitted task IDs, deferred record IDs, or `none`; every admitted task then has its own proof and validation. **Authorization**: local reversible analysis and any separately classified admitted task authority. **Safeguards**: no generic improvement, scope expansion, protected action, or recurrence. **Restoration/Cleanup**: source-only for any admitted local edit. **Expected Immutable Evidence**: final `history.md`, task state, and validation affected by admitted work. **Observed**: the one-shot matrix is recorded in `history.md`; no current-consumer improvement was admitted, and one evidence-backed no-current-consumer candidate remains deferred as `DIC-1`.
