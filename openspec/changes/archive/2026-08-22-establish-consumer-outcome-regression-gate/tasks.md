## Shared Execution Envelope

All tasks use disposable local repositories, exact source/environment identities, one writer at a time, create-new bounded evidence, and attribution-safe cleanup. Provider-free work makes no configured-provider, install, activation, remote, or consumer-project call. The only configured-provider lane in this change is task 5.1, limited to six non-sensitive primary requests for first-baseline establishment after every provider-free prerequisite and cleanup gate is green. A later model-facing candidate comparison has a separate explicit maximum of twelve requests and is not part of this change's live proof. Existing active-change bytes and unrelated work remain untouched.

## 1. Establish Contracts And Ownership

- [x] 1.1 Reconcile current writer and file ownership for `package.json`, CI, validators, proof inventory, shared proof libraries, and model-facing governed paths before production edits; select one explicit accepted staged baseline source rather than the dirty checkout. Observable completion: a source/write manifest identifies no live overlapping writer, the baseline ref and governed-path digest are readable, and no existing active-change byte is changed. Validation: scoped Git/source readback and the existing project inventory terminate with attributable output.
- [x] 1.2 Implement exact schemas for scenario, candidate request, source/environment identity, sample, raw evidence, baseline pointer, friction vector, and evaluation result plus the reviewed two-scenario/three-sample manifest and contained fixture seeds. Use profile `quality-independent`, baseline pointer `config/consumer-outcome-baseline.json`, the design's exact governed paths, 524288-byte sample limit, and 8388608-byte capture limit. Observable completion: the Ordinary Small greeting option and OpenSpec `add-json-output` seeds normalize deterministically while missing/extra fields, path escape, unresolved commands, invalid bounds, or helper-inferred semantics fail before effects. Validation: `npm run test:focused:consumer-outcome` provider-free contract and containment cases pass.
- [x] 1.3 Add the effect-free public CLI/help and mode preflight without capture behavior. Observable completion: `--help` and `-h` describe inputs, effects, evidence, bounds, cleanup, and baseline/capture/replay/evaluate/gate modes, exit zero, and leave the before/after project/process/session manifest unchanged. Validation: focused CLI tests and direct help invocations pass.

## 2. Prove The Provider-Free Evaluator

- [x] 2.1 Implement outcome, validation, proof, permission, forbidden-effect, diagnostics, and cleanup hard gates plus stable three-sample median friction evaluation for explicit `no-regression` and `improvement` expectations. Observable completion: controlled fixtures pass baseline establishment, no-regression, and strict improvement while exact fixtures fail outcome/safety regression, critical unknown, friction regression, and no-strict-improvement. Validation: focused evaluator matrix passes without OpenCode sessions or provider calls.
- [x] 2.2 Implement bounded privacy-safe bundle reading, hash/schema verification, stable replay, and evaluator-only invalidation. Observable completion: two replays of the same bundle have identical result digests and zero live calls; mutation, missing files, critical truncation, secret/private-path markers, and cleanup uncertainty fail closed without rewriting evidence. Validation: replay/privacy/overflow/tamper fixtures pass and fixture cleanup is complete.

## 3. Reach The Installed Capture Boundary

- [x] 3.1 Reuse the maintained OpenCode proof client, model profile, portable process, and existing source-staging owner to implement isolated fixture/session/process capture with alternating `B1,C1`, `C2,B2`, `B3,C3` ordering and one request maximum per sample. Do not duplicate a private source-staging mechanism. Observable completion: an installed local effect-free provider fixture records comparable baseline/candidate arms, exact calls/events/files/validation/cleanup, blocks environment mismatch and request overflow, and leaves no session/process/fixture. Validation: installed provider-free capture/replay exits zero with complete cleanup.
- [x] 3.2 Implement failure ownership and diagnostic preservation for model, tool, validation, evidence, timeout, and cleanup boundaries. Observable completion: each injected failure retains sample/source/environment identity, exit status, bounded stdout/stderr, original cause, terminal cleanup state, and exact unblock condition; an unknown live writer prevents the next sample. Validation: focused failure matrix and preserved-bundle replay pass.

## 4. Bind Baseline And Current Candidate

- [x] 4.1 Implement immutable first-baseline establishment and explicit reviewed baseline replacement metadata. Observable completion: a green baseline emits only `baseline-established`; capture/evaluation cannot mutate the pointer; a reviewed replacement preserves prior reference and reason; stale baseline/environment/evaluator identities block. Validation: provider-free baseline lifecycle fixtures pass with byte-for-byte no-mutation assertions.
- [x] 4.2 Implement the explicit governed-source manifest, separate exact candidate-request expectation, and provider-free current-candidate gate. Observable completion: unchanged governed bytes plus green baseline replay return `baseline-current`; changed bytes without matching evidence return `stale-evidence`; matching no-regression/improvement candidate evidence returns its exact pass result; unlisted or unreadable governed inputs remain blocked rather than ignored. Validation: focused freshness/source-manifest/expectation fixtures pass.
- [x] 4.3 Add package scripts, proof inventory documentation, strict validator routing, and one provider-free gate step to the existing Ubuntu `.github/workflows/validate.yml` job without enabling configured-provider capture or changing Windows/template CI. Observable completion: the CI-equivalent command proves schemas, negative fixtures, replay, privacy, freshness, and current gate with zero provider/session/install/remote effects; documentation names explicit capture authorization and evidence refresh. Validation: `npm run validate:strict`, focused package-script tests, and existing-workflow readback pass.

## 5. Establish The First Real Baseline

- [x] 5.1 After tasks 1-4 are green and the live-attempt gate is clear, capture the explicit accepted baseline through installed OpenCode for both scenarios and three samples each, using at most six configured-primary requests. Observable completion: all six samples satisfy outcome/proof/validation/safety/privacy/cleanup contracts, environment identities match, replay is terminal, and the result is `baseline-established` with no improvement claim. Validation: `npm run proof:consumer-outcome -- --mode baseline ...`, provider-free replay, source/evidence readback, exact provider-call count, and cleanup all pass. Green proof establishes `Development-Stage: MVP` for this capability only.
- [x] 5.2 Run the current provider-free gate against the retained baseline and controlled no-regression/improvement candidate fixtures, then add only the smallest focused regression oracle for any reproduced accepted-outcome defect. Observable completion: baseline-current and both positive expectation paths pass; every required rejection remains covered without another configured-provider call. Validation: focused tests and `npm run proof:consumer-outcome -- --mode gate` pass.

## 6. Privacy Challenge And Complete Validation

- [x] 6.1 Dispatch one fresh test-only SDET against the runtime-proven candidate to challenge only reachable credential/private-prompt/path disclosure, out-of-envelope fixture/session access, and cleanup/liveness escape. Observable completion: SDET returns exactly `critical-risks-reported`, `no-critical-risk`, or `blocked` with Effective Model and candidate attribution, modifies no production artifact, and authors only a smallest critical test oracle if necessary.
- [x] 6.2 Main independently reproduce and disposition every SDET row; correct only a confirmed critical/non-deferrable defect, then regain installed baseline proof before any eligible fresh SDET. Observable completion: every privacy/envelope claim is reproduced, disproved, or shown unreachable and no reviewer result alone authorizes mutation. Validation: affected focused and installed proof lanes pass on the corrected candidate.
- [x] 6.3 Run the same-candidate full gate: `npm test`, `npm run validate:strict`, consumer outcome replay/gate, proof inventory checks, `openspec validate establish-consumer-outcome-regression-gate --strict`, and `openspec validate --all`. Observable completion: this selected change and canonical specs are green, retained evidence is bounded/privacy-safe, no configured-provider call occurs during final validation, and handoff records baseline/candidate/environment identities, limitations, rollback, and no external operations. If `--all` still fails only on an unrelated active change such as `reduce-workflow-ceremony`, preserve that exact external blocker without editing it from this change and keep this candidate at MVP until its owner restores the repository-wide gate.
