# Strategy History

## 2026-08-21 - Reject one umbrella remediation change

- **Objective:** Convert the full repository audit into implementation-ready OpenSpec work.
- **Approach:** Consider one change spanning consumer outcome proof, default-profile reduction, Windows CI, OpenSpec ownership repair, runtime bounds, and code/evidence reduction.
- **Evidence:** The audit separated six independently owned outcomes; active changes already modify Restart, instruction workflow integrity, package/proof files, and runtime behavior.
- **Outcome:** Rejected before artifact creation.
- **Reason:** One umbrella change would overlap active owners, delay the first real signal, and make proof and rollback inseparable across unrelated risk domains.
- **Do-Not-Repeat Condition:** Do not add Windows, runtime optimization, instruction reduction, active-change repair, or general cleanup to this consumer measurement change merely because the audit found them.
- **Evidence-Based Retry Condition:** Reconsider grouping only if a future architecture proves one shared behavior owner, one proof boundary, and one rollback for those outcomes.

## 2026-08-21 - Reject single-sample mandatory improvement

- **Objective:** Create the cheapest gate that proves workflow improvement.
- **Approach:** Compare one baseline and one candidate run per scenario and require every current change, including the measurement tool itself, to show one strict lower friction count.
- **Evidence:** The measurement change does not alter model-facing workflow, so it cannot honestly improve over identical behavior; one model sample is sensitive to nondeterministic tool choice and ordering.
- **Outcome:** Rejected while drafting proposal and specs.
- **Reason:** It would either fabricate improvement for the gate itself or produce a noisy, low-confidence acceptance result.
- **Do-Not-Repeat Condition:** Do not call baseline establishment an improvement and do not reduce the maintained comparison to one sample while model variability remains unqualified.
- **Evidence-Based Retry Condition:** A one-sample gate may be reconsidered only if the selected runtime exposes a verified deterministic seed and identical repeated behavior for both maintained scenarios.

## 2026-08-21 - Select baseline bootstrap plus paired expectation modes

- **Objective:** Establish the smallest trustworthy measurement capability without expanding into optimization work.
- **Approach:** Add one new capability with explicit `baseline-establishment`, `no-regression`, and `improvement` results; use two workflow shapes, three alternating paired samples per arm, hard outcome/safety gates, median exact friction vectors, provider-free replay, and one bounded active evidence set.
- **Evidence:** Existing proof infrastructure already supplies installed session routing, model profiles, source hashes, portable execution, redaction, replay patterns, and cleanup contracts; the audit showed the missing owner is a current-candidate consumer comparison rather than another general workflow.
- **Outcome:** Selected for proposal, spec, design, and tasks.
- **Reason:** It creates the measurement prerequisite for later slim-default and workflow optimizations while keeping provider cost bounded and avoiding active capability overlap.
- **Do-Not-Repeat Condition:** Do not add semantic scoring, automatic baseline promotion, implicit expectation inference, unbounded samples, or per-attempt evidence trees.
- **Evidence-Based Retry Condition:** Revise sample count, metrics, or scenario scope only when retained captures show a concrete reproducibility or coverage failure that the current exact contracts cannot distinguish.

## 2026-08-21 - Reconcile cold-context readiness contradictions

- **Objective:** Make the planning set executable without guessing provider cost, CI ownership, expectation source, or repository-wide validation authority.
- **Approach:** Apply one independent read-only implementation-readiness review, then align the current live lane to six baseline requests, reserve twelve only for a later explicit matched candidate, bind CI to the existing Ubuntu workflow, define exact profile/pointer/path/bounds/fixture seeds, separate candidate expectation from the maintained manifest, and keep unrelated `--all` failures owner-scoped.
- **Evidence:** The review identified contradictory six-versus-twelve bounds, ambiguous generic CI wording, and an impossible requirement to repair `reduce-workflow-ceremony` despite this change's non-goals; it also identified locally resolvable seed and expectation ambiguity.
- **Outcome:** Proposal, spec, design, and tasks were synchronized before implementation.
- **Reason:** Cost authorization and final validation cannot remain implicit, while seed details are agent-owned reversible design decisions that should be resolved now rather than deferred to a cold implementer.
- **Do-Not-Repeat Condition:** Do not widen task 5.1 beyond six provider calls, add Windows/template CI, infer expectation, or mutate an unrelated active change to make this change appear green.
- **Evidence-Based Retry Condition:** Revisit these boundaries only if selected strict validation, provider-free fixtures, or first-baseline evidence proves that the exact current envelope cannot produce the required observations.

## 2026-08-21 - Extend existing proof owners for provider-free gate

- **Objective:** Implement the consumer-outcome measurement capability without duplicating staging or proof-client owners.
- **Approach:** Reuse `portable-process`, `model-profile`, and proof cleanup; stage governed bytes with `git show` from explicit `HEAD` rather than dirty checkout or a copied private stage-source; keep semantics in reviewed manifest/fixture seeds.
- **Evidence:** `source-write-manifest.json` records baseline `ca43ba441c92dd56f2c751f4583aef41e5c765de`, 102 governed paths, digest `669af62ac54df36f78d84e99b7bbef388e10d5227d25fd45eae56ce86320a3e5`, and no live exclusive writer. `npm run test:focused:consumer-outcome` prints `OK: consumer outcome tests=13`. `npm run validate:strict` exits 0.
- **Outcome:** Selected `extend` of repository proof infrastructure. Cross-project discovery is `not-applicable`.
- **Reason:** Existing owners already cover process, profile, redaction, and cleanup; a new package would add lifecycle cost without changing the two scenarios.
- **Do-Not-Repeat Condition:** Do not copy `pre-escalation-recovery` staging, infer expectation, or treat the dirty worktree as the baseline identity.
- **Evidence-Based Retry Condition:** Reopen staging extraction only if capture against a staged ref cannot materialize the reviewed governed path list.

## 2026-08-21 - Establish first accepted baseline and park repo-wide validate --all

- **Objective:** Prove the installed baseline and complete same-candidate validation without claiming improvement or repairing unrelated changes.
- **Approach:** Six configured-primary captures against HEAD, provider-free sanitize of residual `Users\<user>` titles into a create-new bundle, reviewed pointer write, replay/gate, one SDET, then full local validation.
- **Evidence:** Capture result `baseline-established`, 6 provider calls, pointer mutated only by reviewed seed. Two replays digest `f29316b16ece153443df1b9fabe4846fa2a603ff7020f3391720cd325d63843b`. Gate `--source-ref HEAD` returns `baseline-current`. SDET `Action: no-critical-risk` (Effective Model `xai/grok-4.6`). `npm test` 0, `validate:strict` 0, `openspec validate establish-consumer-outcome-regression-gate --strict` 0. `openspec validate --all` fails only on `reduce-workflow-ceremony`.
- **Outcome:** Selected. This change remains MVP because the repository-wide `--all` gate is owned elsewhere.
- **Reason:** Task 6.3 forbids editing the unrelated active change to make this candidate appear green.
- **Do-Not-Repeat Condition:** Do not recapture the six baseline samples or mutate `reduce-workflow-ceremony` from this change.
- **Evidence-Based Retry Condition:** Recapture only if a new privacy/outcome defect is reproduced on the retained bundle or the accepted staged source identity changes.
