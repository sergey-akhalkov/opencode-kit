## 1. Establish The Unchanged-Source Baseline

- [x] 1.1 Materialize and validate the change's reviewed `ownership.json` and `evidence-index.json` records with the existing deterministic inventory owners, declaring `CLC-001`, exact capability/requirement/write ownership, a planning dependency on `establish-practice-owner-agents`, baseline and candidate evidence lanes, mutation state, and task digest without inferring architecture quality; run the current apply operation gate and keep mutation disabled until the shared Practice Owner contract and `architecture-and-change-locality` route are runtime-proven, both records parse, ownership is conflict-free, and pre-baseline lanes are explicitly incomplete rather than absent.
- [x] 1.2 Extend the existing `agent-tooling-ergonomics` authoring proof path with reviewed change-locality fixtures, initial/follow-up turns, exact identity/effect/cleanup capture, and scenario oracles for `CLC-001`; record the runner responsibility map and keep or apply the design's cohesive scenario-data extraction fallback, then verify `npm run proof:tooling-ergonomics -- --help` is effect-free and provider-free preflight over every new scenario exits zero without changing loaded global instructions.
- [x] 1.3 Exercise the proof path through the installed OpenCode authoring entry point against the unchanged global source for every `CLC-001` scenario, preserving complete baseline bundles whose commands, current behavior, changed-file diffs, validation, follow-up locality facts, effective model/variant/permissions, and cleanup are all inspectable and green before any target instruction mutation.
- [x] 1.4 After the baseline happy path is observed, add the smallest focused automated coverage for scenario schema, stable ordering, source/environment mismatch rejection, negative-control facts, follow-up evidence, replay, privacy bounds, and cleanup failure; run the focused test directly and confirm injected invalid evidence fails with cause-preserving diagnostics.

## 2. Implement Canonical Ownership And Role Routing

- [x] 2.1 Consolidate the pay-as-you-go next-change principle into the existing single-responsibility/cohesion owner in `global/principles-of-work.md` and replace overlapping context-efficient-architecture text in `global/AGENTS.md` with the concise main-decision/material-owner ladder; verify a targeted read retains every existing `split-or-justify`, navigation, testability, safety, proof, no-wrapper, and unrelated-debt protection, invokes no owner for zero-trigger work, and never transfers the concrete decision to the owner.
- [x] 2.2 Add the exact `architecture-and-change-locality` Practice Owner trigger/observation/exclusion delta to `openspec-architecture-reviewer` and the applicable responsibility/change-axis preservation delta to `implementation-worker`; keep `code-quality-reviewer` in `simplicity-and-reuse`, and verify instruction/agent inventory shows no new architect agent, no owner mutation/decision authority, no owner call in negative controls, and no duplicated complete principle.
- [x] 2.3 Extend the engineering-quality contract markers and focused fixtures for canonical principle ownership, main decision accountability, registered owner routing, worker preservation, and forbidden autonomous-architect language; run `npm run test:focused:contracts` and prove each removed-marker fixture fails with its exact surface while no validator infers triggers, scores patterns, file counts, or architecture quality.
- [x] 2.4 Run `npm run instruction:budget -- --format json` and the provider-free instruction/proof preflights, then verify the combined committed startup authority remains within its existing boundary and all candidate source, fixture, permission, and command identities match the preserved baseline comparison envelope.

## 3. Prove Candidate Authoring Behavior

- [x] 3.1 Capture the candidate through the same installed authoring path, prompts, fixtures, model, variant, permissions, turn order, and environment as the preserved `CLC-001` baseline; verify every current behavior and runtime command remains green, material scenarios invoke exactly `openspec-architecture-reviewer` for a bounded practice observation before main's decision, accepted follow-ups stay inside the named code owner or narrow boundary, negative controls gain no speculative mechanism or owner call, and cleanup is terminal.
- [x] 3.2 Replay and evaluate the complete baseline/candidate bundles provider-free, then record main's scenario-by-scenario semantic disposition from preserved decisions and diffs without using pattern names, file counts, or deterministic scoring as acceptance; narrow or reject candidate wording for any failed current-outcome, safety, locality, directness, role, or context-cost oracle and recapture only evidence invalidated by a corrected candidate.
- [x] 3.3 Run the candidate's nearest loaded-source canary in a fresh disposable OpenCode session and verify the registered Practice Owner owns only trigger applicability and observation, `main` owns and dispositions the direct-code-or-seam decision, a delegated slice receives the exact boundary without gaining architecture authority, zero-trigger work launches no owner, and no runtime loader collision or private instruction content appears in the evidence.

## 4. Validate The Complete Planning Outcome

- [x] 4.1 Run the focused proof-runner test, `npm run test:focused:contracts`, `npm run validate:strict`, and `npm test`; preserve exit status, stdout/stderr, relevant failures, and artifact paths, and correct only candidate-owned defects before rerunning affected checks.
- [x] 4.2 Run `openspec validate improve-change-locality-guidance --strict` and the current apply operation gate, verify every `library-change-ready-sdlc` and `library-instruction-artifacts` scenario maps to current proof or focused validation including the owner trigger, main decision, and zero-trigger negative control, and record the `CLC-001` maximum claim plus any known non-critical limitation without claiming universal architecture quality.
- [x] 4.3 Confirm no critical-risk SDET trigger is reachable, no installation/activation/commit/push/release/remote effect occurred, running sessions require a fresh load before using changed instructions, and hand off `Outcome: working | blocked | unknown` with exact baseline/candidate evidence and validation paths.
