## 1. Controller contract

- [x] 1.1 Add public `stop` to help/`publicModes` and wrap `stopManagedServer()` with the same elevation and fail-closed identity rules as Restart. Keep `--help` effect-free. **Current Rung:** installed controller without `stop`. **Next Real Boundary:** `node tools/windows/opencode-workstation.ts --help` and unknown-mode rejection. **Blocker/Unblocker:** none. **Observable Proof:** help exits `0`, lists `stop`, and changes no file/task/process. **Authorization/Safeguards:** repository source only. **Restoration/Cleanup:** none. **Immutable Evidence:** help stdout and exit. **Validation:** `node --check tools/windows/opencode-workstation.ts`.

## 2. Quiet launchers and maximized attach

- [x] 2.1 Implement the protected windowless invoker, hidden `elevateInvocation`, rewritten shortcut contract that accepts the previous node-targeted shortcuts as non-drift during repair, windowless serve task action, and protected Alacritty `startup_mode = "Maximized"`. **Current Rung:** 1.1 help. **Next Real Boundary:** stopped repair on this host, then actual Start and one project `.lnk`. **Blocker/Unblocker:** 1.1; server must be stopped before repair. **Observable Proof:** Start creates a healthy listener and zero new consoles; `OpenCode - opencode-kit.lnk` creates exactly one maximized Alacritty attached to that listener. **Authorization/Safeguards:** local admin repair; no provider/model call; no target-repo write. **Restoration/Cleanup:** close only proof-owned Alacritty; leave or restore one healthy server as later tasks need. **Immutable Evidence:** shortcut/task readback, process/window inventory, attach argv, health. **Validation:** installed Status plus `node --check`.

## 3. Tray lamp and logon start

- [x] 3.1 Add the derived tray helper, green/red icons, Highest AtLogon tray task, public Stop used by Exit, and tray menu Restart/Exit. Tray polls secret-free state only. On tray start, invoke controller `start`. **Current Rung:** 2.1 quiet Start/attach. **Next Real Boundary:** actual tray host start (task start equivalent to AtLogon) after Exit. **Blocker/Unblocker:** 2.1. **Observable Proof:** tray shows `opencode-server` green while healthy; Exit stops the managed tree, lamp stays red; Restart replaces PIDs and returns green; next tray-task start after Exit starts the server again; no password in tray argv. **Authorization/Safeguards:** same local loopback/task envelope. **Restoration/Cleanup:** leave one healthy server plus tray after the last green proof, or document the exact stopped/red state. **Immutable Evidence:** tray process/task identity, icon/tooltip observation, before/after PIDs, listener absence after Exit. **Validation:** installed Status and independent task/process inventory.

- [x] 3.2 Change tray Exit to stop the server and close the tray host; make Restart non-blocking so the lamp blinks red/amber during replacement and returns to steady green. **Current Rung:** installed tray from 3.1. **Next Real Boundary:** actual tray Restart and Exit through the command hook and live icon/state file. **Blocker/Unblocker:** none. **Observable Proof:** Restart writes `restarting` then `green` with a new listener PID; Exit removes the listener and the tray process. **Authorization/Safeguards:** local tray/server only. **Restoration/Cleanup:** Start-ScheduledTask tray afterward so one green server remains. **Immutable Evidence:** tray-state.json colors, process inventory, health. **Validation:** installed Status.

## 4. Qualification

- [x] 4.1 After current MVP and complete accepted scope, dispatch one fresh critical-only SDET against quiet launchers, tray Restart/Exit, AtLogon start, credential non-disclosure, and fail-closed stop. **Current Rung:** 3.1 green tray/server proof. **Next Real Boundary:** fresh SDET. **Blocker/Unblocker:** 3.1. **Observable Proof:** SDET returns `critical-risks-reported | no-critical-risk | blocked` with Candidate Reference and Effective Model. **Authorization/Safeguards:** test-only writes. **Restoration/Cleanup:** restore one healthy server and tray. **Immutable Evidence:** SDET report. **Validation:** main disposition of every row.

- [x] 4.2 Run `node --check` tools/windows/opencode-workstation.ts`, installed `--help`/preflight/status, `npm test`, `npm run validate:strict`, `npm run openspec:validate`, and `npx openspec validate add-workstation-tray-and-quiet-launchers --strict`. Record rollback notes and known non-critical limits. **Current Rung:** 4.1 terminal SDET. **Next Real Boundary:** project-native validation. **Blocker/Unblocker:** 4.1. **Observable Proof:** named commands exit `0`; no known reachable critical defect. **Authorization/Safeguards:** local validation only. **Restoration/Cleanup:** retain the requested installed workstation. **Immutable Evidence:** command exits/stdout/stderr. **Validation:** those commands.
