## 1. Freeze The Comparison Boundary

- [x] 1.1 Re-read the active-change ownership inventory, repository candidate snapshot, runtime-source identity, and instruction-context diagnostics immediately before implementation; record the exact candidate/environment identities and confirm `openspec/changes/add-foundation-integrity-autorecovery/**` plus the named kit runtime/proof surfaces are the only planned writes, while `openspec/specs/local-opencode-workstation/spec.md`, `tools/test-workstation-restart-critical.ts`, `tools/windows/README.md`, and `tools/windows/opencode-workstation.ts` remain untouched. **Current Rung:** planning/source inventory. **Next Real Boundary:** provider-free foundation pack preflight. **Blocker/Unblocker:** block mutation until `replace-instruction-limits-with-context-quality` has applied or this change has explicitly rebased on its replacement contract; also stop for a live overlapping writer or unresolved active-change ownership and serialize or transfer ownership under the existing contract. **Observable Proof:** `node tools/openspec-change-inventory.ts --root . --mode ownership`, `node global/bin/repo-candidate-snapshot.ts --root . --summary`, `npm run opencode:sources`, `npm run instruction:inventory -- --format markdown`, and `npm run instruction:canonicalize -- --check .` exit zero and their privacy-safe identities are referenced by `foundation-integrity-autorecovery-v1`. **Authorization:** read-only local commands. **Safeguards:** no broad staging, restore, cleanup, or unrelated mutation. **Validation:** inventory/candidate/context-quality readback.

- [x] 1.2 [automation-dividend] Extend the existing consumer-outcome contracts, fixtures, capture/evaluator modules, CLI pack selection, and focused tests with one `foundation-integrity` pack containing seven configured-session scenarios and explicit terminal rows for every proposal partition; retain the current runner, proof client, source staging, immutable-bundle, privacy, permission, and cleanup owners and verify help/preflight/replay make zero configured-provider calls. **Current Rung:** provider-free Proof Runner. **Next Real Boundary:** unchanged-instruction configured baseline. **Blocker/Unblocker:** task 1.1; malformed fixture/schema/evaluator behavior blocks capture until focused tests pass. **Observable Proof:** `npm run proof:consumer-outcome -- --mode preflight --pack foundation-integrity --source-ref working-tree` and `npm run test:focused:consumer-outcome` exit zero; permissions allow only exact owner observation and writes inside disposable fixture roots and deny questions, external directories, credentials, installs, remote/destructive actions, and unrelated tools. **Authorization:** local disposable writes only. **Safeguards:** no semantic scoring or dependency inference; fixture facts are reviewed explicit IDs/states/hashes. **Restoration/Cleanup:** preflight removes every disposable root. **Validation:** focused consumer-outcome tests plus two identical provider-free replay results.

- [x] 1.3 Capture the unchanged loaded-instruction baseline for the seven `foundation-integrity` scenarios with the configured model/profile and at most seven primary calls, then evaluate it provider-free and preserve source/model/profile/permission/prompt/fixture identities, no-owner/no-recovery facts, before/after hashes, exits, redacted diagnostics, session deletion, and terminal cleanup. **Current Rung:** pre-candidate configured runtime. **Next Real Boundary:** candidate loader-visible owner and skill. **Blocker/Unblocker:** task 1.2 and the standing bounded synthetic provider authorization; a failed capture sets the foundation baseline live-attempt lane to `blocked` until the preserved bundle reaches terminal offline replay or identifies the exact missing observation. **Observable Proof:** `npm run proof:consumer-outcome -- --mode baseline --pack foundation-integrity --candidate-id foundation-integrity-baseline-r1 --evidence-root <new-path> --source-ref working-tree --session-mode configured` exits zero and provider-free evaluation reports seven complete scenario rows without candidate behavior. **Authorization:** at most seven non-sensitive configured-provider calls in disposable repositories. **Safeguards:** matched later candidate envelope; no consumer, remote, credential, install, or protected effect. **Restoration/Cleanup:** delete sessions and disposable roots in `finally`; retain immutable evidence only. **Validation:** baseline evaluation and replay.

## 2. Build And Prove The Minimal Loaded Path

- [x] 2.1 Add the minimal `foundation-integrity-reviewer` agent and `foundation-integrity-recovery` skill, then replace or consolidate the compact Practice Owner and main-routing text so the owner exposes the exact bind/rebind and named-current-contradiction triggers, the skill starts only after main reproduction, and both preserve Ordinary Small silence, evidence-not-authority, protected decisions, serial writes, archive immutability, and one-incident/one-re-review termination. **Current Rung:** candidate instruction source. **Next Real Boundary:** resolved core profile. **Blocker/Unblocker:** task 1.3; baseline must remain immutable before loaded behavior changes. **Observable Proof:** source readback finds one Practice ID, one read-only owner, one main-owned recovery procedure, no nested orchestration or self-mutation, and no plugin/scorer/database/every-task route. **Authorization:** scoped local instruction edits. **Safeguards:** no consumer mutation or protected semantic choice; shared inherited contracts are referenced rather than copied. **Validation:** frontmatter/permission/contract validators and focused source assertions.

- [x] 2.2 Register the sixteenth practice in the reviewed seed, increment the exact count, regenerate `config/practice-owners.view.md`, add the owner and skill to stable-sorted `core` and `all` profile catalogs and constants, update the Practice Owner roster and README/proof maps, and add missing/duplicate/stale/conflict negative tests without changing another owner's binding or profile membership. **Current Rung:** structurally selected candidate. **Next Real Boundary:** context-qualified candidate. **Blocker/Unblocker:** task 2.1; all registry/profile/catalog surfaces change as one coherent slice. **Observable Proof:** `node tools/practice-owners.ts --materialize`, `npm run test:focused:practice-owners`, and `npm run test:focused:library` exit zero; both profiles resolve the new agent and skill exactly once and retain every prior required artifact. **Authorization:** scoped local registry/generated-view/profile/test/documentation edits. **Safeguards:** deterministic validators check reviewed fields only; no inferred trigger or fallback owner. **Validation:** stable ordering, exact count, generated readback, missing/duplicate/conflict fixtures.

- [x] 2.3 Qualify the new discovery metadata and on-demand bodies under the instruction-context contract; remove only evidenced inherited duplicate boilerplate from touched core owner/skill surfaces, and preserve each artifact's specialized trigger, exclusions, inputs, output delta, permissions, and failure behavior. **Current Rung:** complete source candidate under context-quality review. **Next Real Boundary:** disposable installed loader. **Blocker/Unblocker:** task 2.2; canonical ownership, exact-duplicate, context-quality, or loaded behavior failure blocks progress, while diagnostic size growth alone does not. **Observable Proof:** before/after inventory names any removed duplicates and `npm run instruction:inventory -- --format markdown`, `npm run instruction:canonicalize -- --check .`, affected routing tests, and loaded behavior checks exit zero. **Authorization:** scoped local instruction consolidation and disposable proof writes. **Safeguards:** no semantic narrowing justified only by token count and no replacement size ceiling; affected routing tests remain green. **Restoration/Cleanup:** proof runner removes disposable fixtures and retains immutable evidence. **Validation:** context quality, inventory diagnostics, existing description-selection tests, replay.

- [x] 2.4 Materialize the current `core` profile into a disposable generated root and run the installed OpenCode loader and permission proofs; verify the new owner and recovery skill are loader-visible exactly once from the selected candidate source, the owner remains read-only/non-orchestrating, the primary retains recovery authority, and no collision, stale source, hidden fallback owner, or private instruction content appears. **Current Rung:** source-valid candidate. **Next Real Boundary:** configured mismatch/recovery behavior. **Blocker/Unblocker:** tasks 2.1-2.3; loader/source/permission mismatch blocks configured capture. **Observable Proof:** `npm run proof:runtime-surface-loader -- --candidate-id foundation-integrity-loader-r1 --evidence-root <new-path>` and `npm run proof:permissions -- --candidate-id foundation-integrity-permissions-r1 --evidence-root <new-path>` exit zero with exact agent/skill/path/permission facts. **Authorization:** provider-free disposable materialization and installed loader reads. **Safeguards:** isolated XDG/config roots; no global activation, session, model, credential, or target-project write. **Restoration/Cleanup:** remove generated/config/XDG fixtures; preserve evidence. **Validation:** loader and permission evaluators plus manifest readback.

- [x] 2.5 Capture the candidate's `mismatch-unique-recovery` scenario against its preserved baseline using the same configured model, profile, prompt/fixture contract, permissions, and environment; verify the exact owner launches once, main reproduces before mutation, one stable incident reaches `closed`, only dependent active artifacts are rebound/narrowed serially, unrelated files and archived bytes remain unchanged, prior local evidence keeps its truthful narrower ceiling, one corrected-candidate re-review occurs, and cleanup is terminal. **Current Rung:** loader-visible candidate. **Next Real Boundary:** complete reviewed partition pack. **Blocker/Unblocker:** task 2.4 and clear baseline/candidate live-attempt lanes; on failure preserve the bundle and replay the complete evaluator before any causally distinct recapture. **Observable Proof:** selected candidate capture and evaluation for `mismatch-unique-recovery` exit zero and filesystem readback satisfies every named oracle. **Authorization:** one bounded non-sensitive configured-provider primary call plus its exact read-only owner child call(s) inside a disposable repository. **Safeguards:** no questions, external directories, credentials, install, remote/destructive action, consumer project, or protected identity choice. **Restoration/Cleanup:** delete root/child sessions and disposable repository; retain immutable bundle. **Validation:** provider-free evaluator and repeatable replay. **Dependency-Chain Stop Condition:** do not expand to the remaining partitions until this happy path is terminal and green.

## 3. Complete The Recovery Envelope

- [x] 3.1 Capture the remaining six candidate scenarios and compose them with task 2.5 without repeating its live call; verify explicit terminal rows cover falsified findings, overlapping dependent ownership, protected product ambiguity, aligned plus historical-only controls, Ordinary Small plus non-critical architecture controls, and repeated unchanged findings, with no mutation or owner/recovery launch outside the specified material branches and no second incident for the unchanged tuple. **Current Rung:** proved minimal happy path. **Next Real Boundary:** complete matched population. **Blocker/Unblocker:** task 2.5; each failed lane follows its own preserved-bundle offline replay gate and does not authorize unchanged repetition. **Observable Proof:** candidate capture uses at most six additional primary calls, all scenario bundles are terminal, and provider-free composition against task 1.3 reports every `foundation-integrity-autorecovery-v1` population member supported or explicitly unknown without silently dropping a row. **Authorization:** at most six bounded non-sensitive configured-provider calls in disposable repositories. **Safeguards:** exact scenario tool maps; non-critical findings create no change/task/backlog; protected ambiguity asks no synthetic owner question and records only the boundary. **Restoration/Cleanup:** delete all sessions/fixtures and verify archive/unrelated hashes. **Validation:** evaluator, two identical replays, privacy scan, and cleanup readback.

- [x] 3.2 Run focused structural and failure-path regression coverage for registry count/order, owner Practice ID and permissions, core/all profile membership, missing/duplicate/stale/conflicting artifacts, instruction context quality, incident transition uniqueness, dependency disposition readback, archive non-rewrite, and no semantic helper inference; correct only reproduced accepted-outcome defects and repeat affected proof lanes after any candidate mutation. **Current Rung:** complete configured partition capture. **Next Real Boundary:** broad-claim challenge and full project validation. **Blocker/Unblocker:** task 3.1; a product-candidate instruction mutation invalidates affected candidate captures, while evaluator-only mutation requires replay of preserved bundles. **Observable Proof:** `npm run test:focused:practice-owners`, `npm run test:focused:instruction-context`, `npm run test:focused:library`, `npm run test:focused:contracts`, `npm run test:focused:consumer-outcome`, `npm run test:focused:validation`, and `npm run validate:strict` exit zero. **Authorization:** provider-free tests and local test-fixture writes. **Safeguards:** do not alter unrelated workstation files or manufacture semantics in assertions. **Restoration/Cleanup:** test helpers remove disposable fixtures. **Validation:** exact focused command results and scoped invalidation record.

## 4. Close Evidence And Readiness

- [x] 4.1 Update the existing proof inventory row for the consumer-outcome runner and populate `evidence-index.json` for claim `foundation-integrity-autorecovery-v1` with the actual candidate/environment, immutable baseline/candidate/replay lanes, one terminal observation per declared population member, material exclusions, unknowns, archive/unrelated readback, and maximum claim; leave absent observations `unknown`/`missing` and never broaden from deterministic checks alone. **Current Rung:** captured reviewed population. **Next Real Boundary:** fresh evidence-sufficiency challenge. **Blocker/Unblocker:** tasks 3.1-3.2; incomplete or cleanup-unknown bundles cannot support their members. **Observable Proof:** `node tools/evidence-index.ts --index openspec/changes/add-foundation-integrity-autorecovery/evidence-index.json --lane <lane-name>` resolves every named lane, and the OpenSpec apply gate accepts the schema without unsupported observations. **Authorization:** local change-evidence/proof-document edits. **Safeguards:** privacy-safe refs and honest narrowing; no raw secret/provider text. **Validation:** evidence-index schema, digests, lane resolver, claim/population completeness.

- [x] 4.2 Launch one fresh read-only `evidence-sufficiency-reviewer` for the exact completed candidate and declared partitioned-domain claim, preserve Effective Model and inspected candidate attribution, and have main independently disposition every evidence-gap row by correction, falsification, narrowing, or explicit unknown; do not ask the user for review or process approval and do not let reviewer output authorize mutation. **Current Rung:** schema-valid development claim. **Next Real Boundary:** final project-native validation. **Blocker/Unblocker:** task 4.1; a reproduced current claim defect returns only its affected evidence/candidate lanes to development and requires a fresh challenge after correction. **Observable Proof:** one challenge artifact references the actual baseline/candidate/evaluator/environment identities and the evidence index records its terminal status; no broad claim exceeds the observed partitions. **Authorization:** one read-only specialist launch. **Safeguards:** no nested orchestration, reviewer edits, verdict authority, or optional-polish work. **Validation:** main disposition readback plus evidence-index challenge fields.

- [x] 4.3 Run `node global/bin/openspec-operation-gate.ts --root . --operation apply --change add-foundation-integrity-autorecovery`, `openspec validate add-foundation-integrity-autorecovery --strict`, `npm test`, `npm run validate:strict`, and provider-free replays of the final retained bundles; verify the final candidate passes instruction context quality and reports inventory diagnostics without a size ceiling, the broad claim is bounded to the reviewed configured model/partitions, every live lane has terminal cleanup, no critical-SDET trigger is reachable in the local disposable envelope, and no consumer, install, activation, deployment, remote, credential, or protected action occurred. **Current Rung:** challenged complete candidate. **Next Real Boundary:** optional separate maintainer activation followed by fresh sessions, outside this implementation gate. **Blocker/Unblocker:** tasks 4.1-4.2; any non-zero command or non-terminal lane keeps Outcome `blocked` or `unknown` at the exact affected boundary. **Observable Proof:** all named commands exit zero, replay digests are stable, `git diff --check` is clean for intended paths, and the handoff names outcome, candidate, proof, validation, limitations, rollback, cleanup, and external-operation state. **Authorization:** provider-free validation/readback only. **Safeguards:** no archive, commit, push, install, activation, or unrelated cleanup. **Restoration/Cleanup:** none beyond proof-owned fixture cleanup already verified. **Validation:** complete command/status/diagnostic record and final diff review.
