# Runtime Proof

Captured: 2026-07-21

Status: historical pre-amendment evidence. These observations prove the original main-first/runtime-proof/status behavior, but they do not prove the later GPT/xhigh critical-only SDET contract, evidence-gated SDET convergence/stop rule, one-launch non-SDET specialist budget, reviewer risk matrices, code-quality reduction matrix, main risk disposition, or parked-risk owner quiz. Tasks covering those amended requirements remain unchecked.

All runs used process-scoped `OPENCODE_CONFIG_DIR=D:\sa-gh\opencode-kit\global`, explicit main model `openai/gpt-5.6-sol` / `xhigh`, disposable directories under `C:\Users\Sergey\AppData\Local\Temp\opencode`, and no remote/staging/deploy/release operation.

## Structural Boundary

- `npm run validate`: passed after production integration; `skills=24 agents=18 markdown=164 warnings=0 infos=1`.
- Runtime instruction token proxy: 75,122, baseline 76,815.
- Prospective changed runtime corpus token proxy: 49,386, baseline 51,079.
- `global/AGENTS.md` token proxy: 11,960, baseline 12,073.
- Current global/instruction/project-template surfaces contain no old readiness disposition field.
- `global/opencode.json.template` and the active gitignored `global/opencode.json` parse with main model `openai/gpt-5.6-sol` and no `machineOverride`.

## Ordinary Main-Author Proof

- Main session: `ses_07ae6db29ffeX6cXeWUgZqZqOb`.
- Raw event log: `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f851a2f140018gwrmA2O2Oc8NC`.
- Main effective model: `openai/gpt-5.6-sol`.
- Main authored `cli.mjs` directly; no implementation worker or reviewer ran before proof.
- Command: `node cli.mjs Sergey`.
- Observation: stdout `RESULT: HELLO SERGEY`, exit 0, `result.json` exactly `{"input":"Sergey","output":"HELLO SERGEY"}`.
- Focused repeat produced the same result without mutation.
- Observed route: `development -> RC1 -> Done-Done` for the disposable Ordinary Small fixture.

## Material Main/SDET/Review Proof

- Main session: `ses_07ae533fcffeJaiIG5twbjzftZ`.
- Raw event log: `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f851b11860014DNXJlwX8iTdR2`.
- Main effective model: `openai/gpt-5.6-sol`; main authored `transform.mjs` with no implementation worker.
- Initial command: `node transform.mjs alpha`.
- Initial observation: stdout `{"input":"alpha","output":"ALPHA"}`, exit 0, matching compact `transform-result.json`; RC1 assigned only afterward.
- Fresh SDET task: `ses_07ae33c2affedYjcmTlOGKcT3t`; effective model `xai/grok-4.5`; action `authored-tests`; only `transform.test.mjs` changed.
- SDET returned exact `node --test` Execution Request. Main ran it and resumed the same task ID with raw output: 3 passed, 0 failed.
- Test mutation returned status to development. Main reran the CLI proof and complete `node --test`; 3 passed, 0 failed; RC2 assigned afterward.
- Fresh final reviewer: `ses_07ae0c3a1ffe4DlbYvt42dloUf`; effective model `xai/grok-4.5`; verdict approved; Blocking Evidence none.
- Fresh delivery reviewer: `ses_07adfa8c8ffeMmOU1HtLdHr87E`; effective model `xai/grok-4.5`; verdict on plan; Blocking for Acceptance no; Blocking Evidence none.
- Observed route: `development -> RC1 -> development -> RC2 -> Done-Done`.

## Generic Reviewer Model Reporting

- Before always-loaded runtime correction, fresh code-quality reviewer `ses_07adabcd3ffeaX0LYti0nR0YTW` ran on Grok according to raw task metadata but omitted Effective Model from its native report.
- This concrete runtime defect was corrected in always-loaded Shared Reviewer Runtime Invariants.
- After correction, fresh code-quality reviewer `ses_07ad79907ffeljYZIP9JPBmIWa` emitted `Effective Model: xai/grok-4.5` in its native report; raw task metadata also records `providerID: xai`, `modelID: grok-4.5`.
- Probe parent session: `ses_07ad83cf1ffe9BkvxjkM6n9BTl`.

## Optional Worker Raw-Output Resume

- Main session: `ses_07ad6118fffeOxvoSgrTarWTdC`.
- Raw event log: `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f852c007a001b21C0dLDrDUc6D`.
- Worker task: `ses_07ad532bbffen9360dSsxblyJq`; effective model `openai/gpt-5.6-sol`.
- Worker authored only `worker-cli.mjs`, returned exact `node worker-cli.mjs 42` Execution Request, and made no proof claim.
- Main observed stdout `WORKER:42`, empty stderr, exit 0 and resumed the same task ID with raw output.
- The same worker accepted proof and reported no post-execution edits.
- Dream Team implementation was correctly classified as provisional/ineligible for complete proof because its current parent workflow cannot return raw proof and resume the same author; it was not invoked by this fixture.

## Protected And Blocked Routes

- Main session: `ses_07ad2d889ffeVvaaft9tGiF62W`.
- Raw event log: `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f852d76e1001E4GvyNKrNj5T0c`.
- Configured unauthorized staging endpoint/token were not called; no local fallback existed; status remained development with exact runtime-proof blocker and no SDET/reviewer.
- Green compile/unit claims without representative invocation remained development; no substitute harness, polish, or reviewer was dispatched.
- Before mutation, one bounded protocol/API design consultation ran as task `ses_07ad20764ffe4hg8hgz1e9FwGQ`, effective model `xai/grok-4.5`; it returned an owner-owned authorization semantics blocker and no readiness approval.
- `blocked.mjs` command `node blocked.mjs` produced stdout `BLOCKED-PROOF`, exit 0; RC1 assigned. Predeclared `definitely-missing-validator-9f31 --check` then failed as unavailable. The unchanged file remained RC1 with Blocking Evidence; no SDET/review compensated.

## Remaining Proof Gap

- Static validators enforce GPT/xhigh for both implementation agents and Grok/high for SDET plus all fifteen reviewer/delivery roles; live runs prove main, implementation-worker, SDET, code-quality, protocol/API, final, and delivery model routing.
- The local Dream Team GPT implement adapter attempt returned generic failure before a workflow/report. Temporal later showed no running workflow and no tracked mutation.
- Effective runtime model evidence for `dream-team-implementer` and every remaining specialist reviewer is not yet complete. OpenSpec task 6.9 remains unchecked until that gap is closed or explicitly blocked.

## Current Post-Amendment Material Proof

Captured: 2026-07-21

- Fresh main session: `ses_07a24bff4ffeD2fMLowOXFtORQ`.
- Raw JSON event log: `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f85db9ec9001kGD4krhoq9GX1Q`.
- Environment: process-scoped `OPENCODE_CONFIG_DIR=D:\sa-gh\opencode-kit\global`, explicit main `openai/gpt-5.6-sol` / `xhigh`, disposable project `C:\Users\Sergey\AppData\Local\Temp\opencode\make-main-author-runtime-proof-first-final`, no network/credentials/remote/shared/install/deploy/release/publication operation.
- Main directly authored `normalize.ts`; no production worker ran.
- Candidate proof: `node normalize.ts "  hello Material  "` exited 0 and emitted `{"input":"  hello Material  ","output":"HELLO MATERIAL"}`. `normalize-result.json` contained the same compact JSON plus one LF. RC1 was assigned only after that observation.
- One-shot reviewers ran after RC1 proof and before SDET: `final-candidate-reviewer` (`ses_07a231a5bffeXs67HLgI4F8p2c`), `session-delivery-reviewer` (`ses_07a214eb3ffeqtcKyyb1pRwnFu`), `code-quality-reviewer` (`ses_07a201a21ffexXkgwAV5ycDb5V`), and `test-coverage-reviewer` (`ses_07a1f30a6ffe4jF7tMYdVxluO4`). Every reviewer reported effective `xai/grok-4.5`, remained read-only, and returned risk/reduction evidence rather than lifecycle verdict authority.
- Main preserved inspected-RC attribution and dispositioned every reviewer row. No reviewer row reproduced a current-outcome or non-deferrable defect; no review-driven candidate mutation occurred.
- Fresh `sdet-quality-engineer` child `ses_07a1c3677ffehTn4gxKvA0sH72` reported effective `openai/gpt-5.6-sol`. Its initial blocked execution request was continued in the same child identity with raw output. Terminal action was `no-critical-risk`; no test artifact or production file changed, permanently closing SDET for that fixture root.
- Focused validation: actual CLI proof repeated; `node --check normalize.ts` exited 0; `node --test` exited 0 with 0 failures; missing, blank, and extra arguments each exited 2 with empty stdout and actionable stderr.
- No optional parked risks remained, so no empty owner quiz was presented.
- Final fixture route: `development -> RC1 -> Done-Done`, `Final Candidate: RC1`, `External Operations: not performed`.

This proof covers the amended main-default authorship, proof-before-review order, one-shot risk/reduction matrix phase, inspected-RC main disposition, exact GPT/xhigh critical SDET route, same-child raw-output continuation, permanent no-critical stop, validation, empty-quiz suppression, and final-RC linkage. It does not yet prove every fixed-role model at runtime, malformed/model-substituted one-shot exhaustion, multi-cycle confirmed-critical SDET continuation, or selected parked-risk creation as a separate root; those remain assigned to tasks 6.9, 6.11, 6.12, and focused deterministic tests.

## Current Runtime Instruction Non-Growth

Command: `npm run instruction:inventory -- --format markdown`

- All runtime artifacts: token proxy 69,349 versus baseline 76,815 (decrease 7,466).
- `global/AGENTS.md`: token proxy 11,606 versus baseline 12,073 (decrease 467).
- Frozen changed runtime corpus from `pre-change-baseline.md`: token proxy 43,613 versus baseline 51,079 (decrease 7,466).
- The amended policy replaced superseded finite-wave/blocker/verdict text; no unrelated safeguard was removed to satisfy the budget.

## Current Qualification Launch Gap

Captured: 2026-07-22

- Candidate Reference remained `RC1`: HEAD `0aed9fb7d485be755621a6dce560a3e9d523127e` plus tracked runtime/support diff hash `cf2b2bf8650f3308155aaaf130e284f6d35f54ca`.
- The single pre-SDET read-only fan-out created these verified child identities on effective `xai/grok-4.5` / `high`:
  - `instruction-artifact-reviewer`: `ses_079936f3dffetlv3PhCbUVxGx6`
  - `final-candidate-reviewer`: `ses_079936f1cffeIELGTnayzMEXWN`
  - `session-delivery-reviewer`: `ses_079936f11ffeGkPL33RnO6KH2k`
  - `code-quality-reviewer`: `ses_079936f06ffeFinhZ5Cai63OO0`
  - `openspec-architecture-reviewer`: `ses_079936ef4ffehI1uiLtiqRFTb4`
  - `test-coverage-reviewer`: `ses_079936eceffeLa8qqJn4fXf3Ej`
  - `deployment-config-reviewer`: `ses_079936ec2ffeYk9B0cxJeNi1p8`
- The parent calls were interrupted after identity creation and before a report was returned. Exported sessions initially contained zero-token empty assistant messages.
- Same-child continuations were attempted after the owner requested work to resume. Every continuation returned `Task cancelled`; export of the instruction-artifact child records a completed empty assistant message with zero tokens. No role returned a usable risk or reduction matrix.
- Main disposition: every launch is `terminal-unusable`. The launches are consumed, cannot be replaced or retried for this root, and create no writer-liveness hold because every role was read-only.
- Tasks 5.1 and 5.2 therefore remain unsatisfied. The required pre-SDET evidence phase cannot complete, so terminal SDET was not dispatched and Material `Done-Done` is unavailable for this root.
- The working runtime outcome and RC1 proof remain intact. No production/test mutation, external operation, install, activation, archive, push, release, publication, credential use, or remote/shared mutation followed the launch gap.

## Owner Stop Decision

Captured: 2026-07-22

- After the reviewer launch gap, a parallel writer changed the tracked runtime/support Candidate Reference from `cf2b2bf8650f3308155aaaf130e284f6d35f54ca` to `1155f06e873e3444c13f1970037d1e17320bf77c`.
- The new diff added or changed overlapping role/contract surfaces including `global/agents/qwen-local-worker.md`, `global/agents/troubleshooter.md`, `tools/contracts/agents.ts`, `tools/validators/agents.ts`, and existing main-first candidate paths. Main did not author or integrate those mutations and did not overwrite them.
- The foreign Candidate Reference mutation invalidated RC1. Current `Change-Status: development`; `Final Candidate: none`.
- The owner selected `Остановить текущий root`. OpenSpec progress remains 44/60; tasks 5.1 onward that depend on usable one-shot matrices remain incomplete.
- The change must not be archived or represented as Done-Done. A future session must first establish terminal ownership of the overlapping writer and read the current worktree without resetting the consumed reviewer launch history.
- External Operations: not performed.

## Inherited Model Amendment And RC3

Captured: 2026-07-22

- The owner resumed the root, authorized repository-local edits, and selected reusable-agent model inheritance as the new source of truth instead of restoring the fixed GPT/Grok/Qwen matrix.
- The amended contract requires every `global/agents/*.md` definition to omit `model` and `variant`, inherit the invoking primary/global model, and report `Effective Model` as provenance rather than provider eligibility. Machine-owned external-runner settings are not reusable-agent qualification identities.
- Production/config candidate before test updates: tracked runtime/support diff hash `323427b9a58f4f3560f27fc9ecf02206cb5a515e`.
- Fresh loader proof with process-scoped `OPENCODE_CONFIG_DIR=D:\sa-gh\opencode-kit\global` returned `agents=22`, `pinnedAgentModelOrVariant=0`, and `inheritedGlobalModel=openai/gpt-5.6-sol`.
- Fresh real dispatch parent `ses_079461985ffeV7xlUGaWt5ib1h` created `qwen-local-worker` child `ses_07945e000ffeCdg1eT0UKcoKpE`; runtime metadata and child output both reported `openai/gpt-5.6-sol`, and the child returned `INHERITANCE_CHILD_OK`. This proved RC2.
- Fresh test-only SDET child `ses_079447da5ffeMSJLvV79uI9leT` used effective `openai/gpt-5.6-sol`. It identified `INHERIT-AVAIL-001`: stale fixed-provider tests failed before execution and no longer protected the inheritance contract that prevents pinned-provider one-shot launch loss.
- SDET changed only `tools/test-contracts-change-ready.ts`, `tools/test-contracts.ts`, `tools/test-library/validator-change-ready.ts`, `tools/test-library/validator-1.ts`, `tools/test-library/validator-2.ts`, `tools/test-library/dream-team-agents.ts`, and `tools/test-helpers/library.ts`.
- The first parent run exposed one attributable expected-array oracle mismatch. The same SDET child corrected only `tools/test-contracts-change-ready.ts`; main reran `node tools/test-contracts-change-ready.ts && node tools/test-contracts.ts && npm run validate && npm test`, which exited 0. Evidence included contracts 91, library 332, Dream Team 16, validation scripts 3, and every subsequent suite green; full capture: `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f86c3a1b3001MyGmf71sptjboX`.
- Terminal SDET action: `no-critical-risk`. Main disposition for `INHERIT-AVAIL-001`: `fixed-and-verified`; no production defect or additional SDET attempt remained.
- Post-test Candidate Reference: HEAD `0aed9fb7d485be755621a6dce560a3e9d523127e` plus tracked runtime/support diff hash `f560313a1e4319611e32f43a3df4fe69e0a63aa7`.
- Post-test fresh loader re-proof again returned `agents=22`, `pinnedAgentModelOrVariant=0`, and `inheritedGlobalModel=openai/gpt-5.6-sol`. Parent `ses_07939571fffeMjAo9eqw1XFFGV` created inherited child `ses_079390e25fferqGJPxSZEl14IA`, which returned `INHERITANCE_CHILD_OK` and `Effective Model: openai/gpt-5.6-sol`. The re-proven candidate is RC3.
- The prior mandatory non-SDET launches remain terminal-unusable and their root-scoped budgets remain consumed. Model inheritance fixes the recurrence path but cannot retroactively supply the missing matrices, so tasks 5.1-5.3 remain unsatisfied and Material Done-Done remains unavailable for this root.
- External Operations: not performed.

## RC3 Validation And Local Handoff

Captured: 2026-07-22

- Profile: Material.
- Outcome: reusable OpenCode agents inherit the invoking primary/global model with no agent-level `model` or `variant`; main-first runtime-proof routing, one-shot reviewer budgets, critical-only SDET, risk ownership, and protected external-operation boundaries remain operative.
- Candidate Reference: RC3, HEAD `0aed9fb7d485be755621a6dce560a3e9d523127e` plus tracked runtime/support diff hash `f560313a1e4319611e32f43a3df4fe69e0a63aa7`.
- Production author: main. Optional production workers were not used for the inheritance amendment. SDET authored only the seven attributed test/test-support paths recorded above.
- Runtime Proof: pass. Fresh loader: 22 agents, zero model/variant pins, inherited global model `openai/gpt-5.6-sol`. Fresh real child `ses_079390e25fferqGJPxSZEl14IA`: `INHERITANCE_CHILD_OK`, `Effective Model: openai/gpt-5.6-sol`.
- SDET: child `ses_079447da5ffeMSJLvV79uI9leT`, effective `openai/gpt-5.6-sol`, terminal `Action: no-critical-risk`. Main disposition: `INHERIT-AVAIL-001` fixed-and-verified by focused/full tests and RC3 re-proof.
- Mandatory non-SDET launch state: terminal-unusable for `instruction-artifact-reviewer`, `final-candidate-reviewer`, `session-delivery-reviewer`, `code-quality-reviewer`, `openspec-architecture-reviewer`, `test-coverage-reviewer`, and `deployment-config-reviewer`. Their child IDs and exact outcomes remain recorded under `Current Qualification Launch Gap`. No replacement launch or reviewer matrix exists.
- Validation: `node tools/test-contracts-change-ready.ts` passed 91; `node tools/test-contracts.ts` passed 91; `npm run validate:strict` passed with `skills=24 agents=18 markdown=165 warnings=0 infos=1`; `npm test` passed library 332 and every subsequent project suite; `npm run openspec:validate` passed 9/9; `git diff --check` passed.
- Instruction non-growth: runtime artifacts token proxy `76,815 -> 69,532`; `global/AGENTS.md` `12,073 -> 11,690`; frozen changed runtime corpus `51,079 -> 43,703`. All three are below the pre-change baseline.
- Smoke evidence: current deterministic lifecycle fixtures, inherited dispatch, and the complete final isolated replay described below are green against the unchanged RC3 production/support hash.
- Parked-risk quiz: skipped because no optional parked risk exists. Mandatory unusable-review evidence is not optional and cannot enter a waiver quiz. No selected item or separate root was created.
- OpenSpec operation gate: the task's original `--operation prearchive` invocation was rejected because the deterministic registry has no `prearchive` operation. The supported read-only equivalent, `npm run openspec:gate -- --operation archive --change make-main-author-runtime-proof-first`, exited 0 with `status: passed`. This cheap artifact-presence result does not override incomplete tasks or authorize archive; no archive operation was performed.
- Outcome working status: working inside the local/ephemeral proof envelope.
- Change-Status: RC3.
- Final Candidate: none; Material Done-Done is unavailable because tasks 5.1-5.3 lack usable mandatory reviewer/reduction matrices.
- Rollback: restore only this change's scoped instruction/agent/contract/validator/test/documentation preimages. No runtime activation rollback was executed. OpenCode must be restarted before config-time agent changes affect an already-running session.
- External Operations: not performed.

## RC3 Final Isolated Smoke Replay

Captured: 2026-07-22

- Fresh primary session: `ses_07932fe88ffeweZ9F9u6YEcoPc`; effective model `openai/gpt-5.6-sol` / `xhigh`.
- Raw JSON event log: `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f86cd5ad8001rEwYWNlWh0k3H3`.
- Environment: process-scoped `OPENCODE_CONFIG_DIR=D:\sa-gh\opencode-kit\global`; disposable root `C:\Users\Sergey\AppData\Local\Temp\opencode\make-main-author-runtime-proof-first-rc3-smoke`; repository read-only; no network, credentials, remote/shared mutation, install, activation, deployment, release, publication, archive, commit, or push.
- Candidate continuity: before/after source Candidate Reference remained RC3, HEAD `0aed9fb7d485be755621a6dce560a3e9d523127e` plus production/support diff hash `f560313a1e4319611e32f43a3df4fe69e0a63aa7`.
- Ordinary Small: main authored and ran `node ordinary-cli.mjs cedar ordinary-side-effect.txt`; exit 0, stdout `ordinary:cedar->CEDAR`, side effect `CEDAR`.
- Material: main authored and ran `node material-cli.mjs 7 material-side-effect.json`; exit 0, stdout `material:7->49`, side effect `{"input":7,"square":49}`; RC attribution followed proof and no Done-Done claim was made.
- Optional worker: child `ses_079313cf7ffeDhgaMm0j0vjqkg`, effective model `openai/gpt-5.6-sol`, authored only `worker-cli.mjs` and returned a provisional execution request. Parent ran `node worker-cli.mjs delta worker-side-effect.txt`; exit 0, stdout `worker:delta->atled`, empty stderr, side effect `atled`. The same child accepted raw output and terminated with no further edit.
- Staging denial: synthetic `https://staging.invalid/shared` had absent authority; `networkAttempted=false`, no local fallback, no reviewer/SDET launch, status `development`.
- Unproved candidate: `node --check unproved-cli.mjs` exited 0, but no representative invocation or side effect occurred; status remained `development` with no specialist substitution.
- Existing pure event fixtures: `node tools/test-contracts-change-ready.ts` and `node tools/test-contracts.ts` exited 0. They covered one-shot reviewer identity/continuation/exhaustion, critical SDET convergence and permanent stop, inspected-RC main disposition, reduction-matrix obligations/oracle retention, non-deferrable quiz exclusion, separate-root selection, and empty-quiz suppression. Actual current-root reviewer/SDET launches: zero.
- Direct main verification read `ordinary-side-effect.txt`, `material-side-effect.json`, `worker-side-effect.txt`, and the uninvoked `unproved-cli.mjs`; all matched the fresh-process report.
- Repository paths changed by this replay: none. Disposable paths only; the focused fixtures placed temporary artifacts under the disposable root.
- Outcome: all ten task 7.3 smoke classes passed against the same RC3 Candidate Reference. `Change-Status: RC3`; `Final Candidate: none`; Done-Done not claimed.

## Owner-Requested Advisory Reviewer Wave

Captured: 2026-07-22

- The owner explicitly requested that all seven roles whose original launches failed run again. These are new read-only advisory launches and do not replace or replenish the original root-scoped one-shot qualification budgets.
- All seven advisory children completed on inherited Effective Model `openai/gpt-5.6-sol`:
  - `instruction-artifact-reviewer`: `ses_07922a7acffeIj09XcN2Z4W2fo`
  - `final-candidate-reviewer`: `ses_07922a77dffenjTHY5f35grlIH`
  - `session-delivery-reviewer`: `ses_07922a76fffeqo7zB99VKkIKRy`
  - `code-quality-reviewer`: `ses_07922a75fffeuMo2VzrpKkEq3B`
  - `openspec-architecture-reviewer`: `ses_07922a753ffettqRi9HWLzRoPo`
  - `test-coverage-reviewer`: `ses_07922a748ffeC8x4rBaGC2mqo5`
  - `deployment-config-reviewer`: `ses_07922a726ffeoYU14jCpfydylM`
- Every report inspected RC3, HEAD `0aed9fb7d485be755621a6dce560a3e9d523127e`, production/support diff hash `f560313a1e4319611e32f43a3df4fe69e0a63aa7`, and explicitly labeled itself `owner-requested advisory; not original-root qualification evidence`.
- Main independently confirmed by current source inspection:
  - SDET frontmatter grants tool-wide `edit: allow`, and the validator rejects narrower edit rules even though the accepted contract requires an exact test-only write scope.
  - the OpenCode config validator requires the machine-local `global/opencode.json` model to equal the portable default, contradicting owner-selected primary-model overrides;
  - the executable event fixture permits terminal SDET after merely one launch without proving every applicable output usable/dispositioned, permits a repeated SDET attempt on the same RC after a confirmed-critical result, and treats non-deferrable `not-reproduced` as closed;
  - the Universal Development Loop lists Material delivery evidence after SDET/handoff while its own requirement says that review runs before terminal SDET;
  - README and auxiliary quality/adapters routing still contain provider-specific or broad reviewer wording that conflicts with inherited models, proportional Ordinary Small routing, and reduction-only code quality.
- Main disposition: these are confirmed current contract/containment defects, not authority to mutate this frozen candidate. Correcting them would return status to `development`, require updated automated-test ownership and new proof, while this root's terminal `no-critical-risk` permanently closed further SDET attempts. No authorized correction path remains inside the current root.
- Reduction rows `RED-001` through `RED-004` are optional net-reduction opportunities only and remain parked; they do not authorize candidate mutation.
- Config-schema completeness, differential non-default runtime inheritance, historical writer-closure readability, selected-quiz live behavior, and raw-log/manifest portability remain evidence gaps or separate future work. Main independently recomputed the production/support hash and directly observed the RC3 raw smoke process, so advisory hash/raw-smoke uncertainty does not change Candidate Reference.
- Tasks 5.1-5.3 remain incomplete because these second advisory launches cannot retroactively become the original one-shot qualification matrices. `Change-Status: RC3`; `Final Candidate: none`; `External Operations: not performed`.

## Owner-Authorized Production-Only Correction

Captured: 2026-07-22

- The owner explicitly authorized candidate mutation, then selected `Только production сейчас` for test ownership. Main therefore changed only loaded instructions, agent/config contracts, validators, and routing documentation; no automated test, fixture, snapshot, helper, or golden artifact was changed.
- The first production mutation invalidated RC3 immediately. `Change-Status: development`; `Final Candidate: none`.
- Corrections applied:
  - SDET edit authority changed from tool-wide `allow` to runtime-approved `ask`, with blocked behavior when exact test-only approval is unavailable;
  - the portable default-model check moved from machine-local `global/opencode.json` to committed `global/opencode.json.template`;
  - Material delivery review now appears in the canonical post-proof, pre-SDET sequence;
  - Dream Team embedded pre-proof review is explicitly diagnostic rather than qualification evidence;
  - qwen helper provenance, session-delivery triggers, Ordinary Small proportional gates, reduction-only code-quality routing, inherited-model routing, and restart-only config activation wording were corrected.
- Automated event-fixture defects remain intentionally unmodified under the owner's production-only decision: usable/applicable reviewer preconditions before SDET, same-RC SDET retry rejection, and unresolved non-deferrable `not-reproduced` handling still require an eligible test owner.
- Previously checked proof/validation/handoff tasks invalidated by mutation or disproved prerequisites were reopened in `tasks.md`. No RC number is assigned until current production behavior is proven again.
- External Operations: not performed.

### Production-Only Proof And Validation Result

- Current production/support diff hash: `18d5101e26c56f574a9de47be380811aef873d3d`; prior RC3 hash `f560313a1e4319611e32f43a3df4fe69e0a63aa7` is historical and no longer identifies the candidate.
- Fresh `opencode debug agent sdet-quality-engineer` with process-scoped current `OPENCODE_CONFIG_DIR` loaded effective `permission.edit = ask`, retained `bash = deny`, and exposed the runtime-approval requirement in the agent prompt. Fresh `qwen-local-worker` loading exposed mandatory Effective Model provenance and inherited-model wording.
- Disposable config proof used machine-local model `openai/gpt-5.5` with portable template default `openai/gpt-5.6-sol`; `validateOpenCodeConfigFiles` returned `errors=[]`, `warnings=[]`, `infos=[]`. No active machine config was changed.
- `npm run validate` and `npm run validate:strict` passed: `skills=24 agents=18 markdown=165 warnings=0 infos=1`.
- `npm run openspec:validate` passed 9/9. `git diff --check` passed.
- Instruction non-growth remained green: runtime token proxy `76,815 -> 69,585`; `global/AGENTS.md` `12,073 -> 11,690`; frozen changed runtime corpus `51,079 -> 43,771`.
- Automated validation is intentionally red under the owner's production-only decision:
  - `npm test` stopped with one library failure because `validator rejects SDET nested edit permissions` still searches for stale `edit: allow` fixture text;
  - focused `node tools/test-contracts.ts` reported five stale expectations: old Universal Development Loop order, old Portable delivery description, old SDET required-text array, old session-delivery binding array, and old Portable delivery token;
  - the existing pure event fixture still does not enforce usable/applicable reviewer results before SDET, a new Candidate Reference before a continued SDET attempt, or unresolved treatment for non-deferrable `not-reproduced`.
- No automated test artifact was changed. Under the owner's selected production-only boundary, main cannot correct these failures and no eligible current-root SDET attempt remains.
- Runtime Proof cannot freeze a new RC while project-native automated validation is red. `Change-Status: development`; `Final Candidate: none`; restart OpenCode before expecting these config-time changes in an already-running session.
- External Operations: not performed.

### Post-Restart RC4 Proof

- The owner restarted OpenCode after the config-time mutation.
- Current production/support diff hash remained `18d5101e26c56f574a9de47be380811aef873d3d`.
- Fresh loaded-agent inspection again showed `sdet-quality-engineer` with effective `edit: ask`, `bash: deny`, no nested orchestration, and the runtime-approval/test-only scope contract.
- Fresh inherited helper child `ses_0790c451affeGNyu6ezT4nGZiT` returned `RESTARTED_INHERITANCE_OK`, `Effective Model: openai/gpt-5.6-sol`, Candidate Reference `development/18d5101e26c56f574a9de47be380811aef873d3d`, and repository changes `none`.
- The earlier disposable machine-local override proof remains part of this unchanged production hash: owner model `openai/gpt-5.5` with portable template default `openai/gpt-5.6-sol` produced no config-validator error, warning, or info.
- Runtime Proof passed for the current production/config behavior. The root counter is monotonic after RC3 mutation, so the frozen candidate is RC4.
- Project-native automated validation remains red under the explicit production-only test boundary. RC4 therefore cannot become Done-Done, and no final candidate is declared.
- `Change-Status: RC4`; `Final Candidate: none`; `External Operations: not performed`.
- Post-restart read-only archive operation gate exited 0 with `status: passed` and reported 15 unchecked tasks before its own task was marked complete. As before, this artifact-presence gate does not override red automated validation or authorize archive; no archive operation was performed.

### RC4 Production-Only Local Handoff

- Outcome: current loaded production/config behavior is corrected and runtime-proven; automated contract synchronization is intentionally incomplete under the owner-selected production-only boundary.
- Production author: main. No production worker authored these corrections. No automated test artifact was changed.
- Candidate Reference: RC4, HEAD `0aed9fb7d485be755621a6dce560a3e9d523127e`, production/support diff hash `18d5101e26c56f574a9de47be380811aef873d3d`.
- Environment: restarted local OpenCode, process-scoped current global config, disposable local config proof; no credentials, network, remote/shared environment, install, activation, deploy, release, publication, archive, commit, or push.
- Runtime Proof: fresh loader confirmed SDET `edit: ask`; owner model override differed from the portable default without validation failure; inherited helper child `ses_0790c451affeGNyu6ezT4nGZiT` returned `RESTARTED_INHERITANCE_OK` on `openai/gpt-5.6-sol`; no repository mutation followed proof.
- Specialist history: original seven qualification reviewer launches remain terminal-unusable and consumed. The seven later owner-requested reports remain advisory matrices tied to RC3, not original-root qualification evidence. Their identities and main disposition are recorded above.
- Critical SDET: prior child `ses_079447da5ffeMSJLvV79uI9leT` terminated `no-critical-risk` on the earlier candidate and permanently closed current-root SDET. No RC4 SDET launch occurred.
- Validation: `npm run validate:strict` passed; OpenSpec validation passed 9/9; `git diff --check` passed; inventory non-growth passed. `npm test` remains red on one stale library fixture, and focused contracts remain red on five stale expectations. Event-fixture guards named above remain unsynchronized.
- Main disposition: production containment, model-source, ordering, and routing defects are fixed in RC4. Automated-test and event-fixture corrections remain blocked by the explicit production-only ownership decision. Optional reductions remain parked and did not mutate RC4.
- Parked-risk quiz: not reached because complete validation and current-root qualification evidence are unavailable. No quiz item or separate root was created.
- Residual fidelity gaps: no eligible current-root test author; no usable original one-shot reviewer matrices; no valid RC4 terminal SDET evidence; automated contract suite red.
- Rollback: restore only the scoped RC4 production/config corrections to the recorded RC3 preimage if the owner chooses rollback, then restart OpenCode. No rollback was performed.
- `Change-Status: RC4`; `Final Candidate: none`; `External Operations: not performed`.

## Owner-Authorized Current-Root Recovery Amendment And RC5

Captured: 2026-07-22

- The owner rejected a separate correction root and explicitly required this current OpenSpec change to continue until Done-Done. This authorized a protected lifecycle-semantic amendment in the current root.
- The amendment adds exactly one owner-authorized zero-output recovery identity per non-SDET role only when the original child was read-only, adapter-proven terminal, returned zero usable model output, and cannot be continued. It preserves the primary failure and forbids malformed/substantive/unknown-liveness recovery and every third identity.
- An SDET launched before usable mandatory matrices and main disposition is now explicitly precondition-invalid: it remains evidence but neither satisfies nor terminally closes the required post-review SDET gate. Convergence/stop applies to the first precondition-valid attempt.
- The first amendment mutation invalidated RC4 and returned status to `development`.
- Current production/support diff hash: `06d342cdd99c8eb21eb361636a4ca3d1fd589b47`.
- `npm run validate` passed and `npm run openspec:validate` passed 9/9 before proof; `git diff --check` passed.
- Fresh process `ses_0790134c8ffeWvmuVgZ4BxW7SX`, effective model `openai/gpt-5.6-sol`, returned `RECOVERY_POLICY_OK` and restated both exact loaded predicates without changing repository files.
- Runtime Proof passed; the next monotonic candidate is RC5.
- The original seven read-only children at `Current Qualification Launch Gap` meet the terminal-zero-output predicate. The owner explicitly authorized their rerun before the second wave. The seven second-wave identities under `Owner-Requested Advisory Reviewer Wave` are therefore the only possible recovery identities; no third identity is allowed. Their existing matrices remain tied to RC3 and require same-child recovery provenance annotation, not a new review.
- Prior SDET child `ses_079447da5ffeMSJLvV79uI9leT` ran before usable mandatory matrices and is reclassified as precondition-invalid evidence. It does not satisfy or close the required post-recovery SDET gate.
- `Change-Status: RC5`; `Final Candidate: none`; `External Operations: not performed`.

### Recovery Matrix Provenance And Main Disposition

- Every recovery identity was resumed in the same child context and returned `RECOVERY_PROVENANCE` without rereview, mutation, or a third identity. The original terminal-zero-output identity, recovery identity, Effective Model `openai/gpt-5.6-sol`, preserved RC3 matrix IDs, and RC5 correction trace were confirmed for all seven roles.
- Fixed-and-verified in RC5: `IAR-SAFE-001`, `IAR-ORDER-001`, `IAR-ORDER-002`, `IAR-ROUTE-001`, `IAR-ROLE-001`, `IAR-ROLE-002`, `IAR-TRIGGER-001`, `IAR-EVID-001`, `FC-RC3-SDET-001`, `SDR-RC3-001`, `SDR-RC3-002`, `SDR-RC3-003`, `SDR-RC3-004`, `SDR-RC3-005`, `OSAR-001`, `OSAR-003`, `OSAR-005`, `OSAR-007`, `TC-RC3-007`, `CFG-INHERIT-001`, `DOC-INHERIT-004`, and `OPS-RELOAD-005`. Main source/runtime checks, the owner restart, stable hash recipe, recovery proof, SDET `edit: ask`, machine override proof, and RC5 fresh-process proof supply the verification.
- Disproved as current RC5 evidence gaps: `IAR-EVID-002`, `FC-RC3-EVIDENCE-001`. Main independently recomputed the candidate hash and directly observed fresh raw process output.
- Confirmed critical test-oracle defects routed to the precondition-valid fresh SDET: `FC-RC3-LIFECYCLE-001`, `FC-RC3-SAFETY-001`, `FC-RC3-SDET-002`, `OSAR-002`, `OSAR-004`, `TC-RC3-001`, `TC-RC3-002`, `TC-RC3-004`, `TC-RC3-005`, `TC-RC3-006`, and `WA-RC3-001` through `WA-RC3-004`. They cover usable-reviewer preconditions, invalid-order SDET, same-RC retry rejection, unresolved non-deferrable disposition, reduction-oracle retention, separate-root quiz behavior, RC-history behavior, and stale focused expectations.
- Read-only proof gaps to close after test correction without candidate mutation: `OSAR-006`, `TC-RC3-003`, and `CFG-PRECEDENCE-003` require one differential live child dispatch where explicit primary differs from the portable global default.
- Parked optional reductions/limitations: `RED-001` through `RED-004`, `IAR-CONTEXT-001`, and `CFG-SCHEMA-002`. No reduction is necessary for the accepted outcome; full upstream schema validation is a contained diagnostic limitation because the current committed template passed the real loader and OpenCode remains the startup authority.
- No plausible non-deferrable row remains ignored or parked: each is fixed-and-verified or routed to fresh critical test evidence. Source matrices remain attributed to RC3; no reviewer approval of RC5 is claimed.

## Superseding Development-Stage Migration And Current MVP Proof

Captured: 2026-07-25

- The owner-selected `Development-Stage: development | MVP | RC<n> | stable` model supersedes every prior `Change-Status`, Done-Done, reviewer-recovery, and RC-on-proof event in this file. Those earlier sections are historical evidence only and do not establish current maturity.
- Current accepted outcome: loaded authority, project-facing mirrors, roles, production contracts, validators, and OpenSpec artifacts use development/MVP/RC/stable; non-SDET reviewers are optional; Material SDET is critical-only; non-critical limitations are parked; external operations remain separately authorized.
- Fresh local process boundary: `opencode debug skill` loaded `change-ready-sdlc` from `D:\sa-gh\opencode-kit\global\skills\change-ready-sdlc\SKILL.md` and returned the exact `Development-Stage: development | MVP | RC<n> | stable` field plus non-critical non-blocking semantics.
- Inherited model boundary: `opencode debug config` resolved `"model": "openai/gpt-5.6-sol"`; `opencode debug agent sdet-quality-engineer` returned the role without a `model` or `variant` pin. No model invocation, credentials, remote mutation, or billable operation was used for this proof.
- Current structural evidence: `npm run validate` passed with `skills=24 agents=18 markdown=165 warnings=0 infos=1`; `npm run openspec:validate` passed 9/9; `git diff --check` passed.
- Writer closure: the earlier timed-out Dream Team workflow is terminal and no mutation-capable child is running. The current candidate remains the readable local workspace diff plus this OpenSpec change.
- Accepted scope is not complete because automated lifecycle fixtures remain on the superseded model. Current stage is therefore `Development-Stage: MVP`; no current RC or stable candidate is assigned.
- `External Operations: not performed`.

## Current RC1 Qualification And Stable Handoff

Captured: 2026-07-25

- Profile: Material. The change alters loaded lifecycle and safety policy.
- Outcome: loaded authority, project-facing mirrors, reusable roles, contracts, validators, and focused tests consistently use `Development-Stage: development | MVP | RC<n> | stable`. Optional reviewers are non-authorizing, Material SDET is critical-only and convergent, non-critical limitations may remain documented, and external operations remain separately authorized.
- Accepted scope/non-goals: the proposal, design, specifications, and all 13 tasks are complete. This change does not add an application API, migration, release automation, durable workflow database, deployment, publication, or compatibility alias.
- Candidate Reference: `RC1`, HEAD `0aed9fb7d485be755621a6dce560a3e9d523127e` plus tracked runtime/support diff hash `8df8480bf0536cc03d3785e3a74ddb5102e525d5`. Earlier RC3/RC4/RC5 and Done-Done events in this file are superseded historical evidence and do not count as release candidates under the current lifecycle.
- Production author: main. Production changes were local and serial. The earlier timed-out Dream Team writer is terminal; no mutation-capable child remains active.
- Current Runtime Proof: fresh `opencode debug skill` loaded the current `change-ready-sdlc` content and exact Development-Stage field. `opencode debug config` resolved `openai/gpt-5.6-sol`; `opencode debug agent sdet-quality-engineer` exposed no model/variant pin. `npm run doctor -- --project .` reported `active kit required runtime authority | pass`; its overall target-project qualification diagnostic remained blocked only because this kit repository is not itself bootstrapped with a project `AGENTS.md` and validation adapter.
- Differential isolated smoke: fresh primary session `ses_0678b27a5ffetkR52ebYNlgaix`, raw JSON log `C:\Users\Sergey\.local\share\opencode\tool-output\tool_f98763248001i8BPaq0T1raTC8`, ran in `C:\Users\Sergey\AppData\Local\Temp\opencode\make-main-author-runtime-proof-first-stage-smoke-20260725`. Explicit primary `openai/gpt-5.5` differed from portable default `openai/gpt-5.6-sol`. Worker child `ses_0678a094affek64cDn0Niifdhv` inherited `openai/gpt-5.5`, authored only `worker-cli.mjs`, returned an execution request, and accepted raw output in the same child.
- Smoke observations: Ordinary Small emitted `ordinary:cedar->CEDAR`, wrote `CEDAR`, and demonstrated `development -> MVP -> RC1 -> stable`. Material emitted `material:7->49`, wrote `{"input":7,"square":49}`, and correctly stopped at MVP without terminal fixture SDET. Worker emitted `worker:delta->atled` and wrote `atled`. Synthetic staging remained `development` with `networkAttempted=false`; syntax-only unproved work remained `development` with no side effect. `smoke-report.json` was valid, all side effects were reread, the repository status was unchanged by the smoke, and the process returned `CURRENT_STAGE_SMOKE_OK`.
- Critical SDET: fresh test-only child `ses_067cfeaf5ffeOhxL0Xk52kfCaa`, Effective Model `openai/gpt-5.6-sol`, migrated only the eight attributed test/test-support paths and terminated `Action: no-critical-risk`. No production path was edited by SDET. Contracts passed 54/54; the first precondition-valid no-confirmed-critical result permanently stops SDET for this root.
- Validation: `npm run validate:strict` passed with `skills=24 agents=18 markdown=165 warnings=0 infos=1`; `npm test` exited 0 with library 323, Dream Team context 16, validation scripts 3, contracts 54, code-quality inventory 4, Headroom wrapper 11, session plugin 15, feedback ledger 12, init 3, installer 23, OpenSpec gate 8, and pre-push 8. `npm run openspec:validate` passed 9/9. `node tools/test-contracts.ts` passed 54/54. `git diff --check` passed.
- Instruction non-growth: all inventory token proxy `92,176 -> 79,106`; runtime artifacts `76,815 -> 64,889`; `global/AGENTS.md` `12,073 -> 11,427`; frozen changed runtime corpus `51,079 -> 39,198`.
- Known non-critical limitations: Node emitted existing `MODULE_TYPELESS_PACKAGE_JSON` warnings for two copied plugin TypeScript files. `npm audit` reports one high `js-yaml` denial-of-service advisory in the dev-only dependency tree; this change does not modify dependencies and the production dependency list is empty. Malicious local YAML can consume validation-process CPU, but the impact in the accepted local tooling envelope is a recoverable single-process availability failure rather than authorization, privacy, data-integrity, irreversible-action, or system-wide loss. The kit-root doctor target-project bootstrap warning remains diagnostic and does not affect the tested active-authority boundary.
- Critical/non-deferrable disposition: no known confirmed reachable critical or non-deferrable defect remains inside the accepted local operating envelope. The dependency advisory is recorded as a contained non-critical local-tooling limitation.
- Rollback: restore only this change's scoped loaded instructions, roles, mirrors, contracts, validators, tests, and OpenSpec artifacts to their pre-change versions, then restart OpenCode before expecting loaded config-time behavior to change. No rollback was performed.
- Local handoff: complete. The fully qualified current candidate is `RC1`; this same RC is promoted to stable without a soak-time requirement.
- Development-Stage: stable
- Stable Candidate: RC1
- External Operations: not performed. The isolated proof used no credentials, network, remote/shared mutation, deploy, release, publication, activation, archive, commit, or push. An earlier owner-authorized local `npm install` only restored workspace dev dependencies and did not change the lockfile.
