# Strategy History

## 2026-08-31 - Separate successor instead of expanding delivery checkpoints

- **Objective**: Select the smallest owner for proactive recursive task decomposition.
- **Approach**: Compare extending `add-outcome-preserving-delivery-checkpoints` with creating a successor capability.
- **Evidence**: The active predecessor reacts to established delivery drag and is already in final qualification tasks, while the requested behavior triggers before the first parent integration action and has distinct proportional controls and proof scenarios.
- **Outcome**: Selected a separate successor change with an archive-before-acquire dependency on overlapping owners.
- **Reason**: Expanding the predecessor would mix proactive task-shape semantics with reactive route-reconsideration semantics and invalidate its nearly complete evidence population.
- **Do Not Repeat**: Do not fold leaf-first decomposition into the predecessor while its accepted outcome and candidate remain unchanged.
- **Retry Condition**: Reconsider a combined owner only if current canonical evidence after predecessor archive proves the two triggers cannot be expressed or validated independently.

## 2026-08-31 - Reuse the flat dependency DAG

- **Objective**: Represent leaf-before-parent ordering with the least new runtime mechanism.
- **Approach**: Compare a new hierarchical task-network/frontier schema with existing OpenSpec tasks and grind `dependsOn` edges.
- **Evidence**: Frontier schema v1 already stores stable items, explicit dependencies, gates, requirement refs, evidence refs, validates acyclicity, and derives runnable items without semantic inference.
- **Outcome**: Selected existing tasks plus the flat dependency DAG; no parent field, OR-edge, planner store, or schema migration.
- **Reason**: The missing behavior is semantic decomposition and honest task reconciliation, not structural readiness computation.
- **Do Not Repeat**: Do not add a hierarchy schema merely for navigation or because the conceptual model is drawn as a tree.
- **Retry Condition**: Reconsider schema expansion only if an accepted current scenario cannot preserve required ordering, gating, or evidence attribution with existing item ids and dependencies.

## 2026-08-31 - Use evidence boundaries instead of numeric granularity

- **Objective**: Define a project-neutral minimum leaf without over-decomposing routine work.
- **Approach**: Compare numeric file/task/unknown/duration thresholds with an evidence-based independent-failure and proof-boundary criterion.
- **Evidence**: File counts and elapsed time do not reliably distinguish cohesive work from independently failing prerequisites across UI, network, build, hardware, and proof tasks; current kit principles also reject proxy optimization.
- **Outcome**: Selected a semantic leaf contract with explicit direct, mechanical-grouping, same-leaf, and inseparable-integration controls.
- **Reason**: Independent owners, boundaries, oracles, and cleanup paths identify actionable isolation while avoiding Goodhart pressure.
- **Do Not Repeat**: Do not introduce a universal numeric granularity score or depth limit for unchanged evidence.
- **Retry Condition**: Reconsider bounded numeric fields only for explicit storage or runtime capacity safety, never as semantic task-quality inference.

## 2026-09-01 - Close installed population gaps through existing configured lanes

- **Objective**: Complete the declared installed `LFTD-001` population without weakening its proof scope or adding another proof mechanism.
- **Approach**: Extend the existing ordinary configured fixture with grouped-mechanical and integration-only controls, seed the existing OpenSpec fixture with a checked coarse parent that must reopen, and recapture one current-identity request per existing lane.
- **Evidence**: The fresh evidence-sufficiency challenge found that provider-free rows covered all reviewed judgments, but installed observations did not directly exercise grouped mechanical edits, an integration-only failure, or checked-parent reopening; older configured diagnostics also carried prior governed-source digests.
- **Outcome**: Selected current-identity recapture through the two existing configured scenario owners; retained grind owner-gate/checkpoint rows at their production-controller structural ceiling and compaction at its managed-template contract ceiling.
- **Reason**: These are missing observations inside the accepted population, while a new scenario framework, active host synchronization, or a narrowed claim would add mechanism, cross an activation boundary, or reduce accepted proof.
- **Do Not Repeat**: Do not treat provider-free non-frontier green rows, stale configured digests, prescribed expected-file reads, or active-copy drift as current installed population proof.
- **Retry Condition**: Repeat either configured lane only after inspecting a finalized failed run and making a causal fixture, evaluator, source-identity, or environment correction that addresses the observed failure.

## 2026-09-01 - OpenSpec population r1 case-sensitive history oracle failed

- **Objective**: Observe current-identity checked-parent reopening and recursive OpenSpec child correction in one configured request.
- **Approach**: Run `configured-openspec-leaf-first` after provider-free preflight on governed digest `3c2ddef010ae6b6a30253c14bd20f13471b573da90d4bc5b0fcaa4e0738c888a` and pack digest `248b6417b3d5e205838982214accf615189a023767276deca3cc41899ff1e5e9`.
- **Evidence**: Strict disposable OpenSpec validation passed; final proof exited 1 because `history.md` did not contain the exact case-sensitive text `Selected route`; no final result followed, the prompt ended with `UND_ERR_HEADERS_TIMEOUT`, evaluator status was failed, and fixture/process/session cleanup was complete.
- **Outcome**: No OpenSpec population observation accepted from r1.
- **Reason**: The proof oracle overconstrained capitalization of a semantic history label and prevented terminal result materialization; downstream missing event/write-set rows were consequences of that proof failure.
- **Do Not Repeat**: Do not rerun the same case-sensitive history marker or reuse r1 as checked-parent population evidence.
- **Retry Condition**: Normalize semantic planning-marker checks case-insensitively, pass focused fixture/evaluator validation and a new zero-call preflight, then use one new candidate identity.

## 2026-09-01 - OpenSpec population r2 free-form multi-file route stagnated

- **Objective**: Complete the current-identity OpenSpec population after normalizing semantic marker case.
- **Approach**: Let the configured model freely author proactive tasks and then update design, history, and tasks from the reviewed case in one bounded request.
- **Evidence**: Strict OpenSpec validation passed and cleanup was complete, but final proof exited 1 because `design.md` never recorded `leaf-schema-prerequisite`; the prompt again ended with `UND_ERR_HEADERS_TIMEOUT`, so no final event/write-set result was accepted.
- **Outcome**: No OpenSpec population observation accepted from r2; the free-form multi-file mechanism is closed after two similar attempts without reaching the final oracle.
- **Reason**: The one-request lane spends its bounded tool turns authoring four planning states plus the same-leaf control and cannot reliably finish the reviewed recursive delta before the installed prompt timeout.
- **Do Not Repeat**: Do not retry free-form multi-file authoring with wording, marker, or timeout-only changes.
- **Retry Condition**: Use a causally different reviewed fixture-local materializer that applies only the explicit proactive or recursive seed delta after the model selects that phase; preserve one request, exact write-set/effect/cleanup checks, and a prescribed-path claim ceiling.
