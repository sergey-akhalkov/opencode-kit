# Strategy History

## 2026-08-31 - Separate source composition from leaf-first task decomposition

- **Objective**: Select the smallest owner for the requested global composition and independent-testability behavior.
- **Approach**: Compare expanding `add-leaf-first-task-decomposition` with creating an orthogonal successor change.
- **Evidence**: Leaf-first defines execution dependencies, leaf oracles, and parent suppression but explicitly avoids file-count rules and does not govern physical source shape. The requested behavior distinguishes semantic ownership from private implementation modules and changes reuse/implementation decisions even when task structure is unchanged.
- **Outcome**: Selected a separate planning-only successor that composes with leaf-first and waits for its overlapping owners to transfer.
- **Reason**: Combining the changes would mix task-DAG behavior with source ownership, broaden the active claim population, and invalidate attribution for a currently planning-complete predecessor.
- **Do Not Repeat**: Do not fold physical capability composition into leaf-first while its accepted outcome, owners, and evidence population remain unchanged.
- **Retry Condition**: Reconsider one combined capability only if post-archive canonical evidence proves task dependency and source capability shape cannot be specified, implemented, or validated independently.

## 2026-08-31 - Preserve one semantic owner across multiple physical modules

- **Objective**: Prevent monolithic files without creating duplicate behavioral ownership or wrapper-only fragmentation.
- **Approach**: Compare one-owner/one-file, module-per-task, and one semantic owner that may delegate to directly testable private capabilities.
- **Evidence**: Current `extend` semantics forbid copied siblings while current change-locality guidance permits evidenced seams and rejects line quotas. The observed failure mode is the inference that one behavior owner requires one physical source file, while existing reusable modules demonstrate that private component proof and parent integration can remain separate.
- **Outcome**: Selected semantic ownership as the authority boundary and a current contract plus direct oracle as the physical extraction criterion.
- **Reason**: The selected model preserves state/lifecycle/public authority and Rule of Three while allowing one cohesive extraction to reduce current navigation and diagnostic coupling.
- **Do Not Repeat**: Do not use file length, task count, function count, or one semantic owner as an automatic split or no-split decision.
- **Retry Condition**: Reconsider the criterion only if maintained scenarios show a current valuable extraction cannot be identified through contract, oracle, failure/effect boundary, or evidence-backed change axis.

## 2026-08-31 - Extend existing owners and proof families instead of adding a registry or skill

- **Objective**: Make capability reuse and composition globally effective with the least new runtime mechanism.
- **Approach**: Compare a capability registry/new composition skill and proof runner with the current reuse-discovery, architecture/change-locality, Change-Ready, authoring proof, and reuse proof owners.
- **Evidence**: `library-reuse-discovery` already defines bounded current/platform/configured-cross-project/ecosystem search without a registry; loaded main already selects `reuse | extend | build-minimal`; the existing authoring and reuse runners cover the two required permission/effect envelopes.
- **Outcome**: Selected extensions to existing canonical instructions, specs, roles, deterministic contracts, and two current proof families; no new runtime skill, agent, Practice Owner, registry, or third runner.
- **Reason**: The missing behavior is an operational bridge and scenario population, not discovery storage or another decision authority.
- **Do Not Repeat**: Do not add a central inventory, promotion lifecycle, new reviewer, or duplicate proof harness for unchanged evidence.
- **Retry Condition**: Reconsider a new mechanism only if current owner inspection proves an accepted scenario cannot be expressed or safely proved through the existing search, authoring, permission, evidence, and cleanup contracts.

## 2026-09-01 - Separate baseline capture completeness from candidate conformance

- **Objective**: Close unchanged-source reuse characterization without repeating a completed configured capture.
- **Approach**: Inspect the preserved bundles and failed evaluation, then distinguish terminal non-product-mutating capture integrity from reviewed candidate behavior.
- **Evidence**: All four commands exited `0`, sessions and disposable roots were removed, and only `.serena/` operator metadata changed. The first two responses contained every requested field but the collector concatenated commentary and final text without a newline; three baseline observations intentionally differed from the reviewed candidate. Two unchanged evaluations failed because baseline completeness required a parsed observation and byte-order-sensitive whole-map equality.
- **Outcome**: The collector now separates text events, the parser reads the preserved concatenation form, and evaluation reports all changed paths while treating only non-`.serena/` paths as product mutation. Baseline-only evaluation records gaps without demanding candidate conformance; candidate evaluation remains strict.
- **Reason**: A baseline is characterization evidence, not a candidate verdict. Repeating the same provider calls would not correct an evaluator-owned classification error.
- **Do Not Repeat**: Do not recapture a terminal baseline because candidate expectations or operator metadata make baseline evaluation fail.
- **Retry Condition**: Permit another configured capture only after a new finalized capture failure and a causal change to the capture mechanism, oracle, or runtime identity.

## 2026-09-01 - Separate mixed-owner non-expansion from same-owner capability extraction

- **Objective**: Preserve one semantic owner through private capability composition without weakening the existing mixed-responsibility route.
- **Approach**: Compare the `owner-local-extraction` and `mixed-owner-non-expansion` population rows against the delta specs and proposal proof capsule before loaded-source mutation.
- **Evidence**: The reviewed rows require opposite structural outcomes: the same-owner case keeps the semantic owner and delegates privately, while the mixed-owner case removes the superseded mixed path and keeps separate current owners. The earlier delta wording used `mixed`, `navigation-heavy`, or `broadly exercised` as extraction triggers and assigned source-shape selection to Change-Ready.
- **Outcome**: The corrected specs keep mixed-file second responsibility under `architecture-and-change-locality`, treat same-owner private extraction as `extend`, and limit Change-Ready to direct-capability versus parent evidence topology. The proposal capsule now names both paths separately.
- **Reason**: Physical delegation cannot cure unrelated semantic ownership in one file, and exercise breadth is not a falsifiable architecture boundary.
- **Do Not Repeat**: Do not use `mixed`, file size, navigation frequency, exercise breadth, or task count as shorthand for a private capability boundary.
- **Retry Condition**: Reconsider the distinction only if current scenario evidence proves one accepted case cannot preserve semantic ownership, mixed-owner non-expansion, and truthful direct/parent oracles together.

## 2026-09-01 - Separate selected-candidate facts from source-layer availability

- **Objective**: Close the matched reuse population without accepting popularity, misstating candidate fit, or repeating passing configured rows.
- **Approach**: Inspect each failed evaluation fact, scope `Contract fit` to the selected candidate, supply explicit reviewed cross-project metadata where present, and distinguish an immaterial stopped layer from decision-relevant unavailable evidence.
- **Evidence**: The first candidate capture selected `local-minimal` but correctly reported its fit as `unknown`, while the expectation described the rejected popular package as `mismatch`; another row inferred cross-project verification from source verification. After those seed/fixture corrections, the current-repository row still reported `degraded` because the skill made an unconfigured source degraded even after an earlier verified fit had already satisfied the stop rule.
- **Outcome**: The reviewed expectation now records the selected local candidate's unknown fit, fixture metadata distinguishes source verification from cross-project state, and `reuse-discovery` uses `not-applicable` only when a decisive fit makes the later layer immaterial while retaining `degraded` for decision-relevant unavailable evidence. The three passing rows were preserved; only the remaining current-repository row was recaptured after the instruction correction, and all four candidate observations now pass with matched identities, zero product changes, and terminal cleanup.
- **Reason**: Candidate contract fit and discovery-layer availability are independent facts. Treating a rejected package's mismatch as the selected build's fit or treating every stopped layer as degraded makes the compact disposition internally inconsistent.
- **Do Not Repeat**: Do not recapture a passing configured row because another row failed, conflate source verification with cross-project verification, or use `not-applicable` for decision-relevant unavailable evidence.
- **Retry Condition**: Reopen these classifications only if a current reviewed scenario shows the same selected candidate and source-layer state cannot be represented independently under the stop rule.

## 2026-09-01 - Prefer semantic captured facts and explicit permissions after the delegated-row failure

- **Objective**: Complete the configured authoring controls without repeating terminal passing rows or accepting a native CLI failure.
- **Approach**: Inspect the failed delegated bundle, preserve its cleanup and partial facts, replace CLI `--auto` with the already verified explicit proof permissions, align the direct oracle with the prompted private capability, and evaluate brief/source boundaries semantically from captured files and commands.
- **Evidence**: The first delegated command edited the correct owner and private capability and ran both requested entry points, then terminated with Windows `0xC0000409`, empty stderr, no Application crash record, a deleted session/root, and no surviving proof process. Its serialized oracle also required an unprompted `scripts/run-capability.mjs` wrapper and exact brief sentences. Separately, a terminal owner-local row used private `src/_normalize.mjs`, delegated correctly, and produced both outputs, while the old oracle required the arbitrary filename `src/normalize.mjs`.
- **Outcome**: The successor uses explicit allow rules without `--auto`, executes `src/capability.mjs` directly, accepts equivalent semantic boundary markers, and recalculates corrected owner-local facts from immutable captured files and command results. The three terminal first-attempt rows and five causally distinct successor rows all evaluate complete; no candidate row called a skill or Practice Owner, and no proof process survived.
- **Reason**: Permission automation, physical filenames, wrapper paths, and exact prose are not accepted product semantics. Command exit, direct and parent outputs, owner delegation, bounded brief paths, file facts, and cleanup are.
- **Do Not Repeat**: Do not accept `0xC0000409` as success, require an unprompted wrapper or exact sentence as a semantic oracle, or repeat terminal passing configured rows after another row fails.
- **Retry Condition**: Reconsider the explicit-permission invocation or semantic readback only if a current terminal row fails under the same mechanism and its preserved diagnostics identify a different causal boundary.

## 2026-09-01 - Bound the finite claim at one explicit reporting mismatch

- **Objective**: Reconcile the complete `CCO-001` population under one current candidate without hiding a row or chasing configured verdicts.
- **Approach**: Restore byte-identical baseline fixtures, make reuse pairing verify input bytes, correct loaded selected-target and source-layer semantics, capture all authoring and reuse rows under one candidate/source/model/seed identity, and repeat only the provider-free evaluators.
- **Evidence**: `candidate:cco-instructions-r9` shares the same global instruction, scenario-helper, semantic-seed, model, profile, and variant identities across twelve disposable repositories. Both evaluator passes report eight complete authoring rows. Reuse reports three complete rows and one current `popular-contract-mismatch` row whose disposition is `build-minimal`, selected target is `local-minimal`, fit is `unknown`, cost is lower, cross-project is degraded, fixtures match, and product changes are empty; only the ambiguous `Popularity only` field is `yes` instead of the seed's `no`.
- **Outcome**: The population is current and fully enumerated with one explicit reporting mismatch rather than a substituted representative. The maximum claim remains limited to the reviewed installed environment and selection/ownership behavior; it does not claim universal decomposition, reuse, compatibility, line reduction, or exact popularity-label interpretation.
- **Reason**: The accepted selection behavior is observed, but the compact output label can describe either the selected target's basis or the rejected candidate's only supporting evidence. Another configured call would seek a preferred label, not new product behavior.
- **Do Not Repeat**: Do not change disposable fixtures relative to baseline, recapture a terminal population for evaluator wording, or broaden eleven current rows over the one explicit mismatch.
- **Retry Condition**: Reopen configured capture only if a changed loaded candidate or new requirement changes disposition, selected target, contract/cost, source-layer state, effects, or another accepted observable; evaluator-only clarification uses preserved r9 bundles.

## 2026-09-01 - Separate selected-target proof from an ambiguous popularity label

- **Objective**: Close the one reproduced evaluator gap without changing loaded behavior or making another configured call.
- **Approach**: Keep actual output parsing strict, encode accepted `Popularity only` alternatives in reviewed seed data for only `popular-contract-mismatch`, and require the expected disposition and selected target before either label can pass.
- **Evidence**: The preserved r9 row selected `local-minimal`, reported `build-minimal`, unknown fit, lower cost, degraded cross-project evidence, matching fixtures, and no product changes, but emitted `Popularity only: yes`. The red control emits `yes` while selecting `popular-parser` and changing the disposition to `reuse`.
- **Outcome**: A focused regression accepts `yes` or `no` only when the reviewed local target remains selected, while the popularity-only red control still fails. Focused reuse tests report 17 checks, provider-free preflight passes, and two zero-call evaluations of the immutable r9 bundles both report four complete rows with identical SHA-256.
- **Reason**: One label can describe either the chosen target's selection basis or the rejected candidate's only supporting evidence; disposition and selected target are the unambiguous accepted behavior.
- **Do Not Repeat**: Do not score model prose, demand one ambiguous label after the selected target is already explicit, or use evaluator wording to justify another provider call.
- **Retry Condition**: Reopen only if a current row selects the rejected candidate, changes the disposition/fit/cost/layer state, or a new requirement defines one exclusive label meaning.

## 2026-09-01 - Keep the reviewer result at the finite evidence ceiling

- **Objective**: Challenge the frozen CCO candidate once without turning residual evidence gaps into new product behavior or universal claims.
- **Approach**: Supply the original outcome, current candidate identity, complete twelve-row population, configured/provider-free evidence, historical failures, and exact non-goals to one fresh read-only evidence-sufficiency reviewer; main then dispositions every returned risk.
- **Evidence**: The reviewer returned `no-material-finding` for `candidate:cco-instructions-r9`. `CCO-EVD-01` observed that frozen source maps omit `global/principles-of-work.md` and `global/skills/change-ready-sdlc/SKILL.md`; `CCO-EVD-02` observed that two ecosystem rows did not load `reuse-discovery` and negative controls prove explicit filename/stdout predicates rather than semantic uniqueness.
- **Outcome**: Main classifies both rows as claim-ceiling gaps, not current product defects. Runtime claims remain tied to the explicitly hashed r9 surfaces and twelve named oracles; static validation covers the complete principles/Change-Ready source without pretending their bytes were frozen in r9, skill causation is claimed only where observed, and exact negative-control facts are not generalized.
- **Reason**: Reviewer evidence cannot authorize mutation, and neither row reproduces a broken accepted disposition, owner boundary, direct/parent oracle, effect envelope, or cleanup invariant.
- **Do Not Repeat**: Do not convert omitted hash keys into inferred runtime identity, infer skill execution from matching output, or generalize filename controls into universal semantic uniqueness.
- **Retry Condition**: Reopen only if current validation changes a loaded candidate source, a named oracle fails, or new evidence makes one gap affect the accepted finite claim rather than its ceiling.

## 2026-09-01 - Validate the complete finite candidate before archive

- **Objective**: Close CCO implementation with current runtime, structural, repository, and OpenSpec evidence and no activation claim.
- **Approach**: Run focused instruction/reuse/contracts/library/context checks, canonicalization, both provider-free packs, both current configured evaluators, full project tests, strict library validation, instruction inventory, selected/all strict OpenSpec validation, operation-gate readback, diff checks, and scoped status/secret inspection.
- **Evidence**: Focused checks report reuse 17, contracts 76, library 181, and instruction context 15; canonicalization changes zero files; authoring and reuse preflights report zero model calls and removed cleanup; current evaluators report 8 and 4 complete rows; `npm test` exits zero; strict validation reports 34 skills, 22 agents, 1114 Markdown files, zero warnings, and two known infos; all 32 OpenSpec items pass; the apply gate passes; `git diff --check` is clean.
- **Outcome**: Every accepted CCO task and named population member is current at its bounded evidence ceiling. No registry, new owner, numeric score, public dependency, consumer refactor, install, restart, activation, publication, or remote mutation entered the candidate.
- **Reason**: Archive requires complete accepted scope and representative installed proof plus project-native validation, not an unbounded or activated-host claim.
- **Do Not Repeat**: Do not substitute focused tests for the full suite, treat line-ending warnings as diff errors, or report synthetic secret fixtures outside CCO scope as candidate secrets.
- **Retry Condition**: Reopen validation only if archive preparation or ownership transfer mutates current candidate bytes or a final gate identifies a new in-scope failure.
