## 1. Ownership And Baseline

- [x] 1.1 Recheck active OpenSpec ownership, current Git/source/config identity, the canonical `session-completion-guard` requirements, the eight privacy-safe overflow diagnostics, and `completion-arbiter-budget-v1` population before production mutation; record one non-overlapping ownership/evidence manifest or stop on an active writer. **Current Rung:** planning plus read-only source/database investigation. **Next Real Boundary:** current production request builder over a reviewed disposable long-root fixture. **Blocker/Unblocker:** current ownership and source hashes; any overlapping completion-guard/hidden-agent/test/proof writer must become terminal or transfer exact paths. **Observable Proof:** manifest names Product Candidate, Proof Runner, Evaluator, Environment, exact owned paths, current 200,000 byte config, observed overflow byte range/count, and no unrecognized path mutation. **Authorization:** repository planning/evidence writes and privacy-safe read-only local database inspection only. **Safeguards:** no raw session id/content, config change, provider call, install, restart, commit, or active-change mutation. **Restoration/Cleanup:** no runtime state; leave the OpenCode database unchanged. **Expected Immutable Evidence:** task 1.1 ownership/source/incident-count record and matching evidence-index row. **Validation:** operation-gate ownership check, scoped Git status, and direct source/spec readback.

- [x] 1.2 [automation-dividend] Extend the existing long-run proof owner with effect-free `--help`, a reviewed fixture mode, a read-only overflow-incident mode, stable raw/evaluation output, and two-pass replay; capture the unchanged baseline request shape and prove the reviewed fixture exceeds the current budget without creating a child or model call. **Current Rung:** verified ownership and baseline identities. **Next Real Boundary:** candidate production request builder against the same reviewed fixture. **Blocker/Unblocker:** task 1.1; private-content emission, mutable database access, unstable replay, or cleanup uncertainty blocks production mutation. **Observable Proof:** help states inputs/effects/evidence/cleanup; fixture records declared retained counts and baseline exact bytes above 200,000; incident mode finds the exact privacy-safe terminal population without emitting ids/text/paths; two replays have identical evaluated digests and zero model calls. **Authorization:** disposable database/evidence writes and read-only `OPENCODE_DB` access. **Safeguards:** no semantic inference, provider call, raw incident retention, or modification of current overflow rows. **Restoration/Cleanup:** delete proof-owned databases/temp roots and close every read-only handle. **Expected Immutable Evidence:** baseline fixture bundle, privacy scan, incident shape summary, two replay digests, and matching evidence-index row. **Validation:** focused proof CLI tests plus exact cleanup/readback.

## 2. Canonical Request Happy Path

- [x] 2.1 Extend `arbiter-evidence` with the private versioned canonical projection, conflict detection, stable todo memberships, conditional validation-output refs, compact serialization, exact final-byte measurement, and bounded contribution diagnostics while retaining the full source snapshot for epoch/verdict validation. **Current Rung:** baseline fixture reproduces terminal overflow with a trustworthy evaluator. **Next Real Boundary:** production request builder emits and round-trips one candidate request for the same fixture. **Blocker/Unblocker:** task 1.2; do not change public projection, hidden-agent instructions, or controller behavior until canonical readback is green. **Observable Proof:** candidate request is byte-stable, at most 175,000 proof-control bytes and 200,000 runtime bytes, reconstructs the source authority/liveness/todo/validation/truncation/claim matrices, and reports content-free section contributions. **Authorization:** scoped completion-guard production and focused fixture edits only. **Safeguards:** no semantic summarization, dropped required claim/descendant fact, config increase, public schema change, child, or model call. **Restoration/Cleanup:** fixture-only memory/temp state is terminal and removed. **Expected Immutable Evidence:** candidate builder raw/evaluation bundle, source hashes, matrix comparison, exact bytes/contributions, and evidence-index row. **Validation:** focused pure-module tests and two candidate serializations.

- [x] 2.2 Run-observe-correct the smallest complete provider-free controller path: the production `SessionCompletionController` receives the reviewed fixture, crosses `requireBoundedRequest`, creates one fake arbiter child, submits the exact candidate text once, parses one schema-valid correlated verdict, and reaches the expected terminal state. **Current Rung:** canonical production builder fits and round-trips. **Next Real Boundary:** loaded hidden-agent/input-schema and public compatibility boundary. **Blocker/Unblocker:** task 2.1; any byte mismatch between measured and submitted text, duplicate child/prompt, stale correlation, or unknown cleanup blocks dependent work. **Observable Proof:** one request no larger than 200,000 bytes reaches the fake `session.prompt`; child/prompt count is one; verdict/root state and request correlation match; public/session database effects are explicit; cleanup is complete. **Authorization:** provider-free fake client and disposable session/database effects. **Safeguards:** all tools denied, no configured inference, no real OpenCode database write, no retry amplification. **Restoration/Cleanup:** terminate fake client/controller and remove fixture data. **Expected Immutable Evidence:** controller raw/evaluation bundle with exact invocation, request digest/bytes, verdict, state, call counts, diagnostics, and cleanup. **Validation:** focused controller and long-run suites.

- [x] 2.3 Update the hidden arbiter input contract and focused validators atomically with the private request schema, preserve the verdict schema and public `session_delivery_context` schema, and add negative regressions for conflicting canonical refs, irreducible required-claim overflow, descendant graph truncation, and ordinary sub-limit behavior. **Current Rung:** provider-free complete happy path through the production controller. **Next Real Boundary:** privacy-safe replay of all observed overflow sessions on the integrated candidate. **Blocker/Unblocker:** task 2.2; no configured call until hidden-agent validation, public compatibility, and all zero-prompt controls are green. **Observable Proof:** agent/validator accepts only the new private input shape; public projection before/after fixtures are equal; each critical negative control remains terminal with zero child/model/continuation; ordinary sub-limit fixture preserves expected disposition and side-effect class. **Authorization:** hidden-agent instruction, validator, focused test, and disposable fixture edits only. **Safeguards:** arbiter permissions remain all-tools-denied; no verdict/continuation/claim semantics change; no public compatibility shim or config edit. **Restoration/Cleanup:** no loaded activation; remove fixtures. **Expected Immutable Evidence:** input-contract validation, public compatibility diff, negative-control matrix, permission/tool map, focused results, and evidence-index row. **Validation:** agent validator, session-env plugin tests, completion-guard focused suite, and strict selected OpenSpec validation.

## 3. Population And Real-Boundary Proof

- [x] 3.1 Run the integrated candidate's read-only incident mode over the configured database and provider-free replay over the reviewed finite population; verify all eight recorded overflow roots now produce candidate requests within 200,000 bytes or remain explicitly classified as irreducible critical overflow, without persisting private content. **Current Rung:** integrated provider-free candidate and critical controls are green. **Next Real Boundary:** same-actor matched baseline/candidate hidden-arbiter path. **Blocker/Unblocker:** tasks 2.1-2.3; any population mismatch, private marker, request over budget without the declared critical control, unstable evaluator, or unknown database handle keeps this lane blocked. **Observable Proof:** exactly eight incident rows are accounted for by privacy-safe refs/counts; each has source and candidate bytes plus terminal disposition; reviewed fixture/controls have unique current rows; two provider-free replays are identical; zero model calls and zero database writes occur. **Authorization:** read-only local database observation and disposable evidence output. **Safeguards:** no raw ids, paths, prompts, commands, claims, or tool output; no source/provider/host mutation. **Restoration/Cleanup:** close handles and remove temp outputs while retaining bounded redacted bundles. **Expected Immutable Evidence:** population inventory, incident/fixture raw and evaluation bundles, privacy scan, replay digests, cleanup, and evidence-index rows. **Validation:** replay/readback evaluator and focused proof suite.

- [x] 3.2 After provider-free preflight and a clear live-attempt gate, run one low-ambiguity same-actor baseline/candidate pair through the installed disposable OpenCode boundary with identical model/variant, human request, initial database, claim facts, permissions, and environment, then run one separate candidate audit with the reviewed long-root fixture that exceeded the baseline limit; observe bounded candidate requests and expected correlated verdicts without activating ordinary-user config. **Current Rung:** complete provider-free finite population and incident replay. **Next Real Boundary:** terminal matched observation matrix, configured long-root result, and installed candidate cleanup. **Blocker/Unblocker:** task 3.1 plus exact current provider/runtime/source identities; any failed configured/evaluator attempt preserves its raw bundle and blocks unchanged repetition until complete offline replay is terminal. **Observable Proof:** baseline and sub-limit candidate produce the expected disposition, requirement refs/statuses, claim matrix, unresolved cardinality, tool-denied state, root side-effect class, and cleanup; the separate long-root candidate request is within 200,000 bytes and reaches its expected schema-valid correlated verdict; differences in free-form wording are excluded explicitly; at most three configured calls occur. **Authorization:** at most three bounded non-sensitive configured calls and disposable local OpenCode sessions/processes under standing local authorization. **Safeguards:** no credentials in artifacts, no ordinary config activation, no remote/product/deployment/release effect, exact source/environment correlation, finite timeout/calls. **Restoration/Cleanup:** delete proof sessions/processes/config/data/project roots and preserve terminal cleanup evidence. **Expected Immutable Evidence:** baseline/sub-limit-candidate raw bundles, matched observation matrix, separate long-root candidate raw bundle, request bytes/digests, source/environment identities, stdout/stderr, effects, cleanup, offline replay, and evidence-index rows. **Validation:** installed capture evaluator, complete reachable offline finalization replay for both configured paths, and focused regression rerun.

- [x] 3.3 Materialize the current `completion-arbiter-budget-v1` claim rows from reviewed fixture, negative controls, eight incident observations, matched pair, and installed candidate evidence; run one fresh read-only evidence-sufficiency challenge and independently disposition only current evidence-backed rows without widening the proposal's maximum claim. **Current Rung:** current candidate has provider-free and installed real-boundary evidence. **Next Real Boundary:** structurally and semantically closed finite-population claim ready for final validation. **Blocker/Unblocker:** task 3.2; missing/stale/duplicate rows, unqualified real oracle, unavailable challenge, or unresolved cleanup keeps only the broad claim `unknown` while preserving narrower exact proof. **Observable Proof:** every declared population member has one unique current terminal row; baseline/candidate paths and environment match where required; real oracle is observed; independent challenge status is complete; disposition and maximum claim are derived from explicit reviewed records only. **Authorization:** deterministic evidence-index materialization and one read-only specialist challenge. **Safeguards:** no reviewer authority, semantic helper scoring, equivalent verdict-seeking rerun, source mutation from review, or universal model-equivalence claim. **Restoration/Cleanup:** no runtime state; retain bounded claim/challenge refs. **Expected Immutable Evidence:** schema-valid evidence index, observation matrix, challenge reference, main disposition, operation-gate output, and claim evaluator result. **Validation:** evidence-index readback/materialization twice with identical digest and apply operation gate.

## 4. Validation And Handoff

- [x] 4.1 Run applicable project-native validation and inspect the complete candidate diff: focused completion-guard/session-env/agent/proof suites, strict selected/all OpenSpec validation, proof inventory/readiness, instruction/runtime-source inventories, `npm test`, `npm run validate:strict`, and `git diff --check`; correct only accepted-outcome defects and replay affected proof lanes. **Current Rung:** accepted scope and finite-population claim evidence are complete. **Next Real Boundary:** local implementation handoff without ordinary activation. **Blocker/Unblocker:** tasks 1.1-3.3; any non-zero command, stale candidate/environment, private artifact, unknown cleanup, overlapping owner, or mismatched claim row keeps the affected lane incomplete. **Observable Proof:** all applicable commands exit zero with inspected stdout/stderr; source, request, evaluator, environment, claim, permission, cleanup, and public compatibility identities remain current; no unrelated change is modified. **Authorization:** provider-free validation/readback only; no new configured call, install, restart, commit, push, release, or deployment. **Safeguards:** preserve unrecognized worktree changes, exact request limit, critical fail-closed controls, and scoped evidence claims. **Restoration/Cleanup:** remove only proof-owned disposable outputs; retained evidence remains bounded/redacted. **Expected Immutable Evidence:** complete command/result matrix, scoped diff review, final operation gates, candidate/runner/evaluator/environment refs, limitations, rollback/restart note, and cleanup. **Validation:** commands listed above plus final `openspec status --change fit-completion-arbiter-evidence-budget`.
