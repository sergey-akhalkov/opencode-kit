# Completion Arbiter Budget Candidate Validation

Candidate: `completion-arbiter-budget-v1`

## Calibrated Population

- The accepted unchanged reviewed fixture is `254687` bytes in `task-1-2-baseline-r7`, above the unchanged `200000` runtime limit and inside the eight observed source sizes (`214535` through `254691`).
- The candidate request for the same reviewed retained counts and four ordinary claims is `124421` bytes in `task-2-1-candidate-r3`, below both the `175000` proof-control ceiling and the runtime limit.
- Authority, liveness, todo membership, validation/tool relationships, truncation, claim closure, source immutability, and compatibility-alias omission all read back true. The complete fake-controller path submits one exact `124464`-byte request to one child and reaches the correlated expected terminal state. Two offline candidate evaluations are identical.

## Incident Population

- `task-3-1-incidents-r1` reopened the configured database read-only with `query_only=ON`, closed it, and retained no raw ids, paths, prompts, commands, claims, or tool output.
- All eight privacy-safe incident members fit the candidate: source sizes `214535` through `254691` become candidate sizes `141090` through `187726`, each below `200000`.
- Two independent incident replays are terminal and identical with zero child/model calls and zero database writes.

## Failure Controls

- Focused guard tests cover same-ref canonical disagreement as a fail-closed conflict.
- Irreducible claim closure remains terminal overflow and reports only structural surface names and byte counts.
- Descendant/truncation readback stays in the required matrix, and the maintained provider-free suite preserves retry, route, retention, malformed-verdict, scheduler, and cleanup controls.

## Installed Boundary

- OpenCode `1.18.25` loaded the exact reviewed fixture with all declared retained counts and four ordinary claims in one disposable hidden child.
- The installed candidate sent one `124423`-byte private-canonical request, received one correlated `allow_stop` verdict with all four claim rows, made zero primary or external calls, and removed its root, child, process, config, and database fixture.
- Offline replay of the exact installed reviewed-fixture raw bundle is terminal and complete.
- The separate 32-message/2500-character installed profile remains supplemental evidence only; it is not used as a substitute for the reviewed fixture.

## Matched Ordinary Pair

- The same installed executable, two-message/128-character profile, local provider, controller, projection, and side-effect envelope exercised both representations.
- Legacy request schema 1/public evidence schema 2 used `5409` bytes; candidate request schema 2/private evidence schema 1 used `3679` bytes.
- Both arms made three local primary calls including official activation, one hidden arbiter call, zero external calls, returned `allow_stop` with empty requirement and claim matrices, reached `passed`, and completed cleanup.

## Claim Ceiling

Evidence supports exact fixture readback, incident byte/disposition fit, local-provider matched transport/matrix/side effects, and the exact installed reviewed-fixture path only. Incident semantic equivalence, real-model substitution, other providers/models/OpenCode versions, unreviewed shapes, Cartesian maximum-text/cardinality combinations, and universal semantic equivalence remain excluded.
