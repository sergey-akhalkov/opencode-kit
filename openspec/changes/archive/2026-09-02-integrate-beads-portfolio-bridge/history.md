# Strategy History

## 2026-09-01 - Replace existing lifecycle owners with Beads

- **Objective:** Minimize custom workflow mechanisms by making Beads the universal tracker and execution owner.
- **Approach:** Replace Kaizen promotion, OpenSpec tasks, `grind_frontier`, Campaign/Mission orchestration, and writer leases with Beads issues, dependencies, ready state, and claims.
- **Evidence:** Current source shows Kaizen owns privacy-safe signal evidence and decisions, `grind_frontier` owns protected task-scoped gates, and Campaign/Mission own durable source-writer leases. The selected Beads release exposes issue coordination but not those exact safety, decision, proof, archive, or writer-liveness contracts.
- **Outcome:** Rejected in favor of a narrow admitted-portfolio bridge.
- **Reason:** Replace-all would erase unique authority and safety semantics and make a third-party issue state an unsupported source-mutation permission.
- **Do-Not-Repeat Condition:** Do not propose Beads as the universal owner while these distinct current contracts remain accepted and Beads lacks equivalent proven boundaries.
- **Evidence-Based Retry Condition:** Reconsider only through a separate Material proposal after current runtime evidence proves exact contract equivalence and an accepted owner decision selects replacement rather than coexistence.

## 2026-09-01 - Use vendor OpenCode setup and always-loaded guidance

- **Objective:** Integrate Beads quickly with the vendor-maintained OpenCode path.
- **Approach:** Run `bd setup opencode`, use its managed `AGENTS.md` section, load `bd prime`, or add the Beads MCP catalog to normal sessions.
- **Evidence:** Official integration documentation says setup creates or updates managed `AGENTS.md`; the inspected vendor section declares Beads for all issue tracking. The kit already has canonical OpenSpec, Kaizen, frontier, and writer owners, and context-cost improvement is unproved.
- **Outcome:** Rejected in favor of an explicit full `core-beads` profile, one on-demand skill, and a closed CLI adapter.
- **Reason:** Vendor setup creates conflicting always-loaded lifecycle authority, while prime/MCP can increase startup context and permission surface without adding a required current oracle.
- **Do-Not-Repeat Condition:** Do not run or copy vendor setup, prime-at-startup, MCP, or all-tracking instructions in this increment.
- **Evidence-Based Retry Condition:** Reconsider only if a later pinned release provides a non-mutating OpenCode integration mode whose exact loaded behavior is proven compatible and lower-cost through matched outcomes.

## 2026-09-01 - Treat isolated project databases as a cross-project portfolio

- **Objective:** Obtain global opportunity visibility while preserving project isolation.
- **Approach:** Initialize independent embedded Dolt stores per project and describe their combined state as one cross-project graph.
- **Evidence:** Each embedded store owns only its project-local data; no stable selected-release federation, server, or kit-owned aggregate index was proved. Architecture review identified this as an unsupported ownership claim.
- **Outcome:** Corrected to one-project scope with cross-project visibility outside `BPB-001`.
- **Reason:** Isolated databases do not create a truthful shared query, readiness, assignment, or transaction owner.
- **Do-Not-Repeat Condition:** Do not claim cross-project portfolio behavior from multiple project-local stores alone.
- **Evidence-Based Retry Condition:** Reconsider after two project-local pilots and direct proof of one explicitly selected central portfolio, stable federation/server, or read-only aggregate owner with privacy and recovery contracts.

## 2026-09-01 - Add Beads beside Kaizen Grind SQLite work items

- **Objective:** Preserve the active Kaizen Grind design while gaining Beads coordination features.
- **Approach:** Implement Kaizen SQLite `work_items` and mirror admitted items, status, dependencies, assignment, and closure into Beads.
- **Evidence:** The active unimplemented Grind proposal already assigns work-item identity, transitions, claims, routing, and closure to SQLite. Beads offers overlapping issue identity, dependencies, readiness, assignment, and terminal state, creating bidirectional synchronization and crash-recovery ambiguity.
- **Outcome:** Rejected; current design requires one admitted-work owner before production integration.
- **Reason:** Mirroring would create duplicate semantic authority and turn every interruption into a reconciliation problem with no safe precedence.
- **Do-Not-Repeat Condition:** Do not implement either production work-item layer until current planning delegates admitted portfolio identity to exactly one owner.
- **Evidence-Based Retry Condition:** Reconsider only if later evidence proves Beads cannot satisfy the selected bounded contract and the Beads path is removed rather than mirrored.

## 2026-09-01 - Select a bounded one-project portfolio bridge

- **Objective:** Reuse Beads where it lowers custom coordination cost without weakening existing lifecycle owners.
- **Approach:** Pin the tested recovery release, prove it in a disposable Windows root, install it through existing workstation ownership, expose one exact full `core-beads` on-demand profile, enable one project-local embedded store, correlate only evidence-triaged Kaizen decisions, and close through verified OpenSpec terminal evidence.
- **Evidence:** Official CLI documentation exposes exact release, init flags, metadata filters, external refs, dependencies, ready/show, assignment/claim, config, where, doctor, and Dolt behavior. Current kit source provides the retained signal, frontier, profile, workstation, OpenSpec, and writer owners needed around the narrow adapter. Runtime fit remains unproved and is task 1.1.
- **Outcome:** Selected for `BPB-001` planning, with production work gated by disposable proof and Grind ownership reconciliation.
- **Reason:** This is the smallest useful bridge that can test actual value while containing persistence, context, remote, concurrency, and authority risk.
- **Do-Not-Repeat Condition:** Do not expand to another project, central database, server/federation, remotes, production claims, automatic voting, or source mutation inside this change.
- **Evidence-Based Retry Condition:** Revise the route only when task 1.1 or later current proof falsifies the selected pin, embedded topology, adapter boundary, or one-project operational value.

## 2026-09-01 - Compose a Beads-only partial profile with core

- **Objective:** Keep Beads out of default core while allowing an operator to add only its on-demand artifacts.
- **Approach:** Add a partial `beads` profile and describe selection as `core + beads` composition.
- **Evidence:** Current `tools/install-opencode-global.ts` accepts only concrete full `core | all` identities, and `tools/runtime-surface-profile.ts` materializes one named profile rather than a union. A Beads-only tree would omit core authority; adding a skill under full `global/` source also makes it discoverable in all/unprofiled catalogs.
- **Outcome:** Rejected in favor of one exact full `core-beads` profile, with truthful all/unprofiled full-source discovery and unchanged core.
- **Reason:** The partial composition was not an existing runtime mechanism and its proof could pass on a fixture that operators cannot install safely.
- **Do-Not-Repeat Condition:** Do not claim composable partial profiles until the selected current installer/profile owner actually implements and proves that contract.
- **Evidence-Based Retry Condition:** Reconsider composition only after another accepted profile needs the same mechanism and one reviewed full-profile alternative has higher total maintenance/proof cost.

## 2026-09-01 - Use one universal bridge adapter and delete every Grind work-item record

- **Objective:** Minimize the number of new bridge modules and eliminate duplicate admitted-work state.
- **Approach:** Give one adapter install/project/Beads/Kaizen/OpenSpec operations and make Beads replace all Grind `work_items` records.
- **Evidence:** Architecture review of `BPB-SPEC-r1` showed the workstation already owns binary lifecycle, Kaizen owns signal transitions, and the vendor seam should only invoke `bd`. Campaign/Mission still require durable Grind-owned execution/routing/retry/gate correlation even when Beads owns portfolio identity.
- **Outcome:** Corrected to three existing/local owners: workstation binary/profile lifecycle, narrow vendor adapter, and Kaizen-owned promotion/link/terminal orchestrator; Grind retains a non-portfolio execution record referencing the Beads ID.
- **Reason:** The universal facade mixed failure/rollback owners, while deleting every execution record removed required Campaign/Mission handoff state rather than only duplicate portfolio authority.
- **Do-Not-Repeat Condition:** Do not merge installer, vendor CLI, Kaizen lifecycle, OpenSpec terminal decisions, or Grind execution state into one bridge owner.
- **Evidence-Based Retry Condition:** Reconsider only if current implementation proves one of those owners has been removed or their exact state/effect contracts have become identical.

## 2026-09-01 - Roll back installation state independently of bridge-writer liveness

- **Objective:** Keep workstation rollback independent from project-local bridge coordination.
- **Approach:** Allow the workstation owner to remove matching binary, profile, adapter, registration, config, or lock material without acquiring the bridge lease because project disable and binary/profile rollback are separate operations.
- **Evidence:** Corrected-candidate architecture review `BPB-ARCH-007` showed the bridge lock lived in installation-owned state while no contract prevented independent rollback from deleting that state during a live or unknown child `bd`/Dolt write.
- **Outcome:** Rejected; the workstation lifecycle now solely owns lock-artifact storage/identity/deletion, and rollback of registration-referenced managed material must acquire the same lease or preserve the lock and every referenced item as a partial unknown result.
- **Reason:** Removing the coordination artifact or referenced executable/adapter/registration while a late writer can still mutate recreates duplicate-create and unrepaired writer races despite a correct bridge-operation lease.
- **Do-Not-Repeat Condition:** Do not infer safe rollback from timeout, cancellation acknowledgement, elapsed time, or an absent PID, and do not delete the lock or registration-referenced material while writer closure is active or unknown.
- **Evidence-Based Retry Condition:** Reconsider only if a later owner removes project-local child writers entirely or proves another exact revocation mechanism that prevents all late project writes before managed state is removed.

## 2026-09-01 - Initialize an empty repository without prepared Dolt ignore policy

- **Objective:** Prove that the pinned non-interactive embedded initialization changes only `.beads` and the reviewed local exclude file.
- **Approach:** Initialize an empty disposable Git repository with `bd init --prefix BPB --non-interactive --skip-agents --skip-hooks --setup-exclude --sandbox --json` after redirecting Windows profile, AppData, XDG, and global Git identities and blocking outbound traffic for the verified executable.
- **Evidence:** The verified `v1.2.2` process exited 0 and created the expected embedded store and `.git/info/exclude` entries, but also created a project-root `.gitignore` containing Dolt and Beads credential/proxy patterns. The init parent briefly left one proof-owned child `bd.exe`, which terminated naturally before another mutation. No user profile, workspace `.beads`, hook, remote, or tracked-file effect was observed.
- **Outcome:** Rejected as the activation route for a project without a pre-reviewed ignore policy.
- **Reason:** Automatic project-root `.gitignore` creation is outside the accepted activation effect envelope and would make installed-pilot source bytes differ from the reviewed candidate.
- **Do-Not-Repeat Condition:** Do not invoke project activation when the exact required Dolt and Beads ignore entries are absent from the reviewed tracked project baseline, and do not treat an untracked `.gitignore` as harmless merely because no prior tracked bytes changed.
- **Evidence-Based Retry Condition:** Use a fresh disposable repository whose tracked baseline already contains the exact observed ignore entries, then prove byte-for-byte stability across initialization; the future preview must fail before `bd` invocation when that prerequisite is absent or drifted.

## 2026-09-01 - Require a prepared tracked Dolt ignore policy before activation

- **Objective:** Preserve reviewed project bytes while proving the exact pinned embedded lifecycle and atomic Beads correlation boundary.
- **Approach:** Recreate the disposable repository with the exact observed Dolt and Beads credential/proxy ignore entries already present in the tracked index; keep the project-local `.git/info/exclude` change explicit, redirect Windows profile/AppData/XDG/global-Git identities, disable anonymous metrics in the isolated state, and block outbound traffic for the verified executable.
- **Evidence:** The release API and downloaded archive matched SHA-256 `1f00c29cd9599e182a4a4e829f5210daca2da14155920aee2836d8bc613b2feb`; extracted `bd.exe` was SHA-256 `b1f3609fea1d9f0f19b2ed49098b3628acfa6ca115aa28b01a1ee178c3a214de` and reported `1.2.2`. Prepared initialization left the tracked index and `.gitignore` hash unchanged, installed no hooks or remote, and created only the reviewed local exclude and `.beads` state. One `create` atomically committed the external ref and all correlation metadata, exact all-state metadata lookup recovered the same item open and closed, blocker readiness changed only after close, two concurrent claims produced one winner and one explicit rejection, an exact-PID interrupted close left the item safely open with no child, and normal close/reopen/final-close plus history and embedded reopen succeeded. The store contained 15 files totaling 1,454,120 bytes. `doctor` and raw `sql` truthfully reported unsupported in embedded mode. All proof-owned `bd`/Dolt processes, the executable-specific firewall rule, and the unique proof root were terminal or removed; user Git configuration and user/workspace Beads paths remained unchanged. A separate pre-existing OpenCode parent/child pair exited during the final cleanup interval without any process-targeting command, so that environmental exit is not claimed as static-process proof.
- **Outcome:** Selected as the source and adapter precondition for `BPB-001`; task 1.1 passed at the one-workstation disposable boundary.
- **Reason:** This is the smallest observed route that preserves tracked project bytes while retaining exact embedded identity, atomic correlation, advisory readiness/assignment, fail-closed contention, interruption recovery, and no remote effect.
- **Do-Not-Repeat Condition:** Do not activate a project whose reviewed tracked ignore policy is absent or drifted; do not enable metrics, pass `--ignore-schema-skew`, rely on embedded `doctor`/`sql`, infer writer closure from the parent exit alone, or broaden the supported command surface from top-level help.
- **Evidence-Based Retry Condition:** Re-run the live spike only after a changed pin, platform, extracted digest, initialization contract, required command surface, or directly observed environment invalidates this evidence; otherwise consume the exact manifest and capability matrix in tasks 1.3 and 2.1.

## 2026-09-02 - Rebind Kaizen Grind execution state beneath one-project Beads portfolio ownership

- **Foundation Incident ID:** `FI-BPB-KZG-20260901-01`.
- **Candidate Relation:** `candidate:bpb-spec-r4-foundation-reconciled` with `candidate:add-autonomous-kaizen-grind-r3-foundation-reconciled`; the relation is the one BPB-enabled project's admitted portfolio identity versus Grind's controller execution correlation.
- **Reproduction Evidence:** Current `add-autonomous-kaizen-grind` planning assigned accepted-work identity, dependencies, mutable status, assignment-adjacent session correlation, and terminal closure to SQLite `work_items`; current `BPB-001` assigned the same enabled project's identity, graph, readiness, assignment, duplicate relations, and terminal state to Beads. Implementing both unchanged would create competing authorities and ambiguous crash recovery.
- **State Transitions:** `observed -> confirmed -> correcting -> swept -> re-reviewed -> closed`.
- **Selected Correction:** Preserve `KZG-001` population and project routing but rename its controller object to a non-portfolio execution record. The record contains only its execution ref, run/cycle and source-decision refs, project/registration/candidate digests, optional canonical Beads ID, exact execution-prerequisite refs, route, gate/retry, Campaign/Mission/session refs, and resulting execution handoff. For the one BPB-enabled project the Beads ID is mandatory, Beads-owned state is not copied, and Grind only records the Kaizen bridge's ordered terminal result. Another registration has no inferred Beads identity or ownership.
- **Dependent Rebind:** `integrate-beads-portfolio-bridge/{proposal.md,design.md,tasks.md,specs/cross-project-kaizen-loop/spec.md}`.
- **Dependent Narrow:** `add-autonomous-kaizen-grind/{proposal.md,design.md,tasks.md,specs/autonomous-kaizen-grind/spec.md,specs/autonomous-work-campaign/spec.md,specs/cross-project-kaizen-loop/spec.md,specs/local-opencode-workstation/spec.md,specs/unattended-roadmap-orchestration/spec.md}`.
- **Not Dependent:** Both `.openspec.yaml` files; both historical `falsification-review.md` records; `add-autonomous-kaizen-grind/history.md`; BPB `beads-portfolio-bridge`, workstation, and profile deltas; KZG profile delta; canonical specs; archives; and active changes `add-prospective-consequence-rehearsal`, `deliver-continuous-reusable-value`, and `make-team-advice-evidence-triggered`.
- **Owner Boundary Or Unknown:** none for this one-project relation. Extending Beads ownership to another project remains outside `BPB-001`, not a recovery decision.
- **Preserved Evidence:** Pre-correction dependency-cone aggregate SHA-256 `65c99a9d1c9ed80ad139c489708aa3842565ef6a15c83930929a5a547b83dcd0`; rejected-strategy history, immutable falsification records, archives, canonical specs, unrelated active changes, and unrelated dirty worktree paths were not rewritten. Task 1.1 disposable runtime proof remains valid only for the exact pinned one-project spike and does not prove KZG integration or cross-project Beads ownership.
- **Corrected-Candidate Proof:** `openspec validate add-autonomous-kaizen-grind --strict` and `openspec validate integrate-beads-portfolio-bridge --strict` both passed before re-review. The same strict validators passed again after the focused correction; targeted readback found no remaining current `terminal execution record`, `frozen execution record`, direct campaign-to-`handoff-complete`, or non-historical KZG `work_items` wording. `git diff --check` reported no error for the two change roots. No production store, binary, project, runtime, profile, or host state was mutated by this recovery.
- **Fresh Re-Review:** Foundation Integrity reviewer task `ses_fa11de9e9ffe1PyopT9sTJBUG7`, effective model `openai/gpt-5.6-sol`, inspected the exact corrected candidate and reported `FI-BPB-KZG-R01` (the campaign delta named a broader semantic input as the persisted execution record) and `FI-BPB-KZG-R02` (campaign completion could enter execution-handoff completion before the selected portfolio owner's terminal result).
- **Main Disposition:** Both findings were independently reproduced from the current paths. The campaign contract now uses a distinct frozen campaign input that references the exact bounded execution-record projection while keeping accepted-outcome/evidence/path/effect/candidate facts outside `execution_records`. Campaign/Mission completion now enters `awaiting-terminal`; only the Kaizen bridge's ordered Beads-close-then-signal-resolve result permits `handoff-complete` for the BPB-enabled project, while another registration requires its existing Kaizen terminal transition. No second verdict-seeking review was run.
- **Terminal State:** `closed`. Remaining protected owner boundary: none for this one-project relation. Known limitation: BPB-KZG runtime integration remains unproved and task 1.1 evidence remains bounded to the exact disposable pinned-binary spike.

## 2026-09-02 - Rebind registration to the canonical Kaizen project identity

- **Foundation Incident ID:** `FI-BPB-KPREF-20260902-01`.
- **Candidate Relation:** `candidate:bpb-task2.3-project-lifecycle-20260901T2345Z`; Beads registration `projectRef` versus Kaizen v1 `KaizenSignal.projectRefs` for exact task 3.1 `project-change` eligibility.
- **Reproduction Evidence:** For the same native canonical workspace root, Kaizen's current `project_${sha256(root).slice(0,32)}` and registration's former `project:${sha256(root.toLowerCase())}` both matched their private schemas but were unequal; native-root and lowercased-root hashes were also unequal. No repository registration artifact existed, and protected install/registration had not occurred, so there was no current persisted population to migrate.
- **State Transitions:** `observed -> confirmed -> correcting -> swept -> re-reviewed -> closed`.
- **Selected Correction:** Registration now reuses the existing Kaizen/Project Memory identity exactly, without conversion or session-ref substitution. Lifecycle record validation and protected state path derivation use the same 32-hex privacy-safe suffix.
- **Dependent Rebind:** `tools/windows/beads-bridge-registration.ts`, `tools/windows/beads-project-lifecycle.ts`, their focused tests, and tasks 2.2-2.3 proof state.
- **Not Dependent:** BPB requirement prose, release manifest, vendor adapter metadata validation, `add-autonomous-kaizen-grind`, the other three active changes, canonical specs, and archives. Tracked archive tree `1a2f6b50780e24ab79ab63f6bcfee5abb2bbd148` plus unrelated untracked archive directories were not rewritten.
- **Prior Evidence Ceiling:** Former task 2.2-2.3 tests remain historical-only for `project:<64hex>` identity and its 64-hex protected path; their registration-to-Kaizen compatibility claim was invalid.
- **Corrected-Candidate Proof:** Registration tests passed 7/7, including exact equality with the Kaizen v1 canonical-root formula; lifecycle tests passed 12/12, including full enable/check/disable/rollback, drift, red-effect, and unknown-writer oracles under the corrected 32-hex identity. No real registration, binary, profile, project, provider, remote, or host state was created.
- **Fresh Re-Review:** Foundation Integrity reviewer task `ses_fa07a2dd3ffe6xYOFR7PYz5fep`, effective model `openai/gpt-5.6-sol`, inspected `candidate:bpb-task2.3-project-ref-rebound-20260902T0012Z` and reported `no-material-finding`; the exact current relation is aligned while task 3.1 promotion remains unimplemented and unproved.
- **Terminal State:** `closed`. Remaining protected owner boundary: none for this disposable identity rebind. Known limitation: corrected proof establishes same-root identity and task 2.2-2.3 lifecycle compatibility only, not Kaizen-to-Beads promotion.

## 2026-09-02 - Restore the task 3.2 dependency operation contract

- **Foundation Incident ID:** `FI-BPB-T32-DEPARGV-20260902-01`.
- **Candidate Relation:** `candidate:bpb-task3.2-preimplementation-20260902T0113Z`; task 3.2 coarse blocker/readiness projection versus the pinned release capability manifest and the closed production adapter operation schema.
- **Reproduction Evidence:** The exact `v1.2.2` manifest marks `dependencyAdd` supported and task 1.1 history preserves a blocker-ready transition after blocker close, but the current adapter request/parser/planner and the spec's closed operation list contain no dependency mutation. The retained session evidence contains no raw dependency-add argv, cwd, exit, or stream observation, so the command syntax is unknown and cannot be reconstructed from convention.
- **State Transitions:** `observed -> confirmed -> correcting -> swept -> re-reviewed -> closed`.
- **Selected Correction:** The re-downloaded archive and extracted executable matched the reviewed hashes. Exact `dep add --help` admitted positional dependency addition and the bounded `blocks`, `parent-child`, and `supersedes` types. The adapter now exposes only one exact `add-dependency` request, parses both hydrated show dependencies and ready's edge-shaped dependencies, and verifies mutation plus readback. The Kaizen orchestrator requires admitted same-registration items, serializes relation and assignment through the bridge lease, requires explicit semantic-identity confirmation for `supersedes`, and exposes ready/show/assignment as advisory with independent writer authority still required. Production atomic claim remains unavailable.
- **Dependent Rebind:** `openspec/changes/integrate-beads-portfolio-bridge/specs/beads-portfolio-bridge/spec.md`, task 3.2, the vendor adapter, and their focused tests.
- **Not Dependent:** `add-autonomous-kaizen-grind`, `add-prospective-consequence-rehearsal`, `deliver-continuous-reusable-value`, `make-team-advice-evidence-triggered`, canonical specs, all archives, and BPB release/registration/project-lifecycle/promotion behavior already proved through task 3.1.
- **Preserved Evidence:** Current Git `HEAD` is `a8a7957ad5fa1c16723ec35147c52cec44805a44`; tracked canonical-spec tree is `099a5fb72c2851caa0da9b55abbfd4dcb1848d22`; tracked archive tree is `a7456567af677cf80c78cf67d55e057dc735329d`. Task 1.1's historical blocker-ready observation remains valid only at its recorded disposable exact-pin ceiling; it does not supply production adapter argv. Unrelated dirty and untracked work remains untouched.
- **Corrected-Candidate Proof:** Candidate `candidate:bpb-task3.2-dependency-contract-restored-20260902T0145Z` reverified archive SHA-256 `1f00c29cd9599e182a4a4e829f5210daca2da14155920aee2836d8bc613b2feb`, executable SHA-256 `b1f3609fea1d9f0f19b2ed49098b3628acfa6ca115aa28b01a1ee178c3a214de`, executable bytes `145740800`, exact dependency help, and direct production-adapter blocks/parent-child/supersedes mutation plus show readback. The blocker hid its dependent from ready until close; assignment read back exact assignee/task/session refs. No remote, active hook, or retained child process was observed; the unique proof root was removed after terminal inspection. Focused Kaizen/orchestrator, adapter, registration, lifecycle, release, and 28-case cross-project Kaizen suites passed, as did strict BPB OpenSpec validation.
- **Fresh Re-Review:** Foundation Integrity reviewer task `ses_fa0244557ffe3fngi0q5HuGu54`, effective model `xai/grok-4.6`, inspected `candidate:bpb-task3.2-dependency-contract-restored-20260902T0145Z` and reported `no-material-finding` for the pin/manifest/closed-adapter/orchestrator/task relation.
- **Terminal State:** `closed`. Remaining protected owner boundary: none for the current disposable one-project relation contract. Known non-critical limitation: neighboring design/task 2.1 inventory prose still summarizes the earlier adapter subset, while the owning current requirement, manifest, adapter, lease, orchestrator, and task 3.2 surfaces carry the exact restored operation; installed-pilot behavior remains unproved.

## 2026-09-02 - Rebind bridge-writer storage creation to workstation lifecycle

- **Foundation Incident ID:** `FI-BPB-41-001`.
- **Candidate Relation:** `candidate:bpb-fi-41-001-storage-owner-rebound-20260902T0415Z`; completed task 2.2 bridge-writer acquisition versus open task 4.1 workstation ownership of protected `beads-bridge/<projectRef>` storage.
- **Reproduction Evidence:** The current pre-correction `statePaths(..., create=true)` path called `ensureOwnedDirectory` for both `beads-bridge` and the project-ref directory during `acquireBeadsBridgeWriterLease`, so a bridge mutation could instantiate storage whose identity, creation, and managed deletion the accepted design/spec/task assign only to workstation lifecycle. Terminal release deletion of the transient `writer.lock` remains lease-release cleanup; it is not managed-storage deletion. General protected-root rollback remains a task 4.1 dependency rather than a second incident.
- **State Transitions:** `observed -> confirmed -> correcting -> swept -> re-reviewed -> closed`.
- **Selected Correction:** `statePaths` now only resolves and validates paths. Acquire, enabled-state transition, and release require preinstalled storage; enabled inspection reports blocked/unknown when storage is absent; disabled inspection remains effect-free. Disposable registration, project-lifecycle, and Kaizen-orchestrator fixtures explicitly model workstation-precreated storage, and the focused registration oracle proves acquisition returns `writer-liveness-unknown` without creating any directory. Terminal release still archives closure evidence under existing state and unlinks only its correlated `writer.lock` after terminal or write-isolated closure.
- **Dependent Rebind:** `tools/windows/beads-bridge-registration.ts`, `tools/test-beads-bridge-registration.ts`, `tools/test-beads-project-lifecycle.ts`, `tools/test-beads-kaizen-orchestrator.ts`, and the task 2.2 proof ceiling. `tools/windows/beads-project-lifecycle.ts` and `tools/windows/beads-kaizen-orchestrator.ts` remain intentionally unchanged callers because their mutations now consume preinstalled storage and fail closed otherwise. BPB proposal, design, requirement deltas, task 4.1 semantics, exact release manifest, adapter contract, project identity, and registration schema were already aligned and did not require semantic edits.
- **Complete Active-Change Inventory:** `integrate-beads-portfolio-bridge` is the owning `dependent-rebind`; `add-autonomous-kaizen-grind`, `add-prospective-consequence-rehearsal`, `deliver-continuous-reusable-value`, and `make-team-advice-evidence-triggered` are `not-dependent`. All five `.openspec.yaml` metadata files are `not-dependent`. No separate active structured claim record exists beyond `BPB-001` in the owning BPB proposal/tasks.
- **Canonical-Spec Inventory:** The 29 current canonical specs are all `not-dependent`: `agent-workflow-automation`, `autonomous-work-campaign`, `cross-project-kaizen-loop`, `leaf-first-task-decomposition`, `library-bounded-falsification-review`, `library-change-ready-sdlc`, `library-claim-evidence-closure`, `library-complexity-management`, `library-config-portability`, `library-consumer-outcome-regression`, `library-deduplication-audit`, `library-foundation-integrity`, `library-instruction-artifacts`, `library-model-routing`, `library-plugin-architecture`, `library-practice-ownership`, `library-reuse-discovery`, `library-roadmap-delivery-trajectory`, `library-runtime-surface-profiles`, `library-specialist-team-advising`, `library-spec-workflow-integrity`, `library-tools-architecture`, `library-install-init-resilience`, `local-opencode-workstation`, `outcome-preserving-delivery-checkpoints`, `project-memory-context`, `session-completion-guard`, `shared-code-intelligence-runtime`, and `unattended-roadmap-orchestration`.
- **Preserved Evidence And Archives:** Canonical-spec tree `099a5fb72c2851caa0da9b55abbfd4dcb1848d22` and archive tree `a7456567af677cf80c78cf67d55e057dc735329d` were captured before correction and were not rewritten. Prior task 2.2 proof remains historical-only for the claim that acquisition could establish lock storage; one-project registration, identity, serialization, unknown-liveness preservation, and terminal release evidence remain current after focused replay.
- **Corrected-Candidate Proof:** `npm run test:focused:beads-registration`, `npm run test:focused:beads-lifecycle`, and `npm run test:focused:beads-kaizen` passed against disposable roots. No real install, profile selection, project activation, provider, remote, or protected-host mutation occurred.
- **Fresh Re-Review:** Foundation Integrity reviewer task `ses_f9face4aeffewS3a0H1HUnS0FW`, effective model `xai/grok-4.6`, inspected `candidate:bpb-fi-41-001-storage-owner-rebound-20260902T0415Z` and reported `no-material-finding` for the exact storage-owner relation. It confirmed that creating the child closure archive directory under already-installed state does not instantiate the protected storage identity and that task 4.1 remains future implementation rather than a current contradiction.
- **Terminal State:** `closed`. Remaining protected owner boundary: task 4.1 must now implement source-manifest-driven workstation preview/install/check/rollback, precreate and own the exact writer-storage identity, and prevent general protected-root rollback from deleting registration-referenced material while writer closure is active or unknown. Known non-critical limitation: the current reason string groups missing installed storage under `writer-lease-unsafe`; the enforced result remains blocked/unknown.

## 2026-09-02 - Complete source and disposable workstation Beads lifecycle

- **Task:** `4.1`; candidate `candidate:bpb-4.1-source-lifecycle-20260902T0455Z` plus the confirmed `FCR-BPB41-001` cleanup correction.
- **Implementation Disposition:** `extend` the existing workstation semantic owner through one private owner-local `tools/windows/beads-workstation-lifecycle.ts` capability. The public CLI and broad workstation rollback remain in `tools/windows/opencode-workstation.ts`; no second installer or public lifecycle owner was added. The private contract has a direct disposable oracle and reduces total context/proof cost versus adding the Beads axis to the 2,900-line mixed controller. Cross-project verification is `not-applicable` for this exact owner-local protected lifecycle.
- **Behavior:** Public `beads-preview`, `beads-install --source <bd.exe>`, `beads-check`, and `beads-rollback [--dry-run]` dynamically delegate to the private module. Preview reports the exact v1.2.2 archive/executable, protected paths, inherited ACL, non-effects, registration/rollback envelope, schema-2 workstation prerequisite, and restart boundary without mutation. Install requires a current schema-2 workstation layout, exact source bytes/hash, and a closed source inventory; it copies exclusively, reads back every identity, creates only the empty bridge root, and writes one closed lifecycle record. Profile selection, registration, activation, process, service, task, hook, and remote effects remain absent. A private provisioning operation creates one project-ref writer directory only after a matching managed registration exists and rejects nonempty unowned state.
- **Rollback Boundary:** Drift returns bounded cause diagnostics and preserves all material. A held or unknown writer returns partial unknown with the lock and every referenced path retained. A clear registered rollback acquires the same `rollback` lease before workstation-owned deletion. Workstation-wide rollback consults this result before mutation; an incomplete/unknown Beads rollback now returns partial without stopping the server, unregistering tasks, restoring config, removing shortcuts, or recursively deleting the protected root.
- **Direct Proof:** `tools/test-beads-workstation-lifecycle.ts` passed 6/6: exact nonmutating preview; platform/manifest/executable/source-path red cases before managed effects; install/check without profile or registration; post-copy identity failure cleanup plus successful retry; rejection of nonempty unowned writer state; held-writer preservation followed by closed-writer rollback; and drift preservation with bounded cause. The first cleanup regression reproduced an unmanaged `beads/` directory after post-copy identity failure, then passed after attempted destinations were tracked before copy and empty parents were removed.
- **Production Preview:** `node tools/windows/opencode-workstation.ts beads-preview` returned the exact archive SHA-256 `1f00c29cd9599e182a4a4e829f5210daca2da14155920aee2836d8bc613b2feb`, executable SHA-256 `b1f3609fea1d9f0f19b2ed49098b3628acfa6ca115aa28b01a1ee178c3a214de`, bytes `145740800`, protected paths, ACL and non-effects. It truthfully returned `eligible=false`, `reason=workstation-prerequisite`: current protected `manifest.json` is schema 2 but declares no `layoutModule`. Readback confirmed the six Beads protected paths remained absent. No download, executable, install, profile, project, provider, process, service, task, hook, or remote effect occurred.
- **Validation:** Focused release, adapter, registration, project lifecycle, Kaizen orchestrator, workstation config (7), workstation restart-critical (6), and new workstation lifecycle (6) suites passed. `openspec validate integrate-beads-portfolio-bridge --strict` passed. `git diff --check` reported only LF-to-CRLF working-copy warnings.
- **Quality And Risk Review:** Code-quality reviewer task `ses_f9f993d28ffeBlPkKpP2eNpSFp`, effective model `xai/grok-4.6`, identified two safe reductions that were applied (one duplicate absent branch and unused copy/source injection/public type surface); CLI import consolidation was intentionally not applied because it would couple workstation-wide rollback to public-mode elevation without an equivalent parent oracle. Deployment reviewer task `ses_f9f8fb9d9ffenxg2lplcao293b`, effective model `xai/grok-4.6`, reported preview/install prerequisite mismatch, possible leased-writer reaping by broad rollback, and cause-poor drift diagnostics; all three were corrected and re-proved. Final-candidate reviewer task `ses_f9f84cb3fffemlKLscmKorexom`, effective model `xai/grok-4.6`, reported `FCR-BPB41-001`; main independently reproduced and corrected the mid-copy cleanup defect with the sixth focused oracle.
- **Evidence Ceiling And Next Gate:** Task 4.1 proves repository source, disposable workstation fixtures, and the real nonmutating public preview only. It does not prove elevation, effective inherited ACL, antivirus/file-lock behavior, the real 145 MB executable, or protected install/rollback. Task 5.1 remains blocked until the existing workstation lifecycle is safely repaired/reinstalled so its schema-2 manifest declares a present hash-matching layout module; no protected Beads install attempt is authorized by this task.

## 2026-09-02 - Complete the on-demand core-beads runtime surface

- **Task:** `4.2`; candidate `candidate:bpb-4.2-runtime-surface-20260902T0610Z`.
- **Implementation Disposition:** `extend` the existing runtime-surface profile and installer owners. The previously closed Beads adapter/registration/project/Kaizen/release closure moved intact beneath `global/bin/beads-portfolio-bridge/`; one `beads-portfolio-bridge` skill owns on-demand routing. No sibling adapter, thin command, vendor instruction, plugin, MCP, `bd prime`, automatic activation, or missing-binary success path was added. Cross-project verification is `not-applicable` because this slice only packages the already selected one-project source owner.
- **Profile Contract:** `core-beads` is one concrete full `ask` profile equal to current `core` plus exactly the Beads skill and six-file helper directory. `core` remains Beads-free. `all` and unprofiled full source expose the same skill and helper once. Kits without any Beads source still require only `core` and `all`; once either Beads source identity exists, omission or any Beads-only partial profile fails validation. Installer preview, check, keep-existing, and explicit profile selection recognize `core-beads` while default selection remains `core`.
- **Runtime Proof:** Fresh disposable loader roots reported `core` with `beadsSkillCount=0` and no helper, `core-beads` with `beadsSkillCount=1` and all six helper files, and `all` with the same single skill and closure. All three reported `status=passed`, no hidden parent hits or unresolved/staging paths, and no added Beads plugin or MCP. A bounded synthetic `core-beads` routing check copied the existing OAuth record into its disposable data root without reading or emitting it and allowed only the skill tool. The ordinary OpenSpec prompt loaded `openspec-apply-change` and not Beads; the explicit Beads prerequisite prompt loaded exactly `beads-portfolio-bridge`. Both proof-owned processes exited `0`, both sessions were deleted with status `0`, the project fixture entries were unchanged, and the complete disposable root including the copied credential was removed.
- **Validation:** Runtime-profile/library tests passed 184 cases, installer tests passed 32, instruction-context tests passed 15, and focused release/adapter/registration/project-lifecycle/Kaizen/workstation suites remained green. Strict all-change OpenSpec validation passed 34/34. `git diff --check` reported only the repository's LF-to-CRLF working-copy warning.
- **Evidence Ceiling And Next Gate:** Task 4.2 proves source/profile/installer identities, installed OpenCode discovery, and one synthetic stay-quiet-versus-explicit skill-load pair under isolated state. It does not prove installed `bd.exe`, project registration, missing-project adapter output, provider-free `BPB-001` population reuse, or protected profile persistence. Task 4.3 must establish the deterministic reviewed population before the protected task 5.1 path; task 5.1 also remains independently blocked by the undeclared workstation `layoutModule`.

## 2026-09-02 - Materialize one reusable BPB-001 proof population

- **Task:** `4.3`; candidate `candidate:bpb-4.3-population-20260902T0645Z`.
- **Implementation Disposition:** `extend` the existing consumer-outcome proof owner with one owner-local `beads-portfolio-bridge` pack and one reviewed seed; the existing Kaizen orchestrator and workstation lifecycle tests are its first behavior consumers. No generic external-tool runner, installed invocation, hidden command replay, second semantic seed, or retained repository evidence tree was added. Cross-project verification is `not-applicable`: the population remains the accepted one-project `BPB-001` contract and explicitly excludes remote or multi-project claims.
- **Population Contract:** The schema-v1 seed has ten ordered sections: pinned release manifest, repository initialization envelope, Kaizen correlation, dependency/readiness, advisory assignment, interrupted OpenSpec link, ordered terminal projection, core/core-beads/all profile identities, Git effects, and workstation install/rollback. Each section has one deterministic red control with an exact failure identity. The parser rejects unknown top-level/section fields, wrong field types, source-order drift, duplicate red identities, and wrong red correlation. The bundle seals seed, current governed working-tree identity, evaluation, zero-effect facts, cleanup, and its own digest; replay requires the current seed/source/evaluation and rejects tampering.
- **First Consumers:** The Kaizen orchestrator fixture now consumes the seed's prefix, signal kind, decision/owner, dependency readiness, assignment refs, lost-link-response recovery, terminal ordering, and Git/remote/agent-instruction expectations while retaining its existing production entrypoints and fake `bd` boundary. The workstation fixture consumes the seed's preview/install/check, non-effect, active-writer partial-unknown, and terminal rollback expectations. The future installed pilot can import the same validated pack rather than recreate these values.
- **Runtime Proof:** The real consumer-outcome CLI preflight reported `ready` for all ten members and ten red controls. Two create-new provider-free materializations with the same candidate were byte-identical for both `bundle.json` and `evaluation.json`; replay passed all 20 rows. Reported provider, network, remote, project-write, and protected-host-write counts were all zero. Both materialization roots were removed after readback. No binary, profile, project, provider, remote, protected-host, or current global-config effect occurred.
- **Validation:** Consumer-outcome tests passed 51 cases, including two-run byte equality, all red rows, malformed schema, tampered replay, CLI preflight, and cleanup. Focused Beads Kaizen and workstation suites passed against the shared seed. Runtime-profile/library tests passed 184 cases after the initial shell timeout was verified to have no remaining matching Node child and rerun with a sufficient bound. `git diff --check` reported only LF-to-CRLF working-copy warnings.
- **Quality Review:** Code-quality reviewer task `ses_f9f3996f2ffe0fO6YcQ43x9Z8N`, effective model `xai/grok-4.6`, found no safe reduction: the pack remains one cohesive parser/evaluator/materializer/readback owner, the CLI branch matches existing pack dispatch, and the behavior tests retain unique interruption/order and writer-preservation oracles. The module is in the 410-line attention band, not the split-candidate band; `split-or-justify` is `justify` because all phases serve one exact proof-pack lifecycle and extraction would add navigation without an independent owner or oracle. Existing larger consumer proof files remain pre-existing split candidates and were not broadened beyond same-owner dispatch/test rows.
- **Evidence Ceiling And Next Gate:** Task 4.3 proves only the reviewed provider-free source population, current release/profile correlation, its first disposable consumers, deterministic red detection, and stable materialization/readback. It does not prove a protected executable, ACL, antivirus/file locking, profile persistence, project activation, Dolt state, or installed pilot. Task 5.1 remains separately gated by a current schema-2 workstation manifest with a present hash-matching `layoutModule`; the last production preview reported that declaration absent.

## 2026-09-02 - Install and read back the pinned protected binary

- **Task:** `5.1`; candidate `candidate:bpb-5.1-installed-v1.2.2-20260902T0742Z`.
- **Dependency Closure:** The initial read-only preview correctly failed because the schema-2 protected workstation manifest did not declare a layout module. Current local standing authorization and the accepted protected-install outcome permit the smallest prerequisite repair without a new product decision. The managed server task and both managed ports were already stopped/free, so no user-owned process was terminated or restarted. Preflight then exposed a separate missing Alacritty executable. The exact available `Alacritty.Alacritty` `0.17.0` package was restored with winget; its installed version/hash matched the prior workstation identity. The existing transactional workstation `install` path then returned `repaired-stopped`, installed `opencode-workstation-layout.ts`, updated the manifest candidate to `a829d454bd7234495ebde9c62aedd15e7454f1b9496b828dfe2e9a74065b9390`, and retained stopped task state and undisclosed credentials.
- **Release And Install:** The declared GitHub `v1.2.2` Windows amd64 archive was downloaded into one proof-owned temporary root. Its SHA-256 was `1f00c29cd9599e182a4a4e829f5210daca2da14155920aee2836d8bc613b2feb`; the sole extracted `bd.exe` was `145740800` bytes with SHA-256 `b1f3609fea1d9f0f19b2ed49098b3628acfa6ca115aa28b01a1ee178c3a214de`. The public `opencode-workstation.ts beads-install --source <verified-temp-binary>` entrypoint returned `installed` and its immediate `beads-check` returned `current` with no drift or diagnostics.
- **Installed Readback:** `C:\ProgramData\OpenCodeWorkstation\beads\bin\bd.exe --version` reported `bd version 1.2.2 (6c124203e)` and `--help` returned the expected command surface. Installed size/hash exactly matched the reviewed manifest. ACL readback inherited `BUILTIN\Users:RX`, `BUILTIN\Administrators:F`, and `SYSTEM:F`. Rollback dry-run was eligible and selected `remove-matching-beads-material` without a bridge lease because no registration exists.
- **Effects And Cleanup:** Before and after install there were no `bd.exe` or `dolt.exe` processes. The shared workstation task remained `Ready`, listeners on `4096` and `4097` remained zero, and project registration remained absent. No profile was selected, no project was activated, and no Git hook, task, service, provider, project remote, or repository source mutation was performed by the install. The proof-owned archive/extraction root was removed after installed readback. External host effects were the exact Alacritty package restoration, one pinned GitHub archive download, the stopped transactional workstation repair, and the protected Beads files recorded by the lifecycle owner.
- **Validation:** Focused Beads workstation lifecycle and workstation configuration suites passed; `git diff --check` reported only LF-to-CRLF working-copy warnings.
- **Evidence Ceiling And Next Gate:** Task 5.1 proves the exact protected binary, source/runtime copies, manifest identities, ACL, version/help, stopped process state, rollback readiness, and absent registration. It does not prove profile materialization/discovery or any project-local Beads/Dolt behavior. Task 5.2 may now use isolated profile/config/data/cache/state/home roots and fresh proof-owned processes without touching the current global configuration or existing OpenCode processes.

## 2026-09-02 - Prove generated and unprofiled loader identities

- **Task:** `5.2`; candidate `candidate:bpb-5.2-isolated-loader-20260902T0750Z`.
- **Implementation Disposition:** `extend` the existing `runtime-surface-loader.ts` proof owner with one exact `source` profile selector rather than adding a runner. Generated `core`, `core-beads`, and `all` behavior remains unchanged. Source mode points `OPENCODE_CONFIG_DIR` at the current unprofiled `global/` source while isolating config/data/cache/state and external-skill discovery; it removes only its disposable runtime/project root and never removes or writes the source root. Cross-project verification is `not-applicable`: every process used one synthetic unrelated project and the selected kit source only.
- **Run-Observe-Correct:** The first source-mode run found the Beads skill/helper closure correctly but failed because the all-profile evaluator also required an explicit `opencode-pty-bridge.ts` plugin entry. Current unprofiled config loads the session guard and mission launcher but does not list that all-profile-only explicit entry. The evaluator was narrowed only for source mode; all-profile requirements remain unchanged. The corrected source run passed with current full-source plugins, all selected skills/agents/commands, one Beads skill, and all six Beads helpers.
- **Loader Proof:** Fresh isolated OpenCode debug processes passed for all four identities. `core` exposed zero Beads skills and no helper root. `core-beads` exposed exactly one `beads-portfolio-bridge` skill and the six expected helper files. `all` and unprofiled source each exposed exactly one Beads skill and the same helper closure, with no missing selected skills/agents, no staging paths, and no unresolved placeholders. Config, skill, agent, advisor, and agent-list statuses were all zero.
- **Routing And Missing Project:** The bounded synthetic ordinary request loaded `openspec-apply-change` and did not load the Beads skill. The explicit Beads request loaded `beads-portfolio-bridge` exactly once. Both fresh process invocations exited zero; their two session deletions exited zero; the unrelated project entries were unchanged; and the routing root cleanup was complete. Installed public preview/check concurrently established the exact unavailable project state: the binary is current while registration is absent and writer status is `not-registered`.
- **Isolation And Cleanup:** Before/after SHA-256 values for current `global/opencode.json` and `global/opencode.local.instructions.md` were identical. User and process `OPENCODE_CONFIG_DIR` values remained the current global path, the shared workstation task remained `Ready`, ports `4096`/`4097` remained listener-free, all generated profile roots were removed, and no existing OpenCode process or project/config/profile state was mutated.
- **Validation:** The directly exercised core/core-beads/all/source loader evaluations and explicit routing evaluation passed. Focused library tests passed 184 cases; `git diff --check` reported only LF-to-CRLF working-copy warnings.
- **Evidence Ceiling And Next Gate:** Task 5.2 proves isolated loader inventory, on-demand routing, current missing-registration status, exact profile rollback, and no current-config/process effects. It does not prove project enablement, Beads/Dolt issue behavior, response-loss recovery, active-writer preservation, or closed-writer rollback. Task 5.3 owns that one disposable registered-project boundary.

## 2026-09-02 - Refresh installed and loader proof against the tracked baseline payload

- **Tasks:** `5.1` and `5.2`; candidate `candidate:bpb-5.1-5.2-tracked-content-current-20260902`.
- **Dependency Correction:** Task 5.3 requires the exact reviewed `.gitignore` bytes, while the original release manifest retained only their SHA-256. Schema 1 now stores both the UTF-8 payload and digest and rejects any content/hash mismatch. The parser retains one closed legacy installed-manifest form without `content`, represented as `content: null`, solely so a pre-correction protected installation can be checked and rolled back safely; current source and new installations always use the hash-validated payload form.
- **Protected Refresh:** The pre-correction protected installation remained identity-current through the legacy parse. Its exact `145740800`-byte executable with SHA-256 `b1f3609fea1d9f0f19b2ed49098b3628acfa6ca115aa28b01a1ee178c3a214de` was copied to one proof-owned temporary root, the old managed installation rolled back cleanly while registration was absent, and public preview/install recreated it from that verified binary. Current `beads-check` is `current`; source and installed release manifests both have SHA-256 `2113bb01a99e2cfd87e0f4a5249e2bc8b5f1eca8b15427bca1df1761c614aeb2`; version remains `bd version 1.2.2 (6c124203e)`; full help succeeded; executable identity and inherited protected ACL remained exact; and rollback dry-run remains eligible for `remove-matching-beads-material` without a bridge lease. The temporary binary root was removed.
- **Loader Refresh:** Fresh isolated evaluations passed for all four current-source identities. `core` exposed no Beads skill/helper; `core-beads`, `all`, and unprofiled source each exposed exactly one `beads-portfolio-bridge` skill and all six helpers. The synthetic ordinary request loaded only `openspec-apply-change`; the explicit Beads request loaded only `beads-portfolio-bridge`; both processes and session deletions exited zero, project entries were unchanged, and routing cleanup was complete. All retained loader evidence roots were then removed.
- **Isolation And Validation:** Current `global/opencode.json` SHA-256 remained `73428d34524ac2305119229b567e83c781e7f7ff046514f63567ba50fe892928`; process and user `OPENCODE_CONFIG_DIR` still point to the current global source; managed workstation tasks remained `Ready`; ports `4096` and `4097` remained listener-free; and no existing OpenCode process was targeted. Focused release and workstation lifecycle tests plus 184 library cases passed.
- **Evidence Ceiling And Next Gate:** The refreshed proof closes only the source-manifest compatibility, protected install identity, and loader/routing evidence invalidated by the required payload addition. It does not prove project enablement or any installed one-project Beads/Dolt operation. Task 5.3 now owns that disposable registered-project boundary.

## 2026-09-02 - Rebind the tracked ignore prerequisite to the pilot clone

- **Foundation Incident ID:** `FI-BPB-53-CLONEIGNORE-20260902-01`.
- **Candidate Relation:** `candidate:bpb-fi-53-clone-ignore-corrected-20260902`; task 5.3's clean disposable clone of the current `opencode-kit` pilot and unchanged whole tracked-byte oracle versus the task 1.1 empty-repository `.gitignore` whole-file digest.
- **Reproduction Evidence:** Before activation, current `HEAD:.gitignore` was 263 LF bytes with SHA-256 `db332fae8b79aabf380c9e2e9ca7e7210bb2950db9e90ea15880ee0891338ad2`; its Windows worktree and clean-clone copy were 273 CRLF bytes with SHA-256 `7cf1521df4f2bdabb004b2e6f4537e8dd02500f5447930c0aaa6196b0afc7bfd`. The manifest's task 1.1 Beads-only payload was 93 bytes with SHA-256 `9788c26633ae340b1647c267f66c010ca0ded0b3bb039e887497ffeccccc6553`. A firewall-contained real adapter canary against the clean clone confirmed pinned `bd init` appended that exact block, changed `.gitignore` to 366 bytes/SHA-256 `27052ce69b9ab543006f829db4c46264cf8cb818b74314787a0617b43ca31464`, and therefore violated the tracked-source invariant. The same run also proved that `v1.2.2 init --json` exits zero with bounded human stdout rather than JSON; the pre-correction adapter rejected it as `invalid-vendor-json` after the project-local store was created. No registration or protected writer state existed, all `bd`/Dolt children were terminal, and both canary roots were removed after inspection.
- **State Transitions:** `observed -> confirmed -> correcting -> swept -> re-reviewed -> closed`.
- **Selected Correction:** Preserve the named pilot clone and unchanged tracked-source outcome. The canonical `.gitignore` now includes the exact observed Beads/Dolt block. Its manifest content/hash identify that required contiguous block across LF/CRLF rather than a foreign whole-file identity; both adapter and project lifecycle reject a missing block before `bd`, while lifecycle before/after snapshots still require every tracked file byte, index, worktree, and status identity to remain unchanged. The test-only tracked-digest override was removed. Adapter init accepts the exact observed bounded text only after zero exit and direct non-symlink `.beads` readback; all other operations retain JSON parsing. A diagnostic init with the block already present left `.gitignore` at the same 366-byte identity.
- **Dependent Rebind:** `.gitignore`; `global/bin/beads-portfolio-bridge/{beads-release.ts,beads-release.manifest.json,beads-vendor-adapter.ts,beads-project-lifecycle.ts}`; `tools/test-beads-{release,vendor-adapter,project-lifecycle,kaizen-orchestrator}.ts`; BPB `design.md`, `specs/beads-portfolio-bridge/spec.md`, `tasks.md`, and this history; protected installed source copies; task 5.1 installed-source and task 5.2 helper-loader evidence.
- **Complete Active-Change And Claim Inventory:** `integrate-beads-portfolio-bridge` / `BPB-001` is `dependent-rebind`. `add-autonomous-kaizen-grind` / `KZG-001`, `add-prospective-consequence-rehearsal` / `PCR-001`, `deliver-continuous-reusable-value` / `CRVD-001`, and `make-team-advice-evidence-triggered` / `MTAET-001`, including all five `.openspec.yaml` files, are `not-dependent`; none binds the task 5.3 tracked ignore baseline.
- **Canonical-Spec Inventory:** All 29 current canonical specs are `not-dependent`: `agent-workflow-automation`, `autonomous-work-campaign`, `cross-project-kaizen-loop`, `leaf-first-task-decomposition`, `library-bounded-falsification-review`, `library-change-ready-sdlc`, `library-claim-evidence-closure`, `library-complexity-management`, `library-config-portability`, `library-consumer-outcome-regression`, `library-deduplication-audit`, `library-foundation-integrity`, `library-instruction-artifacts`, `library-model-routing`, `library-plugin-architecture`, `library-practice-ownership`, `library-reuse-discovery`, `library-roadmap-delivery-trajectory`, `library-runtime-surface-profiles`, `library-specialist-team-advising`, `library-spec-workflow-integrity`, `library-tools-architecture`, `library-install-init-resilience`, `local-opencode-workstation`, `outcome-preserving-delivery-checkpoints`, `project-memory-context`, `session-completion-guard`, `shared-code-intelligence-runtime`, and `unattended-roadmap-orchestration`.
- **Preserved Evidence And Archives:** Pre-correction Git `HEAD` `a8a7957ad5fa1c16723ec35147c52cec44805a44`, canonical-spec tree `099a5fb72c2851caa0da9b55abbfd4dcb1848d22`, and archive tree `a7456567af677cf80c78cf67d55e057dc735329d` were captured and not rewritten. Task 1.1 remains current only for its prepared 93-byte-file spike. Tasks 2.3 and 3.5 remain component evidence after their corrected real-content fixtures pass, not proof of the current clone. The original core-negative loader proof remains current because core excludes every changed Beads helper.
- **Corrected-Candidate Proof:** Focused release, adapter, project lifecycle, Kaizen orchestrator, and workstation lifecycle suites passed, including real-content precondition and init-text/direct-store oracles; strict BPB OpenSpec validation passed. The protected source was transactionally rolled back and reinstalled from the same verified `145740800`-byte `bd.exe`; `beads-check` is current, version is `1.2.2 (6c124203e)`, rollback dry-run is eligible, and source/installed manifest (`2113bb01...`), release helper (`43cd7ee0...`), and adapter (`c2a303ff...`) hashes match exactly. Fresh isolated `core-beads`, `all`, and unprofiled source loader checks each found one skill and six helpers; ordinary routing loaded only `openspec-apply-change`, explicit routing loaded only `beads-portfolio-bridge`, both sessions deleted successfully, and all proof roots were removed.
- **Fresh Re-Review:** Foundation Integrity reviewer task `ses_f9e8d65e6ffexLT6mJsfWhRtay`, effective model `xai/grok-4.6`, confirmed the old whole-file production comparison was gone but reported `FI-BPB-53-R03` because the corrected worktree was not yet a clean clone identity and `FI-BPB-53-R04` because the prior no-op observation covered a different mixed-EOL file.
- **Main Disposition:** Both rows were independently reproduced and corrected inside this incident without another review. The existing candidate-snapshot mechanism was materialized only in a proof-owned bare seed with a temporary index: source `HEAD`/refs/index remained unchanged, parent `a8a7957ad5fa1c16723ec35147c52cec44805a44` plus only the current `.gitignore` produced tree `ab3342d4b9f89b302285929bbe5224fc09cafaaa` and proof-only commit `0334f48ca6555f304bdc537a64660d1aa6560787`. Its disposable pilot clone was clean and remoteless with an all-CRLF 372-byte `.gitignore` SHA-256 `e8c52038ae6d168de5624968d7a966695916ef0bc1cacc5c3a99c5abffdca235`. The registered production lifecycle then enabled that exact clone through the installed adapter; init left the same bytes/hash, index/worktree/status hashes empty, external-boundary hash unchanged, hooks/remotes unchanged, store present with 13 files, child cleanup terminal, registration enabled, and writer clear. A local `core.autocrlf=true` baseline was required because the intentionally isolated Git-global environment otherwise interpreted the CRLF checkout differently; it existed before the lifecycle snapshot and was unchanged except for the separately reviewed `beads.role` addition.
- **Terminal State:** `closed`. Remaining protected owner boundary: none for the corrected clone/ignore/init relation. The pilot stays enabled under task 5.3 for its remaining feature, relation, assignment, link, terminal, disable, partial-unknown rollback, completed rollback, evidence inspection, and attributable cleanup steps. The proof-only snapshot commit is not a source commit or source ref and remains confined to the disposable root.

## 2026-09-02 - Complete the installed one-project pilot

- **Candidate And Environment:** The protected registration bound project `project_6efed4d8cd49fd902cdbf4fad305346d` to the already-enabled remoteless clone at proof-only commit `0334f48ca6555f304bdc537a64660d1aa6560787`. Its tracked identity remained `00b0b1e82981b1b8d616006d9c1ad0c0eb3eb3b18ac38196c3d9c6b1a18b5405`, index and worktree digests remained empty, `.gitignore` remained 372 CRLF bytes with SHA-256 `e8c52038ae6d168de5624968d7a966695916ef0bc1cacc5c3a99c5abffdca235`, remotes and active hooks remained absent, and isolated external-boundary digest `63a889a0c9325d7b6085f43c816a82febb3898b19abf91d1a09495951fdba9fd` remained exact.
- **Promotion And Portfolio Proof:** Signal `signal_f362ccf3eff83e92075e19065f64e766` and decision `decision_e830c2994086ffab7587be7ec092eb85` created Beads feature `BPB-7fc`; an injected post-create response loss returned `promotion-failed` while leaving the signal `triaged` and writer clear, and replay recovered that exact feature with `created: false` before appending one `promoted` transition. Dependent feature `BPB-ld2` gained one `blocks` relation to `BPB-7fc`; relation replay returned `changed: false`; ready included only the blocker before close and only the dependent after close; exact `agent:bpb-001` / `task:bpb-001` / `session:bpb-001` assignment read back and replay returned `changed: false`.
- **Link And Terminal Proof:** Lost update response on the linked dependent persisted the exact `integrate-beads-portfolio-bridge` / `beads-portfolio-bridge` association, replay returned `changed: false`, and a competing spec link failed `link-conflict`; this diagnostic association remained only in the disposable store and did not mutate the active OpenSpec change. The terminal population used separate signal `signal_582c311c87e0c22f851629f58f1de52a`, decision `decision_03fb628f8d6ed198e8f766a5a9e3b0df`, and feature `BPB-9fr` linked to proof-only `bpb-001-change` / `spec:bpb-001-change`. Its injected lost link response persisted the exact association and replay returned `changed: false`. The disposable change passed strict validation, archived canonically as `openspec/changes/archive/2026-09-02-bpb-001-change`, and its generated main spec was corrected from the OpenSpec `TBD` purpose before exact and full clone validation passed `1/1` and `35/35`. No source implementation or commit occurred.
- **Ordered Reconciliation:** Terminal evidence for candidate `candidate:bpb-5.3-pilot-0334f48` closed `BPB-9fr` with `terminal:2df66b45ccefa09118916b3cf65f5fb1c1a3a53ea74ed3e1948480f84d09e1af`. An injected post-close interruption left the feature closed and signal promoted with writer clear; replay appended only the missing `resolved` transition, and the next replay returned `changed: false` with neither closure nor resolution repeated.
- **Disable, Data, And Project Rollback:** Project disable returned `disabled` with the store preserved. Adapter readback returned three exact admitted features without truncation or remote effect; terminal and dependency history remained represented by two closed features, one open assigned dependent, their exact relation/link metadata, and Kaizen statuses `promoted`, `promoted`, and `resolved`. The embedded store had 15 files totaling 1,454,911 bytes. One transient `bd`/Dolt process disappeared before detailed inspection without intervention; the later pre-rollback and final canaries both reported zero. After the synthetic archive/spec files were inspected and removed, project rollback initially failed closed only because the proof invocation omitted the enable-time external-boundary callback. Recomputing its deterministic isolated-home walk matched the recorded `63a889a0...`; the corrected invocation then returned `rolled-back`, restored the pre-enable Git config/exclude state, and preserved the same store.
- **Protected Rollback And Cleanup:** With a live proof-owned bridge lease, the public workstation rollback returned `partial-unknown`, `writer-liveness-unknown`, and 13 retained paths. Corrected direct instrumentation observed all 12 exact managed files including `writer.lock` present and byte-identical before and after the attempt at digest `ae5e9b93fca6294254d3d2b04af2e5fe90e5c092a60f9a602c123021c530e52b`; terminal lease release restored `beads-check` to `current`. With the writer clear, public rollback returned `rolled-back`; subsequent check returned `absent`, protected Beads paths and name-matched remnants were absent, while the project-local 15-file/1,454,911-byte store was still present for final inspection. The exact proof root and executable-specific outbound firewall rule were then removed. Final readback found neither artifact, no installed Beads material, and zero `bd`/Dolt processes. No provider, source commit, remote, shared server, release, deployment, or user-process action occurred.
- **Evidence Ceiling:** This closes task 5.3 only for the exercised Windows workstation, exact pinned binary, exact proof-only candidate clone, one project, and the installed `BPB-001` population. It does not establish the task 5.4 matched context-cost improvement, cross-project behavior, general Windows compatibility, or final qualification.

## 2026-09-02 - Observe matched consumer outcomes without an improvement claim

- **Task And Candidate:** `5.4`; working-tree governed digest `d3e8168180eadfccbb14ba59bffdcb211253f8329c96d8e457e2cf26de41635a`, OpenCode `1.18.26`, Node `v24.18.1`, model `openai/gpt-5.6-sol`, variant `xhigh`, and profile `quality-independent`.
- **Matched Population:** The maintained `ordinary-small-greeting` and `openspec-add-json-output` scenarios each ran three baseline `core` samples and three candidate `core-beads` samples with the same request, fixture, permission, validation, model, profile, and scenario identities. All 12 command, proof, and validation statuses were zero; permission violations were empty; commit, credential-read, install, and remote effects were absent; and fixture, process, and session cleanup was complete for every sample.
- **Evaluator Result:** Both `no-regression` and `improvement` returned `failed` with the sole reason `friction-regression:openspec-add-json-output:totalToolCallCount:24:26`. The ordinary scenario median remained five total calls with zero failed calls in both arms. The OpenSpec scenario median retained one configured provider request, one failed call, zero duplicate failed invocations, and zero owner questions in both arms, but total calls increased from 24 to 26. `improvedField` was `null`; no context or tool-use improvement is claimed.
- **Observation Route:** The first owning-CLI capture completed 12 live calls and printed a no-regression result, but its finalizer deleted the evidence root before raw rows could be inspected. A second direct in-memory capture completed another 12 calls but a summary formatter typo discarded the only in-memory result. The selected causally distinct observer self-tested its formatter, wrote the sealed raw bundle and bounded summary before formatting, and produced the current inspectable result.
- **Outcome:** Task 5.4 is complete with a truthful observed friction regression. Under the owning requirement, a neutral or worse context result does not invalidate the independently proved functional one-project `BPB-001` claim, but it prohibits any context-benefit claim.
- **Do-Not-Repeat Condition:** Do not run another equivalent 12-call matched capture for this unchanged candidate/population, do not use the owning CLI when raw rows are required after completion, and do not format an in-memory-only bundle before a durable write.
- **Evidence-Based Retry Condition:** Reconsider matched capture only after a material candidate or maintained-scenario change can plausibly alter the observed friction result and a durable observation route is available. Suppression identity: `checkpoint:bpb-5.4-raw-sample-observation`.

## 2026-09-02 - Complete the fresh critical-risk SDET boundary

- **Task And Candidate:** `6.1`; fresh SDET task `ses_f9e15b486ffeWte42mPGmlX6xt` inspected the current working tree at source `HEAD` `a8a7957ad5fa1c16723ec35147c52cec44805a44`. Effective model: `xai/grok-4.6`.
- **Terminal Result:** `Action: no-critical-risk`. The SDET authored no test or production changes and reported no critical-risk rows. Main therefore had no reported row to reproduce or correct; no known confirmed reachable critical or non-deferrable defect remains from this bounded challenge.
- **Named Critical Oracles:** Focused adapter coverage challenged arbitrary argv/shell fallback, identity-before-execution, and unknown child liveness. Registration coverage challenged one-project registration, absent-PID non-takeover, missing-storage fail-closed behavior, and the lock location outside `.beads`. Project lifecycle coverage challenged tracked, hook, remote, external-path, and config mutation plus store/drift preservation. Kaizen coverage challenged duplicate creation, close-before-resolve ordering, incomplete/stale/resolved-open terminal states, and assignment without source authority. Workstation coverage challenged active/unknown-writer preservation and drift-safe rollback.
- **Validation And Cleanup:** `npm run test:focused:beads-adapter`, `npm run test:focused:beads-registration`, `npm run test:focused:beads-lifecycle`, `npm run test:focused:beads-kaizen`, and `npm run test:focused:beads-workstation` each exited zero in the fresh SDET context. Main readback found the worktree path set unchanged and no current `bd` or `dolt` process rows. A pre-existing temporary Kaizen-orchestrator directory and existing Node processes were correctly left untouched because this SDET did not own them.
- **Known Non-Critical Evidence Limits:** The focused tests use disposable fake-`bd` or injected-command boundaries rather than recreating the protected 145,740,800-byte executable proof. The fake list boundary asserts but does not independently re-prove the live vendor `--all` closed-state lookup. No separate elapsed-time stale-lock oracle is needed for the current implementation because acquisition fails closed on any present lock and does not consult `startedAt` or PID liveness. These limits do not broaden task 5.3's exact installed-proof ceiling.

## 2026-09-02 - Complete operator and agent documentation

- **Task And Candidate:** `6.2`; `candidate:bpb-6.2-documented-source-20260902` against source `HEAD` `a8a7957ad5fa1c16723ec35147c52cec44805a44` plus the current working-tree candidate.
- **Documentation Ownership:** Existing owners were extended only: root profile guidance in `README.md`, onboarding in `docs/getting-started.md`, workstation operation in `tools/windows/README.md`, and agent routing in `global/skills/beads-portfolio-bridge/SKILL.md`. No separate Beads guide, thin command, raw-argv passthrough, vendor setup, managed instruction, plugin, MCP, or generic runner was added.
- **Documented Contract:** The docs preserve the exact `v1.2.2` archive/executable provenance, separate binary/profile/registration/activation states, concrete core/core-beads/all/full-source discovery, fresh-process boundary, one-project registration and lifecycle, Kaizen promotion, advisory ready/relation/assignment, exact OpenSpec link and terminal reconciliation, duplicate/stale/unknown repair, privacy, evidence preservation, no-voting, no-remote, version limitations, and stop line. The skill maps only the existing exported closed semantic operations and their exact bounded input fields.
- **Real Readback:** Public help listed only `beads-preview`, `beads-install --source <bd.exe>`, `beads-check`, and `beads-rollback [--dry-run]`. Source `beads-preview` returned the exact pin, protected paths, non-effects, and `activation: none`; source `beads-check` returned `status: absent`, `reason: not-installed`, and `writer: not-registered`. Profile preview reported the concrete 50-owner `core-beads` surface and changed no file or environment value. A fresh isolated unprofiled-source OpenCode process found exactly one Beads skill and all six helper files with zero missing selected artifacts, staging paths, or unresolved placeholders, then cleaned up; no retained matching loader/test child or `bd`/Dolt process remained.
- **Validation And Effects:** Focused library tests passed 184 cases after one bounded shell timeout was verified to leave no matching process and rerun with a sufficient observation window. Instruction-context tests passed 15 and installer tests passed 32. Strict BPB OpenSpec validation passed; task-scoped `git diff --check` reported only LF-to-CRLF working-copy warnings. The scan found no task-authored personal path, credential value, arbitrary `bd` command, thin passthrough, or affirmative vendor-setup instruction. No binary/profile/project activation, provider, user-process, credential, remote, commit, release, or deployment effect occurred.
- **Evidence Ceiling:** This proves the current documented source, profile preview, fresh full-source loader inventory, and absent/unregistered provider-free status path. It does not reinstall Beads, reactivate the task 5.3 project, rerun the matched provider population, or claim context improvement. Task 6.3 owns final whole-candidate validation and cleanup reconciliation.

## 2026-09-02 - Complete BPB-001 final validation

- **Task And Candidate:** `6.3`; `candidate:bpb-6.3-final-working-tree-20260902`, branch `main`, source `HEAD` `a8a7957ad5fa1c16723ec35147c52cec44805a44`, no staged or conflicted path, and the exact current BPB working-tree path set captured by `repo-candidate-snapshot`.
- **Requirement-To-Oracle Reconciliation:** Exact release/pin and closed command behavior map to focused release and adapter suites plus tasks 1.1, 5.1, and 5.3. One-project registration, canonical-root effects, writer serialization, enable/disable, and data-preserving rollback map to focused registration, project-lifecycle, and workstation suites plus the installed task 5.3 path. Kaizen eligibility/promotion, coarse dependencies/readiness, advisory assignment, exact OpenSpec link recovery, and ordered terminal reconciliation map to the focused Kaizen suite and task 5.3 interruption/replay observations. Profile discovery/stay-quiet requirements map to 184 library, 32 installer, and 15 instruction-context cases plus fresh source and core loader checks. Context-cost requirements map to 51 consumer-outcome cases and task 5.4's truthful `24:26` OpenSpec tool-call regression. Cross-owner authority and final artifact consistency map to strict OpenSpec validation and the current planning reconciliation. Critical data-integrity/rollback risk maps to task 6.1's fresh `Action: no-critical-risk` result and main's focused replay.
- **Current Runtime And Cleanup Readback:** The public source entrypoint returned Beads `status: absent`, `reason: not-installed`, `writer: not-registered`, no drift, and no retained path. The exact protected executable, bridge root, lifecycle record/module, release helper/manifest, and registration paths were absent. No current `bd.exe`, `dolt.exe`, runtime-loader, or focused-test process remained. Fresh core loader inspection reported `beadsSkillCount: 0` and no helper files; fresh unprofiled source reported one skill and all six helpers. Process and user `OPENCODE_CONFIG_DIR` remained the same unprofiled current global source.
- **Installed And Dolt Evidence Continuity:** Task 5.3's exact installed production-entrypoint, embedded-Dolt history/integrity/size, response-loss, relation, terminal, disable, partial-unknown rollback, completed rollback, and store-preservation evidence remains current because subsequent candidate changes were documentation and planning only. Its proof-owned project/store and protected installation were deliberately removed after inspection. Recreating either would violate the current cleanup state and the unchanged live-attempt stop condition, so final validation reused that bounded current functional evidence and added the absent-state readback rather than performing another install or project activation.
- **Validation:** Focused release, adapter, registration, project lifecycle, Kaizen orchestrator, workstation lifecycle, workstation configuration, workstation restart, and consumer-outcome suites passed; focused library, installer, and instruction-context suites passed 184, 32, and 15 cases. `npm run validate` reported `skills=35 agents=22 markdown=1153 warnings=0 infos=2`. `openspec validate integrate-beads-portfolio-bridge --strict` passed. `git diff --check` exited zero with only LF-to-CRLF working-copy warnings. The current remote inventory was read-only, `HEAD` stayed unchanged, and the index stayed empty.
- **Known Non-Critical Limitations:** The installed binary is intentionally absent after successful rollback; use requires a future explicit matching install/profile/one-project activation. The maximum claim remains one exercised workstation and one disposable project. Current focused tests use fake or injected `bd` boundaries and do not broaden task 5.3's live exact-pin evidence. The fake list test does not independently re-prove the live vendor `--all` lookup. The matched candidate did not improve context cost and regressed the maintained OpenSpec scenario's total calls from 24 to 26. The pinned release still excludes production work leases, events, federation, HTTP server, remote/team behavior, and voting.
- **External Operations And Stop Line:** This final-validation pass performed no install, profile activation, project activation, provider call, credential read, user-process action, remote operation, commit, merge, release, deployment, shared server, federation, or unrelated cleanup. Earlier authorized task 5.1/5.3 host installation and one pinned download were rolled back with the project proof root removed after inspection. The change remains unarchived; the next boundary is the separate canonical `/opsx-archive` operation.
