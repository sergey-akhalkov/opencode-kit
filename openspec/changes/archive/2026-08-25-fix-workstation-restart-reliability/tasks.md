## 1. Effect-free controller contract

- [x] 1.1 Confirm repository `--help`, `preflight`, and `status` still exit `0`, list `restart`/`stop`, and mutate no file, task, or process. **Current Rung:** live log plus repository source. **Next Real Boundary:** `node tools/windows/opencode-workstation.ts --help`. **Blocker/Unblocker:** none. **Observable Proof:** help stdout lists the public modes and the command exit is `0` with unchanged ProgramData hashes. **Authorization/Safeguards:** repository source only. **Restoration/Cleanup:** none. **Immutable Evidence:** command exits and stdout. **Validation:** `node --check tools/windows/opencode-workstation.ts`.

## 2. Targeted stop and honest tray Restart

- [x] 2.1 In the repository controller, stop only validated supervisor, server-root, and listener PIDs without `/T`; treat already-gone validated PIDs as stopped; stop a matching `starting` tree; change `serve()` child termination to that same targeted kill; keep unmatched/drifted identity fail-closed. Update the derived tray helper so one in-flight Restart stays blinking, a second click starts no second controller, a failed Restart stays red with a secret-free balloon naming `controller-errors.log`, and a leftover listener is not treated as success. **Current Rung:** 1.1 help. **Next Real Boundary:** `node --check` plus installed `--help` after the source edit, before ProgramData repair. **Blocker/Unblocker:** none. **Observable Proof:** source no longer issues `taskkill /T` from `stopManagedServer` or `serve` terminate; tray script contains the failure balloon and no silent post-failure `start`. **Authorization/Safeguards:** repository source only. **Restoration/Cleanup:** none until repair. **Immutable Evidence:** reviewable diff and `--check` exit. **Validation:** `node --check tools/windows/opencode-workstation.ts`.

- [x] 2.2 After a re-verified elevated token, stop the managed server if the current installer requires it, repair the ProgramData controller and tray from the repository source, then invoke Restart once from the live `opencode-server` tray menu. **Current Rung:** installed candidate `8e403f654255d4dc9e85f6d17b23613ecdc98a55ac8126c2bb83124f1a6f4dd9`. **Next Real Boundary:** 3.1c unmatched-listener refusal. **Blocker/Unblocker:** none. **Observable Proof:** `task-2-2n-install/evaluation.json` passes all maintained checks; `task-2-2n-tray-restart-raw.json` shows restarting, supervisor `14616 -> 13504`, server root `5892 -> 9140`, listener `1636 -> 7016`, green health, both tasks Running, and no restart error. **Authorization/Safeguards:** elevated detached writers; exact tray PID `6744` recycled; loopback; one writer; no provider/model call; no target-repo write. **Restoration/Cleanup:** one healthy server and green tray retained. **Immutable Evidence:** `implementation-evidence/task-2-2n-install/{raw,evaluation}.json`, `implementation-evidence/task-2-2n-tray-restart-raw.json`. **Validation:** installed status health/integrity exercised by both bundles.

## 3. Fail-closed and Desktop surfaces

- [x] 3.1 Prove Restart/Stop refuse an unmatched listener and report the ownership diagnostic without killing that process. **Current Rung:** final candidate `8e403f65...` fail-closed proof complete. **Next Real Boundary:** 3.2 Desktop Restart. **Blocker/Unblocker:** none. **Observable Proof:** after installed Stop released the managed tree, writer PID `14972` became sole `4096` owner; tray Restart emitted `Current port owner does not match the managed listener identity.`, stayed red, left PID `14972` alive/owning the port, and disclosed neither credential. **Authorization/Safeguards:** writer-owned disposable loopback listener; no `/T`; captured managed identities only; no replacement while any port owner remained. **Restoration/Cleanup:** released writer socket, proved ports empty, restored one healthy green runtime with both tasks Running and no restore error. **Immutable Evidence:** `implementation-evidence/task-3-1c-raw.json`. **Validation:** terminal bundle `proofPassed: true`, `ok: true`.

- [x] 3.2 Invoke Desktop `OpenCode Server - Restart.lnk` once against a healthy managed server and confirm the same replacement contract as tray Restart. **Current Rung:** complete accepted runtime scope for candidate `8e403f65...`. **Next Real Boundary:** fresh SDET 4.1. **Blocker/Unblocker:** none. **Observable Proof:** actual `.lnk` projection matched protected `wscript.exe`/`invoke.vbs restart`; invoker PID `5560` exited `0`; supervisor `5848 -> 3296`, server root `9848 -> 7740`, listener `11320 -> 12584`; green authenticated health returned and no restart error appeared. **Authorization/Safeguards:** detached local loopback invocation; exact timeout-only invoker cleanup guard remained unused. **Restoration/Cleanup:** one healthy green runtime with both tasks Running; no dialog process remained. **Immutable Evidence:** `implementation-evidence/task-3-2-raw.json`. **Validation:** final installed health/ports/tasks in the bundle.

## 4. Qualification

- [x] 4.1 [automation-dividend] After current MVP and complete accepted scope, dispatch one fresh critical-only SDET against targeted stop, already-gone success, starting-state Restart, unmatched-listener fail-closed, tray failure signaling, and credential non-disclosure. **Current Rung:** terminal `no-critical-risk`. **Next Real Boundary:** project-native validation 4.2. **Blocker/Unblocker:** none. **Observable Proof:** fresh SDET session `ses_fc66ca6c6ffeJiu5K9pnvzsQvO`, Effective Model `xai/grok-4.6`, Candidate `8e403f65...`, authored `tools/test-workstation-restart-critical.ts`; after main corrected one contradictory mock only, exact focused command exits `0` with `OK: workstation restart critical tests=5`; resumed same session returned `no-critical-risk` and no risk rows. **Authorization/Safeguards:** test-only write; no live lifecycle or credential read by SDET. **Restoration/Cleanup:** live host untouched by SDET and remains healthy/green. **Immutable Evidence:** terminal SDET reports plus command output. **Validation:** main reproduced the first harness-only failure, corrected only PID `4242` mock disappearance, reran green, and accepts no critical rows. Known non-critical limits: starting-state source/offline rather than live injection; balloon string source-locked while live 3.1c proves red failure but not notification rendering.

- [x] 4.2 Run `node --check tools/windows/opencode-workstation.ts`, installed `--help`/preflight/status, `npm test`, `npm run validate:strict`, and `npx openspec validate fix-workstation-restart-reliability --strict`. Record known non-critical limits. **Current Rung:** qualification validation complete for candidate `8e403f65...`. **Next Real Boundary:** deterministic complete archive. **Blocker/Unblocker:** none. **Observable Proof:** source check, focused config `7/7`, focused Restart `5/5`, full `npm test`, strict library validation with zero warnings, and strict OpenSpec validation exit `0`; final TypeScript installed validation `r3` confirms exact candidate/controller hash, help lists Stop/Restart, preflight with the manifest configuration exits `0`, status exits `0`, integrity is complete, and both tasks are Running. **Authorization/Safeguards:** local read-only validation; installed runner reads no credential file and invokes no lifecycle mode. **Restoration/Cleanup:** requested installed workstation retained healthy with both tasks Running. **Immutable Evidence:** `implementation-evidence/task-4-2-installed-validation.json` preserves the omitted-config runner failure; `implementation-evidence/task-4-2-installed-validation-r2.json` preserves the passing PowerShell result; `implementation-evidence/task-4-2-installed-validation-r3.json` is the terminal TypeScript-only `ok: true` result; command stdout/stderr retained in the session. **Validation:** all named commands. Known non-critical limits: starting-state is source/offline tested rather than live injected; live 3.1c proves red ownership refusal while balloon text is source-locked rather than UI-captured.
