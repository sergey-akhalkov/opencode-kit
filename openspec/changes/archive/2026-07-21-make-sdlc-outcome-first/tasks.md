## 1. Freeze Outcome, Bootstrap Authority, And Baseline

- [x] 1.1 Main session: record `AC-1` through `AC-6` from this task group as the accepted outcome capsule before loaded-instruction mutation: `AC-1` outcome/protected-boundary scope replaces file-count scope; `AC-2` qualification-attempt failure does not automatically end the root goal; `AC-3` outcome blockers, non-deferrable blockers, contained material limitations, and deferrable debt route distinctly; `AC-4` user blocking requires the complete decision-ready handoff; `AC-5` canonical and mirrored instruction surfaces are non-contradictory without runtime-context growth; `AC-6` deterministic and fresh-session evidence covers autonomous continuation, protected-boundary blocking, debt handoff, and no-progress stopping. Pass condition: each criterion has an observable scenario from the delta specs and no criterion authorizes external operations or critical-risk waiver.
- [x] 1.2 Main session: resolve the active global configuration directory, inventory current workspace ownership and overlap with `integrate-continuous-sdlc-learning`, and freeze the exact production/test write sets, mandatory gates, one current-policy correction wave, reviewer waves, and stop line required by the pre-change loaded authority. Pass condition: the implementing session explicitly states that the candidate cannot retroactively authorize its own scope and that installation, activation, reload, commit, push, merge, release, credentials, destructive action, and remote mutation are forbidden.
- [x] 1.3 Main session: capture baseline outputs for `git status --short`, `npm run instruction:inventory -- --format json`, `npm run validate:strict`, `npm test`, and `npm run openspec:validate`. Pass condition: all exit codes, existing failures or warnings, runtime-artifact counts, `global/AGENTS.md` token proxy, combined intended-runtime-path token proxy, and unrelated changed files are recorded without candidate mutation.

### Frozen Scope Capsule And Baseline

- Frozen on 2026-07-20 before loaded-instruction mutation. Profile: `Material`. Primary outcome: replace process-only Change-Ready blocking with outcome-first local reversible dependency closure while retaining protected-boundary owner authority, non-waivable safety, finite candidate qualification, and decision-ready handoff.
- Frozen acceptance criteria: `AC-1` through `AC-6` exactly as stated in task 1.1. Frozen task IDs: `1.1`, `1.2`, `1.3`, `2.1`, `2.2`, `2.3`, `2.4`, `2.5`, `3.1`, `3.2`, `4.1`, `4.2`, `5.1`, `5.2`, `6.1`, `6.2`, `6.3`, and `7.1`.
- Production write set: `global/AGENTS.md`, `global/skills/change-ready-sdlc/SKILL.md`, `REPO_AGENTS.md`, `instructions/reusable-project-agent-instructions.md`, `instructions/universal-development-loop.md`, `templates/project/AGENTS.md`, `instructions/leaf-reviewer-agent-contract.md`, `global/agents/final-candidate-reviewer.md`, `tools/contracts/reviewer-binding.ts`, `tools/contracts/skills.ts`, `tools/validators/active-authority.ts`, `tools/validators/routing.ts`, and `tools/validators/devkit-contract.ts`. Use fewer paths when sufficient.
- Test write set: only existing `tools/test-contracts-change-ready.ts`, `tools/test-contracts-change-ready-delivery.ts`, `tools/test-contracts.ts`, `tools/test-helpers/library.ts`, `tools/test-library/validator-change-ready.ts`, and `tools/test-library/doctor.ts`. Fresh SDET is the sole automated-test author; use fewer paths when sufficient.
- Main-owned allowed artifacts: checkbox/status and evidence-reference updates in this `tasks.md`; privacy-safe local handoff evidence; no production or test content authored by main during delegated Material authorship.
- Existing workspace ownership: `global/AGENTS.md` had a pre-existing user/teammate addition limited to the `Codebase Memory MCP` section. It is unrelated to this outcome and must be preserved byte-for-byte. `openspec/changes/make-sdlc-outcome-first/**` is the current change. `integrate-continuous-sdlc-learning` is a tracked active planning change with conceptual overlap but no current uncommitted implementation mutation.
- Active global configuration directory: `D:\sa-gh\opencode-kit\global`. The candidate cannot retroactively grant this implementing session new authority. Installation, activation, reload, commit, push, merge, release, archive, credential use, privilege elevation, destructive action, and remote mutation are forbidden.
- Adapter discovery: requirements are this change's proposal/design/delta specs/tasks plus the owner's local-reversible choice; production author is the configured `implementation-worker`; SDET is a fresh `sdet-quality-engineer`; Candidate Reference is a privacy-safe scoped Git diff/path manifest with the pre-existing `global/AGENTS.md` hunk attributed separately; final review is `final-candidate-reviewer`; Material delivery is `session-delivery-reviewer`; native readiness is Change-Ready/Pilot-Ready; external operations are not authorized.
- Mandatory gates: baseline and post-production instruction inventory/strict validation; one isolated six-observation fresh-session proof before SDET and one replay after SDET; one initial fresh SDET; executable parent contract/library suites; one complete-candidate `npm run validate:strict`, `npm test`, and `npm run openspec:validate`; one instruction-artifact review; one fresh final-candidate review; and one Material delivery review.
- Finite attempt: one eligible correction wave, at most one fresh corrected-candidate SDET if consumed, one final review, and one delivery review. Under the active pre-change authority, final or delivery rejection is terminal for this implementation attempt.
- Stop line: the exact acceptance criteria, task IDs, production/test paths, gates, and review waves above. No new runtime/test/support artifact, validator framework, lifecycle service/store, third profile, fuzzy semantic classifier, reusable evidence product, automatic external authority, or rewrite of `integrate-continuous-sdlc-learning`.
- Baseline `git status --short`: pre-existing `M global/AGENTS.md` plus `?? openspec/changes/make-sdlc-outcome-first/`; no other uncommitted path.
- Baseline instruction inventory: pass; 55 artifacts; 92,988 total token proxy; `global/AGENTS.md` token proxy 12,092; combined token proxy for the eight intended runtime instruction paths 32,503. The pre-existing Codebase Memory section is included in those baselines.
- Baseline `npm run validate:strict`: pass; `skills=24 agents=18 markdown=158 warnings=0 infos=1`; top-level allow permission is informational.
- Baseline `npm test`: pass with exit code 0; library 271, contracts 79, and all other suites passed. Existing Node module-type performance warnings are non-failing.
- Baseline `npm run openspec:validate`: pass; 9 passed, 0 failed.

## 2. Implement Canonical Outcome-First Runtime Policy

- [x] 2.1 Production author: replace the closed-world file/task and process-only blocker semantics in `global/AGENTS.md` with concise outcome authority, protected boundaries, local reversible dependency closure, result/debt disposition, and decision-ready blocker handoff. Pass condition: Ordinary Small/Material routing, non-waivable safety, unrelated-work preservation, external-operation authority, and explicit owner control of protected boundaries remain intact; internal revision or correction exhaustion alone cannot trigger a user question.
- [x] 2.2 Production author: update `global/skills/change-ready-sdlc/SKILL.md` so one correction wave, final review, and delivery remain terminal for the inspected qualification attempt while bounded diagnosis and an evidenced local reversible repair can route a new candidate attempt without owner approval of lifecycle bookkeeping. Pass condition: unchanged-candidate retry remains forbidden, every mutation invalidates affected evidence, leaf findings remain non-authorizing, and missing mandatory capability or protected-boundary work produces an exact blocker rather than an invented continuation choice.
- [x] 2.3 Production author: align only contradictory project-facing and role-specific surfaces selected by task 1.2 from `REPO_AGENTS.md`, `instructions/reusable-project-agent-instructions.md`, `instructions/universal-development-loop.md`, `templates/project/AGENTS.md`, `instructions/leaf-reviewer-agent-contract.md`, and `global/agents/final-candidate-reviewer.md`. Pass condition: no surface requires owner approval solely for a necessary local reversible task/path/artifact or internal revision; reviewers remain read-only/non-authorizing; no secondary artifact copies the complete canonical policy.
- [x] 2.4 Production author: replace closed-world marker constants, diagnostics, and configured-surface checks in the existing owners selected by task 1.2 from `tools/contracts/reviewer-binding.ts`, `tools/contracts/skills.ts`, `tools/validators/active-authority.ts`, `tools/validators/routing.ts`, and `tools/validators/devkit-contract.ts`. Pass condition: deterministic checks require outcome/protected-boundary authority, local reversible dependency closure, per-attempt terminal semantics, no process-only approval, unchanged-candidate anti-retry, and decision-ready blocker fields without inferring natural-language necessity, reversibility, cause, safety, reachability, or risk acceptance.
- [x] 2.5 Main session: inspect the production diff for exact ownership and requirement traceability, confirm no automated test artifact changed, capture a readable Candidate Reference, and run `npm run instruction:inventory -- --format json` plus `npm run validate:strict`. Pass condition: production scope matches task 1.2, strict validation passes or yields one attributable failure batch, no runtime artifact was added, and `global/AGENTS.md` plus the combined changed runtime corpus do not exceed their recorded baselines.

### Production Evidence And Attempt History

- Instruction production author session `ses_07fc22044ffedldqdkLa8tyFg8` changed only the eight frozen Markdown paths and preserved the pre-existing Codebase Memory hunk. TypeScript production author session `ses_07fbc9401ffePieSp36ndZDECH` changed only the five frozen contract/validator paths. No automated test path changed.
- Attempt 1 production Candidate Reference before correction: `518653abe723d474dbac549c3fca9480802aede2`. Initial inventory failed AC-5: `global/AGENTS.md` 12,858 > 12,092 and combined runtime paths 34,671 > 32,503. The exact local reversible compression consumed attempt 1's single correction wave.
- Attempt 1 corrected Candidate Reference: `7169274359521735fdae2695f024abd911870054`. Inventory then passed (`global/AGENTS.md` 11,556; combined runtime 30,220), but strict validation exposed 20 omitted established continuation/delivery/Pilot contract tokens. Attempt 1 closed with this candidate-attributable failure; it was not retried unchanged.
- Attempt 2 reused the same instruction author and original path ownership with unchanged outcome/protected boundaries. The author restored the exact missing safeguards before replay. Post-repair inventory passes: 55 artifacts; total token proxy 90,851; `global/AGENTS.md` 11,594 <= 12,092; combined runtime paths 30,366 <= 32,503. `npm run validate:strict` passes with `skills=24 agents=18 markdown=158 warnings=0 infos=1`.
- Production candidate diff reference before OpenSpec evidence-marker recapture: `81437340cfa41caf35c401d0ba645a435c89c99f`. Current changed production scope is exactly 13 frozen paths; no new runtime artifact exists.

## 3. Prove The Candidate Runtime Behavior

- [x] 3.1 Main session: run one isolated fresh-session read-only proof with the candidate `global/` directory as `OPENCODE_CONFIG_DIR`, an explicitly selected authenticated model, JSON output, and no retained fixture mutation. Pass condition: correction-budget exhaustion with available diagnosis continues without `question`; final-review rejection with a local reversible outcome repair routes a new candidate attempt; working output with only deferrable debt returns one residual bundle; a contained material limitation is presented only after proof and before `Pilot-Ready: yes`; a protected boundary produces the complete authority request; repeated unchanged failure stops with evidence and no revision jargon.
- [x] 3.2 Main session: verify the proof transcript preserves failed-candidate evidence, never retries an unchanged candidate, distinguishes `Change-Ready: no` from root-goal completion, and does not perform implementation, installation, activation, remote, credentialed, destructive, or external operations. Pass condition: all six `AC-6` observations are directly evidenced by tool/question order and final handoff content rather than keyword presence alone.

### Pre-SDET Applicable Proof

- Fresh primary session `ses_07fabf58effe7k0xmsoPa8ne1n`; model `openai/gpt-5.5`; JSON output; candidate `OPENCODE_CONFIG_DIR=D:\sa-gh\opencode-kit\global`; empty isolated fixture `C:\Users\Sergey\AppData\Local\Temp\opencode\outcome-first-proof-attempt2-20260720` remained at zero entries.
- Scenario 1 preserved exhausted-attempt evidence and continued read-only diagnosis/local reversible repair without a user question. Scenario 2 kept final-review rejection terminal for the inspected attempt and routed a changed candidate without process approval. Scenario 3 delivered proven outcome with one deferrable-debt bundle and no question. Scenario 4 required one post-proof material-risk acceptance question before `Pilot-Ready: yes`. Scenario 5 stopped before changed API compatibility/elevated credentials and produced a product/consequence authority request. Scenario 6 forbade unchanged retry, preserved evidence, and reported the missing capability without revision jargon or a fictitious question.
- Event order was one `step_start`, one text decision covering all six cases, and one `step_finish`; there were no tool, question, mutation, agent, install, activation, remote, credentialed, destructive, or external-operation events.

## 4. Add Fresh Independent Test Evidence

- [x] 4.1 Fresh SDET: independently derive a risk/oracle matrix from `AC-1` through `AC-6` and author or strengthen only focused automated tests in the existing test paths frozen by task 1.2, selected from `tools/test-contracts-change-ready.ts`, `tools/test-contracts-change-ready-delivery.ts`, `tools/test-contracts.ts`, `tools/test-helpers/library.ts`, `tools/test-library/validator-change-ready.ts`, and `tools/test-library/doctor.ts`. Required oracles cover canonical/mirror markers, forbidden process-only approval language, attempt-versus-root distinction, leaf non-authorization, protected boundaries, residual-debt batching, decision-ready handoff fields, no fuzzy semantic classification, and historical-artifact exclusion. No production artifact or new test/support framework may be created.
- [x] 4.2 Main session: inspect the SDET diff for exact test-only ownership, integrate it, recapture the Candidate Reference, and run `node tools/test-contracts.ts` plus `node tools/test-library.ts`. Pass condition: both executable parent suites pass and reach the new cases; extracted modules are not invoked as standalone scripts; any candidate-attributable failure is preserved with its exact reproducer.

### Fresh SDET And Attempt 2 Correction Eligibility

- Fresh SDET task/session `ses_07fa9eb0cffegS5cTZNB1I9AO6`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed exactly the six frozen test paths, reported `Blocking Evidence: none`, and left all production/OpenSpec paths unchanged. The independent matrix covers AC-1 through AC-6 through real validator subprocess/static contract boundaries; semantic routing remains assigned to fresh-session proof. No new harness or mock framework was added.
- Initial parent replay preserved 10 `node tools/test-contracts.ts` failures and one `node tools/test-library.ts` failure. Nine contract failures are production omissions introduced by attempt-1 compression and still present in attempt 2: stable Pilot wording in global autonomy, Ordinary Small final-review exemption in UDL, Material delivery wording, exact Candidate Reference wording and pre-proof writer closure, actual async/concurrent writer boundary, exact destructive-operation authority, non-portable dual-identity statement, and related lifecycle safeguards. Baseline passed; current candidate fails; each maps to AC-2/AC-5 and fits existing frozen instruction paths.
- Two failures are new SDET-oracle defects: the 16-step UDL parser does not accept stage qualifiers between the backticked name and colon, and the `protected-boundary` seeded-removal case mutates a file where another occurrence leaves the shared marker present. Both map to AC-5/AC-6, have concrete parent-suite reproducers, and fit existing frozen test paths.
- Attempt 2 correction wave is now consumed by one serial owner batch: same production author restores only the nine stable safeguards, then one new fresh corrected-candidate SDET fixes only the two oracle defects and reassesses the corrected candidate. All proof, Candidate Reference, SDET, validation, and review evidence after production mutation must be recaptured/replayed. A later serious failure ends attempt 2.
- Production correction changed only `global/AGENTS.md`, `global/skills/change-ready-sdlc/SKILL.md`, and `instructions/universal-development-loop.md`; post-correction inventory passes at 55 artifacts, total 91,189, global AGENTS 11,629, combined runtime paths 30,704; strict validation passes. Corrected Applicable Proof session `ses_07f96d71bffe1sisDy3ARpQ475` repeated all six AC-6 routes with no tool/mutation event and an empty fixture.
- Fresh corrected-candidate SDET task/session `ses_07f957769ffeLKuwPjYHBekld7`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-contracts.ts` and `tools/test-library/validator-change-ready.ts`: the UDL parser now recognizes parenthesized stage qualifiers while retaining all 16 stages, and the `protected-boundary` negative fixture removes every operative occurrence before validating the expected failure. It reported `Blocking Evidence: none`; same-model SDET correlation remains residual.
- Parent replay after corrected SDET: `node tools/test-contracts.ts` passed 80/80; `node tools/test-library.ts` passed 299/299. Output includes outcome-authority arrays/surfaces, process-only forbidden patterns, attempt/root progress gate, Candidate Reference and writer closure, blocker/debt classes, Material/Pilot safeguards, historical exclusion, no-fuzzy checks, all 16 UDL stages, and the corrected shared-marker negative case. Extracted modules were not run standalone.

## 5. Validate The Complete Candidate

- [x] 5.1 Main session: replay tasks 3.1 and 3.2 against the complete post-SDET candidate and rerun `npm run instruction:inventory -- --format json`. Pass condition: all six behavioral observations still pass, no unexpected question or mutation occurs, no runtime artifact was added, and both instruction non-growth baselines remain satisfied.
- [x] 5.2 Main session: run `npm run validate:strict`, `npm test`, and `npm run openspec:validate` against the same complete candidate. Pass condition: all commands exit `0`; known baseline findings remain attributed; no unchanged-candidate retry or unapproved cleanup is used to obtain green results.

### Post-SDET Candidate Proof

- Tracked production-plus-test diff reference after corrected SDET: `002bccd97eab95531b1d39e359e9aacf430b4a4f`. All 13 production and six test paths remain inside the frozen scope; no runtime/test/support file was added.
- Fresh primary session `ses_07f8f7367ffegCf6UymI2qkrC2`, model `openai/gpt-5.5`, JSON output, candidate global config, and empty isolated fixture `outcome-first-proof-post-sdet-20260720` repeated all six AC-6 routes. Event order remained `step_start` -> one six-scenario text decision -> `step_finish`; no tool, question, mutation, agent, credential, activation, remote, destructive, or external-operation event occurred.
- Post-SDET inventory remains 55 artifacts and total token proxy 91,189; `global/AGENTS.md` 11,629 <= 12,092; combined eight runtime paths 30,704 <= 32,503. No runtime artifact was added.

### Complete Candidate Validation

- `npm run validate:strict` exited `0`: `skills=24 agents=18 markdown=158 warnings=0 infos=1`. The unchanged top-level allow permission remains the single informational finding.
- `npm test` exited `0`: library 299, contracts 80, and every remaining repository suite passed. The unchanged Node module-type performance notices remain non-failing.
- `npm run openspec:validate` exited `0`: 9 passed, 0 failed, including `make-sdlc-outcome-first`. No retry, cleanup, or candidate mutation was used to obtain these results.

## 6. Independent Review And Delivery

- [ ] 6.1 Main session: dispatch one fresh read-only `instruction-artifact-reviewer` over the original owner decision, `AC-1` through `AC-6`, complete candidate, canonical/mirror placement, instruction inventory, fresh-session proof, SDET evidence, validation, protected boundaries, and residual risks. Pass condition: no unresolved finding proves process-only blocking, duplicated authority, unsafe automatic expansion, critical-risk deferral, or instruction-context growth.
- [ ] 6.2 Main session: after accepted SDET and complete validation, dispatch one fresh read-only `final-candidate-reviewer` over the same complete candidate and evidence. Pass condition: verdict is `approved` or `approved_with_notes` with no actionable candidate change; a rejection remains terminal for that inspected attempt and is routed according to the active implementing-session authority.
- [ ] 6.3 Main session: run the conforming Material delivery/readiness review with root-goal continuity, Candidate Reference, proof, SDET, validation, instruction review, final review, risk/debt disposition, rollback separation, and task evidence. Pass condition: delivery is accepted with no `Blocking Evidence`, no unfinished applicable task, and no claim that the candidate was installed, activated, committed, pushed, merged, released, or remotely applied.

### Attempt 2 Instruction Review Rejection And Attempt 3 Scope

- Fresh instruction-artifact review task/session `ses_07f8b1632ffe88VDWXzCJ539rz` returned `material tuning needed`, `Blocking for acceptance: yes`, and one high-confidence P1. Attempt 2 is closed with `Change-Ready: no`; final and delivery review were not run against its rejected candidate.
- Direct source diagnosis confirmed the finding. `agentsAuthorityProblem` accepted copied global authority that named only generic protected boundaries while omitting the eight canonical protected-boundary categories, the non-waivable critical-risk clause, and the complete decision-ready handoff fields. `skillAuthorityProblem` checked several outcome-authority markers against raw body text, so fenced examples could satisfy them. The permissive doctor fixture and its negative matrix preserved both gaps.
- This is an AC-4/AC-6 and task-2.4 omission in the current candidate, not authorization from the reviewer. It has a bounded, local, reversible repair inside the frozen `tools/contracts/skills.ts` and `tools/validators/active-authority.ts` production ownership plus existing frozen test paths; no outcome, protected boundary, runtime artifact, framework, or external operation changes.
- Attempt 3 reuses the original TypeScript production-author context for only that source repair. A new fresh SDET owns any focused validator/doctor fixture changes. Attempt 3 receives one correction wave, one final review, and one Material delivery review; source mutation invalidates Candidate Reference, proof, SDET, validation, and review evidence, so every applicable gate is replayed before readiness.
- Attempt 3 initial TypeScript production changed only those two source paths. Candidate Reference `3d057c7c5beda3154b55555b7d5372b9f3022bdf`; inventory remained 55 artifacts, total 91,189, global AGENTS 11,629, and combined runtime paths 30,704; strict validation passed with zero warnings.
- Initial attempt-3 fresh proof session `ses_07f7b244dffezlaZbrBjhBxbVc` used `openai/gpt-5.5`, JSON output, candidate global config, and empty fixture `outcome-first-proof-attempt3-20260720`. It emitted only `step_start`, one text response, and `step_finish`; the fixture remained empty. Autonomous attempt continuation, rejected-attempt routing, deferrable debt, contained material limitation, and no-progress stopping were correct, but the protected-boundary handoff omitted explicit root-cause status/confidence and attempted-path fields rather than reporting them as unknown. This is a direct AC-4 runtime failure against tasks 3.1/3.2.
- Attempt 3's single correction wave is consumed by the smallest canonical runtime guard: the original instruction author may change only `global/AGENTS.md` to require every decision-ready field to be stated explicitly with `unknown`/`none` when evidence is absent. No mirror, skill, validator, test, new artifact, or protected-boundary semantic change is authorized. All affected evidence is invalidated and must be replayed on the changed candidate.
- Corrected attempt-3 Candidate Reference `7bbce7f68835fcfcfb52baa8ef8be7efec2ef3cf`; inventory passed with 55 artifacts, total 91,223, global AGENTS 11,663 <= 12,092, and combined runtime paths 30,738 <= 32,503; strict validation passed with zero warnings.
- Corrected fresh proof session `ses_07f7836eaffekn1kF4whpEqgHA` used `openai/gpt-5.5`, JSON output, candidate global config, and empty fixture `outcome-first-proof-attempt3-corrected-20260720`. It passed all six routes. Scenario 5 stated all nine decision-ready fields explicitly, including root-cause confidence and attempted paths; event order was `step_start` -> one text response -> `step_finish`; no tool, question, mutation, agent, credential, activation, remote, destructive, or external-operation event occurred.
- Fresh attempt-3 SDET task/session `ses_07f76d2e6fferaW9j630VB6u8h`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-contracts-change-ready.ts`, `tools/test-contracts-change-ready-delivery.ts`, and `tools/test-library/doctor.ts`. It added independent exact-literal oracles and real pure-function/isolated-doctor matrices for all eight protected-boundary categories, the non-waivable clause, all nine handoff fields, and every required skill marker in closed/unclosed fences. No mock, new harness, production, instruction, or OpenSpec artifact changed; `Blocking Evidence: none`; model independence remains unknown.
- Attempt-3 parent replay passed: `node tools/test-contracts.ts` 81/81 and `node tools/test-library.ts` 300/300. The new output groups directly include copied-authority `8+1+9`, doctor qualification blocking, the operative explicit-field guard, and fenced-only skill authority.
- Complete post-SDET Candidate Reference `a079784ca068ba0ed4d4fe6c65058abb803a7179`. Fresh session `ses_07f66f87dffeOsARezb718mITR` repeated all six routes, stated all nine authority-request fields, emitted only one text event between start/finish, and left `outcome-first-proof-attempt3-post-sdet-20260720` empty. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and combined runtime paths 30,738; all remain below baseline.
- Complete attempt-3 validation passed on that same candidate: `npm run validate:strict` exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; `npm test` exited `0` with library 300, contracts 81, and every remaining repository suite passing; `npm run openspec:validate` exited `0` with 9 passed and 0 failed. Only the unchanged informational top-level-allow notice and non-failing Node module-type performance notices remain.
- Fresh attempt-3 instruction-artifact review task/session `ses_07f621656ffeP2eqxhLMG2Y3YH` returned `Verdict: clean`, high confidence, `Blocking for acceptance: no`, no findings, no `Blocking Evidence`, and task-6.1 pass condition met. It directly traced the prior P1 closure through centralized exact markers, operative source checks, independent literals, pure negative matrices, every closed/unclosed fence case, real isolated-doctor blocking, and the runtime explicit-field guard. Residuals are limited to unavailable raw transcript/stdout recomputation and unknown production/SDET model independence; neither contradicts readable source/test evidence.
- Fresh final-candidate review task/session `ses_07f5c60b3ffe9sjqIFDiYkeY7Z` rejected attempt 3 with one high-confidence P1: top-level fence masking does not exclude valid CommonMark container-prefixed examples such as `> ````, so exact markers inside those examples can remain operative. Task 6.2 failed, attempt 3 is terminal with `Change-Ready: no`, and delivery was not run.
- Bounded source diagnosis confirmed the reproduced class. No CommonMark parser dependency exists; `fencedCodeLineMask` intentionally recognizes only zero-to-three-space top-level backtick/tilde fences, while every current `operativeTextOutsideFences` consumer trusts its returned text. The current tests cover only top-level closed/unclosed fences.
- Attempt 4 uses the smallest fail-closed local guard rather than a new Markdown parser: the original TypeScript production author may change only `tools/validators/active-authority.ts` so any fence run outside an already recognized top-level fence, with a prefix other than zero-to-three spaces, is rejected as unsupported active-authority syntax; `operativeTextOutsideFences` must return no operative surface for every consumer, and AGENTS/skill authority must return a specific diagnostic. This covers blockquote, list, nested-container, four-space, and inline-prefixed fence syntax without semantic parsing. Current top-level fences remain supported. A new fresh SDET owns focused container/inline negative tests in existing paths.
- Attempt 4 has one correction wave, one final review, and one Material delivery review. The accepted outcome, protected boundaries, 13 production/six test path ceiling, no-new-artifact stop line, and all forbidden external actions remain unchanged. All candidate, proof, SDET, validation, and review evidence must be recaptured after source mutation.
- Attempt-4 initial production Candidate Reference `ab9a52204324ae7ed80fa3a9ccd3adad5243ca2a`; only `tools/validators/active-authority.ts` changed after attempt 3. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and combined runtime paths 30,738; strict validation passed with zero warnings. Fresh proof session `ses_07f504cbaffeUVP3JE4IZtg271`, model `openai/gpt-5.5`, passed all six routes with every handoff field, one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt4-20260720` fixture.
- Fresh attempt-4 SDET task/session `ses_07f4f4054ffew1NxMZyjQ0Wi0z`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-contracts-change-ready-delivery.ts`, `tools/test-library/validator-change-ready.ts`, and `tools/test-library/doctor.ts`. Four focused cases cover eight non-top-level prefix classes across backticks/tildes, exact privacy-safe direct diagnostics, empty shared operative text, supported top-level controls, delimiter runs inside supported fences, routing subprocess failure, and real isolated-doctor qualification blocking. No mock, new artifact, or production mutation; `Blocking Evidence: none`.
- Attempt-4 parent replay passed: contracts 83/83 and library 302/302. Complete post-SDET Candidate Reference `0a61aa52688f546afc04a3a362d189fcd8e9dc9b`. Fresh post-SDET proof session `ses_07f480da9ffes8YwZi40HJRThL` repeated all six routes with all nine blocker fields, one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt4-post-sdet-20260720` fixture. Inventory remained below the same baselines.
- Complete attempt-4 validation passed on that candidate: strict exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; full `npm test` exited `0` with library 302, contracts 83, and every remaining suite passing; OpenSpec validation exited `0` with 9 passed and 0 failed. No retry, cleanup, or candidate mutation was used to obtain green results; only the unchanged informational top-level-allow and Node module-type performance notices remain.
- Fresh attempt-4 instruction-artifact review task/session `ses_07f43d9a2ffe47hp8FrgO7ozbz` rejected task 6.1 with three high-confidence P1 findings: unsupported syntax returned an empty string that made forbidden/count-based role duplication checks fail open; raw global/skill lifecycle and outcome-authority presence checks still accepted supported-fence-only markers; and the unsupported detector inspected only the first delimiter run per unmasked line.
- Main source diagnosis confirmed all three. `validate-library` calls `validators/skills.ts` and `validators/routing.ts` directly and does not invoke doctor `agentsAuthorityProblem`/`skillAuthorityProblem`, so the raw static checks are independently reachable. `routing.ts` has positive, negative, and count-based consumers with opposite polarity, and the single-match regular expression misses a later unsupported run.
- Attempt 4's single correction wave is consumed by one serial TypeScript owner batch. Existing production ownership expands by the necessary local reversible dependency `tools/validators/skills.ts`; `tools/validators/active-authority.ts` and `tools/validators/routing.ts` remain existing owned paths. This adds no user-visible outcome, protected boundary, dependency, parser, artifact, semantic classifier, or external operation. The correction must expose one structured operative scan that distinguishes supported text from unsupported syntax, inspect every delimiter run, make every routing consumer reject unsupported syntax before positive/negative/count checks, and make canonical Change-Ready lifecycle/continuation presence checks use operative text. A fresh corrected-candidate SDET owns focused tests in the existing six-path test ceiling. Any later serious failure ends attempt 4.
- The correction changed exactly those three source files. `OperativeTextScan` now separates operative content from an unsupported line, the scanner inspects every unmasked delimiter run, all routing policy consumers reject unsupported syntax before positive/negative/count logic and otherwise use operative text, and canonical lifecycle/continuation checks use the same scan. `operativeTextOutsideFences` remains a compatibility wrapper. Current canonical files contain only supported syntax; no test, instruction, OpenSpec, package, dependency, or new artifact was changed by production.
- Corrected attempt-4 Candidate Reference `fce38256715e65d6d22da6650a34e4f8bf95c959`; inventory remained below baseline. Mandatory strict validation then failed exactly once: `implementation-worker handoff fields must include 'Verification': global/AGENTS.md`. The field is intentionally present in the normative fenced Universal Task Briefing Contract schema, so migrating this schema-presence check to operative prose was an overbroad correction, not missing runtime authority.
- Because attempt 4's correction wave was already consumed, that mandatory failure closes attempt 4 with `Change-Ready: no`; no corrected SDET, proof replay, final review, or delivery was run for that failed candidate. Bounded diagnosis identified an authorized attempt-5 repair in existing `tools/validators/routing.ts`: first reject unsupported syntax through the structured scan, keep all authority/routing positive/negative/count checks operative, but check the three `IMPLEMENTATION_WORKER_HANDOFF_FIELDS` against the supported raw document so the intentionally normative fenced schema remains valid. This changes no accepted semantics, protected boundary, path inventory, dependency, or artifact. Attempt 5 receives one correction wave, fresh proof/SDET/validation, one final review, and Material delivery.
- Attempt-5 initial production Candidate Reference `dca1258ebb745fd3c8ba74cf24dff32245849ee3`; only `tools/validators/routing.ts` changed after the failed attempt-4 candidate. Inventory remained below baseline and strict validation passed, closing the exact `Verification` reproducer. Fresh proof session `ses_07f3691a4ffenlhgNlZ6Is2Iz4`, model `openai/gpt-5.5`, passed all six routes/all nine fields with one text event, no tool/question/mutation/external action, and an empty fixture.
- Fresh attempt-5 SDET task/session `ses_07f3566e8ffeHs3w1FXrHEpiul`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-contracts-change-ready-delivery.ts`, `tools/test-library/validator-change-ready.ts`, and `tools/test-library/doctor.ts`. It covered multi-run scanning, opposite-polarity unsupported duplication, supported-fence-only required lifecycle/continuation/authority, supported-fence forbidden/duplicate controls, normative fenced schema, unsupported-before-schema, and representative doctor/subprocess paths with no mocks or production mutation.
- Parent replay passed contracts 84/84. Library produced one SDET-oracle failure after all candidate behavior assertions passed: `validator rejects unsupported role syntax before opposite-polarity policy duplication counts` expected POSIX `global/agents/implementation-worker.md`, while the Windows validator correctly emitted `global\agents\implementation-worker.md`. Attempt 5's single correction wave is consumed by one new fresh corrected-candidate SDET changing only that test assertion to a platform-neutral path oracle; production remains unchanged. Any later serious failure ends attempt 5.
- Fresh corrected-candidate SDET task/session `ses_07f2cfcd0ffeN11wlfXDugdEJm`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-library/validator-change-ready.ts`: the full relative implementation-worker path assertion now uses `path.join` while every failure, line, privacy, and no-misleading-duplication oracle remains. `Blocking Evidence: none`.
- Corrected parent replay passed contracts 84/84 and library 306/306. Complete post-SDET Candidate Reference `c6efef7f2caae31ccb1ee0dd1c7295d20df40d49`. Fresh post-SDET proof session `ses_07f2983dbffev01cWKMpQ1SCar`, model `openai/gpt-5.5`, repeated all six routes/all nine fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt5-post-sdet-20260720` fixture. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and runtime corpus 30,738, all below baseline.
- Complete attempt-5 validation passed on that candidate: strict exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; full `npm test` exited `0` with library 306, contracts 84, and every remaining suite passing; OpenSpec validation exited `0` with 9 passed and 0 failed. No unchanged retry, cleanup, or candidate mutation produced the green results; only the unchanged informational top-level-allow and Node module-type performance notices remain.
- Fresh attempt-5 instruction-artifact review task/session `ses_07f255057ffes7NgDwO0cuaOfC` rejected task 6.1 with two high-confidence P1 findings: the raw handoff-schema fallback applied to all four maintenance surfaces and any supported fence rather than the known global schema block; and unique model-facing UDL/template/REPO safety checks in `tools/validators/devkit-contract.ts` still consumed raw Markdown. Attempt 5's correction wave was consumed by SDET, so the attempt closes with `Change-Ready: no`; final review and delivery were not run.
- Main source diagnosis confirmed both findings. Attempt 6 uses existing `tools/validators/routing.ts` and `tools/validators/devkit-contract.ts` only: routing must accept raw fallback solely from one exact ` ```text ` schema under exact H2 `Universal Task Briefing Contract` in `global/AGENTS.md`, with the complete ordered field shape; all other surfaces and arbitrary/relocated fences remain operative-only. Devkit's model-facing UDL, project-template, and REPO checks must use the shared structured operative scan and explicitly reject unsupported syntax; non-model-facing package/catalog/config checks remain unchanged. No instruction, outcome, protected boundary, dependency, artifact, parser, or external operation changes. Attempt 6 receives one correction wave, fresh proof/SDET/validation, one final review, and Material delivery.
- Attempt-6 initial production Candidate Reference `6555f389f970dbf7c3a27b15ad024886157ccfa9`; only `tools/validators/routing.ts` and `tools/validators/devkit-contract.ts` changed after attempt 5. Exact schema extraction is global-only, exact-H2, one ` ```text ` block, and exact 18-label order/cardinality; UDL/template/REPO model-facing checks use structured operative text. Inventory remained below baseline and strict validation passed. Fresh proof `ses_07f1a9109ffeLIz9RhG1KHE8CN`, model `openai/gpt-5.5`, passed all six routes/all nine fields with one text event, no tool/question/mutation/external action, and an empty fixture.
- Fresh attempt-6 SDET task/session `ses_07f196812ffe5o0jrE9kW57zzT`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-library/validator-change-ready.ts`. Five aggregate tests with 20 isolated subprocess scenarios cover exact schema positive; relocated, partial, reordered, duplicate, wrong-info, duplicate-H2/block, moved, and non-global negatives; unique devkit UDL/template/REPO supported-fence-only markers; and unsupported-before-devkit checks. No mock, new artifact, helper, or production mutation; `Blocking Evidence: none`.
- Parent replay passed contracts 84/84 and library 311/311. Complete post-SDET Candidate Reference `01474835496fcd8abfbc14ef0f9903e9d762ef2d`. Fresh post-SDET proof `ses_07f0edfd1ffeCDmQs5LOtw0ASu`, model `openai/gpt-5.5`, repeated all six routes/all nine fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt6-post-sdet-20260720` fixture. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and runtime corpus 30,738.
- Complete attempt-6 validation passed on that candidate: strict exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; full `npm test` exited `0` with library 311, contracts 84, and every remaining suite passing; OpenSpec validation exited `0` with 9 passed and 0 failed. The first `npm test` invocation produced no semantic result because the command harness reported `Unknown: ChildProcess.kill` at 120 seconds; process inspection confirmed no live workspace validation process, and one unchanged-candidate rerun at a 300-second timeout passed. This is an attributed evidence-environment correction, not retry-until-green. Only unchanged informational top-level-allow and Node module-type notices remain.
- Fresh attempt-6 instruction-artifact review task/session `ses_07f080c40ffe91VozIfC4eF9EV` returned `material tuning needed`, `Blocking for acceptance: yes`, and two high-confidence P1 findings. The new exact-schema extractor still accepts heading whitespace, delimiter indentation/length/trailing-whitespace drift, inserted blank body lines, and relocation below an intervening H1. Separately, model-facing role-contract checks in `tools/validators/agents.ts` still consume raw Markdown, so marker-only indented code can certify implementation, SDET, reviewer, troubleshooter, and configured text contracts. Task 6.1 remains open; final and delivery reviews were not run.
- Main source diagnosis maps both defects to AC-5/AC-6 and task 2.4. The malformed schema shapes are concrete candidate-introduced failures in `extractExactNormativeBriefingSchemaBody`. The role-validator gap is newly reachable for this candidate because its outcome-first lifecycle depends on the registered implementation/SDET/reviewer roles while their static authority checks remain the raw-text exception to the candidate's operative-source model. The smallest complete correction uses existing `tools/validators/routing.ts` plus the necessary local reversible dependency `tools/validators/agents.ts`: require the exact H2/fence/body bytes and section ownership for the global briefing schema; parse agent frontmatter separately; derive required and forbidden model-facing instructions from body text outside supported fences and CommonMark indented code; and permit report-only fields solely through one explicit exact `## Output` report-envelope schema per special role. No instruction text, accepted outcome, protected boundary, dependency, artifact, parser framework, semantic classifier, or external operation changes.
- Attempt 6 consumes its single correction wave for that serial production repair and one fresh corrected-candidate SDET in the existing test write set. Source mutation invalidates the current Candidate Reference, proof, SDET, validation, and instruction review evidence. Any later serious failure ends attempt 6; final and delivery reviews remain gated on the corrected candidate.
- Fresh production-author task/session `ses_07efb0998ffehaIDtmvVxKF0gl` changed only `tools/validators/routing.ts` and the necessary local dependency `tools/validators/agents.ts`; no test, instruction, or OpenSpec artifact changed. The global briefing extractor now requires byte-exact H2, section ownership, fence delimiters, and all 18 body lines. Agent validators parse frontmatter separately, reject unsupported fence syntax, remove supported fenced and indented code from model-facing checks, and use explicit per-role exact `## Output` report-envelope exceptions only for named schema fields. Post-production Candidate Reference `dc82b461b6c652f05bf859207ad5ed675d1fb220`; inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and runtime corpus 30,738; strict validation passed with zero warnings.
- Corrected attempt-6 Applicable Proof session `ses_07ef51607ffee8ZHy1J8pAQpIE`, model `openai/gpt-5.5`, repeated all six AC-6 routes and all nine protected-boundary handoff fields. Event order was `step_start` -> one text response -> `step_finish`; no tool, question, mutation, agent, credential, activation, remote, destructive, or external-operation event occurred. The isolated `outcome-first-proof-attempt6-corrected-20260720` fixture remained empty.
- Fresh corrected-candidate SDET task/session `ses_07ef3a660ffeAatjSZXC1gZjsR`, effective model `openai/gpt-5.6-sol`, returned action `blocked` after changing only `tools/test-library/validator-change-ready.ts`. Its real `validate-library` subprocess reproducer proves `routing.ts` still treats `Actionable Continuation Items` in a four-space CommonMark code block in `sdet-quality-engineer.md` as operative because the role-file path uses fence-only `readOperativeAuthorityText` rather than the frontmatter/fence/indent boundary added in `agents.ts`. Candidate Reference after the SDET reproducer is `07b62013ed81670c24e03e9c0ff76277c5690734`.
- Main parent replay confirmed two library failures. The SDET reproducer emits both the role-specific and production-routing forbidden-action diagnostics for the indented marker. The earlier closed/unclosed role-example control also now fails because it appends a second fence without a new H2 after `implementation-worker.md`'s final `## Output`, so the stricter exact report parser correctly sees two fences in the report-owned section; that fixture needs an explicit separate example heading. These are respectively a production defect and a test-oracle fixture defect. Attempt 6 has exhausted its correction wave and is closed with `Change-Ready: no`; no final or delivery review runs for this candidate.
- Attempt 7 continues the unfinished root goal without changing the accepted outcome or protected boundaries. The smallest production dependency closure is existing `tools/validators/active-authority.ts`, `tools/validators/agents.ts`, and `tools/validators/routing.ts`: expose one shared deterministic model-facing Markdown body scan that strips an exact leading YAML frontmatter block when present, rejects unsupported fence syntax, removes supported fences, and removes tab/four-space indented code; use it in both agent contract checks and routing's role-file forbidden checks while leaving general authority surfaces unchanged. A new fresh SDET may change only existing frozen tests, including moving the unrelated fenced-example control under its own exact H2 and completing the deferred exact-schema/report/role matrix. Attempt 7 receives one correction wave, fresh proof/SDET/validation, one final review, and one Material delivery review.
- Fresh attempt-7 production-author task/session `ses_07eecadc6ffeUAngL6TGE8nB9U` changed only `tools/validators/active-authority.ts`, `tools/validators/agents.ts`, and `tools/validators/routing.ts`. The shared `scanModelFacingMarkdownBody` returns post-frontmatter raw body, operative body without supported fences/indented code, and original-file unsupported-fence line; direct agent checks and routing role-file forbidden scans use it while non-agent authority retains fence-only semantics. Main inspection then found the same role-agent boundary still missing from continuation-positive and count-based routing readers. Attempt 7's correction wave resumed the same author/session and changed only `routing.ts`, routing every role-capable delta, continuation, global-Markdown count, and forbidden scan through the model-facing selector while skills/global/project/UDL/templates/leaf contracts remain general.
- Post-correction strict validation passed with zero warnings. Attempt-7 pre-SDET Candidate Reference `2c6ee65b60b01d29eed99ef8a43755fb2ae834e9`. Fresh Applicable Proof session `ses_07ee31af4ffe21cMyUEFnX7BBo`, model `openai/gpt-5.5`, passed all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt7-20260720` fixture.
- Fresh attempt-7 SDET task/session `ses_07ee1b521ffen1KeadzkYnqCWP`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-library/validator-change-ready.ts`, and reported no Blocking Evidence. It placed the arbitrary closed/unclosed role examples under a separate H2, preserved the indented forbidden-action reproducer, and added exact H2/H1/fence/body schema variants plus direct role behavior, continuation, count polarity, report-envelope boundary, reusable-reviewer, configured text-contract, frontmatter, and exact four-report controls. No mock, new helper/artifact, production, instruction, or OpenSpec mutation occurred.
- Parent replay passed contracts 84/84 and library 319/319. Complete post-SDET Candidate Reference `4cc747ca7dece8307193332a43dd5214abe7dbd3`. Fresh post-SDET proof session `ses_07ed83578ffeOOWd9Of0D024Mn`, model `openai/gpt-5.5`, repeated all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt7-post-sdet-20260720` fixture. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and runtime corpus 30,738.
- Complete attempt-7 validation passed on that same candidate: strict exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; full `npm test` exited `0` with library 319, contracts 84, and every remaining repository suite passing; OpenSpec validation exited `0` with 9 passed and 0 failed. Only the unchanged informational top-level-allow and non-failing Node module-type performance notices remain.
- Fresh attempt-7 instruction-artifact review task/session `ses_07ed47dbeffeNyoDDW9TN1SqRV` returned `material findings`, high confidence, `Blocking for acceptance: yes`, and two P1 findings. The exact briefing/report section boundaries use `/^#{1,2} /`, so a valid CommonMark owner heading with one-to-three leading spaces or a tab after `#` does not end the prior section. Separately, model-facing indent filtering recognizes only a tab in column zero or four literal spaces, leaving one-to-three spaces followed by a tab operative even though tab expansion reaches the four-column code boundary. The reviewer also reported duplicate unsupported-syntax diagnostics as a nonblocking P2. Attempt 7's correction wave is consumed, so the attempt closes with `Change-Ready: no`; final and delivery reviews were not run.
- Main source diagnosis confirmed both P1 control-flow paths and leaves the P2 as residual rather than expanding current scope. Attempt 8 reuses the same production ownership in `tools/validators/active-authority.ts`, `tools/validators/agents.ts`, and `tools/validators/routing.ts`: one deterministic H1/H2 owner-boundary helper must recognize 0-3 leading ASCII spaces plus one/two hashes followed by space or tab while target headings remain byte-exact; the model-facing indentation helper must calculate leading Markdown columns with tabs advancing to the next four-column stop and exclude any line reaching column four. No general parser, instruction, contract, dependency, artifact, diagnostic-dedup mechanism, or external operation change. A fresh SDET may extend only existing frozen tests with indented/tabbed H1/H2 and one-to-three-spaces-plus-tab cases. Attempt 8 receives one correction wave, fresh proof/SDET/validation, one final review, and one Material delivery review.
- Attempt-8 production resumed the same author/session `ses_07eecadc6ffeUAngL6TGE8nB9U` and changed only `tools/validators/active-authority.ts`, `tools/validators/agents.ts`, and `tools/validators/routing.ts`. Shared `isAtxH1OrH2BoundaryLine` now recognizes 0-3 leading spaces, H1/H2 only, and space/tab/EOL after the hash run without relaxing exact target headings. Leading indentation now counts Markdown columns with four-column tab stops, so tab and one-to-three spaces plus tab are non-operative while one-to-three spaces before ordinary text remain operative. Strict validation passed with zero warnings. Pre-SDET Candidate Reference `178c7052fae87579f4c801d5c51936da95630b0b`.
- Fresh attempt-8 Applicable Proof session `ses_07ec61ab6ffe3JnwIzTo5S6qCk`, model `openai/gpt-5.5`, passed all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt8-20260720` fixture.
- Fresh attempt-8 SDET task/session `ses_07ec4cc37ffeBrJDmPpoXbRNKR`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-library/validator-change-ready.ts`, and reported no Blocking Evidence. It added real-validator cases for 0-3-space and tab-separated H1/H2 briefing/report ownership, one-to-three-spaces-plus-tab direct behavior and routing continuation, and one-to-three-space ordinary operative prose controls; all prior exact-schema/report/role matrices remain.
- Parent replay passed contracts 84/84 and library 321/321. Complete post-SDET Candidate Reference `a6393c492ac7885df1369dc3562ac2bfcff97e53`. Fresh post-SDET proof session `ses_07ebe3448ffe5zU84mwq7ykf35`, model `openai/gpt-5.5`, repeated all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt8-post-sdet-20260720` fixture. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and runtime corpus 30,738.
- Complete attempt-8 validation passed on that same candidate: strict exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; full `npm test` exited `0` with library 321, contracts 84, and every remaining repository suite passing; OpenSpec validation exited `0` with 9 passed and 0 failed. Only unchanged informational top-level-allow and non-failing Node module-type notices remain.
- Fresh attempt-8 instruction-artifact review task/session `ses_07eba5d51ffeIgwJK8NvsdFVRZ` returned `material findings`, high confidence, `Blocking for acceptance: yes`, and one P1. The doctor-facing `hasExactH2` still accepts heading whitespace/tab drift and `sectionBodyAfterExactH2` ends only on an unindented space-separated H2, so copied AGENTS authority can attribute routing/briefing/autonomy/reviewer markers across a valid H1 or indented/tab-separated H2 owner boundary. This is the same AC-5/AC-6 exact-ownership class in the remaining `active-authority.ts` consumer; prior attempt-8 briefing/report tests do not reach the pure copied-authority/doctor path.
- Attempt 8 consumes its single correction wave by resuming the same production author in existing `tools/validators/active-authority.ts`: target H2 lookup must use byte equality and `sectionBodyAfterExactH2` must use shared `isAtxH1OrH2BoundaryLine` for termination. A new fresh corrected-candidate SDET may change only existing frozen tests to cover H1, indented/tab-separated H2, and target-heading whitespace drift through pure authority and real doctor boundaries. No parser, instruction, contract, dependency, artifact, diagnostic-dedup, or external operation change. Any later serious failure ends attempt 8.
- Attempt-8 production correction resumed `ses_07eecadc6ffeUAngL6TGE8nB9U` and changed only `tools/validators/active-authority.ts`: all four copied-AGENTS structural target H2s now use byte equality, and section bodies end on shared supported H1/H2 ownership boundaries. The obsolete local regex escape helper was removed. Strict validation passed. Corrected pre-SDET Candidate Reference `65f0ccfbaa2d7342c77e415b15c4d06dd459de63`.
- Corrected Applicable Proof session `ses_07eadfc56ffeA5w7sh1BIunn1v`, model `openai/gpt-5.5`, passed all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt8-corrected-20260720` fixture.
- Fresh corrected-candidate SDET task/session `ses_07eacc90fffe2ry7NxIoYskyIu`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-contracts-change-ready-delivery.ts` and `tools/test-library/doctor.ts`, and reported no Blocking Evidence. Pure `agentsAuthorityProblem` matrices now cover target tab/repeated/leading/trailing/closing-hash drift, four routing owner boundaries, a second section caller, and H3/four-space non-boundary controls; isolated doctor cases require blocked qualification and exact privacy-safe details for target drift and cross-section marker ownership.
- Parent replay passed contracts 86/86 and library 322/322. Complete corrected post-SDET Candidate Reference `2a70b6a6207e68ca847a7cee06d0cdd2b06df23f`. Fresh post-SDET proof session `ses_07ea7abc3ffeKkNWSoz3cewxxj`, model `openai/gpt-5.5`, repeated all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt8-corrected-post-sdet-20260720` fixture. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and runtime corpus 30,738.
- Complete corrected attempt-8 validation passed on that same candidate: strict exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; full `npm test` exited `0` with library 322, contracts 86, and every remaining repository suite passing; OpenSpec validation exited `0` with 9 passed and 0 failed. Only unchanged informational top-level-allow and non-failing Node module-type notices remain.
- Fresh corrected attempt-8 instruction-artifact review task/session `ses_07ea3f29bffeVDsEHLcj7ZV9aR` returned `clean`, high confidence, `Blocking for acceptance: no`, no P0/P1, no Blocking Evidence, and task-6.1 pass condition met. It independently traced all ACs, 15 production/six test paths, every prior fence/schema/role/doctor defect class, current SDET/proof/validation evidence, and non-growth baselines. Nonblocking residuals are one missing bare-H1/H2 EOL regression despite correct current helper behavior, duplicate unsupported-fence diagnostic noise, and unavailable raw transcript/stdout recomputation.
- Fresh final-candidate review task/session `ses_07e9ca7caffedoleky35bzM3lO` returned `blocked`, high confidence, and Blocking Evidence because the complete final corrected-candidate `SDET_QUALITY_REPORT` was referenced only by session id and summarized rather than directly readable in the review package. It found no separate current source/test P0/P1 and treated bare-H1/H2 EOL coverage, duplicate diagnostics, transcript recomputation, and unknown model independence as residuals. Attempt 8 is terminal with `Change-Ready: no`; delivery was not run.
- Attempt 9 continues the unfinished root goal with a handoff-package-only correction. Production/test Candidate Reference remains `2a70b6a6207e68ca847a7cee06d0cdd2b06df23f`; no production, test, instruction, contract, proof, SDET, or validation mutation/replay is authorized or required. Main renders the already terminal session `ses_07eacc90fffe2ry7NxIoYskyIu` report below as a complete privacy-safe English evidence body without semantic additions. Prior clean instruction review and same-candidate proof/SDET/validation remain applicable. Attempt 9 receives one fresh final review and, if accepted, one Material delivery review.

#### Directly Readable Final Corrected-Candidate SDET Report

The following is the complete privacy-safe English rendering of the terminal report returned by fresh corrected-candidate SDET session `ses_07eacc90fffe2ry7NxIoYskyIu`. The report's Candidate Reference is intentionally the pre-test reference and was invalidated by the reported test mutation; main later recaptured the complete candidate as `2a70b6a6207e68ca847a7cee06d0cdd2b06df23f` and ran every requested procedure.

```text
SDET_QUALITY_REPORT
Action: authored-tests
Status: done
SDET Identity: Fresh corrected-candidate SDET for attempt 8; test-only; no production or prior attempt-8 test authorship
Candidate Reference: 65f0ccfbaa2d7342c77e415b15c4d06dd459de63 (pre-SDET; invalidated by test changes)
Effective Model: openai/gpt-5.6-sol
Model Independence: unknown

Summary
- Added independent pure-boundary and real isolated-doctor checks for exact headings and H2 section ownership. Static inspection found no production defect.

Risk And Oracle Matrix
- AC-5/AC-6, byte-exact target | tab separator, repeated separator, leading space, trailing space, closing hash | exact `AGENTS.md missing exact heading ## Change-Ready SDLC Routing` error | pure `agentsAuthorityProblem` matrix | `node tools/test-contracts.ts` | real pure function | mock: none.
- AC-5/AC-6, section ownership | markers below `# Other`, ` # Other`, `  ## Other`, or `##\tOther` | exact empty-routing-section error | pure ownership matrix | parent contract suite | real shared H1/H2 boundary | mock: none.
- Shared-helper invariant | Autonomous Work Contract markers below an indented H2 | exact empty-autonomous-section error | pure second-caller case | parent contract suite | real second caller | mock: none.
- Boundary non-regression | H3 and four-space heading-like line inside routing | copied authority remains conforming | positive pure control | parent contract suite | real pure function | mock: none.
- Doctor qualification boundary | tab-drifted target and markers below an indented H2 | command exit 0; `status: warn`; `qualificationStatus: blocked`; authority `warn`; `blocksQualification: true`; exact privacy-safe detail | isolated-doctor subprocess matrix | `node tools/test-library.ts` | temporary filesystem and real doctor subprocess | mock: none.
- Positive authority | conforming copied authority and current root | existing positive controls remain accepted | existing contract/doctor tests | both parent suites | real boundaries | mock: none.

Test Changes Or Existing Evidence
- `tools/test-contracts-change-ready-delivery.ts`: added five target-drift cases, four routing ownership boundaries, one second-required-section caller, and H3/four-space positive control.
- `tools/test-library/doctor.ts`: added two isolated-doctor cases with exact machine-readable oracles.
- Preserved existing positive controls: `contracts: supported top-level fences and nested delimiter examples preserve active authority` and `doctor accepts complete copied override authority without source or template equality`.

Requested Validation Procedures
- Inspect the diff: only the two named test files changed and no artifact was added; then recapture Candidate Reference.
- Run `node tools/test-contracts.ts`; require exit 0 including the two new contract tests, expected 86/86.
- Run `node tools/test-library.ts`; require exit 0 including the new doctor test, expected 322/322.
- Repeat corrected applicable proof for all six AC-6 routes; require one text event, no tool/question/mutation/external action, and an empty fixture.
- Run `npm run instruction:inventory -- --format json`, `npm run validate:strict`, full `npm test`, and `npm run openspec:validate`; require every mandatory command to exit 0 without new warnings or failures.
- Run fresh instruction, final-candidate, and Material delivery reviews against one recaptured Candidate Reference.

Blockers
- none

Blocking Evidence
- none

Residual Risks
- SDET did not run tests because shell was explicitly forbidden; executable evidence belongs to main.
- Doctor uses representative tab-drift and indented-H2 cases; the complete shape matrix is covered at the pure boundary, so an exhaustive cross-product over all four section callers was not added.
- Production-author effective model was not provided; model independence and same-model correlation risk remain unknown.
- The report's Candidate Reference became invalid after test-only mutation.

Follow-up Candidates
- none
END_SDET_QUALITY_REPORT
```
- Fresh attempt-9 final-candidate review task/session `ses_07e9674d6ffehuGMUZdpGf9NOA` accepted the directly readable SDET report and package continuity but returned `rejected`, high confidence, with one P1. `agentsAuthorityProblem` checks only existence and first-body ownership for the four required exact copied-AGENTS H2 sections; a second exact `## Change-Ready SDLC Routing` or other required section with contradictory active instructions can remain uninspected while doctor reports conforming authority. Attempt 9 is terminal with `Change-Ready: no`; delivery was not run. Bare-H1/H2 EOL test coverage, duplicate diagnostics, compact proof provenance, and unknown model independence remain nonblocking residuals.
- Attempt 10 continues the unfinished root goal through the smallest local reversible production repair in existing `tools/validators/active-authority.ts`: each of `Change-Ready SDLC Routing`, `Universal Task Briefing Contract`, `Autonomous Work Contract`, and `Shared Reviewer Runtime Invariants` must occur exactly once as a byte-exact unfenced H2 before its body is trusted. Duplicate exact targets require a deterministic section-specific diagnostic; current target/body/boundary semantics remain unchanged. A fresh SDET may modify only existing frozen tests for pure four-section cardinality and representative isolated-doctor blocking. No instruction, contract, parser, dependency, artifact, evidence-package, P2 cleanup, or external operation change. Attempt 10 receives one correction wave, fresh proof/SDET/validation, one instruction review, one final review, and one Material delivery review.
- Attempt-10 production resumed author/session `ses_07eecadc6ffeUAngL6TGE8nB9U` and changed only `tools/validators/active-authority.ts`. Pure `countExactH2` counts byte-exact unfenced targets; `exactMandatoryH2CardinalityProblem` preserves missing diagnostics and rejects duplicates before any of the four section bodies are trusted. Strict validation passed. Pre-SDET Candidate Reference `021e485631d9bfc337cc9b4eeb9fc48a05ad618a`.
- Fresh attempt-10 Applicable Proof session `ses_07e8f17b5ffep9IF9iRnTLMMBp`, model `openai/gpt-5.5`, passed all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt10-20260720` fixture.
- Fresh attempt-10 SDET task/session `ses_07e8df19fffePTGuc8zYk9jDXU`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-contracts-change-ready-delivery.ts` and `tools/test-library/doctor.ts`, and reported no Blocking Evidence. Pure tests cover all four duplicate exact titles plus fenced/non-exact controls; isolated doctor cases cover routing and reviewer duplicates with exact blocked/privacy-safe outcomes. Its complete directly readable report follows below.
- Parent replay passed contracts 88/88 and library 322/322. Complete post-SDET Candidate Reference `61948f2cb12c8d29fc68e88d88d8ab1439da4843`. Fresh post-SDET proof session `ses_07e88d8a1ffepH65D9gCqlvBYI`, model `openai/gpt-5.5`, repeated all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty `outcome-first-proof-attempt10-post-sdet-20260720` fixture. Inventory remained 55 artifacts, total 91,223, global AGENTS 11,663, and runtime corpus 30,738.
- Complete attempt-10 validation passed on that same candidate: strict exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`; full `npm test` exited `0` with library 322, contracts 88, and every remaining repository suite passing; OpenSpec validation exited `0` with 9 passed and 0 failed. Only unchanged informational top-level-allow and non-failing Node module-type notices remain.

#### Directly Readable Attempt-10 SDET Report

```text
SDET_QUALITY_REPORT
Action: authored-tests
Status: done
SDET Identity: fresh attempt-10 SDET; did not author production
Candidate Reference: 021e485631d9bfc337cc9b4eeb9fc48a05ad618a (pre-SDET; invalidated by test-only mutation)
Effective Model: openai/gpt-5.6-sol
Model Independence: unknown

Summary
- Added focused pure-function and real isolated-doctor regression evidence for exact cardinality-one enforcement across the four mandatory copied-AGENTS H2 sections. No production files were edited. Execution remains with the main session.

Risk And Oracle Matrix
- AC-5/AC-6 exact cardinality | append a second unfenced exact H2 after a conforming body for each mandatory title | exact section-specific `AGENTS.md duplicate exact heading ## <title>` | four-title pure matrix | run parent contracts | real `agentsAuthorityProblem` boundary | mock exception: none.
- Fenced examples are non-operative | repeated exact headings inside supported closed and unclosed fences beside one valid outside target | authority remains accepted | four-title positive matrix | run parent contracts | real fence scanner and pure authority boundary | mock exception: none.
- Byte-exact matching | append representative tab-separated and closing-hash lookalikes | authority remains accepted; existing replacement matrix continues requiring missing-heading diagnostics | positive control plus existing target-drift test | run parent contracts | real pure boundary | mock exception: none.
- Doctor qualification mapping | duplicate routing and Shared Reviewer exact headings with private body sentinels | exit 0; report `warn`; qualification `blocked`; authority `warn`; `blocksQualification: true`; exact privacy-safe detail; sentinel absent | isolated-doctor cases | run parent library suite | temporary filesystem and real doctor subprocess | mock exception: none.
- Non-regression | missing headings, section ownership, current canonical root, and conforming copied authority | existing exact diagnostics and accepted states remain unchanged | preserved existing contract and doctor tests | run both parent suites | real pure/subprocess boundaries | mock exception: none.

Test Changes Or Existing Evidence
- `tools/test-contracts-change-ready-delivery.ts`: added the four-title duplicate matrix, closed/unclosed fenced-copy controls, and representative non-exact-copy controls.
- `tools/test-library/doctor.ts`: added routing and Shared Reviewer duplicate cases to the real isolated-doctor matrix, including exact report-state and privacy oracles.
- Preserved existing tests: `contracts: copied AGENTS target headings are byte-exact`, `contracts: copied AGENTS markers remain owned by their exact H2 sections`, `contracts: supported top-level fences and nested delimiter examples preserve active authority`, and `doctor accepts complete copied override authority without source or template equality`.

Requested Validation Procedures
- Inspect attribution and confirm this SDET changed only the two named test files; recapture Candidate Reference.
- Run `node tools/test-contracts.ts`; require exit 0, both new named tests to pass, and no regression in existing tests. Expected count is 88/88 absent other changes.
- Run `node tools/test-library.ts`; require exit 0 and the renamed duplicate-cardinality doctor test to pass all four internal cases. Expected count remains 322/322 absent other changes.
- Confirm regression sensitivity against the prior existential implementation: duplicate pure fixtures would return `null`, and duplicate doctor fixtures would not block qualification.
- Replay the six-route applicable proof and require one text event, no tool/question/mutation/external action, and an empty fixture.
- Run `npm run instruction:inventory -- --format json`, `npm run validate:strict`, full `npm test`, and `npm run openspec:validate`; require exit 0 with no new failures or warnings.
- Run fresh instruction, final-candidate, and Material delivery reviews against the recaptured candidate.

Blockers
- none

Blocking Evidence
- none

Residual Risks
- Tests were not executed because shell use was explicitly forbidden; executable outcomes remain pending main-session validation.
- Doctor coverage is representative for routing and Shared Reviewer; all four titles are covered at the pure boundary.
- Bare-H1/H2 EOL coverage remains explicitly out of scope.
- Production-author effective model was not supplied, so same-model correlation risk is unknown.
- The supplied Candidate Reference no longer identifies the complete candidate after test mutation.

Follow-up Candidates
- none
END_SDET_QUALITY_REPORT
```
- Fresh attempt-10 instruction-artifact review task/session `ses_07e8500e9ffe6YhaPu97gk1Kee` returned `material findings`, high confidence, and one P1 Blocking Evidence item. `tools/test-contracts-change-ready.ts` uses fence-only `operativeTextOutsideFences` for installed skill/agent fresh-testing classification, so a future role route marker moved only into four-column or mixed-tab indented code could still satisfy that regression classifier even though current runtime role validation correctly treats it as non-operative. The reviewer confirmed the attempt-10 four-section duplicate-cardinality repair itself is correct and found no current instruction-text, runtime-role, protected-boundary, or doctor P0/P1.
- On 2026-07-21 the owner explicitly stopped further correction/review polishing and prioritized the working result. The remaining item is retained as accepted deferrable regression-test debt: it affects future drift detection, not the current loaded role text or current runtime behavior. No additional production/test mutation, SDET, instruction review, final review, or delivery review is run. This owner stop does not convert the rejected qualification gate into acceptance: task 6.1 remains open, tasks 6.2/6.3 remain unrun, and `Change-Ready: no`.
- On 2026-07-21 the owner explicitly resumed the pending OpenSpec tasks. Attempt 11 keeps `AC-1` through `AC-6`, the accepted outcome, protected boundaries, production/test path ceilings, mandatory gates, and external-operation prohibitions unchanged. The reproduced task-6.1 blocker is test-only: an installed role marker moved to CommonMark indented code remains visible to `operativeTextOutsideFences` while `scanModelFacingMarkdownBody` correctly removes it. Production dispatch is `N/A`; one fresh SDET may change only existing `tools/test-contracts-change-ready.ts` so role-agent classification uses the shared model-facing scan while skill classification retains general authority semantics. Attempt 11 has one correction wave, fresh applicable proof and complete validation, one instruction review, one final review, and one Material delivery review. Installation, activation, reload, commit, push, merge, release, archive, credentials, elevation, destructive action, and remote mutation remain forbidden.
- Attempt-11 fresh SDET task/session `ses_07c3a72f7ffeOMdZXkASWI2CDM`, effective model `openai/gpt-5.6-sol`, changed only `tools/test-contracts-change-ready.ts`: installed role-agent classification now uses `scanModelFacingMarkdownBody`, skill classification retains `operativeTextOutsideFences`, and seeded four-space/mixed-tab role-marker cases reproduce and close the prior fence-only gap. Parent contracts preserved one attributable SDET-oracle failure because the existing `implementation-worker` class marker `Production-only` lives only in stripped YAML frontmatter. Attempt 11 consumes its correction wave for one new fresh corrected-candidate SDET changing only that marker oracle to an existing operative body marker. The same parent run also exposed four pre-existing overlapping-work failures outside this SDET edit; they remain baseline-attributed pending reconciliation and do not authorize mutation of instruction, skill, or neighboring test scope.
- Fresh corrected-candidate SDET task/session `ses_07c364d7cffeOD49ikXIRJWIcz`, effective model `openai/gpt-5.6-sol`, changed only the one `implementation-worker` class-marker literal to the operative body marker `bounded production implementation worker`; no scanner, seeded case, production, instruction, or OpenSpec content changed. The second parent replay removed that exact failure but exposed the next frontmatter-dependent role-table marker, `Fresh-context test-only SDET`, plus the same four overlapping-work failures: three current root-stop-budget/continuation exact-text mismatches and strict validation rejecting portable-hardcode token `gh ` in the current skill text. This is a second serious failure after attempt 11's correction wave, so attempt 11 closes with `Change-Ready: no`; no proof, instruction review, final review, or delivery review runs on the red candidate. The prior green Candidate Reference is stale, current root stop-budget used/remaining is unknown because the overlapping policy mutation is not recorded in this change, and all current workspace changes remain preserved without revert or external operation.
- The owner selected integrated reconciliation of the current combined candidate. Attempt 12 preserves `AC-1` through `AC-6`, current accepted outcome/protected boundaries, all current instruction semantics, no-new-artifact limits, and every external-operation prohibition. Root-cause diagnosis is complete: `tools/validators/skills.ts` performs raw substring matching for command-like portable-hardcode tokens, so `gh ` falsely matches the suffix of ordinary word `through `; `tools/test-contracts-change-ready.ts` retains one frontmatter-only SDET role marker and three exact literals superseded by the current semantically equivalent root-stop wording. Attempt-12 production scope is only existing `tools/validators/skills.ts`, using boundary-safe deterministic matching without weakening standalone command/tool-name rejection. Fresh SDET scope is only existing `tools/test-contracts-change-ready.ts` and `tools/test-library/validator-change-ready.ts`, covering the current model-facing SDET body marker, compact accepted root-stop phrases, and a real strict-validator negative/positive boundary for `through ` versus standalone `gh `. No instruction, policy, outcome, dependency, parser framework, profile, or artifact change is authorized. The current post-green cycle remains open with root budget used `1/2` and remaining `1`; attempt 12 receives one correction wave, fresh proof/SDET/validation, one instruction review, one final review, and one Material delivery review.
- Attempt-12 production author task/session `ses_07c313567ffe3aXWm1xeaDjS6m` changed only `tools/validators/skills.ts`. The deterministic matcher now requires a left ASCII word boundary for word-led tokens, a right boundary for letter/digit-ended exact tokens, preserves punctuation literals, and keeps trailing-underscore prefix semantics so `dream_team_` still rejects concrete `dream_team_*` names. No test, instruction, contract, OpenSpec, dependency, or new artifact changed. Applicable Proof `npm run validate:strict` exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`, closing the current-skill `through `/`gh ` false positive at the real validator boundary. Pre-SDET tracked Candidate Reference: `c5e785ae3e0bbfd197131041043e26c72ad516da`.
- Fresh attempt-12 SDET task/session `ses_07c2dfed8ffeHOBeFC6XT3kNhb`, effective model `openai/gpt-5.6-sol`, action `authored-tests`, changed only `tools/test-contracts-change-ready.ts` and `tools/test-library/validator-change-ready.ts`. It aligned the SDET role marker with operative model-facing body text, retained the 18/10/3 catalog and seeded indentation guards, aligned three root-stop assertions to the accepted current phrases, and added a real validator subprocess matrix for `through `/`gh `, `dream_team_`, `Windows`, and `.github/` boundaries. No production, instruction, contract, OpenSpec, dependency, helper framework, or new artifact changed; `Blocking Evidence: none`; production/SDET model independence remains unknown.
- Parent replay passed `node tools/test-contracts.ts` 89/89 and `node tools/test-library.ts` 323/323. Complete tracked Candidate Reference: `17b4251a531bbd95384f5909199282372d503bf8`; post-validation recapture is identical and `git diff --check` is clean.
- Fresh post-SDET Applicable Proof session `ses_07c25b6ccffe472xyaoYmvmDEf`, model `openai/gpt-5.5`, JSON output, candidate global config, and isolated fixture `outcome-first-proof-attempt12-post-sdet-20260721` passed all six AC-6 routes. Event order was one `step_start`, one text decision, and one `step_finish`; no tool, question, mutation, agent, credential, activation, remote, destructive, implementation, or external-operation event occurred; fixture readback remained empty. The proof preserved failed evidence, forbade unchanged retry, separated candidate readiness from root-goal completion, batched deferrable debt, placed contained-risk acceptance after proof, stated every protected-boundary handoff field explicitly, and reported the no-progress capability blocker without revision jargon or a fictitious choice.
- Complete attempt-12 validation passed on the same candidate. Inventory reports 55 artifacts, total token proxy 92,176, `global/AGENTS.md` 12,073 <= 12,092, and the eight intended runtime paths 31,691 <= 32,503; no runtime artifact was added. `npm run validate:strict` exited `0` with `skills=24 agents=18 markdown=158 warnings=0 infos=1`. Full `npm test` exited `0`, including library 323 and contracts 89; unchanged Node module-type performance notices remain non-failing. `npm run openspec:validate` exited `0` with 9 passed and 0 failed.

#### Directly Readable Attempt-12 SDET Report

```text
SDET_QUALITY_REPORT
Action: authored-tests
Status: done
SDET Identity: Fresh test-only SDET for make-sdlc-outcome-first attempt 12; did not author production or prior attempt-11 tests
Candidate Reference: c5e785ae3e0bbfd197131041043e26c72ad516da (pre-SDET; invalidated by test mutation)
Effective Model: openai/gpt-5.6-sol
Model Independence: unknown

Summary
- Reconciled model-facing role and root-stop oracles with current operative policy.
- Added a real-validator subprocess matrix for portable-hardcode boundaries.
- No tests ran in the SDET role; execution remained with main.

Risk And Oracle Matrix
- Model-facing role ownership | SDET marker must come from operativeBody, not YAML/frontmatter or code | installed classification retains cardinalities 18/10/3 and recognizes the body phrase | tools/test-contracts-change-ready.ts | parent contract suite | real shared Markdown scanner and repository files | mock: none.
- Attempt/root continuation | changed candidates require current defect, capacity, and progress; unchanged retries remain forbidden | exact current skill phrases are present | existing correction-routing contract case | parent contract suite | real canonical skill section | mock: none.
- Root stop budget | authority remains non-reset, bounded, and honest about open cycles, replay limits, residual-only work, and stop handling | exact current global and skill markers are present | existing stop-budget contract case | parent contract suite | real global/skill sections | mock: none.
- Blocker/debt routing | blocker classes remain distinct and continuation authority stays in the dedicated budget policy | current autonomy phrases and decision-ready fields are present | existing blocker/debt contract case | parent contract suite | real global autonomy section | mock: none.
- Left token boundary | ordinary through must not match configured gh token | validator subprocess exits successfully | portable-boundary matrix | parent library suite | temporary fixture plus real validate-library subprocess | mock: none.
- Standalone and prefix rejection | standalone gh and concrete dream_team_review remain forbidden | subprocess fails with exact token-and-file diagnostic | portable-boundary matrix | parent library suite | real validator subprocess | mock: none.
- Right token boundary | WindowsService passes while standalone Windows fails with the exact diagnostic | success/failure pair | portable-boundary matrix | parent library suite | real validator subprocess | mock: none.
- Punctuation literal | .github/ remains forbidden | exact token-and-file diagnostic | portable-boundary matrix | parent library suite | real validator subprocess | mock: none.

Test Changes Or Existing Evidence
- Changed tools/test-contracts-change-ready.ts.
- Changed tools/test-library/validator-change-ready.ts.
- Preserved the configured-token-array assertion, classification cardinalities, scanner split, seeded four-space/mixed-tab cases, and existing fence/indent/role tests.
- Added test: validator enforces portable-hardcode token boundaries at the canonical skill subprocess.

Requested Validation Procedures
- Confirm only the two named test paths changed; recapture Candidate Reference.
- Run node tools/test-contracts.ts and node tools/test-library.ts; require exit 0 and reach the updated and new cases.
- Run instruction inventory, strict validation, full npm test, and OpenSpec validation; require exit 0.
- Replay the authorized six-scenario behavioral proof, then run fresh instruction, final-candidate, and Material delivery reviews.

Blockers
- none

Blocking Evidence
- none

Residual Risks
- Tests were not executed by SDET because shell use was forbidden; executable evidence belongs to main and is recorded above.
- The supplied Candidate Reference became stale after test mutation; main recaptured 17b4251a531bbd95384f5909199282372d503bf8.
- Production-author effective model was not supplied; same-model correlation risk cannot be excluded.
- The boundary matrix is representative rather than an exhaustive configured-token cross-product.

Follow-up Candidates
- none
END_SDET_QUALITY_REPORT
```

## 7. Handoff

- [x] 7.1 Main session: produce the local handoff with outcome achieved status, changed artifacts, observable proof, automated validation, review results, contained limitations, deferrable technical debt, protected boundaries, complete rollback notes, `Pilot-Ready`, `Change-Ready`, and `External Operations: not performed`. Pass condition: the user can decide whether to authorize later activation or follow-up debt reduction without reconstructing failures or internal revision history from the session transcript.

### Local Handoff

- Outcome status: implemented and working. Outcome-first authority, protected boundaries, attempt-versus-root continuation, blocker/debt routing, decision-ready handoff, exact schema ownership, model-facing role scanning, and copied-authority doctor guards are present in the repository candidate.
- Candidate Reference: `61948f2cb12c8d29fc68e88d88d8ab1439da4843` for the complete 15-production/six-test tracked diff. The pre-existing `global/AGENTS.md` Codebase Memory section remains separately attributed and preserved.
- Observable proof: fresh post-SDET session `ses_07e88d8a1ffepH65D9gCqlvBYI` passed all six AC-6 routes and protected-boundary fields with one text event, no tool/question/mutation/external action, and an empty fixture.
- Automated evidence: parent contracts 88/88; parent library 322/322; strict validation pass with zero warnings; full `npm test` pass; OpenSpec 9/9; inventory 55 artifacts and 91,223 token proxy, with global AGENTS 11,663 <= 12,092 and runtime corpus 30,738 <= 32,503.
- Review result: the latest instruction review blocks formal qualification only on the future-drift regression-classifier gap described above. No current runtime/outcome/protected-boundary defect remains identified. Final and delivery reviews were intentionally not run after the owner stop.
- Residual debt: one regression classifier should eventually use `scanModelFacingMarkdownBody` for `global/agents/*.md`; bare-H1/H2 EOL has no direct regression case; unsupported-fence diagnostics can repeat; production/SDET model independence remains unknown; raw proof transcripts are represented by privacy-safe summaries.
- Rollback/containment: repository candidate only; no install, activation, reload, deployment, publication, or remote mutation occurred. No runtime rollback is required. Leaving the candidate unactivated preserves the prior active environment.
- Protected boundaries: unchanged and still owner-controlled; no critical-risk waiver was introduced.
- Pilot-Ready: not requested.
- Change-Ready: no.
- External Operations: not performed.

### Archive Handoff (2026-07-21)

- Complete attempt-12 tracked Candidate Reference: `17b4251a531bbd95384f5909199282372d503bf8`.
- Working outcome: implemented and observably exercised. The six-scenario fresh-session proof passed without tool, question, mutation, credential, activation, remote, destructive, or external-operation events; the isolated fixture remained empty.
- Automated validation: contracts 89/89, library 323/323, strict validation with zero warnings, full `npm test` pass, OpenSpec 9/9, and clean `git diff --check`. Inventory remained 55 artifacts; global AGENTS and the eight intended runtime paths remained below their recorded baselines.
- Attempt-12 instruction-artifact review task/session `ses_07c211f44ffeSfwOrruMspYjjs` returned `material findings`, high confidence, `Blocking for acceptance: yes`, and three P1 Blocking Evidence items: current-consequence priority is ambiguous for validator/parser residuals; general/canonical instruction scans can still accept frontmatter or CommonMark indented-code markers; and canonical skill duplicate-heading checks miss valid indented ATX headings. Task 6.1 remains failed; tasks 6.2 and 6.3 were not run.
- Owner stop: on 2026-07-21 the owner explicitly stopped further polishing and authorized archive, commit, and push of the current combined working result. No attempt-13 mutation or review replay is performed. The remaining review findings are preserved as accepted unresolved blockers, not relabeled as passed gates.
- Overall task progress at archive: 15/18 complete. Incomplete: 6.1, 6.2, and 6.3.
- Spec synchronization: skipped at owner-directed stop. The standard archive attempt failed without changing files because the `SDLC-002` MODIFIED delta omits six scenarios present in the current main spec (`post-mutation polish is residual`, `candidate-attributable defect receives one in-scope correction`, `out-of-scope P0 blocks without expanding work`, `unknown does not become implementation authority`, `broad all-tasks request uses the frozen snapshot`, and `owner expansion creates a new revision`). Re-authoring the delta would be additional polishing. The archive therefore preserves delta specs as historical evidence while the main specs remain unsynchronized.
- Rollback/containment: repository candidate only; no installation, activation, reload, deployment, release, credential use, destructive action, or runtime external mutation occurred. Repository rollback must preserve unrelated work and use project-native non-destructive restoration; no runtime rollback is required.
- Pilot-Ready: not requested.
- Change-Ready: no.
- External Operations: archive, local commit, and Git push explicitly authorized; installation, activation, reload, deployment, and release remain not performed and not authorized.
