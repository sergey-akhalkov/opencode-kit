## 1. Baseline And Contract

- [x] 1.1 Capture the current autonomous-question baseline and root cause before mutation. **Current Rung**: installed runtime/source inspection. **Next Real Boundary**: task 3.1 installed autonomous reply. **Blocker/Unblocker**: production protocol lacks an answer matrix; tasks 2.1-2.3 unblock. **Observable Proof**: active OpenCode/plugin/SDK identity, controller/spec/proof branch map, focused suite, and attempted installed prompts show no real autonomous question event while the production branch rejects instead of answering. **Authorization**: standing bounded configured-provider inference. **Safeguards**: disposable sessions, no repository writes by proof roots, no external/destructive/protected action. **Restoration/Cleanup**: disposable sessions exit/delete; retained evidence is privacy-safe. **Expected Immutable Evidence**: invocation, session refs, stdout/stderr, session-delivery event/tool counts, config/plugin identity. **Observed**: `history.md` records OpenCode 1.18.18, plugin/SDK 1.18.15, focused 27/27, rejection source path, owner-only proof gap, and three invalid-route prompts with zero question/tool evidence.

- [x] 1.2 Finalize the correlated question/answer/provenance contract and validate planning readiness. **Current Rung**: executable source and official SDK schema. **Next Real Boundary**: task 2.1 controller integration. **Blocker/Unblocker**: none. **Observable Proof**: strict OpenSpec validation and propose/apply gates agree on exact bounds, owner authority, race semantics, evidence topology, and installed proof boundary. **Authorization**: local documentation only. **Safeguards**: no production or test mutation in this task. **Restoration/Cleanup**: source-only rollback. **Expected Immutable Evidence**: OpenSpec command outputs and readable artifacts. **Dependency-chain stop condition**: do not mutate production if exact answer/provenance ownership remains ambiguous. **Observed**: propose gate passed every available check; `openspec validate make-grind-questions-autonomous --strict` passed; status reports proposal/design/specs/tasks complete.

## 2. Production Candidate And Earliest Proof

- [x] 2.1 Implement bounded question normalization and correlated verdict answer validation in a cohesive internal module, extend state/arbiter request/agent contract, and require the official reply capability at startup. Remove the obsolete reject/corrective-prompt path rather than retaining compatibility code. Do not modify automated test artifacts in this task. **Current Rung**: production source. **Next Real Boundary**: task 2.3 installed OpenCode question tool. **Blocker/Unblocker**: task 1.2 contract. **Observable Proof**: direct production-module replay validates exact single/multi answer matrices and rejects custom-only, unoffered, duplicate, incomplete, oversized, stale, owner, and non-question answers before side effects. **Authorization**: local repository edits. **Safeguards**: exact labels only, bounded payload, no raw question logs, no protected authority. **Restoration/Cleanup**: source-only rollback. **Expected Immutable Evidence**: candidate diff, Bun direct replay output, diagnostics. **Dependency-chain stop condition**: no reply integration until matrix validation is green. **Observed**: `question.ts` owns bounded normalization/cardinality/exact-label validation; `questionAnswers` and exact question payload are correlated through the arbiter protocol; startup requires `question.list/reply/reject`; direct Bun replay rejected all three invalid matrices and accepted only `[["Recommended"]]`; changed production diagnostics are empty and strict validation is green.

- [x] 2.2 Implement race-safe official question reply, persisted non-evicting synthetic provenance, session-delivery reclassification, and privacy-safe status/diagnostics. Human reply, disable, interrupt, revision, deletion, not-found, and duplicate effects must remain terminally safe. Do not modify automated test artifacts in this task. **Current Rung**: local controller integration. **Next Real Boundary**: task 2.3 installed OpenCode process. **Blocker/Unblocker**: task 2.1 validated payload/answer owner. **Observable Proof**: SDK-shaped controller replay shows persist-before-reply, one exact reply, original request correlation, zero reject/corrective prompt, human winner cleanup, disable/stale suppression, and projection of guard answers outside human replies. **Authorization**: local in-memory/disposable state. **Safeguards**: serialized status persistence, final epoch checks, no raw labels in diagnostics, capacity fail-closed. **Restoration/Cleanup**: disposable state only. **Expected Immutable Evidence**: call/event order, state/metadata snapshots, side-effect counts, logs. **Dependency-chain stop condition**: do not call a configured model until local reply/provenance replay is terminal green. **Observed**: `proof:guard-question` exited 0 with answer `Recommended`, one reply, zero reject/root prompt, owner-required preservation, and session projection `answered` with zero human replies. Persisted request/call refs reconstruct real 1.18.18 tool answers; changed controller/status/evidence diagnostics are empty.

- [x] 2.3 Build the maintained installed-runtime runner by extending the existing proof client and proof inventory, then run-observe-correct one disposable grind-enabled root through a real `question.asked`, exact automatic reply, answer observed by the original tool call, downstream completion marker, and terminal completion audit. **Current Rung**: local integration with configured provider. **Next Real Boundary**: terminal root behavior under actual loaded OpenCode entry point. **Blocker/Unblocker**: tasks 2.1-2.2; if the selected primary model refuses the required question event, record invalid-route evidence and switch only to a causally distinct already-configured route. **Observable Proof**: event stream contains asked request/options, one official reply with exact offered label, no human input/reject/corrective prompt, original tool continuation with that label, completion marker, guard terminal state, and cleanup. **Authorization**: standing bounded configured-provider inference. **Safeguards**: disposable root, tool policy denies file/task/bash/external/protected effects, bounded calls, privacy-safe capture. **Restoration/Cleanup**: delete sessions and terminate disposable server in `finally`; cleanup failure is terminal. **Expected Immutable Evidence**: command, candidate/config/OpenCode/plugin/SDK/model identity, raw bounded event stream, exit/stdout/stderr/logs/status, cleanup. **Dependency-chain stop condition**: red/no-question runtime remains `development`; preserve complete bundle before another provider attempt. **Observed**: maintained runner and terminal replay on fresh OpenCode 1.18.18/current source used only `question`, reached one real call, rejected two malformed arbiter answer shapes, recovered on exact `Recommended`, resumed the original tool, emitted the marker, projected one guard `answered` intervention and zero human replies, reached completion `passed`, and deleted root plus one child. The first CLI-agent route is preserved invalid-route evidence; the first runner capture against a stale server source was rejected by its projection oracle.

## 3. Accepted Scope Completion

- [x] 3.1 Complete the unattended-grind scenario matrix through production-owned replay without adding automated tests: owner-required question remains open; multi-question/multi-select succeeds; custom-only/invalid/ambiguous fails closed; human, disable, interrupt, stale, retry, provider failure/recovery, PTY/task wait, duplicate idle, continuation, compaction/restart metadata restoration, provenance capacity, terminal pass, and sibling/default-off isolation preserve their contracts. Update README/proof inventory/current specs evidence and re-run the installed happy path after any production mutation. **Current Rung**: current MVP candidate. **Next Real Boundary**: fresh critical SDET. **Blocker/Unblocker**: task 2.3 runtime proof. **Observable Proof**: one privacy-safe matrix records each scenario, expected/actual state/effect, exact command, status, and artifact; all accepted production lanes green or exact documented non-critical limitation. **Authorization**: local disposable execution and configured inference only. **Safeguards**: no external/destructive/protected operation; owner questions remain open. **Restoration/Cleanup**: disposable sessions/processes removed; evidence retained. **Expected Immutable Evidence**: scenario matrix, proof bundles, docs diff, runtime identities. **Dependency-chain stop condition**: any red happy path or reachable non-deferrable race returns candidate to `development` and is corrected before SDET. **Observed**: `implementation-evidence/scenario-matrix.md` records the green production-owned matrix, fresh OpenCode 1.18.18 installed proof and cleanup, bounded configured-arbiter retry transport, project-native PTY/task/sibling/default-off lanes, and the exact pre-SDET test-only fixture drift.

## 4. Independent Critical SDET

- [x] 4.1 After current runtime proof and accepted-scope completion, dispatch one fresh test-only `sdet-quality-engineer` with the original user goal, contract, candidate reference, installed proof bundle, operating envelope, authority boundaries, and exact test-only write scope. Require `critical-risks-reported | no-critical-risk | blocked`; main independently reproduces every row. **Current Rung**: proven installed candidate. **Next Real Boundary**: SDET-authored critical regression oracles. **Blocker/Unblocker**: task 3.1 complete. **Observable Proof**: terminal SDET action, Effective Model, inspected candidate, critical risk matrix, attributed test-only diff, and main disposition. **Authorization**: local test-only mutation and disposable configured inference. **Safeguards**: SDET cannot edit production or ask user; only reachable critical autonomy/authority/data-integrity incidents. **Restoration/Cleanup**: no live/external effects; writer attempt closed before integration. **Expected Immutable Evidence**: task identity, report, tests, raw failing/passing outputs. **Dependency-chain stop condition**: another fresh SDET only after a main-confirmed critical defect, production correction, and renewed runtime proof; first no-confirmed-critical attempt terminates SDET. **Observed**: fresh task `ses_0058c21fdffeR34pZXpfTRd75s`, Effective Model `xai/grok-4.6`, returned `no-critical-risk`; changed only `tools/test-session-completion-guard.ts`. It synchronized mandatory `questionAnswers: null` fixtures, replaced the deleted correction oracle with an official-reply disable race, and added exact-label/owner/null-answer protocol assertions. Main inspected the diff and independently reran the full focused suite green `28/28`; SDET is terminal for this root.

## 5. Qualification And Handoff

- [x] 5.1 Run all SDET-authored and existing focused guard/session/config/installer/contract tests, full `npm test`, `npm run validate:strict`, `npm run openspec:validate`, strict target validation, proposal/apply gates, installed runtime source/config/agent/capability diagnostics, `git diff --check`, and the current installed autonomous-question proof. Resolve only current critical/non-deferrable defects; park non-critical limitations. **Current Rung**: complete tested candidate. **Next Real Boundary**: local stable handoff. **Blocker/Unblocker**: terminal task 4.1. **Observable Proof**: all applicable checks green, no unchecked accepted tasks, current candidate/proof identity, no known reachable critical/non-deferrable defect. **Authorization**: local validation and bounded configured inference. **Safeguards**: no commit/push/release/install/activation/restart. **Restoration/Cleanup**: all disposable sessions/processes removed. **Expected Immutable Evidence**: commands, exits, summaries, diagnostics, proof bundle, diff/status. **Dependency-chain stop condition**: any production mutation invalidates proof and requires a new monotonic RC candidate after requalification. **Observed**: `implementation-evidence/final-validation.md` records final installed OpenCode 1.18.18 proof with offered-label capture and cleanup, terminal SDET, full `npm test`, guard `28/28`, session-plugin `17/17`, contracts `67/67`, installer `25/25`, validation `3/3`, OpenSpec `14/14`, strict target validation, proposal/apply gates, permission/capability diagnostics, zero strict warnings, and clean `git diff --check`.

- [x] 5.2 Freeze the next monotonic session-completion-guard RC and write the complete stable local handoff with outcome/non-goals, Candidate Reference, runtime environment, proof topology, SDET terminal reason, validation, known non-critical limitations, rollback/disable/restart instructions, and external-operation state. **Current Rung**: validated RC candidate. **Next Real Boundary**: owner-controlled restart of future long-running TUI sessions. **Blocker/Unblocker**: task 5.1 green; restart is not required to complete source handoff because proof uses a fresh actual entry point. **Observable Proof**: `Development-Stage: stable`, `Stable Candidate: RC<n>`, coherent handoff, and explicit statement that source edits do not hot-reload this current chat. **Authorization**: documentation only. **Safeguards**: no activation, restart, deployment, release, commit, or remote mutation. **Restoration/Cleanup**: `/disable-grind` and coherent source rollback documented. **Expected Immutable Evidence**: implementation evidence/handoff and final status. **Observed**: `implementation-evidence/stable-handoff.md` freezes `make-grind-questions-autonomous-RC1`, records outcome/non-goals, environment, proof/evaluator topology, terminal SDET, validation, split-or-justify, residual limits, disable/rollback/restart instructions, and confirms no install/activation/restart/commit/push/release.

## Session-Derived Improvements

- [x] I.1 Extend the installed autonomous-question proof wait beyond the observed arbiter retry window. **Trigger/Evidence**: the current-server runtime runner exceeded its outer 300-second evaluator timeout while two invalid `questionAnswers[0]` attempts retried and the preserved root later reached `passed`; the maintained runner default was only 90 seconds. **Why**: evaluator timeout can orphan otherwise successful proof roots and invite an unsafe duplicate provider attempt. **Prerequisites**: preserved terminal replay inspected; satisfied. **Scope/Non-Goals**: change only the maintained runner's terminal wait and use a matching invocation timeout; no controller retry-policy or scenario expansion. **Implementation**: set `waitForTerminalGuard` to 420 seconds and invoke the runner with a longer process timeout. **Observable Proof**: the next fresh installed run emits its result and `cleanup: complete` before process exit. **Validation**: source diagnostics, proof inventory consistency, and the fresh task 3.1 runtime run. **Owner Blocker**: none. **Observed**: fresh runner invocation with a 600-second process timeout emitted terminal JSON and `cleanup: complete` before exit; no root was orphaned.

- [x] I.2 Give the hidden arbiter one exact autonomous `questionAnswers` shape example without weakening validation. **Trigger/Evidence**: two installed audits failed `Invalid completion verdict field: questionAnswers[0]` before a later exact `[["Recommended"]]` result. **Why**: malformed answer rows delay unattended progress and consume unnecessary hidden-model retries. **Prerequisites**: I.1 wait correction; satisfied. **Scope/Non-Goals**: agent wording and existing deterministic validation only; no schema, authority, model, or retry change. **Implementation**: state that one single-select `Recommended` answer is `"questionAnswers":[["Recommended"]]` and reject object rows. **Observable Proof**: current validator accepts the instruction artifact and the next installed question audit returns a schema-valid exact matrix without shape recovery, or records any remaining retry as evidence rather than relaxing validation. **Validation**: `npm run validate:strict` and fresh installed runtime proof. **Owner Blocker**: none. **Observed**: strict validation passed and the next fresh installed question audit accepted exact `[["Recommended"]]` on its first arbiter result.
