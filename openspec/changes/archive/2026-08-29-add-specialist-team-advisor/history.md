# Strategy History

## Strategy 1: Add More Main-Session Checklists And Compaction Reminders

- **Objective:** Prevent main from forgetting relevant specialist agents during long work.
- **Approach:** Consider regular all-agent checklists, done-done review, and generic post-compaction reminders.
- **Evidence:** Current main already retains outcome, safety, proof, worktree, routing, implementation, and integration obligations. Maintained `CLC-001` evidence shows two missed architecture-owner launches despite loaded routing rules, while another owner-routing scenario over-launched protocol and wire owners. Generic reminders would add context without preserving a detected task-specific obligation.
- **Outcome:** Rejected for the current increment.
- **Reason:** It increases the same main cognitive load, detects omissions late, and encourages ceremony or over-routing without transferring team-topology work.
- **Do-Not-Repeat Condition:** Do not add an all-agent checklist, fixed reviewer sequence, launch-count target, or vague `remember specialists` compaction text.
- **Evidence-Based Retry Condition:** Reconsider only if future runtime evidence shows that a bounded task-specific advisor cannot operate while a smaller explicit checklist measurably fixes the same maintained missed/extra-routing scenarios without context or latency regression.

## Strategy 2: Use A Routing Skill Without A Fresh Advisor

- **Objective:** Reduce routing latency by injecting a specialist-selection procedure directly into main.
- **Approach:** Add one skill that makes main classify the task, inspect available roles, and form its own team.
- **Evidence:** Skills are appropriate for procedural memory but execute inside the same main context. The accepted problem is that main simultaneously owns mission state, implementation, proof, and routing; skill-only routing leaves that high-load semantic decision and initial anchoring unchanged.
- **Outcome:** Rejected as the semantic team-selection owner.
- **Reason:** It structures main's work but does not independently challenge or remove the routing decision from main. A parallel skill would also duplicate advisor triggers and engagement-map semantics.
- **Do-Not-Repeat Condition:** Do not add a routing skill that repeats catalog interpretation, team selection, or reconsultation logic.
- **Evidence-Based Retry Condition:** Reconsider only if matched evidence shows a thin non-semantic packaging skill removes measurable context or latency without duplicating advisor behavior or restoring team selection to main.

## Strategy 3: Extend An Existing Readiness Or Generic Helper Agent

- **Objective:** Avoid adding a new agent by reusing `implementation-readiness-reviewer` or `qwen-local-worker`.
- **Approach:** Consider a team-routing mode on the readiness owner or generic first-pass routing by the helper.
- **Evidence:** `implementation-readiness-reviewer` already owns the registered `outcome-readiness` practice and is the selected challenger for active bounded-falsification work. `qwen-local-worker` is a generic first-pass helper, has no root-team contract, and is absent from default `core`. Team topology covers default non-trivial root missions, mid-task changes, skills, Practice Owners, workers, and conditional timing.
- **Outcome:** Rejected; choose one dedicated non-owning advisor.
- **Reason:** Extending either live role would mix responsibilities, create trigger overlap, or make default availability depend on an optional helper profile.
- **Do-Not-Repeat Condition:** Do not add a readiness-reviewer team mode, promote qwen helper to router, or retain two semantic advisors.
- **Evidence-Based Retry Condition:** Reconsider only if current role ownership changes independently and matched behavior proves one existing cohesive role satisfies every maintained team-advice scenario with lower total context and no practice/helper regression.

## Strategy 4: Dedicated Advisor With An Auto-Discovered Global Plugin

- **Objective:** Give a fresh advisor the actual root-effective agent and skill catalog without asking main to copy a static roster.
- **Approach:** Add `specialist-team-advisor` and initially place `specialist-catalog.ts` under `global/plugin/`, using a standalone custom tool.
- **Evidence:** Architecture and instruction reviews supported the dedicated-advisor responsibility but found core renderer, validator, proof, compaction, and model-route gaps. Execution-safety review identified that this repository's `global/` is an active `OPENCODE_CONFIG_DIR` and `global/plugin/` is auto-discovered, so source presence could activate the tool on a later restart without a separate installation operation.
- **Outcome:** Corrected to the retained strategy: dedicated advisor plus an explicitly configured, non-auto-discovered `global/extensions/specialist-catalog.ts` source, inert initialization, execute-local fail-closed behavior, and isolated profile proof.
- **Reason:** The corrected mechanism preserves one cohesive catalog owner and avoids unrequested active-host activation.
- **Do-Not-Repeat Condition:** Do not place the catalog source in an auto-discovered plugin directory, extend the mixed `session-env.ts` owner, expose a static roster, or treat source creation as installation authority.
- **Evidence-Based Retry Condition:** Use an auto-discovered source only if the active global source is no longer live or a separately authorized activation change proves exact containment and rollback; neither condition exists in this change.

## Strategy 5: Supersede Instruction-Size Ceilings With Context Quality

- **Objective:** Reconcile the retained advisor architecture with the repository's
  replacement instruction-context contract before implementation begins.
- **Approach:** Keep inventory size and token-proxy measurements as diagnostics while
  gating advisor/profile instruction changes on canonical ownership, exact-duplicate
  detection, mechanical canonicalization, context quality, and loaded behavior.
- **Evidence:** `replace-instruction-limits-with-context-quality` removed the maintained
  numeric instruction-budget owner. Advisor privacy, authority, profile, and behavior
  boundaries do not require a text-size maximum.
- **Outcome:** Selected as the current planning contract; the replacement change must
  apply first or this change must explicitly rebase before mutation.
- **Reason:** Unique advisor behavior must not fail solely because it increases a size
  proxy, while duplicate routing authority and behavior regressions must still fail
  closed.
- **Do-Not-Repeat Condition:** Do not restore `instruction:budget`, reviewed token-proxy
  ceilings, or compensating deletion requirements in this change.
- **Evidence-Based Retry Condition:** Reconsider a numeric instruction acceptance limit
  only if a separately accepted requirement identifies a concrete safety or runtime
  boundary that context-quality and loaded behavior checks cannot enforce.

## Strategy 6: Rebase Archived Owners And Serialize Remaining Changes

- **Objective:** Open one conflict-free advisor implementation window without weakening later active changes.
- **Approach:** Treat the five archived predecessors as the current baseline, preserve trajectory's declared dependency on advisor, keep Kaizen mutation-disabled, and serialize advisor then trajectory then Kaizen.
- **Evidence:** `openspec list --json` reports only those three active changes. The ownership inventory reports advisor/trajectory overlaps as `unresolved=false`; current instruction inventory and canonicalization pass with no changes or deterministic errors. Fresh readiness review `ses_fb64658bcffeT4Cz2ca9FuP56O` returned `no-material-finding` for planning candidate r2.
- **Outcome:** Selected. Advisor mutation may begin only after the task-1.2 runtime API gate.
- **Reason:** Archived owners cannot race current writes, and one serial order preserves every later change without path transfer or simultaneous mutation.
- **Do-Not-Repeat Condition:** Do not restore archived dependencies, enable a later writer concurrently, or edit a shared root after another active change acquires it.
- **Evidence-Based Retry Condition:** Re-run ownership reconciliation only if an active change, mutation flag, write root, transfer, or archive state changes before advisor archive.

## Strategy 7: Materialized Core Preflight With An Isolated Database

- **Objective:** Prove the complete `team-advising` pack and generated `core` loader without a provider call or repository candidate source.
- **Approach:** Extend the existing consumer-outcome runner, materialize `core` into one temporary root, overlay exact scenario permissions, and invoke config/agent/skill readback against an isolated OpenCode database.
- **Evidence:** The initial config, agent, and skill commands all exited `1` with `unable to open database file`. The configured `OPENCODE_DB` parent did not exist. After the proof runner created only its cache/config/data/state parents, the same loader boundary passed with nine scenarios, ten turns per arm, twenty matched-call maximum, zero model calls, and terminal fixture cleanup. Evidence: `evidence/task-1-3-team-advising-preflight-r1.md`.
- **Outcome:** Corrected and selected for baseline/candidate capture.
- **Reason:** The failure was Proof Runner setup, not Product Candidate behavior or OpenCode API absence. Creating the proof-owned DB parent is the smallest cause-specific correction.
- **Do-Not-Repeat Condition:** Do not start a generated-runtime proof with an `OPENCODE_DB` path whose parent has not been created, and do not classify that startup failure as a candidate or provider defect.
- **Evidence-Based Retry Condition:** Revisit database setup only if a current OpenCode version rejects the existing isolated directory layout or cleanup cannot prove the owned root removed.

## Strategy 8: Capture The Opaque Configured-Root Failure Before Baseline Retry

- **Objective:** Distinguish Product Candidate, Proof Runner, provider environment, and cleanup causes for the first failed unchanged-source baseline turn without repeating the ten-turn baseline.
- **Approach:** Preserve and replay the failed bundle through the terminal evaluator, seed the isolated model cache using the existing configured-proof helper, add bounded server-log capture and cleanup diagnostics, then run only `trivial-owner-local-direct` in explicit `diagnose` mode.
- **Evidence:** `evidence/team-advising-baseline-r1/bundle.json` records one configured request, command exit `1`, an `UnknownError` server ref, no tool or file effect, unchanged governed source, missing result/proof, and unqualified session-list cleanup. Both `replay-r1.json` and `replay-after-runner-fix-r1.json` reached the same terminal `blocked` evaluation with zero live calls. The original runner had disabled model fetches in an isolated cache without seeding the selected `openai/gpt-5.6-sol` identity and did not retain server logs.
- **Outcome:** Baseline repetition remains blocked. One single-scenario diagnostic capture is selected; it is evidence acquisition, not baseline proof.
- **Reason:** The raw bundle cannot distinguish an unavailable cached model from another server failure and cannot qualify the negative cleanup observation. Seeding the exact model identity and retaining debug stderr is causally different from repeating the original invocation.
- **Do-Not-Repeat Condition:** Do not run another complete baseline, change only timeouts/prompts, or treat the opaque server error as model/product behavior.
- **Evidence-Based Retry Condition:** Retry baseline only after the diagnostic bundle identifies the failing layer, proves terminal session/process/fixture cleanup, the cause-specific correction is in place, and every preserved failed bundle has reached the terminal offline evaluator.

## Strategy 9: Restore The Internal Provider While Keeping External Plugins Suppressed

- **Objective:** Prove one configured root turn and its cleanup after the diagnostic identified provider initialization, rather than product behavior, as the failing layer.
- **Approach:** Replace the hand-built capture environment with the existing `configuredProofServerEnvironment`, which keeps explicit generated config, isolated cache/database/state, external skills disabled, and `OPENCODE_PURE=1` while retaining OpenCode's internal provider plugins and the existing local credential store. Parse a session-list JSON array after any leading runtime output. Run provider-free preflight checks for the exact model route and OpenAI credential identity, then repeat only the one-scenario diagnostic with debug logs.
- **Evidence:** `evidence/team-advising-baseline-diagnostic-r1/bundle.json` records `ProviderModelNotFoundError` for `openai/gpt-5.6-sol` while the catalog suggested the unqualified model IDs. The runner had set `OPENCODE_DISABLE_DEFAULT_PLUGINS=1`; the repository's configured-proof helper deliberately does not. The diagnostic cleanup error was `session-list:invalid-json`, while the proof-owned runtime root was removed. Offline replay is terminal in `evidence/team-advising-baseline-diagnostic-r1/replay-r1.json`. Corrected provider-free preflight reports `modelRouteListed=true`, `providerCredentialConfigured=true`, `modelCalls=0`, and temporary-root removal.
- **Outcome:** The original baseline remains blocked; one corrected diagnostic call is unblocked as cause-specific recovery evidence.
- **Reason:** Enabling the internal configured provider changes the mechanism that failed, while preserving the no-external-plugin and isolated-state envelope. Tolerating leading output corrects the cleanup observer without weakening terminal root removal.
- **Do-Not-Repeat Condition:** Do not disable default/internal provider plugins for configured model captures, expose credential values, or treat a model-route listing alone as a successful configured turn.
- **Evidence-Based Retry Condition:** Start the complete unchanged-source baseline only if this corrected diagnostic writes and validates the exact result contract and reports zero remaining sessions plus removed fixture/process state.

## Strategy 10: Qualify Empty Session-List Output As An Empty Isolated Database

- **Objective:** Close the cleanup observer false negative without another model call and reopen the unchanged-source baseline only on terminal provider-free evidence.
- **Approach:** Reproduce `opencode session list --format json --pure` in the same generated-core, isolated-database environment during preflight. Treat status `0` plus empty stdout as an empty session set, while still requiring an actual JSON array for non-empty stdout, successful API deletion attempts, and recursive proof-root removal.
- **Evidence:** The task 1.4 diagnostic recorded a successful configured root command, exact `trivial-owner-local-direct` result, representative proof exit `0`, expected changed paths, unchanged governed source, and no forbidden effect. Its only negative field was `session-list:invalid-json`; the runner removed the proof-owned root. Provider-free reproduction showed session-list status `0` with exactly empty stdout. After the parser correction, preflight reports `cleanupObserverReady=true`, exact model route and provider identity, `modelCalls=0`, and terminal temporary-root removal. Strategy 22 later removed the privacy-unsafe raw diagnostic bundle while preserving this causal record.
- **Outcome:** The cleanup finding is a Proof Runner observer defect, not unknown writer/session liveness. The complete unchanged-source baseline is unblocked under the corrected source digest.
- **Reason:** OpenCode 1.18.25 represents an empty isolated session database as successful empty stdout rather than `[]`. Accepting only that exact empty-success case is narrower than weakening cleanup generally.
- **Do-Not-Repeat Condition:** Do not spend another configured request to rediscover the empty-output convention, accept malformed non-empty session output, or infer cleanup from a successful root command alone.
- **Evidence-Based Retry Condition:** Revisit this observer only if a non-empty status-zero payload is not a JSON array, API deletion fails, the isolated root remains, or a later OpenCode version changes the empty-database output.

### Baseline Completion

The corrected unchanged-source capture completed all nine `STA-001` members in ten root turns. Its original sealed bundle digest was `154cf9e49b72ca18e835e8a3b6c34ecf654c9276244b57fc87f082d1bc4c0665`; capture and provider-free replay returned evaluation digest `2b1fdde1224105ac7773c3e95507f623c9a4a4f301f633ab879fcfe126f0840e` with every baseline row passed, unchanged governed source, and terminal cleanup. Strategy 22 removed the privacy-unsafe raw and replaced it with provenance-bound `evidence/team-advising-baseline-r2-privacy-redacted-r1/bundle.json` digest `be238f7c1d893800605250c49fbeed0b7c885d27143ca32171c94590fd7373a3`. Task 1.4 evidence is summarized in `evidence/task-1-4-team-advising-baseline-r2.md`.

## Strategy 11: Compare File-Backed Legacy And V2 Catalog Routes

- **Objective:** Explain why a parsed file-backed advisor was absent from the v2 dispatchable catalog without changing advisor semantics.
- **Approach:** After source and environment controls remained empty, use one `OPENCODE_CONFIG_DIR` source and compare official SDK `/agent` versus `/api/agent` and `/skill` versus `/api/skill` in the same provider-free disposable server, with exact config and location readback.
- **Evidence:** `evidence/task-2-2-agent-route-diagnostic-r2.json` records HTTP `200` and twelve root-effective agent ids from `/agent`, including `specialist-team-advisor`, plus two root-effective skill ids from `/skill`. Both v2 routes returned successful nested empty arrays under the same directory. Config readback contained all five configured agent ids; the disposable root and session were removed. Troubleshooter session `ses_fb5ed395cffeGOUrmOpdO9jSq8` selected this probe after body, model-cache, provider, project-config, root-session, and pure-mode controls.
- **Outcome:** The task-2.1 inline-v2 fixture was not representative of file-backed runtime availability. Reopen task 2.1 and use `client.app.agents/skills` for the production projection while retaining the verified session attribution API.
- **Reason:** The official app routes are the only observed complete file-backed listing pair in installed OpenCode 1.18.25. A static roster or empty v2 catalog would violate the accepted active-catalog requirement.
- **Do-Not-Repeat Condition:** Do not continue toggling model, provider, pure, project-config, or agent-body inputs against the empty v2 routes, and do not treat config ingestion as dispatchable-catalog proof.
- **Evidence-Based Retry Condition:** Revisit v2 listing only when a changed OpenCode runtime or SDK demonstrates non-empty file-backed agent and skill arrays under a matched location.

## Strategy 12: Preserve The Compaction Agent In Generated Profiles

- **Objective:** Load task 3.1's synchronized `Team Advice State` compaction mirror through disposable `core` and `all` profiles without copying unrelated template configuration into core.
- **Approach:** The first task-3.1 loader evaluated `agent.compaction.prompt` from actual `opencode debug config` after materialization. Source inspection then compared that absence with `renderRuntimeSurfaceConfig` and the materialized template.
- **Evidence:** `evidence/task-3-1-runtime-surface-core-r1/raw.json` records successful config, advisor, agent-list, and skill commands plus green routing, Practice Owner, permission, profile, and cleanup facts; only `compactionTeamAdviceMirror=false` failed. `renderRuntimeSurfaceConfig` copied instructions and plugins but discarded the template `agent` block for both modes.
- **Outcome:** Correct the renderer to preserve only the existing `agent.compaction` object, assert its canonical fields and non-inference boundary in generated core/all configs, then recapture the provider-free core loader.
- **Reason:** The missing prompt is a Product Candidate renderer defect, not an OpenCode readback or instruction-source defect. A narrow compaction-only copy preserves the required runtime mirror without importing unrelated future agent overrides into core.
- **Do-Not-Repeat Condition:** Do not change the debug-config lookup path, weaken the loader oracle, copy the entire template agent map into core, or treat the r1 bundle as successful task-3.1 proof.
- **Evidence-Based Retry Condition:** Revisit the lookup path only if a generated config containing `agent.compaction.prompt` still yields a successful OpenCode config readback with `compactionTeamAdviceMirror=false`.

Retry evidence met that condition: `evidence/task-3-1-runtime-surface-core-r2/raw.json` again records `compactionTeamAdviceMirror=false` after the renderer correction, while the focused materialization test proves both generated profiles contain the prompt. The interim hypothesis was that public `debug config` omitted the prompt, so the next invocation was classified as bounded evidence capture using the generated input for marker evaluation.

`evidence/task-3-1-runtime-surface-core-r3-capture/raw.json` falsified that interim hypothesis: `debugConfigExposesCompactionPrompt=true`. The exact generated and debug-visible prompt instead omitted the explicit marker `does not reconsult solely because compaction occurred`, using an ambiguous shared `or reconsult` clause. The next attempt is unlocked only after the portable prompt contains each explicit negative marker and static validation passes; this is a Product Candidate mirror correction rather than another lookup-path retry.

## Strategy 13: Admit Task-Scoped Team-Advising Captures

- **Objective:** Capture the approved serial `STA-001` task groups without front-loading all nine configured candidate scenarios or weakening the complete-population final claim gate.
- **Approach:** Invoke candidate capture with task 3.2's two exact scenario ids after a green current-source preflight and before any other candidate member.
- **Evidence:** The CLI exited before creating an evidence root, session, process, or model call with `The team-advising baseline/candidate pack runs its complete nine-member population.` The help contract already advertises `--scenarios`; `captureTeamAdvisingPack` already filters exact scenario ids; and tasks 3.2, 3.3, 4.1, and 4.2 explicitly serialize the population as 2 + 1 + 5 + 1 members.
- **Outcome:** Remove only the contradictory CLI guard, scope the pack/evaluator/request bound/maximum claim to selected ids, and add provider-free full-baseline subset replay coverage before retrying the same two-member capture.
- **Reason:** The failure is a pre-session Proof Runner admission defect. Exact subset capture minimizes time to the first semantic signal and preserves the final complete-population requirement for task 4.3.
- **Do-Not-Repeat Condition:** Do not run the complete nine-member candidate to bypass the task sequence, broaden arbitrary packs, create a second runner, or claim complete `STA-001` coverage from a subset.
- **Evidence-Based Retry Condition:** Retry the two-member configured capture only after focused tests prove canonical subset selection, narrowed request/claim bounds, provider-free replay against the full baseline, zero live calls, and unchanged full-population behavior.

## Strategy 14: Load The Configured Catalog Plugin In Team-Advising Proofs

- **Objective:** Give the captured advisor exactly one runtime-loaded `specialist_catalog` tool while retaining the isolated generated-core source, configured provider route, and bounded scenario permissions.
- **Approach:** Preserve the first two-member candidate bundle and replay it provider-free before changing the proof environment.
- **Evidence:** `team-advising-candidate-r1-task-3-2/bundle.json` records a completed `specialist-team-advisor` child with `Team Advice: unknown`, `catalogCalls: 0`, and `specialist_catalog` reported unavailable. The root and child cleaned up completely and the active config hash remained unchanged. Pinned OpenCode `1.18.25` plugin source sets configured external plugins to an empty list when `RuntimeFlags.pure` is true; the team proof inherited `OPENCODE_PURE=1` from the general configured-provider helper even though its generated core config contained the catalog plugin exactly once.
- **Outcome:** Override `OPENCODE_PURE=0` only in the team-advising environment, keep default internal provider plugins enabled, expose that fact in provider-free preflight, and count only selected scenario calls in subset evaluation.
- **Reason:** This is a Proof Runner environment defect, not an advisor selection or catalog implementation defect. Changing the shared helper would unnecessarily alter unrelated configured-provider proofs.
- **Do-Not-Repeat Condition:** Do not retry with pure mode, disable default plugins, change the advisor oracle to accept `unknown`, treat a configured plugin path as loaded-tool evidence, or modify the active installed config.
- **Evidence-Based Retry Condition:** Retry the same two-member candidate only as a new immutable bundle after focused tests and provider-free preflight prove the selected population, `configuredPluginLoading: true`, bounded request accounting, and unchanged source/active-config identities.

## Strategy 15: Bridge The Installed Legacy Plugin Client To Current Catalog Routes

- **Objective:** Return the root-effective agent and skill catalog through the installed OpenCode `1.18.25` plugin boundary instead of degrading to `unknown` after a successful tool call.
- **Approach:** Preserve and replay task 3.2 r2, which loaded and called `specialist_catalog` exactly once but returned a catalog API gap.
- **Evidence:** `team-advising-candidate-r2-task-3-2/bundle.json` records `catalogCalls: 1`, child tool `specialist_catalog`, complete cleanup, no unavailable capability in main's result, and advisor text `Catalog returned status unknown because the catalog API was unavailable`. The configured plugin dependency is SDK `1.18.15`: its injected legacy client has `app.agents` but no `app.skills`, while its retained request transport and generated v2 client define the installed `/session/{id}`, `/agent`, and `/skill` routes. The source candidate currently requires the newer flat `client.app.agents({ directory })`, `client.app.skills({ directory })`, and `client.session.get({ directory, sessionID })` surface.
- **Outcome:** Keep the newer public API as the primary path and add one installed-runtime adapter over the injected client's existing request transport for those same three routes. Exercise both paths against the provider-free isolated server.
- **Reason:** This is a concrete installed-client compatibility boundary. It preserves the official root-effective server APIs without returning to the previously falsified empty `client.v2.*.list` mechanism or adding a second catalog source.
- **Do-Not-Repeat Condition:** Do not retry unchanged r2, accept `unknown` for an available catalog, use checkout scanning as catalog truth, retry the empty v2 list path, update/install dependencies, or change the active config.
- **Evidence-Based Retry Condition:** A third configured task 3.2 capture is unlocked only after provider-free candidate-plugin proof returns `status: ok` through both the modern and installed-legacy transport shapes, focused contracts are green, exact subset preflight remains ready, and source/active identities are rechecked.

## Strategy 16: Rebind Configured Captures To The Actual Core Catalog

- **Objective:** Make the configured-session catalog identity match the declared generated `core` profile before composing `STA-001` lanes.
- **Approach:** Preserve and replay task 4.1 r1, inspect the unavailable-capability contradiction, and invoke the Foundation Integrity Practice Owner before further capture.
- **Evidence:** `runtimeEnvironment()` materialized core and then spread the full `quality-independent` profile through `OPENCODE_CONFIG_CONTENT`; that profile contains non-core `protocol-api-reviewer`. Task 4.1 r1's actual advisor catalog listed it as available. Foundation incident `FI-STA-CORE-001` and Practice Owner task `ses_fb586ca4affeBKFloQGvFhP6Yi` confirm the profile/environment/oracle contradiction. Prior task 3.2/3.3 bundles remain valid only for the exact historical hybrid environment.
- **Outcome:** Pass only the CLI-selected top-level model plus scenario permissions inline, expose the root-effective generated-core agent ids in provider-free preflight, assert the non-core control absent, reopen invalidated accepted lanes, and preserve all raw bundles.
- **Reason:** The full profile overlay was unnecessary because the root command already pins model and variant; it silently changed catalog availability and made cross-lane core claims false.
- **Do-Not-Repeat Condition:** Do not spread a model profile's full `agent` map into a bounded runtime-surface proof, compose historical hybrid and corrected-core rows as one current population, add `small_model`, mutate active config, or rewrite raw evidence.
- **Evidence-Based Retry Condition:** Corrected configured captures begin only after focused tests and provider-free preflight show the exact core catalog, unchanged generated compaction ownership, absent `protocol-api-reviewer`, zero sessions/calls, and unchanged active-config identity.

## Strategy 17: Disambiguate Direct, Conditional, And Unavailable Routes

- **Objective:** Close the four routing-semantic failures preserved in task 4.1 r1 and the corrected-core unavailable control without changing the reviewed scenario population or evaluator.
- **Approach:** Replay the terminal bundles provider-free and inspect exact root/advisor/result records after foundation incident `FI-STA-CORE-001` closed.
- **Evidence:** `procedural-skill-no-fresh-agent` and `overstaffing-negative-control` executed direct because the trivial exception did not distinguish performing an already selected action from deciding whether to use or omit a maintained route. `conditional-later-specialist` called the advisor, but the advisor omitted rather than represented the dormant package and returned `main-alone`. The corrected-core unavailable control now records exact missing `protocol-api-reviewer` and no substitution, but the advisor converts the gap-recording work into `main-alone`; only disposition oracles fail.
- **Outcome:** Narrow the direct exception to already selected actions, represent dormant catalog-listed packages explicitly with `Timing: conditional`, and return `unknown` when an exact required capability is unavailable with no valid substitute.
- **Reason:** These are current Product Candidate routing ambiguities against already accepted `STA-001` semantics. The foundation/catalog identity is now aligned and must not be changed again for these rows.
- **Do-Not-Repeat Condition:** Do not edit the scenario pack or evaluator, force advisor calls through fixture-specific wording, dispatch conditional roles early, treat main's gap-recording work as capability satisfaction, or recapture unchanged semantics.
- **Evidence-Based Retry Condition:** Recapture affected task 4.1 members only after focused agent/instruction/contracts/context tests and provider-free exact-core preflight pass with unchanged active-config identity.

## Strategy 18: Enforce Advisor-Before-Selection And Exact Unknown Transfer

- **Objective:** Close the two remaining task 4.1 r2 failures while preserving the three newly passing members and the corrected-core foundation.
- **Approach:** Replay r2 provider-free and inspect root tool events, advisor output, and `result.json` for only `procedural-skill-no-fresh-agent` and `active-profile-capability-unavailable`.
- **Evidence:** The procedural root still made no advisor or skill call despite the general non-trivial classification; it directly wrote a `reuse-discovery` disposition. In the unavailable row, advisor top-level state was correctly `unknown`, but package prose invented `mainDisposition=capability-unavailable`; main emitted `main-alone` and a descriptive capability string instead of exact id `protocol-api-reviewer`.
- **Outcome:** Require advisor consultation before selecting, omitting, or invoking any maintained route; require main to preserve advisor `unknown`; and require exact catalog ids for unavailable capabilities. Require the advisor's unknown package prose to name only the valid `unknown` disposition.
- **Reason:** The prior wording described classification but did not prohibit early route selection or define lossless state transfer from advisor to main.
- **Do-Not-Repeat Condition:** Do not change fixtures/evaluator, add scenario names to instructions, accept descriptive capability strings, invent a fourth disposition, or recapture already passing members before the two affected rows pass under one current source identity.
- **Evidence-Based Retry Condition:** Capture only the two affected rows after focused tests and corrected-core preflight pass. If both pass, capture the other three rows under the unchanged source identity for one composable current task 4.1 population.

## Strategy 19: Put The Omission Trigger At The Dispatch Selection Surface

- **Objective:** Make `overstaffing-negative-control` consistently consult the advisor before concluding that no available role adds value.
- **Approach:** Preserve task 4.1 r3 complement after it passed multi-domain and conditional but skipped the advisor for overstaffing, despite r2 having passed that same member.
- **Evidence:** The canonical body rule was present under governed source `bd3e...`, yet the root made zero advisor/catalog calls. The root-visible task description still said only “Use by default” and “Stay quiet for one existing-owner local action,” allowing the omission decision to occur before the detailed body rule was applied.
- **Outcome:** Move the exact before-selecting-or-omitting condition into the advisor description used at Task dispatch selection, and state that any mission asking whether an artifact adds value requires the advisor.
- **Reason:** This changes the causal selection surface rather than repeating another hidden body-only classification phrase. It preserves the same accepted semantics and no-dispatch negative-control outcome.
- **Do-Not-Repeat Condition:** Do not accept an old-revision pass as current, force calls in the runner, add the scenario id to portable instructions, broaden the advisor to trivial already-selected actions, or recapture the complement before the one-member probe passes.
- **Evidence-Based Retry Condition:** Probe only `overstaffing-negative-control` after focused agent/contracts/context checks and exact-core preflight pass. Capture the four-member complement only if that probe passes without any source mutation.

## Strategy 20: Bind Continuity Main And Compaction Routes To Their Actual Owners

- **Objective:** Capture task 4.2's two actual compaction/readback controls without invalidating the immutable `STA-001` baseline fixture.
- **Approach:** Extend the existing consumer-outcome CLI with a separate `--continuity` lane and reuse the status-scope generated-config route reader for its first local setup.
- **Evidence:** The first capture stopped before server start or any model call with `Configured status-scope route is incomplete: build`. Generated `core` intentionally keeps compaction in `agent.compaction`, while the selected quality-independent profile and inline configured environment own the main build route.
- **Outcome:** Read build from `quality-independent` and compaction from materialized core, add a continuity-specific provider-free route/prompt preflight, preserve the failed empty evidence root, and use a fresh capture root.
- **Reason:** This corrects a Proof Runner identity binding defect. It does not change product behavior, the reviewed continuity fixture, or `STA-001` semantics.
- **Do-Not-Repeat Condition:** Do not add a synthetic `agent.build` entry to generated core, reuse the status-scope two-route parser, delete the failed evidence root, or launch continuity capture before the new local preflight and focused tests pass.
- **Evidence-Based Retry Condition:** Retry once at a fresh evidence root only after provider-free preflight reports the exact quality-independent main route, generated compaction route, all Team Advice State fields, zero model calls, and terminal temporary-root cleanup.

Retry evidence at `evidence/team-advice-continuity-r2-task-4-2/bundle.json` reached the isolated server but made zero model/session calls because the installed `v2.agent.list` route did not become ready within 15 seconds. The bundle proves server and fixture termination but exposed two Proof Runner diagnostic defects: it discarded the causal route-list error and labeled no attempted session as uncleared. The next mechanism uses the exact reviewed main/profile and generated compaction routes already proven by installed config readback, validates those routes through provider availability, preserves the cause chain, and records no-session-attempt as terminal. Do not retry `v2.agent.list` or increase its timeout.

## Strategy 21: Compare Continuity Package Identity Without Conflating State Rendering

- **Objective:** Finish task 4.2 from the preserved actual-compaction r3 bundle without another configured call.
- **Approach:** Replay r3 through the terminal evaluator, then inspect only its sanitized compaction and reconstruction evidence.
- **Evidence:** Both summaries retain all eleven exact Team Advice State fields and omit catalog noise. Unchanged engagement returns `reconsult=false`. Changed catalog returns `reconsult=true`, `stalePackages=["test-coverage-reviewer"]`, and `terminalPackages=["architecture-review"]`. The evaluator alone expected serialized state tokens `test-coverage-reviewer:on-failed-runtime-proof` and `architecture-review:terminal` in fields explicitly named as package lists.
- **Outcome:** Normalize only package-list comparisons to package ids while retaining exact field/value checks in the compaction summary and exact candidate/catalog/reconsultation checks in reconstruction.
- **Reason:** This corrects an evaluator representation defect without changing accepted semantics, the reviewed fixture, raw capture, compaction prompt, or candidate behavior.
- **Do-Not-Repeat Condition:** Do not edit the fixture, recapture r3, accept missing package identity, weaken exact summary state checks, or treat evaluator-only replay as new runtime evidence.
- **Evidence-Based Retry Condition:** Replay the preserved r3 bundle provider-free through the corrected terminal evaluator. A matching package-id set and all other existing checks must pass with `liveCalls=0` before task 4.2 can close.

## Strategy 22: Rebuild Review Evidence With Privacy-Safe Owning Oracles

- **Objective:** Correct task 5.1's reproduced privacy and proof-identity defects without changing the immutable nine-member population, baseline, product catalog, or accepted routing semantics.
- **Approach:** Treat r5 as behaviorally informative but privacy-invalid; extend root-path redaction to JSON-escaped forms, make sample/bundle sealing and evaluation fail closed on private paths, derive forbidden-effect rows from tool-input/path and worktree-status sentinels, and give continuity its own loaded-core preflight instead of spreading STA-001 topology facts.
- **Evidence:** Execution-safety review `ses_fb502b874ffe1xAUyOppZ56bS1` found escaped private roots in r5 and hardcoded false effect rows. Architecture review `ses_fb5078a71ffeLxHxGP53QdobmK` found continuity preflight relabeling a topology-subset preflight. Instruction review `ses_fb50e43e9ffef4cS5kK90GDcpc` found the discovery contract and README route out of sync with the loaded/validated rule.
- **Outcome:** Preserve all three frozen-candidate reports, remove privacy-unsafe r5 evidence, run focused provider-free falsification first, then capture one fresh full candidate only after the corrected runner blocks the old leak and all preflights report zero model calls.
- **Reason:** Privacy containment is non-deferrable, and the existing r5 bundle cannot support its own task 4.3 privacy claim. A changed proof runner also invalidates the governed source identity, so one corrected full capture is the smallest composable route.
- **Do-Not-Repeat Condition:** Do not recapture unchanged r5, mutate the baseline or scenario fixture, treat hardcoded absence as an observation, compose old and new governed identities, expose private values in reports, or fold continuity into STA-001.
- **Evidence-Based Retry Condition:** A fresh candidate capture is unlocked only after focused tests prove escaped-path rejection, legacy unsafe replay failure, sentinel-bearing candidate rows, continuity-only preflight identity, unchanged active config, and provider-free `liveCalls=0`.

## Strategy 23: Apply Generic Private-Home Redaction At The Capture Boundary

- **Objective:** Close the remaining fail-closed private-path leak without guessing another environment-specific root or weakening the privacy oracle.
- **Approach:** Preserve the terminal r6 and r7 attempt facts: each stopped after its first configured turn, cleaned up synchronously, wrote no bundle, and r7 localized the remaining hit to `$.commands[0].stdout`. Replace exact-root expansion with the already-tested generic Windows/Linux private-home redactor as the final transform on every captured text value and retain a redaction count.
- **Evidence:** r6 still failed after JSON-escaped root handling; r7 still failed after adding OS-home and repository-parent roots. The privacy-derived historical baseline proved the generic redactor removes the same nested JSONL path class while preserving sealed sample behavior and source-bundle provenance.
- **Outcome:** Exercise the generic transform provider-free with no supplied roots, rerun the full focused suite and exact preflight, then allow at most one fresh full capture under the new governed source identity.
- **Reason:** Two exact-root attempts did not reach a sealed first sample. A generic syntax-class transform is causally different and portable across randomized temp roots and nested JSON escaping.
- **Do-Not-Repeat Condition:** Do not add another guessed root, weaken `assertPrivacySafe`, retain unsafe diagnostic text, reuse either failed evidence root, or retry without a zero-hit scoped scan and provider-free preflight.
- **Evidence-Based Retry Condition:** One r8 capture is unlocked only when the no-root generic-redaction test reports both expected hits, focused consumer-outcome tests pass, scoped evidence privacy scan reports zero files, current STA-001 preflight reports zero model calls and a new governed source digest, and active config remains unchanged.

## Strategy 24: Preserve Per-Scenario Checkpoints Before Long-Run Retry

- **Objective:** Recover from r8's outer-supervisor timeout without treating absence as a candidate failure or repeating another evidence-losing full run.
- **Approach:** Close r8 by verifying no attributable process remains, confirm its evidence root is empty, remove its single attributable `mid-task-topology-change` temp root, and extend the existing team-advising capture owner to seal one privacy-safe checkpoint bundle after each completed scenario. Successful capture deletes checkpoints after writing the terminal bundle; interruption retains the latest sealed partial bundle.
- **Evidence:** r8 exceeded the 900,000 ms shell limit with no report. Process inventory found no owned Node/OpenCode process after termination. The empty evidence root and one recent `team-advising-mid-task-topology-change-*` temp root show it reached the seventh scenario but the runner wrote no progress artifact before finalization.
- **Outcome:** Run focused checkpoint/privacy tests and exact preflight first. Then use one fresh evidence root and a supervisor window sufficient for the already observed ten-turn duration; checkpoint persistence, not timeout increase, is the recovery mechanism.
- **Reason:** The candidate had downstream progress but the proof runner made it uninspectable. Incremental sealed evidence closes that diagnostic gap and satisfies interruption recovery without changing the scenario pack, product candidate, baseline derivative, or evaluator semantics.
- **Do-Not-Repeat Condition:** Do not infer failure from timeout, reuse the empty r8 root, launch while any writer is live, raise only the timeout, retain checkpoints after a successful terminal bundle, or compose a partial checkpoint as the full population.
- **Evidence-Based Retry Condition:** One r9 capture is unlocked only after focused tests cover bundle sealing and the checkpoint writer path, exact preflight reports a new governed digest and zero model calls, scoped privacy scan is zero, active config is unchanged, and no attributable process/temp root remains. The successful r9 directory must then prove checkpoint cleanup by containing only terminal artifacts.
