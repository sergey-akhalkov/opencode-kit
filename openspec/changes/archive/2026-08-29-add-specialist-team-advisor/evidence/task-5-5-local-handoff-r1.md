# Local Handoff

## Outcome

`Outcome: working`

The repository candidate adds one non-authorizing `specialist-team-advisor` for new non-trivial parentless root missions. The advisor returns the smallest sufficient main/skill/specialist map; active primary main remains the sole mission owner, role selector, dispatcher, integrator, runtime-proof owner, finding disposer, and final synthesizer.

## Accepted Mission And Non-Goals

The accepted mission was to add current root-effective specialist discovery and one bounded advisor route without static roster drift, broad fan-out, passive-main behavior, or new lifecycle ceremony. The direct exception remains limited to execution of one already-selected trivial owner-local action. Practice Owners retain their exact independent triggers.

This change does not claim universal expert selection, semantic answer quality, timing improvement, cross-model equivalence, host-wide safety, active-host behavior, installation, deployment, or stable qualification. It does not install or activate the candidate, edit the active gitignored config, dispatch specialists itself, or grant review output authority.

## Responsibility And Architecture

- Main forages enough to state the mission, constraints, observed evidence, candidate identity, and material unknowns.
- The advisor reads only the privacy-safe root-effective catalog and returns `main-alone | team-recommended | unknown` plus bounded packages and a reconsultation condition.
- Main independently accepts or omits packages, dispatches selected specialists, preserves role/session attribution and writer closure, integrates results, proves behavior, and completes the mission.
- Specialists remain bounded by their own roles and never inherit mission authority from the advisor.
- Compaction preserves only accepted package state and evidence references; resumed main verifies candidate, catalog, and liveness before relying on pending advice.

Architecture disposition: `build-minimal`. No existing owner supplied a current root-effective privacy-safe catalog with caller/root attribution. The change extends the existing runtime-surface renderer and consumer-outcome owner, adds one local catalog plugin and one advisor agent, and avoids a routing skill, static roster, scoring mechanism, or second advisor.

## Candidate And Runtime Proof

- Candidate: `add-specialist-team-advisor-task-5-1-checkpointed-r9`
- Governed source: `4f964ad2ed38cc23fe3629c85a9c27210c794985fd16f6a152e0ef5cde94a8cb`
- Pack: `team-advising-r1`, digest `7cad8bec084c94337d529729ff88cea995d8b2e65558d4649a8a725bd79390bc`
- Candidate bundle: `6bafab7a346d97442f1649e5ceed5548ee88d3279257885d4b00ad1c788b9bc1`
- Privacy-derived baseline: `be238f7c1d893800605250c49fbeed0b7c885d27143ca32171c94590fd7373a3`
- Terminal evaluation: `e66de16b01b018926599e6b9f366e31bee22595cfaed1b9cdd7b5ebe620fe3cd`
- Runtime: installed OpenCode `1.18.25`, executable SHA-256 `59b379b53354da72d2c5262119fe70c44b4e473826ebbaa94d47a2d58a359b1a`
- Model/profile: `openai/gpt-5.6-sol`, `xhigh`, `quality-independent`, generated `core`

All nine named `STA-001` scenarios passed the configured candidate and privacy-derived historical baseline evaluator. Provider-free replay reproduced the same digest with `liveCalls=0`. Generated `core` and `all` profiles loaded the advisor and exactly one catalog plugin through the installed OpenCode entry point; both loader records passed and reported terminal cleanup.

## Claim Ceiling

`STA-001` is terminal `narrowed`, with 9/9 reviewed observations. Support is limited to source `4f964ad2`, OpenCode `1.18.25`, `openai/gpt-5.6-sol` `xhigh`, the `quality-independent` generated-core environment, the nine reviewed scenarios, and the provenance-bound privacy-derived historical baseline. The immutable pack/evaluator phrase `matched unchanged-source baseline` is not evidence of byte identity. No fresh-live-baseline, universal, timing, semantic-quality, cross-model, host-wide-safety, active-host, or deployed-runtime claim is supported.

## Independent Evidence

- `instruction-artifact-reviewer`, `openspec-architecture-reviewer`, and `execution-safety-reviewer` inspected the frozen candidate; main reproduced and dispositioned all rows. Their retained observations did not authorize work.
- Fresh SDET returned `no-critical-risk`. The engine-session caller path remains a non-critical substitution gap; plugin caller/root attribution is retained and independently proven at the installed provider-free boundary.
- Fresh `evidence-sufficiency-reviewer` challenge identified the baseline-substitution and immutable-wording ceilings. Main accepted the narrowed claim; challenge session `ses_fb4ab7663ffe3uK03TsYrqqLSu`, effective model `xai/grok-4.6`.

## Validation

The complete ledger is `evidence/task-5-4-validation-r1.md`. Focused catalog, agent, profile, model, permission, compaction, consumer-outcome, and installer tests pass. Instruction inventory/context quality, strict library validation, the complete test suite, selected and all-item strict OpenSpec validation, blocking apply gate, diff check, privacy scan, generated `core` loader, and generated `all` loader pass.

## Known Non-Critical Limitations

- The historical baseline is privacy-derived and provenance-bound; byte comparison to the deleted privacy-unsafe raw is intentionally unavailable.
- The immutable evaluator wording is broader than the authoritative claim record; consumers must use `evidence-index.json` for the supported ceiling.
- The SDET did not run an actual non-advisor model session tool call through the engine path; installed plugin tests still prove denied callers receive no entries.
- Host-wide effects beyond the recorded sentinel, git-status, permission, and cleanup oracles are not claimed.
- Unrelated campaign-orchestration worktree changes remain outside this candidate.

## Rollback And Cleanup

Rollback before activation is repository-local: remove the advisor, catalog plugin, profile entries, renderer/instruction changes, and associated proof/tests as one candidate change. No persisted user data or migration exists. All attributable disposable sessions, processes, generated profile staging roots, and scenario roots were terminally cleaned. The active `global/opencode.json` remained byte-unchanged with SHA-256 `0050d9de6b28e9b5574b57a519c5a3c09766910dc97afd0dcaf2b4a778628144`.

`External Operations: not performed`

Installation, activation, restart, commit, push, release, deployment, and remote mutation remain outside this change and require their separately authorized operation.
