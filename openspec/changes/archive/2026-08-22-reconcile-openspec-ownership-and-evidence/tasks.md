## Shared Execution Envelope

Begin advisory/read-only. Do not block all OpenSpec operations until current active changes have reviewed manifests/indexes and mechanical integrity defects are corrected. This change never selects a product winner, implements another change's unfinished live product work, deletes evidence, or replaces official OpenSpec validation/archive. After explicit owner authorization it MAY restore omitted selected-strict scenarios, honest completion claims, ownership/evidence manifests, and bounded indexes on other active changes.

## 1. Define Deterministic Contracts

- [x] 1.1 Add exact schemas for `ownership.json` and `evidence-index.json`, including capability/requirement/write roots, mutation state, DAG/transfer, task text digest, candidate/environment/boundary/invocation/artifacts/cleanup, and evidence lane/file/byte facts; verify missing/extra/escape/cycle/unknown cases fail explicitly.
- [x] 1.2 Implement stable read-only ownership/evidence inventories and effect-free help; verify current conflicting/invalid/evidence-heavy fixtures reproduce AUD-001/AUD-006/AUD-007/AUD-008 without choosing a winner or rewriting files.

## 2. Prove Truthful State Evaluation

- [x] 2.1 Compose selected strict OpenSpec output, artifact/task state, evidence freshness/envelope, ownership conflict, and repository validation into a pure state evaluator; verify invalid-complete, stale task, weaker entrypoint, overlap, and unrelated-failure fixtures return exact local/repository states.
- [x] 2.2 Implement 64-file/25-MiB default retention checks and explicit finite exception schema; verify over-limit blocks capture/completion, evaluator-only failure routes replay, and no file is deleted automatically.

## 3. Migrate Current Active Changes Under Their Owners

- [x] 3.1 Generate a read-only migration report for every active change and have each owner review/add its own manifests/index rows; verify planning-only dependencies and one mutation-enabled owner for every overlap.
- [x] 3.2 Restore `reduce-workflow-ceremony` selected-strict omitted scenarios and honest stage, publish one mutation-enabled Restart writer plus planning-only dependents, and index/bound mission plus other active-change evidence without deleting files or implementing unfinished live product work; verify `openspec validate --all` and advisory inventory have no unresolved AUD-001/006/007/008 without an explicit planning-only or incomplete disposition.
- [x] 3.3 Re-run advisory inventory until no active ownership ambiguity, invalid completed claim, stale checked task, or unindexed/over-limit evidence remains without an explicit planning blocker.

## 4. Integrate Operation Callers

- [x] 4.1 Add focused readers to propose/apply/doctor qualification/archive gates while preserving official OpenSpec diagnostics; verify propose reports ownership, apply blocks overlap before mutation, qualification blocks false completion, and archive blocks stale evidence before official archive.
- [x] 4.2 Enable blocking enforcement only after migration readback passes; verify historical archives remain untouched and unsupported facts produce unknown at the dependent gate.

## 5. Complete Validation

- [x] 5.1 Run operation/doctor/archive fixtures, current active inventory, `npm test`, `npm run validate:strict`, every selected strict active-change validation, and `openspec validate --all`; selected behavior can reach MVP with an exact unrelated-owner blocker, but repository-wide green is required before enforcement activation or qualification completion.
