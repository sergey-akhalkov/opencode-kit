## 1. Source-Scoped Inventory Happy Path

- [x] 1.1 Reuse maintained runtime-source/config discovery to build a bounded loader-visible instruction manifest with evidence classes, explicit supported local `instructions` paths, stable unknown rows, and no repository-wide consumer walk. **Current Rung:** catalog-only inventory plus manual runtime-source reconciliation. **Next Real Boundary:** installed loader-visible inventory over a disposable consumer with global, parent, project, `.opencode`, explicit external-local, unsupported, and unreadable sources. **Blocker/Unblocker:** none. **Observable Proof:** the manifest includes only relevant instruction sources, excludes vendor/evidence trees, preserves unknowns, and emits no instruction/config content. **Owner Authorization:** local read-only source files and proof-owned fixtures. **Safeguards:** redact external locations; no provider, model, credential, network, install, activation, target-project mutation, or excerpt output. **Restoration/Cleanup:** delete only fixtures and preserve manifests, hashes, argv, exits, stdout/stderr, and cleanup. **Expected Immutable Evidence:** privacy-safe source manifest and side-effect inventory. **Validation:** installed CLI runtime proof before dependent budget behavior. **Dependency-Chain Stop Condition:** stop if discovery requires arbitrary config serialization, an unbounded project walk, or an unsupported loaded-state inference.

- [x] 1.2 Add explicit `catalog` and `loader-visible` inventory scopes, keep catalog compatibility, and report startup-visible candidates, discovery metadata, on-demand bodies, unknowns, and aggregate token proxies separately; invoke both scopes through the actual CLI. **Current Rung:** bounded manifest from task 1.1. **Next Real Boundary:** installed kit catalog and disposable-consumer inventory calls. **Blocker/Unblocker:** task 1.1. **Observable Proof:** default catalog output remains compatible, loader-visible categories remain separate, and external instruction text cannot appear in repeated-line or artifact output. **Owner Authorization:** local read-only process/file access. **Safeguards:** deterministic proxy only; no claim of exact model tokens or final prompt inclusion. **Restoration/Cleanup:** preserve proof outputs and remove fixtures. **Expected Immutable Evidence:** before/after catalog shape comparison and loader-visible aggregate report. **Validation:** maintained provider-free inventory proof. **Dependency-Chain Stop Condition:** do not add budget enforcement while source categories can be conflated or leak content.

## 2. Budget Ownership And Validation

- [x] 2.1 Add one versioned reviewed instruction-budget seed containing limits only, materialize current measurements from source, preserve historical lower targets as explicit debt, and fail strict validation on malformed seed or boundary growth. **Current Rung:** runtime-proven source-scoped inventory with unenforced prose ceilings. **Next Real Boundary:** installed strict validation against unchanged, over-limit, and malformed disposable candidates. **Blocker/Unblocker:** tasks 1.1-1.2 provide trustworthy derived inputs. **Observable Proof:** unchanged candidate passes, one-byte/token-proxy growth beyond a named maximum fails with actual/maximum/regeneration command, and the seed contains no derived hash/count/order/drift fields. **Owner Authorization:** local repository and disposable-file validation only. **Safeguards:** do not delete safety instructions or silently reset historical targets to make validation green. **Restoration/Cleanup:** restore/delete only proof-owned fixture copies; preserve raw validation output. **Expected Immutable Evidence:** reviewed seed, deterministic materialization/readback, and drift matrix. **Validation:** installed validator boundary proof. **Dependency-Chain Stop Condition:** stop if two budget owners emerge or generated values must be hand-synchronized.

## 3. Integrated Runtime Proof And Documentation

- [x] 3.1 Update token-economy/inventory documentation and run one maintained provider-free proof over the current kit plus a disposable consumer, covering catalog compatibility, loader-visible categories, redaction, unknowns, no vendor walk, budget pass/fail, deterministic rerun, and cleanup. **Current Rung:** individually proven inventory and budget slices. **Next Real Boundary:** actual installed operator workflow from inventory through strict validation. **Blocker/Unblocker:** tasks 1.1-2.1. **Observable Proof:** a cold operator can distinguish catalog cost from startup-visible candidates and reproduce every budget verdict without sensitive output. **Owner Authorization:** local read-only kit/fixture access and disposable writes. **Safeguards:** no provider/model calls, external consumer mutation, install, activation, or remote effect. **Restoration/Cleanup:** deterministic fixture cleanup; preserve immutable report bundle. **Expected Immutable Evidence:** Candidate Reference, environment, argv/exits, reports, seed/readback, side effects, and cleanup. **Validation:** maintained proof and schema/materialization checks. **Dependency-Chain Stop Condition:** any nondeterminism, content disclosure, or category ambiguity returns the candidate to development.

## 4. Qualification

- [x] 4.1 After runtime proof and accepted-scope completion, dispatch one fresh test-only SDET to challenge instruction-content disclosure, secret-bearing config leakage, unbounded traversal, unknown-to-zero conversion, budget fail-open, and catalog compatibility; main reproduces and dispositions every plausible critical row. **Current Rung:** frozen complete candidate with installed provider-free proof. **Next Real Boundary:** critical-only local challenge over the same manifest/inventory/validator boundary. **Blocker/Unblocker:** task 3.1 green and no open mutator. **Observable Proof:** terminal `critical-risks-reported`, `no-critical-risk`, or exact `blocked` evidence with Effective Model. **Owner Authorization:** local test-only files/processes. **Safeguards:** no production/spec/instruction edits by SDET and no real private instruction payload persisted in fixtures. **Restoration/Cleanup:** preserve accepted test evidence and remove disposable state. **Expected Immutable Evidence:** SDET matrix and main disposition. **Validation:** smallest justified critical regression or privacy oracle. **Dependency-Chain Stop Condition:** first precondition-valid attempt without a main-confirmed critical defect permanently stops SDET for this root.

- [x] 4.2 Run complete project-native validation, strict OpenSpec validation, deterministic inventory readback, and local handoff documenting proxy limits, historical debt, privacy boundary, unsupported sources, rollback, and external-operation state. **Current Rung:** terminal critical-SDET candidate. **Next Real Boundary:** none in this local inventory increment. **Blocker/Unblocker:** task 4.1 terminal and no known reachable critical/non-deferrable defect. **Observable Proof:** validation is green, the checked-in budget matches derived source facts, and no handoff text claims exact prompt or provider token cost. **Owner Authorization:** local validation only. **Safeguards:** no install, activation, publication, release, or remote mutation. **Restoration/Cleanup:** preserve validation logs. **Expected Immutable Evidence:** validation transcript and Candidate Reference. **Validation:** `npm run validate:strict`, focused inventory/materialization checks, full test suite, and `openspec validate measure-loader-visible-instruction-budget --strict`. **Dependency-Chain Stop Condition:** red validation or unresolved privacy/data-integrity risk prevents RC/stable claims.

## 5. Final History Retrospective

- [x] 5.1 After every other currently known task is complete, analyze the complete `history.md` once using the canonical six-cell `Quality`, `Cycle Speed`, and `Token Economy` matrix across `Working Repository` and `opencode-kit`; classify each evidence-backed candidate by Impact Horizon, Concrete Consumers, Execution Class, Earliest Safe Point, Invalidated Evidence, and Observable Payback; append and immediately execute every admitted exact-current-consumer improvement under `## Session-Derived Improvements`, preserve no-current-consumer candidates as non-checkbox deferred history records, or record `none` without inventing work. Apply, archive, compaction, and generated tasks MUST NOT create or rerun this final-history analysis.

## Session-Derived Improvements

- [x] SDI-1 Reuse the maintained portable process capture in the inventory proof runner. **Impact Horizon:** opencode-kit. **Concrete Consumers:** tasks 1.1, 1.2, and 3.1 in this change require installed inventory calls and immutable proof bundles; `global/bin/portable-process.ts::runPortableCommand` is the existing owner already used by maintained proof runners. **Execution Class:** before-task-3.1. **Earliest Safe Point:** while implementing the provider-free proof runner, before its first installed package-entry capture. **Invalidated Evidence:** runner mutation invalidates only captures driven through that runner; evaluator-only changes require replay against preserved raw output. **Observable Payback:** Windows package shims resolve on the first process attempt, complete stdout/stderr and exit facts remain in the raw bundle, and concise deterministic facts avoid placing full inventory output in operator context. **Trigger/Evidence:** predecessor change `reduce-inventory-tooling-duplication` DIC-2 records two failed direct Node spawn attempts (`npm` undefined streams and `npm.cmd` `EINVAL`) before host-native PowerShell succeeded, then verifies the portable owner and existing consumers. **Why:** this change is the named future owner with exact remaining proof consumers, so deferral is no longer valid and reuse avoids another process abstraction. **Prerequisites:** define create-new raw evidence, evaluator readback, environment identity, and deterministic cleanup. **Scope/Non-Goals:** do not change `runPortableCommand`, suppress raw evidence, add another process abstraction, call providers, or mutate target projects. **Implementation:** call `runPortableCommand` with capture enabled, persist privacy-safe argv/status/stdout/stderr and source identities, and derive concise verdict facts in a separate evaluator path. **Observable Proof:** installed catalog, loader-visible, and budget commands execute through the portable owner on Windows; raw outputs and evaluator facts agree; replay performs no product command. **Validation:** proof-runner help, integrated provider-free proof, offline evaluator replay, cleanup verification, and `git diff --check`.

- [x] SDI-2 Remove the proof runner's process-global fixture-path handshake. **Impact Horizon:** Working Repository. **Concrete Consumers:** task 4.2 requires a final deterministic capture/readback from `tools/proofs/instruction-inventory-budget.ts`; `buildFixture` already returns the other proof-only paths consumed by the same capture. **Execution Class:** before-task-4.2. **Earliest Safe Point:** before the final task 4.2 capture/readback, after terminal SDET because this is runner-only and does not mutate product behavior or tests. **Invalidated Evidence:** the runner mutation invalidates captures whose child environment it constructs; preserved raw product observations remain trustworthy, but the maintained capture lane must be rerun and replayed. **Observable Payback:** capture no longer mutates parent `process.env`, so concurrent or failed proof setup cannot leak fixture routing into later commands; raw output remains path-redacted. **Trigger/Evidence:** read-only reduction review `ses_ffef4399effeKCZw53EodbRdet` found three proof-only `process.env` writes and two delete blocks used only to pass values from `buildFixture` to the immediately following child env. **Why:** hidden process-global mutation is introduced by this change and can make proof behavior order-dependent, while an existing local return object removes it without a new abstraction. **Prerequisites:** preserve the same child `HOME`, `USERPROFILE`, `OPENCODE_CONFIG`, `OPENCODE_CONFIG_DIR`, and inline config values; keep fixture paths out of raw expected output. **Scope/Non-Goals:** do not change product code, test or seed semantics, command argv, `runPortableCommand`, evidence schema, or accepted oracles. **Implementation:** return the three paths from `buildFixture`, consume them directly in the capture env, and strip them alongside `budgetRoot` and `project` before persistence. **Observable Proof:** a new provider-free capture and offline replay pass all 17 facts, persist no absolute fixture/source paths, and clean the fixture. **Validation:** new capture/replay, strict validation, scoped `git diff --check`, and source-hash readback.
