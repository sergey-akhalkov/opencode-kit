## Shared Execution Envelope

All tasks inherit the local/disposable, non-remote envelope from `design.md`: preserve unrelated active changes, serialize overlapping writers, retain protected-action and live-attempt gates, capture privacy-safe command/model evidence, and clean only attributable fixtures. A task states a local delta only when its boundary differs.

## 1. Freeze Baseline And Consistency Owner

- [x] 1.1 Freeze the current source/diff ownership, loader-visible context classes, active/template prompt drift, doctor self-check, and matched Ordinary Small, Material, protected-boundary, blocker, and grind scenarios. Observable completion: one privacy-safe baseline bundle reproduces the spec/validator contradiction and current model-call/permission behavior without mutating active changes. Validation: inventory, doctor, current strict validation, and offline baseline evaluator all terminate with attributable output. Evidence: `evidence-task-1-1-baseline-index.md` and its indexed immutable bundles; the zero-live-call replay completes all five rows, provider-free inventory/doctor/permission evaluations are terminal, strict validation exits `0`, cleanup is complete, and the unsafe full-source snapshot attempt is removed and recorded in `history.md`.
- [x] 1.2 Add one deterministic effective-contract consistency owner used by strict validation and doctor, including active delta additions/modifications/removals. Observable completion: a fixture with an unresolved require/forbid conflict fails with both paths, while this change's explicit removal delta resolves the historical retrospective conflict. Validation: focused validator/doctor checks and strict validation over controlled fixtures. Evidence: `evidence-task-1-2-contracts-r2/{raw,evaluation}.json`; the provider-free fixture observes added/modified/removed operations, unresolved conflict paths, resolved removal, complete cleanup, and `pass=true`; `npm.cmd run validate:strict` exits `0`; kit doctor reports `workflow contract consistency: pass`.

## 2. Remove OpenSpec Completion Ceremony

- [x] 2.1 Remove final-history, pending/deferred improvement, and generated process-task semantics from maintained global authority, compaction, propose, apply, archive, context/rules, validators, and docs; preserve strategy history only for materially distinct attempts and live-attempt evidence. Observable completion: fresh proposal and complete archive fixtures contain no retrospective task and complete from accepted tasks plus gates. Validation: propose/apply/archive contract checks and disposable CLI proof. Evidence: source, operation-gate 11/11, strict validation, selected OpenSpec validation, and `evidence-task-2-1-controller-r1` are green; the stale `Process Improvement` test oracle is removed, focused contracts pass, and the full `npm test` suite exits zero.
- [x] 2.2 Replace repeated per-task questionnaires with the shared change envelope plus task-specific deltas in OpenSpec context, templates, examples, and validators. Observable completion: a generated multi-task change is implementation-ready with one shared envelope, compact tasks, and a stronger task-local gate only where required. Validation: artifact-instruction readback, strict fixture validation, and token comparison against the prior task form. Evidence: `evidence-task-2-source-r1.md`; current task instructions emit one shared context and compact task rules, strict validation is green, and token proxy per task drops from 378 to 139 in the representative prior/current forms.
- [x] 2.3 After all overlapping writers are closed, reconcile only compatible active changes that still carry the removed retrospective as process-only scope; do not alter archived evidence or another change's accepted product tasks. Observable completion: each touched active change either drops the process-only checkbox with preserved history or records an exact unresolved ownership blocker. Validation: selected strict OpenSpec validation and scoped diff review. Evidence: current-session writer readback found no live mutation-capable child; source identities were recaptured; only mission task 7.2 was removed and its completed proof-correction section renamed; every product task and archived artifact remains intact; strict mission validation is green.

## 3. Compact Runtime Authority And Lifecycle

- [x] 3.1 Reduce `global/AGENTS.md` to compact universal routing/safety and move detailed Material qualification to `change-ready-sdlc`; convert roles, templates, UDL, README, and validators to canonical-owner pointers or deltas. Observable completion: committed global authority is at or below 13,279 token proxy and matched loaded scenarios preserve every frozen safety oracle. Validation: instruction inventory/budget, source readback, baseline/candidate evaluation, and fresh loader proof. Evidence: token proxy remains below the target; strict validation and focused contracts pass; `evidence-task-3-routing-philosophy-r3.md` records current source identities and fresh loaded behavior.
- [x] 3.2 Implement verified-outcome reporting for Ordinary Small and qualification-scoped Development/MVP/RC/stable semantics; update handoff, merge-request, validator, and documentation contracts coherently. Observable completion: the Ordinary Small scenario finishes with outcome/proof/validation and no RC fields, while explicit qualification retains monotonic stage and external-operation separation. Validation: focused contract checks and matched loaded scenarios. Evidence: `evidence-task-3-routing-philosophy-r3.md` records `Outcome` plus proof/validation/limitations with no SDET/stage/RC for Ordinary Small, and qualification-only stage, monotonic RC, and external-operation separation.
- [x] 3.3 Implement named-risk SDET triggering and post-proof main ownership of focused regression tests while preserving fresh independent critical SDET when triggered. Observable completion: a non-critical Material scenario completes without SDET, a reachable security scenario requires it, and neither path grants reviewer mutation or lifecycle authority. Validation: loaded routing scenarios and role-permission readback. Evidence: `evidence-task-3-routing-philosophy-r3.md` records no SDET for non-critical Material, fresh test-only non-authorizing SDET for reachable authorization compromise, and installed permission proof `outcome: pass`.
- [x] 3.4 Encode the working philosophy at the start of global runtime authority and as compact deltas in relevant maintainer, project, UDL, OpenSpec, tuning, and workflow guidance; enforce smallest-authorized-layer correction or removal of concrete impediments without weakening safety or scope. Observable completion: source and loaded behavior retain quality without proxy substitution, shortest verified path, autonomy until a real owner boundary, maximum token economy, evidence-backed continuous improvement, and proactive impediment removal. Validation: deterministic marker checks, instruction budget, strict validation, and matched loaded scenarios. Evidence: the first-section validator and marker contract pass; `evidence-task-3-routing-philosophy-r3.md` reports philosophy, priority, current-outcome, proof-gate, unrelated-scope, and owner-boundary oracles.

## 4. Make Context Budgets Truthful

- [x] 4.1 Version instruction budget data and tooling for separate global-startup, discovery-metadata, and on-demand-body maxima; prohibit materializer increases and retain privacy-safe loader-visible measurement. Observable completion: growth above a maximum fails without seed mutation, lowering succeeds, and catalog bodies are never reported as startup cost. Validation: focused budget/inventory checks, malformed-seed cases, and strict validation. Evidence: schema-v2 seed boundaries pass at startup `13,183/13,279`, discovery `2,132/2,239`, and on-demand `61,420/66,244`; focused inventory tests and strict validation pass; `evidence-task-4-1-budget-r1` plus provider-free replay report every separation, redaction, lowering, growth-rejection, malformed-seed, determinism, and cleanup fact true.

## 5. Preserve Permission Precedence

- [x] 5.1 Remove completion-guard top-level permission mutation and update config/runtime diagnostics while retaining explicit portable `permission: "allow"` only where configured. Observable completion: loaded disposable ask/deny/allow configurations remain unchanged by guard enablement and specialist denies remain effective. Validation: focused permission contracts plus actual loaded plugin/config readback. Evidence: `evidence-task-5-1-permissions-r1/{raw,evaluation}.json` records installed OpenCode `1.18.18` preserving final-merge `allow`, `ask`, and `deny`, 189 explicit specialist denies, all 13 arbiter tools disabled, and complete status; `npm.cmd run test:focused:session-completion-guard` passes `35/35`; strict selected OpenSpec validation is green.

## 6. Add Certified Deterministic Completion

- [x] 6.1 Extend existing guard metadata/epoch and mission structured-result contracts with the versioned terminal certificate and configured issuer policy, without a new dependency or top-level service. Observable completion: provider-free valid, forged, stale-revision, stale-lease, missing-requirement, pending-question, and unknown-issuer cases produce the specified terminal or fallback state. Validation: existing guard/mission proof runners and evaluator replay. Evidence: focused guard tests pass `36/36`, including exact valid/forged/stale-revision/stale-lease/missing-requirement/pending-question/unknown-issuer/unsafe-evidence/disposition cases; `evidence-task-6-1-contract-r1` completes the existing mission preflight; the installed captures and zero-live-call replays below validate the same structured result parser.
- [x] 6.2 Integrate certificate issuance at the existing deterministic mission verification boundary and handle certificate/audit races through current epoch cancellation. Observable completion: one installed certified root reaches passed with zero arbiter calls, while an otherwise identical ambiguous root invokes the bounded arbiter and unresolved/owner-required roots never short-circuit. Validation: disposable installed runtime capture, call-count/effect assertions, preserved-bundle replay, and cleanup readback. Evidence: current `evidence-task-6-2-certified-r5` reports deterministic requirements/strict validation, one primary call, zero arbiter calls, `passed`, `completed`, and complete cleanup; `evidence-task-6-2-ambiguous-r2` reports no certificate and exactly one arbiter; `evidence-task-6-2-owner-required-r2` reports no certificate, one arbiter, terminal owner-required exit `3`, and complete ownership cleanup. Their `*-replay-*` successors are all complete without live calls. Startup/race failures and causal corrections remain preserved in `history.md` and the immutable r1-r4 bundles.

## 7. Correct Doctor, Drift, And Evidence Consumption

- [x] 7.1 Add contract-based kit self-identification to doctor while retaining consumer AGENTS/adapter gates. Observable completion: kit checkout qualification no longer fails on intentional layout, a similarly named unbootstrapped consumer remains blocked, and real workflow conflicts block both correctly. Validation: focused doctor fixtures and actual kit/consumer CLI reports. Evidence: the focused repository-contract fixture passes kit/consumer plus require/forbid conflict cases; `evidence-task-7-doctor-drift-r1` completes the maintained doctor proof with exact self-hosted package/authority/script classification, consumer-safe basename control, qualification exit assertions, no project validation execution, and complete cleanup; the actual checkout exits `0` for `--require qualification`.
- [x] 7.2 Add privacy-safe template/active managed-prompt drift status and a bounded evidence manifest/index contract without printing prompt or raw evidence content. Observable completion: known compaction-prompt drift reports `different`, same/missing/unknown fixtures classify correctly, and continuation resolves a named lane from the index without reading unrelated bundles. Validation: source-inventory/doctor checks and evidence-index fixture readback. Evidence: runtime-source and doctor reports classify the current managed field as `different` using only SHA-256 and marker IDs; focused same/different/missing/unknown and unrelated-lane fixtures pass; `evidence-index.json` resolves all eight named lanes through `tools/evidence-index.ts`, with the task-7 readback recorded in `evidence-task-7-index-r1/evaluation.json`; strict change validation passes.

## 8. Prove Complete Candidate And Migrate Tests

- [x] 8.1 Complete remaining source, validator, installer, docs, proof-inventory, and active-config synchronization for one readable candidate, then run the full matched installed workflow matrix. Observable completion: every delta-spec scenario has terminal evidence, startup/task token reductions are observed, safety behavior is no weaker, and no unrelated active-change byte is overwritten. Validation: affected proof replay, fresh installed loader, and scoped diff/source identities. Evidence: `evidence-task-8-1-scenario-matrix-r1.md` maps all 40 delta scenarios to terminal evidence for candidate `reduce-workflow-ceremony-task-8-r2`; the five frozen configured-model rows report baseline/candidate complete with cleanup removed; current task-3 runtime hashes preserve Ordinary Small/Material/qualification routing; fresh permissions and doctor bundles complete; provider simulation r7 and controller r1 complete two archives with one structured transient recovery, zero retrospective tasks, protected stop, and complete cleanup; active managed prompt and certificate options are synchronized with ignored rollback preimage `global/opencode.local.json`, while no running owner session was restarted. Current budgets are startup `13,183/13,279`, discovery `2,132/2,239`, on-demand `61,420/66,244`, and representative task form `378 -> 139`; installer `27/27`, OpenSpec gates `11/11`, library `155/155`, strict validation, selected strict OpenSpec validation, nine-lane index readback, and scoped diff check pass. No consumer, remote, commit, release, deployment, or unrelated active-change mutation occurred.
- [x] 8.2 Dispatch one fresh test-only SDET for the reachable critical permission-precedence and forged/stale-certificate incidents; have main reproduce every row and correct only confirmed defects before renewed proof. Observable completion: terminal critical-risk report and smallest retained critical test oracles, with no production edit by SDET. Validation: SDET-authored focused tests, main reproduction, and affected runtime reproof. Evidence: fresh test-only SDET `ses_fdee99975ffeqVwBbaoowsHr0O` (`xai/grok-4.6`) returned terminal `no-critical-risk` for `SCG-PERM-WIDEN-001` and `SCG-CERT-SHORTCIRCUIT-001` on candidate `reduce-workflow-ceremony-task-8-r2`. It retained only issuer-mismatch plus explicit rejected/null-certificate assertions in `tools/test-session-completion-guard.ts` and changed no production file. Main inspected the diff, reran the focused guard suite `36/36`, and reproduced permission precedence through fresh `evidence-task-8-1-permissions-r1`; valid certified, ambiguous, and owner-required installed lanes remain terminal. `evidence-task-8-2-sdet-r1/evaluation.json` records the disposition.

## 9. Complete Validation And Handoff

- [x] 9.1 Run focused suites, `npm test`, `npm run validate:strict`, instruction inventory/budget checks, doctor gates, applicable guard/mission proofs, `openspec validate reduce-workflow-ceremony --strict`, `openspec validate --all --strict`, and `git diff --check` on the same candidate. Observable completion: every applicable command exits zero, no normative/runtime contradiction remains, no known reachable critical or non-deferrable defect remains, and the handoff records limitations, rollback/restart, installed-sync state, and no unauthorized external operation. Evidence: `evidence-task-9-1-validation-r1/evaluation.json`; full `npm test`, strict library validation (`skills=29`, `agents=18`, `markdown=517`, warnings `0`), structural and qualification doctor gates, selected strict validation, all OpenSpec validation `17/17`, ten-lane readback, and `git diff --check` pass. Current budget remains startup `13,183/13,279`, discovery `2,132/2,239`, on-demand `61,420/66,244`; current prompt drift is `same`; terminal fresh SDET reports `no-critical-risk`. Candidate freezes as `RC1`. The current owner process was not restarted; fresh disposable OpenCode processes loaded the synchronized source. Rollback preimage remains ignored at `global/opencode.local.json`. No archive, commit, push, consumer migration, deployment, release, or other unauthorized external operation occurred.
