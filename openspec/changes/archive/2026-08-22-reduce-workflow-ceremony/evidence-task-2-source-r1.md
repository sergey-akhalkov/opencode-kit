# Tasks 2.1-2.3 Source Evidence

Candidate: `reduce-workflow-ceremony-task-2-r1`

## Source Result

- Maintained propose/apply/archive skills and commands, `openspec/config.yaml`, and `openspec/project.md` already prohibited mandatory retrospective/product-task expansion.
- Removed the canonical loop's numbered `Process Improvement` completion step and corresponding exact-marker validation.
- Replaced current reflection/improvement-matrix documentation with continuity and live-attempt oracles only.
- Removed the retrospective from the maintained roadmap provider's generated beta fixture.
- Reconciled `add-autonomous-roadmap-mission-runtime` after source-hash and current-session writer readback: renamed the completed proof-correction section and removed only unchecked process-only task 7.2. Completed proof corrections and every product task remain unchanged.

## Proof And Validation

- `evidence-task-2-1-controller-r1/`: provider-free disposable controller/archive proof reports `status=complete` with terminal cleanup.
- `npm.cmd run test:focused:openspec-gate`: 11/11 pass.
- `npm.cmd run validate:strict`: exits `0`, warnings `0`.
- `openspec.cmd validate reduce-workflow-ceremony --strict`: valid.
- `openspec.cmd validate add-autonomous-roadmap-mission-runtime --strict`: valid after process-only task removal.
- `openspec.cmd instructions tasks --change reduce-workflow-ceremony --json`: shared seven-field context is emitted once; task rules request dependency, observable result, and focused validation without repeated authorization/cleanup fields.
- Prior repeated task form: 16 tasks, token proxy 6039, 378 per task. Shared-envelope form: 17 tasks, token proxy 2358, 139 per task.
- `npm.cmd run test:focused:contracts`: one stale automated oracle fails because it still expects `Process Improvement` as step 12; all other contract rows pass. Current loaded Material policy reserves this test-only migration until post-proof fresh SDET, so task 2.1 remains unchecked.

No archive, consumer migration, remote operation, model call, or unrelated source mutation occurred.
