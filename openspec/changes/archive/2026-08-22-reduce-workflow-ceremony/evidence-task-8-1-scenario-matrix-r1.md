# Task 8.1 Complete Candidate Matrix

Candidate: `reduce-workflow-ceremony-task-8-r2`

The current preflight source identities retain the exact `global/AGENTS.md` and `change-ready-sdlc` hashes inspected by the task-3 loaded routing matrix. The five frozen blocker/completion scenarios were freshly captured after active-config synchronization and reevaluated under this candidate. Older slice evidence is reused only where its production source is unchanged; affected mission, permission, doctor, config, and OpenSpec paths have fresh task-8 evidence.

| Capability | Scenario | Terminal evidence |
|---|---|---|
| change-ready-sdlc | Bounded local fix is complete | Task-3 `ordinary_small`; task-8 matched `outcome-achieved`; current source hashes in `evidence-task-8-1-matched-preflight-r2`. |
| change-ready-sdlc | Material change has no qualification request or critical-risk trigger | Task-3 `material_noncritical`; current global/skill hash continuity in matched preflight r2. |
| change-ready-sdlc | Project requires qualification | Task-3 `explicit_qualification`; focused contracts `69/69`. |
| change-ready-sdlc | Material change has no reachable critical incident | Task-3 `material_noncritical.require_sdet=false`; current hash continuity. |
| change-ready-sdlc | Security boundary is reachable | Task-3 `security_critical` routes fresh test-only SDET; task 8.2 owns the actual challenge. |
| change-ready-sdlc | Ordinary regression is reproduced after proof | Current `global/AGENTS.md` and qualification skill source readback; focused contracts `69/69`. |
| change-ready-sdlc | Optional workflow improvement appears after proof | Current global compaction/feedback source readback plus strict validation. |
| change-ready-sdlc | Accepted behavior remains incomplete | Task-8 matched `checked-unmet` continues outcome work; OpenSpec gate `11/11`. |
| change-ready-sdlc | A real boundary requires physical access | Task-3 protected-action oracle plus task-8 OpenSpec simulation protected stop before executor. |
| instruction-artifacts | Product work completes without process retrospective | `evidence-task-8-1-openspec-simulate-r7`: two archives and `retrospectiveTasks=0`. |
| instruction-artifacts | Repeated workflow loss receives an explicit owner | Current `complain` contract source and focused contracts `69/69`. |
| instruction-artifacts | Spec requires behavior that validator forbids | `evidence-task-1-2-contracts-r2`; task-7 doctor conflict fixture. |
| instruction-artifacts | Historical proof directory contains many captures | `evidence-index.json`; `evidence-task-7-index-r1`; all named lanes resolve. |
| instruction-artifacts | Canonical loop ends product completion before optional feedback | Current 11-step UDL source, explicit MODIFIED delta, strict selected OpenSpec validation. |
| instruction-artifacts | Compact routing replaces Material detail | Current startup proxy `13,183/13,279`; task-3 loaded matrix with current hash continuity. |
| instruction-artifacts | A workflow impediment conflicts with the working philosophy | Task-3 impediment-boundary oracles reject weaker safety and unrelated cleanup. |
| instruction-artifacts | On-demand bodies remain large | Current budget reports on-demand `61,420/66,244` separately from startup. |
| instruction-artifacts | Several tasks share one local proof envelope | Current OpenSpec task instructions and this change's shared envelope; task-2 compact-form evidence. |
| instruction-artifacts | A task crosses a different boundary | This change's task 8.2 names the stronger fresh SDET boundary; task-8 simulation preserves the protected stop. |
| instruction-artifacts | Startup authority exceeds the compact maximum | Task-4 growth fixture fails closed; current measured boundary remains below maximum. |
| instruction-artifacts | Materializer encounters growth | Task-4 materializer growth-rejection and no-mutation evidence. |
| instruction-artifacts | Loader-visible consumer has machine-local instructions | Task-4 loader-visible inventory fixture keeps machine-local source separate and seed unchanged. |
| spec-workflow-integrity | Proposal establishes the shared envelope | Current `openspec instructions tasks` output plus task-2 source evidence. |
| spec-workflow-integrity | Task has no boundary delta | Current compact task rules; representative prior/current token proxy remains `378 -> 139` per task. |
| spec-workflow-integrity | Edit without proof remains incomplete | Task-8 matched `checked-unmet`; controller unchecked-completion rejection. |
| spec-workflow-integrity | Successful task evidence permits completion | Task-8 matched `outcome-achieved`; task-8 simulation completes and archives two proven slices. |
| spec-workflow-integrity | Optional process observation does not become scope | Current apply/config source readback; task-8 archives contain zero retrospective tasks. |
| spec-workflow-integrity | Unchecked accepted task blocks complete archive | OpenSpec gate `11/11`; controller incomplete-task oracle. |
| spec-workflow-integrity | No retrospective exists | Task-8 simulation reports `retrospectiveTasks=0`. |
| session-completion-guard | Owning workflow certifies terminal completion | `evidence-task-6-2-certified-r5` plus terminal replay: one primary, zero arbiter, passed. |
| session-completion-guard | Certificate is stale or mismatched | Focused guard `36/36`, including forged/stale revision/stale lease/missing requirement/unknown issuer. |
| session-completion-guard | Root remains ambiguous | `evidence-task-6-2-ambiguous-r2` and replay: no certificate, exactly one arbiter. |
| session-completion-guard | Project main permissions require ask | Fresh `evidence-task-8-1-permissions-r1` preserves allow/ask/deny precedence. |
| session-completion-guard | Hidden arbiter remains denied | Fresh permission proof reports all arbiter tools disabled and specialist restrictions preserved. |
| config-portability | Portable template remains permissive | Fresh permission proof resolves configured top-level allow without guard mutation. |
| config-portability | Consumer narrows permissions | Fresh permission proof preserves configured ask and deny. |
| config-portability | Active compaction prompt differs from template | Task-7 observed `different`; explicit task-8 sync now reports `same` with retained preimage and fresh loaded processes. |
| install-init-resilience | Doctor inspects the kit checkout | Fresh `evidence-task-8-1-doctor-r1`: qualification pass through self-hosted contract. |
| install-init-resilience | Doctor inspects an unbootstrapped consumer | Fresh doctor proof and focused basename-mimic fixture remain blocked. |
| install-init-resilience | Individually valid artifacts contradict each other | Task-1.2 consistency proof and focused kit/consumer conflict reports block qualification. |

## Candidate Results

- Frozen baseline/candidate evaluator: `baselineComplete=true`, `candidateComplete=true`, five rows, fresh capture cleanup removed.
- Fresh process: five configured-model captures completed after managed-config synchronization; current owner session was not restarted.
- OpenSpec: provider simulation r7 and controller r1 complete; two synthetic archives, two deterministic execution calls, one structured transient recovery, three controller processes, zero retrospective tasks, protected slice blocked before executor, cleanup complete.
- Permissions: loaded allow/ask/deny precedence and specialist/arbiter restrictions complete.
- Doctor: self-hosted kit and consumer-safe controls complete; cleanup complete.
- Installer: focused suite `27/27`; existing local config remains byte-preserved while missing config is materialized from the template.
- Context: startup `13,183/13,279`, discovery `2,132/2,239`, on-demand `61,420/66,244`; task form `378 -> 139` token proxy per task.
- Live-Attempt Gate: clear. The configured-model capture, provider-free evaluator, and cleanup are terminal; failed OpenSpec simulation attempts made zero configured-provider calls and the corrected provider-free lane reaches its terminal evaluator.
