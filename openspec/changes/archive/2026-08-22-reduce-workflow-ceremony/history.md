# Strategy History

## 2026-08-20 - Keep a shorter mandatory retrospective

- **Objective:** Reduce completion cost without changing the current final-history requirement.
- **Approach:** Retain the retrospective and generated improvement task but remove fields and shorten the prompt.
- **Evidence:** The normative specs would still create new process scope after product completion, while `tools/validators/devkit-contract.ts` already forbids that task and strict validation does not expose the contradiction.
- **Outcome:** Rejected in favor of removing mandatory retrospective completion scope.
- **Reason:** Fewer fields reduce text but do not remove the extra model pass, full-journal ingestion, or completion dependency.
- **Do-Not-Repeat Condition:** Do not reintroduce a required final retrospective, pending-improvement task, or equivalent renamed completion gate.
- **Evidence-Based Retry Condition:** Only an explicit owner outcome that requires a bounded retrospective artifact as product evidence, with measured benefit over the direct completion path.

## 2026-08-20 - Split global authority into more always-loaded files

- **Objective:** Bring `global/AGENTS.md` below its token target while preserving all current wording.
- **Approach:** Move sections into additional files that are still loaded at startup.
- **Evidence:** Loader-visible inventory separates startup authority from discovery and on-demand bodies; moving text among always-loaded files leaves startup cost and precedence complexity unchanged.
- **Outcome:** Rejected in favor of compact routing plus lazy `change-ready-sdlc` detail.
- **Reason:** File movement alone does not reduce runtime context or duplicated authority.
- **Do-Not-Repeat Condition:** Do not claim a context reduction from relocating unchanged policy into another startup-loaded artifact.
- **Evidence-Based Retry Condition:** Loader evidence proves the moved artifact is genuinely on-demand and matched scenarios preserve all named safety oracles.

## 2026-08-20 - Preserve universal RC and SDET bookkeeping

- **Objective:** Reduce handoff friction while retaining the current qualification path for every behavior change.
- **Approach:** Keep Development/MVP/RC/stable and mandatory fresh SDET, but shorten their reports or widen SDET tool access.
- **Evidence:** Ordinary Small already has main-owned proof and focused validation, while independent critical challenge is useful only for a reachable critical hypothesis or explicit qualification requirement.
- **Outcome:** Rejected in favor of verified-outcome reporting and named-risk SDET triggering.
- **Reason:** Shorter reports do not remove unnecessary stage transitions or independent model runs and would preserve artificial test ownership.
- **Do-Not-Repeat Condition:** Do not make RC numbering or SDET mandatory solely because code/config changed or Material routing was selected.
- **Evidence-Based Retry Condition:** Project policy, owner request, or a concrete reachable critical consequence explicitly requires qualification or independent challenge.

## 2026-08-20 - Infer deterministic completion from transcript markers

- **Objective:** Avoid an arbiter call for obviously completed grind roots.
- **Approach:** Stop the root when transcript text, task markers, or an executor exit appears terminal.
- **Evidence:** Those signals do not prove accepted requirement identity, current revision/lease ownership, pending questions, or writer closure; the existing guard already owns correlated epochs and idle preflight.
- **Outcome:** Rejected in favor of a versioned terminal certificate issued by an existing deterministic workflow owner.
- **Reason:** Uncorrelated markers can be stale, forged, incomplete, or race the active audit.
- **Do-Not-Repeat Condition:** Do not bypass arbitration from prose, unchecked command exit, or marker presence alone.
- **Evidence-Based Retry Condition:** A future trusted owner exposes stronger structured facts that can be correlated through the same fail-closed preflight without adding another scheduler or arbiter.

## 2026-08-20 - Keep guard permission override behind enablement

- **Objective:** Preserve unattended grind behavior while reducing surprise from unconditional permission mutation.
- **Approach:** Force top-level `permission: "allow"` only after the operator enables grind mode.
- **Evidence:** `applyPermissionAllow` replaces merged policy, including project and specialist restrictions; completion-mode enablement is not authorization to widen security policy.
- **Outcome:** Rejected in favor of preserving normal OpenCode permission precedence and reporting denied capabilities.
- **Reason:** A mode switch cannot safely override explicit project, inline, managed, or specialist denies.
- **Do-Not-Repeat Condition:** Do not make the guard approve, replace, or persist permission policy.
- **Evidence-Based Retry Condition:** Only a separately accepted permission-policy change with explicit owner semantics and loaded precedence proof.

## 2026-08-20 - Satisfy kit doctor with duplicate consumer files

- **Objective:** Remove the kit checkout's false qualification blockers with minimal doctor changes.
- **Approach:** Add a root `AGENTS.md` and consumer adapter solely to satisfy current path checks.
- **Evidence:** The kit intentionally owns authority through `REPO_AGENTS.md`, `global/AGENTS.md`, the qualification skill, and package validation scripts; duplicating those owners would add loader-visible authority and drift risk.
- **Outcome:** Rejected in favor of contract-based kit/consumer classification.
- **Reason:** The workaround hides a faulty diagnostic and creates another source of truth.
- **Do-Not-Repeat Condition:** Do not add duplicate instruction or validation owners merely to make doctor green.
- **Evidence-Based Retry Condition:** Repository identity evidence changes so the kit no longer has a distinct self-hosted contract.

## 2026-08-20 - Full working-tree source snapshot inside change evidence

- **Objective:** Freeze the exact pre-mutation instruction source for matched baseline/candidate model scenarios.
- **Approach:** Run the existing `pre-escalation-recovery.ts --mode stage-source --git-ref working-tree` helper with an evidence root inside this active change.
- **Evidence:** The provider-free helper exited successfully, but the next `npm run validate:strict` discovered copied `global/opencode.json` and `global/opencode.json.template` under the evidence root and exited non-zero with two permission warnings. The snapshot could include machine-local config, so it was removed without content inspection; the three independent baseline proof bundles remained green.
- **Outcome:** Rejected as an unsafe and validation-polluting baseline mechanism. No Product Candidate behavior was exercised or changed.
- **Reason:** The working-tree snapshot owner copies loader/config files rather than producing a privacy-safe source manifest, and repository validators recursively inspect the resulting duplicate tree.
- **Do-Not-Repeat Condition:** Do not place a full working-tree stage-source result under repository evidence and do not preserve a copied machine-local config as proof.
- **Evidence-Based Retry Condition:** Use source-derived privacy-safe hashes and bounded manifests only, or first change the existing stage-source owner to exclude ignored/machine-local config and prove the exclusion in a disposable root outside the repository.

## 2026-08-20 - Pure CLI loaded-routing proof

- **Objective:** Prove the working philosophy and proportional Ordinary Small, non-critical Material, critical-security, and explicit-qualification routing through one fresh loaded OpenCode process.
- **Approach:** Start `opencode run` in a new disposable Git root with the current `global/` source, configured `openai/gpt-5.6-sol` route, one fixed synthetic read-only matrix prompt, `--pure`, and no authorized tool or external effect.
- **Evidence:** Session `ses_fdfc3cb93ffeJQptc584UJKskU` failed before provider usage with `Unexpected server error` reference `err_3f276710`; exported session facts showed model `openai/gpt-5.6-sol`, OpenCode `1.18.18`, zero cost, zero input/output/reasoning tokens, and only the synthetic user message. The failed session and proof-owned fixture were deleted after readback.
- **Outcome:** Rejected as candidate behavior evidence; no model answer or product mutation occurred.
- **Reason:** Exact server cause is unknown because the invocation did not enable runtime logs. A plausible but unproved cause is that `--pure` removed a runtime dependency needed by the configured route.
- **Do-Not-Repeat Condition:** Do not repeat the same pure/no-log invocation or infer instruction failure from the pre-provider error.
- **Evidence-Based Retry Condition:** Use one causally distinct fresh process without `--pure`, enable runtime logs, retain the same synthetic oracle, and preserve provider/token/session plus cleanup facts.

## 2026-08-20 - Certified runtime capture r1 readiness timeout

- **Objective:** Prove a deterministically certified roadmap root through the installed disposable OpenCode server with zero arbiter calls.
- **Approach:** Run the existing `roadmap-mission-runtime` capture in `continue` mode against the current candidate and a local provider simulator.
- **Evidence:** `evidence-task-6-2-certified-r1/failure.json` records a bound loopback server whose `/path` and `/session/status` probes timed out during configured-plugin initialization, zero primary or arbiter calls, no executor launch, and complete fixture/process cleanup. `npm.cmd run validate:strict` remained green, and direct Bun imports of the guard, executor, and certificate module succeeded.
- **Outcome:** Inconclusive Proof Runner/Environment startup failure; no Product Candidate certificate behavior was reached.
- **Reason:** The preserved diagnostics prove neither a plugin exception nor a certificate defect; the process stayed alive but emitted no post-config readiness evidence before the existing deadline.
- **Do-Not-Repeat Condition:** Do not treat r1 as certificate failure and do not immediately repeat the identical `continue` capture or merely increase its timeout.
- **Evidence-Based Retry Condition:** First run a causally distinct existing startup/control scenario with the same isolated server and plugin config. Repeat certified capture only if that control reaches readiness or new diagnostics identify and correct the exact startup cause.
- **Successor-Unlock Evidence:** The `interrupted` control reproduced the identical bootstrap stall with zero executor/provider calls and complete cleanup. Read-only troubleshooter task `ses_fdf691777ffeXNlhtblvkDLkL4` then identified the awaited config-hook diagnostic; direct source readback confirms `this.log` uses the bootstrapping server's `client.app.log`. The candidate now defers that diagnostic without awaiting it, removing the re-entrant dependency while preserving configured-policy reporting.
- **Next Distinct Check:** Run the existing interrupted startup/control once against the corrected candidate. A responsive `/path` and `/session/status` unlocks one new certified capture; another identical stall blocks repetition and requires loader-level instrumentation.

## 2026-08-20 - Certified runtime capture r2 remains settling

- **Objective:** Prove accepted deterministic completion after the corrected startup control reached readiness.
- **Approach:** Run one certified `continue` capture with a complete disposable OpenSpec task set and the configured roadmap issuer.
- **Evidence:** `evidence-task-6-2-certified-r2/raw.json` records deterministic apply verification `remaining=0`, exact requirement match, strict OpenSpec validation status `0`, one issued root/revision/lease-bound certificate, one primary call, zero arbiter calls, terminal executor timeout at guard state `settling-idle`, and complete session/process/fixture cleanup. Provider-free replay in `evidence-task-6-2-certified-replay-r2` reaches the same blocked evaluator result without another live invocation.
- **Outcome:** Certificate issuance works, but guard terminal application remains unobserved.
- **Reason:** The preserved root terminal poll omitted the guard's privacy-safe certificate status/reason, so it cannot distinguish an un-rechecked challenge from rejection or expiry.
- **Do-Not-Repeat Condition:** Do not repeat r2 without additional state discrimination and do not alter certificate correlation that already matched.
- **Evidence-Based Retry Condition:** Preserve the guard certificate status and reason in the existing timeout diagnostic, then run one bounded successor to identify or clear the post-issuance state transition.
- **Later Startup Evidence:** Certified r3 reached terminal `passed`, but the corrected-certificate-race r4 candidate again stalled during configured-plugin initialization before executor/provider activity. This proves that deferring `client.app.log` with a zero-delay timer still races bootstrap. The next candidate performs no server I/O in the config hook: it stores only the privacy-safe policy class and emits the diagnostic on the first ordinary runtime event.
- **Revised Startup Cause:** The no-config-I/O startup control still stalled, disproving config-hook logging as the sufficient cause. Source inspection then found the shared runner aborting each `/session/status` request after five seconds even though that request starts project/plugin bootstrap; successful captures completed bootstrap in about 18 seconds. The next runner preserves the existing overall plugin deadline but stops repeatedly cancelling the bootstrap-triggering request.
- **Readiness Probe Correction:** Startup control r4 showed the first long readiness request began before the server listener signal was observed; project bootstrap started only after that request was cancelled. The runner now waits for the actual `opencode server listening` signal before sending the one bounded bootstrap-triggering request, avoiding both the pre-listener hang and repeated initialization aborts.
- **Control r5 And Attempt Closure:** The listener-gated control reached complete runtime/evaluator cleanup, but its Bun parent remained alive after printing success until the shell timeout terminated it. Process readback then found no surviving proof command or child. The runner had not awaited the local provider server's terminal `stop(true)` promise; the next candidate awaits that cleanup so process-level closure is observable.

## 2026-08-20 - Complete-candidate simulation omitted certificate dependency

- **Objective:** Re-prove the canonical OpenSpec propose/apply/archive path on the task-8 candidate without provider or external effects.
- **Approach:** Run the maintained roadmap provider simulation against its copied global source.
- **Evidence:** `evidence-task-8-1-openspec-simulate-r1/evaluation.json` records zero provider/simulation calls and complete cleanup. The controller failed before execution because the copied `roadmap-mission/contracts.ts` imported an unstaged `extensions/session-completion-guard/terminal-certificate.ts`.
- **Outcome:** Proof Runner defect; no Product Candidate behavior or external effect occurred.
- **Reason:** The shared mission source manifest and two proof staging lists were not extended when the certificate dependency crossed the prior `bin/` boundary.
- **Do-Not-Repeat Condition:** Do not stage roadmap mission files from a hand-maintained bin-only subset or approve copied runtime completeness without all import dependencies.
- **Evidence-Based Retry Condition:** Extend the existing mission source manifest and staging/hash lists with the certificate module, prove copied-source preflight, then rerun the provider-free simulation.
- **Successor-Unlock Evidence:** `evidence-task-8-1-mission-preflight-r2` and `-r3` complete after global-relative source identity resolution; the simulation then reached the executor boundary.

## 2026-08-20 - Complete-candidate simulation emitted no structured executor result

- **Objective:** Continue the corrected provider-free OpenSpec simulation through transient recovery and archive.
- **Approach:** Re-run the same simulation after copied runtime completeness passed.
- **Evidence:** `evidence-task-8-1-openspec-simulate-r2/evaluation.json` records zero provider calls, one local synthetic failure, and complete cleanup. Controller evidence classifies the missing result file as `paused-unknown` after attempt one.
- **Outcome:** Proof Runner contract defect; production correctly failed closed on an uncorrelated executor exit.
- **Reason:** The synthetic executor still used the legacy exit-only protocol after production required a correlated bounded result file for every disposition.
- **Do-Not-Repeat Condition:** Do not infer transient or completion from executor exit status without a current structured result.
- **Evidence-Based Retry Condition:** Make the synthetic executor emit correlated transient and completed result records through the existing result-path contract, then replay the full provider-free chain.
- **Successor-Unlock Evidence:** `evidence-task-8-1-openspec-simulate-r7/evaluation.json` and `evidence-task-8-1-controller-r1/evaluation.json` reach two archives, one structured transient recovery, three process boundaries, zero retrospective tasks, protected stop before executor, and complete cleanup while reusing the production-owned mission source manifest.
