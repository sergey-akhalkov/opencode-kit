## 1. Baseline And Planning Readiness

- [x] 1.1 Capture the current loaded propose/apply baseline with the same non-sensitive synthetic change journal and task intent planned for candidate proof. Establish that current `/opsx-propose` does not require the one initially-last history analysis task and current `/opsx-apply` does not explicitly run the compaction matrix over complete `history.md`. **Current Rung:** installed loaded instruction behavior before Product Candidate mutation. **Next Real Boundary:** candidate fresh loaded propose/apply behavior. **Blocker/Unblocker:** none; configured provider inference is authorized and tools/effects can be denied for this baseline. **Observable Proof:** exact OpenCode/config/command/model identity, invocation, exit, stdout/stderr, and response show the missing final-task/history-input behavior without repository or fixture mutation. **Authorization:** standing bounded non-sensitive configured-provider inference. **Safeguards:** deny edit/bash/task/question/external effects; no archive, remote operation, credential, installation, activation, or another-project mutation. **Restoration/Cleanup:** delete attributable disposable sessions; retain privacy-safe baseline evidence in this change. **Expected Immutable Evidence:** `implementation-evidence/baseline.md` with commands, identities, tool envelope, observations, cleanup, and candidate oracle. **Dependency-Chain Stop Condition:** do not mutate loaded policy until the baseline distinguishes current behavior from the requested behavior. **Observed:** installed OpenCode `1.18.18`, `openai/gpt-5.6-sol` `xhigh`; both command runs exited `0`; propose explicitly denied the final task requirement and apply explicitly denied full-history six-cell analysis or one-time scheduling; denied tools produced no mutation and both sessions were deleted.

- [x] 1.2 Re-read the current dirty diff and freeze the additive ownership plan across global authority, maintained propose/apply/archive skill-command mirrors, project OpenSpec rules, delta specs, and focused contracts; validate proposal/design/spec/task readiness without overwriting unrelated active-change edits. **Current Rung:** source and OpenSpec planning evidence. **Next Real Boundary:** focused candidate contract, then fresh loaded behavior. **Blocker/Unblocker:** task 1.1 baseline plus readable current files; any direct semantic conflict with concurrent edits blocks only the overlapping file until reconciled. **Observable Proof:** one traceability record maps each requirement to an exact owner and records `reuse` of the existing compaction matrix/admission/task contract with no new semantic analyzer. **Authorization:** local read and planning-artifact writes only. **Safeguards:** preserve unrelated worktree/index changes and archived histories; no production or automated-test mutation in this task. **Restoration/Cleanup:** planning files remain reviewable; no disposable state. **Expected Immutable Evidence:** `implementation-evidence/implementation-inventory.md`, successful propose gate, and strict selected-change validation. **Observed:** implementation inventory records `reuse`, exact owner/mirror map, and pre-existing overlapping edits; propose/apply gates and strict selected-change validation exited `0`; no automated-test or foreign-change artifact was mutated.

## 2. Implement And Prove The Loaded Workflow

- [x] 2.1 Update the smallest coherent loaded policy surfaces so new change authoring appends exactly one initially-last final-history analysis task, apply runs it only after all other known tasks by reusing the canonical compaction six-cell matrix/admission/task format with complete `history.md` as input, immediately executes every admitted task or records `none`, never schedules a second analysis, and archive cannot bypass incomplete work. Keep maintained skill/command mirrors synchronized and preserve pre-existing concurrent edits. Do not modify automated test artifacts in this task. **Current Rung:** model-facing source mutation. **Next Real Boundary:** actual fresh loaded propose/apply commands. **Blocker/Unblocker:** tasks 1.1-1.2; exact canonical compaction wording is already available in global authority and hidden prompt. **Observable Proof:** source readback shows the same matrix rows/columns, admission gate, task fields, target ownership, and authority boundaries; propose owns creation once, apply owns history execution, archive owns no semantic analysis, and no helper/classifier is added. **Authorization:** local repository edits only. **Safeguards:** no hidden compaction algorithm mutation, no retroactive active/archive migration, no dependency, no external operation, no overwrite of unrelated changes. **Restoration/Cleanup:** source-only rollback is limited to this candidate if runtime comparison regresses. **Expected Immutable Evidence:** scoped diff, synchronized mirror inventory, candidate reference, and source diagnostics. **Dependency-Chain Stop Condition:** do not run provider candidate proof if any maintained surface defines a second algorithm, recursive scheduling, or mandatory invented improvement. **Observed:** `candidate-source.md` records the exact owner map and structural readback; compaction config is unchanged; strict validation and diff check pass; no test artifact was edited. The aggregate focused suite's unrelated concurrent arbiter-marker failure is preserved in `history.md` without crossing ownership.

- [x] 2.2 Invoke the actual fresh loaded `/opsx-propose` boundary with a disposable synthetic change and verify its authored `tasks.md` contains exactly one unchecked final-history analysis item as the initially last task, then invoke the actual fresh loaded `/opsx-apply` boundary over representative history evidence and a no-evidence journal. Correct until admitted candidates use the existing fields and immediate work routing, `none` creates no task, and neither lane creates a second analysis item. **Current Rung:** installed local OpenCode/OpenSpec integration with configured provider. **Next Real Boundary:** future new change in another real project, outside this bounded change. **Blocker/Unblocker:** task 2.1 source candidate and provider/loader preflight; tool-denied semantic proof may precede one disposable file-authoring lane if needed to observe actual `tasks.md`. **Observable Proof:** candidate response/artifact facts distinguish baseline, include exactly one initial analysis task, analyze `history.md` through all six canonical cells, persist every admitted task with standard fields, immediately continue apply, accept `none`, and prohibit repeat scheduling. **Authorization:** standing bounded configured-provider inference and disposable local OpenSpec fixture writes only. **Safeguards:** non-sensitive synthetic content; no real change archive, commit, push, install, activation, remote state, credential, or protected operation; restrict tools to the disposable root when enabled. **Restoration/Cleanup:** delete disposable sessions and fixture root after preserving privacy-safe evidence; cleanup failure is terminal. **Expected Immutable Evidence:** `implementation-evidence/runtime-proof.md` containing Product Candidate, Proof Runner/invocation, Evaluator/oracles, Environment Identity, raw stdout/stderr/exits/artifact facts, side effects, and cleanup. **Dependency-Chain Stop Condition:** red or unobservable propose/apply happy path leaves `Development-Stage: development`; do not broaden static checks instead of reaching this loader boundary. **Observed:** same-model command baselines reproduced the gap; candidate propose/apply/two-candidate/`none` lanes exited `0`; a complete fresh disposable `/opsx-propose` run created exactly one ordinary task followed by one retrospective, passed propose gate and strict validation at `4/4`, left `note.md` absent, and all attributable sessions/roots were removed. `runtime-proof.md` records Product Candidate, runner, evaluator, environment, raw observations, effects, and the earlier incomplete-environment chain. Candidate reached `Development-Stage: MVP`.

## 3. Complete Accepted Scope

- [x] 3.1 Synchronize project OpenSpec task-authoring guidance and current normative delta evidence, document the loaded-source/restart boundary, and inspect all realistic requirement-linked cases: one task only, physical initial ordering, compaction-added work before eligibility, every admitted candidate retained, cross-target/protected authority preserved, `none`, no recursive scheduling, no retrofit, and unchecked archive refusal. Re-run task 2.2 after any model-facing mutation. **Current Rung:** proven candidate plus complete instruction scope. **Next Real Boundary:** final history retrospective and independent critical SDET. **Blocker/Unblocker:** task 2.2 current Runtime Proof. **Observable Proof:** a cold reader can trace authoring, execution, generated-task ownership, completion, archive, and restart behavior without a new algorithm or hidden state; candidate runtime remains green. **Authorization:** local source/docs and bounded configured inference only. **Safeguards:** no edits to another project, hidden compaction behavior, archived changes, external state, or public product protocol. **Restoration/Cleanup:** remove disposable runtime state; retain proof records. **Expected Immutable Evidence:** scoped docs/spec diff, edge-case disposition matrix, current runtime bundle, and strict selected-change validation. **Observed:** `openspec/project.md`, current normative specs, and `edge-case-matrix.md` now trace all named cases; strict library/change validation and `git diff --check` pass; no loaded source changed after Runtime Proof, and all attributable sessions/fixtures were removed. Fresh OpenCode restart remains required for future sessions to load the retained source candidate.

## 4. Final History Retrospective

## Session-Derived Improvements

- [x] H.1 Add a provider-free complete-environment preflight rule for OpenSpec slash-command proof fixtures to the existing `tools/proofs/README.md` inventory. **Trigger/Evidence:** the first file-authoring proof reached correct retrospective task creation but required two additional diagnostics because the disposable root first lacked the `openspec:gate` adapter and then lacked `openspec/config.yaml`; the complete third environment reached propose gate and strict validation in one run. **Why:** move explicit fixture completeness failures before configured model inference and preserve terminal proof attribution. **Prerequisites:** existing trusted OpenSpec operation gate, current project config, current command/skill loader, and preserved failed/green proof evidence. **Scope/Non-Goals:** proof inventory operating rule only; no Product Candidate, hidden compaction prompt, OpenSpec CLI, helper, dependency, test artifact, other project, or semantic evaluator change; do not claim measured time/token savings. **Implementation:** document that an OpenSpec command-proof fixture must provider-free verify the candidate command/skill source, complete `openspec/config.yaml`, project-native gate adapter/argv, writable contained root when authoring is required, and cleanup/evidence plan before a configured-provider call. **Observable Proof:** construct one disposable preflight fixture from current sources, verify all required paths/script/command loading without inference, and reject a deliberately incomplete fixture missing `openspec/config.yaml`; no model call or external effect occurs. **Validation:** read back the proof inventory, run strict library validation, strict selected-change validation, and `git diff --check`. **Observed:** the proof inventory now states the complete provider-free preflight; a complete disposable fixture returned `passed=true`, an otherwise identical fixture without `openspec/config.yaml` returned `passed=false` with `missing=["openspec-config"]`, both recorded `modelCalls=0`, and both roots were removed. Strict validation reports `skills=26 agents=18 markdown=320 warnings=0`, selected-change validation and `git diff --check` exit `0`.

- [x] 4.1 After every other currently known task is complete, analyze the complete `history.md` exactly as compaction analyzes a session: use the canonical `Quality`, `Cycle Speed`, and `Token Economy` rows across `Working Repository` and `opencode-kit`; apply the existing observed-evidence, original-goal causal-link, local/reversible, low-cost, no-scope-expansion, ownership, and authority rules; and use the existing `Session-Derived Improvements` fields. Append every admitted concrete small improvement immediately before this final item, start and complete that work at once, or record `none` without inventing a task. Do not create or run another final-history analysis. After generated work is complete, dispatch one fresh Material critical-only SDET against the resulting candidate, independently disposition its rows, rerun every invalidated runtime/validation lane, run focused contracts, `npm run validate:strict`, `npm test`, strict selected/all OpenSpec validation, operation gates, `npm run prepush:validate`, instruction inventory, `git diff --check`, and write the complete local stable handoff only when all current evidence is green. **Current Rung:** complete accepted implementation scope with accumulated strategy journal. **Next Real Boundary:** future newly proposed real-project change after a fresh OpenCode process loads the retained artifacts. **Blocker/Unblocker:** tasks 1-3 complete; generated improvements become ordinary accepted scope and must finish before this item can be checked. **Observable Proof:** one six-cell history matrix, admitted task IDs or `none`, no second analysis task, terminal SDET `critical-risks-reported | no-critical-risk | blocked` with main disposition, current candidate/proof/validation identity, no known reachable critical/non-deferrable defect, `Development-Stage: stable`, and `Stable Candidate: RC<n>` only when earned. **Authorization:** local repository/test-only SDET/disposable configured inference and validation; no archive, commit, push, install, activation, release, deployment, or remote mutation. **Safeguards:** SDET edits only automated test artifacts; preserve unrelated work; generated instruction changes require dedicated workflow comparison; owner/protected boundaries remain exact; no lifecycle claim from checkbox state alone. **Restoration/Cleanup:** close every writer, delete attributable disposable sessions/fixtures, preserve immutable raw evidence, and document restart requirement. **Expected Immutable Evidence:** history analysis record, generated task evidence or `none`, SDET report/effective model/main disposition, final commands/exits/stdout/stderr, Candidate Reference/RC history, known limitations, external-operation state, and stable handoff. **Observed:** complete `history.md` six-cell analysis admitted only H.1 and did not schedule another retrospective; H.1 passed provider-free positive/negative proof and validation. Fresh SDET `ses_005818e43ffe8WhlOvSY1UWuLo`, Effective Model `xai/grok-4.6`, returned `no-critical-risk`; main inspected its sole test-only diff and replayed focused contracts at `67/67`. Full tests, strict library validation, strict selected/all OpenSpec validation (`14/14`), propose/apply gates, instruction inventory, `git diff --check`, and `npm run prepush:validate` passed. Evidence and stable local handoff are preserved; no archive or external operation was performed.
