## 1. Reproduce And Correct Retention Recovery

- [x] 1.1 Preserve the production-log baseline and reproduce the finite-retention deadlock with two old idle guard-owned `auditing` children, including busy, unknown, recent, current, ownership-invalid, and unrelated negative controls. **Current Rung:** deterministic preserved-log replay and existing component proof infrastructure. **Next Real Boundary:** local proof runner using real guard modules and disposable session state. **Blocker/Unblocker:** none. **Observable Proof:** the unchanged candidate returns `Retained completion arbiter child limit reached with no eligible terminal child` for the exact old-idle scenario and mutates none of the negative controls. **Owner Authorization:** local read-only logs and disposable local fixtures only. **Safeguards:** redact session identities in durable evidence; no provider calls or live session mutation. **Restoration/Cleanup:** remove disposable fixture state after capture. **Expected Immutable Evidence:** baseline command, candidate hash, exit status, stdout/stderr, exact failure, fixture before/after state, and cleanup result under this change's `evidence/` directory. **Validation:** baseline evaluator distinguishes the known failure from every protected negative control.
- [x] 1.2 Implement timeout-plus-settle-aged canonically idle interrupted-audit quarantine inside the existing retained-child owner, with immediate ownership/metadata/active-status rechecks and unchanged fail-closed behavior for every protected negative control. **Current Rung:** reproduced local component failure from task 1.1 plus loaded status-semantics capture. **Next Real Boundary:** corrected local proof runner using real guard modules. **Blocker/Unblocker:** task 1.1 and `candidate-runtime-status-r2-failure.md` provide the exact regression/runtime state; stop this dependency chain if the status request or child re-fetch is unreadable. **Observable Proof:** eligible old canonically idle guard children become terminal `stale`, normal rotation frees one slot, and no explicitly active/status-unreadable/recent/current/ownership-invalid/unrelated child is changed or deleted. **Owner Authorization:** local source and disposable fixtures. **Safeguards:** no new config, dependency, API, model call, or unrelated cleanup; preserve original errors. **Restoration/Cleanup:** proof fixture teardown is deterministic. **Expected Immutable Evidence:** candidate diff plus corrected before/after fixture capture. **Validation:** focused production-module proof passes before broader testing.

## 2. Runtime Proof And Local Recovery

- [x] 2.1 Invoke the corrected plugin through the actual loaded OpenCode entry point against disposable persisted state at retention limit, then restart against the same state. **Current Rung:** corrected local proof runner with real guard modules. **Next Real Boundary:** loaded OpenCode plugin in a disposable local runtime. **Blocker/Unblocker:** task 1.2; local configured model inference is already authorized. **Observable Proof:** first load quarantines only eligible interrupted children and completes a new audit; second load emits no retention-limit `input-state` error and preserves unrelated sessions. **Owner Authorization:** bounded synthetic inference through configured providers, disposable local database/session directory only. **Safeguards:** synthetic non-sensitive prompt, exact project/root correlation, no credentials in evidence, finite calls, no remote repository effects. **Restoration/Cleanup:** terminate the runtime and delete only the disposable state after terminal capture. **Expected Immutable Evidence:** exact invocation/input, source and runtime identities, exit status, stdout/stderr/logs, child metadata before/after, model call count, restart result, and cleanup status under this change's `evidence/` directory. **Validation:** evaluator reaches a terminal green verdict from preserved raw facts.
- [x] 2.2 Run the corrected loaded plugin against the affected local project root and verify the recurring startup banner is eliminated without changing unrelated sessions. **Current Rung:** disposable loaded-plugin proof from task 2.1. **Next Real Boundary:** affected local OpenCode session store. **Blocker/Unblocker:** task 2.1 must be terminal green; stop if child ownership, status, age, or root correlation is unknown. **Observable Proof:** the known affected root no longer contains a retention-blocking child set, a subsequent project load has no matching retention error, and unrelated child/session identities remain present. **Owner Authorization:** the user's request authorizes repair of the local guard-owned stale audit state; no remote state. **Safeguards:** capture privacy-safe inventory before mutation, mutate only exact guard-owned eligible children, preserve unrelated state, and retain rollback-relevant metadata. **Restoration/Cleanup:** no bulk deletion; close disposable processes and retain only redacted evidence. **Expected Immutable Evidence:** redacted before/after inventory, exact loaded candidate identity, relevant logs, restart result, and unchanged unrelated-session count. **Validation:** no new `Retained completion arbiter child limit reached` entry for the affected root after the verification load.

## 3. Critical Testing And Qualification

- [x] 3.1 Dispatch one fresh test-only SDET after tasks 1-2 are proven; challenge only critical/non-deferrable ownership, active-child deletion, unrelated-session deletion, and restart-loop risks, and add the smallest justified regression oracle. **Current Rung:** loaded corrected plugin proven in disposable and affected local state. **Next Real Boundary:** fresh independent automated critical oracle over the production candidate. **Blocker/Unblocker:** tasks 1.2, 2.1, and 2.2; SDET must not author production. **Observable Proof:** SDET returns exactly `critical-risks-reported`, `no-critical-risk`, or `blocked` with Effective Model and candidate attribution; main independently dispositions every critical row. **Owner Authorization:** local test-only writes and bounded configured model inference. **Safeguards:** no live session mutation, no unrelated test changes, no scope expansion. **Restoration/Cleanup:** remove disposable test state; retain useful regression tests. **Expected Immutable Evidence:** SDET report, changed test paths if any, exact commands and raw outcomes. **Validation:** independently run each authored regression against the current candidate.
- [x] 3.2 Run focused guard tests, proof replay, strict OpenSpec validation, complete project validation, and the actual plugin `--help`/loader diagnostics applicable to this repository; freeze the next RC only if all gates are green and no known reachable critical/non-deferrable defect remains. **Current Rung:** terminal fresh SDET on the proven candidate. **Next Real Boundary:** complete project-native validation and local handoff. **Blocker/Unblocker:** task 3.1 terminal evidence; any production mutation returns to task 2 proof before qualification. **Observable Proof:** all applicable commands exit zero, preserved evidence evaluates green, and diagnostics retain the original cause for ineligible retention conflicts. **Owner Authorization:** local validation only. **Safeguards:** no install, commit, push, release, or remote mutation; preserve unrelated work. **Restoration/Cleanup:** terminate spawned processes and verify no disposable sessions remain. **Expected Immutable Evidence:** exact commands, versions, exit codes, stdout/stderr, candidate diff/hash, cleanup, RC history, and known limitations. **Validation:** `npm run test:focused:session-completion-guard`, relevant proof commands, `openspec validate fix-session-completion-guard-retention-recovery --strict`, and `npm run validate:strict` plus any repository gate discovered during implementation.

## 4. Final History Retrospective

- [x] 4.1 After every other currently known task is complete, analyze the complete `history.md` once using the canonical compaction improvement matrix: rows `Quality`, `Cycle Speed`, and `Token Economy`; columns `Working Repository` and `opencode-kit`. For every evidence-backed candidate record `Impact Horizon`, `Concrete Consumers`, `Execution Class`, `Earliest Safe Point`, `Invalidated Evidence`, and `Observable Payback`; admit only exact remaining current-change consumers under `## Session-Derived Improvements`, append and immediately execute each admitted task, and preserve no-current-consumer evidence as non-checkbox deferred history records. If neither exists, record `none` without inventing work. Apply, archive, compaction, and generated tasks MUST NOT create or rerun this final analysis. **Current Rung:** complete change history and qualification evidence. **Next Real Boundary:** none beyond the accepted local handoff. **Blocker/Unblocker:** every other known task must be complete. **Observable Proof:** one recorded six-cell analysis with every candidate correctly admitted, deferred, or `none`, and every admitted task executed and validated. **Owner Authorization:** local planning/source changes only. **Safeguards:** no scope expansion or protected-boundary action. **Restoration/Cleanup:** N/A; retain the history record. **Expected Immutable Evidence:** final `history.md`, any appended task evidence, and validation results. **Validation:** confirm exactly one final-history-retrospective task exists and no generated successor analysis was created.
