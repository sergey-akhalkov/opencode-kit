## Fidelity Ladder

`current source/spec/runtime audit -> provider-free mission preflight in a disposable real OpenSpec project -> deterministic state/transition replay with no model -> installed OpenCode configured-provider disposable multi-change mission -> separately authorized target-project mission`. Product Candidate roles are the mission/preflight/controller, canonical workflow and guard/evidence code; Proof Runner roles live under `tools/proofs/`; Evaluator roles consume immutable transition/session evidence; Environment Identity includes kit/OpenCode/OpenSpec/Node/Git/project/mission hashes. No task in this change authorizes deployment, release, push, hardware, remote repository, credential mutation, or another protected external effect.

Profile: `Material`. Main owns production implementation and run-observe-correct. After complete accepted-scope Runtime Proof, fresh test-only SDET owns any automated critical regression artifacts.

## 1. Freeze Contract And Baseline

- [x] 1.1 Validate the proposal/design/delta specs/history and record the exact current runtime baseline: installed kit source and versions, loaded guard/commands/skills, existing lower-level owners, active OpenSpec changes, current clean worktree, reuse disposition, and known reliability gaps. **Current Rung:** source/spec/runtime inspection. **Next Real Boundary:** actual new provider-free preflight entrypoint in a disposable project. **Blocker/Unblocker:** none. **Observable Proof:** propose operation gate and strict change/all validation green with one self-contained baseline evidence record. **Owner Authorization:** local read-only and OpenSpec artifact writes. **Safeguards:** no provider, target project, external, install, or runtime config effect. **Restoration/Cleanup:** artifact rollback only; preserve unrelated active change. **Expected Immutable Evidence:** `evidence/task-1-1-baseline.md`; propose gate, strict selected/all OpenSpec validation, and diff check exit 0.

## 2. Provider-Free Mission Preflight MVP

- [x] 2.1 Implement the versioned mission definition parser, normalization, digest, dependency/order validation, effect/checkpoint rules, persisted-state schema primitives, and stable explicit result types in one portable `global/bin` owner. Reuse existing process/archive/runtime-source/adapter primitives; add no dependency and no roadmap-semantic ranking. **Current Rung:** production component. **Next Real Boundary:** the installed CLI preflight over disposable valid/invalid real repositories. **Blocker/Unblocker:** task 1.1 baseline. **Observable Proof:** deterministic JSON for eligible and blocked definitions, with unsupported facts reported unknown and zero mutation. **Owner Authorization:** local source edit only. **Safeguards:** contained paths, argv arrays, no shell/provider/writer. **Restoration/Cleanup:** source rollback only. **Expected Immutable Evidence:** `evidence/task-2-p0-preflight.md` and `evidence/p0-preflight-r9/**`; current production source hashes and schema v1 recorded.
- [x] 2.2 Extend preflight through real project facts: project/runtime authority, complete validation adapter argv, Git/worktree overlap, OpenSpec version/list/status, canonical global workflow inventory, project same-name overlays, installed loader identity, checkpoint support, next-slice protected effects, live-attempt evidence, and open writer lease. Fail before model/mutation on any blocking unknown. **Current Rung:** provider-free local integration. **Next Real Boundary:** disposable project run through the actual global entrypoint. **Blocker/Unblocker:** task 2.1 parser. **Observable Proof:** valid project returns one exact eligible slice; stale overlay, missing adapter, ambiguous active change, dirty overlap, invalid checkpoint, and protected-effect fixtures each return stable blocking rows and no side effect. **Owner Authorization:** local read-only project inspection. **Safeguards:** privacy-safe paths/output, no config rewriting, no process kill. **Restoration/Cleanup:** no target mutation; disposable fixtures removed. **Expected Immutable Evidence:** `evidence/canonical-workflow-r1/**` records the current post-migration source hashes, exact eligible `slice-a`, all required blocked fixtures, zero project mutations, and complete cleanup.
- [x] 2.3 Add `tools/proofs/roadmap-mission.ts` provider-free preflight/capture/evaluate modes and inventory documentation, then invoke the actual production entrypoint against a disposable OpenSpec project. Inspect output, process effects, filesystem manifest, and cleanup; run-observe-correct until the eligible happy path and fail-closed stale-overlay path are green. **Current Rung:** disposable production-equivalent CLI. **Next Real Boundary:** canonical installed workflow plus state transitions. **Blocker/Unblocker:** tasks 2.1-2.2. **Observable Proof:** immutable raw bundle proves exact eligible slice and zero provider/mutation; invalid overlay blocks before mutation. **Owner Authorization:** disposable local effects only. **Safeguards:** temp root, no credentials/providers/remote, bounded process timeout. **Restoration/Cleanup:** delete disposable root in `finally`; cleanup failure terminal. **Expected Immutable Evidence:** `evidence/canonical-workflow-r1/raw.json` and `evaluation.json`; status complete, cleanup complete, mutation count zero.

## 3. Canonical Workflow And Project Migration

- [x] 3.1 Make standard OpenSpec propose/apply/archive skills and commands one canonical installed global surface. Replace target-project npm assumptions with portable explicit-global-source operation-gate/archive invocations; add deterministic source/mirror/loader collision checks and migration diagnostics while keeping domain constraints in OpenSpec config/adapters/namespaced helpers. **Current Rung:** proven mission preflight. **Next Real Boundary:** installed loader in an unrelated disposable project. **Blocker/Unblocker:** task 2.3. **Observable Proof:** actual loaded standard names resolve only to the global source; a legacy same-name overlay blocks unattended mode; manual files are never deleted. **Owner Authorization:** local kit/config source edits. **Safeguards:** no target overwrite/install in this task; preserve specialist permissions. **Restoration/Cleanup:** version-control rollback. **Expected Immutable Evidence:** `evidence/canonical-workflow-r1/**`, current `opencode debug skill --pure` locations under `global/skills`, global gate output, strict validator and selected-change validation exit zero.
- [x] 3.2 Extend bootstrap, doctor, runtime-source diagnostics, templates, README/routing, and project adapter contracts for unattended readiness, explicit aggregate validation argv, checkpoint policy, canonical workflow ownership, and non-destructive overlay migration. **Current Rung:** canonical disposable loader. **Next Real Boundary:** preview/write/doctor against a disposable non-JavaScript project. **Blocker/Unblocker:** task 3.1. **Observable Proof:** new project provisions no workflow copies; legacy project is reported incompatible without mutation; ordinary and unattended readiness remain separate. **Owner Authorization:** local disposable bootstrap writes. **Safeguards:** backups on explicit overwrite, no hidden package-manager default. **Restoration/Cleanup:** fixture deletion and backup verification. **Expected Immutable Evidence:** `evidence/project-readiness-r3/raw.json` and `evaluation.json` record fresh/legacy preview-write manifests, selected mission and loaded canonical workflow checks, exact preserved overlay hash, explicit-overwrite backup hash, ordinary qualification pass, expected long-run guard blocker, privacy-safe runtime source identities, and cleanup complete. `evidence/canonical-workflow-r2/**` records the current mission entrypoint identity and zero-mutation preflight scenarios.

## 4. Persisted Mission Controller And Checkpoints

- [x] 4.1 Implement atomic mission state/immutable transition records, definition and prior-state correlation, one contained writer lease, restart reconciliation, and provider-free replay/evaluator modes. **Current Rung:** disposable canonical preflight/workflow. **Next Real Boundary:** kill/restart replay between archive and successor activation. **Blocker/Unblocker:** tasks 3.1-3.2. **Observable Proof:** restart resumes at post-archive readback without duplicate archive; corrupt/missing transition and unknown writer state fail closed. **Owner Authorization:** disposable local state writes/process termination. **Safeguards:** atomic create-new records, bounded paths, no live process kill outside proof-owned process. **Restoration/Cleanup:** proof-owned process/root deletion; retain immutable evidence. **Expected Immutable Evidence:** `evidence/roadmap-state-r2/raw.json` and `evaluation.json` record the five-transition hash chain, atomic current projection, proof-owned child termination/stale-lease quarantine, exactly one archive, sequence-5 replay, blocked duplicate archive, missing transition, and unknown writer, with cleanup complete. `evidence/canonical-workflow-r3/**` re-proves current source identity including `state.ts`.
- [x] 4.2 Implement serial propose/apply/status/proof-validation/archive/readback/successor transitions over existing owners, finite per-slice recovery, and terminal owner/external stop. Model output remains evidence only. **Current Rung:** persisted provider-free state machine. **Next Real Boundary:** actual installed OpenCode synthetic multi-change mission. **Blocker/Unblocker:** task 4.1 plus canonical workflow. **Observable Proof:** fake/no-model executor completes two changes, rejects unchecked-task completion, archives once, activates only dependency-valid successor, and stops before blocked effect. **Owner Authorization:** disposable project mutation only. **Safeguards:** one writer; no protected/remote effects; exact process timeout. **Restoration/Cleanup:** fixture deletion and session/process terminal closure. **Expected Immutable Evidence:** `evidence/roadmap-controller-r2/raw.json` and `evaluation.json` record A=2 executor calls after one rejected unchecked completion, B=1, protected C=0, exactly two archives, terminal blocked cursor 2, the 15-transition chain including archive-launch, valid replay, and cleanup complete. `evidence/roadmap-state-r3/**` and `evidence/canonical-workflow-r4/**` re-prove the invalidated state/preflight lanes; focused operation-gate tests pass 11/11.
- [x] 4.3 Implement `evidence-only`, explicitly authorized `local-commit`, and `external` checkpoint enforcement, scoped ownership/staging, hook result capture, no-push invariant, and recovery behavior. **Current Rung:** serial local controller. **Next Real Boundary:** disposable Git mission with local checkpoint and external-stop variants. **Blocker/Unblocker:** task 4.2. **Observable Proof:** multi-change evidence-only non-disposable mission blocks; local commit includes only owned paths and no remote; external mode waits for verified checkpoint identity. **Owner Authorization:** disposable local Git commits only. **Safeguards:** no broad staging, no push, clean isolated repository. **Restoration/Cleanup:** delete fixture repository. **Expected Immutable Evidence:** `evidence/roadmap-controller-r12/raw.json` and `evaluation.json` record exact scoped committed paths, hook `ran`, unchanged remote refs, stale external identity rejection, verified resume without repeated executor/archive work, and cleanup complete.

## 5. Completion Guard Long-Run Reliability

- [x] 5.1 Bound every session-delivery evidence surface and exact final arbiter request; add explicit truncation facts and terminal overflow classification. Enforce finite retained-child behavior and preserve root/epoch correlation. **Current Rung:** production component with existing installed guard proof. **Next Real Boundary:** offline long-root projection and installed disposable guard. **Blocker/Unblocker:** none after task 2.3; serialize writes with mission changes. **Observable Proof:** oversized source input yields bounded request or terminal no-prompt overflow; retained child count remains within policy. **Owner Authorization:** local disposable session/database only. **Safeguards:** no prompt/credential leakage, no provider call for overflow. **Restoration/Cleanup:** delete fixtures/sessions. **Expected Immutable Evidence:** `evidence/guard-long-run-r3/raw.json` and `evaluation.json` record bounded human/todo/question/permission/execution surfaces, 18 stable truncation rows, exact projection/request bytes, terminal input-state overflow before child/prompt creation, oldest-idle-terminal retained-child rotation, production hashes, and cleanup complete. R1 is retained failed evidence with cleanup pending; R2 is superseded by stable truncation surfaces.
- [x] 5.2 Add error-class-aware finite retry, arbiter prompt timeout, persisted attempts, wait rechecks/task-result fallback, startup reconciliation of grind-enabled roots and async ownership, duplicate-child handling, and liveness diagnostics. Preserve disable/interruption/stale late-effect gates. **Current Rung:** bounded evidence/retention. **Next Real Boundary:** installed local server restart and provider-failure simulation. **Blocker/Unblocker:** task 5.1. **Observable Proof:** transient failure retries within limit; immutable failure does not retry; restart resumes one safe epoch; unknown child/writer waits; lost task result gets one fallback; disable suppresses all late effects. **Owner Authorization:** local disposable server/session/model simulation; configured provider only in later task 6.1. **Safeguards:** bounded timers, no user process kill, privacy-safe diagnostics. **Restoration/Cleanup:** terminate proof-owned server and delete sessions. **Expected Immutable Evidence:** `evidence/guard-restart-r16/raw.json` and `evaluation.json` record fresh OpenCode 1.18.18, isolated local transport, attempt `1 -> 2`, same audit/child across process restart, one non-guard child preserved, root `audit-retrying -> passed`, and complete session/process/fixture cleanup under a supervising parent plus offline evaluator. `evidence/guard-long-run-r5/**` records immutable no-retry, transient exhaustion, duplicate retry-child block, exactly-once disable-safe task fallback, finite terminal wait exhaustion, diagnostics, and cleanup.
- [x] 5.3 Correct permission normalization so permissive main defaults never replace explicit specialist-agent restrictions, and prove effective runtime rules for arbiter/reviewers/workers. **Current Rung:** loaded config behavior. **Next Real Boundary:** installed permission proof. **Blocker/Unblocker:** task 5.2 candidate. **Observable Proof:** main remains autonomous while arbiter/reviewer denied edit/bash/task/question capabilities remain denied and arbiter tool map is false. **Owner Authorization:** local read-only loader proof. **Safeguards:** no permission replies or config-secret output. **Restoration/Cleanup:** none. **Expected Immutable Evidence:** `evidence/guard-permissions-r2/**` records OpenCode 1.18.18, permissive top-level main, 195 effective specialist deny decisions matching configured policy, and 18 all-false hidden-arbiter tools. `evidence/project-readiness-r4/**`, `guard-long-run-r6/**`, and `guard-restart-r17/**` re-prove loaded unattended readiness, bounded guard behavior, restart recovery, and cleanup on the current runtime source.

## 6. Integrated Proof, Critical Challenge, And Validation

- [x] 6.1 Run the configured-provider installed disposable mission proof: two serialized synthetic changes, one causally recoverable local failure, process restart, archive/readback/checkpoint, and terminal protected-effect blocker. Preserve complete identities, raw outputs, sessions, side effects, and cleanup. **Current Rung:** provider-free complete candidate. **Next Real Boundary:** configured OpenCode provider and installed loader in disposable projects. **Blocker/Unblocker:** tasks 2-5 green; standing bounded model-call authorization applies. **Observable Proof:** terminal evaluator is complete, two archives occur exactly once, restart resumes correctly, no blocked effect/provider retry escapes bounds, and cleanup is complete. **Owner Authorization:** configured model inference and disposable local writes only. **Safeguards:** non-sensitive prompts, finite calls/time, no remote/hardware/credentials, effect-denied tools. **Restoration/Cleanup:** delete sessions, servers, fixtures; cleanup failure terminal. **Expected Immutable Evidence:** Product Candidate, Runner, Evaluator, Environment, raw bundles and terminal verdict.
- [x] 6.2 After complete accepted-scope Runtime Proof, dispatch one fresh test-only `sdet-quality-engineer` with exact Candidate Reference, mission/guard invariants, restart/writer/permission/archive/protected-effect risks, current evidence, and local disposable boundary. Main independently reproduces and dispositions plausible critical rows; one no-confirmed-critical attempt is terminal. **Current Rung:** current integrated MVP. **Next Real Boundary:** terminal critical-risk evidence. **Blocker/Unblocker:** task 6.1. **Observable Proof:** `critical-risks-reported`, `no-critical-risk`, or exact blocked report with Effective Model; any confirmed critical fix is re-proven before one fresh successor. **Owner Authorization:** local test-only writes and provider inference. **Safeguards:** SDET edits tests only, no production/external action. **Restoration/Cleanup:** no external state. **Expected Immutable Evidence:** attributed SDET matrix and main disposition.
- [x] 6.3 Run focused mission/guard/bootstrap/doctor/operation/archive tests and proofs, permission proof, strict repository validation, full tests, code-quality inventory/readback, strict selected/all OpenSpec validation, and `git diff --check`. Re-run only invalidated lanes. **Current Rung:** terminal SDET candidate. **Next Real Boundary:** local operator handoff. **Blocker/Unblocker:** terminal SDET and current proof. **Observable Proof:** all applicable commands exit zero and diagnostics/readme/proof inventory match installed behavior. **Owner Authorization:** local validation only. **Safeguards:** no install/activation/remote action. **Restoration/Cleanup:** disposable outputs only. **Expected Immutable Evidence:** commands/exits/stdout/stderr and scoped diff.

## 7. Handoff

- [x] 7.1 Record Profile, outcome, Candidate Reference/RC history, mission/guard Runtime Proof, raw bundles, environment, architecture/split-or-justify, diagnostics, terminal SDET, validation, migration/rollback, limitations, external-operation state, target-project prerequisites, and `Development-Stage`. Verify a new process is required before loaded instruction/plugin changes can affect an operator. **Current Rung:** qualified local candidate. **Next Real Boundary:** separately authorized install/restart and target mission, outside this change. **Blocker/Unblocker:** task 6.3. **Observable Proof:** cold-reader handoff and final OpenSpec status are complete without claiming target mission execution. **Owner Authorization:** local reporting only. **Safeguards:** no install, activation, target mutation, commit, push, deployment, or release. **Restoration/Cleanup:** preserve evidence. **Expected Immutable Evidence:** final handoff/status.
- [x] 7.2 Perform the final complete-change history retrospective after every other currently known task is complete. Apply the canonical six-cell `Quality` / `Cycle Speed` / `Token Economy` by `Working Repository` / `opencode-kit` evidence gate to complete `history.md`; append every admitted improvement under `## Session-Derived Improvements` and continue through implementation/proof/validation, or record `none`. Do not create or run another final-history retrospective. **Current Rung:** complete change history. **Next Real Boundary:** archive readiness. **Blocker/Unblocker:** task 7.1 and every generated improvement complete. **Observable Proof:** one terminal retrospective record with generated task ids complete or explicit none. **Owner Authorization:** local evidence/process work. **Safeguards:** no scope expansion/protected action. **Restoration/Cleanup:** documentation rollback only. **Expected Immutable Evidence:** final history/tasks readback.

## Session-Derived Improvements

- [x] I1 Retarget canonical OpenSpec workflow and permission test contracts. **Trigger/Evidence:** `tools/test-contracts-change-ready-delivery.ts`, `tools/test-helpers/library.ts`, and `tools/test-install-opencode-global.ts` still name or omit current canonical portable owners; the first two still name `.opencode/skills/openspec-*`, `.opencode/commands/opsx-*`, and `node tools/openspec-operation-gate.ts` after task 3.1 moved the canonical owner to `global/`; `tools/test-session-completion-guard.ts` still expects permission normalization to replace specialist maps with allow-all after task 5.3 preserved declared specialist restrictions. **Why:** stale tests can hide drift or reject the installed canonical and permission surfaces. **Prerequisites:** accepted-scope Runtime Proof is complete; execute under the fresh test-only SDET lane. **Scope/Non-Goals:** retarget current-owner path lists, fixtures, and permission oracles only; do not rewrite archive behavior, edit a target project, weaken specialist restrictions, or add domain-specific behavior. **Implementation:** point assertions and fixtures at `global/skills`, `global/commands`, and `global/bin/openspec-operation-gate.ts`; retain `tools/openspec-operation-gate.ts` only where a compatibility import is intentionally verified; expect top-level main allow while each specialist's configured permission map remains unchanged; provision every currently required `global/bin/**` path in copied installer fixtures. **Observable Proof:** focused contract/gate/guard tests and `npm run validate` exit zero with no required reference to deleted project-local standard OpenSpec files and no allow-all specialist requirement. **Validation:** focused contracts/gate/guard tests and `git diff --check`.
- [x] I2 Share the mission production-source inventory with its proof runner. **Trigger/Evidence:** the first split-owner proof copied only `roadmap-mission.ts` and failed `workflow:canonical-source` because `contracts.ts` and `preflight.ts` were omitted. **Why:** duplicated production-module lists can make a valid split look incomplete or let proof identity drift. **Prerequisites:** none; task 3.1 established the stable canonical module owner. **Scope/Non-Goals:** expose and consume one source-path list; do not introduce a framework, broaden runtime behavior, or change proof scenarios. **Implementation:** export `MISSION_SOURCE_PATHS` from the production preflight module and use it for proof installation and production-source evidence alongside the canonical gate path. **Observable Proof:** the post-migration disposable preflight capture loads every listed module, while omission of a listed module remains a deterministic `workflow:canonical-source` failure. **Validation:** `evidence/canonical-workflow-r1/raw.json` records all four production sources; `npm run validate`, selected-change strict validation, and `git diff --check` exit zero.
- [x] I3 Resolve bare Windows native executables in portable-process. **Trigger/Evidence:** checkpoint R6 split the spaced Git commit subject into pathspecs because bare `git` was routed through `cmd.exe`; R7 then showed Windows environment-key casing bypassed the first resolver. **Why:** production local checkpoints require argv-preserving Git commits on Windows. **Prerequisites:** task 4.3 checkpoint implementation. **Scope/Non-Goals:** resolve PATH `.exe`/`.com` and spawn with `shell: false`; retain `.cmd`/`.bat` fallback and reject shell metacharacters before shell fallback, while direct native argv remains literal and may contain multiline/metacharacter values; add no PowerShell runner and do not weaken commit-subject policy. **Implementation:** resolve bare native commands from the supplied case-insensitive Windows PATH, use direct argv spawn, build and validate a Windows shell command line only for shell fallback, remove Git format-placeholder dependencies, and normalize checkpoint inventories with `--no-renames`. **Observable Proof:** `evidence/roadmap-controller-r12/**` completes local-commit and external checkpoint scenarios with the production entrypoint and no pathspec split; a native executable receives literal multiline/metacharacter argv without a second shell command, while shell fallback rejects the same metacharacters before child execution. **Test-Only Owner:** `tools/test-library/portable-workflow-tools.ts` owns the staged-validation native/shell oracle. **Validation:** portable-process absolute-native/literal-argv and shell-fallback rejection oracles pass; controller proof, syntax checks, `npm run validate`, and `git diff --check` exit zero.
