# Strategy History

## 2026-08-16 - Invoke troubleshooter before every blocker escalation

- **Objective**: Ensure no recoverable blocker reaches the user without an independent diagnostic pass.
- **Approach**: Require a `troubleshooter` invocation immediately before every blocker-related owner question, including credentials, elevation, destructive or remote actions, cost, deployment, and protected semantic decisions.
- **Evidence**: The current role explicitly treats product, legal, security, credential, destructive-operation, and user-owned decisions as bad fit. Specialist dispatch is serial, and the completion guard already skips troubleshooting for a proven `owner_required` verdict.
- **Outcome**: Rejected during proposal design; not implemented or runtime-tried.
- **Reason**: The route cannot remove an authority boundary, adds latency to exact owner actions, and risks teaching the model to diagnose or soften decisions that only the owner can make.
- **Do-Not-Repeat Condition**: Do not reintroduce mandatory consultation for blockers whose exact protected owner action and lack of safe substitute are already proven.
- **Evidence-Based Retry Condition**: Reconsider only if runtime evidence shows a diagnosis-only consultation materially improves a specific currently owner-only class without delaying action, simulating authority, weakening the boundary, or changing the accepted outcome.

## 2026-08-16 - Add a standalone recovery section or new blocker skill

- **Objective**: Make the pre-escalation behavior highly visible and reusable.
- **Approach**: Add a complete standalone `Pre-Escalation Gate` section to global authority and/or create a new `blocker-recovery` skill that main loads before asking the user.
- **Evidence**: `global/AGENTS.md` already owns attempt control, autonomy, and self-contained owner handoff; `troubleshooter` already supplies the independent diagnosis context. Maintained mirrors are required to use pointers or role deltas, and a skill runs in the same context rather than providing an independent view.
- **Outcome**: Rejected during proposal design; not implemented or runtime-tried.
- **Reason**: A standalone mechanism duplicates existing authority and increases retrieval ambiguity and context cost. A new skill adds routing and context without replacing the independent child role.
- **Do-Not-Repeat Condition**: Do not add another complete policy owner or skill while the canonical autonomy/handoff contract and existing troubleshooter can express and prove the behavior coherently.
- **Evidence-Based Retry Condition**: Reconsider only if loaded behavior evidence shows the consolidated canonical rule is not discoverable or the existing role cannot satisfy the case-file/report contract without an unrelated responsibility conflict.

## 2026-08-16 - Hybrid pre-escalation recovery filter

- **Objective**: Maximize autonomous goal-preserving blocker removal while keeping exact owner actions fast and protected.
- **Approach**: Immediately before the first blocker question, execute any unused safe causally distinct local mechanism; otherwise skip specialist delay only for a proven exact owner action with no safe substitute, and route technical or uncertain blockers through at most one diagnosis-only `troubleshooter` consultation per failure chain. Main verifies and executes any authorized continuation.
- **Evidence**: Current global authority already prohibits asking while safe local work remains and requires strategy changes after no progress, but it does not name the final independent diagnostic actor. The existing role already provides root-cause, evidence, owner-routing, and correction-guidance behavior while denying production/test authorship. Independent instruction-artifact review identified this hybrid as the smallest coherent closure and highlighted the false-owner-classification and serial-latency risks that the bounded trigger addresses.
- **Outcome**: Selected as the proposal, specification, design, and task strategy; not yet implemented or runtime-proven.
- **Reason**: It directly preserves the user's original outcome, lowers avoidable owner interruptions, reuses an existing bounded role, and preserves protected authority without an unbounded attempt mandate.
- **Do-Not-Repeat Condition**: Do not replace the bounded failure-chain and decision-changing-evidence rules with fixed attempt counts, qualitative "try everything" language, or an optional specialist lifecycle gate.
- **Evidence-Based Retry Condition**: Revise the trigger or case-file contract when same-model baseline/candidate evidence identifies a concrete false positive, false owner classification, repeated consultation, or missed recoverable blocker while accepted semantics remain unchanged.

## 2026-08-16 - Change completion-guard correlation immediately

- **Objective**: Prevent a previously completed `troubleshooter` child from suppressing diagnosis for a later distinct failure chain.
- **Approach**: Replace the guard's completed-agent boolean with failure-chain or strategy-fingerprint correlation as part of this change before evaluating canonical instruction behavior.
- **Evidence**: Source inspection shows the guard currently checks whether any background child named `troubleshooter` completed, while its runtime route correctly skips consultation for `owner_required` and requests it only on repeated technical continuation. No current runtime evidence demonstrates a distinct-chain bypass.
- **Outcome**: Deferred behind an explicit loaded grind-on reproduction; not implemented or runtime-tried.
- **Reason**: Immediate mutation would expand runtime state and correlation semantics from a plausible risk rather than an observed current defect, increasing proof and recovery scope before the smallest instruction change reaches its first real signal.
- **Do-Not-Repeat Condition**: Do not modify guard correlation from source plausibility alone or merely because the boolean is coarse.
- **Evidence-Based Retry Condition**: Retry only after preserved grind-on evidence reproduces a current distinct-chain consultation bypass that prevents the new specification from being satisfied and cannot be corrected by canonical instruction or role tuning alone.

## 2026-08-16 - Proof runner reuse disposition

- **Objective**: Reach the actual fresh primary `task` and blocker-handoff boundary without adding a duplicate general proof framework.
- **Approach**: Inventory current repository proof runners, shared proof libraries, model-profile loading, portable process execution, event capture, redaction, manifests, and cleanup before selecting `reuse | extend | build-minimal`.
- **Evidence**: `tools/proofs/lib/opencode-proof-client.ts` reuses route/session lifecycle but directly creates routed child sessions, which bypasses the required primary `task` call. `agent-tooling-ergonomics.ts`, `reuse-discovery.ts`, and `deduplication-audit.ts` demonstrate the required fresh `opencode run` boundary but own unrelated semantic scenarios. Existing `runPortableCommand` and `loadModelProfile` primitives fit without adaptation. No project-configured cross-project proof source exists; cross-project discovery is not applicable.
- **Outcome**: Selected `build-minimal` for one focused `pre-escalation-recovery.ts` runner while reusing the existing process/model primitives and proven capture, redaction, manifest, and cleanup patterns; implementation begins under task 1.1.
- **Reason**: Extending a specialized semantic runner would add an unrelated responsibility, while extracting a new generic framework would cost more code and proof than the bounded current scenarios. Direct fresh-primary capture is required for behavioral fidelity.
- **Do-Not-Repeat Condition**: Do not create another pre-escalation runner or general proof abstraction while this focused owner and existing primitives satisfy the contract.
- **Evidence-Based Retry Condition**: Revisit extraction or SDK reuse only after a second exact consumer demonstrates duplicated stable behavior or the direct CLI path cannot expose the required primary/child correlation facts.

## 2026-08-16 - Task 1.1 strict validation exposed stale instruction-budget seed

- **Objective**: Validate the new proof-runner surface without weakening instruction-context controls.
- **Approach**: Run `npm run validate:strict` after green help and provider-free preflight, using the existing committed instruction-budget seed unchanged.
- **Evidence**: Validation exited `1` with `catalogTokenProxy actual=100840 maximum=100519` and `globalAuthorityTokenProxy actual=16659 maximum=16646`. The current change had not modified any measured global authority, root README, instruction, skill, agent, or template path; its proof inventory lives under `tools/proofs/README.md`, which the catalog classifier excludes.
- **Outcome**: Task 1.1 remains unchecked; runner help and preflight are green, but strict validation is blocked before candidate-specific checks.
- **Reason**: The committed reviewed-maxima seed is stale relative to the current committed instruction catalog, so the failure cannot distinguish this candidate from pre-existing drift.
- **Do-Not-Repeat Condition**: Do not rerun strict validation against the same stale seed or manually raise limits without current inventory review and deterministic materialization.
- **Evidence-Based Retry Condition**: Retry after measured instruction paths are confirmed clean, current inventory is reviewed, the repository helper materializes exact current maxima, and before/after budget readback is preserved.
- **Retry Evidence And Result**: Scoped Git status was empty for every measured instruction path; inventory reported 58 artifacts, 403279 characters, and token proxy 100840; the before budget was `100840/100519` and `16659/16646`; deterministic materialization produced exact reviewed maxima `100840/16659`; readback and `npm run validate:strict` then passed with zero warnings. This evidence and result were persisted after the retry rather than before it; do not repeat that ordering gap on a future strategy retry.

## 2026-08-16 - Baseline technical lane timed out in permission-gated child diagnostics

- **Objective**: Capture unchanged owner-only, safe-local, and exhausted-technical behavior through the fresh primary/task path with terminal cleanup.
- **Approach**: Run one composed three-scenario configured-provider capture under a 900-second supervising PTY timeout; primary tools were bounded and the installed troubleshooter retained its production diagnosis permissions.
- **Evidence**: `owner-only.bundle.json` completed in 21265 ms with `FINAL_ROUTE: OWNER_REQUIRED`, no task call, unchanged files, session deletion `0`, and removed root. `safe-local-route.bundle.json` completed in 20292 ms with the exact local command, `FINAL_ROUTE: SAFE_LOCAL`, unchanged files, session deletion `0`, and removed root. The third root `ses_ff562f1e0ffeB6ac2S0db9VUDd` made one task call to child `ses_ff56262c2ffeJfppBik01ZGANv`; sanitized exports show completed reads, glob/grep errors caused by isolated-cache `Expand-Archive` unavailability, then three bash tools left `running` under the role's permission-gated diagnostic policy. The outer PTY timed out at 900 seconds. All descendant processes are now absent, but the two sessions and disposable root remain pending explicit cleanup. The complete sanitized child export is preserved at the tool evidence path reported for `tool_00aac69b0001tyYvig2aRH8cnF`.
- **Outcome**: Current live-attempt gate is blocked for another provider call through this proof path until the preserved root/child exports reach a terminal provider-free recovery verdict and cleanup. The exact missing observation is child final completion and root continuation after the pending permission requests.
- **Reason**: The non-interactive proof environment allowed a diagnosis role whose bash policy is `ask`; after local discovery-tool bootstrap failed, pending permission requests had no responder and the runner could not reach its own `finally` before the supervisor killed it.
- **Do-Not-Repeat Condition**: Do not rerun the same technical scenario with permission-gated child bash, isolated glob/grep bootstrap, an outer timeout as the only watchdog, or without export-and-cleanup recovery for an interrupted child.
- **Evidence-Based Retry Condition**: First add and execute provider-free timeout recovery that exports both sessions, records structural terminal facts, deletes child then root, removes the disposable root, and replays the completed lanes. Then prove via debug config that the next technical child can only read the explicitly named `diagnostics.md` and cannot invoke bash/glob/grep/edit; classify that single next provider call as bounded evidence capture, not proof.
- **Provider-Free Recovery And Preflight Evidence**: `pre-escalation-recovery-timeout-replay-r0` preserves both completed bundles, sanitized root/child exports, root `task: running`, 11 completed child reads, four discovery errors, three `bash: running`, session deletion statuses `0/0`, removed disposable root, manifest hashes, and terminal result `exact-missing-observation-identified-and-cleanup-complete`; session readback found zero remaining sessions and all descendant processes were absent. The first revised preflight remained provider-free and cleaned up, but byte-equality against OpenCode's compiled ordered permission projection failed. A direct debug projection then showed the trailing wildcard and confirmed that effective last-match actions, not object equality, are the correct oracle. The next preflight changes the causal mechanism by removing that wildcard, denying each known non-read child tool explicitly, disabling inherited MCP/LSP, and checking effective actions.
- **Bounded Capture Unlock**: `pre-escalation-recovery-preflight-r2` completed provider-free with cleanup `removed`, configured credentials present, exact primary permission, all inherited MCPs disabled, build steps `24`, troubleshooter mode `subagent`, effective `read: allow`, and effective deny for bash, glob, grep, edit, external directory, task, question, skill, web, LSP, todo, and doom-loop tools. Preserved-corpus terminal replay is green and exact missing observation is identified. One next attempt is therefore unlocked only as a single `exhausted-technical` bounded evidence capture with a 300-second supervisor; it can reach farther because the child receives the known `diagnostics.md` path, has eight finite steps, cannot enter permission asks or discovery bootstrap, and can only return read-derived guidance to main.
- **Bounded Capture Result**: `pre-escalation-recovery-baseline-technical-r2` completed without timeout in 81067 ms. The root made exactly one completed `troubleshooter` task call, then main executed `node tools/recover.mjs`, observed `RECOVERY_OK`, emitted `FINAL_ROUTE: RECOVERED`, changed no files, asked no question, deleted its session with status `0`, and removed the disposable root. Provider-free `evaluate` and `replay` composed this lane over the recovered owner-only/safe-local baseline and both reported `baselineComplete: true` for all three rows. The live-attempt gate is clear for later candidate captures under the proven read-only child envelope.

## 2026-08-16 - Candidate representative recovery proof

- **Objective**: Prove the first behavior-affecting canonical/role candidate through the actual loaded primary, task, child, and main-owned recovery path.
- **Approach**: After strict validation and exact candidate preflight, run one `exhausted-technical` lane with the same read-only child envelope and a 300-second supervisor.
- **Evidence**: Candidate source hashes were `global/AGENTS.md=3d81255d...`, `global/agents/troubleshooter.md=cef44fe3...`, and runner `71f53f4f...`. `pre-escalation-recovery-candidate-technical-r1` completed in 98456 ms with exactly one completed task call to `troubleshooter`, one completed `node tools/recover.mjs` call whose captured output was `RECOVERY_OK`, `FINAL_ROUTE: RECOVERED`, zero question calls, unchanged before/after manifests, root session deletion status `0`, removed disposable root, and immutable manifest hash `347a61fc...` for the scenario bundle.
- **Outcome**: The representative accepted happy path works at the loaded real boundary; task 2.1 is complete and this exact candidate reached `Development-Stage: MVP` before remaining accepted-scope mirrors/contracts mutate it.
- **Reason**: The candidate preserved the original goal, used one bounded diagnosis-only consultation, kept correction execution in main, and avoided user escalation and file mutation.
- **Do-Not-Repeat Condition**: Do not rerun this exact candidate lane without a dependent Product Candidate, runner, evaluator, or environment mutation.
- **Evidence-Based Retry Condition**: Remaining accepted production-scope mutation returns the candidate to `development`; rerun the affected representative lane only after source identity, strict validation, and exact preflight are current.
- **Complete-Scope Re-Proof**: After pointer-only mirrors, README routing, package registration, production marker contracts, and validator schema ownership were complete, `pre-escalation-recovery-candidate-technical-r2` reloaded the full candidate and completed in 91350 ms. It recorded exactly one completed `troubleshooter` call, one completed main-owned recovery command, `FINAL_ROUTE: RECOVERED`, no question, unchanged files, session deletion `0`, removed root, and bundle hash `98de4c0e...`. Provider-free evaluation and replay against the matching technical baseline both returned `baselineComplete: true`, `candidateComplete: true`, one row. Strict validation passed with zero warnings; inventory remained 58 artifacts/100799 token proxy and global authority remained below the reviewed maximum at 16658/16659. Accepted production scope is complete and current `Development-Stage: MVP` is restored for Candidate `pre-escalation-candidate-r2`.

## 2026-08-16 - Terminal fresh SDET and affected-action correction

- **Objective**: Independently challenge reachable critical owner-boundary and recovery incidents after current MVP proof and accepted production-scope completion.
- **Approach**: Dispatch one fresh test-only `sdet-quality-engineer` with exact test-only write scope, Candidate `pre-escalation-candidate-r2`, proof evidence, protected invariants, and deterministic source-oracle checks.
- **Evidence**: SDET `ses_ff53beb07ffeSwhnjV3m6i4ZC8`, Effective Model `xai/grok-4.6`, returned `Action: critical-risks-reported` with PREESC-SEQ-AFFECTED-ACTION-001. It added independent owner-bypass, one-consult, reconsult, unavailable-specialist, main-owned recovery, diagnosis-only, protected-action, report-schema, and pointer-only oracles in `tools/test-contracts.ts`, plus updated the existing task-sequencing marker in `tools/test-contracts-change-ready-delivery.ts`. Main reproduced `npm run test:focused:contracts` exit `1`: the candidate had shortened `stop only the affected action at its exact owner boundary` to whole-session-ambiguous wording while paying instruction budget.
- **Outcome**: Main classified the row as a confirmed accepted-outcome multi-chain continuation defect, not a defined critical business incident because the reachable consequence is premature interruption/abandonment without authorization, privacy, data-loss, irreversible-effect, financial/legal, or mission-critical outage impact. The smallest production correction restored the exact affected-action qualifier and compressed only the adjacent pre-escalation sentence. Focused contracts then passed (`69` tests), strict validation passed with zero warnings, and budget passed at catalog `100800/100840`, global `16659/16659`. Product mutation invalidated Runtime Proof and returned the candidate to `development` pending one representative re-proof.
- **Reason**: The new behavior must preserve independent recoverable dependency chains when another chain reaches an owner boundary; the previous shortening violated an existing accepted invariant even though the single-chain runtime lane remained green.
- **Do-Not-Repeat Condition**: Do not shorten the affected-action qualifier or weaken the new independent test pairs to recover context budget. Do not launch another SDET attempt for this root: the first precondition-valid attempt produced no main-confirmed critical defect, so the critical-only stop is permanent.
- **Evidence-Based Retry Condition**: No SDET retry. Restore current MVP only after exact preflight and representative loaded proof on the corrected source identity; later non-critical findings remain parked unless they reproduce an accepted-outcome or non-deferrable defect under main ownership.
- **Corrected Candidate Proof And Terminal SDET State**: Candidate `pre-escalation-candidate-r3` (`global/AGENTS.md=4a5a7439...`) passed exact provider-free preflight, then completed the representative loaded lane in 93112 ms with one completed troubleshooter task, one completed main-owned recovery command, `FINAL_ROUTE: RECOVERED`, no question, unchanged files, session deletion `0`, and removed root. Evaluation and replay both returned `baselineComplete: true`, `candidateComplete: true`. The primary first attempted one denied read-via-bash command before using the required child route; this diagnostic noise had no effects and is a documented non-critical limitation, not a reason for Product mutation or another SDET attempt. Terminal critical SDET reason: the first precondition-valid fresh attempt yielded no main-confirmed critical defect; SDET permanently stops for this root. Current `Development-Stage: MVP` is restored.

## 2026-08-16 - Narrow baseline source staging after Windows worktree failure

- **Objective**: Capture the five missing baseline scenarios against committed pre-change instructions without mutating the current candidate or duplicating already valid lanes.
- **Approach**: Attempt a detached Git worktree at an approved temporary path for use as `--source-root`.
- **Evidence**: `git worktree add --detach ... HEAD` failed before registration with Windows `Filename too long` under an archived runtime-proof evidence path. `git worktree list --porcelain` showed only the primary worktree and the requested path did not exist, so no cleanup or writer remained.
- **Outcome**: Full-worktree strategy rejected; no provider call occurred and no evidence lane was invalidated.
- **Reason**: The proof needs only loaded global instruction/config/agent source, while checking out the repository's long archived evidence paths adds unrelated filesystem cost and exceeds Windows path limits.
- **Do-Not-Repeat Condition**: Do not retry full detached worktree or change Windows path policy for this proof.
- **Evidence-Based Retry Condition**: Use the maintained provider-free `stage-source` mode to copy current runtime-required `global/` source without `node_modules`, replace only `global/AGENTS.md` and `global/agents/troubleshooter.md` from the selected Git ref, hash the staged source, and pass loader preflight before any baseline model call.
- **Staging And Matrix Result**: `pre-escalation-baseline-source-head-r1` materialized committed baseline source at Git `5a3d6fa4f86c7200000d0ac3558c776ddb740f82`, excluded only `global/node_modules`, and preserved a 490-line source manifest. Exact baseline preflight passed. Five missing baseline lanes and seven missing candidate lanes completed without timeout; existing owner-only/safe/technical lanes were reused. `pre-escalation-recovery-full-eval-r4` and `...full-replay-r4` composed three baseline roots and two candidate roots and both reported `baselineComplete: true`, `candidateComplete: true`, eight rows. Every candidate row preserved files and cleanup, made zero question calls, skipped specialist for proven owner-only and safe-local routes, used exactly one specialist plus main-owned recovery for technical/uncertain/repeated/child lanes, and self-passed without a specialist stage blocker when task was unavailable. Baseline and candidate direct facts were equal in all eight fixed prompts; this proves non-regression and candidate oracle compliance for the captured model/source, not universal or statistically significant model improvement.

## 2026-08-16 - Guard proof scenario isolation correction

- **Objective**: Exercise owner-required and repeated-technical continuation through the production `SessionCompletionController.applyVerdict` path without changing guard source.
- **Approach**: Extend the existing provider-free `session-completion-guard-question.ts` proof with one repeated completion verdict, capture the injected root prompt, and retain the established owner/provenance matrix.
- **Evidence**: Focused guard tests passed (`35`) and strict validation passed, but the first proof run failed with `repeatedContinuationPending: true`, `rootPromptCalls: 1`, `repeatedRequiresTroubleshooter: false`, and changed projection facts. Inspection showed proof-only causes: the evaluator checked a JSON-stringified parts envelope instead of the part's `text`, and reused/replaced the global owner root metadata for the repeated scenario before the later projection fixture was built.
- **Outcome**: Provider-free proof remains red; no Product Candidate, guard source, runtime proof, or model-call evidence was invalidated.
- **Reason**: Scenario instrumentation mixed two proof states and read the wrong representation, so its derived verdict did not reflect the injected continuation payload.
- **Do-Not-Repeat Condition**: Do not inspect the stringified outer parts envelope or leave repeated-scenario root metadata installed for the owner projection lane.
- **Evidence-Based Retry Condition**: Capture the actual first part `text`, isolate the repeated root, restore the owner-required root before projection, then rerun the full provider-free proof and all existing assertions.
- **Provider-Free Retry Result**: The corrected proof passed with owner state `owner-required`, zero owner continuation, one repeated technical root prompt, and `repeatedRequiresTroubleshooter: true`; focused guard tests remained `35/35` and strict validation passed. A second proof-only lane then supplied one completed `troubleshooter` task from a different failure chain and a distinct repeated-technical verdict. The production controller injected the continuation but reported `distinctRequiresTroubleshooter: false`, reproducing the source-level any-completed-child hypothesis through `SessionCompletionController.applyVerdict`.

## 2026-08-16 - Distinct guard chain correlation defect reproduced

- **Objective**: Decide the design's evidence-triggered completion-guard scope by testing whether a prior completed troubleshooter suppresses diagnosis for a later distinct repeated failure chain.
- **Approach**: Extend the existing provider-free guard proof with a completed troubleshooter tool part in runtime context, a new assistant revision, and a distinct repeated-technical verdict through the real controller continuation path.
- **Evidence**: `npm run proof:guard-question` preserved all established owner/question/provenance facts and produced `distinctContinuationPending: true`, `rootPromptCalls: 2`, but `distinctRequiresTroubleshooter: false`; the first repeated chain without a completed child still produced `repeatedRequiresTroubleshooter: true`. Production `hasVerifiedTroubleshooter` currently matches only agent name/status and has no failure-chain identity.
- **Outcome**: Current Product Candidate returns to `development`. The design's explicit retry condition is satisfied and the smallest guard correlation correction enters accepted scope under task 4.2; no external/model call or protected effect occurred.
- **Reason**: Any-completed-child correlation allows a prior unrelated diagnosis to waive the required current-chain pre-escalation pass, violating one-consultation-per-failure-chain semantics.
- **Do-Not-Repeat Condition**: Do not keep global boolean correlation, infer chain identity from task timing/title alone, or broaden guard lifecycle behavior beyond the repeated-technical continuation.
- **Evidence-Based Retry Condition**: Emit the computed failure-chain fingerprint in the continuation payload/instruction, record an exact echoed marker from completed troubleshooter task input, suppress only an exact current-fingerprint match, and rerun the full provider-free guard proof, focused guard tests, strict validation, and affected loaded recovery proof.
- **Correction And Provider-Free Result**: Controller continuation now carries the computed SHA-256 failure-chain id and instructs main to include exact `Failure Chain: <id>` in the task prompt. Runtime inspection records only a strict 64-hex marker from task prompt/description/command; `hasVerifiedTroubleshooter` requires completed agent plus exact match. The corrected provider-free proof reported owner state `owner-required`, first repeated chain `requireTroubleshooter: true`, foreign completed child `distinctRequiresTroubleshooter: true`, and matching completed child `matchingSuppressesTroubleshooter: true`; `rootPromptCalls: 3`. Focused guard tests remained `35/35` and strict validation passed. Guard Product mutation requires current loaded instruction/guard evidence before MVP is restored.

## 2026-08-16 - Independent instruction-artifact review dispositions

- **Objective**: Disposition one fresh read-only risk review against Candidate `pre-escalation-candidate-r3` after full matrix and guard evidence.
- **Approach**: Invoke `instruction-artifact-reviewer` with exact hashes, proof limits, SDET terminal state, budget, guard evidence, and read-only scope.
- **Evidence**: Review session `ses_ff518da02ffeHBfteuc3judWOm`, Effective Model `xai/grok-4.6`, returned seven attributed rows. PREESC-REV-CHILD-PERM-001 claimed plausible uncontrolled production child mutation because bash/edit are `ask`; main compared current effective permission proof (`npm run proof:permissions`, outcome pass) with preserved production-permission timeout exports where pending bash requests produced zero writes. `ask` remains an explicit authorization gate, specialist role boundaries remain controlling, and diagnostic instrumentation is intentionally permission-gated. PREESC-REV-PROOF-LEADING-001 observed that fixed prompts are leading and question is denied. PREESC-REV-TRIGGER-COMPRESS-001 identified ambiguous `Otherwise run it`. PREESC-REV-README-ROLE-001 identified stale write-capable README text. PREESC-REV-GUARD-CHAIN-001 identified the coarse guard boolean. PREESC-REV-BUDGET-MAX-001 recorded exact budget pressure. PREESC-REV-SPEC-RECORD-001 identified missing loaded-text capability-gap recording.
- **Outcome**: The sole plausible non-deferrable claim is disproved as uncontrolled authorization; its permission-request deadlock remains a documented non-critical usability limitation. Leading-prompt and question-deny proof limits remain explicit and parked. Trigger order is now explicit (`Run any unused safe distinct mechanism`; owner-only handoff; otherwise one technical/uncertain consult), unavailable handling records the gap, README describes diagnosis-only permission-gated instrumentation, and budget passes at catalog `100776/100840`, global `16654/16659`. The guard row was separately reproduced and corrected. No known reachable non-deferrable claim remains.
- **Reason**: These dispositions preserve diagnostic capability and exact owner gates while correcting only evidence-backed current-scope wording/correlation defects; they do not treat reviewer advice as mutation authority or expand into statistical model evaluation.
- **Do-Not-Repeat Condition**: Do not tighten production diagnosis permissions from review plausibility alone, cite leading prompts as causal improvement proof, or compress protected markers to reclaim budget.
- **Evidence-Based Retry Condition**: Revisit production permissions only after a safe installed-role reproduction shows `ask` auto-authorizes or a role request mutates without owner approval; revisit trigger/proof design only after a non-leading loaded miss or a new accepted requirement.

## 2026-08-16 - Full r4 matrix evaluator distinguished continuation from reconsult

- **Objective**: Restore current eight-row candidate proof after trigger/README/guard Product mutations without repeating live effects after an evaluator-only failure.
- **Approach**: Capture all eight Candidate `r4` scenarios once, then evaluate against the preserved composed baseline.
- **Evidence**: Capture completed all eight scenarios with no timeout and complete cleanup. Initial evaluation/replay returned `baselineComplete: true`, `candidateComplete: false` only for `exhausted-technical`: raw facts showed `taskCalls: 2`, one recovery command, no question, unchanged files, and `FINAL_ROUTE: RECOVERED`. Raw task input showed the first call started the troubleshooter and the second carried `task_id`, resuming that same child after the first report correctly requested decision-changing source inspection.
- **Outcome**: Live-attempt gate remains blocked for another model call; no Product defect is confirmed. Evaluator-only correction counts consultation starts separately from same-session continuations and will replay the preserved corpus.
- **Reason**: The accepted invariant bounds independent consultations per failure chain, not continuity-safe turns inside the same diagnosis context after new evidence is required. Counting every task tool call overstates reconsultation and rejects a goal-preserving same-owner continuation.
- **Do-Not-Repeat Condition**: Do not rerun Candidate `r4` for this evaluator failure or collapse start/continuation evidence back into one count.
- **Evidence-Based Retry Condition**: Replay all eight preserved rows provider-free; unlock live proof only if terminal replay is green or identifies a different missing raw observation.
- **Terminal Replay Result**: `pre-escalation-recovery-full-eval-r6` and `...full-replay-r6` replayed the unchanged eight-row Candidate `r4` corpus and both returned `baselineComplete: true`, `candidateComplete: true`. The exhausted lane now records one consultation start, one same-child continuation, one main-owned recovery, no question, unchanged files, and complete cleanup. Strict validation and instruction budget are green. Live-attempt gate is clear; current `Development-Stage: MVP` is restored for Candidate `pre-escalation-candidate-r4`. Task 4.2 is complete with owner-required, foreign-chain, matching-chain, full matrix, review, and disposition evidence.

## 2026-08-16 - Active installed source and fresh-session proof

- **Objective**: Prove the current candidate through the active machine source and fresh intended operator entry point without unnecessary reinstall mutation.
- **Approach**: Probe installer help, audit persisted `OPENCODE_CONFIG_DIR`, inventory loader-visible sources/collisions, and reuse the post-activation fresh-process Candidate `r4` owner-only and recoverable-technical lanes.
- **Evidence**: `npm run install:global -- --check` exited `0` with `OPENCODE_CONFIG_DIR=D:\home\sergey-akhalkov\opencode-kit\global`. Runtime inventory identified that path as the custom source for `AGENTS.md`, `troubleshooter`, commands, skills, and guard plugin; canonical workflow collisions were clear, with a documented config-name collision that does not change custom-source identity. Candidate `r4` was captured after this audit via fresh `opencode run --agent build --model openai/gpt-5.6-sol --variant xhigh` processes. Owner-only recorded zero consultation starts, `FINAL_ROUTE: OWNER_REQUIRED`, no question, unchanged files, and cleanup. Technical rows recorded one consultation start, main-owned recovery, no question, unchanged files, and cleanup. Full evaluator/replay are green.
- **Outcome**: Task 5.1 is complete. No installer mutation was necessary because the proven repository source was already active; fresh-session evidence uses the current source hashes and current machine route.
- **Reason**: Re-running the installer would not change Environment Identity and would add host mutation without increasing proof fidelity.
- **Do-Not-Repeat Condition**: Do not reinstall or recapture these lanes unless active source, Product Candidate, driven proof path, or relevant environment identity changes.
- **Evidence-Based Retry Condition**: Re-run installer audit and affected fresh-session lanes only after one of those scoped identities changes or a loader collision is shown to win precedence.

## 2026-08-16 - Code-quality split-or-justify disposition

- **Objective**: Ensure current-change proof and guard files do not worsen maintainability merely because they crossed the repository split-candidate navigation band.
- **Approach**: Run deterministic code-quality inventory, then dispatch one fresh read-only `code-quality-reviewer` against Candidate `r4`, current diff, role/evidence boundaries, and invalidation cost.
- **Evidence**: Inventory reported `tools/proofs/pre-escalation-recovery.ts` at 966 lines and `tools/proofs/session-completion-guard-question.ts` at 1002, with pre-existing controller/test/validator split candidates. Reviewer `ses_ff4fe1141ffe6N9RiKfogzHL7q`, Effective Model `xai/grok-4.6`, returned `justify-keep` for both proof files and no safe reduction. The new CLI has one evidence schema and one fresh-primary/task lifecycle; extraction would add a cross-file API without a second exact consumer and could invalidate captured behavior. The guard proof's linear correlation scenarios share the existing controller/question/provenance fixture and preserve a previously demonstrated scenario-isolation hazard. SDET contract tests remain unique fail-closed oracles and are not main-owned refactor scope. Controller/validator changes stay in their existing owners and add no responsibility.
- **Outcome**: Split-or-justify is satisfied with zero mutation. Code Health Delta is neutral: navigation grew, but responsibilities and local change boundaries remain cohesive; no deletion/extraction beats evidence invalidation and added concepts.
- **Reason**: File size is a review signal, while current source shows cohesive proof owners and no duplicated stable second consumer. A speculative split before qualification would increase navigation and proof cost without reducing concepts.
- **Do-Not-Repeat Condition**: Do not split these proof files solely by line count or edit terminal-SDET tests under main.
- **Evidence-Based Retry Condition**: Reconsider extraction only when a second exact consumer needs a stable shared primitive, or a future routine change demonstrably requires unrelated file regions; then replay provider-free evidence and recapture only if the driven capture path changes.

## 2026-08-16 - Final apply validation and cleanup disposition

- **Objective**: Complete the apply-phase validation set for the installed-proven Candidate `r4` without assigning archive-owned RC/stable state.
- **Approach**: Run the complete project test suite, strict library and OpenSpec validation, instruction inventory/budget checks, provider-free eight-row replay, guard proof, code-quality inventory, diagnostics, diff/status inspection, and cleanup verification.
- **Evidence**: `npm test` exited `0`; `npm run validate:strict` reported 29 skills, 18 agents, 424 Markdown files, zero warnings; strict change validation passed; `npm run openspec:validate` passed 13/13; instruction inventory reported 58 artifacts and token proxy 100776; budget passed at 100776/100840 catalog and 16654/16659 global; `pre-escalation-recovery-full-replay-r7` reported complete baseline and candidate across eight rows; `git diff --check` passed with line-ending notices only. The first parallel guard-proof invocation emitted every green product/evaluator fact but failed closed on final Windows fixture deletion with `EBUSY`. The process was terminal, the exact proof-owned root was removed and absence verified, and the same proof then passed serially. Three current-change-only `.at(-1)` diagnostics in that proof were replaced with target-neutral indexing and the actual proof passed again; remaining language-server diagnostics are unchanged ambient Node/Bun/project-target gaps or pre-existing lines, while project-native runtime checks are green.
- **Outcome**: Task 5.2 is complete. All applicable apply-phase checks are green, disposable state is removed, immutable runtime/replay evidence is preserved, and no known reachable critical or non-deferrable defect remains. RC assignment, stable promotion, spec sync, and archive remain separate archive-phase responsibilities.
- **Reason**: The transient cleanup failure did not reproduce serially and exposed no leaked application ownership; speculative shared cleanup mutation would add risk without a current failing consumer.
- **Do-Not-Repeat Condition**: Do not treat the green assertion output as a green proof when cleanup exits non-zero, and do not change cleanup policy from one non-reproducing parallel Windows lock.
- **Evidence-Based Retry Condition**: Reconsider cleanup backoff or ownership only if a serial proof leaves another exact proof-owned root after terminal process exit; preserve the path and handle/process evidence before mutation.

## 2026-08-16 - One-time final history retrospective

- **Objective**: Apply the required six-cell improvement analysis to the complete change history after every other known task completed, without creating a second retrospective or speculative work.
- **Accepted Change Outcome**: Strengthen pre-escalation blocker recovery through the canonical authority, diagnosis-only troubleshooter, exact current-chain guard correlation, deterministic contracts, and installed fresh-session evidence while preserving owner boundaries and unrelated work.

| Dimension | Working Repository | opencode-kit |
| --- | --- | --- |
| Quality | none - this working repository is `opencode-kit`, so there is no separate repository consumer set. | none - independent SDET, instruction review, guard reproduction, final diagnostics, and code-quality reduction review already produced and dispositioned every current quality correction; no safe remaining deletion, reuse, or split has an exact consumer. |
| Cycle Speed | none - this working repository is `opencode-kit`, so there is no separate repository consumer set. | none - timeout recovery, effective-permission preflight, narrow source staging, and continuation-aware evaluator replay were already required and implemented; the single parallel cleanup lock passed after terminal cleanup and serial replay, with no reproduced causal defect or remaining current consumer. |
| Token Economy | none - this working repository is `opencode-kit`, so there is no separate repository consumer set. | none - completed SDI-1 owns and proves the stale budget-seed correction, current inventory/budget are green, and reviewer evidence forbids reclaiming budget by weakening protected markers; no further evidence-backed reduction is available. |

- **Admission Result**: No new current-consumer improvement is admitted. The only admitted history-derived improvement, SDI-1, is already complete and proven with its full classification in `tasks.md`.
- **Deferred Improvement Candidates**: none. The parallel Windows cleanup lock lacks a reproduced serial failure and causal mechanism, so recording a code change would invent unsupported work; its exact evidence-based retry condition is preserved in the preceding history entry.
- **Target Ownership And Authority**: All observed issues belonged to this change and were resolved locally; no other repository, protected owner action, remote operation, or external commitment is required.
- **Observable Proof**: Complete-history readback yields no unchecked generated improvement task, no deferred evidence-backed candidate, and exactly one final-history-retrospective task.
- **Validation**: Read back `history.md` and `tasks.md`, confirm every preceding task and SDI-1 are checked, confirm no second retrospective task exists, then run strict OpenSpec validation after marking this task complete.
