## 1. Baseline And Proof Boundary

- [x] 1.1 Inventory existing proof-runner primitives, record `reuse | extend | build-minimal`, then add the smallest focused pre-escalation recovery proof runner and proof-inventory entry without changing loaded instruction behavior. **Current Rung:** R0 direct-source review. **Next Real Boundary:** the runner's actual `--help`, preflight, and disposable OpenCode loader path. **Blocker/Unblocker:** none; reuse existing process, model-profile, capture, redaction, evaluator, and cleanup primitives where cohesive, otherwise record why a focused runner is the smaller owner. **Owner Authorization:** bounded non-sensitive configured-provider calls are already authorized; help and preflight make no model calls. **Safeguards:** deny credentials, external directories, remote/destructive/install/publish effects, nested arbitrary agents, and user authority simulation; use explicit input/output schemas and stable ordering. **Restoration/Cleanup:** remove disposable sessions and projects in `finally`; retain create-new immutable evidence roots. **Expected Immutable Evidence:** source hashes, runner/evaluator hashes, exact redacted argv/exits/stdout/stderr, model/profile/permission identity, manifests, cleanup result, and artifact paths. **Observable Proof:** `--help` exits `0` with no writes or model calls, preflight proves route/tool availability, and the proof inventory states boundary, inputs, effects, cleanup, and limits. **Validation:** focused runner checks plus `npm run validate:strict` for the new maintained tooling surface.

- [x] 1.2 Capture the unchanged baseline for a proven owner-only blocker, an unused safe local route, and an exhausted technical blocker before mutating the Product Candidate. **Current Rung:** R1 proof runner available. **Next Real Boundary:** actual loaded baseline OpenCode main/task/question behavior in disposable projects. **Blocker/Unblocker:** task 1.1; stop this dependency chain if preflight cannot prove the configured model, task adapter, source identity, or safe cleanup. **Owner Authorization:** the fixed synthetic non-secret calls are covered by standing kit-validation authorization. **Safeguards:** identical prompts, model, variant, permissions, environment, and initial state will be reused for candidate capture; no protected effects or owner decisions are performed. **Restoration/Cleanup:** dispose sessions/projects after each capture and verify absence; preserve evidence only. **Expected Immutable Evidence:** one complete baseline bundle per scenario with tool calls, assistant text, source identity, outputs, final route, and cleanup. **Observable Proof:** baseline evaluator reports observed behavior without asserting candidate success and records any missing observation explicitly. **Validation:** runner baseline completeness and offline evaluator/replay over the preserved bundles.

## 2. Canonical Recovery Behavior

- [x] 2.1 Consolidate the canonical pre-escalation recovery rule into the existing autonomy and owner-handoff authority in `global/AGENTS.md`, and update `global/agents/troubleshooter.md` to accept the complete goal-preserving case file and return one bounded recovery disposition while remaining diagnosis-only. **Current Rung:** R1 baseline captured. **Next Real Boundary:** one disposable loaded candidate scenario in which an exhausted technical blocker receives exactly one consultation and main executes the authorized route without a user question. **Blocker/Unblocker:** task 1.2; no candidate mutation before baseline identity and completeness are proven. **Owner Authorization:** local reversible instruction and agent edits only. **Safeguards:** preserve every credential, elevation, destructive/remote, deploy/release, cost, protected-semantic, security/privacy/authorization, legal, and external-capability owner boundary; keep `troubleshooter` production/test mutation, question, task, and lifecycle authority denied. **Restoration/Cleanup:** repository diff remains reversible; do not activate globally yet. **Expected Immutable Evidence:** candidate source hashes, focused diff, retained permission contract, and raw representative loaded-run bundle. **Observable Proof:** the technical recovery scenario advances the original synthetic task through main-owned action, emits no blocker question, and shows no production/test edit by the child. **Validation:** run-observe-correct through the loaded candidate boundary; green proof promotes the candidate to MVP, while a red result returns it to `development` and requires causal correction before continuing.

- [x] 2.2 Complete the accepted production scope with concise routing/pointer updates, required-text contracts, validator behavior, proof script registration, and proof inventory documentation without copying the full policy outside the canonical owner. **Current Rung:** R2 representative candidate behavior proven. **Next Real Boundary:** reload the complete scoped candidate and replay the representative technical recovery after every behavior-affecting instruction or validator mutation. **Blocker/Unblocker:** task 2.1; no dependent test design until the complete production scope has current proof. **Owner Authorization:** local reversible source, configuration-template, documentation, validator, and package-script edits only; no activation yet. **Safeguards:** keep `global/AGENTS.md` the only complete policy source, preserve pointer-only mirrors, do not add a new skill/plugin/classifier, and pay for always-loaded text by consolidating superseded overlap rather than deleting unrelated safety authority. **Restoration/Cleanup:** preserve unrelated work and retain the baseline/candidate source identities. **Expected Immutable Evidence:** complete scoped diff, instruction inventory and budget snapshots, validator diagnostics, and refreshed representative runtime bundle. **Observable Proof:** installed-source candidate inventory contains the canonical rule and role delta with no conflicting complete mirror; the representative recovery remains green. **Validation:** instruction inventory/budget commands, relevant validator invocation, and runtime re-proof after the final Product Candidate mutation.

## 3. Independent Critical Testing

- [x] 3.1 After MVP and accepted production-scope completion, dispatch one fresh test-only SDET to challenge only reachable critical incidents: false owner-only classification, protected-action substitution, repeated equivalent consultation, missing-specialist behavior, child production/test mutation, and user escalation despite an authorized recovery. Main shall independently reproduce and disposition every row; a confirmed critical defect returns ownership to the production author, invalidates dependent proof, requires a corrected loaded proof, and permits one new fresh SDET attempt, while the first precondition-valid attempt with no main-confirmed critical progress permanently stops SDET for this root. **Current Rung:** R2 complete candidate with current representative proof. **Next Real Boundary:** SDET-authored focused automated tests and the nearest real loaded boundary available to each critical oracle. **Blocker/Unblocker:** tasks 2.1 and 2.2; an unavailable eligible fresh SDET is recorded as the exact Material qualification blocker and does not authorize production-authored test artifacts. **Owner Authorization:** no external or protected effects; test-only writes stay within explicit test/fixture scope. **Safeguards:** SDET edits no production/instruction/config source, asks no user questions, dispatches no nested agents, and does not approve lifecycle state. **Restoration/Cleanup:** disposable test state is removed; retained test artifacts must provide unique critical or compatibility value. **Expected Immutable Evidence:** SDET risk/oracle matrix, changed test paths, exact test exits/stdout/stderr, main reproductions, dispositions, and terminal SDET reason. **Observable Proof:** terminal `critical-risks-reported | no-critical-risk | blocked` evidence follows the critical-only continuation rule, with every confirmed production correction re-proven before another eligible attempt. **Validation:** run the SDET-authored focused tests and inspect their failure/success observations at the stated boundary.

## 4. Complete Behavioral Evaluation

- [x] 4.1 Capture and evaluate the complete same-model candidate matrix against the frozen baseline: proven owner-only, unused safe local route, exhausted technical blocker, authorized recovery, uncertain owner-like blocker, equivalent repeat, unavailable specialist, and child-role boundary. **Current Rung:** R2 candidate with terminal SDET evidence. **Next Real Boundary:** actual disposable loaded main/task/question paths for every scenario, followed by provider-free evaluator replay. **Blocker/Unblocker:** task 3.1; a Product Candidate mutation invalidates candidate captures and requires representative re-proof before recapture, while evaluator-only mutation replays preserved raw bundles without new model calls. **Owner Authorization:** bounded fixed synthetic calls only. **Safeguards:** baseline/candidate parity, no secrets or protected effects, one failure chain per scenario unless the scenario explicitly tests repetition, privacy-safe evidence, and no fuzzy score or inferred semantic classifier in helper code. **Restoration/Cleanup:** delete disposable roots and sessions deterministically; retain immutable raw bundles and derived reports separately. **Expected Immutable Evidence:** complete baseline/candidate manifests, tool-call facts, final-route facts, evaluator decisions, source/environment identity, cleanup, and explicit truncation or missing-observation diagnostics. **Observable Proof:** every normative spec scenario has a direct observed fact, candidate safety is no worse than baseline, technical recovery satisfies the new oracles, and offline replay reaches the same terminal verdict. **Validation:** proof runner `capture`, `evaluate`, and `replay` modes plus focused permission/contract tests.

- [x] 4.2 Exercise grind-on owner-required and repeated-technical scenarios to decide the evidence-triggered completion-guard scope, then run one bounded read-only instruction-artifact review against the exact candidate and disposition its risks. **Current Rung:** R2 complete disposable behavior matrix. **Next Real Boundary:** opt-in completion-guard continuation through the actual loaded route, without modifying guard runtime unless a current distinct-chain bypass is reproduced. **Blocker/Unblocker:** task 4.1; guard mutation is blocked by design unless raw evidence proves canonical instruction tuning cannot satisfy the requirement. **Owner Authorization:** local disposable guard state and read-only review only. **Safeguards:** `owner_required` never gains autonomous authority, reviewer evidence never authorizes mutation or lifecycle state, and any reproduced guard defect receives the smallest scoped correction followed by affected proof replay. **Restoration/Cleanup:** remove disposable guard sessions/state; preserve correlated raw evidence and the review matrix. **Expected Immutable Evidence:** guard invocation/status events, consultation count, failure-chain correlation facts, exact reviewed Candidate Reference, Effective Model, main dispositions, and replay results if mutation occurs. **Observable Proof:** owner-required skips consultation, repeated technical continuation gets the required current-chain route, and no speculative guard source change is made when the bypass is absent. **Validation:** focused session-completion-guard tests, loaded guard scenario capture, and independent instruction-artifact risk disposition.

## 5. Installed Runtime And Qualification

- [x] 5.1 Install or activate only the proven candidate global source, start a fresh OpenCode session because instruction artifacts do not hot-reload, and replay one proven owner-only and one recoverable technical scenario through the intended operator entry point. **Current Rung:** R2 disposable candidate and guard evidence green. **Next Real Boundary:** R3 fresh installed OpenCode session using the resolved active global source. **Blocker/Unblocker:** tasks 4.1 and 4.2; do not activate a candidate with red safety or technical-recovery oracles. **Owner Authorization:** standing local-machine authorization covers local installation/activation and bounded synthetic calls; no remote deployment, release, publication, or credentials. **Safeguards:** verify active-source identity before interpretation, preserve the prior reproducible installed-source identity, use non-sensitive prompts, and perform no protected action. **Restoration/Cleanup:** on failure restore the previous installed source through the documented installer/input path, restart, and verify restored identity; remove synthetic session state while retaining evidence. **Expected Immutable Evidence:** installer invocation/result, before/after active-source inventory, fresh-session model/config identity, exact scenario events and outputs, cleanup, and restoration result when used. **Observable Proof:** the fresh installed session directly hands off the exact owner-only action without consultation and removes the recoverable technical blocker without a user question. **Validation:** runtime source diagnostics plus representative installed-entry-point captures.

- [x] 5.2 Run complete project-native validation, strict OpenSpec validation, instruction inventory/budget checks, proof replay, and final diff/diagnostic inspection; prepare the next RC/stable handoff for separate archive qualification without assigning it during apply. **Current Rung:** R3 installed runtime proof green. **Next Real Boundary:** terminal local qualification and handoff for the same readable candidate. **Blocker/Unblocker:** task 5.1; any behavior-affecting mutation returns to `development` and invalidates dependent proof, while evaluator-only changes require replay rather than new live calls. **Owner Authorization:** local validation and handoff only; no commit, push, merge, release, or publication unless separately requested. **Safeguards:** preserve exact command exits/stdout/stderr, do not waive red applicable validation, park only documented non-critical limitations, and keep external-operation authority separate from stage labels. **Restoration/Cleanup:** remove disposable validation state and preserve immutable evidence paths; leave unrelated work untouched. **Expected Immutable Evidence:** `npm run validate:strict`, `npm test`, focused contract/guard results, `npm run openspec:validate`, inventory/budget reports, proof replay verdict, diff/status inspection, terminal SDET reason, and residual-risk record. **Observable Proof:** all applicable checks are green for the installed-proven candidate, no known reachable critical/non-deferrable defect remains, and the handoff reports `Development-Stage: stable` with the same `Stable Candidate: RC<n>`. **Validation:** the exact complete command set and local handoff evidence named above.

## Session-Derived Improvements

- [x] SDI-1 Re-materialize the reviewed instruction-budget seed from the current committed instruction catalog so strict validation can evaluate this change instead of failing on pre-existing seed drift. **Impact Horizon:** Current Change. **Concrete Consumers:** task 1.1 `npm run validate:strict` and task 5.2 complete validation. **Execution Class:** gate-closer. **Earliest Safe Point:** before task 1.1 can complete. **Invalidated Evidence:** prior instruction-budget seed verdict only; proof-runner preflight and raw runtime evidence remain valid. **Observable Payback:** `npm run instruction:budget -- --format json` and `npm run validate:strict` no longer fail before reaching candidate-specific checks. **Trigger/Evidence:** task 1.1 strict validation reported `catalogTokenProxy actual=100840 maximum=100519` and `globalAuthorityTokenProxy actual=16659 maximum=16646`; none of the measured catalog/global-authority paths has a current working-tree diff from this change. **Why:** the repository helper identifies the seed as reviewed maxima and names `--materialize-seed` as the correction; leaving stale committed maxima blocks every current validation consumer without protecting the accepted outcome. **Prerequisites:** inspect current catalog inventory, verify measured instruction paths are clean relative to Git, and preserve before/after budget reports. **Scope/Non-Goals:** update only `config/instruction-budget.json`; do not change instruction semantics, hide candidate growth, or absorb later unreviewed Product Candidate text. **Implementation:** run the deterministic materializer once against current committed source and read back exact limits. **Observable Proof:** before report fails at the two named boundaries, after report passes with exact current maxima, and strict validation advances past instruction-budget checks. **Validation:** `npm run instruction:budget -- --format json`, `npm run validate:strict`, and focused diff/readback of `config/instruction-budget.json`.

## 6. Final History Retrospective

- [x] 6.1 After every other currently known task is complete, run the one-time final-history-retrospective over the complete `history.md`; do not let apply, archive, compaction, this task, or generated tasks create or rerun another final analysis. Analyze the existing `Quality`, `Cycle Speed`, and `Token Economy` rows across `Working Repository` and `opencode-kit`. For every evidence-backed candidate, record `Impact Horizon`, `Concrete Consumers`, `Execution Class`, `Earliest Safe Point`, `Invalidated Evidence`, `Observable Payback`, `Trigger/Evidence`, `Why`, `Prerequisites`, `Scope/Non-Goals`, `Implementation`, `Observable Proof`, `Validation`, and `Owner Blocker` only when applicable. Apply the canonical admission/deferral, target-ownership, authority-boundary, and current-consumer rules; append every admitted current-consumer item under `## Session-Derived Improvements` and immediately execute and prove it before completion, preserve no-current-consumer evidence as non-checkbox deferred history records, or record `none` without inventing work. **Current Rung:** R3 stable candidate with all preceding tasks complete. **Next Real Boundary:** the first exact consumer boundary of any admitted improvement, or retrospective readback when none is admitted. **Blocker/Unblocker:** every task 1.1 through 5.2; live-attempt or non-deferrable safety blockers retain priority over retrospective work. **Owner Authorization:** only local reversible outcome-preserving improvements with proven current consumers; protected-boundary or other-repository work records the exact owner/target blocker instead of mutating silently. **Safeguards:** no generic best practices, no scope expansion, no unsupported savings claims, no second retrospective, and scoped evidence invalidation for every admitted mutation. **Restoration/Cleanup:** follow each admitted task's explicit cleanup; preserve deferred records without making them completion scope. **Expected Immutable Evidence:** complete-history matrix, classification fields, admitted task diffs and proofs or explicit `none`, deferred records, and final readback showing one retrospective. **Observable Proof:** every admitted current-consumer improvement is implemented and validated, every valid non-current candidate is preserved correctly, or `none` is recorded with no fabricated work. **Validation:** tasks/history consistency, exact single-retrospective count, all generated task checkboxes/proofs, strict OpenSpec validation, and affected project validation replay.
