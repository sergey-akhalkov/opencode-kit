## 1. Establish The Discriminating Proof Boundary

- [x] 1.1 Extend the existing `tools/proofs/pre-escalation-recovery.ts` owner and `tools/proofs/README.md` inventory with one help-listed stale-proof-path scenario, a stable `REPLAN_NO_OWNER` route, exact real-local-command observation, paired owner-only safety oracle, file-manifest check, evaluator/replay support, and no new helper or permission surface; do not change loaded instruction behavior or automated test artifacts in this task. **Current Rung:** R0 direct source, current contracts, instruction budget, and external incident evidence. **Next Real Boundary:** provider-free `--help`/`-h` and preflight, then a same-model loaded baseline through the actual primary entry point. **Blocker/Unblocker:** none; reuse the current runner's scenario, model-profile, disposable-project, capture, redaction, immutable-bundle, replay, and cleanup owners. **Owner Authorization:** local proof-tooling source plus provider-free execution only; configured-provider calls begin in task 1.2 under standing kit-validation authority. **Safeguards:** primary remains edit/read/external-directory/question denied; allow only exact existing local commands; no credential, remote, machine, destructive, install, activation, publication, protected effect, fuzzy scoring, or synthetic success. **Restoration/Cleanup:** help and preflight remove every proof-owned temporary root and create no model session; later capture cleanup remains `finally`-owned. **Expected Immutable Evidence:** runner/inventory hashes, help/preflight argv, exits, stdout/stderr, permission readback, scenario/evaluator schema, and cleanup result. **Observable Proof:** help exits `0` with the new scenario and effects documented, preflight proves the active route and exact permissions with zero model calls, and a fixture mutation that introduces edit or arbitrary shell authority fails before capture. **Validation:** focused type/source diagnostics, actual help/preflight, proof inventory readback, and `git diff --check`. **Dependency-Chain Stop Condition:** no model baseline or instruction mutation until the runner can distinguish stale-path replan from true owner-only without granting new effects.

- [x] 1.2 Capture and replay the current loaded baseline for the stale-path and true-owner pair with one exact model/profile/config/permission/workspace shape, preserving the complete raw bundles and whether current authority executes the local route, asks the user, invokes `troubleshooter`, or returns owner-required. **Current Rung:** green provider-free runner boundary. **Next Real Boundary:** the actual loaded current primary `opencode run` path in a disposable project. **Blocker/Unblocker:** task 1.1; configured model route and task/question event capture must pass preflight. **Owner Authorization:** bounded non-sensitive configured-provider inference already authorized for kit validation. **Safeguards:** one baseline capture per paired scenario, no edit/external/credential/remote/protected effect, exact allowed commands only, privacy-safe redaction, no target-repository mutation, and no claim that baseline failure proves universal model behavior. **Restoration/Cleanup:** delete root/child sessions and disposable project in `finally`; retain create-new immutable evidence and manifest only. **Expected Immutable Evidence:** candidate/source/model/profile/permission identities, prompt hashes, argv, exits, stdout/stderr, event/tool facts, command output, file before/after manifest, route verdict, costs/tokens, session deletion, evaluator result, and byte-exact replay. **Observable Proof:** both scenarios reach terminal captured results, the owner-only baseline performs no protected action, all files remain unchanged, cleanup is complete, and replay reproduces every normalized fact without a provider call. **Validation:** runner `capture`, `evaluate`, and `replay` modes plus manifest/hash inspection. **Dependency-Chain Stop Condition:** incomplete session cleanup, unknown writer liveness, permission drift, missing raw output, or nonterminal replay blocks instruction mutation; a semantically green stale-path baseline is recorded rather than manufactured red.

## 2. Implement And Prove Outcome-Relative Recovery

- [x] 2.1 Replace the conflicting canonical clauses in `global/AGENTS.md` so authority is a fidelity ceiling, consistent pending tasks remain required process controls rather than owner scope, owner-only requires a protected action necessary for the original accepted outcome, and path-only protected prerequisites trigger autonomous artifact reconciliation with a narrower real claim; align only required qualification/mirror and deterministic production-contract markers, remove superseded wording, and do not modify automated test artifacts. **Current Rung:** immutable loaded baseline plus reviewed proposal/design/spec requirements. **Next Real Boundary:** same-model loaded candidate comparison through the task 1 runner. **Blocker/Unblocker:** task 1.2 baseline complete; exact catalog/global token-proxy maxima and current source identities captured. **Owner Authorization:** local reversible instruction, documentation, and deterministic contract edits only; no activation or external operation. **Safeguards:** preserve every credential, machine/safety, compatibility, authorization, destructive/remote, deployment/release, cost, restoration/cleanup, real-boundary sufficiency, non-waivable risk, specialist, and arbiter boundary; no new policy owner, skill, plugin, dependency, classifier, or budget increase. **Restoration/Cleanup:** repository diff is version-controlled and reversible; running sessions remain baseline and are not candidate evidence. **Expected Immutable Evidence:** focused diff, before/after instruction inventory and token proxies, operative marker ownership, source hashes, and unchanged troubleshooter/arbiter permissions. **Observable Proof:** direct read presents one coherent outcome-versus-path rule, every maintained runtime artifact has only its complete owner or concise delta, stale "highest rung"/"affected chain"/task-as-owner operators are absent from operative text, and both reviewed budget maxima remain unexceeded. **Validation:** `npm run instruction:inventory -- --format json`, `npm run instruction:budget -- --format json`, focused deterministic contract/validator checks, source diagnostics, and `git diff --check`. **Dependency-Chain Stop Condition:** any safety-boundary loss, duplicate complete policy, budget overflow, or test-authorship requirement returns the candidate to `development` before a provider call.

- [x] 2.2 Run-observe-correct the instruction candidate through the disposable loaded stale-path and true-owner pair, replay every preserved bundle after evaluator-only correction, and retain the candidate only when the stale path executes the allowed real local observation and returns `REPLAN_NO_OWNER` while the true-owner path remains `OWNER_REQUIRED`. **Current Rung:** structurally valid instruction candidate with unchanged proof-runner identity from task 1.2 or an explicitly recaptured runner baseline. **Next Real Boundary:** actual loaded primary entry point under the candidate source. **Blocker/Unblocker:** task 2.1; provider route, model, variant, permissions, and create-new evidence roots pass preflight. **Owner Authorization:** bounded non-sensitive configured-provider inference in a disposable local project. **Safeguards:** no user question, specialist call, protected/bypass command, disallowed file mutation, arbitrary shell, synthetic/end-to-end success, or broad claim in the stale-path lane; paired owner handoff must not execute or weaken its protected action. **Restoration/Cleanup:** delete every session/root, preserve raw failures before correction, and replay evaluator-only changes without another provider call. **Expected Immutable Evidence:** baseline/candidate source hashes, exact identical scenario inputs and environment identity, argv/exits/stdout/stderr/events, tool calls, local command observation, file manifests, route verdicts, cleanup, evaluation, and replay. **Observable Proof:** candidate stale-path facts show `LOCAL_ROUTE_OK`, zero question/task calls, blocked-path plus narrow-claim disposition, and `REPLAN_NO_OWNER`; owner-only facts show zero protected action/consultation and `OWNER_REQUIRED`; all safety, cleanup, and identity oracles pass. **Validation:** candidate capture, baseline/candidate evaluate, candidate replay, raw-bundle inspection, and current instruction budget. **Dependency-Chain Stop Condition:** red happy path, owner-boundary regression, unknown cleanup, or file/permission drift returns the Product Candidate to `development`; another same-path provider capture requires a causal Product Candidate/Runner/Environment change and a green terminal offline replay where applicable.

- [x] 2.3 Load the same readable candidate from the explicitly resolved active kit global source in a fresh OpenCode process and replay the stale-path plus true-owner pair through the intended operator entry point, proving current source identity and startup-time loading without mutating host configuration when it already points at that source. **Current Rung:** R2 disposable loaded candidate proof green. **Next Real Boundary:** fresh active-source process, then accepted-scope completion. **Blocker/Unblocker:** task 2.2; active global source, same-name source collisions, current model/profile, and rollback path are resolved before launch. **Owner Authorization:** bounded fresh local process and configured-provider validation; any installation, activation, or source switch remains separately recorded and is unnecessary when active-source identity matches. **Safeguards:** same scenario permissions and no protected effects; do not treat the running authoring session as fresh-candidate evidence; do not edit another colliding config source. **Restoration/Cleanup:** terminate/delete fresh sessions and disposable roots, leave active config unchanged, and retain privacy-safe evidence. **Expected Immutable Evidence:** active-source path/hash inventory, OpenCode/model/profile identity, exact invocation/exits/stdout/stderr/events, paired verdicts, file manifests, session/root cleanup, and comparison with R2. **Observable Proof:** the fresh process loads the candidate hashes and reproduces `REPLAN_NO_OWNER` plus `OWNER_REQUIRED` with no forbidden effect or question. **Validation:** fresh active-source capture/evaluate/replay and runtime-source diagnostics. **Dependency-Chain Stop Condition:** source mismatch, collision ambiguity, startup loading of baseline text, or red paired behavior blocks MVP and any SDET/review progression.

## 3. Complete Accepted Instruction Scope

- [x] 3.1 Synchronize the maintained proof inventory, README/routing text, concise qualification and reusable-project deltas, canonical delta traceability, and path-scoped Live-Attempt Gate reporting without copying the complete policy or changing `troubleshooter`/completion-arbiter authority; inspect touched instruction responsibilities and record `split-or-justify`. **Current Rung:** current MVP proof from the fresh active-source process. **Next Real Boundary:** deterministic complete-scope validation against the same instruction candidate. **Blocker/Unblocker:** task 2.3; no Product Candidate behavior mutation is permitted without returning to `development` and replaying affected R2/R3 lanes. **Owner Authorization:** local reversible documentation, instruction-delta, and proof-inventory edits only. **Safeguards:** no product-specific PMAC wording, new runtime artifact, duplicated ladder/recovery contract, optional feature, broad refactor, or claim that documentation proves model behavior. **Restoration/Cleanup:** no external state; keep unrelated work unchanged. **Expected Immutable Evidence:** exact changed paths, responsibility map, pointer/delta readback, traceability to both modified capabilities, and candidate/runner/evaluator/environment classification. **Observable Proof:** a cold reader finds one complete global owner, concise role deltas, exact path-scope reporting, unchanged specialist/arbiter boundaries, and no stale contradictory operator. **Validation:** focused contract/validator commands, strict selected OpenSpec validation, instruction inventory/budget, source diagnostics, and `git diff --check`. **Dependency-Chain Stop Condition:** any behavior-affecting wording change invalidates current proof and must complete task 2.2 and 2.3 again before review or SDET.

## 4. Independent Risk Discovery And Critical SDET

- [x] 4.1 After current MVP proof and accepted-scope completion, invoke one fresh read-only `instruction-artifact-reviewer` against the exact Candidate Reference to assess false path-owner classification, proof weakening, pending-task optionality, Live-Attempt Gate clearing, role-authority drift, mirror duplication, and budget/runtime evidence gaps; main independently reproduces and dispositions every row. **Current Rung:** complete accepted instruction candidate with current R2/R3 proof. **Next Real Boundary:** direct candidate/source and preserved loaded-evidence review; any confirmed production correction returns to R2/R3 before SDET. **Blocker/Unblocker:** task 3.1; reviewer identity and exact read-only scope are available. **Owner Authorization:** local read-only review only. **Safeguards:** reviewer cannot edit, ask, dispatch, authorize, block lifecycle, or turn optional findings into scope; missing or unusable output is recorded but is not itself a stage blocker. **Restoration/Cleanup:** no mutation; preserve report identity and candidate attribution. **Expected Immutable Evidence:** specialist task/session identity, Effective Model, inspected Candidate Reference, evidence-backed risk matrix, and main dispositions. **Observable Proof:** every plausible non-deferrable row is reproduced, disproved, or shown unreachable; any authorized correction is minimal, re-proven through tasks 2.2-2.3, and revalidated before SDET. **Validation:** report schema/readback, main reproduction commands, current paired proof, and scoped diff. **Dependency-Chain Stop Condition:** a plausible unresolved authorization, safety, compatibility, data-integrity, irreversible-action, or envelope-escape claim blocks SDET and qualification until dispositioned.

- [x] 4.2 Dispatch one fresh test-only `sdet-quality-engineer` after task 4.1 dispositions and current proof, supplying original requirements, Candidate Reference, raw paired proof, safe runner, criticality rubric, prior SDET state, and exact write scope limited to existing contract test artifacts; require it to challenge only reachable critical incidents and return its terminal action. **Current Rung:** reviewed current MVP candidate with accepted scope complete. **Next Real Boundary:** SDET-authored focused automated oracles at the nearest candidate contract boundary. **Blocker/Unblocker:** task 4.1; no open production/reviewer writer, current source/proof identities, and safe test-only isolation are confirmed. **Owner Authorization:** local test-only mutation in explicitly supplied existing test paths; no production/instruction/OpenSpec/proof-runner write. **Safeguards:** fresh context, no nested agent, user question, external/protected effect, production authorship, lifecycle verdict, non-critical expansion, coverage-percentage target, or mock-based confidence inflation. **Restoration/Cleanup:** close the writer attempt with a terminal report before integration; remove disposable test state and retain only unique critical or compatibility oracles. **Expected Immutable Evidence:** SDET task/session identity, Effective Model, `critical-risks-reported | no-critical-risk | blocked`, risk/oracle matrix, exact changed test paths, focused test exits/stdout/stderr, and candidate attribution. **Observable Proof:** oracles distinguish stale-path replan from true owner-only, reject lower-boundary overclaim and protected bypass, preserve consistent pending tasks and role boundaries, and fail against a behaviorally defective candidate. **Validation:** SDET-focused tests, test diff inspection, and writer-liveness closure. **Dependency-Chain Stop Condition:** only an immediately prior main-confirmed reachable critical/non-deferrable defect permits a production correction and one new fresh SDET after restored R2/R3 proof; the first precondition-valid attempt without confirmed-critical progress permanently ends SDET for this root.

- [x] 4.3 Main independently reproduce and disposition every SDET row, integrate only test-only changes with unique critical value, apply the smallest authorized production correction for any confirmed defect, and follow the terminal continuation rule until SDET ends; replay every proof/validation lane invalidated by production, runner, evaluator, environment, or test changes. **Current Rung:** task 4.2 terminal report with closed writer attempt. **Next Real Boundary:** focused oracle execution against the exact candidate, then complete project validation. **Blocker/Unblocker:** task 4.2; raw SDET evidence and changed paths are readable. **Owner Authorization:** main-owned local reproduction and authorized correction only; reviewer/SDET evidence grants no mutation authority by itself. **Safeguards:** park non-critical findings, never weaken protected actions or proof sufficiency, do not repeat SDET without the exact confirmed-critical predicate, and do not preserve redundant tests. **Restoration/Cleanup:** remove disposable reproduction state; ensure no open mutator before candidate freeze. **Expected Immutable Evidence:** each Risk ID disposition, reproducer command/output, retained test rationale, production correction and fresh paired proof when applicable, terminal SDET reason, and final Candidate Reference. **Observable Proof:** no known confirmed reachable critical/non-deferrable defect remains, the current candidate has green paired R2/R3 proof, and SDET is terminal under the one-continuation rule. **Validation:** retained focused tests, paired capture/replay as invalidated, instruction budget, and scoped status/diff. **Dependency-Chain Stop Condition:** unresolved plausible non-deferrable risk or missing writer closure blocks final validation; optional and non-critical rows are documented and parked.

## 5. Complete Validation And Pre-Freeze Evidence

- [x] 5.1 Run complete project-native validation against the same readable candidate: focused instruction/recovery/session contracts, `npm run validate:strict`, `npm test`, strict selected and all OpenSpec validation, instruction inventory and budget, paired proof replay, active runtime-source diagnostics, code-quality inventory for touched responsibility owners, `git diff --check`, and scoped secret/path/status review; prepare but do not freeze the final RC while the final history retrospective remains incomplete. **Current Rung:** terminal SDET candidate with current R2/R3 proof and no open writer. **Next Real Boundary:** final history retrospective, any admitted current-consumer correction, then final RC/stable local handoff. **Blocker/Unblocker:** tasks 4.1-4.3; all candidate/proof/test identities and known baseline limitations are attributable. **Owner Authorization:** local validation and read-only inventories only; no commit, push, archive, install, activation, release, publication, or remote mutation. **Safeguards:** preserve exact exits/stdout/stderr, do not waive red candidate checks, distinguish unrelated baseline failures, keep budget maxima unchanged, and do not mark the change complete from artifact existence or marker presence. **Restoration/Cleanup:** delete disposable validation/proof state, preserve immutable evidence, and leave unrelated changes untouched. **Expected Immutable Evidence:** exact command matrix, exits and diagnostic summaries, Candidate/Runner/Evaluator/Environment/Raw Bundle identities, SDET terminal state, residual risks, diff/status, and external-operation state. **Observable Proof:** every applicable candidate check is green, proof replay is exact, no known critical/non-deferrable defect remains, and the only unchecked initially known task is the final history retrospective. **Validation:** the complete command set above and readiness/handoff readback. **Dependency-Chain Stop Condition:** any attributable red validation or behavior mutation keeps the candidate at `MVP`/`development` as applicable and must be corrected and re-proven before task 6.1 can complete.

## Session-Derived Improvements

- [x] SDI-1 Complete the proof source identity before instruction mutation and recapture the paired baseline. **Impact Horizon:** Current Change. **Concrete Consumers:** task 2.1 Product Candidate path selection and task 2.2 baseline/candidate source attribution. **Execution Class:** gate-closer. **Earliest Safe Point:** before the first `global/AGENTS.md`, qualification-skill, or maintained-loop mutation. **Invalidated Evidence:** task 1.1 preflight and task 1.2 paired capture/evaluate/replay become historical when the runner source-identity inventory changes; their raw facts remain immutable and establish the prior runner behavior. **Observable Payback:** every intended runtime instruction owner changed by task 2.1 is present in preflight/capture source hashes, so the candidate comparison cannot silently omit a loaded Material skill or canonical loop. **Trigger/Evidence:** `sourceHashes()` currently includes `global/AGENTS.md` and reusable instructions but omits `global/skills/change-ready-sdlc/SKILL.md` and `instructions/universal-development-loop.md`, both named by proposal impact and design ownership. **Why:** a loaded behavior proof with incomplete Product Candidate identity cannot support task 2.2 or active-source proof. **Prerequisites:** preserved task 1.2 baseline bundle, green provider-free evaluate/replay, and no Product Candidate instruction mutation. **Scope/Non-Goals:** add only the two missing source paths to the existing runner inventory, rerun help/preflight, and recapture the same paired baseline once; do not change prompts, permissions, evaluator semantics, instructions, tests, model/profile, or budget. **Implementation:** extend `sourceHashes()`, capture the new runner hash, run provider-free help/preflight, then perform one bounded paired baseline capture plus evaluate/replay under the unchanged scenario environment. **Observable Proof:** new preflight and capture list both added paths with hashes, paired facts remain `REPLAN_NO_OWNER` and `OWNER_REQUIRED`, files/permissions are unchanged, cleanup is complete, and replay is exact. **Validation:** actual help/preflight, paired capture/evaluate/replay, source-hash readback, runner diff, and `git diff --check`. **Owner Blocker:** none; the causal runner identity change and green preserved replay satisfy the repeated configured-provider attempt gate.

## 6. Final History Retrospective

- [x] 6.1 After every other currently known task is complete, analyze the complete `history.md` exactly once using the existing compaction improvement matrix with rows `Quality`, `Cycle Speed`, and `Token Economy` across columns `Working Repository` and `opencode-kit`; apply the canonical `Impact Horizon`, `Concrete Consumers`, `Execution Class`, `Earliest Safe Point`, `Invalidated Evidence`, and `Observable Payback` classification plus admission/deferral, target ownership, authority, `Trigger/Evidence`, `Why`, `Prerequisites`, `Scope/Non-Goals`, `Implementation`, `Observable Proof`, and `Validation` fields. Append and immediately execute every admitted exact current-consumer improvement under `## Session-Derived Improvements`, including affected proof/validation replay; preserve evidence with no current consumer as non-checkbox `Deferred Improvement Candidate` history entries, or record `none` without inventing work. Then freeze the next monotonic RC only when accepted scope, proof, SDET, and validation remain current, and publish the complete local stable handoff for that same RC. Apply, archive, compaction, generated improvement tasks, and this task itself MUST NOT create or rerun another final-history analysis. **Current Rung:** complete pre-freeze evidence from task 5.1. **Next Real Boundary:** current-consumer proof for any admitted improvement, otherwise final local handoff; no external operation is authorized. **Blocker/Unblocker:** task 5.1 and all other initially known tasks complete; live-attempt and non-deferrable safety gates remain controlling. **Owner Authorization:** local history/task updates, admitted local reversible dependency closure, scoped proof/validation, and local handoff only; protected actions and external operations remain separately owner-controlled. **Safeguards:** no generic improvement, optional polish, duplicate retrospective, silent cross-repository mutation, budget increase, stale evidence reuse, or RC/stable claim after invalidating mutation. **Restoration/Cleanup:** remove disposable improvement/proof state, retain immutable evidence, and leave unrelated work untouched. **Expected Immutable Evidence:** six-cell matrix, classifications and consumers, admitted-task completion or deferred records/`none`, replay results, final Candidate Reference, RC history, validation, limitations, rollback/source-loading notes, and external-operation state. **Observable Proof:** every evidence-backed current consumer is either implemented and proven or correctly deferred outside accepted scope, no mandatory task remains, the final candidate retains green paired behavior and validation, and the handoff reports `Development-Stage: stable` with the same `Stable Candidate: RC<n>`. **Validation:** all lanes invalidated by admitted work, final strict selected/all OpenSpec validation, instruction inventory/budget, scoped diff/status, and complete handoff readback. **Dependency-Chain Stop Condition:** an admitted incomplete improvement, invalidated red proof, unresolved critical/non-deferrable risk, or incomplete cleanup prevents RC/stable and keeps this task unchecked.
