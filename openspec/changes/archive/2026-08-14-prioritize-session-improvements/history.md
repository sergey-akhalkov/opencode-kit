# Strategy History

## 2026-08-13 - Preserve All Admits Without Execution Classification

- **Objective:** Determine whether the current compaction contract already schedules improvements early enough and highlights same-repository reuse.
- **Approach:** Inspect loaded instructions, compaction prompt, apply/archive routing, normative specs, and focused contracts; run a synthetic continuation through `opencode run --agent compaction` with one current-path candidate, one shared repository consumer set, and one unsupported future candidate.
- **Evidence:** The loaded `compaction · grok-4.6` persisted one combined parser improvement and placed it before task 4.2, but emitted no canonical impact horizon, execution class, earliest safe point, invalidated-evidence, or observable-payback fields. Existing source requires persistence before substantial work and completion before archive, not a stable per-consumer execution contract.
- **Outcome:** Current behavior is directionally useful but under-specified; a bounded policy correction is warranted.
- **Reason:** Preservation prevents loss but does not independently guarantee critical-path execution or consistent repository-multiplier recognition.
- **Do-Not-Repeat Condition:** Do not treat task persistence alone as evidence that execution timing and cross-change impact are specified.
- **Evidence-Based Retry Condition:** Repeat the identical loaded continuation only after the candidate policy and prompt require the new classification and sequencing fields.

## 2026-08-13 - Integrated Repository Qualification Before Concurrent Change Completion

- **Objective:** Qualify the proven improvement-routing candidate through the repository's full trusted validation surface.
- **Approach:** After current runtime proof and terminal fresh SDET, run strict library validation, strict selected/all OpenSpec validation, instruction/source inventories, full `npm test`, pre-push validation, operation gates, and diff checks without modifying concurrent-change-owned tests or fixtures.
- **Evidence:** `npm run validate:strict`, strict selected/all OpenSpec validation, instruction inventory, runtime-source diagnostics, apply gate, and `git diff --check` passed. `npm test` and `npm run prepush:validate` each reported 51 library failures. The repeated diagnostics name unattended adapter/template and portable global-bin expectations introduced by `add-unattended-roadmap-orchestration`, removed `.opencode/skills/openspec-*` paths owned by its task I1, and related doctor/model-profile fixture drift. No failure named the new impact/consumer/execution/deferred contract. Raw outputs are preserved at `C:\Users\Sergey\.local\share\opencode\tool-output\tool_ffc28c303001ZB11B8nSuKAssz` and `C:\Users\Sergey\.local\share\opencode\tool-output\tool_ffc28c57d001GhVqUU4jgn3Cce`.
- **Outcome:** Current policy behavior remains runtime-proven at `MVP`, but integrated repository qualification is red; no RC or stable claim is permitted.
- **Reason:** A concurrent active Material change has mutated production bootstrap/doctor/canonical workflow contracts before completing its owned fixture and path migrations. Fixing those failures here would overlap its explicit ownership and unrelated accepted outcome.
- **Do-Not-Repeat Condition:** Do not rerun broad tests or pre-push on the same integrated candidate merely to obtain the same 51-failure inventory, and do not repair concurrent-change-owned fixtures from this change.
- **Evidence-Based Retry Condition:** Rerun affected full validation after `add-unattended-roadmap-orchestration` completes task I1 and its fixture/validation closure, or after new evidence shows a failure is caused by this change's policy surfaces.

## 2026-08-14 - Integrated Qualification After Concurrent Change Completion

- **Objective:** Close task 3.3 after the concurrent canonical-workflow migration completed, while rerunning only proof lanes invalidated by current Product Candidate identities.
- **Approach:** Confirm the archived change completed I1 and full validation; compare the recorded Product Candidate hashes; replay the current-consumer and deferred compaction scenarios because `global/AGENTS.md` changed; then run focused contracts, strict library validation, full repository tests, strict selected/all OpenSpec validation, instruction inventory, runtime-source diagnostics, propose/apply operation gates, pre-push validation, and `git diff --check`.
- **Evidence:** `add-unattended-roadmap-orchestration` is archived with I1 and task 6.3 checked. The active/template compaction prompts, canonical workflows, active machine-local prompt, workflow documentation, and SDLC surfaces retained their identities. Current `global/AGENTS.md` hash is `99388032b38d2cf9340f3b1c2e25fecd5655c060`; current normative instruction/workflow/SDLC spec hashes are `c112af2224625090de273350e06af4e597497ce2`, `8ad80008e91e11ec660aca141ead6ffbe93e5f88`, and `044aa1c5c089c8ca54bf55f35f0a2e422eb92115`. Both fresh `opencode run --agent compaction` lanes used the exact inputs preserved in `implementation-evidence/runtime-proof.md`, loaded `compaction · grok-4.6`, exited 0, produced the required classification/order/deferred behavior, and performed no tool or repository effect. `npm run test:focused:contracts` passed 67 contracts; `npm run validate:strict` reported `skills=29 agents=18 markdown=358 warnings=0 infos=2`; `npm test` exited 0; strict selected validation passed; strict all validation passed 13/13; instruction inventory and runtime-source diagnostics completed; propose/apply gates passed; `npm run prepush:validate` passed; `git diff --check` emitted no error.
- **Outcome:** The prior 51-failure and three-ENOENT families are closed. Current Runtime Proof and every task 3.3 validation lane are green; local qualification may proceed to the handoff task.
- **Reason:** The recorded retry condition was satisfied by completion and archive of the concurrent change; its fixture/path migration removed the attributed failures. The loaded-policy hash change was handled by replaying only the affected model-facing lanes.
- **Do-Not-Repeat Condition:** Do not rerun these full lanes without a Product Candidate, proof runner, environment, or validation-owner change that invalidates their evidence.
- **Evidence-Based Retry Condition:** Rerun only a lane whose recorded Product Candidate, runner/evaluator, environment, or validation source changes, or when a new diagnostic names this change's behavior.

## 2026-08-14 - Final Complete-Change History Retrospective

- **Original User Goal:** Every evidence-backed improvement is classified by impact horizon and execution point; active-change improvements run at their earliest safe consumer boundary, concrete same-repository multipliers receive explicit attention, and future-only work remains non-blocking.
- **Evidence Scope:** The complete strategy journal above: the under-specified baseline, the integrated validation attempt before concurrent migration closure, and the successful proof/validation replay after that migration archived.

| Direction | Working Repository | opencode-kit |
| --- | --- | --- |
| Quality | `none` - the observed classification gap is the accepted change outcome and is already implemented/proven; the validation failures were attributed to another owning change and are now closed; no distinct unresolved quality loss or remaining current-change consumer is evidenced. | `none` - the journal records no distinct unresolved kit-quality loss outside this change's already implemented/proven policy correction. |
| Cycle Speed | `none` - the only stalled loop was a dependency wait on the concurrent I1 migration; the retry condition was obeyed and the next permitted replay passed, with no repeated current mechanism or remaining consumer that a new local improvement would accelerate. | `none` - the journal records no repeated kit workflow step with an exact remaining consumer after current validation closure. |
| Token Economy | `none` - the journal does not evidence repeated context reconstruction, duplicated current output, or another remaining consumer that would pay back a local reversible reduction. | `none` - the journal supplies no observed kit token loss, concrete consumer, or causal improvement candidate. |

- **Admitted Improvement Task IDs:** none.
- **Deferred Improvement Candidate IDs:** none.
- **Result:** No candidate passed the observed-evidence, causal-link, current-consumer/deferral, local-reversible, low-cost, no-scope-expansion, and target-ownership gates. No task or deferred record was created.
- **Analysis Status:** complete. This is the creation-authored terminal retrospective and SHALL NOT be rerun after this record.
