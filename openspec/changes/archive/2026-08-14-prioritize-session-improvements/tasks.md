## Fidelity Ladder

`current loaded prompt/source baseline -> focused structural contracts -> fresh installed compaction candidate with identical synthetic input -> fresh critical-only SDET -> repository validation and local handoff`. Product Candidate roles are the loaded global policy, hidden compaction prompt, canonical apply/archive workflows, docs, and normative specs. The installed `opencode run --agent compaction` process is the real entry point. No task authorizes install, deployment, release, remote mutation, credentials, or implementation in another active change.

Profile: `Material`. Main owns production instruction edits and run-observe-correct. A fresh test-only SDET owns any new automated critical regression artifact after current runtime proof and accepted-scope completion.

## 1. Freeze Baseline And Contract

- [x] 1.1 Inspect the loaded compaction source, global policy, canonical apply/archive routes, normative specs, focused contracts, active changes, and overlapping worktree changes; run the synthetic current-path/repository-multiplier/future-only continuation through `opencode run --agent compaction`. **Current Rung:** installed loaded baseline. **Next Real Boundary:** identical fresh candidate run after policy mutation. **Blocker/Unblocker:** none. **Observable Proof:** baseline preserves useful evidence and reveals whether impact horizon, exact consumers, execution class, earliest safe point, invalidated evidence, and observable payback are canonical. **Owner Authorization:** bounded configured inference and local read-only inspection. **Safeguards:** non-sensitive prompt, no tools/mutation/remote effects, preserve unrelated work. **Restoration/Cleanup:** no fixture writes; retain output in history/evidence. **Expected Immutable Evidence:** exact OpenCode version/route/input, exit status, observed classification/order, source identities, and worktree inventory. **Observed:** OpenCode 1.18.18 loaded `compaction · grok-4.6`; exit 0; it placed one parser before task 4.2 and rejected the unsupported dashboard, but the six classification fields were not a required stable record.
- [x] 1.2 Author and strictly validate proposal, design, three delta specs, strategy history, and this execution plan without changing loaded behavior. **Current Rung:** planning controls. **Next Real Boundary:** focused structural contract after source edits. **Blocker/Unblocker:** task 1.1 baseline. **Observable Proof:** `openspec validate prioritize-session-improvements --strict` exits zero and the change is apply-ready. **Owner Authorization:** local OpenSpec artifacts only. **Safeguards:** no implementation mutation before baseline; no retrofit of other changes. **Restoration/Cleanup:** artifact rollback only. **Expected Immutable Evidence:** strict validation and operation-gate output.

## 2. Implement Classification And Early Execution

- [x] 2.1 Update `global/AGENTS.md`, the active/template compaction prompt, canonical global apply/archive skill and command, final-history routing where needed, workflow documentation, and current normative specs with one coherent preservation/admission/execution contract. Require the six classification fields; current consumers admit work, repository multipliers require current proof plus another exact consumer, and no-current-consumer evidence becomes a non-blocking history record. **Current Rung:** loaded baseline. **Next Real Boundary:** focused contracts over every maintained surface. **Blocker/Unblocker:** tasks 1.1-1.2. **Observable Proof:** maintained surfaces agree on safety-first order, `gate-closer | do-now | before-task-<id> | before-freeze | separate-change`, correct ownership, and no deferred completion blocker. **Owner Authorization:** local instruction/config/docs/spec edits. **Safeguards:** preserve unrelated prompt/config bytes; no scorer, scheduler, automatic other-change mutation, or weakened archive gate. **Restoration/Cleanup:** source rollback only. **Expected Immutable Evidence:** scoped diff and source readback. **Observed:** canonical surfaces and current normative specs now carry one synchronized contract; active/template JSON parse; unrelated template guard-limit changes remain intact.
- [x] 2.2 Run source readback, JSON parsing, strict selected-change validation, and existing focused contracts without changing automated test artifacts; distinguish candidate failures from the pre-existing canonical-path test blocker owned by `add-unattended-roadmap-orchestration` task I1. **Current Rung:** candidate source. **Next Real Boundary:** installed fresh compaction candidate. **Blocker/Unblocker:** task 2.1. **Observable Proof:** every maintained candidate surface contains the classification/order/deferred contract, both configs parse, strict selected validation passes, and any focused-contract failure is attributed by exact path/message. **Owner Authorization:** local read-only validation. **Safeguards:** no semantic classifier, test edit, or overlap with I1 ownership. **Restoration/Cleanup:** none. **Expected Immutable Evidence:** exact commands, exits, source markers, and blocker attribution. **Observed:** marker readback and selected strict validation pass; focused contracts have exactly three pre-existing `ENOENT` failures for removed `.opencode/skills/openspec-*` files, already owned by active task I1.

## 3. Runtime Proof And Qualification

- [x] 3.1 Start a fresh installed OpenCode process and replay the exact task 1.1 synthetic continuation through `opencode run --agent compaction`; compare baseline and candidate. Retain the candidate only if it preserves original goal/safety/all-candidate evidence, emits all six fields, schedules current work before its first consumer, recognizes the exact same-repository multiplier without implementing extra consumers, and defers unsupported/no-current-consumer work without blocking completion. **Current Rung:** structurally green candidate. **Next Real Boundary:** fresh critical-only SDET. **Blocker/Unblocker:** tasks 2.1-2.2 and current active config prompt. **Observable Proof:** configured entry point exits zero with exact classification/order and no mutation. **Owner Authorization:** bounded configured inference. **Safeguards:** non-sensitive prompt, no tools, no repository/remote/protected effects. **Restoration/Cleanup:** close proof session after privacy-safe evidence capture. **Expected Immutable Evidence:** Product Candidate hashes, exact invocation/input, route/model, stdout/stderr/exit, observations, effects, and comparison verdict. **Observed:** both current-consumer and deferred candidate lanes exit 0 through `compaction · grok-4.6`; `implementation-evidence/runtime-proof.md` records identities, inputs, output facts, side effects, and green comparison.
- [x] 3.2 After current accepted-scope runtime proof, dispatch one fresh test-only `sdet-quality-engineer` against the exact Candidate Reference and critical risks: deferred work accidentally blocking completion, current work postponed past its consumer, cross-change mutation authorization, all-candidate evidence loss, and live-attempt gate preemption. SDET may add the smallest focused structural regression oracle on canonical global surfaces if needed; it must not claim semantic model enforcement. Main independently reproduces and dispositions every plausible row. **Current Rung:** current MVP candidate. **Next Real Boundary:** trusted repository validation. **Blocker/Unblocker:** task 3.1. **Observable Proof:** terminal `critical-risks-reported | no-critical-risk | blocked` with Effective Model; any confirmed critical correction gets fresh proof before a permitted successor. **Owner Authorization:** local test-only writes and configured inference. **Safeguards:** SDET never edits production or performs external action; preserve I1 ownership of stale canonical-path migrations. **Restoration/Cleanup:** remove disposable state; no external state. **Expected Immutable Evidence:** attributed SDET matrix, any focused test diff, and main disposition. **Observed:** fresh task `ses_003de93a6ffe0JZttRUXRDIDCi`, Effective Model `xai/grok-4.6`, terminal `no-critical-risk`, no test edits, and no critical matrix rows; `implementation-evidence/critical-sdet.md` records disposition and residual risk.
- [x] 3.3 Run focused contracts, `npm run validate:strict`, `npm test`, strict selected/all OpenSpec validation, instruction inventory, source diagnostics, relevant operation gates, and `git diff --check`; rerun only invalidated proof lanes. **Current Rung:** terminal SDET candidate. **Next Real Boundary:** local operator handoff. **Blocker/Unblocker:** terminal task 3.2; the prior concurrent I1 blocker closed when `add-unattended-roadmap-orchestration` completed and archived. **Observable Proof:** all applicable commands exit zero without modifying unrelated work. **Owner Authorization:** local validation only. **Safeguards:** no install/activation/archive/commit/push/release. **Restoration/Cleanup:** disposable outputs only. **Expected Immutable Evidence:** commands, exits, relevant stdout/stderr, source identities, and scoped diff. **Observed:** both hash-invalidated compaction lanes exit 0 with the required current-consumer/deferred behavior; focused contracts pass 67/67; strict library validation reports zero warnings; `npm test`, strict selected/all OpenSpec validation (13/13), inventories, runtime-source diagnostics, propose/apply gates, pre-push validation, and `git diff --check` all pass. Exact inputs, identities, output facts, effects, and retry closure are in `implementation-evidence/runtime-proof.md` and `history.md`.

## 4. Handoff

- [x] 4.1 Record outcome, Candidate Reference, baseline/candidate proof, environment, architecture, diagnostics, terminal SDET, validation, known limitations, rollback/restart boundary, and external-operation state; freeze RC and stable only when current evidence earns them. **Current Rung:** qualified local candidate. **Next Real Boundary:** future OpenCode process loading the retained files in ordinary project work. **Blocker/Unblocker:** task 3.3. **Observable Proof:** cold-reader handoff names exact behavior and states that a restart/new session is required. **Owner Authorization:** local reporting only. **Safeguards:** no install, activation, archive, commit, push, deployment, or release. **Restoration/Cleanup:** preserve evidence. **Expected Immutable Evidence:** final handoff and status. **Observed:** `implementation-evidence/mvp-handoff.md` records current Candidate Reference, refreshed loaded-boundary proof, environment, architecture, diagnostics, terminal SDET, green validation, limitations, rollback/restart, owner-blocker, and external-operation state; source readback, strict selected validation, and `git diff --check` pass. Apply lifecycle remains `MVP` pending the separate archive/stable handoff boundary.
- [x] 4.2 Perform the final complete-change history retrospective after every other currently known task is complete. Apply the canonical six-cell `Quality` / `Cycle Speed` / `Token Economy` by `Working Repository` / `opencode-kit` evidence gate to complete `history.md`, including the current impact/execution classification contract; append and immediately execute every admitted improvement or record `none`. Do not create or run another final-history retrospective. **Current Rung:** complete change history. **Next Real Boundary:** archive readiness. **Blocker/Unblocker:** task 4.1 and every generated admitted improvement complete; deferred records remain non-blocking. **Observable Proof:** one terminal retrospective record with generated task ids complete or explicit none and separately listed deferred records. **Owner Authorization:** local evidence/process work. **Safeguards:** no scope expansion/protected action. **Restoration/Cleanup:** documentation rollback only. **Expected Immutable Evidence:** final history/tasks readback. **Observed:** the terminal retrospective in `history.md` analyzes all three strategy entries across the canonical six cells; every cell records `none` with its evidence reason, admitted task IDs are none, deferred record IDs are none, source readback and strict selected validation pass, and the record explicitly prohibits rerun.

## Session-Derived Improvements

None recorded yet.
