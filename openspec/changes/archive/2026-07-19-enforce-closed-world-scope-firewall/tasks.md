## 1. Freeze And Baseline

- [x] 1.1 Before the first runtime-instruction or validator mutation, freeze this eight-task ID snapshot, every exact instruction/production-support/SDET-test path from `design.md`, zero new files, one initial fresh SDET, one correction wave, at most one fresh corrected-candidate SDET when that correction is consumed, one final review, and one delivery review; run `npm run validate:strict`, `npm test`, and `npm run openspec:validate`, and record each baseline outcome without fixing unrelated baseline debt.

## 2. Production Authority

- [x] 2.1 Dispatch one serial `implementation-worker` with production write scope limited to the exact instruction and production-support paths in `design.md`; update `global/AGENTS.md`, `global/skills/change-ready-sdlc/SKILL.md`, `REPO_AGENTS.md`, `instructions/universal-development-loop.md`, `instructions/reusable-project-agent-instructions.md`, and `templates/project/AGENTS.md` so post-freeze scope only shrinks, P0/P1 and failed gates reject readiness without authorizing scope, exact frozen IDs/gates/paths control corrections, persistent evidence infrastructure becomes a separate prerequisite, and qualification has one correction wave; run `npm run validate` and require no new error from these surfaces.
- [x] 2.2 In the same serial production ownership, update `instructions/leaf-reviewer-agent-contract.md`, every exact reviewer agent named in `design.md`, and `global/agents/sdet-quality-engineer.md` so outputs contain no `Required Next Actions`, `Actionable Continuation Items`, or `changes_requested`; require frozen-criterion evidence, candidate attribution, `Blocking Evidence`, and non-authorizing `Follow-up Candidates`; final review uses `approved | approved_with_notes | rejected | blocked`; final and delivery rejection terminate the attempt; run `npm run validate` and require no new role-contract error.
- [x] 2.3 In the same production ownership, update exactly the seven production-support files named in `design.md` to require the closed-world markers and reject the superseded P0/P1 expansion, reviewer/SDET action lists, persistent evidence-tool exception, and replay beyond one correction wave; do not add a support file or fuzzy semantic classifier; run `npm run validate:strict` and require exit code 0 or an exactly attributed unchanged baseline failure.

## 3. Proof And Independent Tests

- [x] 3.1 Run `npm run validate` against the production candidate; capture Candidate Reference as `git status --short` plus scoped `git diff --` over every frozen path; require the live validator to accept every configured authority surface, no path outside the frozen write sets, and no new file before SDET dispatch. `code-quality-reviewer` is `N/A` because no application abstraction or new support file is introduced and complete static contracts/tests cover the bounded validator edits.
- Revision 1 terminal history, task 3.2 (not completed): the initial fresh SDET changed the seven allowed test paths, but complete validation later exposed stale doctor fixtures outside that revision's scope.

## 4. Qualification

- Revision 1 terminal history, task 4.1 (not completed): `npm run validate:strict` passed and `npm test` failed with 34 stale-fixture failures; the single correction wave was consumed and blocked on out-of-scope `tools/test-library/doctor.ts`.
- Revision 1 terminal history, task 4.2 (not started): final and delivery review were correctly not dispatched after failed validation.

Revision 1 evidence (2026-07-19): `npm run validate:strict` passed, then `npm test` failed because conforming doctor authority fixtures in `tools/test-library/doctor.ts` lack the new closed-world markers. The single fresh corrected-candidate SDET returned `blocked` because that path was outside the frozen test scope. The correction wave was exhausted and Revision 1 terminated with `Change-Ready: no`.

## 5. Owner-Approved Revision 2

- [x] 5.1 Freeze Revision 2 active task IDs `5.1`-`5.4`, the prior exact candidate paths plus the single user-approved SDET addition `tools/test-library/doctor.ts`, zero new files, one initial fresh SDET, one correction wave, at most one fresh corrected-candidate SDET, one final review, and one delivery review; run `npm run validate:strict` and `npm run openspec:validate`, retain the known Revision 1 `npm test` failure as baseline evidence, and do not reuse prior qualification evidence.
- [x] 5.2 Dispatch one new fresh `sdet-quality-engineer` with the original ten rules, Revision 2 scope capsule, complete current candidate, and test-only write scope limited to the eight exact paths in `design.md`; require central conforming `REPO_AGENTS.md` and active `AGENTS.md`/`change-ready-sdlc` doctor fixtures to include the new markers while all negative tests retain their intended diagnostics; no production, OpenSpec, or new-file edits.
- [x] 5.3 Recapture Candidate Reference after Revision 2 SDET, then run `npm run validate:strict`, `npm test`, and `npm run openspec:validate`; require all three to exit 0, or consume the single Revision 2 correction wave only for a reproducible candidate-attributable frozen-criterion defect fully correctable inside the exact Revision 2 paths, followed by one fresh corrected-candidate SDET and one complete rerun. Any further failure terminates Revision 2.
- Revision 2 terminal history, task 5.4 (not completed): the one final reviewer returned `blocked` because its evidence bundle did not include the complete corrected-candidate SDET report body; delivery was correctly not dispatched.

Revision 2 final evidence (2026-07-19): all three validation gates passed after the single correction wave. The one fresh final reviewer returned `blocked` because the dispatch bundle summarized but did not inline the complete corrected-candidate SDET report. Under the frozen stop line, final review is not replayed and delivery is not dispatched. Revision 2 is terminal with `Change-Ready: no`; task 5.4 remains incomplete. The final reviewer and SDET also emitted an empty legacy action-list section despite the candidate forbidding it, so a new OpenCode session is required before any owner-approved later qualification revision can test the newly loaded role contracts.

## 6. Owner-Approved Revision 3 After Restart

- [x] 6.1 After restarting OpenCode with this repository's `global/` config loaded, freeze Revision 3 task IDs `6.1`-`6.4`, the unchanged Revision 2 candidate/path set, zero mutation authority, zero correction waves, one assessment-only fresh SDET, one validation run, one final review, and one delivery review; capture Candidate Reference with `git status --short` plus scoped `git diff --` and require no path/content change from the validated Revision 2 candidate.
- [x] 6.2 Dispatch one new fresh `sdet-quality-engineer` with no write scope, the original ten rules, complete unchanged candidate, current tests, Revision 2 green validation evidence, and newly loaded role contract; require `Action: assessed-existing-tests`, complete risk/oracle and existing-test evidence, empty `Blocking Evidence`, and no superseded action-list field. Any mutation, blocked action, or stale output terminates Revision 3.
- [x] 6.3 Run `npm run validate:strict`, `npm test`, and `npm run openspec:validate` once against the unchanged Candidate Reference; require all three to exit 0 and recapture identical scoped candidate evidence. Any failure terminates Revision 3 because its correction budget is zero.
- [ ] 6.4 Dispatch one fresh read-only `final-candidate-reviewer` with the complete Revision 3 SDET report body inline plus current Candidate Reference and validation outputs, then one `session-delivery-reviewer` only after final `approved | approved_with_notes`; accept only an accepted Material delivery result with empty `Blocking Evidence` and no superseded action-list field. Any missing evidence, rejection, block, or stale output terminates Revision 3 with `Change-Ready: no`.

Revision 3 final evidence (2026-07-20): the fresh assessment-only SDET conformed and all three validation gates passed on the unchanged 37-path candidate. The one fresh final reviewer returned `rejected` with two P1 findings: registered reviewer and shared delegation contracts still expose equivalent action-authoring fields such as `Missing Tests`, `Required Evidence`, `Benchmark Suggestions`, generic actionable continuation items, or `Suggested Next Options`; deterministic enforcement rejects only three exact legacy labels despite the delta spec requiring equivalent-field rejection. Delivery was correctly not dispatched. Revision 3 is terminal with `Change-Ready: no`; task 6.4 remains incomplete and the findings authorize no mutation or replay.

## 7. Owner-Approved Revision 4 Strict Removal

- [x] 7.1 Freeze Revision 4 acceptance IDs `AC-R4-1`-`AC-R4-4`, task IDs `7.1`-`7.5`, the exact production and SDET write paths in `design.md`, zero new files, one initial fresh SDET, one correction wave, at most one fresh corrected-candidate SDET, one final review, and one delivery review; run `npm run validate:strict`, `npm test`, and `npm run openspec:validate` before production mutation and record each baseline outcome without reusing Revision 3 qualification evidence.
- [x] 7.2 Dispatch one serial `implementation-worker` with production writes limited to the exact Revision 4 production paths; remove every frozen equivalent action-authoring field from registered reviewer/shared delegation outputs, route its information only through `Blocking Evidence`, `Residual Risks`, or non-authorizing `Follow-up Candidates`, and extend only the existing exact deterministic contract arrays/validator requirement; do not edit tests, add paths/files, or add fuzzy classification.
- [x] 7.3 Run `npm run validate` as observable loaded-contract proof, inspect production write scope, and capture Candidate Reference with `git status --short` plus scoped `git diff --` over all prior candidate paths; require `AC-R4-1` and `AC-R4-2` to be visibly satisfied before SDET dispatch.
- [x] 7.4 Dispatch one fresh `sdet-quality-engineer` with original requirements, Revision 4 capsule, production Candidate Reference, baseline outcomes, and test-only writes limited to the four exact Revision 4 test paths; require independent exact positive/negative coverage for `AC-R4-1`-`AC-R4-4`, no production/OpenSpec/new-file edits, and action `authored-tests | assessed-existing-tests | blocked`.
- [ ] 7.5 Recapture Candidate Reference after SDET, run `npm run validate:strict`, `npm test`, and `npm run openspec:validate`, and use the single correction wave only for a reproducible candidate-attributable frozen-criterion violation fully correctable inside Revision 4 paths; after accepted SDET and complete green validation, dispatch one fresh final review with complete inline evidence and then one Material delivery review only after final approval. Any second serious failure, final rejection/block, or delivery rejection terminates Revision 4 with `Change-Ready: no`.

Revision 4 baseline evidence (2026-07-20): before production mutation, `npm run validate:strict`, `npm test`, and `npm run openspec:validate` all exited 0. The test run reported 230 library tests, 76 contract tests, and all remaining suites passing; OpenSpec validation reported 9 passed and 0 failed. Full combined output: `tool_f7c60a3ec001Hp1vzSrTtHN2ZW`.

Revision 4 production evidence (2026-07-20): the serial `implementation-worker` completed with no test/OpenSpec/new-file/remote mutation. `npm run validate` exited 0 with `skills=24 agents=18 markdown=148 warnings=0 infos=1`; `git diff --check` was clean. Source inspection found the frozen fields absent from registered role/leaf contracts and the two shared phrases absent from `global/AGENTS.md` and `REPO_AGENTS.md`; the only repository agent-directory matches were the explicitly out-of-scope non-registered `qwen-local-worker`. Candidate Reference is `git status --short` plus the 37-path scoped diff (`812 insertions`, `172 deletions`) saved as `tool_f7c650b57001GbCPtDzAhIYSw7`.

Revision 4 SDET and validation evidence (2026-07-20): fresh SDET returned `Action: authored-tests`, `Blocking Evidence: none`, changed only the four frozen test paths, covered all 12 registered-role fields, both loaded-surface equivalents, the three evidence buckets, and the non-registered qwen boundary. `npm run validate:strict`, `npm test`, and `npm run openspec:validate` all exited 0; OpenSpec reported 9 passed and 0 failed. Library tests increased from 230 to 238: six added cases expand the role-field set from 6 to 12 and two added dynamic cases cover the two loaded-surface patterns. Complete validation output: `tool_f7c6b98890010NbfryrnuEzGaB`. The Revision 4 correction wave remains unused.

Revision 4 final evidence (2026-07-20): the one fresh final reviewer returned `blocked`. It accepted the production/test behavior, 37-path scope, all 12 exact field removals, qwen boundary, and green validation, but found the mandatory SDET evidence handoff non-conforming: the SDET risk/oracle matrix did not provide a separate `AC-R4-4` disposition, and the main-session inline final-review bundle omitted the SDET report's actual `Requested Validation Procedures` section. Under the frozen stop line, final review is not replayed, the unused correction wave cannot be consumed after final review, and delivery is not dispatched. Revision 4 is terminal with `Change-Ready: no`; task 7.5 remains incomplete and the findings authorize no mutation or replay.

## 8. Owner-Approved Revision 5 Evidence-Only Qualification

- [x] 8.1 Freeze Revision 5 task IDs `8.1`-`8.4`, unchanged acceptance IDs `AC-R4-1`-`AC-R4-4`, the exact prior 37-path candidate, zero write scope, zero new files, zero correction waves, one assessment-only fresh SDET, one validation run, one final review, and one delivery review; capture Candidate Reference and require no candidate content/path change from the validated Revision 4 candidate.
- [x] 8.2 Dispatch one new fresh `sdet-quality-engineer` with no write scope, original requirements, unchanged complete candidate, current tests, Revision 5 capsule, and current Candidate Reference; require `Action: assessed-existing-tests`, `Status: done`, separate risk/oracle dispositions for `AC-R4-1`, `AC-R4-2`, `AC-R4-3`, and `AC-R4-4`, every exact required report section including `Requested Validation Procedures`, empty `Blocking Evidence`, and no superseded/equivalent action field. Any mutation, missing criterion disposition, missing/renamed section, blocked action, or stale field terminates Revision 5.
- [x] 8.3 Preserve the complete SDET report body exactly, run `npm run validate:strict`, `npm test`, and `npm run openspec:validate` once against the unchanged candidate, and recapture identical Candidate Reference. Any failure or candidate mutation terminates Revision 5 because its correction budget is zero.
- [ ] 8.4 Dispatch one fresh read-only `final-candidate-reviewer` with the exact complete SDET report body inline plus current Candidate Reference and validation outcomes; only after final `approved | approved_with_notes`, dispatch one `session-delivery-reviewer` with the same complete SDET report, final report, candidate evidence, validation, and current task/evidence state. Accept only an accepted Material delivery result with empty `Blocking Evidence`. Any incomplete transfer, rejection, or block terminates Revision 5 with `Change-Ready: no`.

Revision 5 initial evidence (2026-07-20): Candidate Reference remains exactly the prior 37 tracked paths with `860 insertions`, `172 deletions`, clean `git diff --check`, readable scoped diff `tool_f7c6c7f5f001dx1EjW6VH3GDl5`, and scoped diff hash `5d92518e7650af20620c68ff98aba498639f5c14`. The only untracked path is the already-approved OpenSpec change directory; no candidate mutation or newly authorized path exists.

Revision 5 SDET evidence (2026-07-20): one fresh assessment-only SDET returned `Action: assessed-existing-tests`, `Status: done`, a separate risk/oracle row for each `AC-R4-1` through `AC-R4-4`, exact current test/procedure evidence, every required report section including `Requested Validation Procedures`, `Blocking Evidence: none`, and no candidate mutation. The complete report body is retained in the current session for verbatim final/delivery transfer; historical Revision 4 validation is cited only as existing-test context, not reused as Revision 5 qualification evidence.

Revision 5 validation evidence (2026-07-20): pre- and post-validation Candidate References are identical at 37 tracked paths, `860 insertions`, `172 deletions`, clean `git diff --check`, and scoped hash `5d92518e7650af20620c68ff98aba498639f5c14`. `npm run validate:strict`, `npm test`, and `npm run openspec:validate` each exited 0; tests reported 238 library tests, 76 contract tests, and all remaining suites passing; OpenSpec reported 9 passed and 0 failed. Complete combined output: `tool_f7c811cde0015a4azvGEAc24jc`. This checkbox/evidence update is allowed orchestration status metadata and does not mutate the frozen 37-path candidate.

Revision 5 final and delivery evidence (2026-07-20): the one fresh final reviewer returned `approved` with high confidence, no findings, and empty `Blocking Evidence`. The one Material delivery reviewer then returned `blocked`, `Change-Ready: no`, with two P0 provenance blockers: its successful `session_delivery_context` output did not contain the actual owner selection returned by the `question` tool and still treated the historical owner-decision todo as unresolved; the transferred SDET envelope said its session ID was not supplied even though the orchestrator's dispatch adapter returned child task identity `ses_08383f3b6ffedzBCz8oVED32d0`. Under the frozen stop line, delivery is not replayed and neither provenance gap authorizes mutation. Revision 5 is terminal with `Change-Ready: no`; task 8.4 remains incomplete. Any later evidence-only revision requires a new explicit owner message and must transfer adapter-proven child identity in the evidence package.
