# Harvest 5.1 Docs

- Snapshot: `node global/bin/repo-candidate-snapshot.ts --help` exit 0.
- Gate has no `--help` (unknown option) — documented as existing CLI shape, not a new mode.
- README names dividend archive rules, snapshot CLI, and the two-scenario Windows claim ceiling.
- Proof inventory already describes snapshot effects/truncation.
- No secrets or absolute private paths added.
