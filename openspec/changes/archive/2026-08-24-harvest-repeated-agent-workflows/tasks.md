## 1. Ownership And Baseline

- [x] 1.1 Reconcile every active writer and current byte owner that overlaps the global workflow instructions, OpenSpec propose/apply/archive surfaces, operation gate/archive core, evidence contracts, validators, package scripts, consumer proof, and modified main specs; inspect this change's history and attributable diffs without restoring any current work, then verify the accepted consumer baseline is current or capture the minimum new baseline needed to preserve at least two attributable manual Git-inspection sequences. **Current Rung:** planning from current source, archived ceremony rationale, active global-source identity, and existing consumer proof contracts. **Next Real Boundary:** provider-free `npm.cmd run proof:consumer-outcome -- --mode preflight --source-ref HEAD` and replay/gate of the accepted baseline before candidate instruction mutation; bounded baseline capture only if identity or recurrence evidence is absent. **Blocker/Unblocker:** unknown overlapping writer liveness blocks mutations to its paths until terminal closure/isolation and final-byte integration; a stale baseline is unblocked by a create-new baseline capture under the existing reviewed scenario/provider bound. **Observable Proof:** an ownership/path matrix has no unresolved concurrent writer, baseline identity matches its governed source or is honestly re-established, hard oracles are green, and raw tool facts identify the repeated manual candidate sequence or the artifacts are updated to a different evidenced candidate. **Authorization/Safeguards:** read-only repository/installed-source inspection plus disposable provider-free preflight/replay; at most the existing explicitly bounded non-sensitive baseline calls if required, with no install, activation, credential, remote, commit, push, or owner-process effect. **Restoration/Cleanup:** delete only proof-owned sessions/processes/fixtures and preserve immutable baseline evidence plus all unrelated work. **Immutable Evidence:** ownership identities, source/config/model/environment digests, exact redacted invocations, baseline bundle/evaluation or replay, recurrence rows, stdout/stderr, and cleanup. **Validation:** consumer preflight, baseline replay/evaluation, and current-candidate gate exit zero at their supported claim ceiling.

## 2. Portable Snapshot Happy Path

- [x] 2.1 [automation-dividend] Implement the minimal portable repository candidate snapshot under the active global CLI owner by reusing `portable-process.ts`; prove effect-free `--help`/`-h`, schema-versioned mixed-worktree JSON, bounded staged/unstaged patches, HEAD/branch/upstream/history facts, stable path ordering, and no repository mutation, then invoke it once against this change's current worktree as its first real consumer and record the current task/evidence-index row with `build-minimal`, recurrence source, before/after operation identities, candidate/environment, output/effects, and cleanup. **Current Rung:** green ownership/baseline and attributable recurrence from 1.1. **Next Real Boundary:** the actual global-source CLI against one disposable Git repository containing staged, unstaged, and untracked changes, followed by summary-mode inspection of this current repository. **Blocker/Unblocker:** 1.1; failure to prove recurrence or lower-cost owner non-fit requires updating the declaration/task/history before implementation, while any open overlapping `global/bin` writer blocks only that path. **Observable Proof:** both help forms exit zero without Git access; one CLI invocation reports the exact direct-Git facts within the 131072-byte ceiling, changes no Git/file identity, leaves no process, and replaces the declared multi-command sequence in the current consumer. **Authorization/Safeguards:** repository production/test/evidence edits and disposable local Git only; shell-free Git, external diff/text conversion disabled, no untracked contents, hook, fetch, credential, remote, commit, push, install, or activation. **Restoration/Cleanup:** remove only disposable worktrees/processes and preserve the current repository bytes/index exactly except for attributable implementation/evidence edits. **Immutable Evidence:** source/task/helper hashes, fixture before/after manifests, direct-Git oracle, CLI argv/status/stdout/stderr, truncation/effect facts, current consumer invocation, and cleanup. **Validation:** new focused snapshot test command plus actual help and mixed-worktree CLI invocations exit zero.

- [x] 2.2 Complete the snapshot's declared failure and boundary states: clean worktree, staged-only, unstaged-only, binary/truncated patches, conflicts, detached HEAD, missing upstream, malicious external-diff configuration, missing Git, non-worktree root, unsupported arguments/schema, and bounded original-cause diagnostics; add the maintained CLI/proof inventory entry and verify no state silently becomes complete review after truncation. **Current Rung:** working mixed-worktree happy path from 2.1. **Next Real Boundary:** the same actual CLI across disposable provider-free Git fixtures for every declared state. **Blocker/Unblocker:** 2.1; a Git state that cannot be produced safely remains a named unsupported fixture and blocks only its corresponding claim until a safe direct oracle exists. **Observable Proof:** every fixture result equals direct Git facts, expected failures are non-zero and actionable, malicious config executes no external program, output remains within bounds, and before/after manifests plus process inventory are unchanged. **Authorization/Safeguards:** disposable local files/processes only under exact fixture roots; no network, credentials, remote, owner worktree mutation, or arbitrary repository-configured executable. **Restoration/Cleanup:** terminate only attributed fixture processes and delete fixture roots in `finally`; cleanup uncertainty blocks the next fixture. **Immutable Evidence:** ordered scenario rows, direct oracles, CLI outputs/exits, sentinel non-execution fact, manifests, truncation counts, diagnostics, and cleanup. **Validation:** complete focused snapshot suite and CLI help readback exit zero with concise success output.

## 3. OpenSpec Dividend Integration

- [x] 3.1 Extend the existing proposal/task/evidence contracts and portable operation gate/archive checks for the exact `Automation Dividend` declaration, `[automation-dividend]` task cardinality, exempt path, task digest, helper/candidate/environment correlation, and green evidence result without semantic inference; prove propose/apply/archive behavior in disposable changes for missing, duplicate, malformed, unchecked, stale, current required, and current exempt cases. **Current Rung:** proven snapshot CLI and evidence shape from 2.2. **Next Real Boundary:** actual global operation-gate and archive core over disposable OpenSpec changes with fake official archive/validation adapters and no mutation of active changes. **Blocker/Unblocker:** 2.2 and closure/integration of overlapping gate/evidence writers from 1.1; unsupported existing evidence schema blocks only required-dividend completion until minimally extended. **Observable Proof:** propose rejects malformed declarations, apply correlates one required task, complete archive fails only the explicit incomplete required facts, an exempt Ordinary Small fixture remains archive-eligible, product gates stay independent, and no gate scores prose or chooses a mode. **Authorization/Safeguards:** repository edits plus disposable local Git/OpenSpec/evidence fixtures; no official archive of a real change, provider call, main-spec sync, remote action, commit, push, install, or activation. **Restoration/Cleanup:** remove fixture repositories and fake processes while retaining bounded test output; active changes remain unmoved. **Immutable Evidence:** contract/gate hashes, exact fixture inputs, normalized check rows, exits/stdout/stderr, fake archive counts, state manifests, and cleanup. **Validation:** focused operation-gate, archive/change-inventory, evidence-contract, and workflow-contract suites exit zero.

- [x] 3.2 Update the single canonical loaded workflow owner and synchronized propose/apply/archive command/skill surfaces, project templates, validation contracts, and context guidance to select one proposal-time dividend for Material/explicitly eligible changes, preserve concrete Ordinary Small exemption, use the snapshot at matching Git checkpoints, and keep optional reflection outside completion; verify active-source resolution, mirror consistency, effect-free help, and startup/discovery/on-demand context ceilings without copying the full contract into always-loaded text. **Current Rung:** provider-free gate integration from 3.1. **Next Real Boundary:** generated/disposable global-source loading plus `opencode debug`/inventory/contract validation before any configured-model capture. **Blocker/Unblocker:** 3.1 and terminal overlap for each instruction owner; a context-ceiling breach is unblocked by narrowing or removing duplicate wording, not by raising the reviewed budget automatically. **Observable Proof:** one canonical owner and every maintained mirror agree on required/exempt/dividend semantics and CLI invocation; loaded source exposes the intended routing, no stale unconditional non-blocking clause remains, help is effect-free, and all context classes stay within reviewed maxima. **Authorization/Safeguards:** repository instruction/config/test edits and disposable generated config only; no machine-local sync, running-session claim, provider call, credential, remote, install, or activation. **Restoration/Cleanup:** remove generated loader fixtures and preserve active host config/running sessions unchanged. **Immutable Evidence:** source/loader manifests, instruction inventory/budget rows, mirror/contract diagnostics, help output, exits/stdout/stderr, and cleanup. **Validation:** focused contracts/install/runtime-source tests, workflow-contract consistency proof, instruction inventory/budget, and strict structural validation exit zero.

## 4. Matched Workflow Improvement Proof

- [x] 4.1 Extend only the maintained OpenSpec-backed consumer fixture/request needed to require one candidate-state inspection while preserving exactly two reviewed scenarios and every existing outcome/safety/cleanup field; materialize a reviewed candidate request with expectation `improvement`, then run provider-free consumer preflight and current-candidate gate against the staged source and verify the baseline/candidate comparison identities are matchable. **Current Rung:** provider-free loaded instruction candidate from 3.2. **Next Real Boundary:** existing consumer runner preflight/gate over the exact scenario manifest, fixture, accepted baseline pointer, candidate source manifest, and candidate-request JSON. **Blocker/Unblocker:** 3.2; any scenario/environment/source mismatch blocks configured-provider capture until the reviewed input or source identity is corrected, never by evaluator inference. **Observable Proof:** preflight reports two stable scenario ids, exact sample/provider/evidence bounds, contained disposable fixtures, matching comparison fields, `improvement` expectation, and no session/provider/effect. **Authorization/Safeguards:** reviewed repository config/fixture/candidate-request edits and provider-free reads only; no capture, install, remote, credentials, or target-project mutation. **Restoration/Cleanup:** no runtime fixture should survive preflight; remove only accidental proof-owned temp state. **Immutable Evidence:** manifest/candidate-request/source digests, normalized preflight/gate output, exits/stdout/stderr, and zero-live-effect facts. **Validation:** `npm.cmd run proof:consumer-outcome -- --mode preflight --source-ref working-tree` and provider-free gate with the reviewed candidate request exit zero.

- [x] 4.2 Run one governed matched candidate capture against the accepted baseline through the existing consumer runner, then run provider-free replay/evaluation with expectation `improvement`; require all output/state/proof/validation/permission/forbidden-effect/diagnostic/cleanup oracles green, no friction-vector regression, and a strict median `totalToolCallCount` reduction in at least one scenario before retaining the loaded workflow claim. **Current Rung:** green matchable preflight from 4.1. **Next Real Boundary:** installed disposable OpenCode main sessions with identical configured model/profile, permissions, scenarios, pair order, fixtures, and source-independent environment fields. **Blocker/Unblocker:** 4.1; one evidence-only failure blocks unchanged capture repetition until the complete preserved bundle replays through terminal evaluation or identifies the exact missing raw observation. **Observable Proof:** evaluator returns `passed-improvement`, names the exact improved scenario/counts, preserves every sample and identity, reports no hard-oracle or friction regression, and proves terminal session/process/fixture cleanup. **Authorization/Safeguards:** exactly the reviewed bounded non-sensitive configured-provider samples under standing kit authorization, disposable local writes only, no credential output, install, remote, commit, push, target-project, or owner-session effect. **Restoration/Cleanup:** delete every proof session/process/fixture in `finally`; unknown liveness or cleanup blocks integration and another capture. **Immutable Evidence:** accepted baseline reference, create-new candidate bundle, source/model/profile/OpenCode/scenario identities, raw events/tool calls/friction vectors, output manifests, evaluator/replay results, stdout/stderr, and cleanup. **Validation:** provider-free replay and evaluate produce the same terminal digest, and the current-candidate gate accepts the exact candidate bundle/request.

## 5. Documentation, Validation, And Handoff

- [x] 5.1 Update operator and proof documentation for snapshot discovery, effects, output/truncation semantics, proposal declarations, reuse/extend/build-minimal selection, evidence correlation, exemption, gate diagnostics, matched-proof limits, and the non-goals of retrospectives and generic workflow recording; verify every documented effect-free command through the actual current entrypoint and ensure no absolute private path, secret, stale mode, or universal improvement claim appears. **Current Rung:** current matched improvement evidence from 4.2. **Next Real Boundary:** a cold local operator follows CLI help, snapshot summary on a disposable repository, operation-gate help/checks, and proof inventory references without implementation context. **Blocker/Unblocker:** 4.2 supplies exact supported commands and claim ceiling; documentation cannot broaden a failed or unmeasured claim. **Observable Proof:** all links/commands resolve, effect-free commands exit zero, docs match schemas and inventory, and limitations name Windows-only matched evidence plus unmeasured repository classes. **Authorization/Safeguards:** repository documentation edits and disposable/read-only command use only; no provider capture, install, activation, remote, commit, or push. **Restoration/Cleanup:** delete disposable doc fixtures/processes and preserve host state. **Immutable Evidence:** doc diff, command argv/exits/stdout/stderr, link/strict validation, privacy scan, and cleanup. **Validation:** markdown, inventory, workflow-contract, CLI help, and strict selected OpenSpec checks exit zero.

- [x] 5.2 Freeze one readable current candidate and run the complete applicable validation set: snapshot and operation-gate focused suites, workflow/contract/install/runtime-source tests, consumer replay/evaluate/gate, `npm.cmd test`, `npm.cmd run validate:strict`, `npm.cmd run openspec:validate`, and `openspec.cmd validate harvest-repeated-agent-workflows --strict`; finish with the snapshot CLI read-only candidate inspection and a local development handoff recording outcome/non-goals, candidate and proof identities, claim ceiling, validation, cleanup, known non-critical limitations, overlap disposition, and external-operation state. **Current Rung:** complete accepted source/docs and current raw improvement evidence from 5.1. **Next Real Boundary:** complete project-native validation plus final actual CLI readback on the same source candidate; planning/implementation remains local and uninstalled. **Blocker/Unblocker:** any candidate-attributable red check returns only its dependent lane to correction and re-proof; unrelated active-change failures are preserved and reported without mutating their owners. **Observable Proof:** every candidate-applicable command exits zero, current source/evidence/task identities correlate, final snapshot is complete or explicitly truncated, no proof orphan or open overlapping writer affects the candidate, and the handoff makes no RC/stable or universal-friction claim. **Authorization/Safeguards:** local validation/readback only; no configured-provider recapture unless 4.2's evidence-based unlock condition is met, and no install, activation, commit, push, archive, release, deployment, remote, or unrelated-work mutation. **Restoration/Cleanup:** stop validation/proof processes, remove disposable state, retain immutable required evidence, and leave host/active changes unchanged. **Immutable Evidence:** candidate manifest/hash, complete command table with exits/stdout/stderr/artifact paths, replay/evaluation digest, final snapshot, cleanup, and handoff. **Validation:** all named commands green on one current candidate; lifecycle remains `Development-Stage: development` until implementation proof later establishes otherwise.
