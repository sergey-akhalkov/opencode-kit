## 1. Permission Candidate And Earliest Runtime Signal

- [x] 1.1 Change the canonical SDET permission, operative wording, contract, validator, focused oracle, normative base spec, and installed permission proof from approval-gated edit to explicit unattended test-only edit; invoke the fresh installed `opencode debug agent sdet-quality-engineer` and maintained permission proof before any provider-backed run. **Current Rung:** provider-free baseline readback proves final `edit: ask` overrides the global allow default. **Next Real Boundary:** provider-free fresh-process candidate readback through the installed OpenCode loader. **Blocker/Unblocker:** none. **Observable Proof:** effective SDET edit resolves to `allow`, no final ask-level edit rule remains, every other declared deny remains unchanged, and focused contracts pass. **Owner Authorization:** local source edits and read-only local process execution. **Safeguards:** preserve bash/network/delegation/question/external-directory denies and exact test-only scope; no install, activation, credentials, provider call, production project, or remote effect. **Restoration/Cleanup:** no disposable state; source-local rollback is `allow` to `ask` with synchronized wording. **Expected Immutable Evidence:** scoped diff, source identities, resolved permission JSON facts, command exits/stdout/stderr. **Validation:** focused contracts, strict selected-change validation, and `npm run proof:permissions`. **Dependency-Chain Stop Condition:** do not invoke a model while resolved edit remains ask/unknown or another SDET restriction drifts. **Observed:** installed OpenCode `1.18.18` resolved `edit=allow`; `bash`, task, question, skill, web, todo, external-directory, LSP, and doom-loop remained denied; `proof:permissions`, direct validator, exact SDET contract oracle, selected strict OpenSpec validation, and diff check passed. The aggregate contracts command also passed the SDET row but retained one unrelated pre-existing README doctor-copy failure from concurrent work.

## 2. Actual SDET Edit Boundary

- [x] 2.1 Invoke a fresh actual installed `sdet-quality-engineer` in a disposable project with current synthetic Runtime Proof and one exact test-only output path; observe the edit without a permission reply, inspect every file/process/session effect, and clean up. **Current Rung:** provider-free candidate permission readback. **Next Real Boundary:** provider-backed SDET tool invocation through the same installed loader and agent entry point used by qualification. **Blocker/Unblocker:** task 1.1 green and fresh-process preflight confirming candidate source/model/permissions. **Observable Proof:** the agent exits terminally, creates only the supplied test artifact, no interactive permission reply is requested, no production or external state changes, and cleanup succeeds. **Owner Authorization:** one bounded non-sensitive configured-provider call plus disposable local files/processes. **Safeguards:** isolated project/config data, no credentials in input/output, no production source, network target, commit, push, install, or external operation. **Restoration/Cleanup:** delete the proof-owned OpenCode session and disposable project after preserving privacy-safe facts; cleanup failure is terminal. **Expected Immutable Evidence:** exact invocation/input, Candidate and Environment identity, exit/stdout/stderr, permission-reply count or equivalent event evidence, file-effect inventory, session identity, and cleanup result. **Validation:** candidate permission evaluator over the preserved run facts. **Dependency-Chain Stop Condition:** a prompt, out-of-scope edit, unobservable permission event, or incomplete cleanup leaves the candidate at `development` and blocks qualification. **Observed:** routed child `sdet-quality-engineer` completed one exact `edit`; 1,155 permission polls observed zero requests; the single 18-byte file matched exactly; sessions/server cleaned. Initial fixture deletion hit transient Windows `EPERM`; no live retry occurred. Provider-free `replay-r1` verified the preserved product facts and later proof-owned fixture removal terminally green, and current-runner `preflight-r2` cleaned all state.

## 3. Qualification And Handoff

- [x] 3.1 After current Runtime Proof and accepted-scope completion, dispatch one fresh test-only SDET against permission precedence, accidental production authority, missing-scope fail-open, restriction drift, and false prompt-free claims; main reproduces and dispositions every plausible critical row. **Current Rung:** complete current candidate with actual disposable edit proof. **Next Real Boundary:** fresh critical-only challenge of the same source and local runtime envelope. **Blocker/Unblocker:** task 2.1 green and no open mutator. **Observable Proof:** terminal `critical-risks-reported`, `no-critical-risk`, or exact `blocked` evidence with Effective Model; a confirmed critical defect is corrected and re-proven before any rule-valid successor. **Owner Authorization:** local test-only inspection/mutation and bounded configured inference. **Safeguards:** SDET edits no production, instruction, config, proof-runner, OpenSpec, or external state and asks no user question. **Restoration/Cleanup:** preserve accepted test evidence, delete only disposable state, and close writer liveness before integration. **Expected Immutable Evidence:** SDET identity/report, risk matrix, attributed test-only diff or none, and main disposition. **Validation:** smallest critical regression oracle only when justified. **Dependency-Chain Stop Condition:** the first precondition-valid attempt without a main-confirmed critical defect permanently stops SDET for this root. **Observed:** fresh child `ses_fff55e574ffextnn5Q5RfJZZRW`, Effective Model `xai/grok-4.6`, returned `no-critical-risk`, no matrix rows, no test changes, and no execution request. Main dispositioned every residual evidence gap in `implementation-evidence/critical-sdet.md`; the adapter-proven child id repairs the report's `unknown` identity field without a prohibited repeat. SDET is terminal for this root.

- [x] 3.2 Run focused and complete project-native validation, fresh permission readback, strict OpenSpec checks, scoped diff/secret review, and freeze the next monotonic RC candidate with restart behavior, rollback, residual semantic-containment risk, external-operation state, and candidate identity. **Current Rung:** terminal Material SDET candidate. **Next Real Boundary:** final history retrospective, then future operator qualification after one OpenCode restart. **Blocker/Unblocker:** task 3.1 terminal, current Runtime Proof, and no known reachable critical/non-deferrable defect. **Observable Proof:** every applicable command exits `0`, the effective candidate remains prompt-free for edit with other denies intact, and validation records `Development-Stage: RC1`; the final retrospective promotes the same RC only after all generated work is complete. **Owner Authorization:** local validation and documentation only. **Safeguards:** no install, activation, restart, commit, push, release, publication, or remote mutation. **Restoration/Cleanup:** remove validation-owned disposable state and preserve raw evidence. **Expected Immutable Evidence:** exact validation commands/exits, current scoped diff/source identities, runtime readback, SDET terminal reason, and RC handoff. **Validation:** `npm run validate:strict`, `npm test`, `npm run proof:permissions`, `openspec validate allow-unattended-sdet-edits --strict`, `openspec validate --all --strict`, and `git diff --check`. **Dependency-Chain Stop Condition:** red validation or an unresolved critical/non-deferrable risk prevents RC/stable claims. **Observed:** `allow-unattended-sdet-edits-RC1` is frozen. Strict library validation passed with zero warnings; full `npm test` exited `0`; permission proof reports SDET edit allow with every named deny intact; selected strict OpenSpec validation passed; all strict OpenSpec validation passed 16/16; apply gate and `git diff --check` passed. Code-quality inventory places the cohesive scenario runner at 460-line attention, not split-candidate; fresh reduction review found no safe net reduction. No secret, install, activation, restart, commit, push, release, publication, or remote action occurred.

## 4. Final History Retrospective

- [x] 4.1 After every other currently known task is complete, analyze the complete `history.md` once using the canonical six-cell `Quality`, `Cycle Speed`, and `Token Economy` matrix across `Working Repository` and `opencode-kit`; classify each evidence-backed candidate by Impact Horizon, Concrete Consumers, Execution Class, Earliest Safe Point, Invalidated Evidence, and Observable Payback; append and immediately execute every admitted exact-current-consumer improvement under `## Session-Derived Improvements`, preserve no-current-consumer candidates as non-checkbox deferred history records, or record `none` without inventing work. After generated work is complete, finalize the same RC's stable local handoff without another analysis. Apply, archive, compaction, and generated tasks MUST NOT create or rerun this final-history analysis. **Observed:** the complete history matrix is recorded in `history.md`; all six cells were evaluated; previously evidenced SDK-route and cleanup-replay improvements were already consumed and proven; no remaining admitted or deferred candidate exists; no generated task was added; `allow-unattended-sdet-edits-RC1` is finalized as stable in `implementation-evidence/stable-handoff.md`.
