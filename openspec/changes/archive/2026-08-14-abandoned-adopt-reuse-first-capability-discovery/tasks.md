## Fidelity Ladder

All behavior slices use one fidelity ladder: **Rung 1** deterministic offline schema/corpus replay; **Rung 2** actual portable client execution against disposable local Git registry/producer/consumer repositories; **Rung 3** same-model fresh OpenCode sessions through the real loader and local client; **Rung 4** activation against the owner's actual private registry and project groups. Rung 4 is an owner-controlled follow-up outside this change and SHALL NOT be executed or represented as current proof.

## 1. Freeze Scope, Inventory, and Baselines

- [x] 1.1 Freeze the accepted Outcome Capsule, exact Product Candidate paths, proof/evaluator roles, root RC history, Material SDET stop rule, and unrelated dirty-worktree inventory in `implementation-evidence/pre-change-baseline.md`; inspect `tools/proofs/README.md`, `tools/proofs/lib/opencode-proof-client.ts`, `global/bin/portable-process.ts`, current config/tool validators, and current instruction mirrors before selecting new owners. Run `git status --short`, `npm run project:inventory -- --root . --format json`, `npm run instruction:inventory -- --format json`, `npm run code-quality:inventory -- --root . --format json --attention-lines 400 --split-lines 800`, `npm run validate:strict`, `npm test`, and `npm run openspec:validate`, preserving exact exit/stdout/stderr and attributing pre-existing failures without mutation. **Current Rung**: Rung 1 inventory. **Next Real Boundary**: Rung 3 same-model baseline through the installed OpenCode loader. **Blocker/Unblocker**: none; this task must complete before Product Candidate mutation. **Observable Proof**: a readable baseline names current instruction token proxies, validation state, touched responsibilities, retained proof library, and every unrelated change. **Authorization**: local read-only commands only. **Safeguards**: no staging, cleanup, source edits, external calls, or printing private model/config values. **Restoration/Cleanup**: none required; preserve raw outputs under `implementation-evidence/baseline/`. **Expected Immutable Evidence**: command transcript, environment/tool versions, and baseline artifact hashes.

- [x] 1.2 Extend the existing proof-client ownership with the smallest reusable `tools/proofs/reuse-discovery.ts` runner and package invocation needed to capture baseline/current scenario facts; add its exact modes, effects, authorization, cleanup, and limits to `tools/proofs/README.md`, then run offline preflight and the eight baseline scenarios (`inventory-refresh`, `local-owner`, `registered-peer`, `stale-record`, `typical-external`, `no-match`, `trivial-fix`, `registry-unavailable`) with the `quality-independent` profile before any loaded instruction, command, scanner, or registry Product Candidate mutation. The runner is Proof Runner/capture tooling, not an automated test or Product Candidate, and SHALL reuse `tools/proofs/lib/opencode-proof-client.ts` or record why direct `opencode run` capture is the smaller existing boundary. **Current Rung**: Rung 1 runner preflight followed by Rung 3 baseline sessions. **Next Real Boundary**: Rung 2 candidate bootstrap/query client in disposable repositories. **Blocker/Unblocker**: task 1.1 baseline identity; a red/unsafe preflight blocks provider calls until corrected. **Observable Proof**: identical synthetic inputs, profile/model route, tool availability, exact prompts, tool calls, token/context and elapsed facts when exposed, outputs, side effects, and cleanup are captured per scenario without quality scoring. **Authorization**: standing bounded synthetic-model authorization for `opencode-kit`; use non-sensitive prompts and the minimum eight baseline calls, with no purchases or quota changes. **Safeguards**: disposable roots, disabled remote/destructive tools, privacy-safe output, no real registry/project scan or public ecosystem mutation. **Restoration/Cleanup**: delete proof sessions and disposable roots in `finally`; unknown cleanup is terminal and preserved. **Expected Immutable Evidence**: `implementation-evidence/baseline-sessions/` raw JSON/bundles, runner hash, route identity, exit/stdout/stderr, and cleanup verdict.

## 2. Deliver and Prove the Smallest Complete Reuse Path

- [x] 2.1 Implement the project-neutral registry/schema/template, explicit private config and resolved-plan parser, stable logical project/group/capability/generated-project model, minimal exact-committed-tree bootstrap, exact-term bounded query, candidate enqueue/local sync, and thin CLI entrypoint under cohesive `global/bin/` owners; reuse existing `zod`, `global/bin/portable-process.ts`, staged-validation temporary-worktree/path guards, project inventory logic, and validator conventions rather than adding a parser, dependency, package-manager assumption, or broad framework. Add only production proof/capture tooling needed to create disposable private registry, producer, and consumer Git repositories. **Current Rung**: Rung 1 schema/corpus replay. **Next Real Boundary**: Rung 2 actual core `bootstrap`, `validate`, `enqueue`, `sync`, `status`, and `query` against unrelated disposable Git repositories. **Blocker/Unblocker**: tasks 1.1-1.2 freeze ownership and baseline; no external blocker. **Observable Proof**: the core scans only exact committed source for two explicit projects, writes generated structure/entrypoints/candidates with checkpoints, leaves curated records empty until one source-verified candidate is enqueued/synced, then returns that exact curated match with `limit=10`, stable total/`hasMore`, redacted roots, and no unselected capability. **Authorization**: local disposable file/Git effects only. **Safeguards**: explicit temp parent and resolved plan, path containment, dirty/untracked sentinels, no owner paths, credentials, network, clone/fetch/pull, package install, commit, push, or command execution from records. **Restoration/Cleanup**: runner deletes disposable repositories and records unknown cleanup as non-zero. **Expected Immutable Evidence**: `implementation-evidence/runtime-proof/minimal-bootstrap-client/` with invocation, candidate/environment identity, stdout/stderr/exit, registry/config/plan/commit bytes or hashes, generated/curated file effects, and cleanup.

- [ ] 2.2 Add the reusable `global/commands/reuse-inventory.md` free-form command, compact canonical new-mechanism trigger in `global/AGENTS.md`, lazy `global/skills/reuse-discovery/SKILL.md`, and only the minimum orchestration/disposition behavior needed for the complete bootstrap-to-reuse path; consolidate superseded reuse/modularity wording rather than copying the workflow into project templates or roles. The command SHALL resolve exact local registry/projects/group/refs, request a local path rather than clone when given only a URL, show a concise preview, and invoke the deterministic plan without exposing flags. Do not create an agent or automated tests. **Current Rung**: Rung 2 green minimal bootstrap/client from task 2.1. **Next Real Boundary**: Rung 3 one fresh `inventory-refresh` command session followed by one `registered-peer` new-mechanism session through the real loader, command, skill, generated candidate verification, sync, query, and selected disposable source. **Blocker/Unblocker**: task 2.1; if bootstrap/client proof is red, stop this dependency chain and correct it before loaded-policy expansion. **Observable Proof**: a plain-language request inventories only two named committed projects, promotes one verified candidate, and records exact checkpoint/artifacts; the subsequent registered-peer task triggers before production code, loads detail once, queries only the enabled group, verifies source, records `reuse`, and adds no duplicate mechanism. **Authorization**: two bounded non-sensitive configured-provider calls under standing authorization. **Safeguards**: disposable registry/projects, dirty and unallowlisted sentinels, no dependency install/source copy/network/clone/fetch/pull/commit/push, and exact tool map. **Restoration/Cleanup**: delete sessions and disposable roots; preserve failure bundle before rerun. **Expected Immutable Evidence**: `implementation-evidence/runtime-proof/bootstrap-to-reuse-mvp/` with raw command/session/tool traces, resolved plan/preview, commit/checkpoint/query/source facts, output, side effects, diagnostics, and cleanup. Green proof establishes the first `MVP`; red proof remains `development`.

## 3. Complete Inventory, Registry, Privacy, Verification, and Outbox Behavior

- [ ] 3.1 Complete named-group intersection, machine-local exact-root/Codebase Memory bindings, deterministic index derivation, finite query maximum, pagination facts, root redaction, schema/reference/path validation, and untrusted registry-data handling; add focused production validators/contracts without automated test fixtures. Product Candidate mutation returns the change to `development`. **Current Rung**: Rung 2 minimal client and Rung 3 registered-peer proof. **Next Real Boundary**: Rung 2 group-isolation and malformed-record CLI lanes. **Blocker/Unblocker**: task 2.2 MVP; none after current candidate mutation. **Observable Proof**: actual CLI rejects path escape, unknown group/project, index drift, duplicate IDs, over-limit requests, and instruction-like command execution while never printing the sentinel unallowlisted project/root; the corrected registered-peer lane restores current `MVP`. **Authorization**: local disposable repositories only. **Safeguards**: explicit bindings, privacy-safe diagnostics, no `list_projects` enumeration, no external-directory writes outside disposable/configured roots. **Restoration/Cleanup**: deterministic fixture cleanup; preserve any unknown write state and stop. **Expected Immutable Evidence**: `implementation-evidence/runtime-proof/privacy-validation/` raw inputs, outputs, intended effects, hashes, diagnostics, and restored MVP reference.

- [ ] 3.2 Complete selected-source verification, stale/missing/mismatched entrypoint diagnostics, exact bound Codebase Memory navigation contract, validated private cache identity, and explicit registry/cache degraded mode; do not let registry metadata establish compatibility or add hidden expiry/scoring heuristics. **Current Rung**: Rung 1 captured registry/source corpus and Rung 2 client. **Next Real Boundary**: Rung 2 disposable source mutation followed by real `query`/verification with registry available, cache-only, mismatched cache, and no-cache states. **Blocker/Unblocker**: task 3.1 exact privacy/binding authority. **Observable Proof**: a removed symbol becomes `stale`, readable source can be verified without Codebase Memory, mismatched cache is rejected, matching cache is labeled, and no-registry/no-cache emits degraded cross-project evidence instead of an empty success; replay the registered-peer happy path afterward to restore current `MVP`. **Authorization**: local disposable indexing/navigation only; no all-project inventory output. **Safeguards**: exact configured project identity, source as truth, no unallowlisted fallback, no remote fetch. **Restoration/Cleanup**: delete disposable index/project/cache state or preserve exact cleanup path on failure. **Expected Immutable Evidence**: `implementation-evidence/runtime-proof/source-cache-degraded/` with before/after source hashes, query/verifier facts, diagnostics, and cleanup.

- [ ] 3.3 Implement atomic private `enqueue` and local `sync` with `synced | pending | not-applicable` status, collision preservation, complete-registry validation, derived-index regeneration, pending visibility, and zero commit/push/publication behavior; reuse existing failure-atomic replacement patterns rather than a service/database. **Current Rung**: Rung 1 outbox state-machine replay. **Next Real Boundary**: Rung 2 actual outbox writes and registry sync in disposable repositories. **Blocker/Unblocker**: task 3.1 validated registry ownership and task 3.2 config/cache identity. **Observable Proof**: unavailable registry creates one contained schema-valid pending item; query excludes it; a conflicting central record remains unchanged with pending preserved; a non-conflicting sync updates only the intended capability/index, validates complete state, clears pending status, and performs zero Git/remote effects; rerun registered-peer proof for current `MVP`. **Authorization**: local disposable writes inside explicit outbox/registry roots. **Safeguards**: atomic replacement, path containment, no secret fields, no conflict overwrite, no producer mutation, no commit/push. **Restoration/Cleanup**: restore disposable baseline between lanes and delete roots only after state hashes are captured; unknown state is terminal. **Expected Immutable Evidence**: `implementation-evidence/runtime-proof/outbox-sync/` with file manifests/hashes, statuses, process diagnostics, Git status showing no commit, and cleanup.

- [ ] 3.4 Complete capability qualification, `local-reusable | portable-proven | adopted-external` validation, deprecation/replacement references, concise adopted-library records, and the total-lifecycle-cost disposition contract; keep rejected ecosystem results out of registry and prevent one-use helpers from being generalized for registration. **Current Rung**: Rung 1 preserved decision corpus. **Next Real Boundary**: Rung 2 actual client validation/query using owned-tool, two-consumer local, portable-proven, adopted-external, and ineligible-helper records. **Blocker/Unblocker**: tasks 3.1-3.3 stable registry/outbox format. **Observable Proof**: valid records remain navigational and bounded; invalid portability/external claims fail; the one-use helper yields `not-applicable`; accepted external data cannot authorize installation; registered-peer proof remains green. **Authorization**: local preserved ecosystem metadata and disposable repositories; no public package mutation. **Safeguards**: no network required for deterministic proof, no copied package docs, no vulnerability/license claim without evidence. **Restoration/Cleanup**: disposable data cleanup with hashes retained. **Expected Immutable Evidence**: `implementation-evidence/runtime-proof/qualification-external/` with exact accepted/rejected record outcomes and restored MVP reference.

- [ ] 3.5 Complete structure-and-architecture adapters plus incremental committed-tree refresh: save exact scan ref/full commit/tree/scanner/schema/policy identity in each atomic generated project file; implement same-commit no-op, rename-aware aggregate ancestor diff, changed/deleted fact updates, affected package/workspace expansion, non-ancestor/version full fallback, curated `needs-review` impact, and checkpoint preservation on every failure. Reuse existing `tools/project-inventory.ts` knowledge and Git/worktree helpers; optional Codebase Memory enrichment SHALL be exact-project and non-blocking. **Current Rung**: Rung 2 initial bootstrap from task 2.1. **Next Real Boundary**: Rung 2 actual rescan across a disposable commit series and rewritten-history fixture. **Blocker/Unblocker**: tasks 3.1-3.4 establish identity, validation, source verification, and candidate semantics; no external blocker. **Observable Proof**: same commit is byte-identical no-op; descendant commits update added/changed/renamed/deleted paths and only safe owner scopes; workspace-manifest change broadens with reason; non-ancestor/scanner-policy change performs full fallback; dirty/untracked bytes are excluded; injected scan/write/validation failure leaves the prior file/checkpoint/index authoritative; changed curated owner becomes `needs-review`; rerun bootstrap-to-reuse proof restores current `MVP`. **Authorization**: local disposable Git commits/worktrees only. **Safeguards**: no fetch/pull, no active-worktree mutation, no submodule recursion unless separately selected, exact before/after hashes, fail-safe broadening instead of hidden under-scan. **Restoration/Cleanup**: remove disposable worktrees/repos after raw capture; preserve unknown cleanup/recovery paths and stop. **Expected Immutable Evidence**: `implementation-evidence/runtime-proof/incremental-rescan/` with commit graph, aggregate diffs, scan plans/modes/reasons, file/checkpoint/index hashes, diagnostics, and cleanup.

## 4. Complete Runtime Policy and Same-Model Product Proof

- [ ] 4.1 Synchronize the free-form command, compact trigger, lazy skill, reusable/project/maintainer pointers, registry/config templates, README/profile/catalog, proof inventory, deterministic contract markers, and focused validator ownership; remove superseded duplicate reuse/modularity/inventory wording so `global/AGENTS.md` and the maintained always-loaded/shared runtime corpus do not exceed task 1.1 baselines. Do not instantiate or scan a real private registry/project or add inventory/capability data to instructions. **Current Rung**: Rung 1 instruction/command inventory and static mirror replay. **Next Real Boundary**: Rung 3 complete same-model candidate matrix. **Blocker/Unblocker**: all task 3 product lanes green; token-baseline failure requires consolidation before live candidate calls. **Observable Proof**: `npm run instruction:inventory -- --format json` meets both non-growth baselines, strict validation names no command/trigger/mirror/catalog drift, `/reuse-inventory` remains lazy, and a fresh trivial-fix preflight exposes no reuse skill or registry invocation. **Authorization**: local instruction/command/config/template edits and checks only. **Safeguards**: no private roots/groups/refs, no model/variant pins, no new agent, no loaded catalog, preserve unrelated instruction authority. **Restoration/Cleanup**: N/A for local source; record candidate hashes and correct only this task's candidate if proof fails. **Expected Immutable Evidence**: `implementation-evidence/instruction-integration.md` with before/after token proxies, canonical/command/mirror map, validation output, and candidate reference.

- [ ] 4.2 Run the reuse-discovery proof runner preflight, then execute all eight candidate scenarios with the exact task 1.2 model/profile, inputs, disposable environment, and tool envelope; evaluate baseline/candidate raw bundles offline before any repeated live/provider attempt. Correct only confirmed accepted-outcome or non-deferrable defects and rerun affected lanes; preserve the first complete green candidate as current `MVP`. **Current Rung**: Rung 2 complete local product boundary. **Next Real Boundary**: Rung 3 same-model fresh sessions. **Blocker/Unblocker**: task 4.1 green instruction budget and preflight; one evidence-only failure blocks another live attempt until preserved-corpus evaluator replay reaches its terminal verdict and identifies the causal correction or missing fact. **Observable Proof**: free-form inventory-refresh resolves only named/configured projects, uses initial then incremental/no-op/fallback checkpoint semantics, and promotes only a verified candidate; local-owner and registered-peer reuse without duplicate mechanisms; stale entry rejected; typical-external total-cost disposition without blind install; no-match smallest concrete owner without speculative API; trivial-fix zero registry/skill calls; unavailable registry explicit degraded disposition and pending registration path; no unallowlisted disclosure; outcome/proof quality preserved. **Authorization**: eight bounded non-sensitive configured-provider calls under standing authorization, with exact model/profile evidence and no purchases/quota changes. **Safeguards**: identical baseline/candidate prompts and initial state, disposable roots, disabled remote/destructive tools, no real registry/project scan, no clone/fetch/pull/commit/push, full raw capture, offline replay before retry. **Restoration/Cleanup**: delete every proof session/root after immutable capture; unknown cleanup blocks qualification. **Expected Immutable Evidence**: `implementation-evidence/candidate-sessions/`, `implementation-evidence/baseline-candidate-evaluation.md`, Candidate/Environment identities, raw tool/token/time/output facts, evaluator result, and `Live-Attempt Gate` state.

## 5. Fresh Material Critical SDET and Main Disposition

- [ ] 5.1 After task 4.2 current MVP and accepted production scope completion, freeze a readable Candidate Reference and dispatch one fresh `sdet-quality-engineer` with original requirements, raw proof bundles, wrong-project resolution/private-data/path-escape/dirty-tree/checkpoint-skip/partial-inventory/outbox-corruption criticality rubric, exact test-only write scope, forbidden production paths, disposable runner, and required `critical-risks-reported | no-critical-risk | blocked` return. Main SHALL independently reproduce every row; test mutation returns the candidate to `development`, and affected product proof SHALL restore `MVP`. Continue SDET only after an immediately prior main-confirmed critical defect, production correction, and new current proof; otherwise stop permanently for this root and park non-critical findings. **Current Rung**: Rung 3 current complete Product Candidate. **Next Real Boundary**: fresh SDET tests against the same disposable Rung 2/3 boundaries; Rung 4 remains out of scope. **Blocker/Unblocker**: tasks 4.1-4.2 green and all mutation-capable attempts closed. **Observable Proof**: terminal SDET state, main reproduction/disposition for each row, any smallest critical regression test, correction history, current proof after candidate/test mutation, and no production edits by SDET. **Authorization**: local disposable tests only; no real registry/projects, network, credentials, remote state, or user questions. **Safeguards**: fresh test-only context, privacy/commit/checkpoint sentinels, exact write scope, critical-only stop rule. **Restoration/Cleanup**: disposable state removed with terminal cleanup evidence; unknown writer liveness or cleanup blocks freeze. **Expected Immutable Evidence**: `implementation-evidence/critical-sdet.md`, test diff if any, Effective Model, raw reproduction commands/results, terminal reason, and current Candidate Reference.

## 6. Complete Validation, Freeze, and Handoff

- [ ] 6.1 Run focused registry/contract/config/instruction suites introduced by fresh SDET, then `npm run validate:strict`, `npm test`, `npm run code-quality:inventory -- --root . --format markdown --attention-lines 400 --split-lines 800`, `npm run instruction:inventory -- --format json`, `npm run openspec:validate`, and `npm run openspec:gate -- --operation prepush`; rerun the actual disposable registry CLI proof and offline baseline/candidate evaluator against the frozen candidate. Attribute every failure and correct only accepted-scope or non-deferrable defects, returning to `development` and replaying affected proof after Product Candidate mutation. **Current Rung**: Rung 1 complete static/automated validation plus preserved Rung 2/3 proof. **Next Real Boundary**: none inside the accepted envelope; Rung 4 owner activation is explicitly separate. **Blocker/Unblocker**: terminal usable task 5.1 SDET and closed writers. **Observable Proof**: every applicable command exits zero, instruction budgets remain at/below baseline, no split-or-justify or diagnostic regression remains, disposable proof/evaluator are green, and no known confirmed reachable critical/non-deferrable defect remains. **Authorization**: local validation only. **Safeguards**: preserve unrelated work, no broad staging, no remote effects, inspect complete diagnostics before correction. **Restoration/Cleanup**: proof runner cleans disposable state; preserve any failing raw bundle and stop repeated live attempts until offline replay unlocks. **Expected Immutable Evidence**: `implementation-evidence/final-validation.md` with commands, exits, stdout/stderr summaries and artifact paths, final token/code inventory, cleanup, and `Live-Attempt Gate`.

- [ ] 6.2 Freeze the next monotonic RC only after task 6.1 is green, then write a complete local handoff covering outcome/non-goals, architecture and split-or-justify decisions, diagnostics, Product Candidate/Proof Runner/Evaluator/Environment/Raw Bundle identities, baseline/candidate behavior, registry/outbox limitations, SDET terminal state, validation, rollback, pending risks, and explicit external-operation state. Report the same candidate `stable` only when the handoff is complete. **Current Rung**: Rung 3 locally qualified candidate. **Next Real Boundary**: Rung 4 owner-controlled creation/population/activation of the actual private registry in a separate task. **Blocker/Unblocker**: task 6.1 and no known confirmed critical/non-deferrable defect; actual registry root/groups are not blockers to local kit stability. **Observable Proof**: `Development-Stage: stable`, `Stable Candidate: RC<n>`, complete evidence references, and `External Operations: not performed`; no claim that an owner registry exists or is populated. **Authorization**: local documentation/handoff only. **Safeguards**: no commit, push, installation, activation, private registry creation, dependency mutation, or remote operation. **Restoration/Cleanup**: retain pending outbox/cache data only in disposable proof evidence; no owner state exists to restore. **Expected Immutable Evidence**: `implementation-evidence/stable-handoff.md` and final readable Candidate Reference/RC history.
