## 1. Freeze The Candidate And Proof Boundary

- [x] 1.1 Capture a create-new pre-change evidence bundle containing the repository candidate, effective elevation/OS/architecture/Node/npm/OpenCode/global-prefix identities, loaded config sources and collision scan, exact `global/opencode.json` and local-instruction preimage hashes, Nuphus package/command/config/model state, and relevant OpenCode/Notepad process baseline; verify the bundle contains no credential or screen content and fails closed on an unexpected existing owner.
- [x] 1.2 [automation-dividend] Add the smallest `tools/proofs/nuphus-desktop.ts` owner plus `proof:nuphus-desktop` package command and `tools/proofs/README.md` inventory row, reusing `global/bin/portable-process.ts`, `tools/proofs/lib/opencode-proof-client.ts`, and `tools/proofs/lib/proof-process-cleanup.ts`; verify `--help` and `-h` have zero package/config/process/network effects and that preflight/replay modes use explicit create-new paths, bounded privacy-safe diagnostics, cause-preserving failures, stable readback, and terminal proof-owned cleanup.

## 2. Install And Load The Exact MCP

- [x] 2.1 Reconfirm npm registry integrity/signature metadata and the current prefix immediately before mutation, install `@nuphus/nuphus-mcp@0.2.2` with matching `@nuphus/nuphus-mcp-win32-x64@0.2.2`, and run the proof runner's direct stdio capture; verify package and executable/library identities, MCP `initialize`, exactly 38 listed tools including the required observation/input operations, screen-size response, screenshot-to-new-PNG behavior, original stderr/exit preservation, and cleanup before any OpenCode config edit.
- [x] 2.2 Extend only gitignored `global/opencode.json` with an enabled local stdio `nuphus` command, 60000 ms timeout, and `NUPHUS_MCP_CONFIRM_WRITE=0`, and add the minimal observe-target-act-observe/screenshot-to-Read guidance to `global/opencode.local.instructions.md`; verify official config parsing plus complete readback show the intended entry and guidance, unchanged unrelated bytes/semantics, no secret, no portable-template/profile/shared-installer mutation, and an exact rollback preimage.

## 3. Prove The Real Desktop Outcome

- [x] 3.1 Reuse `configuredProofServerEnvironment` to start one proof-owned `opencode serve` on a newly allocated loopback port with isolated DB/XDG/test-home/project roots, point it at the active custom config by recorded digest, and overlay only `enabled: false` for Serena, Codebase Memory, and Graphify without defining Nuphus; capture resolved Nuphus projection, PID tree, exact base URL, startup logs, MCP status, sibling child inventory, and a bounded configured-provider screen-size request, and verify `nuphus connected` came from the matching active entry, no sibling MCP child started, no confirmation argument was requested, managed 4096/4097 listener/process identities stayed unchanged, and the proof tree is terminal before integration.
- [x] 3.2 Launch a new proof-owned Notepad instance, record its PID/HWND/title association, save a bounded screenshot to a proof-owned PNG, inspect that PNG with OpenCode Read, target the same current window, enter one unique non-sensitive marker through Nuphus, and obtain a fresh Read/perception observation; verify the marker is visibly present in only the attributed Notepad window, the result is not inferred from the input RPC, only the proof-owned Notepad PID is closed, the PNG is removed, and stale-identity/activation failure aborts before input.
- [x] 3.3 Exercise `desktop_perceive` once against the proof-owned Notepad state and preserve model download URLs/cache paths/file hashes plus OCR and icon availability; verify returned OCR coordinates correspond to the visible marker or report a cause-preserving degraded result, no Nuphus cloud vision credential/request exists, and screenshot-to-Read remains the sufficient fallback.

## 4. Reversibility And Focused Risk Proof

- [x] 4.1 Rehearse the exact rollback against the recorded installed identities by removing only `mcp.nuphus`, restoring only the changed local-instruction bytes, uninstalling the exact global Nuphus package, and rerunning task 3.1's same isolated ephemeral-port active-config route; verify Nuphus is absent from the restored config digest, unrelated config/packages/processes/models/worktree and managed 4096/4097 identities remain untouched, drift blocks deletion, then restore the reviewed candidate and repeat the attributed connected screen-size proof.
- [x] 4.2 After the real happy path is green, add the smallest focused regression coverage for option validation, MCP framing/error/timeout paths, text-base64 versus saved-PNG handling, exact config patch/rollback with unrelated-byte preservation, target-identity rejection, privacy-safe evidence, and proof cleanup; verify `node --test tools/test-nuphus-desktop.ts`, `npm run test:focused:portable-process`, and deterministic offline replay pass on the installed candidate.
- [x] 4.3 Run the instruction-artifact and configuration Practice Owner reviews against the installed candidate, and dispatch one fresh test-only SDET challenge for reachable wrong-window authorization, screen-privacy, rollback-drift, and proof-cleanup incidents; main shall reproduce or falsify every material row, apply only accepted-outcome corrections, repeat tasks 3.1 and 3.2 on the corrected candidate, and revalidate task 3.3 through current cache hashes plus offline replay without violating its recorded unchanged-live-retry prohibition before any readiness claim.
- [x] 4.4 Position the globally loaded machine-local Nuphus guidance explicitly for UI development and debugging when the required fact or effect is visible only in the running interface, while keeping source/log/test evidence primary for non-visual facts and retaining the existing observe-target-act-observe/privacy safeguards; verify the focused instruction regression and case-sensitive `opencode debug config --pure` projections from this repository plus at least two unrelated projects resolve the same enabled Nuphus entry and canonical instruction path, obtain one fresh instruction-artifact Practice Owner review of the concrete candidate, and retain privacy-safe evidence without restarting the user-owned OpenCode session or repeating live GUI/perception proof.

## 5. Validate And Hand Off

- [x] 5.1 Run `openspec validate install-nuphus-windows-desktop-mcp --strict`, `npm run validate:strict`, the relevant focused tests, `npm run doctor`, `npm run opencode:sources`, a Nuphus package/config/status readback, and a secret/private-screen-content scan of changed and evidence files; verify all applicable checks pass or retain the exact cause and narrow the claim.
- [x] 5.2 Re-read the final candidate and evidence, confirm no proof-owned process/window/temp PNG remains, record the exact installed package/config/model identities and active OpenCode restart requirement, and hand off Outcome, current claim ceiling, proof commands/results, rollback command, known non-critical limitations, screenshot privacy boundary, and explicit absence of commit/push/release/remote/cloud-vision effects.
