## 1. Baseline And Change Coordination

- [x] 1.1 Run the archive gate for `add-lightweight-sdet-pr-ready-sdlc`, prove all current tasks complete, archive it with stale delta-spec sync skipped, and require `npm run openspec:validate` plus `npx openspec list` to show no active historical conflict.
- [x] 1.2 Capture clean candidate identity, current instruction inventory, `npm run validate`, `npm run validate:strict`, and OpenSpec validation before runtime instruction mutation; record the known strict failure caused only by template `permission: "allow"`.
- [x] 1.3 Add an explicit dependency to `integrate-continuous-sdlc-learning` so its runtime baseline task cannot start until this change is complete and its baseline includes the accepted priority authority.

## 2. Canonical Priority And Maintained Pointers

- [x] 2.1 Replace scattered automation/token/caution wording in `global/AGENTS.md` with one ordered quality/safety -> autonomy -> speed authority, preserving proof, protected boundaries, blocker-only questions, non-critical parking, and no net token growth.
- [x] 2.2 Add only concise conceptual pointers or role-specific checks to the Universal Development Loop, maintainer/project templates, audit/porting guidance, instruction tuning/reviewer artifacts, README, token-economy, quality-gate, and adapter docs; do not copy the complete block into general skills or agents.

## 3. Permission Policy And Deterministic Drift Guards

- [x] 3.1 Align README, OpenSpec project guidance, and canonical config portability spec with intentional global `permission: "allow"`, separate repo-root `ask`, instruction-level safety limits, and restart behavior.
- [x] 3.2 Update `tools/validators/opencode-config.ts` so only exact root-relative `global/opencode.json.template` and `global/opencode.json` broad permissions are informational; unrelated and near-miss configs remain warnings.
- [x] 3.3 Add canonical priority and duplicate-block contracts to `tools/contracts/skills.ts` and operative-text enforcement to `tools/validators/routing.ts` without extending doctor requirements or introducing fuzzy semantic checks.

## 4. Observable Production Proof

- [x] 4.1 Run focused production checks and require `npm run validate:strict` to pass with informational global permission diagnostics while broad unrelated configs remain warning-class by source inspection.
- [x] 4.2 Run one bounded fresh OpenCode process in a disposable local workspace with explicit model and active global config; require a clear reversible task to proceed without a routine question, preserve proof despite a speed prompt, avoid unnecessary reviewers, and return concise evidence. Preserve invocation, input, environment identity, exit status, stdout/stderr, side effects, and artifacts.
- [x] 4.3 Re-run instruction inventory and require `global/AGENTS.md <= 13279` token proxy and complete inventory `<= 84513`; inspect removed text to ensure no unrelated safety authority was deleted.

## 5. Fresh SDET Test Ownership

- [x] 5.1 Dispatch one fresh `sdet-quality-engineer` after current proof with test-only write scope limited to focused validator fixtures under `tools/test-library/`; forbid production edits and require exact current candidate/proof evidence.
- [x] 5.2 Require SDET to cover missing operative priority markers, fenced decoys, copied full blocks, exact global template/machine-local INFO handling, near-miss/workspace WARN handling, and strict-mode outcomes; main independently reproduces any reported critical defect.

## 6. Validation Review And Stable Handoff

- [x] 6.1 Run `npm test`, `npm run validate:strict`, `npm run instruction:inventory -- --format markdown`, `npm run code-quality:inventory -- --root . --format markdown --attention-lines 400 --split-lines 800`, `npm run openspec:validate`, and `npm run openspec:gate -- --operation prepush`; require every applicable command to pass.
- [x] 6.2 Dispatch one fresh `instruction-artifact-reviewer` against the exact current candidate, runtime proof, token budget, permission semantics, removed prose, and validation; main shall disposition every risk row without treating the reviewer as lifecycle authority.
- [x] 6.3 Freeze the next RC only when accepted scope is complete, Material SDET is terminal, validation is green, and no known reachable critical/non-deferrable defect remains; record restart-required activation, residual global-allow enforcement risk, external-operation state, and stable handoff.
