# Strategy History

## 2026-08-23 - Use A Universal Vertical Closure Instead Of A Consumer Rule

- **Objective:** Prevent a representative real case and exhaustive lower-fidelity evidence from being promoted into an unsupported population or milestone claim across projects.
- **Approach:** Add one universal claim-evidence contract, compact always-loaded trigger, focused substitution skill, independent evidence-sufficiency reviewer, structured OpenSpec/archive facts, and completion-arbiter handling.
- **Evidence:** The observed consumer incident passed declared tasks, full validation, and consistency review while its broad completion claim composed different production paths and evidence populations. Current kit rules already own most individual proof mechanisms but no shared entailment record.
- **Outcome:** Selected for this proposal.
- **Reason:** A project-specific instruction would not protect other domains, while a prose-only reminder, skill-only fix, or optional reviewer-only fix could be skipped and would not stop archive or completion.
- **Do-Not-Repeat Condition:** Do not add the motivating project's names, paths, corpus, controller, protocol, hardware, or thresholds to portable instructions, fixtures, schemas, or reviewer prompts.
- **Evidence-Based Retry Condition:** A domain-specific adapter may be added only in that domain repository when its semantics cannot be represented by the universal population/path/oracle contract and the portable contract remains unchanged.

## 2026-08-23 - Keep Lifecycle And Evidence Breadth Orthogonal

- **Objective:** Preserve fast representative Runtime Proof without allowing `MVP`, RC, stable, checked tasks, or phase labels to imply broader evidence coverage.
- **Approach:** Retain existing lifecycle stages and add an independent claim record with exact case/population/path/environment/ceiling and `supported | narrowed | blocked | unknown` disposition.
- **Evidence:** Existing lifecycle requirements correctly support early representative proof, but completion can still be formally consistent with a weak or heterogeneous acceptance contract.
- **Outcome:** Selected.
- **Reason:** Redefining lifecycle labels as coverage levels would conflate candidate freshness with domain breadth and make ordinary fixes expensive.
- **Do-Not-Repeat Condition:** Do not encode population, compatibility, customer, safety, or milestone breadth as an implicit meaning of `MVP`, RC, stable, task completion, or archive status.
- **Evidence-Based Retry Condition:** Revisit only if executable evidence proves the separate claim axis cannot be projected or validated without duplicating lifecycle state.

## 2026-08-23 - Extend The Evidence Index And Consumer Harness

- **Objective:** Enforce closure mechanically and prove loaded behavior without adding another evidence database or model-evaluation framework.
- **Approach:** Add claim references to the existing bounded evidence index and a focused decision-gap pack to the existing matched consumer-outcome capture/replay/evaluator.
- **Evidence:** The repository already owns candidate/environment/evidence lane identities, archive operation gates, matched source staging, privacy, cleanup, and provider-free replay. A second store or harness would duplicate those responsibilities.
- **Outcome:** Selected as `extend`.
- **Reason:** Reviewed semantic records plus fact-only validation meet the outcome with less drift and lifecycle cost.
- **Do-Not-Repeat Condition:** Do not create a second claim database, weighted confidence score, semantic classifier, prompt-ranking helper, or standalone evaluation framework while existing owners remain sufficient.
- **Evidence-Based Retry Condition:** Build a new owner only if current schema/runtime proof demonstrates an unresolvable responsibility or boundedness conflict and the proposal is updated with that evidence before mutation.

## 2026-08-23 - Require Independent Challenge Only For Broad Claims

- **Objective:** Catch evidence that is internally consistent but does not entail the original broad outcome without making every local fix enter review ceremony.
- **Approach:** Add one fresh read-only evidence-sufficiency role and require its evidence only for finite-population, partitioned-domain, real-system equivalence, compatibility/interchangeability, safety, and phase/milestone claims.
- **Evidence:** Existing test-coverage and final reviewers focus on declared requirements or optional candidate risk; critical SDET owns independent critical test evidence. None has the distinct responsibility to challenge whether heterogeneous evidence supports the claim itself.
- **Outcome:** Selected.
- **Reason:** The limited trigger addresses the recurrence while preserving Ordinary Small exact-case speed.
- **Do-Not-Repeat Condition:** Do not turn the reviewer into a test author, approver, lifecycle authority, user-question route, generic final reviewer, or mandatory gate for exact-case Ordinary Small work.
- **Evidence-Based Retry Condition:** Merge or remove the role only if matched loaded-session evidence shows an existing reviewer can own the same entailment contract with lower total cost and no trigger ambiguity.

## 2026-08-23 - Serialize Completion Runtime Ownership

- **Objective:** Avoid invalidating or overwriting active completion-runtime candidates while planning a semantics change.
- **Approach:** Publish this change with `mutationEnabled: false` and explicit planning dependencies on the active mission-runtime and completion-hot-path changes.
- **Evidence:** The current worktree contains active proof/runtime changes and large retained evidence sets; one change explicitly excludes verdict semantics while another relies on the current guard behavior.
- **Outcome:** Selected.
- **Reason:** Planning can complete independently, but production and proof mutation must begin from one current transferred owner and candidate identity.
- **Do-Not-Repeat Condition:** Do not edit overlapping guard, arbiter, proof, contract, or validation paths merely because this proposal is apply-ready.
- **Evidence-Based Retry Condition:** Enable mutation only after both dependencies archive or publish an explicit acyclic transfer and current writer/liveness inventory is terminal-clear.

## 2026-08-23 - Explicit Baseline Does Not Reproduce The Long-Context Failure

- **Objective:** Determine whether the current loaded workflow over-promotes a finite-population claim when the path and observation mismatch is stated directly.
- **Approach:** Run one bounded read-only synthetic decision through the installed `build` agent with a 12-member finite population, complete offline path-A results, one real path-B result, and the original all-members path-B outcome stated explicitly.
- **Evidence:** The session returned `phaseStatus=incomplete`, supported only path-A population evidence and one representative path-B member, blocked path-B safety for all 12 and phase completion, named the 11 missing real observations and A/B equivalence, made zero tool calls, and was deleted successfully. The working-tree governed source digest was `ff6f687715c05ef3684a8b82aa9271efeac2c548dcdeaf5c934dec029aa91882`; the provider-free maintained gate separately reported `blocked` because its preserved evaluator is stale.
- **Outcome:** The compact explicit baseline does not reproduce the failure. Retain the change as structural closure/no-regression work rather than claim a prompt-level improvement from this sample.
- **Reason:** The motivating failure occurred through accepted requirement composition and lifecycle promotion, not because the direct claim-ceiling rule was wholly absent. Removing the change would leave archive, evidence-index, and completion-arbiter paths unable to prove that heterogeneous evidence entails the original outcome.
- **Do-Not-Repeat Condition:** Do not tune wording or rerun the same explicit baseline seeking a failing answer, and do not represent a correct baseline response as candidate improvement.
- **Evidence-Based Retry Condition:** Repeat only as the matched focused pack against a changed loaded candidate, with the same reviewed facts/model/environment and a `no-regression` expectation, or when new evidence identifies a distinct implicit-composition scenario.

## 2026-08-23 - Narrow Root-Wide Blocking To The Actual Owner Path

- **Objective:** Continue safe implementation without racing the active completion-runtime writer.
- **Approach:** Reconcile the declared ownership graph, remove completion-runtime paths from this change's active write set, enable non-overlapping claim/OpenSpec/instruction/reviewer/proof roots, and defer only the completion slice until archive or transfer.
- **Evidence:** The apply gate passed. Ownership inventory reported one overlap for this change: `global/plugins` with `bound-completion-runtime-hot-paths`; the declared dependency makes it `unresolved=false`. `add-autonomous-roadmap-mission-runtime` has no published overlapping owner. Treating both active changes as a root-wide implementation block prevented safe independent work without additional protection.
- **Outcome:** Selected. Tasks 2, 3, and 5 may proceed; task 4 remains dependency-blocked.
- **Reason:** The global recovery contract requires a path-only blocker to stay path-scoped while independent safe work advances the accepted outcome.
- **Do-Not-Repeat Condition:** Do not ask the owner to choose a process order when inventory proves a non-overlapping safe route, and do not re-add completion-runtime roots before exact transfer.
- **Evidence-Based Retry Condition:** Add and mutate completion-runtime roots only after fresh ownership/liveness evidence proves archive or explicit transfer from both dependent changes.

## 2026-08-23 - Owner Prioritizes Full Claim Closure Before Other Changes

- **Objective:** Implement the accepted change completely, including completion-runtime semantics, before returning to other active changes.
- **Approach:** Reconcile process/session liveness, planning-pause both dependencies without reverting their bytes, transfer `global/plugins`, and restore the full completion-runtime write set to this change.
- **Evidence:** Owner explicitly selected this delivery order. A first process scan matched only its own PowerShell diagnostic wrappers; the causally corrected scan excluded shells and found zero associated processes. Session inventory showed one persisted 2026-08-18 apply session but no matching live process. Ownership manifests and histories preserve the paused candidates and transfer.
- **Outcome:** Selected. `enforce-claim-evidence-closure` is the sole current mutation owner for the full accepted scope.
- **Reason:** This satisfies the owner's delivery priority while closing concurrent-writer risk and preserving unrelated work.
- **Do-Not-Repeat Condition:** Do not delete historical sessions/evidence, revert dependency bytes, or resume either paused change before current handoff.
- **Evidence-Based Retry Condition:** Reopen another change only after current terminal handoff/archive and fresh ownership/candidate invalidation review.

## 2026-08-23 - Extend Existing Qualification And Reviewer Owners

- **Objective:** Add substitution qualification and evidence challenge without creating a second lifecycle, reviewer framework, or loader harness.
- **Approach:** Reuse the `change-ready-sdlc` same-boundary comparison contract, the bounded evidence index, the shared leaf-reviewer contract, runtime-surface profiles, and the existing disposable loader proof; add only one focused skill and one focused reviewer.
- **Evidence:** Repository search found substitution baseline/effect/cleanup guidance in `change-ready-sdlc`, generic reviewer permissions/output binding in `instructions/leaf-reviewer-agent-contract.md` and agent validators, and config-source materialization/discovery in `tools/proofs/runtime-surface-loader.ts`. No existing skill owns the full skip/omit/cache/replay/emulation/replacement closure trigger, and existing reviewers do not compare the original broad outcome to the maximum entailed claim.
- **Outcome:** `extend`; no dependency. Cross-project discovery is `not-applicable` because the selected contracts are kit-owned model-facing policy artifacts rather than reusable product implementations.
- **Reason:** Extending current owners minimizes authority drift, install surface, proof cost, and context while preserving the distinct trigger and independent role required by the accepted spec.
- **Do-Not-Repeat Condition:** Do not add another evidence database, lifecycle, generic reviewer, loader harness, or model-scoring helper.
- **Evidence-Based Retry Condition:** Reconsider only if current validators or loaded proof show the focused skill/reviewer cannot be discovered, permission-bounded, or referenced without duplicating an existing complete owner.

## 2026-08-23 - Clear The Completion Capture Gate Offline And Keep Claim Parsing Out Of The Copied Plugin

- **Objective:** Reach the terminal provider-free guard verdict before one installed claim capture and add structured closure without breaking the self-contained session-delivery plugin.
- **Approach:** Replay the current guard and copied-plugin suites first; keep claim projection types and transport in `session-delivery-context`, extract bounded OpenSpec claim-file parsing into the completion-guard owner, and extend the existing installed restart runner for the four task 4.4 decisions rather than create another server harness.
- **Evidence:** `node tools/test-session-completion-guard.ts` reached `OK: session completion guard tests=38`; `node tools/test-session-env-plugin.ts` reached `OK: session env plugin tests=17`. The corrected tests cover stale, malformed, oversized, and claim-limit omissions, representative-overclaim continuation, supported exact stop, and claim-bound terminal certificates. The exact missing task 4.4 observation is installed request/result/root-state/provider-bound/cleanup evidence for those four decisions.
- **Outcome:** The completion-runtime live-attempt gate is clear for one bounded local effect-free installed capture. `split-or-justify`: the mixed projection file receives schema/transport fields only; the claim reader is cohesive in the guard directory; controller changes stay at its existing arbiter/certificate integration seam; the existing restart runner owns installed server identity and cleanup and is extended instead of duplicated.
- **Reason:** The first implementation imported OpenSpec parser code from outside the copied plugin surface and failed its self-contained loader test. Moving only project-file parsing to the guard preserves one canonical claim parser and the copied plugin contract.
- **Do-Not-Repeat Condition:** Do not reintroduce a `session-delivery-context` runtime import on `global/bin`, do not start a configured external provider for task 4.4, and do not treat the installed capture as matched model-behavior proof for task 5.3.
- **Evidence-Based Retry Condition:** Repeat the installed claim capture only after its preserved bundle is replayed to a terminal evaluator result and a causally distinct candidate or exact missing observation is recorded.

## 2026-08-23 - Replace Repeated SDK Readiness With The Proven HTTP-First Mechanism

- **Objective:** Reach the installed claim guard after capture r1 stopped before product execution.
- **Approach:** Preserve r1 manually from the raw command result, add provider-free failure evaluation to the maintained runner, replay it terminally, and change only the claims lane from repeated abortable SDK `session.status` probes to the existing proven HTTP-first `/path` then `/session/status` readiness mechanism.
- **Evidence:** `evidence/task-4-4-guard-claims-r1/{failure,evaluation}.json` records `blocked-before-guard`, zero primary/arbiter/external calls, server-listen plus configured-plugin-init timeout, complete process/fixture cleanup, and a blocked live-attempt gate. The paused mission history independently records this SDK readiness failure shape and a later green HTTP-first mechanism.
- **Outcome:** r1 remains final and non-reusable. The exact retry condition is met for one create-new r2 bounded local capture using the causally distinct HTTP-first observer; no claim behavior is inferred from r1.
- **Reason:** Repeating the same SDK readiness loop or increasing timeouts would not change the known failure mechanism. HTTP-first avoids accumulating aborted SDK bootstrap requests while preserving the same installed plugin, isolated config, provider bounds, and cleanup envelope.
- **Do-Not-Repeat Condition:** Do not rerun r1, increase readiness bounds, or treat listener readiness alone as guard readiness.
- **Evidence-Based Retry Condition:** If r2 fails before guard execution, replay its complete preserved bundle and select another causally distinct mechanism; do not repeat HTTP-first unchanged.

## 2026-08-23 - Installed Claim Capture Remains Blocked After Module Import Bisect

- **Objective:** Classify the r2 HTTP-first failure without a third installed attempt.
- **Approach:** Terminally evaluate r2, obtain one read-only troubleshooter diagnosis, then run its bounded provider-free direct ESM import bisect over `node:path`, the exact PTY bridge, and the exact completion-guard wrapper.
- **Evidence:** `evidence/task-4-4-guard-claims-r2/{failure,evaluation}.json` is `blocked-before-guard` with zero provider calls and complete cleanup. `evidence/task-4-4-module-import-r1.json` records successful bridge and guard imports with exact source digests. No correlated process or fixture remained after r2.
- **Outcome:** The module-import hypothesis is falsified. The blocker is narrowed to the Proof Runner/Environment path inside OpenCode plugin factory, project bootstrap, or isolated runtime readiness. Task 4.4 remains incomplete and the installed live-attempt gate remains blocked; tasks 4.1-4.3 retain provider-free proof.
- **Reason:** Both SDK and HTTP-first installed starts failed before guard execution, while exact module imports pass. Another server attempt would require a new decision-changing mechanism and complete preserved replay, not a timeout or observer variation.
- **Do-Not-Repeat Condition:** Do not start another installed OpenCode claim capture, rerun r1/r2, increase startup bounds, or call the direct import result installed behavior proof.
- **Evidence-Based Retry Condition:** A later attempt requires new evidence that identifies and corrects the OpenCode factory/bootstrap/environment cause or proves a genuinely different installed entry point can preserve request/result/root-state/cleanup observations.

## 2026-08-23 - Standalone Factories Pass; Installed Hook Application Remains Unknown

- **Objective:** Narrow the r1/r2 installed readiness blocker without starting another OpenCode process.
- **Approach:** Extend the provider-free guard suite with a Bun subprocess that invokes the exact PTY bridge and completion-guard factories using a bounded plugin input, verifies both return hooks, and disposes the guard before deferred reconciliation.
- **Evidence:** `evidence/task-4-4-factory-preflight-r1.json` records both factory returns, zero provider/external effects, and `OK: session completion guard tests=39`. The first fake input lacked OpenCode's private transport and failed immediately, so the corrected preflight supplied only the bounded transport shape required to reach factory setup; no network call was made.
- **Outcome:** Standalone module import and standalone factory setup are falsified as the direct hang layer. The remaining unknown is OpenCode's installed hook application/project bootstrap/isolated-environment interaction after factory return. Task 4.4 and the live-attempt gate remain blocked.
- **Reason:** The preflight advances the failure boundary without repeating SDK readiness, HTTP-first readiness, or an installed process start, while preserving the claim ceiling.
- **Do-Not-Repeat Condition:** Do not treat fake plugin input as installed proof, do not rerun r1/r2, and do not start another installed capture without a bounded observation at the remaining hook-application/bootstrap/environment boundary.
- **Evidence-Based Retry Condition:** Another installed attempt requires instrumentation or an alternate installed entry point that distinguishes hook application, project bootstrap, and isolated-environment readiness and preserves its raw diagnostics plus cleanup.

## 2026-08-23 - Focused Decision Pack Reuses The Existing Consumer Harness

- **Objective:** Add the four generic claim decisions without changing the maintained general scenarios or baseline pointer.
- **Approach:** Add one fixed focused pack and fixture set, generalize only explicit sample-count/pair-order/request-bound handling, retain existing capture/environment/safety/cleanup/evaluator owners, and add exact decision-oracle evaluation plus provider-free replay.
- **Evidence:** `evidence/task-5-1-2-focused-harness-r2/{bundle,evaluation,replay}.json` records eight passing baseline/candidate oracle rows, `passed-no-regression`, zero configured-provider calls, current source/scenario/evaluator identities, no baseline-pointer mutation, and complete cleanup. `node tools/test-consumer-outcome.ts` passes 15 tests. r1 is preserved but superseded by the TypeScript-only fixture correction. The read-only reduction review in `evidence/task-5-1-2-code-quality-r1.json` found no qualifying deletion.
- **Outcome:** Tasks 5.1 and 5.2 are complete at the provider-free local-harness boundary. `split-or-justify`: `contracts.ts`, `capture.ts`, and `evaluate.ts` remain cohesive owners despite attention-line status; the large guard/doctor test files receive only related oracles at existing seams. No new dependency, baseline, runner, or generic abstraction was added.
- **Reason:** A separate pack preserves the established general baseline and lets configured task 5.3 reuse the same source/environment/capture/replay boundary without inferring expected decisions.
- **Do-Not-Repeat Condition:** Do not promote the focused pack to the maintained baseline, derive apply/proof answers from evaluator code, raise sample count or provider bounds, or call local-harness results matched model behavior.
- **Evidence-Based Retry Condition:** Change the pack only when a provider-free oracle exposes an ambiguity or a later matched configured capture proves one reviewed scenario cannot be observed through the current exact decision file contract.

## 2026-08-23 - Instrument The Exact Installed Startup Gap Before One Evidence Capture

- **Objective:** Capture the missing installed observation that r1/r2 could not distinguish, without treating the next run as claim proof.
- **Approach:** For the claims scenario only, generate bounded wrappers around the exact PTY bridge and completion-guard plugins. Record factory enter/exit/error and hook enter/exit/error names to a proof-owned JSONL trace; include the bounded trace in startup failure or success evidence. Run a provider-free wrapper/factory/dispose self-check before starting OpenCode.
- **Evidence:** Direct imports and standalone factories already pass. Source inspection narrows the next unknown to OpenCode's application of returned hooks, project bootstrap, or isolated environment interaction. The wrappers record stage names only, never arguments, payloads, paths, or errors, and other restart scenarios keep their original plugin paths.
- **Outcome:** The exact missing raw observation is identified and instrumented. The live-attempt gate is clear for one create-new r3 bounded local evidence capture after its provider-free wrapper self-check; the capture remains evidence-only until its terminal evaluator and cleanup complete.
- **Reason:** This is causally distinct from SDK readiness, HTTP-first readiness, timeout changes, and standalone imports. It can separate factory return, config-hook application, and post-hook bootstrap without widening provider or external effects.
- **Do-Not-Repeat Condition:** Do not rerun r1/r2, omit the trace, print hook arguments/errors, raise timeouts, or call listener/factory readiness claim behavior.
- **Evidence-Based Retry Condition:** If r3 fails, terminally evaluate and preserve it; another attempt requires the trace to identify a corrected layer or a different installed entry point.

## 2026-08-23 - r3 Clears Startup And Finds The Exact Provider Bound

- **Objective:** Use the instrumented evidence capture to distinguish startup from downstream claim execution.
- **Approach:** Run one create-new r3 claims capture after the wrapper self-check, preserve its trace/provider/cleanup facts, then extend failure evaluation for a reached-guard provider-bound result before any successor attempt.
- **Evidence:** `evidence/task-4-4-guard-claims-r3/failure.json` records both factories and both `config` hooks entering/exiting, `claims-server-ready`, four arbiter and eight primary local-simulator calls, no stderr error, and complete process/fixture cleanup. Its terminal `evaluation.json` is `blocked-after-guard`. The request order shows four initial primaries plus two calls for each of the two continued roots, so the exact total is twelve rather than the stale six-primary/ten-total assumption.
- **Outcome:** The installed startup blocker is cleared. r3 remains final and non-reusable. The stale proof-runner bound is corrected to eight primary/twelve total; successor failures preserve case evidence. Trace output is narrowed to factory plus `config`/`dispose` to avoid routine event noise. The live-attempt gate is clear for one create-new r4 capture after focused provider-free checks.
- **Reason:** This is a current proof-runner defect, not product behavior or scope expansion; the observed bounded continuation path determines the smallest sufficient limit.
- **Do-Not-Repeat Condition:** Do not overwrite r3, restore the six/ten bound, infer four decision oracles from provider counts alone, or retain per-event trace noise.
- **Evidence-Based Retry Condition:** r4 is allowed only with the corrected exact bound and case-preserving failure record; any further attempt requires r4 terminal replay and new decision-changing evidence.

## 2026-08-23 - r4 Returns To Pre-Factory Readiness; Installed Gate Blocks

- **Objective:** Capture the four installed decisions with the corrected exact provider bound and case-preserving failure record.
- **Approach:** Re-run the same instrumented claims path once as r4 after r3 terminal replay and focused provider-free checks, then immediately replay its create-new failure bundle.
- **Evidence:** `evidence/task-4-4-guard-claims-r4/{failure,evaluation}.json` is `blocked-before-guard`, with zero primary/arbiter calls, empty installed plugin trace, the same 2487 stderr/119 stdout sizes, and complete cleanup. The wrapper self-check passed before server start, while no installed `factory-enter` marker appeared. r3 remains the only reached-guard capture but did not preserve its completed case records before the stale bound assertion.
- **Outcome:** The corrected provider bound is unproven at the installed boundary. The remaining nondeterminism is narrowed before factory entry to OpenCode's installed plugin resolution/import environment; task 4.4 remains incomplete. The live-attempt gate is blocked and tasks 5.3/5.4 cannot start.
- **Reason:** r4 used the exact r3 mechanism plus the justified bound correction yet regressed to the earlier readiness shape. Repetition cannot establish claim behavior and would violate the causal-retry rule.
- **Do-Not-Repeat Condition:** Do not run r5, raise readiness bounds, remove r3/r4, infer decision success from r3 provider counts, or treat provider-free/local-harness results as installed/model proof.
- **Evidence-Based Retry Condition:** Another installed attempt requires decision-changing evidence from OpenCode's plugin resolver/importer before factory entry or a genuinely different installed entry point that preserves request/result/root-state/provider/cleanup facts.

## 2026-08-23 - Isolated Debug Config Warms The Plugin Resolver Before Serve

- **Objective:** Obtain decision-changing plugin-resolver evidence without another `opencode serve` capture, then unlock one causally distinct installed claims attempt.
- **Approach:** Inspect OpenCode 1.18.18 `Plugin.init`/`Config.waitForDependencies`/`Npm.install` source; run `opencode debug config` and `debug startup` against the same isolated claims-like config; then add a same-env `debug config` warmup before `serve` so the isolated Bun cache is populated before HTTP-first readiness.
- **Evidence:** `evidence/task-4-4-plugin-resolver-r1.json` records OpenCode `1.18.18`, seeded `@opencode-ai/plugin@1.18.15` via a junction, `debug config` status 0 in 20651ms with both file plugin origins, `debug startup` status 0 in 986ms, isolated cache entries `bun` and `opencode`, zero provider calls, no global plugin mutation, and complete cleanup. r1/r2/r4 share empty factory traces after cold isolated starts.
- **Outcome:** The live-attempt gate is clear for one create-new r5 claims capture that must run `opencode debug config` in the same isolated env before `serve`. This does not establish claim behavior.
- **Reason:** Serve was racing a cold isolated plugin import/`waitForDependencies` path. Debug config is a different installed entry that completes resolver work and warms the isolated cache; repeating HTTP-first serve alone would not change that cause.
- **Do-Not-Repeat Condition:** Do not rerun r1-r4, start serve without the same-env debug-config warmup, treat debug config as guard-decision proof, or raise readiness bounds.
- **Evidence-Based Retry Condition:** r5 is allowed only with the recorded warmup; any further installed attempt requires r5 terminal replay plus a new resolver or entry-point cause.

## 2026-08-23 - Installed Claim Capture r5 Completes The Four Guard Decisions

- **Objective:** Prove representative-overclaim continuation, supported exact stop, truncated closure, and Ordinary Small idle behavior at the installed guard boundary.
- **Approach:** Warm the isolated Bun cache with `opencode debug config` in the same env, then run one HTTP-first `opencode serve` claims capture with the corrected provider bound.
- **Evidence:** `evidence/task-4-4-guard-claims-r5/{raw,evaluation}.json` records OpenCode 1.18.18, warmup 14137ms, factory-enter/exit for both plugins, verdicts continue/allow_stop/continue/allow_stop, arbiter 4, primary 8, external 0, and complete cleanup.
- **Outcome:** Task 4.4 is complete at the installed disposable guard boundary.
- **Reason:** The debug-config warmup changed the isolated plugin-import environment that blocked r1/r2/r4.
- **Do-Not-Repeat Condition:** Do not rerun r5, omit the warmup, or treat r3 provider counts as the four decisions.
- **Evidence-Based Retry Condition:** Repeat installed claims capture only after a new product or runner mutation that changes those four decisions.

## 2026-08-23 - Configured Decision Pack Writes Invalid Enum Tokens

- **Objective:** Observe the four loaded claim decisions through `opencode run --pure`.
- **Approach:** One matched configured capture of the focused pack, then provider-free replay of the preserved bundle.
- **Evidence:** `evidence/task-5-3-configured-r1/{bundle,evaluation,replay}.json` records eight live calls, cleanup complete, and all eight oracles failed. `check-decision.ts` rejected written `decision.json` files for invalid `completionDisposition` or `claimDisposition`. Replay reproduced the same failed digest with zero live calls. Command stdout was sliced to 4000 characters, so the written enum values were not retained.
- **Outcome:** r1 is final. Task 5.3 remains incomplete. The live-attempt gate is clear for one create-new r2 after naming allowed tokens in the reviewed request and preserving observed values on proof failure.
- **Reason:** The request named the field keys but not the allowed tokens, so the loaded session wrote a file the oracle could not classify. Repeating the same request cannot distinguish vocabulary failure from a wrong claim ceiling.
- **Do-Not-Repeat Condition:** Do not rerun r1, raise the provider bound, or infer the written enum from hashes.
- **Evidence-Based Retry Condition:** r2 is allowed only with explicit allowed-token request text and proof errors that include the observed values.

## 2026-08-23 - Configured r3 Passes All Four Decision Oracles

- **Objective:** Observe the four loaded claim decisions with spec-aligned representative `narrowed` and continue-when-unsupported request text.
- **Approach:** Refresh the provider-free harness after the reviewed seed update, then run one create-new configured capture and replay it.
- **Evidence:** `evidence/task-5-3-configured-r3/{bundle,evaluation}.json` and replay show 8/8 decision oracles passed, cleanup complete, no forbidden effects, eight live calls. Evaluator status is `failed` only for `friction-regression:unavailable-real-oracle:failedToolCallCount:1:2`. Harness r3 is `passed-no-regression`.
- **Outcome:** Task 5.3 is complete on the task retain criteria. Friction-only fail is a Known Non-Critical Limitation. No productivity or universal-model claim is made.
- **Reason:** The accepted retain gate is the four decisions plus safety/outcome/cleanup, not the general friction matrix.
- **Do-Not-Repeat Condition:** Do not rerun r3, raise sample count, or treat the friction row as decision failure or improvement.
- **Evidence-Based Retry Condition:** Repeat configured capture only after a product mutation that changes those four decisions.

## 2026-08-23 - Independent Challenge Via Fresh Loaded Process

- **Objective:** Complete task 5.4 independent challenge without impersonating `evidence-sufficiency-reviewer`.
- **Approach:** Keep the already-complete instruction-artifact-reviewer r1. After the host Task adapter rejected the new role, start a fresh `opencode run` against the current kit global source so the child session could load `evidence-sufficiency-reviewer`, then bind the report and accept narrowed claim ceilings.
- **Evidence:** `evidence/task-5-4-reviewers-r1.json` remains the blocked adapter attempt. Fresh debug/agent listing saw the role. Dispatch session `ses_fd06e25ecffeLwVo0oOx4JXwrg` created child `ses_fd06db598ffefi7CObDzXnP7Aw` as `evidence-sufficiency-reviewer` with Effective Model `xai/grok-4.6`. Report: `evidence/task-5-4-esr-r2.md`. Main disposition: `evidence/task-5-4-reviewers-r2.json`.
- **Outcome:** Independent challenge is complete. Broad claims stay inside their recorded maxima as accepted narrowing. CEC-EXACT remains supported. No production mutation.
- **Reason:** Repeating the in-session Task adapter cannot discover a role loaded after that process started. A new process with the current config is a distinct dispatch path and preserves role identity.
- **Do-Not-Repeat Condition:** Do not impersonate the reviewer, rerun r1, or treat host Task rejection as missing product behavior.
- **Evidence-Based Retry Condition:** Repeat the challenge only after a product or claim-record mutation that changes the original outcome or the four claim ceilings.
