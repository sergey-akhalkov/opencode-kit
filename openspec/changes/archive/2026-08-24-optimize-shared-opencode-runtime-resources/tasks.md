## 1. Baseline And Integration Authority

- [x] 1.1 Reconcile the active workstation candidate and capture the pre-change resource/behavior baseline before any production mutation. Read `fix-workstation-restart-reliability/history.md` and its current tasks/evidence, verify no mutation-capable attempt is still live, preserve its current controller/tray changes as the integration base, and record any satisfied retry condition before reusing a prior strategy. Add the smallest reusable capture/evaluator under `tools/proofs/` and its inventory entry, then run identical provider-free disposable idle, one-project, and two-project local-Graphify workloads plus a read-only current-host process/config snapshot. **Current Rung:** planning from source, installed help, and point-in-time live process observations. **Next Real Boundary:** installed OpenCode/Graphify binaries with the current local MCP configuration in disposable loopback/project state; no provider or model request. **Blocker/Unblocker:** unknown writer liveness or an uncleared overlapping live-attempt gate blocks production edits until the active change evidence closes it; existing owner windows do not block the disposable baseline and MUST NOT be closed. **Observable Proof:** versioned raw/evaluation bundles identify two local Graphify service trees for two projects, one Codebase Memory internal daemon, project-scoped Serena/LSP sets, retained launch chains when observed, exact tool inventory/results, repeated timing/private-byte/working-set samples, exits, and complete proof-owned cleanup. **Authorization/Safeguards:** read-only current-host inspection plus disposable local processes/files only; fixed graph is read-only; no ProgramData/global-config/target-repo write, provider call, remote PR call, or owner-process termination. **Restoration/Cleanup:** stop only proof-owned servers/MCPs/clients and remove disposable state; preserve owner clients and the managed runtime. **Immutable Evidence:** source/installed/environment hashes and versions, exact redacted commands, process/listener trees, samples, Graphify tool/result digest, config-source hash, exits/stdout/stderr, and cleanup inventory. **Validation:** proof runner `--help`, provider-free baseline capture, evaluator replay, `node --check` for new proof source, and current installed Status.

## 2. Disposable Shared-Service Happy Path

- [x] 2.1 Implement the cohesive shared-tool module and effect-free configuration contract without expanding the workstation controller's responsibilities. Add strict Graphify executable/module/graph/endpoint schema validation; exact local-entry recognition; minimal `jsonc-parser` edit planning; original/managed byte and ACL identities; protected credential/state projections; process/listener identity; authenticated/unauthenticated MCP probes; and cause-preserving redaction. Extend the repository machine configuration with the fixed graph, Python identity, and loopback port, and add the narrow `graphify-global` repo-argument tool hook without changing other MCP tools. **Current Rung:** complete baseline from 1.1 and installed source contracts for Graphify HTTP, OpenCode remote MCP, Serena active-project ownership, and Codebase Memory daemon behavior. **Next Real Boundary:** repository help/preflight plus a temp JSON/JSONC config transformation and readback using the actual module. **Blocker/Unblocker:** 1.1; a divergent/missing Graphify local entry or occupied `4097` is a reported preflight collision, not authority to rewrite/kill it. **Observable Proof:** preflight emits one secret-free plan with exact source/config/graph/tool/process identities; the temp edit replaces only `mcp.graphify-global`, preserves every unrelated byte, is schema-valid, and exact rollback restores the original bytes; missing/wrong/duplicate fields and listeners fail before effects. **Authorization/Safeguards:** repository production/proof edits and disposable temp files only; no host config, ProgramData, task, process, endpoint, credential, or target-repo mutation. **Restoration/Cleanup:** delete transformation fixtures and leave the managed runtime/config unchanged. **Immutable Evidence:** candidate/module hashes, before/after/diff/rollback digests without config contents, schema diagnostics, exits/stdout/stderr, and cleanup. **Validation:** controller/module `--help` or public contract probe, `node --check`, configuration validator, strict negative matrix, and existing focused workstation/config checks.

- [x] 2.2 Implement and run the first real shared-Graphify integration in a disposable environment. Start one authenticated stateless Graphify service from the new module, create an isolated OpenCode configuration whose same-named `graphify-global` entry is remote, start one installed OpenCode server, and connect two distinct disposable project instances to that server without prompts or provider calls. Exercise MCP initialize/list-tools and representative fixed-graph queries from both directories; exercise missing explicit `repo` rejection before any PR/remote work; observe one Graphify tree, one OpenCode listener, and no local Graphify child per project. **Current Rung:** effect-free source/config boundary from 2.1. **Next Real Boundary:** installed Graphify Streamable HTTP and OpenCode remote MCP loader with two real project contexts. **Blocker/Unblocker:** 2.1; unavailable HTTP support, config env substitution, tool-prefix preservation, or secret-safe diagnostics blocks host installation until corrected locally. **Observable Proof:** unauthenticated Graphify returns `401`; authenticated tool inventory and representative result digests match the baseline for both projects; explicit supported arguments pass unchanged; two projects create one Graphify service tree; no secret appears; readiness remains within the specified allowance. This is an integration signal, not workstation MVP. **Authorization/Safeguards:** disposable loopback ports/state, generated proof-only credential, fixed graph read-only, no provider/model/remote PR call, no global config/ProgramData/target-repo write. **Restoration/Cleanup:** stop all attributed OpenCode/Graphify/MCP processes, delete credentials/temp directories, and prove ports/fixtures absent. **Immutable Evidence:** candidate/environment identity, config projection, redacted invocations, endpoint statuses, tool inventory/result digests, process/listener samples, timing, exits/stdout/stderr/log tails, and cleanup. **Validation:** provider-free capture plus evaluator replay and applicable source/static checks.

## 3. Workstation Candidate And Runtime Proof

- [x] 3.1 Integrate the shared-tool module and launch handoff into the reconciled workstation controller. Extend manifest/state/install/preflight/serve/status/start/restart/stop/exit/rollback and derived tray behavior for one Graphify sibling/credential/listener and composite healthy/degraded state; start Graphify before OpenCode; preserve OpenCode after a post-readiness Graphify exit; reject new launches while degraded; and include exact config backup/managed identities. For project `launch` only, wait on the direct elevated controller with `Process.WaitForExit()` rather than PowerShell `Start-Process -Wait`, while preserving the invoker's exit code and popup. **Current Rung:** green disposable remote-MCP integration from 2.2 and current integrated Restart source from 1.1. **Next Real Boundary:** actual source controller help/preflight/status and disposable Windows process-handoff probes before protected installation. **Blocker/Unblocker:** 2.2 and the integrated active Restart candidate; any unresolved overlapping writer or identity conflict stops this slice. **Observable Proof:** source preflight reports both endpoints and all protected changes without effects; direct-controller success releases only launch wrappers while the detached fixture remains; post-elevation failure returns non-zero and the popup contract is observable; Start/Restart/Stop semantics outside launch remain synchronous; no secret enters argv/state/logs. **Authorization/Safeguards:** repository edits and disposable local process fixtures only; no ProgramData/global config/task/owner process mutation yet. **Restoration/Cleanup:** terminate only fixture identities and remove temp state. **Immutable Evidence:** integrated diff/candidate hash, exact help/preflight output, process timelines, direct exit codes, popup observation, secret scans, exits/stdout/stderr, and cleanup. **Validation:** `node --check`, focused controller/config/process-handoff checks, `npm test` only where existing tests are applicable, and provider-free evaluator replay.

- [x] 3.2 Perform one stopped identity-safe workstation repair and prove the actual one-client happy path. Re-verify the effective elevated token; record current protected/controller/config/task/process/listener/ACL hashes; stop only the positively identified managed runtime when required; install and hash the controller plus shared-tool module; generate/protect the Graphify key; atomically apply the validated minimal `graphify-global` config edit; and Start through the actual managed entry point. Launch one actual project shortcut and observe the attached project, both shared services, remote Graphify tool readiness, and launch-chain closure. **Current Rung:** complete source candidate from 3.1. **Next Real Boundary:** protected ProgramData runtime, machine-local config loader, Scheduled Task, actual Graphify/OpenCode endpoints, and one Desktop project workflow. **Blocker/Unblocker:** 3.1; unknown managed identity, config drift, occupied port, missing elevation, or failed backup/rollback preflight blocks only installation until reconciled. **Observable Proof:** one authenticated Graphify service and one authenticated OpenCode listener become healthy; unauthenticated probes fail; the client uses its exact `--dir`, exposes all Graphify/Codebase Memory/Serena/LSP capabilities, and creates no local Graphify/server; within the bounded handoff no launch-owned `wscript`/Node/elevation PowerShell remains while Alacritty/pwsh/attach stay active; installed config contains only the env reference and preserves unrelated bytes. Green proof promotes this candidate to `Development-Stage: MVP`. **Authorization/Safeguards:** standing local admin, loopback only, one writer, stopped repair, exact identities/backups, no provider/model call, no target-repo content write, no owner-window termination. **Restoration/Cleanup:** close only the proof-owned client; on install/proof failure restore exact prior config/protected bytes and prior safe runtime or leave the exact stopped state documented; never kill ambiguous processes. **Immutable Evidence:** before/after hashes/ACLs/config edit identity, token/task/process/listener tree, endpoint/auth/tool/project observations, launch timeline, exits/stdout/stderr/log references, and cleanup/restoration. **Validation:** actual Start/Status/project `.lnk`, independent process/listener/config/task inventory, secret scan, and source-installed hash correlation.

## 4. Accepted Scope, Failure Boundaries, And Resource Result

- [x] 4.1 Prove two simultaneous actual projects and the complete hybrid ownership/resource contract. With a clean attributed workload, keep one proof-owned `pmac-emulator` client ready, launch proof-owned `opencode-kit` immediately, exercise representative Graphify queries from both, verify missing `repo` rejection and explicit repo pass-through without performing a remote PR operation, and inventory Codebase Memory/Serena/OpenCode LSP ownership. Close only the second client and prove first-client/shared-service survival, then clean the first. Evaluate the candidate against the preserved equivalent baseline. **Current Rung:** one-client installed MVP from 3.2. **Next Real Boundary:** two actual Desktop clients on the installed shared runtime and current workstation memory/process accounting. **Blocker/Unblocker:** 3.2; owner-existing windows are preserved and excluded, while an environment that cannot produce equivalent attributed workloads blocks only the resource verdict until a clean proof-owned lane is available. **Observable Proof:** one unchanged Graphify tree, one OpenCode listener, one Codebase Memory daemon, and separate Serena/LSP sets serve both exact directories; no hidden launcher chains or local Graphify duplicates remain; first client survives second cleanup; complete tool/result equivalence passes; median two-client Graphify private bytes improve by at least 35%, and readiness stays within the accepted allowance. **Authorization/Safeguards:** actual local GUI/loopback only, provider-free representative MCP calls, no remote PR call, no target-repo write, no server Restart, and termination limited to proof-owned client roots. **Restoration/Cleanup:** remove both proof clients and transient capture processes; leave one healthy managed runtime and owner windows unchanged. **Immutable Evidence:** paired baseline/candidate raw bundles, process/memory/timing samples, tool/result/project digests, listener/PID continuity, launcher absence, exits/logs, evaluator result, and cleanup. **Validation:** actual shortcut pair, evaluator and preserved-bundle replay, installed Status, independent inventory, and secret scan.

- [x] 4.2 Complete required degradation, collision, diagnostics, Restart, and rollback/reinstall behavior. Challenge wrong/missing Graphify auth, unrelated `4097` listener, managed Graphify post-readiness exit, new-launch rejection in degraded state, tray red state, actual tray and Desktop Restart replacement of both service identities, direct-launch post-UAC failure popup, config drift refusal, and exact identity-matched rollback/reinstall. Do not repeat a live failure path until its raw bundle has passed the complete offline evaluator/finalization replay required by the current gate. **Current Rung:** complete two-client scope and resource verdict from 4.1. **Next Real Boundary:** actual installed failure/recovery/rollback paths with bounded local effects. **Blocker/Unblocker:** 4.1; any unknown listener/process/config identity or blocked live-attempt lane stops its destructive action while safe offline/disposable cases continue. **Observable Proof:** no credential is sent to an unrelated listener; OpenCode/clients survive Graphify degradation; status/tray are red and a new launcher fails actionably; Restart removes only prior managed identities and restores both services/green; launch failure still pops the log pointer and leaves no chain; drift is preserved; rollback restores exact prior config/artifacts and reinstall recreates the candidate. **Authorization/Safeguards:** standing local admin, exact positive controls for absence sources, one live mutation at a time, bounded waits, no provider/model/remote/target-repo effect, recorded restoration before each mutation. **Restoration/Cleanup:** remove collision/fixture processes by exact identity, complete required live restoration, close only proof-owned clients, and finish with one explicitly reported healthy candidate or exact restored prior state. **Immutable Evidence:** per-scenario raw bundles, causal logs/stacks, identities/hashes/ACLs, endpoint/popup/tray observations, preserved replay results, restoration and cleanup. **Validation:** installed help/preflight/status/rollback dry-run, actual Restart/rollback/reinstall paths, evaluator replay, independent process/listener/config/task inventory, and secret scan.

- [x] 4.3 Update repository operator and proof documentation for the implemented contract. Document the hybrid sharing matrix, Graphify endpoint/auth/config source, explicit `repo` rule, Codebase Memory daemon/frontends, Serena/LSP project scope, degraded/recovery behavior, exact preflight/install/status/rollback commands, resource proof invocation and limits, and the fact that Node-to-Bun/runtime consolidation is not part of the optimization. **Current Rung:** current installed behavior and evidence from 4.2. **Next Real Boundary:** a cold operator follows effect-free help/preflight/status and the proof inventory against the installed candidate. **Blocker/Unblocker:** 4.2 supplies exact commands and observed limits; documentation never authorizes another live mutation. **Observable Proof:** every documented no-effect command exits `0` through its actual entry point and matches current help/config/artifact paths; no secret or stale process claim appears. **Authorization/Safeguards:** repository documentation edits and read-only installed commands only. **Restoration/Cleanup:** none beyond stopping transient inspection commands. **Immutable Evidence:** doc diff, command outputs/exits, and secret scan. **Validation:** documentation/link/strict validators plus actual `--help`/preflight/status readback.

## 5. Material Qualification And Handoff

- [x] 5.1 After current MVP proof and accepted-scope completion, dispatch one fresh test-only SDET against the exact candidate. Supply the original requirements, hybrid service boundary, baseline/candidate raw bundles, protected identity/auth/config invariants, launcher handoff, degradation/restart/rollback envelope, safe disposable runner, and current existing tests. Permit only the smallest automated critical reproducer/regression oracle and require `critical-risks-reported | no-critical-risk | blocked` with Effective Model; main independently reproduces and dispositions every row. **Current Rung:** accepted scope complete and current installed proof from 4.3. **Next Real Boundary:** fresh critical-risk challenge of authorization, wrong-project routing, process ownership, config restoration, and required-service outage behavior. **Blocker/Unblocker:** 4.3; an unavailable/unusable SDET leaves the Material candidate at MVP and never authorizes main to invent test evidence. **Observable Proof:** terminal SDET output is attributable to the candidate; any confirmed critical/non-deferrable defect is corrected by production, invalidates affected lanes, and receives fresh proof before any eligible successor SDET. **Authorization/Safeguards:** test-only writes; disposable/local effects only unless an exact already-authorized installed reproduction is necessary; no provider use outside bounded configured kit validation, no target-repo mutation, remote effect, release, or deployment. **Restoration/Cleanup:** close SDET fixtures/processes and preserve the installed candidate/owner state. **Immutable Evidence:** SDET report/model/session, authored test diff if any, main reproducer/disposition, candidate references, and cleanup. **Validation:** smallest SDET-authored test plus replay of all affected runtime/validation lanes after any correction.

- [x] 5.2 Run complete validation and produce the local handoff on one current candidate. Run controller/shared-module syntax and focused checks; proof capture/evaluator replays; existing tests; `npm run validate:strict`; `npm run openspec:validate`; strict target validation; final source/private-candidate identities; and a read-only installed config/task/ACL/process/listener/tool inventory. Record outcome/non-goals, Candidate Reference, RC history, runtime proof, baseline/candidate resource result, SDET terminal reason, known non-critical limitations, exact rollback state, and external-operation state. Freeze the next RC only when accepted scope is complete, candidate-applicable validation is green, and no known reachable critical/non-deferrable defect remains; promote that same RC to stable only with the complete local handoff. **Current Rung:** terminal main SDET disposition from 5.1 and current runtime proof from 4.2/4.3. **Next Real Boundary:** complete project-native validation plus final read-only current-host observation. **Blocker/Unblocker:** 5.1; red candidate validation or stale behavior/resource proof returns only dependent lanes to correction. A repository-wide aggregate failure attributable only to an unrelated active change is preserved and reported without mutating that change. Owner-prohibited ProgramData activation is recorded as an external operation, not inferred from source/private proof. **Observable Proof:** every candidate-applicable command exits `0`; source/private identities correlate; one healthy authenticated Graphify and OpenCode runtime remains untouched; hybrid process/tool ownership and resource threshold remain current; no secret, duplicate Graphify, hidden launcher chain, or proof orphan exists. **Authorization/Safeguards:** local validation/readback only, no commit/push/release/deployment/installation/remote call/target-repo mutation. **Restoration/Cleanup:** stop validation/proof processes, remove disposable state, preserve owner windows, and retain the requested final installed runtime. **Immutable Evidence:** exact commands/exits/stdout/stderr, candidate/environment identities, proof/evaluator/SDET references, final inventory, rollback eligibility and cleanup. **Validation:** the complete candidate-applicable named validation set, attributed aggregate result, and final diff/status inspection.
