## 1. Runtime-Source CLI Boundary

- [x] 1.1 Add effect-free `--help`/`-h`, reject unknown options before inventory, and preserve `--root` behavior in `tools/opencode-runtime-sources.ts`; invoke the actual CLI for help, invalid-option, and representative inventory paths before dependent doctor integration. **Current Rung:** source-inspected CLI with observed help fallthrough. **Next Real Boundary:** installed provider-free CLI invocation over a disposable project. **Blocker/Unblocker:** none. **Observable Proof:** help exits `0` with usage and no report, an unknown option exits `1` without a report, and `--root` still emits a privacy-safe report. **Owner Authorization:** local process execution and disposable files only. **Safeguards:** isolated roots; no provider, credential, install, activation, target-project mutation, or remote effect. **Restoration/Cleanup:** delete only proof-owned fixtures and preserve invocation/stdout/stderr/exits. **Expected Immutable Evidence:** focused CLI transcript and fixture identity. **Validation:** focused runtime-source CLI checks. **Dependency-Chain Stop Condition:** do not integrate this parser into doctor while help or invalid options can still trigger inventory.

## 2. Doctor Gate And Collision Integration

- [x] 2.1 Add `--require structural|qualification|unattended`, derive stable named blocker arrays from existing checks, align blocking wording, and preserve the no-selector report contract; invoke doctor against disposable pass, warning, qualification-blocked, and unattended-blocked projects. **Current Rung:** source-proven mismatch between top-level qualification status and structural exit. **Next Real Boundary:** installed doctor invocation over disposable project fixtures. **Blocker/Unblocker:** task 1.1 establishes the reusable CLI argument/help convention. **Observable Proof:** each selected gate's report, blocker list, and exit agree while the default report remains compatible. **Owner Authorization:** local disposable file/process effects only. **Safeguards:** do not execute project validation argv or mutate target projects. **Restoration/Cleanup:** remove proof-owned fixtures and preserve exact diagnostics. **Expected Immutable Evidence:** JSON/Markdown outputs and exits for every gate. **Validation:** focused doctor tests. **Dependency-Chain Stop Condition:** stop collision integration if blocker lists are independently recomputed or can disagree with top-level status.

- [x] 2.2 Compose the import-safe runtime-source inspector into doctor, classify only maintained canonical OpenSpec skill/command collisions as lifecycle blockers, and retain non-canonical additive sources as diagnostics; invoke doctor with controlled custom-global, host-default, and project layouts. **Current Rung:** separate source inventory and doctor outputs over the same consumer. **Next Real Boundary:** installed doctor invocation over a disposable canonical-collision layout. **Blocker/Unblocker:** tasks 1.1 and 2.1 provide the source parser and gate result owners. **Observable Proof:** unknown canonical precedence blocks qualification and unattended modes with both redacted locations, while additive instruction/config layers do not block by multiplicity alone. **Owner Authorization:** local read-only source inspection and disposable files. **Safeguards:** never print config contents, prompts, provider options, credentials, or infer an undocumented winner. **Restoration/Cleanup:** remove fixtures; preserve source hashes and reports. **Expected Immutable Evidence:** collision/no-collision report matrix. **Validation:** focused doctor/runtime-source integration tests. **Dependency-Chain Stop Condition:** stop if composition requires a second source model, secret-bearing reads, or a precedence claim unsupported by runtime evidence.

## 3. Integrated Runtime Proof And Documentation

- [x] 3.1 Update README/quality-gate guidance and run one maintained installed-CLI proof covering help, all three doctor gates, multiple blockers, canonical collision, additive layering, redaction, and cleanup. **Current Rung:** individually proven local CLI slices. **Next Real Boundary:** complete provider-free consumer bootstrap/diagnostic workflow in a disposable repository. **Blocker/Unblocker:** tasks 1.1-2.2 complete. **Observable Proof:** a cold operator can select the intended gate and automation receives a matching exit and complete blocker set without private output or project command execution. **Owner Authorization:** disposable local repository/process effects only. **Safeguards:** no model/provider call, install, activation, live target, or remote state. **Restoration/Cleanup:** deterministic fixture cleanup with failure made terminal. **Expected Immutable Evidence:** candidate/source identities, argv, exits, stdout/stderr, report facts, side-effect inventory, and cleanup. **Validation:** maintained proof plus focused tests. **Dependency-Chain Stop Condition:** any report/exit disagreement or leaked source content returns the candidate to development before broader validation.

## 4. Qualification

- [x] 4.1 After runtime proof and accepted-scope completion, dispatch one fresh test-only SDET to challenge critical fail-open qualification, secret disclosure, unintended project-command execution, and canonical-collision acceptance; main reproduces and dispositions every plausible critical row. **Current Rung:** frozen complete candidate with installed provider-free proof. **Next Real Boundary:** critical-only automated challenge of the same disposable CLI boundary. **Blocker/Unblocker:** task 3.1 green and no open mutator. **Observable Proof:** terminal `critical-risks-reported`, `no-critical-risk`, or exact `blocked` evidence with Effective Model; confirmed critical defects are corrected and re-proven before any eligible successor. **Owner Authorization:** local test-only files/processes. **Safeguards:** SDET edits no production, instructions, specs, or external state. **Restoration/Cleanup:** preserve accepted test evidence and delete only disposable non-evidence state. **Expected Immutable Evidence:** SDET matrix and main disposition. **Validation:** smallest critical regression oracle when justified. **Dependency-Chain Stop Condition:** the first precondition-valid attempt without a main-confirmed critical defect permanently stops SDET for this root.

- [x] 4.2 Run complete project-native validation, strict OpenSpec validation, candidate readback, and local handoff with proof identity, diagnostics, compatibility note for the default exit, limitations, rollback, external-operation state, and lifecycle stage. **Current Rung:** terminal critical-SDET candidate. **Next Real Boundary:** none in this local diagnostic change. **Blocker/Unblocker:** task 4.1 terminal and no known reachable critical/non-deferrable defect. **Observable Proof:** all applicable validation is green and the handoff names the exact stable explicit-gate contract without claiming loader precedence. **Owner Authorization:** local validation only. **Safeguards:** no install, activation, release, publication, or remote mutation. **Restoration/Cleanup:** preserve validation logs; no external state. **Expected Immutable Evidence:** validation transcript and Candidate Reference. **Validation:** `npm run validate:strict`, focused tests, full test suite, and `openspec validate make-doctor-qualification-automation-safe --strict`. **Dependency-Chain Stop Condition:** red validation or an unresolved non-deferrable risk prevents RC/stable claims.

## 5. Final History Retrospective

- [x] 5.1 After every other currently known task is complete, analyze the complete `history.md` once using the canonical six-cell `Quality`, `Cycle Speed`, and `Token Economy` matrix across `Working Repository` and `opencode-kit`; classify each evidence-backed candidate by Impact Horizon, Concrete Consumers, Execution Class, Earliest Safe Point, Invalidated Evidence, and Observable Payback; append and immediately execute every admitted exact-current-consumer improvement under `## Session-Derived Improvements`, preserve no-current-consumer candidates as non-checkbox deferred history records, or record `none` without inventing work. Apply, archive, compaction, and generated tasks MUST NOT create or rerun this final-history analysis.
