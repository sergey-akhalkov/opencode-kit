## 1. Proof Fixtures And Current-Source Baseline

- [x] 1.1 Extend `tools/proofs/pre-escalation-recovery.ts` and its proof inventory entry with fixed `broken-observer`, `qualified-absence`, and `straightforward-local-defect` scenarios while retaining the existing true `owner-only` control; add provider-free rejected/accepted oracle preflight, exact allowed diagnostic commands, claim-ceiling/tool/question/consultation facts, create-new evidence, and deterministic cleanup without changing loaded production authority. **Current Rung:** reviewed current runner, specs, and reproduced incident facts. **Next Real Boundary:** `npm run proof:pre-escalation-recovery -- --mode preflight --evidence-root <new-path> --capture-kind baseline --candidate-id <id>` over disposable fixtures with zero provider calls. **Blocker/Unblocker:** none; this proof-runner work must precede baseline capture and does not authorize instruction mutation. **Observable Proof:** each compliant fixture passes, each deliberately incomplete fixture fails, help exits `0` without effects, the combined broken-observer case exposes stale identity/wrong layer/failed positive control/contradictory direct evidence, and every fixture is removed. **Authorization/Safeguards:** local provider-free files and commands inside disposable roots only; fixed command allowlist; no target project, production/config mutation, question, credential, network, remote, or protected effect. **Restoration/Cleanup:** delete proof-owned fixtures and preserve only privacy-safe immutable evidence; cleanup failure is terminal. **Immutable Evidence:** exact redacted argv, source/runner/prompt hashes, scenario manifests, accepted/rejected oracle facts, provider-call count, exits/stdout/stderr, cleanup. **Validation:** runner syntax, `--help`, provider-free preflight, manifest readback, and `npm run test:focused:contracts` where the shared proof contract is touched.

- [x] 1.2 Extend the existing installed completion-guard proof owner `tools/proofs/session-completion-guard-autonomous.ts` with one bounded no-question technical-blocker scenario and a replayable evaluator that can observe missing self-diagnostic evidence, one stale-safe synthetic continuation, claim ceiling, terminal disposition, and root/child cleanup without changing `CompletionVerdict` schema version `1`. **Current Rung:** existing installed autonomous-question/terminal-audit runner and current `unresolved`/`strategyAssessment` source contract. **Next Real Boundary:** provider-free help/oracle checks followed by a current-source grind-enabled baseline against a disposable installed OpenCode server. **Blocker/Unblocker:** 1.1 supplies the canonical broken-observer fixture semantics; stop this dependency chain if the existing verdict fields cannot represent the required continuation and record that exact schema evidence instead of adding fields silently. **Observable Proof:** help has zero effects; deterministic evaluation rejects `allow_stop`, `owner_required`, no-continuation, stale continuation, or leaked ownership for the technical fixture; current owner-only and question modes retain their existing oracles. **Authorization/Safeguards:** proof-owned loopback runtime and disposable root only; bounded configured-provider calls occur only in 1.3; no target project, installation, activation, remote, credential, or protected effect. **Restoration/Cleanup:** delete guard children then root, stop only proof-owned server/processes, remove fixture, and retain immutable bundles. **Immutable Evidence:** runner/source hashes, scenario/input digest, root/child/audit refs, verdict/continuation facts, terminal status, model-call classes, stdout/stderr, cleanup. **Validation:** syntax, `--help`, provider-free evaluator fixture/replay, and `npm run test:focused:session-completion-guard`.

- [x] 1.3 Capture the immutable same-model current-source baseline before any loaded instruction, agent, arbiter, or validator candidate mutation: run the selected `pre-escalation-recovery` scenarios and the installed completion-guard technical-blocker lane with one fixed profile/model/input/environment and record the observed gaps and controls. **Current Rung:** green provider-free runner/oracle preflight from 1.1-1.2. **Next Real Boundary:** actual loaded current-source primary, `troubleshooter`, and grind-enabled completion-guard routes in disposable synthetic repositories. **Blocker/Unblocker:** 1.1 and 1.2; a provider/evidence-only failure blocks only its capture lane until preserved-bundle replay reaches a terminal verdict or identifies the exact missing observation. **Observable Proof:** baseline bundles show actual tool/task/question behavior, consultation count, layer/observer/claim markers, guard continuation/terminal state, unchanged files, and complete cleanup for every selected scenario; no candidate source is loaded. **Authorization/Safeguards:** standing authorization for the minimum non-sensitive configured-provider calls; fixed synthetic prompts, exact tool permissions, finite scenarios, one writer, no target project or external/protected effect. **Restoration/Cleanup:** terminally delete every proof session/root and stop/remove every proof-owned process/fixture before another capture. **Immutable Evidence:** Product/Runner/Evaluator/Environment identities, exact invocations/input hashes, raw events, exits/stdout/stderr, model route, file manifests, session/process cleanup, baseline evaluation. **Validation:** provider-free replay of all preserved baseline bundles through terminal evaluation and finalization; record the baseline candidate reference in `history.md`.

## 2. Loaded Self-Diagnostic Authority

- [x] 2.1 Update the smallest existing owners: add the concise signal-based trigger and owner-routing delta to `global/AGENTS.md`; add absence-source qualification, layer classification, scoped invalidation, finite-probe, and claim-ceiling rules to `global/skills/change-ready-sdlc/SKILL.md`; add required case/report fields to `global/agents/troubleshooter.md`; and update `global/agents/session-completion-arbiter.md` so incomplete technical diagnosis produces the existing structured `continue` path. Preserve obvious-local-defect flow, exact owner-only bypass, one-consultation limit, protected boundaries, live-attempt replay, and verdict schema version `1`. **Current Rung:** immutable same-model current-source baseline from 1.3 and accepted delta specs. **Next Real Boundary:** candidate loaded-source primary/troubleshooter capture in 3.2. **Blocker/Unblocker:** 1.3; no new skill, dependency, top-level mechanism, domain-specific rule, persisted semantic record, or completion schema is permitted. **Observable Proof:** source inspection shows one concise always-loaded trigger and role-specific detail only; negative evidence cannot support product/owner claims without qualification; an obvious known cause remains direct; true owner-only remains consultation-free; current structured continuation fields remain sufficient. **Authorization/Safeguards:** local reversible source edits only; no install, activation, provider call, target-project edit, or external effect. **Restoration/Cleanup:** no runtime state is created; preserve the pre-mutation baseline and scope any later invalidation to candidate-dependent lanes. **Immutable Evidence:** reviewed diff, source hashes, owner/mirror inventory, requirement-to-surface map, and explicit `reuse + narrow extension` disposition. **Validation:** focused source/contract checks from 2.2 followed by actual candidate behavior in 3.2-3.3; production mutation keeps `Development-Stage: development`.

- [x] 2.2 Extend the existing deterministic contract owners in `tools/contracts/troubleshooter.ts`, the focused pre-escalation/change-ready contract checks under `tools/test-contracts*.ts`, and only the exact maintained mirror/source inventory that already owns the affected marker. Reject missing trigger, observer qualification, claim ceiling, contradiction/probe fields, owner-only bypass, or one-consultation limit without scoring semantic quality; update `tools/proofs/README.md` after runner modes stabilize. **Current Rung:** candidate instruction/agent/arbiter text from 2.1 and current structural contract owners. **Next Real Boundary:** installed source/profile/permission readback and provider-free proof preflight in 3.1. **Blocker/Unblocker:** 2.1; do not add fuzzy scores, duplicate full policy into portable mirrors, or enlarge an unrelated validator owner. **Observable Proof:** focused mutation controls fail when each critical marker/permission/source identity is removed and pass for the candidate; proof inventory names boundary, modes, effects, evidence, cleanup, and limits. **Authorization/Safeguards:** provider-free local validation only; mutation controls operate on disposable copies and preserve the worktree. **Restoration/Cleanup:** remove temporary mutation fixtures and leave source unchanged except intended contract/inventory edits. **Immutable Evidence:** exact focused commands, exits/stdout/stderr, mutation-control matrix, source hashes, cleanup. **Validation:** `npm run test:focused:contracts`, `npm run test:focused:session-completion-guard`, `npm run test:focused:model-routing`, and `npm run validate:strict` focused diagnostics for touched contracts.

## 3. Candidate Runtime Proof

- [x] 3.1 Run provider-free candidate preflight through the actual CLI/help, proof fixture/evaluator, installed source inventory, resolved profile/agent permissions, and loader-visible instruction/agent identities before any candidate model capture. **Current Rung:** candidate source and deterministic contracts from 2.1-2.2. **Next Real Boundary:** same-model loaded primary/troubleshooter behavior in 3.2. **Blocker/Unblocker:** 2.2; any source collision, missing loaded identity, permission expansion, oracle ambiguity, or cleanup failure blocks capture until corrected. **Observable Proof:** both proof runners expose zero-effect help; preflight/replay are terminal green; the resolved source contains exactly the candidate global authority, arbiter, and troubleshooter; specialist remains diagnosis-only; no prompt/provider call occurs. **Authorization/Safeguards:** local read-only loader/config inspection and disposable fixtures; no installation, activation, credentials, network, target project, or protected effect. **Restoration/Cleanup:** remove all disposable config/project state and prove no proof process/session exists. **Immutable Evidence:** exact argv/exits/stdout/stderr, source/profile/permission identities, scenario/oracle facts, manifests, cleanup. **Validation:** proof `--help`/preflight/replay, `npm run proof:permissions`, `npm run opencode:sources -- --format json` against isolated source as supported, and the focused checks from 2.2.

- [x] 3.2 Capture and evaluate the candidate loaded primary and `troubleshooter` routes against the preserved 1.3 baseline using the same selected model/profile/input/environment for the combined broken-observer case and controls; run-observe-correct only reproduced current-scope defects until the primary performs the safe diagnostic route without a user question or costly repeat. **Current Rung:** green provider-free candidate preflight from 3.1. **Next Real Boundary:** actual installed loaded-agent same-model capture in disposable repositories. **Blocker/Unblocker:** 3.1 and a clear provider live-attempt lane; after any evidence-only failure, replay every bundle in that failure chain through evaluator/finalization before another provider call. **Observable Proof:** candidate executes only the permitted decision-changing probe, classifies Proof Runner/Environment rather than Product/Authority for the broken observer, preserves direct observations and blocked-path claim ceiling, uses at most one correlated consultant when needed, accepts qualified absence narrowly, fixes the straightforward local case without consultation, preserves true owner-only, asks no process question, changes no fixture file, and cleans every session/root. **Authorization/Safeguards:** minimum standing-authorized synthetic provider calls, exact permissions, finite scenarios, disposable roots, no product target, remote, credential, install, activation, or protected effect. **Restoration/Cleanup:** delete child/root sessions, stop proof processes, remove fixtures, and retain immutable baseline/candidate bundles. **Immutable Evidence:** same-model/environment correlation, tool/task/question events, consultant role/task refs, assistant structural markers, file manifests, exits/stdout/stderr, cleanup, baseline/candidate evaluator verdict. **Validation:** terminal provider-free evaluator replay over both corpora and focused contract checks after the last Product Candidate mutation. A fully green broken-observer happy path establishes `Development-Stage: MVP`; later candidate mutation returns to `development` until re-proven.

- [x] 3.3 Run the installed grind-enabled completion-guard technical-blocker scenario and existing owner/question controls against the same candidate, proving that missing self-diagnostic evidence yields one current structured continuation, that the continuation advances diagnosis through existing fields, and that exact owner-only remains terminal without redundant consultation. **Current Rung:** current MVP primary/troubleshooter behavior from 3.2. **Next Real Boundary:** actual installed completion guard, hidden arbiter, synthetic continuation, and terminal root lifecycle in a disposable project. **Blocker/Unblocker:** 3.2; if existing schema fields cannot represent the accepted continuation, stop and preserve that reproduced scope blocker rather than mutating schema under this change. **Observable Proof:** technical blocker cannot reach `allow_stop` or `owner_required` before its bounded diagnostic action; continuation is revision-correlated and stale-safe; owner-only and question controls retain expected outcomes; no consultant loop, late continuation, question, file drift, child/root leak, or unauthorized effect occurs. **Authorization/Safeguards:** bounded configured primary/arbiter calls through a proof-owned loopback runtime, isolated tools, finite terminal wait, no target project, credential, install, activation, remote, or protected effect. **Restoration/Cleanup:** delete guard children then root, stop server/process tree, remove fixture/config state, preserve immutable bundles. **Immutable Evidence:** candidate/environment ids, exact invocation/input, root/child/audit/continuation refs, verdict/unresolved/strategy facts, model-call classes, terminal state, stdout/stderr, cleanup. **Validation:** provider-free replay/finalization, `npm run proof:guard-question`, existing affected guard proof controls, and `npm run test:focused:session-completion-guard`. Accepted production scope is complete only when 3.2 and 3.3 are current and green.

## 4. Material Critical Risk Disposition

- [x] 4.1 Freeze the readable Product Candidate Reference and complete primary/troubleshooter/guard raw bundles, then dispatch one fresh test-only Material SDET to challenge only reachable critical incidents: false owner escalation that strands the accepted outcome, protected-action bypass during observer qualification, unsafe repeat of a governed live attempt, loss of trustworthy direct evidence, or an unbounded diagnosis/consultant loop. **Current Rung:** current MVP and complete accepted scope from 3.2-3.3. **Next Real Boundary:** fresh-context SDET against the disposable real runners and preserved evidence, preferring installed routes over mocks. **Blocker/Unblocker:** 3.3; unavailable or unusable SDET is recorded as `blocked` and leaves the candidate at MVP rather than being replaced by production-authored test artifacts. **Observable Proof:** SDET returns exactly `critical-risks-reported`, `no-critical-risk`, or `blocked` with Effective Model and exact candidate attribution; any authored artifact is the smallest test-only critical oracle. **Authorization/Safeguards:** no production/config/instruction edits by SDET, no nested agents, no target/remote/protected effect, and no lifecycle verdict. **Restoration/Cleanup:** use existing proof cleanup and leave no writer/session/process live. **Immutable Evidence:** SDET report, critical risk/oracle matrix, test-only changed paths if any, exact commands/results, model provenance, candidate ref. **Validation:** main inspects and independently reproduces every plausible critical/non-deferrable row.

- [x] 4.2 Main-disposition the SDET result: reproduce, disprove, or show unreachable every plausible critical/non-deferrable claim; if a defect is confirmed, make only its smallest production correction, regain affected 3.2-3.3 runtime proof, and invoke a new fresh SDET only when the canonical confirmed-critical continuation predicate is met; otherwise record the terminal no-critical or blocked state and park non-critical findings. **Current Rung:** fresh SDET evidence from 4.1. **Next Real Boundary:** exact reported reproducer, then the affected installed candidate proof lane after any correction. **Blocker/Unblocker:** 4.1; the first precondition-valid attempt without a main-confirmed critical defect permanently ends SDET for this root. **Observable Proof:** every critical claim has a main disposition and evidence; corrected candidates have new current proof; reviewer severity alone never authorizes mutation; non-critical findings do not prolong qualification. **Authorization/Safeguards:** same disposable local envelope, one writer, no optional polish, scope expansion, or protected effect. **Restoration/Cleanup:** close each reproducer and preserve pre/post-correction bundles. **Immutable Evidence:** main reproduction/disposition, causal diff, affected proof replay, SDET terminal history, parked limitations. **Validation:** affected proof/evaluator lanes and focused checks for any corrected candidate.

## 5. Validation And Local Handoff

- [x] 5.1 Run complete project-native validation and strict OpenSpec consistency on one current candidate: `npm test`, `npm run validate:strict`, `npm run openspec:validate`, `npm run proof:permissions`, complete provider-free replay for both changed proof owners, and `openspec validate add-blocker-self-diagnostic-gate --strict`; inspect final installed source/agent/profile identities and record rollback, limitations, SDET disposition, external-operation state, and local handoff. Record eligibility for the next monotonic RC only when accepted scope is complete, validation is green, terminal Material SDET is usable, and no known reachable critical/non-deferrable defect remains; this apply operation remains capped at MVP and leaves RC/stable assignment to the separate qualification/archive handoff. **Current Rung:** terminal main SDET disposition and current runtime proof from section 4. **Next Real Boundary:** full local validation and final installed-source readback; no installation or consumer activation is authorized. **Blocker/Unblocker:** 4.2; red validation or stale proof keeps the candidate at MVP/development as applicable and must be corrected through its owning lane. **Observable Proof:** every named command exits `0`, strict change validation passes, resolved sources match the candidate, proof bundles remain replayable, no disposable state remains, and handoff states exact claims and limitations. **Authorization/Safeguards:** local validation/readback only; no commit, push, install, activation, release, deployment, target-project mutation, or remote effect. **Restoration/Cleanup:** stop validation processes and remove all disposable config/session/project state without touching unrelated work. **Immutable Evidence:** exact commands/exits/stdout/stderr, validation report, final diff/candidate digest, source identities, cleanup, RC history, handoff. **Validation:** the commands named in this task plus focused reruns required by any observed failure.

## 6. Final History Retrospective

- [x] 6.1 After every other currently known task is complete, perform exactly one final history retrospective over the complete `history.md` using the existing compaction matrix rows `Quality`, `Cycle Speed`, and `Token Economy` across `Working Repository` and `opencode-kit`. For every evidence-backed candidate record **Impact Horizon**, **Concrete Consumers**, **Execution Class**, **Earliest Safe Point**, **Invalidated Evidence**, and **Observable Payback**, then apply the canonical admission/deferral, target-ownership, and authority gates. Append every admitted exact-current-consumer task under `## Session-Derived Improvements` and immediately implement, prove, validate, and check it off; each generated task SHALL include Trigger/Evidence, Why, Prerequisites, Scope/Non-Goals, Implementation, Observable Proof, Validation, and Owner Blocker only when applicable. Preserve evidence-backed candidates with no current consumer as non-checkbox deferred history records, or record `none` without inventing work. Apply, archive, compaction, generated tasks, and this task itself SHALL NOT create or rerun another final-history analysis. **Current Rung:** complete validated change history and terminal candidate from 5.1. **Next Real Boundary:** the exact named current consumer for an admitted improvement, otherwise no additional execution. **Blocker/Unblocker:** every task 1.1-5.1 and every admitted Session-Derived Improvement is complete; no optional idea may delay this task. **Authorization/Safeguards:** no scope expansion or protected action; process controls may adapt only under accepted semantics and target ownership. **Restoration/Cleanup:** close any admitted improvement runner and update handoff/evidence after scoped invalidation. **Immutable Evidence:** completed six-cell matrix, classification records, admitted proof or deferred/`none` record, final task state. **Validation:** exact generated-task checks when admitted, otherwise history/readback proving one retrospective and no successor analysis.
