# Tasks 2.2-2.3 Delivery Trajectory Context Evidence

- Product Candidate: `global/bin/delivery-trajectory-context.ts`, exercised through its exported core and actual CLI.
- Contract: explicit absolute project root, Horizon id, and current archive id; stable JSON or factual Markdown; default bounds of eight archives, two MiB, and 30 seconds with finite hard caps.
- Archive window: stable archive-id lexical order; only current and preceding proposals linked to the exact Horizon are retained. Later, unrelated, legacy, and explicit-none archives do not receive support. An over-bound linked population blocks rather than truncates.
- Fact boundary: the Horizon, ordered requirement references, and selected archive `proposal.md`, `tasks.md`, `history.md`, and `evidence-index.json` produce relative path, support, byte size, and SHA-256 facts. Source payloads, private roots, untracked contents, task counts, semantic summaries, timestamps, Git state, and model-owned classifications are absent.
- Runtime Proof: `npm.cmd run test:focused:delivery-trajectory-context` exited 0 with four groups. Both help forms ran outside a repository. Core and actual CLI JSON/Markdown matched; two CLI JSON invocations were byte-identical; the disposable tree was unchanged.
- Failure Proof: unsupported and malformed Horizons, escaping and symlink references, missing and unreadable references, malformed inspected proposals, current-link mismatch, missing current archive, parent fallback, aggregate byte limit, archive count limit, deterministic timeout, and pre-aborted cancellation produced distinct blocked errors. CLI failures emitted no clean stdout and did not disclose the root.
- Regression Proof: `npm.cmd run test:focused:delivery-horizon` exited 0 with four tests. Scoped `git diff --check` reported no whitespace defect.
- Effects/Cleanup: zero writes, process starts beyond the proof-owned CLI, provider/model/network calls, Git operations, or OpenSpec operations. Every disposable fixture and process terminated or was removed.
- Split-Or-Justify: `global/bin/delivery-trajectory-context.ts` is in the 800+ split-candidate navigation band. Fresh reduction review found one cohesive change axis: bounded collection, archive linkage window, deterministic formatting, and the thin CLI for the same fact contract. Existing Horizon/manifest owners are reused. Splitting would add the generic inventory layer rejected by the design without reducing concepts; retain one file unless a later semantic or Git/inventory responsibility is added.
- Claim ceiling: provider-free helper contract, CLI behavior, failure visibility, bounds, privacy, effects, and cleanup only. No trajectory trigger, forecast, cost-axis interpretation, disposition, loaded skill, runtime-profile availability, or successor behavior is supported.
