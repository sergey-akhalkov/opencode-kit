# Tasks 3.1-3.2 Trajectory Skill And Profile Evidence

- Product Candidate: `roadmap-delivery-trajectory-routing-r2`.
- Skill Contract: `global/skills/roadmap-delivery-trajectory/SKILL.md` is a main-executed, on-demand owner for an explicit current Delivery Horizon with a supported post-archive material trigger. Its metadata stays quiet for ordinary retrospectives, cohesive local work, changed-code review, focused complexity, next-step, exhaustive audit, and campaign execution.
- Review Contract: the skill binds explicit outcome, exit-predicate, invariant, non-goal, archive, helper, evidence, and receipt facts; keeps five cost axes and `N`, `K`, automated per-item work, and irreducible evidence separate; reports honest unknown forecasts; and selects exactly one closed disposition.
- Receipt Contract: every valid triggered review persists through `materializeTrajectoryReviewReceipt` at the schema-owned immutable path. Only `measure-next-boundary` and `replan-outcome-preserving` may route one ordinary same-Horizon OpenSpec successor. The archived change remains immutable and protected semantic changes remain `owner-required`.
- Instruction Review: fresh instruction-owner review reported `IG-RDT-001` for asymmetric receipt persistence and `IG-RDT-002` for a missing README routing-map entry. Main reproduced both and corrected only the skill, focused test, and README route. One permitted corrected-candidate re-review returned `no-material-finding` for both rows. Effective reviewer model was `xai/grok-4.6`.
- Static Validation: current-candidate refresh exited 0 for `test:focused:roadmap-delivery-trajectory` (4 groups), `test:focused:library` (187 tests), `test:focused:contracts` (72 tests), and `test:focused:instruction-context` (15 tests).
- Runtime Profile: `core` includes the skill and exact helper closure `delivery-trajectory-context.ts`, `delivery-horizon.ts`, and `manifest.ts` as explicit files. `all` includes the skill and owns the same closure through `global/bin` without duplicate file ownership. Custom omission remains structurally valid and the skill requires exact capability-unavailable output with no adjacent-skill fallback.
- Installed Loader Proof: provider-free installed OpenCode runs over disposable generated `core` and `all` roots passed. Core reported the trajectory skill exactly once, all three generated helper paths, zero missing core skills, zero hidden-parent hits, zero permission failures, one catalog plugin, and cleanup complete. All reported zero missing selected skills, agents, plugins, commands, or trajectory-closure files, zero unresolved placeholders/staging paths, and cleanup complete.
- Evaluation Digests: current core `0ad7c6495a8a0289a35652b679b0a7a6cd33280d29e6d5d7386a2ece0f09909a`; current all `b2a38c4b8539d906cd7985949f90345f14a3e90ab43010167f8b423d7bf7f4a6`.
- Effects/Cleanup: generated profile, unrelated fixture, and isolated runtime/config trees only; all removed. Zero sessions, model/provider calls, credentials, install, activation, remote mutation, Git operation, or consumer effect.
- Claim Ceiling: provider-free skill contract and installed generated-profile availability only. The running user session predates the new skill and remains intentionally stale. No always-loaded OpenSpec route, configured semantic behavior, trigger quality, forecast, receipt, successor, or broad population behavior is claimed.
