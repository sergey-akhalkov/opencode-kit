# Strategy History

## 2026-08-27 - Restore A Mandatory Final-History Retrospective

- **Objective:** Detect poor project trajectory after every completed change using an existing completion point.
- **Approach:** Reintroduce final-history analysis as a required archive task that reviews prior changes and appends improvement work before completion.
- **Evidence:** Archived `add-final-history-retrospective` required broad history analysis and extra completion tasks; archived `reduce-workflow-ceremony` removed that behavior because it delayed product completion, self-expanded scope, and conflicted with terminal validators. The current requirement keeps archive terminal and optional process learning outside product completion.
- **Outcome:** Rejected.
- **Reason:** It would make process analysis a second product-completion gate and reopen the exact ceremony this change is intended to avoid.
- **Do Not Repeat Until:** Archive is no longer required to be terminal and non-blocking under accepted kit policy.
- **Evidence-Based Retry Condition:** A separately accepted lifecycle change proves that every-change retrospective analysis is necessary for product correctness and can remain bounded without adding archive tasks or invalidating terminal completion.

## 2026-08-27 - Use Velocity Or A Mutable Roadmap Status Database

- **Objective:** Forecast delivery cheaply and make trajectory decisions deterministic.
- **Approach:** Aggregate task, archive, duration, line, token, or completion counts into a score and persist a generated current roadmap, forecast, or backlog projection.
- **Evidence:** The five cost axes are not interchangeable, archive/task counts do not establish outcome movement, and the PMAC example separates reducible engineering setup from irreducible per-program runtime evidence. Current deterministic-automation policy forbids semantic effectiveness scoring and hidden inference.
- **Outcome:** Rejected.
- **Reason:** An aggregate score would invite Goodhart behavior, hide unknown evidence, and duplicate OpenSpec as mutable current-status authority.
- **Do Not Repeat Until:** A concrete accepted operational constraint requires one factual threshold that does not claim quality, progress, forecast, or strategy value.
- **Evidence-Based Retry Condition:** Maintained real evidence demonstrates a narrow factual limit with an exact failure consequence that cannot be enforced through explicit Horizon intent, separate cost observations, and model-owned review.

## 2026-08-27 - Infer Horizon Membership From Existing Artifacts

- **Objective:** Avoid adding a declaration to every new proposal and include legacy history automatically.
- **Approach:** Assign changes to a roadmap from names, capabilities, tasks, files, dates, nearby archives, or semantic similarity.
- **Evidence:** Those signals can associate unrelated outcomes, legacy changes have no reviewed horizon intent, and inferred membership would make forecast and duplicate-review identity irreproducible. The selected contract can instead preserve legacy/unrelated work as explicitly unlinked.
- **Outcome:** Rejected.
- **Reason:** Semantic inference would create false population/progress evidence and silently change project-level scope.
- **Do Not Repeat Until:** Projects expose a canonical authoritative linkage that is equivalent to an explicit reviewed declaration.
- **Evidence-Based Retry Condition:** A maintained project schema supplies exact immutable change-to-horizon membership with provenance and compatibility evidence, eliminating the need to infer from prose or repository layout.

## 2026-08-27 - Extend Archive, Complexity, Next-Step, Or Campaign As The Sole Owner

- **Objective:** Reuse an existing workflow instead of adding a new delivery-trajectory skill.
- **Approach:** Put cross-archive forecast and successor disposition into canonical archive, `complexity-management`, `next-step`, or the planned autonomous campaign.
- **Evidence:** Archive owns operation integrity and must remain terminal; complexity owns current comprehension/change locality; next-step is recommendation-only; and the first campaign increment accepts only `audit-remediate`. None owns explicit cross-archive Horizon forecast, five-axis cost separation, duplicate review receipts, and autonomous outcome-preserving successor planning together.
- **Outcome:** Rejected and narrowed to reuse of their compatible lower-level mechanisms only.
- **Reason:** Extending any one would mix lifecycle, architecture, recommendation, or execution responsibilities and create ambiguous authority.
- **Do Not Repeat Until:** One existing owner is explicitly changed to own the complete trajectory contract without weakening its current invariants.
- **Evidence-Based Retry Condition:** Implementation evidence proves the selected thin skill duplicates an existing owner's trigger, input, disposition, effects, proof, and stay-quiet contract at higher total cost; then merge only the proven identical boundary.

## 2026-08-27 - Keep Program Or Change As The Engineering Unit

- **Objective:** Preserve simple one-item-at-a-time planning and evidence ownership.
- **Approach:** Continue creating source-specific planning and implementation work for each PMAC program or analogous item, relying on local completion to eventually close the horizon.
- **Evidence:** The read-only PMAC Phase 1 case advanced exact evidence but retained 75 sources, 93 role cells, 216 mapping gaps, 243 observation gaps, and zero closed manifests. The selected corpus strategy identifies shared semantic/mapping owners `K`, automated per-program processing `N`, and separately retained serial per-program controller evidence.
- **Outcome:** Rejected as the default engineering mechanism when current repeated-touch/shared-owner evidence is present; retained for irreducible per-item evidence.
- **Reason:** One-item engineering repeats setup that belongs to shared owners, while treating all work as shared would improperly promote one member's runtime evidence to another.
- **Do Not Repeat Until:** Current evidence shows item-specific engineering is genuinely unique or the shared owner cannot preserve exact accepted semantics.
- **Evidence-Based Retry Condition:** A bounded early consumer falsifies the shared-owner mechanism, demonstrates materially distinct item semantics, or shows that batching increases total delivery cost without reducing any accepted evidence requirement.

## 2026-08-27 - Use A Cheap Post-Archive Signal With Triggered Review

- **Objective:** Detect a materially poor roadmap trajectory early enough to change future work without delaying completed product scope or weakening quality.
- **Approach:** Add explicit versioned Delivery Horizons, one fact-only bounded context helper, a compact signal after successful linked archive, deep semantic review only for current material trigger evidence, one immutable tuple-keyed receipt, and ordinary OpenSpec successors for outcome-preserving replans.
- **Evidence:** The PMAC case demonstrates the wrong-unit failure and N/K correction; current archive, history, complexity, next-step, campaign, evidence, profile, and consumer-proof owners provide compatible lower-level mechanisms but no project-level composition. User decisions selected phase/roadmap scope, cheap-signal-plus-trigger cadence, explicit linkage, a project-declared useful window, and autonomous outcome-preserving replan authority.
- **Outcome:** Selected for proposal, delta specifications, design, and tasks.
- **Reason:** It is the smallest complete mechanism that preserves terminal archive, separates unlike costs and unknowns, prevents equivalent review churn, and keeps scope/quality/protected semantics at the owner boundary.
- **Do Not Repeat Until:** N/A - selected strategy.
- **Evidence-Based Retry Condition:** Provider-free contracts, configured archive/successor behavior, generic partition evidence, or the read-only PMAC diagnostic falsifies a named selected boundary; revise only that boundary and preserve unaffected evidence rather than returning to mandatory retrospective, scoring, inference, or mixed ownership.

## 2026-08-27 - Bounded Falsification Correction: Canonicalize Review Identity

- **Objective:** Remove a planning contradiction that could either suppress a still-current material trigger or permit an equivalent second successor.
- **Approach:** One fresh implementation-readiness challenge tested the six required falsification classes against the original request; main confirmed the one material row and narrowed receipt identity and retry semantics across proposal, design, specs, and tasks.
- **Evidence:** Review session `ses_fbd55ee0effe275baj1E3V5lDH` (`xai/grok-4.6`) found `RT-001`: proposal/specs keyed horizon/context/evidence, design included model-owned trigger class in the digest, later archive identity changed context, and workflow integrity treated every changed tuple as requiring a fresh decision. Direct inspection confirmed those rules could disagree for a later archive with unchanged repeated-touch evidence.
- **Outcome:** Corrected candidate keys receipts by horizon id plus reviewed decision-context and trigger-evidence digests over ordered refs/content digests. Archive identity, timestamps, model prose, and semantic trigger labels are non-key metadata. An unchanged key reuses one receipt; changed evidence receives another deep review only for a materially distinct current trigger or a satisfied prior evidence-based retry condition.
- **Reason:** Duplicate identity now tracks decision evidence rather than volatile operation metadata or model classification, while changed evidence cannot silently inherit stale disposition.
- **Do Not Repeat Until:** Do not restore whole-helper/archive identity or semantic trigger labels to the receipt key, and do not treat every later archive as an automatic deep-review retry.
- **Evidence-Based Retry Condition:** Revisit only if provider-free or configured duplicate/changed-evidence proof shows two materially equivalent reviews receive different keys, or materially changed trigger evidence incorrectly reuses a stale receipt.

## 2026-08-29 - Correct The Disposable Archive Proof Inputs

- **Objective:** Prove task 4.1 at the provider-free canonical archive boundary without making any configured or semantic trajectory claim.
- **Approach:** Initialize one disposable OpenSpec project, author one explicitly linked completed change, invoke the unchanged canonical archive helper, then read the archived bytes and fact-only trajectory context.
- **Evidence:** Attempt `trajectory-routing-task-4-1-r1` returned canonical archive `status: archived`, `archivedAs: 2026-08-29-task-4-1-fixture`, `specsUpdated: true`, and one added requirement. All five archived planning/spec hashes matched their pre-archive values, the active change was absent, no review directory existed, and corrected absolute-root context readback was complete with zero writes, model/provider/network/OpenSpec operations, or process starts. The first context invocation correctly rejected relative `--root`; strict all-spec validation then failed because the disposable delta omitted the main-spec `Purpose` section even though pre-archive change validation and the archive helper's maintained post-validation passed.
- **Outcome:** Archive and context behavior passed; the first disposable proof fixture was insufficient for the stronger strict all-spec support check.
- **Reason:** The proof invocation and fixture, not the Product Candidate, supplied a relative root and an incomplete main-spec shape.
- **Do Not Repeat Until:** Do not reuse r1 or omit absolute helper roots and a strict-valid `Purpose` section from a fresh fixture.
- **Evidence-Based Retry Condition:** One create-new disposable fixture uses an absolute context root and a strict-valid delta that remains green after canonical archive while preserving the same archive/hash/no-receipt oracles.

## 2026-08-29 - Prevalidate An Existing Capability With The Official Template

- **Objective:** Close the provider-free task 4.1 proof with strict post-archive validation instead of iterating on hand-authored new-capability shape.
- **Approach:** After r2 again archived successfully but retained a strict warning, query `openspec templates --schema spec-driven --json`, read the package-owned `spec.md` template, and use its explicit `Purpose` contract to prevalidate an existing disposable main capability before applying an additive delta and invoking archive.
- **Evidence:** Attempt `trajectory-routing-task-4-1-r2` returned canonical `status: archived`, preserved all five pre-archive hashes, removed the active change, created no review directory, and produced complete absolute-root context with zero writes/model/provider/network/OpenSpec operations/process starts. `openspec validate fixture-capability --type spec --strict --json` isolated the only red support check to `Purpose section is too brief (less than 50 characters)`. The package template states that new-capability Purpose must be one or two sentences of 50+ characters and must be absent from a delta for an existing capability.
- **Outcome:** The archive/context candidate remains green, but r2 did not satisfy the deliberately stronger strict all-spec support check.
- **Reason:** A second manually authored new-capability fixture repeated shape discovery at the post-archive boundary; another wording-only retry would violate the causally-different retry rule.
- **Do Not Repeat Until:** Do not make another new-capability Purpose wording retry or rely on change validation to predict strict main-spec validation.
- **Evidence-Based Retry Condition:** A create-new fixture first validates one official-template-shaped existing main capability under `--type spec --strict`, applies an additive delta without a Purpose section, and only then invokes canonical archive and strict all-spec readback.

## 2026-08-29 - Reuse Portable Process For Configured Fixture Validation

- **Objective:** Close the provider-free configured-fixture preflight and strict-validation chain on Windows before any model call.
- **Approach:** Materialize the exact canonical archive dependency closure in generated `core`, then reuse its maintained `runPortableCommand` mechanism for the fixture's `openspec validate --all --strict --json` oracle instead of spawning a platform shim directly.
- **Evidence:** The first configured preflight failed closed because generated `core` exposed `openspec-archive-change` but omitted `bin/openspec-archive.ts`. After adding the exact dependency closure, preflight became ready and a fresh disposable archive plus fact-only context readback succeeded. The fixture oracle then observed `spawnSync openspec ENOENT` through a contextless `undefined` diagnostic, and a platform-name retry observed `spawnSync openspec.cmd EINVAL` under Node `v24.18.1`; both failures occurred before a configured session or provider call.
- **Outcome:** Selected as the causally distinct next provider-free mechanism; configured/model capture remains blocked until its terminal oracle is green.
- **Reason:** The repository already owns safe Windows `.cmd` resolution and cause-preserving diagnostics in `portable-process.ts`; another command-name or shell retry would duplicate that mechanism and repeat the same failure class.
- **Do Not Repeat Until:** Do not directly spawn `openspec` or `openspec.cmd`, enable `shell: true`, or authorize configured capture while the portable-process oracle is unproved.
- **Evidence-Based Retry Condition:** Revisit only if a fresh fixture using the generated `core` portable-process helper cannot reach terminal strict validation with attributable status, output, and cleanup.

## 2026-08-29 - Redact Nested Changed-Text Paths Before Diagnostic Sealing

- **Objective:** Preserve a privacy-safe terminal diagnostic for the bounded configured no-trigger archive scenario.
- **Approach:** Redact the complete diagnostic before sealing, extend the existing text redactor to nested JSON path escaping, and freeze one changed `archive-result.json` absolute-path regression before a fresh candidate attempt.
- **Evidence:** Configured attempt `task-4-2-configured-no-trigger-r1` exited one with `configured diagnostic contains a private path` after at most one allowed provider request and wrote no diagnostic. Source inspection identified the unredacted `changes` field, whose new archive result contains the disposable absolute archive path nested inside JSON text. Post-failure probes found zero matching fixture roots and zero matching processes. Focused consumer-outcome tests now prove the nested path becomes `<proof-root>` and is privacy-safe; the attempt record preserves the redacted invocation and keeps provider count and exact cleanup at diagnostic-write time unknown.
- **Outcome:** The r1 live attempt is terminal for liveness but supports no semantic observation and is non-reusable. A fresh materially changed runner candidate may retry only the same bounded no-trigger scenario after current preflight.
- **Reason:** Global redaction at the diagnostic boundary protects every retained field without weakening privacy validation or reconstructing discarded live output.
- **Do Not Repeat Until:** Do not relabel r1 as a configured observation, reconstruct its missing diagnostic, or rerun the unchanged runner candidate.
- **Evidence-Based Retry Condition:** Current provider-free tests, configured preflight, and create-new evidence root are green for a candidate containing the global-redaction correction.

## 2026-08-29 - Materialize The Effective Configured Core

- **Objective:** Make the bounded configured session use the exact generated `core` surface with a resolvable `quality-independent` build route and enforced scenario permissions.
- **Approach:** Copy generated `core` into the disposable proof root, relocate its absolute instruction/plugin paths, merge only the selected build route and bounded permissions into that copy, and retain the effective config tree in the privacy-safe runtime manifest.
- **Evidence:** Attempt `task-4-2-configured-no-trigger-r2` preserved a terminal diagnostic with cleanup complete, zero provider requests, and route-readiness failure `Configured proof agent has no model route: build`; provider-free replay reproduced failed `evaluationDigest=6364492089bdab01f64c030bad5125afdc4d03cbbf13e4c2e1b3007e54a88a44`. The generated config's `agent.compaction` file object replaced the inline profile's build map under the observed OpenCode merge. The corrected materializer passes focused tests and installed `opencode debug agent build --pure` reports `openai/gpt-5.6-sol`, variant `xhigh`, with the final bounded deny/read rules and no model request. Fresh no-trigger r3 and repeated-touch r1 each used one request, retained effective-config digest `a0f2c9629a346a5d1f33d3d54d8131fa32d336bcd2b5558fcb1d63b361e39737`, passed with evaluation digests `edd8997533529f1f74bff65a70c7e9540c532ba6c29fd654bcad1537fe013b67` and `a9e6bdf691d32cef67207af501102c741cbf280f8b6a596a9484fc1a131be2e4`, and replayed A/B byte-identically.
- **Outcome:** r2 is terminal and supports no trajectory observation. The causally distinct materialized-config candidate passed both bounded configured partitions and their provider-free A/B replays.
- **Reason:** A disposable effective config preserves the actual generated surface and selected route without pinning a model in portable `core`, exposing unrelated profile agents, or trusting a shallow runtime merge.
- **Do Not Repeat Until:** Do not retry the inline-only merge, relabel the initialized expected route as a resolved route, or start a configured session without effective-config identity in the evaluator.
- **Evidence-Based Retry Condition:** Current provider-free tests, exact effective-config readback, installed route readback, configured preflight, and a create-new evidence root are all green.
