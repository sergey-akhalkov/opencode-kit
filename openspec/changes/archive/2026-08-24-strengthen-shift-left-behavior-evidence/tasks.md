Profile: `Material` because the candidate may conditionally correct loaded instruction behavior. This change does not request stable/full qualification and has no named reachable critical consequence, so it reports verified `Outcome` evidence without RC numbering or mandatory SDET. The shared fidelity, authorization, safeguards, cleanup, evidence, and `SLBE-001` claim envelope live in `proposal.md` and `design.md`; each task below records only its changed rung or gate.

## 0. Close Broad-Claim Apply Readiness

- [x] 0.1 Materialize the reviewed `SLBE-001` development record, update the canonical `openspec-propose` skill and maintained command so broad claims create an honest unknown/missing-challenge record and run the effect-free apply gate before reporting ready, and add a deterministic drift test. **Current Rung:** propose gate exited `0` with a missing-record warning, then the unchanged apply gate exited `1` before mutation. **Next Real Boundary:** the actual portable apply operation gate over this change. **Blocker/Unblocker:** missing structured claim row; create it from reviewed proposal fields without inventing observations or supported closure. **Observable Proof:** the record parses as `unknown`; canonical skill and command contain the same readiness rule; focused tests prove broad missing-record handoff is not ready, broad development-record handoff reaches apply with warning-only claim closure, and concise exact claims remain record-free; the actual apply gate exits `0`. **Validation:** `node tools/test-openspec-operation-gate.ts`, `npm run test:focused:contracts`, `openspec validate strengthen-shift-left-behavior-evidence --strict`, the portable `--operation apply` command, and `git diff --check` for the touched paths. **Dependency-chain stop condition:** do not modify proof tooling or other loaded instructions until this task's apply gate is green.

## 1. Build The Provider-Free Proof Prerequisite

- [x] 1.1 [automation-dividend] Extend the existing consumer-outcome CLI and focused-pack contracts with one explicit `shift-left` pack, add the reviewed two-case manifest and contained generic fixture, and preserve byte-isolation of the general baseline and `claim-evidence` pack. **Current Rung:** direct source/spec inspection. **Next Real Boundary:** actual effect-free CLI help and `preflight --pack shift-left`. **Blocker/Unblocker:** none; this is the smallest prerequisite for configured evidence. **Observable Proof:** both help forms exit `0` with pack inputs/effects/evidence/cleanup documented; preflight reports exactly two ordered scenarios, one sample per arm, explicit source/provider/evidence bounds and maximum claim; malformed, extra-field, missing-field, non-contained, and unknown-pack fixtures fail before session/provider/fixture effects. **Validation:** `npm run test:focused:consumer-outcome`, actual `npm run proof:consumer-outcome -- --help`, `node tools/proofs/consumer-outcome-regression.ts -h`, and `npm run proof:consumer-outcome -- --mode preflight --pack shift-left --source-ref HEAD`. **Dependency-chain stop condition:** no configured capture or instruction edit until all provider-free preflight and cleanup facts are green.

- [x] 1.2 Extend the focused decision adapter and local checker so the reviewed decision artifact exposes current rung, selected sufficient boundary, first action, deferred dependents, protected-action disposition, and claim ceiling; add provider-free positive, wrong-order, unnecessary-climb, missing-claim, safety-regression, environment-mismatch, integrity, privacy, cleanup, and replay fixtures without fuzzy or model-based grading. **Current Rung:** pack schema and actual preflight from 1.1. **Next Real Boundary:** provider-free terminal evaluation/replay over complete synthetic arm bundles. **Blocker/Unblocker:** task 1.1. **Observable Proof:** correct fixtures pass; dependent expansion, higher-rung selection without need, malformed observation, weaker hard gate, or incomplete cleanup fails with exact pack/scenario/arm/sample attribution; two replays of the same immutable bundles produce the same digest and zero provider/session effects; existing general and claim-evidence tests remain unchanged and green. **Validation:** `npm run test:focused:consumer-outcome`, focused replay/evaluate invocations for `--pack shift-left`, and `git diff --check` on the touched proof-tooling and fixture paths. **Dependency-chain stop condition:** no live baseline until evaluator output can distinguish Product Candidate, Proof Runner, Evaluator, Environment, and cleanup failures from preserved facts.

## 2. Reach The Loaded Instruction Boundary

- [x] 2.1 Freeze the explicit pre-correction governed instruction source and use the green task-1 runner to capture both shift-left baseline scenarios through a fresh installed OpenCode primary with the selected `quality-independent` model route. **Current Rung:** terminal provider-free preflight and replay. **Next Real Boundary:** configured installed loaded-source baseline, one checked decision per scenario. **Blocker/Unblocker:** tasks 1.1-1.2 and a clear configured-capture Live-Attempt Gate. **Observable Proof:** one immutable baseline root records exact source/runner/evaluator/environment/prompt/permission identities, at most two configured-primary requests, both checked decisions, commands/exits/stdout/stderr/effects, terminal session/process/fixture cleanup, and the narrow baseline disposition without changing loaded instructions. **Validation:** actual `npm run proof:consumer-outcome -- --mode baseline --pack shift-left --candidate-id shift-left-baseline-r1 --evidence-root <new-absolute-path> --source-ref HEAD --session-mode configured`, followed by provider-free evaluation/replay of that root. **Dependency-chain stop condition:** an incomplete arm, unknown identity, unqualified negative observation, or cleanup failure blocks instruction correction and unchanged configured repetition until preserved replay is terminal or the exact missing observation is identified.

- [x] 2.2 Classify the complete baseline before mutation: when both decisions pass, record that no instruction correction is required; when a reproducible Product Candidate defect remains after runner/evaluator/environment qualification, correct only the smallest canonical shift-left instruction owner and required maintained mirrors while preserving safety precedence, owner authority, conditional qualification/SDET routing, and instruction budgets. **Current Rung:** qualified installed baseline from 2.1. **Next Real Boundary:** readable candidate source and affected deterministic routing/marker checks. **Blocker/Unblocker:** task 2.1; a red but unqualified proof path is a diagnosis blocker, not authority to edit instructions. **Observable Proof:** one explicit disposition names `no-instruction-change` or the exact evidenced decision defect and touched owner; any correction retains the required shift-left markers on all maintained surfaces, introduces no sixth principle or duplicated ladder, and leaves unrelated instruction hashes unchanged. **Validation:** `npm run test:focused:contracts`, `npm run instruction:inventory`, `npm run instruction:budget`, `npm run validate:strict`, and source/diff readback for only the affected instruction surfaces. **Dependency-chain stop condition:** no candidate configured capture until the readable source and all affected deterministic checks are green.

- [x] 2.3 Capture the readable candidate for both shift-left scenarios with the same runner, evaluator, model, variant, OpenCode version, prompt bytes, fixture state, permission envelope, and OS class as baseline, then replay the complete pair provider-free through the terminal `SLBE-001` verdict. **Current Rung:** readable candidate and qualified baseline. **Next Real Boundary:** matched installed candidate behavior plus offline terminal comparison. **Blocker/Unblocker:** task 2.2 and a clear candidate-capture Live-Attempt Gate. **Observable Proof:** exactly two candidate decisions pass characterization-first and sufficient-lower-rung hard oracles; every environment field matches; no safety, cleanup, evidence, or claim-ceiling fact is weaker; the general baseline pointer and other pack bytes remain unchanged; replay emits only the bounded two-scenario claim and named unresolved observations. **Validation:** configured `capture --pack shift-left --session-mode configured` into a new immutable candidate root, then provider-free `evaluate` and two `replay` runs over the baseline/candidate roots with equal terminal digest and zero live effects. **Dependency-chain stop condition:** any Product Candidate mutation invalidates this lane; evaluator-only correction must replay preserved bundles before another configured request.

## 3. Complete Accepted Scope And Validation

- [x] 3.1 Update the maintained proof inventory and requirement/evidence traceability with the shift-left pack's help, preflight, configured effects, exact inputs, hard oracles, evidence bounds, cleanup, general-baseline isolation, replay procedure, current candidate identities, unresolved observations, and maximum claim. **Current Rung:** terminal matched evidence from 2.3. **Next Real Boundary:** cold-reader and repository validation readback of one complete proof contract. **Blocker/Unblocker:** task 2.3. **Observable Proof:** a cold reader can locate one existing runner, one separate pack, exact commands, four-call maximum, immutable evidence roots, zero-live replay, and the non-universal claim without finding contradictory general-baseline or marker-as-proof wording. **Validation:** proof inventory readback, selected delta-to-main spec comparison, `npm run test:focused:consumer-outcome`, `npm run test:focused:contracts`, and `openspec validate strengthen-shift-left-behavior-evidence --strict`.

- [x] 3.2 Run complete applicable validation against the same readable candidate and evidence identities, including `npm run test:focused:consumer-outcome`, `npm run test:focused:contracts`, `npm test`, `npm run validate:strict`, the provider-free diagnostic `npm run proof:consumer-outcome -- --mode gate --source-ref working-tree`, `openspec validate strengthen-shift-left-behavior-evidence --strict`, `openspec validate --all --strict`, and `git diff --check`; if task 2.2 changed a governed instruction source, also satisfy the existing general consumer no-regression/current-candidate gate without promoting or rewriting its accepted baseline. **Current Rung:** complete accepted implementation, matched configured proof, and synchronized inventory/specs. **Next Real Boundary:** local verified-outcome handoff. **Blocker/Unblocker:** tasks 3.1 and all invalidated lanes replayed after the last Product Candidate, runner, evaluator, or environment mutation. **Observable Proof:** every applicable validation command exits `0`; the unchanged general pointer diagnostic remains a contained pre-existing `stale-evaluator` result because no governed instruction source changed and pointer promotion is outside scope; evidence indexes resolve, cleanup is terminal, no known accepted-outcome or non-deferrable defect remains, the handoff reports `Outcome`, proof, validation, limitations, external-operation state, and the exact `SLBE-001` ceiling, and no RC/stable claim is emitted. **Dependency-chain stop condition:** red runtime proof or applicable validation remains required work; optional workflow observations do not create another completion task.
