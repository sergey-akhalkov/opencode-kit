## Fidelity Ladder

This change uses one ladder: **Rung 1** deterministic OpenSpec and instruction-contract validation; **Rung 2** identical fresh `opencode run` sessions through the real loader before and after the policy edit; **Rung 3** later observation in a real active project change. Rung 3 is outside this bounded change because the synthetic Rung 2 continuation exercises the intended operator entry point without mutating another project.

## 1. Freeze Contract and Baseline

- [x] 1.1 Freeze the accepted outcome, preserve the overlapping pre-existing owner-only pause edits, record the current instruction inventory, and run the identical two-candidate synthetic continuation through a fresh loaded `openai/gpt-5.6-sol` `xhigh` session with mutation tools denied before policy mutation. **Current Rung**: Rung 2 baseline. **Next Real Boundary**: Rung 2 candidate session through the same loader/model/prompt/envelope. **Blocker/Unblocker**: none; a failed or non-distinguishing baseline requires correcting the proof prompt before policy edits. **Observable Proof**: baseline output states whether each of two admitted candidates becomes a `tasks.md` item, required fields, completion obligation, and summary-only disposition. **Authorization**: one bounded non-sensitive configured-provider call under standing authorization. **Safeguards**: tools denied, synthetic change only, no repository/remote effects or credentials in output. **Restoration/Cleanup**: no fixture writes; retain the local session as evidence until candidate comparison is recorded. **Expected Immutable Evidence**: exact CLI/version/model/prompt, exit status, assistant output, and inventory totals.

## 2. Implement and Prove Durable Improvement Tasks

- [x] 2.1 Update the smallest coherent model-facing surfaces: replace the global single-candidate advisory rule with immediate all-candidate active-change task persistence plus `Pending Improvement Tasks` fallback; update OpenSpec apply skill/command to reconcile and execute those tasks; update archive skill to reconcile before invoking the deterministic helper; update task-authoring rules with the compact evidence template; preserve protected-boundary and no-scope-expansion authority. Add focused static contract assertions for required and contradictory wording without adding a semantic classifier. **Current Rung**: Rung 1. **Next Real Boundary**: Rung 2 candidate fresh session. **Blocker/Unblocker**: task 1.1 baseline. **Observable Proof**: focused contracts find immediate persistence, all-candidate retention, required fields, pre-archive completion, pending-compaction fallback, and owner/target blocker semantics across maintained surfaces; existing owner-only pause contracts remain green. **Authorization**: local repository edits only. **Safeguards**: additive/orthogonal edits over the dirty tree; no overwrite of unrelated work, no automatic external-repository mutation, no weakening of owner authority. **Restoration/Cleanup**: source-only change; revert only this task's candidate if behavior comparison regresses. **Expected Immutable Evidence**: focused diff and contract output.

- [x] 2.2 Run the candidate fresh session with the exact task 1.1 model, prompt, tool denial, and loader path; compare baseline and candidate outputs against the observable oracle and retain the instruction change only if candidate behavior requires both admitted candidates to become structured unchecked tasks, neither to remain summary-only, and both to be implemented/proven before archive. **Current Rung**: Rung 2 candidate. **Next Real Boundary**: fresh critical-only SDET over the current candidate. **Blocker/Unblocker**: task 2.1 static contracts; a red or ambiguous comparison returns the candidate to `development` for the smallest instruction correction. **Observable Proof**: exact candidate output names both tasks, the required evidence fields, mandatory pre-archive completion, and owner-blocker handling without using tools. **Authorization**: one bounded non-sensitive configured-provider call under standing authorization. **Safeguards**: same synthetic prompt and denied tools as baseline, no model/config changes or external effects. **Restoration/Cleanup**: preserve both outputs and close local proof sessions when safely correlated. **Expected Immutable Evidence**: baseline/candidate transcript comparison, route, exits, and current source hash/diff.

## Session-Derived Improvements

- [x] I.1 Synchronize the dedicated hidden-compaction prompts with the all-candidate persistence contract. **Trigger/Evidence**: fresh SDET `CR-PSI-01`; main reproduced `npm run test:focused:contracts` exit non-zero because `global/opencode.json.template` lacks `Pending Improvement Tasks`; source inspection shows both `global/opencode.json.template` and active `global/opencode.json` still select exactly one highest-ROI action, while `docs/workflows/session-reflection.md` records that an isolated compaction prompt does not inherit candidate `global/AGENTS.md`. **Why**: automatic compaction is the write-unavailable producer of continuation summaries; leaving its real prompt stale recreates the exact loss this change is intended to prevent and can permit false normal completion. **Prerequisites**: tasks 2.1-2.2 current build-agent proof and main-confirmed SDET defect; satisfied. **Scope/Non-Goals**: replace only the improvement-matrix tail of the template and active compaction prompts; preserve model/variant, stagnation, Live-Attempt Gate, strategy-history, privacy, and unrelated config; do not add a backlog service or semantic classifier. **Implementation**: require every admitted candidate to be emitted under `Pending Improvement Tasks` with the standard fields, require the next session to persist all under `Session-Derived Improvements`, retain one `Next-Session Action` only for safety ordering, and carry exact `Owner Blocker` details for target/protected-boundary cases. **Observable Proof**: the SDET focused contract passes and a fresh real `--agent compaction` run over the two-candidate synthetic continuation emits both complete pending records, neither summary-only, while retaining gate-first ordering. **Validation**: `npm run test:focused:contracts`, `npm run validate:strict`, strict active-change OpenSpec validation, and candidate compaction runtime capture. **Owner Blocker**: none; local loaded config/template edits and bounded synthetic inference are authorized.

- [x] I.2 Mirror pre-helper pending-improvement reconciliation into the real `/opsx-archive` command entry point. **Trigger/Evidence**: fresh SDET attempt 2 `CR-PSI-02`; main reproduced `npm run test:focused:contracts` exit non-zero naming `.opencode/commands/opsx-archive.md`, and source comparison shows the command invokes the helper directly while only `.opencode/skills/openspec-archive-change/SKILL.md` contains reconciliation. **Why**: apply explicitly routes operators to `/opsx-archive`, and the deterministic helper sees only on-disk checked tasks, not chat-only admitted candidates; command/skill asymmetry can therefore archive falsely complete work. **Prerequisites**: I.1 compaction fix and current proof; satisfied. **Scope/Non-Goals**: mirror only the existing archive-skill reconciliation contract before command execution; preserve helper argv, deterministic merge, validation, store handling, and no-release authority; do not change helper semantics or archive any real change. **Implementation**: inspect current continuation for every admitted candidate and `Pending Improvement Tasks`, persist owned items under `Session-Derived Improvements`, require proof/checkoff, carry exact `Owner Blocker`, and invoke the helper only when reconciliation is empty and all persisted improvement tasks are checked. **Observable Proof**: SDET focused contract passes and a fresh tool-denied `/opsx-archive` command invocation with two synthetic pending candidates refuses helper/archive execution and requires both tasks to be persisted and completed. **Validation**: `npm run test:focused:contracts`, `npm run validate:strict`, strict active-change OpenSpec validation, and command-loader runtime capture. **Owner Blocker**: none; proof is non-mutating and provider-authorized.

## 3. Qualify and Handoff

- [x] 3.1 After current Rung 2 proof and accepted-scope completion, dispatch one fresh test-only `sdet-quality-engineer` to challenge only reachable critical instruction/workflow incidents, with writes limited to the smallest focused test artifact if needed; main independently reproduces and dispositions every row under the Material SDET stop rule. **Current Rung**: current loaded Rung 2 candidate. **Next Real Boundary**: deterministic/fresh-session validation selected by SDET; no external project mutation. **Blocker/Unblocker**: task 2.2 green and no open writer. **Observable Proof**: terminal `critical-risks-reported | no-critical-risk | blocked`, Effective Model, any focused test diff, main reproduction, and corrected current proof if a critical defect is confirmed. **Authorization**: local/disposable tests only. **Safeguards**: fresh test-only context, no production edits, provider/remote/project mutation, or user questions. **Restoration/Cleanup**: remove disposable state; unknown writer liveness blocks freeze. **Expected Immutable Evidence**: `implementation-evidence/critical-sdet.md` and exact reproduction results.

- [x] 3.2 Reconcile `Pending Improvement Tasks` and all candidates admitted during this session, then run focused contracts, strict library validation, full tests, strict OpenSpec validation for this change and all changes, and a final fresh loaded candidate smoke if later Product Candidate mutation invalidated task 2.2. Freeze the next RC and write the local handoff only when every session-derived improvement task is checked, applicable validation is green, terminal Material SDET evidence is usable, and no known reachable critical/non-deferrable defect remains. **Current Rung**: Rungs 1-2 qualified evidence. **Next Real Boundary**: none inside the accepted envelope; later real-project observation is not required for local policy stability. **Blocker/Unblocker**: task 3.1 and no unresolved owner/target blocker. **Observable Proof**: all tasks checked, strict validation green, baseline/candidate behavior comparison preserved, `Development-Stage: stable`, `Stable Candidate: RC<n>`, and no archive/commit/push claim. **Authorization**: local validation and documentation only. **Safeguards**: preserve unrelated dirty work and do not archive another active change. **Restoration/Cleanup**: close proof PTYs/sessions where safely attributable; no disposable roots remain. **Expected Immutable Evidence**: `implementation-evidence/final-validation.md`, `implementation-evidence/stable-handoff.md`, current diff/reference, and external-operation state.
