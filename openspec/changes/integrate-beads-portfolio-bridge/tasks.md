## 1. Operational Fit And Ownership Gate

- [x] 1.1 Recheck the current upstream release, workstation, Git, OpenCode runtime, worktree, and proof-root identities, then execute the isolated pinned `v1.2.2` Windows amd64 spike from `BPB-001`: verify archive/executable hashes and required help, initialize embedded Dolt with skipped agents/hooks, inspect every Git/path/config effect, prove one `bd create` command atomically commits the external ref and complete bridge metadata visible through exact open-and-closed-state lookup, exercise dependency/ready/assign/two-process claim/interruption/reopen/close/history/integrity/storage behavior, and close every proof-owned process and file. **Current Rung:** source and official-documentation planning with `bd` absent. **Next Real Boundary:** the exact downloaded executable commits and reopens expected rows plus atomic correlation in one temporary Git root while tracked bytes, hooks, remotes, external paths, installed config, and existing processes remain unchanged. **Blocker/Unblocker:** a mismatched release, unsupported or non-atomic create/lookup command, schema skew, unexpected init effect, ambiguous process/lock, corrupt reopen, or incomplete cleanup blocks all production bridge tasks; continue only after a causally distinct current mechanism or corrected pin is reviewed without expanding the accepted claim. **Observable Proof:** exact URL/version/platform/hashes, argv/cwd/environment, create transaction/readback across all states, exits/stdout/stderr, before/after inventories, issue/dependency/status/assignee/claim rows, process tree/locks, Dolt history/integrity/size, and cleanup. **Authorization/Safeguards/Cleanup:** one bounded public release download, isolated temporary home/data/config/repository, no credentials/provider/project/install/profile/hook/remote effect, remove only proof-owned temporary material after inspection. **Focused Validation:** direct spike oracle plus `git diff --check` for planning/source changes. Claim row: `BPB-001` runtime and environment fit.
- [x] 1.2 After task 1.1 passes, reread the exact current `add-autonomous-kaizen-grind` candidate and reconcile only its overlapping planning controls so Kaizen retains signals, occurrences, decisions, cycles, member claims, gates, budgets, and routing; Grind retains a non-portfolio execution record containing only its execution ref, run/cycle and source-decision refs, project/registration/candidate digests, optional canonical Beads ID, exact execution-prerequisite refs, route, gate/retry, Campaign/Mission/session refs, and resulting execution handoff; and Beads alone owns portfolio identity, status, dependencies, priority, assignment, duplicate relations, and terminal authority for the one explicitly enabled project. Require that project's canonical Beads ID, copy no Beads-owned state, and infer no Beads identity or ownership for another Grind registration. Preserve every unrelated user change and block production bridge work if current ownership or accepted intent conflicts. **Current Rung:** two decision-material plans declare competing portfolio owners. **Next Real Boundary:** strict OpenSpec readback shows one coherent owner map, one project-scoped Beads portfolio identity, and one distinct non-portfolio Grind execution record without copied Beads state or cross-project ownership inference. **Blocker/Unblocker:** task 1.1 and current attributable ownership of the untracked Grind artifacts; ambiguous overlap blocks only tasks 2.2 onward, not independent source-only release/adapter work. **Observable Proof:** current candidate refs/diffs, updated owner statements and dependency tasks, exact execution-record fields, selected-project Beads binding, absent Beads binding for another registration, zero production/store effects, and strict validation of both changes. **Authorization/Safeguards/Cleanup:** planning artifacts only, no discard/rewrite of unrecognized work, no binary/project/runtime mutation. **Focused Validation:** `openspec validate add-autonomous-kaizen-grind --strict` and `openspec validate integrate-beads-portfolio-bridge --strict`.
- [x] 1.3 Materialize the exact reviewed release manifest, command capability matrix, extracted executable digest, initialization-effect allowlist, and schema-skew refusal learned from task 1.1; update planning history instead of silently changing the version or supported feature set when evidence differs. **Current Rung:** disposable executable behavior. **Next Real Boundary:** source manifest/readback deterministically accepts the observed archive/executable and rejects wrong version/platform/hash or an unobserved feature before execution. **Blocker/Unblocker:** task 1.1 current evidence; any required semantic change to the pinned dependency reopens proposal/design/spec review before dependent production code. **Observable Proof:** stable manifest schema/readback, deliberate wrong-version/platform/hash/capability fixtures, and cause-preserving rejection. **Authorization/Safeguards/Cleanup:** repository source/tests only, no download/install/activation in tests. **Focused Validation:** focused manifest/contract tests and `git diff --check`.

## 2. Closed Adapter And Project Envelope

- [x] 2.1 Implement the smallest owner-local vendor direct-process adapter with closed Beads operation/input/output schemas, exact executable/root resolution, non-shell invocation, bounded JSON/text parsing, timeout/process ownership, secret/path redaction, and provider-free project check/enable/disable, list/ready/show, atomic create-feature, exact update/assign, and close-feature behavior; reject install/profile/Kaizen/OpenSpec decisions, arbitrary argv, setup, prime, hooks, server, remote/sync, destructive repair, and unsupported commands. **Current Rung:** reviewed release manifest and capability matrix. **Next Real Boundary:** direct production-module invocation against the task 1.1 fixture returns bounded current ready/show/status facts and exact vendor failures without writing Kaizen/OpenSpec, installing the binary/profile, or causing Git/remote effects outside the explicitly invoked disposable Beads operation. **Blocker/Unblocker:** tasks 1.1 and 1.3; missing structured CLI behavior is handled by one explicit bounded parser for the observed pin rather than a generic shell wrapper. **Observable Proof:** accepted/rejected schema matrix, exact child invocation, exit/stdout/stderr/cause, truncation, timeout/child terminality, and side-effect readback. **Authorization/Safeguards/Cleanup:** repository code/tests plus disposable fixture only. **Focused Validation:** adapter contract/process/error/privacy tests and direct ready/show/status proof.
- [x] 2.2 Implement one closed machine-local registration schema with at most one enabled canonical project, semantic owner class, unique safe prefix, derived privacy-safe project ref, exact binary/profile/adapter digests, and reviewed local effect policy; add one project-scoped bridge-writer lease in workstation-owned protected state outside `.beads` whose artifact storage/identity/deletion belongs only to the workstation lifecycle, serializes project-local mutations, remains held until every child `bd`/Dolt writer is terminal or write-isolated, and fails closed on drift or unknown liveness without conflating tracker and source-writer authority. **Current Rung:** read-only adapter. **Next Real Boundary:** two disposable adapter processes targeting one project serialize mutating operations, a timed-out live child keeps the lock unknown and preserves registration-referenced managed identities against rollback, a second project/duplicate prefix/wrong root is rejected, and read-only queries remain available without granting project writes. **Blocker/Unblocker:** tasks 1.2-2.1 and current project/process identity; ambiguous prior bridge writer remains a repair gate. **Observable Proof:** exact config keys/ranges/readback, owner-class/project predicates, canonical containment, workstation-owned lock path outside vendor data, competing lease result, timeout/late-child/stale/unknown behavior, attempted rollback preservation, no source-writer transition, and cleanup. **Authorization/Safeguards/Cleanup:** disposable Git roots and machine-config fixtures only, no installed config or real project mutation. **Focused Validation:** config, root, owner, prefix, lease, child-liveness, rollback-coordination, privacy, and multi-project rejection tests.
- [x] 2.3 Implement preview/enable/disable/check/rollback for one project-local embedded store using the smallest task 1.1-proven non-interactive flags and exclusion mechanism; capture before/after tracked bytes, index/worktree, hooks, remotes, relevant Git config, `.git/info/exclude`, `.beads`, paths, processes, and installed state, and preserve project data plus drift on rollback. **Current Rung:** serialized adapter and registration. **Next Real Boundary:** one disposable clean repository is enabled and disabled through the production adapter with the exact reviewed local effects, while deliberate tracked-file/hook/remote/external-path mutations fail before success. **Blocker/Unblocker:** tasks 1.1-2.2 and the observed initialization allowlist; no `--stealth`, vendor setup, or alternate exclusion mode is admitted without direct updated proof. **Observable Proof:** preview plan, exact argv/effects/readback, enabled/disabled status, deliberate red fixtures, original-cause diagnostics, process closure, and attributable cleanup. **Authorization/Safeguards/Cleanup:** disposable repositories only, no production project, profile, managed instruction, remote, or user-owned process effect. **Focused Validation:** project lifecycle/Git effect/rollback tests and one direct adapter enable-disable proof.

## 3. Kaizen Portfolio Correlation

- [ ] 3.1 Extend the Kaizen owner with exact eligibility and serialized idempotent promotion orchestration: `project-change` requires `ownerClass=current-project` plus the enabled project ref in the signal's recorded project refs; `kit-candidate` requires `ownerClass=opencode-kit` plus an enabled kit-owner registration; triage-session project ref alone never matches. Use the vendor adapter's one-command atomic external-ref/metadata create, order create/readback before the existing Kaizen promoted transition, recover a lost create response through all-state lookup, and fail on zero-owner, wrong-project, unsafe payload, multiple-correlation, or competing-portfolio state. **Current Rung:** one enabled disposable project with one bridge writer. **Next Real Boundary:** direct Kaizen-owner production entry over consumer-project and kit-candidate fixtures creates exactly one correctly routed feature, an injected post-create interruption reuses it on resume, and Kaizen records only the missing promoted transition. **Blocker/Unblocker:** tasks 1.2 and 2.1-2.3; any need for a new Kaizen persisted event/schema or non-atomic vendor create blocks this task and reopens the design rather than silently migrating v1. **Observable Proof:** exact owner/project eligibility matrix, create argv/metadata/external ref/readback across all states, feature cardinality, Kaizen state before/after interruption, duplicate repair gate, redaction, exits/diagnostics, and cleanup. **Authorization/Safeguards/Cleanup:** disposable Kaizen and Beads stores only, no provider/OpenSpec/source/installed/remote effect. **Focused Validation:** promotion/idempotency/interruption/duplicate/project-routing/privacy tests plus direct happy-path proof.
- [ ] 3.2 Implement coarse dependency/parent/duplicate projection, bounded ready/show, and advisory assignment with child task/session refs through the Kaizen orchestrator and vendor adapter while preserving current frontier and Campaign/Mission writer authority; keep production atomic claim unavailable and counts/endorsements non-authoritative. **Current Rung:** one canonical promoted feature. **Next Real Boundary:** one blocker hides its dependent feature from Beads ready, closing the blocker makes it portfolio-ready, assignment reads back exactly, and current source execution remains blocked when the independent writer gate is absent. **Blocker/Unblocker:** task 3.1; unsupported relation or metadata behavior in the pin narrows the adapter and claim rather than emulating another graph. **Observable Proof:** graph/status/ready transitions, assignment/session refs, current frontier unchanged, absent-writer rejection, occurrence-count invariance, and zero source effect. **Authorization/Safeguards/Cleanup:** disposable graph fixtures only, no source/provider/remote mutation. **Focused Validation:** dependency/readiness/assignment/authority tests and direct graph proof.
- [ ] 3.3 Implement exact idempotent OpenSpec link orchestration under the same bridge lease: match project/change/correlation, write `spec_id` plus `changeRef`, read back, recover lost response, and reject a different existing link or multiple correlation without overwrite. **Current Rung:** assigned feature without implementation identity. **Next Real Boundary:** one disposable feature links once to one exact project-local change, an injected post-update interruption returns the same link on retry, and a mismatched second change stays blocked. **Blocker/Unblocker:** tasks 3.1-3.2 and current project/change identity; missing atomic or exact update behavior in the pin blocks linking rather than weakening the mismatch guard. **Observable Proof:** accepted/rejected link matrix, update argv/readback, interruption recovery, unchanged feature cardinality, no OpenSpec mutation by the link operation, diagnostics, and cleanup. **Authorization/Safeguards/Cleanup:** disposable Beads/OpenSpec fixtures only, no source/provider/archive/remote effect. **Focused Validation:** link/idempotency/mismatch/correlation tests and direct link proof.
- [ ] 3.4 Implement ordered terminal validation and reconciliation: verify exact linked OpenSpec accepted tasks, current runtime proof, applicable validation, archive identity, external-effect state, and terminal writer/cleanup ownership; close/read back Beads before resolving Kaizen; repair only a closed-item/unresolved-signal projection and leave incomplete evidence stale-open. **Current Rung:** exactly linked feature with no terminal claim. **Next Real Boundary:** one disposable linked change closes and resolves only after all current terminal facts pass, while injected post-close interruption appends only the missing Kaizen resolution and every missing/stale fact leaves both records safely open. **Blocker/Unblocker:** tasks 3.1-3.3 and current canonical OpenSpec/proof/archive readback; no summary, task checkbox alone, Beads status, or assignee may substitute. **Observable Proof:** accepted/rejected terminal matrix, exact link/correlation, ordered state observations, interruption recovery, no repeated implementation/archive effect, diagnostics, and cleanup. **Authorization/Safeguards/Cleanup:** disposable OpenSpec/Kaizen/Beads fixtures, no real archive, source writer, commit, remote, or destructive cleanup. **Focused Validation:** close/reconcile/proof-staleness/writer-liveness tests and direct terminal projection proof.
- [ ] 3.5 Execute the smallest complete provider-free one-project happy path through the separate workstation fixture, vendor adapter, and Kaizen orchestrator: enable a disposable repository, promote one signal, add a blocker/dependent relation, observe ready, assign and link one disposable OpenSpec change, satisfy its real local proof/validation/archive fixtures, close/resolve in order, disable, and retain exact diagnostics and project evidence. **Current Rung:** each bridge component proved directly. **Next Real Boundary:** the same production entrypoints complete the full `BPB-001` lifecycle with one feature and no tracked-source, instruction, hook, remote, provider, or writer-authority effect. **Blocker/Unblocker:** tasks 2.1-3.4 and complete writer/process cleanup; integration-only defects remain at this parent boundary without reopening unrelated passing leaves. **Observable Proof:** all identity chains, graph transitions, Kaizen/OpenSpec/Beads readback, Git/process/effect inventories, exits/stdout/stderr, disable state, and cleanup. **Authorization/Safeguards/Cleanup:** proof-owned repositories/stores only, no installed profile/binary or external effect. **Focused Validation:** affected adapter/Kaizen/OpenSpec/profile-independent suites and the direct happy-path invocation. Claim row: provider-free `BPB-001` integration.

## 4. Workstation And Runtime Exposure

- [ ] 4.1 Extend the existing protected workstation lifecycle with source-manifest-driven Beads preview/install/check/rollback and exact protected executable/adapter/registration/bridge-lock identities; make that lifecycle the sole storage/identity/deletion owner for the lock artifact; keep install separate from profile selection and project activation; require rollback of registration-referenced managed material to acquire the same bridge lease and preserve the lock plus every referenced item when writer closure is active or unknown; reject drift and preserve all project/Kaizen/OpenSpec/Beads evidence. **Current Rung:** provider-free adapter/orchestrator happy path. **Next Real Boundary:** non-mutating production preview renders one exact protected plan, deliberate manifest/platform/hash/path/drift fixtures fail before host mutation, and an active/unknown-writer fixture returns partial unknown without deleting its lock or referenced managed material. **Blocker/Unblocker:** tasks 1.3, 2.2, and 3.5 plus current workstation owner/path conventions; source manifest presence remains effect-free and unknown writer liveness blocks only affected rollback cleanup. **Observable Proof:** schema/readback, protected paths/ACL plan, lock ownership, install/check/rollback effects, active/unknown-writer preservation, wrong-identity fixtures, secret scan, no task/service/process/profile/project effect, and cleanup. **Authorization/Safeguards/Cleanup:** repository source/tests and disposable workstation fixtures only. **Focused Validation:** workstation installer/manifest/ACL/lease-coordination/rollback tests and production preview.
- [ ] 4.2 Add one exact full `core-beads` profile equal to current core plus the on-demand Beads skill/helper closure, extend installer/manifest/profile validation for that concrete identity, keep core free of Beads, expose the same skill once in all/unprofiled full source, and enforce an exact stay-quiet trigger plus adapter-only execution; add no thin command, vendor `AGENTS.md`, `bd prime`, plugin, MCP, missing-binary success, or automatic project activation. **Current Rung:** source adapter/orchestrator plus inert installer lifecycle. **Next Real Boundary:** generated core, core-beads, and all manifests plus unprofiled full-source inventory prove exact discovery identities; fresh isolated OpenCode processes load only the expected on-demand surface and ordinary OpenSpec/Kaizen/grind controls do not trigger it. **Blocker/Unblocker:** tasks 2.1 and 4.1 plus current profile/schema/source identity; unknown OpenCode config shape is verified against current schema before write and a Beads-only partial profile remains invalid. **Observable Proof:** exact core union, installer accepted identities, core/core-beads/all/full-source manifests and loader inventories, stay-quiet/explicit-trigger cases, source/catalog identities, on-demand body, negative managed-instruction/plugin/MCP controls, process closure, and cleanup. **Authorization/Safeguards/Cleanup:** repository artifacts plus isolated generated profiles/process roots, no active global config edit/restart/install/project effect. **Focused Validation:** runtime-profile, skill/instruction, catalog, installer, config-schema, context-quality, and fresh-loader tests.
- [ ] 4.3 [automation-dividend] Extend the existing workstation and consumer-outcome proof owners with one deterministic reviewed `BPB-001` population for manifest, repository, Kaizen correlation, dependencies, assignment, link interruption, terminal projection, profile identities, Git effects, install/rollback, and deliberate red cases; reuse it for provider-free and installed consumers without adding a generic external-tool runner. **Current Rung:** complete source behavior and its first repeated proof boundary. **Next Real Boundary:** two provider-free materializations are stable and effect-bounded, then the same seed drives the protected installed pilot without semantic inference or hidden command replay. **Blocker/Unblocker:** tasks 3.5 and 4.1-4.2; changed upstream/runtime observations update reviewed seed facts and invalidate only dependent samples. **Observable Proof:** stable seed/schema/readback, second-run equality where ordering is non-semantic, deliberate red results, exact first consumers, zero provider/remote/project effects in provider-free mode, and cleanup. **Authorization/Safeguards/Cleanup:** reviewed synthetic data and disposable roots only until task 5.1. **Focused Validation:** focused proof-owner/consumer-outcome suites and `git diff --check`.

## 5. Installed One-Project Proof

- [ ] 5.1 With task 1.1 live-attempt evidence current, run protected preview/install/check for the exact pinned binary in one proof-owned installation, inspect effective elevation, archive/executable/manifest/ACL identities, version/help, process closure, and rollback plan, and stop on any antivirus/filesystem/schema/capability mismatch before profile or project activation. **Current Rung:** green source and disposable behavior. **Next Real Boundary:** the protected installed executable passes production check and starts no persistent process while existing OpenCode services, configs, projects, packages, tasks, hooks, and remotes remain unchanged. **Blocker/Unblocker:** tasks 4.1 and 4.3 plus clear live-attempt gate and proof-owned target; a failed install attempt is finalized and requires a causal mechanism change before retry. **Observable Proof:** exact invocation/status/stdout/stderr, effective token, hashes/ACL/readback, before/after process/host inventories, rollback readiness, and cleanup. **Authorization/Safeguards/Cleanup:** bounded local protected installation only, no user-process restart/termination, project/profile/provider/remote effect. **Focused Validation:** installed lifecycle check plus affected workstation tests.
- [ ] 5.2 Materialize the exact full `core-beads` profile against an isolated global target, start fresh proof-owned OpenCode processes with isolated state for core, core-beads, all, and unprofiled full-source identities, verify exact on-demand Beads discovery/stay-quiet and missing-project status, then roll back the profile target without touching existing OpenCode processes or current global configuration. **Current Rung:** checked installed executable with source profile fixtures. **Next Real Boundary:** the actual loader resolves the selected skill/helper once for core-beads/all/full-source, omits it for core, stays quiet during ordinary lifecycle work, and exposes no plugin/MCP/vendor instruction/project state. **Blocker/Unblocker:** tasks 4.2 and 5.1; stale source/config/profile/loader identity, unsupported installer profile, or unknown process ownership blocks the loader claim. **Observable Proof:** source/config/profile digests, exact effective manifests, process invocations/PID trees, skill/helper inventory, loaded on-demand body, stay-quiet/explicit-trigger controls, exits/logs, rollback readback, and cleanup. **Authorization/Safeguards/Cleanup:** isolated proof process/config/data/cache/state/home only, no existing process/config/project mutation. **Focused Validation:** installed profile/readiness and loader behavior checks.
- [ ] 5.3 Execute the installed `BPB-001` population in one disposable clone registered through the protected bridge: enable, promote with injected response loss, graph/ready, assign, link with injected response loss, complete one representative local OpenSpec proof/validation/archive path without source commit, close/resolve, disable, inspect data/history/size/effects, prove an active/unknown-writer rollback attempt preserves its lock and registration-referenced managed material, then prove closed-writer rollback of binary/profile/registration material while preserving and inspecting the project-local store before proof-owned cleanup. **Current Rung:** installed binary and loader proved separately. **Next Real Boundary:** the actual installed workstation/vendor-adapter/Kaizen-orchestrator entrypoints complete the one-project outcome with exact state, terminal writer/process cleanup, unchanged tracked source/hooks/remotes/instructions, fail-closed partial rollback under unknown liveness, and no network/remote effect beyond the already completed release download. **Blocker/Unblocker:** tasks 3.5 and 5.1-5.2, one clear bridge writer for successful cleanup, clean disposable clone, and current terminal evidence; any unknown writer/effect blocks close and rollback-complete claims while preserving the lock and every referenced managed item. **Observable Proof:** candidate/environment/config/registration/lock/process/feature/signal/change/archive refs, all CLI JSON/exits/diagnostics, Git and network/remote canaries, loader state, Dolt integrity/size/history, ordered create/link/closure recovery, partial-unknown and completed rollback identities, and attributable cleanup. **Authorization/Safeguards/Cleanup:** one proof-owned project and installed target, no provider, local commit, push, merge, release, deploy, shared server, remote, or user-process effect; remove only proof-owned clone/install/profile material after inspection and terminal writer closure. **Focused Validation:** installed bridge/profile/workstation/Kaizen/OpenSpec suites and the actual production-entrypoint happy path. Claim row: installed `BPB-001` maximum claim.
- [ ] 5.4 Run matched current-workflow and Beads-bridge consumer outcomes with the same model/profile, permissions, environment, initial state, accepted outcome, proof, safety, diagnostics, and cleanup oracles; report exact tool-call/context observations without claiming improvement unless every hard oracle passes, no friction field regresses, and one maintained scenario improves. **Current Rung:** current installed functional candidate. **Next Real Boundary:** maintained evaluator consumes current baseline/candidate samples and returns an exact bounded result attributed to both identities. **Blocker/Unblocker:** task 5.3 and comparable current samples; missing or non-equivalent evidence leaves context benefit unknown without blocking the functional claim. **Observable Proof:** baseline/candidate ids, matched inputs/oracles, raw bounded samples, evaluator result, tool-call/context fields, side effects, and cleanup. **Authorization/Safeguards/Cleanup:** bounded synthetic provider calls only when the existing maintained harness requires them under current local authorization, no project/remote effect. **Focused Validation:** consumer-outcome capture/evaluator suites and current matched run.

## 6. Critical Risk And Final Validation

- [ ] 6.1 After current installed proof and accepted-scope completion, dispatch one fresh test-only SDET for reachable tracked/unrelated project mutation, Kaizen or Beads evidence loss, duplicate feature creation, false terminal resolution, writer-envelope escape, unsafe stale-lock takeover, and destructive rollback; main shall independently reproduce every row and make only the smallest confirmed critical correction before rerunning affected proof. **Current Rung:** complete current `BPB-001` candidate. **Next Real Boundary:** independent tests exercise the same exact candidate and destructive/data-integrity boundaries without production authorship. **Blocker/Unblocker:** tasks 5.3-5.4 and terminal writer/process ownership; missing fresh SDET leaves only the named critical evidence gate blocked. **Observable Proof:** attributed SDET terminal result, exact reproducer, main disposition, corrected candidate identity if any, affected proof replay, and no unresolved confirmed reachable critical/non-deferrable defect. **Authorization/Safeguards/Cleanup:** test-only disposable scope, no production code by SDET, no real project/backlog/config/remote effect. **Focused Validation:** SDET-authored critical tests plus all invalidated focused proofs.
- [ ] 6.2 Complete operator and agent documentation for pin/provenance, preview/spike/install/check/rollback, exact core/core-beads/all/full-source discovery, fresh-process activation, skill trigger/stay-quiet behavior, one-project registration/enable/disable/status, Kaizen promotion, advisory ready/assign, OpenSpec idempotent link/terminal close, duplicate/stale/unknown repair, privacy, data preservation, version limitations, no-voting, no-remote, and stop line; verify examples resolve from installed source without personal paths, credentials, arbitrary argv, thin-command passthrough, or vendor setup. **Current Rung:** proved final profile/adapter/orchestrator behavior. **Next Real Boundary:** a fresh installed-source readback follows the documented provider-free status and disabled-project path to the stated observation. **Blocker/Unblocker:** tasks 4.2-6.1 and final schema/profile/skill shape. **Observable Proof:** docs/profile/skill/helper cross-reference validation, parsed examples, installed path readback, secret/personal-path/vendor-instruction scan, and no new activation. **Authorization/Safeguards/Cleanup:** documentation/read-only installed inspection only. **Focused Validation:** documentation, instruction, profile, config, and source-link validators.
- [ ] 6.3 Run strict OpenSpec validation, all affected focused suites, full repository validation, current installed-entrypoint readback, Dolt integrity, profile/core negative controls, no-remote/worktree-scope checks, and writer/process/profile/install/project cleanup; inspect the current candidate against every `BPB-001` requirement and keep the change unarchived until all accepted tasks and proof gates are complete. **Current Rung:** corrected documented candidate. **Next Real Boundary:** canonical `/opsx-archive` only after every requirement-linked oracle is green. **Blocker/Unblocker:** tasks 1.1-6.2, terminal critical evidence, no known confirmed reachable critical/non-deferrable defect, and complete attributable cleanup. **Observable Proof:** exact commands/exits/stdout/stderr, candidate/environment identity, requirement-to-oracle mapping, installed and rollback state, known non-critical limitations, external-operation state, and clean attributable cleanup. **Authorization/Safeguards/Cleanup:** no production activation, remote operation, commit, merge, release, deployment, shared server/federation, or unrelated worktree cleanup. **Focused Validation:** `openspec validate integrate-beads-portfolio-bridge --strict`, applicable focused suites, `npm run validate`, and current installed boundary readback.
