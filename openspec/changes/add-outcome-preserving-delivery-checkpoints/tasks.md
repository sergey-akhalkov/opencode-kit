## 1. Acquire Owners And Materialize The Scenario Contract

- [x] 1.1 After `make-grind-blockers-task-scoped` and `add-cross-project-kaizen-loop` are archived with terminal writers, re-run active-change ownership, source/runtime identity, scoped diff, and canonical-spec inspection; rebase this change against their resulting main/OpenSpec/compaction/frontier/Kaizen contracts, enable mutation only for exact transferred owners, and verify `ownership.json` reports no overlapping mutation-enabled owner. **Current Rung:** planning-only ownership manifest with two archive-before-acquire dependencies. **Next Real Boundary:** provider-free reviewed scenario materialization in task 1.2. **Blocker/Unblocker:** both predecessor archives or explicit terminal owner transfers; planning may continue but no production/proof-owner mutation may start. **Observable Proof:** one current ownership record names exact acquired capability requirements and write roots, candidate source identities, no live predecessor writer, and any outcome-preserving artifact correction made after rebase. **Validation:** ownership inventory/readback, strict selected OpenSpec validation, and scoped status/diff inspection.
- [x] 1.2 [automation-dividend] Extend the smallest existing instruction/consumer-outcome and completion-frontier proof owners with one reviewed `OPDC-001` seed population covering similar-retry stagnation, different-defect late discovery, coarse invalidation amplification, failed costly repetition, measured dominant setup, advancing long work, one cheap failure, irreducible cost, duplicate suppression, independent sibling work, and scope/proof weakening; provide effect-free help, provider-free evaluation over disposable fixtures, stable explicit fields, and deliberate red controls without deterministic semantic scoring. **Current Rung:** reviewed proposal/spec population. **Next Real Boundary:** production loaded-main trigger and ordinary-root behavior in task 2.1. **Blocker/Unblocker:** task 1.1 ownership acquisition; fixture expected judgments remain reviewed data outside helper code. **Observable Proof:** evaluation exits zero, expected pass/fail rows are exact and stable, cost arithmetic is explicit, no helper infers similarity/dominance/quality, source remains unchanged, provider calls are zero, and disposable state is removed. **Validation:** focused proof-runner tests, seed/readback schema, stable second pass, malformed/overflow controls, and proof inventory readback.

## 2. Deliver The Universal Ordinary-Root Happy Path

- [x] 2.1 Add the compact project-neutral delivery-drag trigger and bounded checkpoint behavior to the canonical loaded main authority and exact maintained mirrors, preserving existing stagnation, blocker diagnosis, live-attempt, substitution, owner-authority, and Kaizen boundaries; verify focused mutation controls reject missing positive patterns, negative controls, outcome/proof preservation, suppression, and no-owner-question language. **Current Rung:** provider-free reviewed scenario pack from task 1.2. **Next Real Boundary:** one configured disposable ordinary root in task 2.2. **Blocker/Unblocker:** task 1.2; do not add a new skill, optimizer agent, timer, score, persistence service, or copied detailed owner contract. **Observable Proof:** current source contains one canonical activation owner, mirrors contain only role deltas, all supported patterns and exclusions resolve, and focused contracts distinguish an outcome-preserving route from scope/proof reduction. **Validation:** focused instruction/change-ready contracts, instruction context/budget checks, runtime-source diagnostics, and provider-free scenario evaluation.
- [x] 2.2 Run the first configured happy path through the actual installed/loaded entry point in a disposable non-OpenSpec project: supply current facts for two materially different failures at one costly late boundary, observe exactly one checkpoint before another costly action, select an earlier sufficient canary, retain the original outcome/oracle/population, and continue to the canary marker without a question. **Current Rung:** source and deterministic contracts from task 2.1. **Next Real Boundary:** OpenSpec route continuity in task 3.1. **Blocker/Unblocker:** exact loaded source/model/environment identity and bounded synthetic provider authority; after a failed configured run inspect status, diagnostics, effects, and cleanup, then require a causally different mechanism or exact missing observation before another equivalent attempt. **Observable Proof:** ordered events show trigger, bounded checkpoint, selected route, unchanged accepted facts, zero expensive/protected effect, one continuation marker, duplicate suppression on unchanged facts, and terminal session/process cleanup. **Validation:** configured current-run evaluator, source/environment readback, provider-call bound, worktree effects, and cleanup.

## 3. Preserve OpenSpec And Compaction Continuity

- [x] 3.1 Extend canonical OpenSpec apply and managed compaction source/template so a materially changed checkpoint route updates only affected design/tasks/history/attempt/proof controls, a confirmed current route creates no artifact churn, and unresolved checkpoint identity/current facts/next oracle survives compaction as current dependency-closure work; verify Kaizen remains optional and non-authorizing. **Current Rung:** proved ordinary-root behavior from task 2.2. **Next Real Boundary:** configured disposable OpenSpec apply plus compaction in task 3.2. **Blocker/Unblocker:** task 2.2 and current post-predecessor compaction contract; do not create a mandatory retrospective, optional improvement task, second history ledger, or consumer-project cross-write. **Observable Proof:** provider-free fixtures show exact changed paths for a shift-left route, zero planning changes for `irreducible`, owner parking for population reduction, one compaction continuation block for a due checkpoint, and no Kaizen dependency. **Validation:** focused OpenSpec/instruction/compaction contracts, active/template marker parity, strict source schema, and provider-free scenario evaluation.
- [x] 3.2 Through the loaded entry point in a disposable OpenSpec project, reproduce a late-discovery route change and observe main append one strategy-history entry, update only dependency-relevant task/design controls, continue the new route without process approval, compact before its oracle, and resume the same suppression identity and next action in a fresh continuation. **Current Rung:** candidate OpenSpec/compaction source from task 3.1. **Next Real Boundary:** grind task-scoped enforcement in task 4.1. **Blocker/Unblocker:** task 3.1 and bounded configured-provider path; after a failed live-dependent run inspect that invocation and require a causally different mechanism or exact missing observation before another equivalent call. **Observable Proof:** exact artifact diffs, event order, compaction summary, continuation marker, no duplicate checkpoint/history entry, unchanged accepted scope/proof, zero archive/remote/protected effect, and terminal cleanup. **Validation:** configured current-run evaluator, strict disposable change validation, operation-gate readback, source/status effects, and cleanup.

## 4. Enforce Task Scope In Grind

- [x] 4.1 Extend the current task-scoped frontier/arbiter instruction and existing frontier fixtures so a due delivery checkpoint is a process item or dependency, dependent costly work is not runnable first, independent siblings remain runnable, omitted due checkpoints reconcile, `irreducible` can close the process item without an owner question, and a tempting proof-scope reduction remains a separately parked product decision only when required. **Current Rung:** ordinary and OpenSpec behavior proved in tasks 2.2 and 3.2. **Next Real Boundary:** installed grind scenario in task 4.2. **Blocker/Unblocker:** current archived frontier schema and task 3.2; deterministic code validates structure/correlation/readiness only and never infers delivery drag or cost dominance. **Observable Proof:** provider-free frontier/verdict rows expose exact runnable/process/product refs, reject costly-before-checkpoint and process-to-product verdicts, retain independent siblings, and preserve existing safety/live-attempt/writer-liveness gates. **Validation:** provider-free frontier evaluation, focused completion-guard/verdict/question tests, malformed/stale controls, and current source diff.
- [x] 4.2 Run the smallest configured grind happy path in a disposable root: one costly item depends on a due checkpoint, one sibling is independent, the sibling completes, the checkpoint selects and reaches its bounded oracle, and only then the original costly item becomes eligible under unchanged gates; verify no product question or protected effect occurs. **Current Rung:** green provider-free frontier behavior from task 4.1. **Next Real Boundary:** complete cross-project `OPDC-001` population in task 5.1. **Blocker/Unblocker:** task 4.1, exact primary/arbiter routes, and proof-owned disposable runtime; after any configured failure inspect status, diagnostics, effects, and cleanup before selecting a causally different attempt. **Observable Proof:** ordered frontier generations and events show sibling marker, checkpoint marker, dependent eligibility, zero invented answer/product question, unchanged scoped gates, bounded calls, and terminal liveness/cleanup. **Validation:** configured guard evaluator, provider-free disposable fixtures, session-delivery readback, process/question/effect counts, and cleanup.

## 5. Complete The Broad Claim And Validate

- [x] 5.1 Complete every `OPDC-001` population member against one readable candidate across at least two unrelated disposable repositories and ordinary/OpenSpec/grind task classes, including advancing-long, one-cheap-failure, unchanged-duplicate, irreducible, and scope-reduction controls; update the proposal's population summary with exact observed/missing rows and keep the maximum claim narrow. **Current Rung:** configured happy paths from tasks 2.2, 3.2, and 4.2. **Next Real Boundary:** focused regression decision in task 5.2. **Blocker/Unblocker:** all three configured paths are current and every failed configured attempt was diagnosed before another attempt; no representative row may stand in for another declared member. **Observable Proof:** each member has current candidate/environment/path/oracle/event/effect/cleanup observations, positive rows checkpoint exactly once, negative rows do not checkpoint, proof scope never narrows, and unresolved observations remain explicit. **Validation:** population evaluator over disposable fixtures, cross-project isolation, stable second pass, privacy/effect scan, and claim-ceiling check.
- [x] 5.2 After current Runtime Proof, add only the smallest main-owned focused regression for a reproduced accepted-outcome behavior not already distinguished by the scenario pack, or record `none` with exact existing oracle facts; rerun the affected production boundary after any test change. **Current Rung:** complete current population from task 5.1. **Next Real Boundary:** fresh critical-only SDET in task 5.3. **Blocker/Unblocker:** task 5.1; do not add coverage-count, style, optional optimization, or duplicate model-verdict tests. **Observable Proof:** one unique requirement-linked regression fails against the defective fixture and passes current behavior, or the no-gap disposition maps every realistic regression to an existing oracle. **Validation:** affected focused tests and configured/provider-free boundary evaluation.
- [x] 5.3 Dispatch one fresh test-only critical SDET challenge for protected-action retry, accepted population/oracle weakening, synthetic product-decision authority, infinite checkpoint repetition, stale suppression/frontier continuation, and concurrent writer or dependent-item execution; require `critical-risks-reported | no-critical-risk | blocked`, current candidate attribution, and Effective Model, with no production/instruction/OpenSpec mutation. **Current Rung:** proved accepted scope and focused regression disposition from task 5.2. **Next Real Boundary:** main risk disposition and affected re-proof in task 5.4. **Blocker/Unblocker:** task 5.2, frozen readable candidate, closed production writers, and exact test-only scope. **Observable Proof:** every risk row names a reachable critical consequence and exact oracle; any test-only artifact distinguishes a defective fixture from the current candidate. **Validation:** SDET-authored focused tests, report schema, candidate attribution, and terminal writer closure.
- [x] 5.4 Main independently reproduce, disprove, or show unreachable every SDET row, retain only unique critical oracles, apply the smallest accepted-outcome correction for any confirmed defect, and regain every invalidated provider-free/configured/population path before recording the terminal SDET disposition. **Current Rung:** fresh SDET result from task 5.3. **Next Real Boundary:** broad-claim challenge in task 5.5. **Blocker/Unblocker:** task 5.3; reviewer severity never authorizes mutation and no plausible reachable authorization, data-integrity, protected-action, or infinite-loop claim may be parked. **Observable Proof:** every risk id has a main disposition, corrected behavior is current when needed, all writers/processes are terminal, and no accepted requirement or critical invariant remains red. **Validation:** affected proof paths, retained critical tests, current-run comparison, and scoped diff.
- [x] 5.5 Freeze the corrected candidate and obtain one fresh read-only evidence-sufficiency challenge for the declared `OPDC-001` finite population, project-neutral scope, installed paths, trigger controls, and maximum claim; main narrows or blocks unsupported rows without mutating during the challenge. **Current Rung:** terminal critical-risk disposition and current proof from task 5.4. **Next Real Boundary:** complete project validation in task 5.6. **Blocker/Unblocker:** task 5.4, one readable candidate, and bounded current results; missing reviewer output blocks only the broad claim and preserves narrower observations. **Observable Proof:** every declared member is current or explicitly missing/unknown, no representative or project-specific example is generalized silently, and every reviewer row receives a main disposition. **Validation:** challenge result, candidate identity match, and zero mutation during review.
- [ ] 5.6 Run focused instruction/OpenSpec/compaction/completion tests, provider-free disposable scenario evaluation, all configured current-run evaluators, instruction context/budget and runtime-source diagnostics, `npm test`, `npm run validate:strict`, strict selected and all OpenSpec validation, operation gates, `git diff --check`, and final scoped status/diff inspection; correct only current-change failures and preserve unrelated dirty files. **Current Rung:** challenged broad claim from task 5.5. **Next Real Boundary:** separately authorized installation/restart and real operator use after implementation handoff. **Blocker/Unblocker:** tasks 1.1-5.5 and current source identities; any behavior mutation invalidates dependent proof. **Observable Proof:** applicable checks exit zero for one readable candidate, accepted tasks and claim rows are current, no known confirmed reachable critical/non-deferrable defect remains, known limitations and claim ceiling are explicit, and no timer/scorer/new skill/scheduler/persistence service/scope reduction entered the candidate. **Validation:** the exact command set above and final OpenSpec status/readback.
