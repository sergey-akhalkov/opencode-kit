# Task 2.1 Loaded Main Source

- Candidate: `opdc-loaded-main-r2`
- Environment: `windows-node-24.18.1-opencode-1.18.25-instruction-source-r2`
- Result: complete at the source/provider-free boundary
- Runtime ceiling: the current OpenCode session loaded pre-edit instructions; configured loaded behavior remains unproved until task 2.2

## Production Contract

- `global/AGENTS.md` now owns one compact outcome-preserving delivery-checkpoint contract.
- Existing similar-attempt stagnation remains its own checkpoint path.
- Additional triggers are limited to different defects at the same costly late boundary, coarse unchanged-prerequisite invalidation, failed-costly repetition after only local correction, and observed repeatable setup/validation cost with declared remaining cardinality and explicit dominant-cost evidence.
- Elapsed time, task counts, one cheap failure, advancing long work, and irreducible required work remain exclusions.
- Checkpoints preserve the outcome, envelope, oracle, population, invariants, independent work, live-attempt/safety gates, and protected authority. Product/proof semantic changes stay at the owner boundary; ordinary process choice does not become a question.
- Unchanged checkpoint identity suppresses duplicates. The contract reuses current stagnation, Change-Ready, behavioral-substitution, OpenSpec, grind-frontier, compaction, and Kaizen owners.
- Project/reusable templates carry pointer-only role deltas and add no trigger or scheduler.

## Practice Owner Disposition

- Reviewer session: `ses_fab698ab8ffe2kJ2xVKTdX430a`
- Effective Model: `xai/grok-4.6`
- Candidate reviewed: `opdc-loaded-main-r2`
- `IA-OPDC-R2-01`: confirmed. The first draft broadened dominant-cost activation beyond the canonical cardinality-backed setup/validation case. Corrected in global authority and fail-closed marker tests.
- `IA-OPDC-R2-02`: confirmed. The first mutation oracle did not require the proof-scope owner-boundary clause. Corrected by requiring and mutation-testing that exact clause.
- Reviewer residuals for similar-attempt ownership and `unknown` disposition were addressed by naming the existing rule as that pattern's checkpoint and recording `irreducible | unknown` with limiting evidence.

## Validation

- Red-first oracle: the initial focused contracts run failed only the new delivery-checkpoint test because canonical source was absent.
- `node tools/run-focused-test.ts tools/test-contracts.ts`: `OK: contracts tests=74` after implementation and after reviewer corrections.
- `npm.cmd run test:focused:instruction-context`: `OK: instruction context quality tests=15` after implementation and after reviewer corrections.
- `git diff --check` over global authority, mirrors, and focused test: exit `0` (line-ending warnings only).
- Provider-free materialize/replay R2: exit `0`; source digest `f403727e5e83c4a3b348785ef330ee94eecc85e474c90211e7e63760045ee3aa`; evaluation digest `37a1e7c8a2da1c3fb0e5d0c69720fb5d2667b69c50936e37cf026d880e0e0845`; 12 green and 12 deliberate red rows matched; provider/model/network/process/source-write/remote effects remained zero.
- Runtime-source inventory resolved canonical OpenSpec commands/skills to the custom kit source but retained a three-config collision and managed compaction `content-differs`; no synchronization, restart, install, or activation occurred.
