## 1. Provider-Free Contracts And Admission

- [x] 1.1 Implement the one-to-100 slice bound, versioned structured executor result, stop intent, and terminal disposition contracts in the existing mission contract/state owners. **Current Rung:** reviewed source/spec contracts. **Next Real Boundary:** actual installed `roadmap-mission` parser/result decoder against disposable valid, 101-slice, malformed-result, and correlation-mismatch inputs. **Blocker/Unblocker:** none. **Observable Proof:** valid input normalizes; over-bound input rejects before provider/mutation; malformed or mismatched result becomes terminal/unknown rather than retryable. **Authorization/Safeguards:** local provider-free execution only; no repository mutation outside disposable state and no model/remote call. **Restoration/Cleanup:** remove disposable runtime roots after preserving evidence. **Immutable Evidence:** exact argv, inputs, exit codes, stdout/stderr, normalized/result JSON, state transition hashes, environment identity. **Validation:** `npm run proof:roadmap-mission`, `npm run proof:roadmap-state`, and focused contract/static checks applicable to touched production files.
- [x] 1.2 Extend preflight to derive the exact expected active set from remaining `continue` slices and require checkpoint-clean artifacts plus terminal-clear mission lease, process, session, and pending-question ownership. **Current Rung:** existing single-active preflight and new delta spec. **Next Real Boundary:** installed provider-free preflight in disposable real OpenSpec repositories with listed dormant actives, a missing declared active, an unlisted active, overlapping dirt, live ownership, and unreadable ownership. **Blocker/Unblocker:** 1.1 supplies bounded mission/result contracts. **Observable Proof:** only the exact clean dormant set is eligible and every unknown blocks before session creation. **Authorization/Safeguards:** local disposable Git/OpenSpec reads and fixtures; never delete, alter, archive, or clean an existing active change. **Restoration/Cleanup:** deterministic disposable repository removal after capture. **Immutable Evidence:** OpenSpec status JSON, expected/observed ids, Git/checkpoint facts, lease/session/process/question facts, exits, diagnostics, hashes. **Validation:** `npm run proof:roadmap-mission` plus the project-native focused checks selected after SDET owns any required test edits.

## 2. Same-Runtime Grind Executor

- [x] 2.1 Add the project-neutral session executor that accepts only a verified loopback current-runtime URL, checks project and installed capability identity, creates one fresh parentless grind-enabled root, invokes the exact `continue` apply command, and writes a bounded versioned result. **Current Rung:** source/type evidence that `PluginInput.serverUrl`, SDK session creation/command APIs, and grind metadata are available. **Next Real Boundary:** one executor invocation against an installed disposable OpenCode runtime using a deterministic no-product-mutation command fixture and the real completion guard. **Blocker/Unblocker:** 1.1 contracts. **Observable Proof:** the root is created on the supplied runtime, grind is enabled before command execution, no nested server starts, terminal-clear returns `completed`, and root/process cleanup is observed. **Authorization/Safeguards:** loopback local runtime, synthetic project, no protected/remote effect, one writer, finite timeout. **Restoration/Cleanup:** abort/delete executor-owned disposable sessions, stop fixture processes, preserve unrelated roots. **Immutable Evidence:** runtime/project/plugin identities, root ref/metadata, command/event sequence, guard state, result JSON, exits, stdout/stderr, cleanup facts. **Validation:** installed executor proof mode and applicable type/static checks.
- [x] 2.2 Add exact `propose` then `apply` phase sequencing and map completion-guard/session outcomes to `completed | owner-required | paused | transient | terminal` without treating exit code or model prose as lifecycle authority. **Current Rung:** green 2.1 same-runtime continue boundary. **Next Real Boundary:** disposable same-runtime phase runs covering terminal-clear, provider-transient fixture, malformed status, bounded question owner-required, and interrupted root. **Blocker/Unblocker:** 2.1. **Observable Proof:** each phase/result is correctly correlated; owner-required leaves no live question wait; retryable classification is finite; missing evidence is unknown/terminal. **Authorization/Safeguards:** local synthetic runtime; configured-provider use remains deferred until 4.1; no archive/checkpoint. **Restoration/Cleanup:** terminally close or isolate every created root/process before another case. **Immutable Evidence:** phase refs, messages/events/status, question disposition, result schemas, causes/stacks, cleanup and liveness facts. **Validation:** executor proof/evaluator replay plus applicable focused production checks.

## 3. Visible Operator Launcher And Stop Control

- [x] 3.1 Add the thin mission launcher extension with safe-id-only run/resume/status commands, current-runtime correlation, existing `/pty-open-background-spy` opening, and one named controller PTY through `SHARED_PTY_MANAGER` with `notifyOnExit: false`; register it in installed config/profile validation without adding scheduler logic. **Current Rung:** verified PTY Web UI, shared manager callbacks, TUI command API, and direct-spawn lease incompatibility. **Next Real Boundary:** actual installed OpenCode slash command in a disposable project opening the existing cockpit and launching a harmless fixed controller fixture. **Blocker/Unblocker:** 1.1 and 2.1 provide safe contracts/current-runtime argv. **Observable Proof:** cockpit opens before spawn, the controller tab is live, arbitrary ids/paths/argv reject, manager callback/toast/status correlate, and the interactive root is not held by an unattributed awaited PTY. **Authorization/Safeguards:** local browser/loopback PTY only, fixed kit argv, no shell, no model or project mutation. **Restoration/Cleanup:** stop and clean only launcher-owned fixture PTYs/server UI state; retain bounded logs. **Immutable Evidence:** slash input digest, TUI result, manager identity/list, PTY ref/title/buffer/status, root metadata, exits and cleanup. **Validation:** installed launcher proof plus config/profile validator checks.
- [x] 3.2 Add `/mission-stop`, controller signal handling, prefixed child stdout/stderr streaming, graceful terminal closure, and hard-cockpit-kill/runtime-loss reconciliation to `paused-unknown`. **Current Rung:** visible harmless launcher from 3.1 and structured executor outcomes from 2.2. **Next Real Boundary:** disposable installed controller fixture observed and stopped through slash command, then repeated once with cockpit Kill and once with runtime loss. **Blocker/Unblocker:** 3.1. **Observable Proof:** graceful stop prevents successor start and records `paused` only after closure/isolation; every child stream is visible in the controller tab; hard kill/runtime loss blocks resume until liveness reconciliation. **Authorization/Safeguards:** local disposable processes only; one stop request; bounded shutdown; no broad process kill or unrelated PTY cleanup. **Restoration/Cleanup:** prove terminal cessation or revoke isolated write authority before removing each fixture; retain unknown state if proof is unavailable. **Immutable Evidence:** stop source/signal, process tree and session refs, prefixed buffers, last transition, reconciliation report, unblock condition, exits and cleanup. **Validation:** launcher/controller stop proof and preserved-bundle evaluator replay.

## 4. Controller Integration And Runtime Proof

- [ ] 4.1 Integrate structured executor results into the existing controller and prove the smallest complete one-slice `continue` happy path through current-runtime grind execution, deterministic verification, archive, post-readback, and authorized local commit. **Current Rung:** provider-free controller and installed executor/launcher boundaries from 1-3. **Next Real Boundary:** bounded configured-provider one-slice mission in a disposable generic repository through the installed slash command. **Blocker/Unblocker:** 1.2, 2.2, and 3.2. **Observable Proof:** cockpit shows controller and any agent PTYs/child streams; one exact active change completes apply/archive/readback/local commit; state reaches terminal complete with no remote effect. **Authorization/Safeguards:** standing authorization for minimum synthetic model calls; local-commit mission authorization; safe synthetic input; one writer; finite attempts; no push/deploy/release. **Restoration/Cleanup:** close sessions/PTYs, verify process cessation and clean disposable repository after preserving the raw bundle. **Immutable Evidence:** Product Candidate/Environment identities, exact slash/controller/executor invocations, model/session refs, PTY buffers, transitions, OpenSpec/Git before/after, validation/archive output, commit id, cleanup. **Validation:** evaluator verdict over raw evidence, `npm run proof:roadmap-controller`, `npm run proof:roadmap-provider`, and affected project-native checks. This task establishes `MVP` only when the actual observations are green.
- [ ] 4.2 Enable declared-slice auto-chain and prove one `propose` slice followed by one `continue` slice with the exact queued dormant active set and no operator confirmation or successor invention. **Current Rung:** current one-slice MVP from 4.1. **Next Real Boundary:** installed two-slice configured-provider disposable campaign in the same cockpit/runtime. **Blocker/Unblocker:** 4.1. **Observable Proof:** first archive/checkpoint durably precedes second activation, both fresh roots use exact changes, queued-active rules remain green, campaign stops complete after slice two, and no third outcome is generated. **Authorization/Safeguards:** same bounded synthetic-provider/local-commit envelope as 4.1; serial writer; immutable campaign digest; no target project. **Restoration/Cleanup:** terminally close all roots/PTYs/processes and remove the disposable repository after capture. **Immutable Evidence:** ordered transition chain, root/PTY refs, change artifacts/status, archive/readback/commit facts, queue exhaustion and cleanup. **Validation:** complete mission evaluator, roadmap controller/provider proofs, and affected static checks; production mutation returns the candidate to `development` until this proof is green.
- [ ] 4.3 Complete owner-required, bounded autonomous question, compaction, local blocker recovery, unlisted/dirty active rejection, malformed result, mid-slice crash, hard kill, and unknown-writer lanes; replay every evidence-only failure through terminal evaluator/finalization before any repeated configured-provider attempt. **Current Rung:** green two-slice campaign from 4.2. **Next Real Boundary:** nearest safe lane per case, preferring preserved/offline replay, then local deterministic runtime, and only the minimum configured-provider case where real model behavior is required. **Blocker/Unblocker:** 4.2; any evidence-only failure blocks its live lane until preserved-corpus replay is terminal-green or names the missing raw observation. **Observable Proof:** autonomous bounded questions continue; owner-only questions stop with a fresh-root resume handoff and no live wait; local blockers use a changed mechanism; all unknown writer/crash/kill cases fail closed without successor mutation. **Authorization/Safeguards:** local disposable effects, bounded synthetic model use only where necessary, no repeated high-cost attempt without unlock evidence. **Restoration/Cleanup:** each mutation-capable attempt must be terminal or isolated; preserve failed bundles before cleanup. **Immutable Evidence:** per-lane raw bundles, causal changes, replay coverage/results, guard/question/compaction state, process/session liveness, transitions, cleanup and unlock condition. **Validation:** terminal evaluator/finalization replay across the complete preserved corpus plus applicable roadmap/guard proof commands.

## 5. Installation, Diagnostics, And Operator Contract

- [ ] 5.1 Update kit config templates, installer/profile mirrors, validators, doctor unattended readiness, CLI/help/proof inventory, and operator documentation for the launcher/executor/cockpit contract, including exact status/stop behavior and rollback. **Current Rung:** runtime behavior proven through 4.3 but not yet fully installed/documented as one candidate. **Next Real Boundary:** fresh profile installation into a disposable config source followed by actual `--help`, doctor, source inventory, OpenCode loader, cockpit launch, and one no-mutation status command. **Blocker/Unblocker:** 3.2 and 4.3. **Observable Proof:** fresh install loads one PTY bridge, one guard, and one launcher; help has zero effects and exits zero; doctor reports supported readiness; docs match executable commands and fail-closed states. **Authorization/Safeguards:** local disposable installation/config only; preserve the user's active config and unrelated files; no activation against a consumer project. **Restoration/Cleanup:** restore disposable config/environment and stop loaded processes; retain install/readback evidence. **Immutable Evidence:** source/generated digests, install argv/exits, config readback, loader inventory, help/doctor stdout/stderr, cockpit/status observations, cleanup. **Validation:** `npm run test:focused:install`, `npm run proof:project-unattended`, `npm run doctor -- --format json`, and applicable config/contract validators after SDET-owned test updates.

## 6. Complete Candidate Proof And Critical SDET

- [ ] 6.1 Run the full installed disposable proof against one readable Product Candidate and freeze its Candidate Reference, Environment Identity, raw bundles, evaluator verdicts, cleanup, and known non-critical limitations. **Current Rung:** complete accepted production scope and installed path from 5.1. **Next Real Boundary:** installed configured-provider two-slice operator workflow plus all provider-free/local interruption lanes using the same candidate/environment identity. **Blocker/Unblocker:** 5.1 and a clear live-attempt gate from 4.3. **Observable Proof:** every delta-spec scenario has correlated green evidence or an exact owner-blocked path; happy path, visibility, stop/recovery, queued actives, archive/local commit, question/compaction/blocker behavior, and final cleanup are terminal. **Authorization/Safeguards:** standing bounded synthetic-call authorization, disposable local repository/config, finite attempts, no consumer/remote/protected effects. **Restoration/Cleanup:** verify every writer/process/session/PTY terminal or isolated before deleting disposable state; retain immutable redacted bundles. **Immutable Evidence:** Candidate/Runner/Evaluator/Environment identities, exact invocations/input, exits, stdout/stderr, PTYs, sessions, transitions, Git/OpenSpec effects, cleanup and verdicts. **Validation:** complete proof inventory commands and evaluator replay; restore `MVP` only on green current proof.
- [ ] 6.2 Dispatch one fresh test-only Material SDET against the Candidate Reference from 6.1 to challenge only reachable critical business incidents and author any smallest critical automated regression oracle. **Current Rung:** current MVP proof and complete accepted scope from 6.1. **Next Real Boundary:** fresh-context SDET execution against the disposable real runner and preserved evidence, preferring real boundaries over mocks. **Blocker/Unblocker:** 6.1; unavailable/unusable SDET leaves the candidate at MVP and is recorded rather than replaced by production-authored tests. **Observable Proof:** SDET returns exactly `critical-risks-reported`, `no-critical-risk`, or `blocked` with Effective Model and candidate attribution; any test artifact is test-only and critical. **Authorization/Safeguards:** no production/config/instruction edits by SDET, no nested agents, no remote/protected effects. **Restoration/Cleanup:** SDET runner uses the existing deterministic cleanup and does not leave a writer live. **Immutable Evidence:** SDET report, risk/oracle matrix, changed test-only paths, exact checks, model provenance, candidate ref. **Validation:** main inspects and independently reproduces every reported critical row.
- [ ] 6.3 Main-disposition the SDET result: if a critical/non-deferrable defect is reproduced, make only its smallest production correction, regain current runtime proof, and invoke a new fresh SDET only when the canonical continuation predicate is met; otherwise record the terminal no-critical or blocked state and park non-critical findings. **Current Rung:** SDET evidence from 6.2. **Next Real Boundary:** exact reproducer for each critical claim, then affected installed proof lane after any correction. **Blocker/Unblocker:** 6.2; the first precondition-valid attempt without a confirmed critical defect permanently ends SDET for this root. **Observable Proof:** every plausible non-deferrable row is reproduced, disproved, or shown unreachable; corrected candidates have new proof; no reviewer severity alone authorizes mutation. **Authorization/Safeguards:** same local disposable envelope; no optional polish or scope expansion. **Restoration/Cleanup:** close every reproduction writer and preserve pre/post-correction bundles. **Immutable Evidence:** main reproduction/disposition, causal diff, affected proof replay, SDET terminal history, parked limitations. **Validation:** affected proof/evaluator lanes and focused checks for any corrected candidate.

## Admitted Proof Corrections

- [x] SI-1 Consolidate proof-owned Windows process-tree and fixture cleanup used by the current runtime gate. **Impact Horizon:** Working Repository. **Concrete Consumers:** current `tools/proofs/roadmap-mission-runtime.ts` task 2.2 lane and existing `tools/proofs/session-completion-guard-restart.ts` installed guard restart lane. **Execution Class:** gate-closer. **Earliest Safe Point:** before another task 2.2 live capture. **Invalidated Evidence:** cleanup/finalization verdicts from `evidence-task-2-1-runtime-r1`, `evidence-task-2-2-propose-r1`, `evidence-task-2-2-propose-r2`, and `evidence-task-2-2-server-preflight-r1`; green task 1 provider-free evidence is unaffected. **Observable Payback:** the independently supervised guard preflight and mission runtime runner both terminate, delete their proof-owned fixtures, and produce terminal evaluator evidence instead of `EBUSY` or outer timeout. **Trigger/Evidence:** two different installed-runtime proof runners reproduced Windows fixture-lock cleanup failure; the guard runner reached `retention-server-1-ready`, proving startup while cleanup failed. **Why:** current task 2.2 cannot distinguish executor behavior until the shared local runtime cleanup boundary is reliable. **Prerequisites:** all correlated prior attempts are terminal and no proof fixture remains. **Scope/Non-Goals:** proof-owned local processes/fixtures only; no product runtime cleanup policy, unrelated proof migration, broad process kill, or test oracle changes. **Implementation:** add the smallest shared helper using standard Node process/filesystem APIs, reuse it in exactly the two named consumers, and record `reuse + narrow extension`; cross-project discovery is not applicable. **Observable Proof:** independently supervised guard restart preflight and one mission runtime scenario both reach terminal cleanup with no correlated process or fixture. **Validation:** syntax checks, each consumer's provider-free evaluator/replay, and process/fixture readback.

- [x] SI-2 Isolate installed proof-server config/home authority and reuse installed runtime dependencies without network. **Impact Horizon:** Working Repository. **Concrete Consumers:** `tools/proofs/roadmap-mission-runtime.ts` task 2.2 installed-runtime lanes and `tools/proofs/session-completion-guard-restart.ts` provider-free loaded-guard preflight. **Execution Class:** gate-closer. **Earliest Safe Point:** before another installed server attempt. **Invalidated Evidence:** all prior installed-runtime captures whose environment merged host config, downloaded/reified a dependency, disabled configured plugins, or initialized unrelated defaults, including task 2.1 r2, task 2.2 propose r3, and isolated preflight r6; provider-free task 1 evidence is unaffected. **Observable Payback:** the server loads isolated config within 15 seconds and configured plugins within a further 60 seconds using abortable probes, with loaded guard controls, unrelated default services disabled, zero inherited inline/custom config authority, no ripgrep download or fresh config dependency reify, and complete process/fixture cleanup. **Trigger/Evidence:** readiness diagnostics r3/r4 showed host config plus a GitHub ripgrep download; owner r3 showed pure-mode guard suppression; owner r6 and later startup attempts showed local configured-plugin initialization varying across blind 30/45-second bounds; OpenCode 1.18.18 source confirms all mechanisms and supported isolation switches. **Why:** host config merging, proof-time network/dependency/default-plugin preparation, pure-mode configured-plugin suppression, and blind duplicated readiness semantics made the installed boundary non-deterministic or removed the product under proof. **Prerequisites:** cause-rich diagnostics are preserved, no proof-owned server remains, and the pinned kit ripgrep plus `global` package/lock/node_modules tree are readable. **Scope/Non-Goals:** disposable proof environments only; no host config/cache mutation, dependency installation, product command-timeout change, configured provider call, or remote effect. **Implementation:** extend the existing proof-client helper with one explicit isolated server environment, dependency seeder, and staged readiness constants; clear inherited `OPENCODE_CONFIG`/`OPENCODE_CONFIG_CONTENT`; explicitly keep pure mode off while disabling auto-update/model fetch and unrelated default plugins; isolate XDG/home roots; prepend installed ripgrep; link pinned kit dependencies; use one 15-second config milestone plus 60-second configured-plugin stage with abortable five-second probes in exactly the two consumers. **Observable Proof:** loaded-guard preflight records `enable-grind`/`disable-grind`, omits host config and ripgrep download, records readiness at or below 75 seconds after the required config milestone, and one owner-required runtime capture records exactly one arbiter plus command/handoff primary calls with terminal cleanup. **Validation:** syntax/help checks, provider-free guard preflight, owner capture/evaluator replay, and process/fixture readback.

## 7. Qualification And Handoff

- [ ] 7.1 Run complete project-native validation, strict OpenSpec consistency, installed loader/doctor checks, and local handoff for the terminal Material candidate; freeze the next RC only if validation is green and no known reachable critical/non-deferrable defect remains. **Current Rung:** terminal SDET disposition and current proof from section 6. **Next Real Boundary:** full local validation and final installed readback on the same candidate; no consumer mission is authorized. **Blocker/Unblocker:** 6.3. **Observable Proof:** `npm test`, `npm run validate:strict`, `npm run openspec:validate`, applicable proof commands, doctor/loader readback, and strict change validation are green; handoff records candidate, proof, SDET, limitations, rollback, and no external operations. **Authorization/Safeguards:** local validation only; no commit/push/archive/release unless separately requested; preserve unrelated `.serena/project.yml` work. **Restoration/Cleanup:** stop validation processes and leave no disposable runtime/project state. **Immutable Evidence:** command/exits/stdout/stderr, validation report, final diff/candidate digest, cleanup, handoff and RC history. **Validation:** the commands named above plus `openspec validate add-autonomous-roadmap-mission-runtime --strict`.
