# Task 2.3 Integrated Proportional Routing Evidence

## Outcome And Ownership

- Candidate: `continuous-complexity-management-routing-r1`.
- Environment: repository instruction source plus disposable generated `core`, Windows, Node `24.18.1`, OpenCode `1.18.23`; provider-free loader debug only.
- Decision: `extend` the existing architecture, project-template, exhaustive-audit, ledger, and loader-proof owners. No new owner, reviewer, skill, score, evaluator, package script, or route was introduced.
- `global/AGENTS.md` replaces its previous architecture bullet with one ordered proportional route: direct zero-pressure work; exact architecture Practice Owner for a seam/mixed-responsibility trigger; focused `complexity-management` only for an explicit existing-project assessment or unresolved current comprehension pressure after foraging/owner routing; explicit exhaustive coverage through discovered audit/ledger owners; visible unavailable states without source guessing or coverage approximation.
- `instructions/reusable-project-agent-instructions.md` and `templates/project/AGENTS.md` contain only the project-facing role delta and preserve global ownership.
- `codebase-audit-loop` and `codebase-audit-ledger` add exactly one review-only `Complexity Pressure Matrix` row with area/subsystem, scenario, pressure, evidence, owner, entrypoint, admission class, and focused-scenario reference. It creates no per-block map/rehearsal and authorizes no mutation.

## Structural Proof

- Contract readback: exit `0`; stdout `OK: complexity-routing global=1 project=2 matrix-fields=8 review-only=1`; stderr empty.
- The global route contains exactly one `Proportional context-efficient architecture` marker, `Zero-trigger work launches no owner`, the maintained line-count marker, focused/exhaustive unavailable states, and no duplicate same-seam launch.
- Both project-facing surfaces name focused and project unavailable behavior without copying the full focused workflow.
- The audit and ledger surfaces contain all exact matrix fields plus review-only/no-mutation constraints.
- `npm run instruction:inventory -- --format markdown`: artifacts `74`, context quality passed, safe/deterministic/review-only findings `0`.
- `npm run instruction:canonicalize -- --check .`: changed files `0`, duplicate exceptions `26/26`, findings `0`.

## Loaded Provider-Free Boundary

- Final invocation: `npm run proof:runtime-surface-loader -- --candidate-id continuous-complexity-management-routing-r1 --evidence-root openspec/changes/add-continuous-complexity-management/implementation-evidence/task-2-3-runtime-core-r2 --profile core`.
- Result: `passed`; `skillStatus=0`, `agentStatus=0`, `complexityRoutingTrigger=true`, missing/extra core skills `[]`, hidden parent hits `[]`, permission failures `[]`, helper/skill paths all under `<generated>`.
- Effects: disposable generated core/project/XDG roots and local debug processes only; retained bundle records `cleanup=complete`.
- The first `task-2-3-runtime-core/` capture proved the integrated marker before canonical engineering-marker corrections. The final r2 capture follows those corrections and is authoritative; both bundles remain retained.

## Run-Observe-Correct Record

- The first one-off contract command used Markdown backticks inside a PowerShell double-quoted argument and failed in PowerShell parsing before Node or source effects. The corrected readback removed shell-sensitive literals and passed.
- Strict validation first reported the canonical markers `Line count is a navigation signal, not a quota` and `Zero-trigger work launches no owner` missing after sentence consolidation. Their exact wording was restored in the same bullet without adding policy copies; strict validation then passed.
- One full library run exceeded the 120-second tool timeout during existing doctor tests. A `Win32_Process` query found no live `node.exe` command for `tools/test-library.ts`, establishing terminal writer/process closure before a causally unchanged rerun with a sufficient 300-second tool timeout. The rerun completed normally.

## Validation And Claim Ceiling

- `node tools/test-library.ts`: exit `0`; `OK: library tests=177`.
- `npm run validate:strict`: exit `0`; skills `33`, agents `21`, markdown `874`, warnings `0`, infos `2`.
- Actual core loader: exit `0`, final evaluation digest retained in `task-2-3-runtime-core-r2/evaluation.json`.
- No current-session instruction reload, installed-source replacement, configured model call, semantic assessment, fixture refactor, or population member was performed. The supported ceiling is integrated source and generated-core route availability with exact review-only project-mode output; semantic routing effectiveness remains unknown.
