## 1. Ownership And First Fact Boundary

- [x] 1.1 Re-read current active-change ownership and current instruction/tool identities, then record one exact non-overlapping implementation manifest or dependency/transfer order for `continuous-complexity-management-v1`; verify `openspec change-inventory`, scoped Git status/diff, active foundation/campaign artifacts, and the resolved `OPENCODE_CONFIG_DIR` leave no unknown shared writer or owner. **Current Rung:** planning evidence. **Next Real Boundary:** provider-free inventory CLI. **Blocker/Unblocker:** mutation is blocked until `replace-instruction-limits-with-context-quality` has applied or this change has explicitly rebased; unresolved overlap blocks only affected paths until transfer, dependency, or narrower ownership is recorded. **Observable Proof:** ownership inventory, `npm run instruction:inventory -- --format markdown`, `npm run instruction:canonicalize -- --check .`, and source readback agree on every planned path and context-quality status. **Owner Authorization:** local read/planning only. **Safeguards:** no active-change edits, install, activation, consumer mutation, remote action, or replacement size ceiling. **Restoration/Cleanup:** preserve all unrelated work. **Expected Immutable Evidence:** implementation manifest and preflight output.
- [x] 1.1.1 Run one current bounded falsification episode for planning candidate r3 plus the task-1.1 ownership graph, grounded independently in the accepted project-neutral complexity outcome; attempt all six task-fit classes, admit only complete current material rows, correct the smallest confirmed defect, and serialize the terminal privacy-safe record before production mutation. **Current Rung:** current reviewed planning and conflict-free ownership. **Next Real Boundary:** provider-free inventory schema. **Blocker/Unblocker:** one fresh `implementation-readiness-reviewer`; at most one fresh corrected-candidate re-review only if a confirmed correction changes the challenged decision surface. **Observable Proof:** original request and candidate are separately referenced, all six attack classes have explicit states, every material finding has main disposition/correction evidence, no optional work is added, and the apply gate accepts the compact record. **Owner Authorization:** one read-only specialist observation plus local planning/evidence correction. **Safeguards:** no raw request/reviewer transcript persistence, third generic review, finding-count target, reviewer authority, production mutation, install, activation, or remote action. **Restoration/Cleanup:** no runtime state; preserve attributed record. **Expected Immutable Evidence:** reviewer/session/model identity, falsification matrix, main disposition, current record, and operation-gate output.
- [x] 1.2 Define the versioned complexity-foraging input/output schema and disposable cohesive/noisy/unsupported/unreadable fixture records, including reviewed scope classes, candidate detector evidence, privacy-safe root identity, stable ordering, partial/blocked support, nested-IO causes, cancellation, and explicit entry/byte/time bounds; verify deterministic schema round-trip without creating the future CLI. **Current Rung:** source/schema fixture. **Next Real Boundary:** CLI scan of a disposable project. **Blocker/Unblocker:** task 1.1 owns paths. **Observable Proof:** valid fixture records parse and round-trip to the exact reviewed shape and ordering while intentional invalid schema-version, scope-class, root-identity, detector-evidence, support-state, cause, and bound records are rejected by disposable schema validation for the named field. **Owner Authorization:** local disposable fixture/schema files only. **Safeguards:** no CLI implementation, semantic score, source payload, absolute private path, network, or target-project write. **Restoration/Cleanup:** fixture temp roots are removed after synchronous process exit. **Expected Immutable Evidence:** schema fixtures and deterministic round-trip/invalid-record output.
- [x] 1.3 [automation-dividend] Implement the smallest `global/bin/complexity-foraging-inventory.ts` happy path for effect-free `--help`, explicit root, default/reviewed scope, candidate facts, JSON output, and fallback state while reusing only contract-compatible traversal/redaction/rendering owners and remaining self-contained or importing only exact `global/bin` files shipped in the same profile; run it against the cohesive and noisy fixtures and inspect exit status, stdout, stderr, counts, exclusions, candidate evidence, effects, and cleanup. **Current Rung:** provider-free local CLI. **Next Real Boundary:** loaded focused workflow consuming its output. **Blocker/Unblocker:** task 1.2 schema. **Observable Proof:** help creates no files, the cohesive fixture identifies maintained roots/candidates, and the noisy fixture isolates declared corpus/evidence while retaining counts and the absence-warning. **Owner Authorization:** local tool and disposable fixture mutation. **Safeguards:** explicit bounds, stable traversal, privacy redaction, no semantic inference, target edit, or import from repository-only `tools`. **Restoration/Cleanup:** no persistent runtime state; disposable roots removed after terminal process closure. **Expected Immutable Evidence:** raw help/happy-path bundles and schema readback.

## 2. Focused Workflow Happy Path

- [x] 2.1 Add one thin main-executed `complexity-management` skill for focused Architecture Comprehension Map, Change Rehearsal, abstraction-value ladder, finding admission classes, and same-scenario recheck; narrow adjacent `code-quality-audit` and `service-architecture-design` descriptions plus README/catalog routing only enough to provide reverse stay-quiet, while keeping `delta`, changed-code smells, new service design, seam-only Practice Owner routing, and exhaustive coverage in current owners; verify one cohesive focused trigger/output and mutually exclusive discovery metadata. **Current Rung:** candidate instruction source. **Next Real Boundary:** profile-available configured workflow. **Blocker/Unblocker:** task 1.3 provides fact input. **Observable Proof:** a cold reader and catalog can identify one owner for focused project comprehension without loading it for a cohesive delta, changed-code review, new service design, exhaustive request, or seam-only Practice Owner trigger. **Owner Authorization:** local loaded-instruction/catalog source edit. **Safeguards:** no new Practice Owner, architecture score, output-contract transfer, automatic mutation, or exhaustive default. **Restoration/Cleanup:** source diff is the rollback unit; no install in this task. **Expected Immutable Evidence:** scoped skill/description/catalog diff and contract readback.
- [x] 2.2 Add the focused skill and exact self-contained helper import closure to default `core` and `all` profiles before any always-loaded routing references them; keep changed-code/exhaustive skills `all`-only, regenerate/read back profiles/catalogs, and verify `core` resolves focused mode while reporting explicit exhaustive mode unavailable without approximation. **Current Rung:** candidate skill/helper source. **Next Real Boundary:** consolidated loaded routing. **Blocker/Unblocker:** tasks 1.3 and 2.1 provide the exact files. **Observable Proof:** generated `core`/`all` contain the focused skill/helper, custom omission degrades, core exhaustive stays visibly unavailable, and no profile guesses a parent source. **Owner Authorization:** local profile/catalog edits and generation. **Safeguards:** exact helper closure only, no whole `global/bin` core install, no code-quality/audit ownership transfer, and canonical ownership, context quality, and loaded behavior remain hard gates. **Restoration/Cleanup:** generated/profile diff is read back and reversible. **Expected Immutable Evidence:** profile manifests, generated-source inventory, context diagnostics/quality output, and missing-capability fixtures.
- [x] 2.3 Add the exact `Complexity Pressure Matrix` fields to existing review-only codebase audit/ledger rows, then consolidate the existing global/project-facing architecture bullet so zero-pressure work stays direct, current comprehension pressure after exact owner routing uses `complexity-management`, seam-only questions retain the architecture Practice Owner route, and explicit exhaustive requests use the existing audit owner only when discovered; verify exact trigger/owner/profile markers, inventory diagnostics, context quality, and loaded behavior. **Current Rung:** profile-available integrated instructions. **Next Real Boundary:** configured loaded facade scenario. **Blocker/Unblocker:** task 2.2 proves route availability before always-loaded mutation. **Observable Proof:** static readback shows one ordered proportional route, named project matrix fields, no duplicate exhaustive workflow, no automatic audit-and-fix, no mandatory reviewer, and no duplicate authority. **Owner Authorization:** local instruction/template/audit edits only. **Safeguards:** replace overlapping wording rather than append policy copies; preserve all safety/authority rules and do not introduce a size ceiling. **Restoration/Cleanup:** scoped source diff. **Expected Immutable Evidence:** routing/audit diff, instruction inventory/context-quality output, and marker validation output.
- [ ] 2.4 Run the candidate through an actual configured OpenCode root against a disposable useful-facade fixture: provide one consumer currently coordinating several stable internals, request a focused complexity assessment, implement or materialize the approved narrow facade in the fixture, and execute the same representative consumer scenario before and after; inspect tool calls, map/rehearsal, diff, exit status, stdout/stderr, effects, errors, validation, and cleanup. **Current Rung:** configured loaded disposable integration. **Next Real Boundary:** reviewed behavior partition pack. **Blocker/Unblocker:** tasks 1.3 and 2.1 through 2.3; unavailable configured runtime leaves this lane blocked but does not authorize static substitution. **Observable Proof:** candidate uses the portable facts, admits the facade for current encapsulation value, preserves visible failure/effect semantics, and demonstrates a smaller essential consumer context without extra wrappers. **Owner Authorization:** bounded synthetic model calls and disposable local mutation under current standing authority. **Safeguards:** isolated fixture, finite calls/time/output, no credentials in artifacts, no network/remote/protected effects. **Restoration/Cleanup:** prove child/process/writer closure before fixture removal and retain privacy-safe raw bundle. **Expected Immutable Evidence:** baseline scenario, candidate map/rehearsal, candidate diff, runtime outputs, and cleanup record.

## 3. Complete Deterministic And Semantic Scope

- [ ] 3.1 Complete Markdown output, reviewed scope-file handling, invalid scope/version/root diagnostics, unreadable-root failure, nested unreadable partial support, entry/byte/time/cancel blocking, unsupported/unknown states, stable readback, root redaction, and repository wrapper/validation for the inventory; after task 2.4 proof, add the smallest focused regression oracles and run the exact global entrypoint over every provider-free fixture. **Current Rung:** proven helper happy path. **Next Real Boundary:** full loaded behavior population. **Blocker/Unblocker:** task 2.4 preserves the first real signal. **Observable Proof:** all schema/CLI/error/bound/privacy/order scenarios pass with cause-preserving diagnostics, no partial scan becomes complete, and target projects require no package script or mutation. **Owner Authorization:** local source/test edits and disposable execution. **Safeguards:** distinct inventory contract; no generic framework or semantic classifier. **Restoration/Cleanup:** temp roots close and delete; raw failures retained. **Expected Immutable Evidence:** focused test output plus representative JSON/Markdown/error bundles.
- [ ] 3.2 Extend a separately named complexity configured-session fixture pack with exact partitions `cohesive-small-project`, `modular-multi-component-project`, `noisy-corpus-or-evidence-project`, `mixed-owner-module`, `useful-current-consumer-facade`, `frozen-compatibility-and-current-extension`, `redundant-wrapper-chain`, `speculative-generic-abstraction`, `explicit-review-only-project-assessment`, `default-core-availability`, `unreadable-root`, and `unsupported-ecosystem`; verify baseline and candidate use identical prompts/model/variant/permissions/environment and each result preserves trigger, owner, context, path, and maximum-claim facts. **Current Rung:** one loaded facade proof. **Next Real Boundary:** matched partition evaluation. **Blocker/Unblocker:** tasks 2.4 and 3.1. **Observable Proof:** fixture manifests/readback cover every proposal population member by the same exact identifier without embedding the desired semantic verdict in deterministic helper code or changing the existing Practice Owner routing pack. **Owner Authorization:** local fixture and bounded synthetic model-call preparation. **Safeguards:** no fuzzy scoring, hidden prompts, source secrets, or consumer repository writes. **Restoration/Cleanup:** disposable roots and sessions have terminal cleanup records. **Expected Immutable Evidence:** versioned fixture pack and matched invocation manifests.
- [ ] 3.3 Extend the existing configured-session proof evaluator only with schema/readback and exact fact-diff checks for consumer context, public surface, edit/proof sets, effects/errors, and behavior; keep useful-facade, wrapper, and framework semantic expectations in reviewed fixture oracle records and main/reviewer disposition, then verify helper code reports mismatch/unknown without selecting or rejecting a design. **Current Rung:** fixture and raw observation records. **Next Real Boundary:** configured baseline/candidate replay. **Blocker/Unblocker:** task 3.2. **Observable Proof:** deterministic output correlates exact facts and paths while reviewed semantic oracles, not helper inference, distinguish the represented outcomes. **Owner Authorization:** local deterministic proof-harness/test edits. **Safeguards:** no quality score, semantic classifier, refactor authorization, or new evaluator owner. **Restoration/Cleanup:** no external state; preserve raw/evaluated correlation. **Expected Immutable Evidence:** proof-harness tests, reviewed oracle seeds, and terminal fact-diff rows.
- [ ] 3.4 Update exact contracts, validators, package/proof inventory, docs/catalogs, generated-profile readback, consumer-outcome no-regression coverage, and instruction context-quality checks for the new skill/capability and exact portable helper closure; verify project, code-quality, instruction, and OpenSpec inventories retain their prior public contracts, `core` resolves focused but not exhaustive mode, `all` resolves both, adjacent skill discovery stays mutually exclusive, and the new helper remains separately callable. **Current Rung:** complete local candidate behavior. **Next Real Boundary:** configured loaded comparison. **Blocker/Unblocker:** tasks 3.1 through 3.3 and profile availability from 2.2. **Observable Proof:** focused structural/profile/consumer-outcome validation passes, intentional missing-marker/missing-core-helper/forbidden-score/context-quality fixtures fail for the expected reason, diagnostic inventory remains available, and unchanged inventories retain their fixtures. **Owner Authorization:** local kit source/test/docs/profile edits. **Safeguards:** no unrelated refactor, dependency addition, Practice Owner change, active-change mutation, or replacement size ceiling. **Restoration/Cleanup:** scoped diff rollback; no runtime state. **Expected Immutable Evidence:** validator/profile/consumer-outcome/test outputs and inventory compatibility readback.

## 4. Broad Claim And External Case Evidence

- [ ] 4.1 Run matched configured baseline/candidate sessions through the same loaded disposable-project production/comparison path for every reviewed behavior partition, replay preserved raw records through the existing proof harness, and inspect exact attribution, tool calls, outputs, diffs, diagnostics, side effects, and cleanup; correct only reproduced accepted-outcome defects and rerun affected lanes. **Current Rung:** provider-free and one loaded happy path. **Next Real Boundary:** broad-claim challenge. **Blocker/Unblocker:** tasks 3.2 through 3.4; a failed evidence-only configured run requires preserved-corpus terminal replay before another equivalent live call. **Observable Proof:** candidate alone applies proportional modes, useful abstraction gate, negative controls, existing-owner routing, core focused availability with explicit exhaustive unavailability, all-profile project routing, and honest unsupported states across the reviewed population. **Owner Authorization:** bounded configured synthetic model calls only. **Safeguards:** same-model/environment/path comparison, finite bounds, no consumer or remote effects, no unchanged live repetition after evidence failure. **Restoration/Cleanup:** terminal session/process closure and disposable-root cleanup before integration. **Expected Immutable Evidence:** immutable baseline/candidate bundles and terminal comparison matrix.
- [ ] 4.2 Resolve the active global source exactly and run the installed candidate in read-only diagnostic mode against the named `pmac-emulator` checkout with a reviewed scope file; verify it surfaces the typed pipeline/controller/core-adapter benefits, misleading onboarding, frozen/current extension paths, mixed maintained owners, and corpus/evidence/spec pressure while excluding absolute private paths and avoiding build, test, controller, network, or source mutation. **Current Rung:** reviewed generic partitions. **Next Real Boundary:** separate exact-case diagnostic handoff. **Blocker/Unblocker:** task 4.1 and readable external checkout; absence leaves only this diagnostic unknown and does not block or substitute for generic population closure. **Observable Proof:** privacy-safe inventory plus focused map/rehearsal reproduces the bounded diagnostic classes and labels one-case evidence outside the broad population. **Owner Authorization:** read-only local external-directory access. **Safeguards:** no PMAC source change, Cargo invocation, controller contact, network, secret expansion, or project-specific universal policy. **Restoration/Cleanup:** N/A for read-only target; close all child processes and retain only redacted evidence. **Expected Immutable Evidence:** exact invocation, candidate/environment identity, stdout/stderr, redacted inventory/map, and no-mutation Git readback.
- [ ] 4.3 Materialize the current schema-valid `continuous-complexity-management-v1` evidence index from reviewed generic seed observations, then obtain one fresh `evidence-sufficiency-reviewer` challenge against the proposal population, coverage, homogeneous loaded production/comparison paths, environment, oracle, unresolved observations, and maximum claim; main shall disposition every row without broadening implementation authority. **Current Rung:** complete reviewed generic raw lanes. **Next Real Boundary:** final practice/instruction review and project validation. **Blocker/Unblocker:** task 4.1; task 4.2 is separate diagnostic evidence and cannot support or block generic members. Missing observations remain `unknown`/`missing` with non-supported disposition. **Observable Proof:** generated/read-back claim record correlates every generic member to exact loaded artifacts and reviewer evidence supports or narrows each broad dimension. **Owner Authorization:** local evidence writes and one read-only reviewer. **Safeguards:** no invented observation, diagnostic-case substitution, reviewer authorization, or population/generalization claim. **Restoration/Cleanup:** preserve source records and generated drift check. **Expected Immutable Evidence:** evidence-index seed/output, challenge matrix, and main dispositions.

## 5. Integration, Validation, And Handoff

- [ ] 5.1 Obtain bounded maintenance observations from the exact triggered `architecture-and-change-locality`, `simplicity-and-reuse`, and `instruction-governance` owners against the same candidate, then reproduce and disposition each risk/reduction row; verify no report adds scope, a new owner, a score, or reviewer authority. **Current Rung:** broad evidence candidate. **Next Real Boundary:** full project validation. **Blocker/Unblocker:** task 4.3; unavailable optional quality evidence narrows the corresponding practice claim but does not authorize weakened safety or instruction integrity. **Observable Proof:** owner reports cite exact candidate paths and main's disposition closes, disproves, narrows, or parks every row. **Owner Authorization:** read-only specialist observations. **Safeguards:** no nested dispatch, edits, questions, or lifecycle verdicts by reviewers. **Restoration/Cleanup:** no reviewer writes outside any separately allowed feedback ledger. **Expected Immutable Evidence:** attributed reports and main disposition matrix.
- [ ] 5.2 Run the exact installed/loaded entrypoint smoke check, focused inventory/change-locality/instruction tests, `npm run validate:strict`, `npm test`, code-quality and instruction inventories, strict selected/all OpenSpec validation, `git diff --check`, and scoped diff review; inspect exit status/stdout/stderr and resolve only in-scope defects. **Current Rung:** reviewed integrated candidate. **Next Real Boundary:** local handoff. **Blocker/Unblocker:** task 5.1 and terminal writer closure. **Observable Proof:** the same readable candidate passes all applicable project-native checks and loaded routing still produces the proven focused output. **Owner Authorization:** local validation and bounded configured smoke only. **Safeguards:** no install outside active source, commit, push, archive, release, remote action, or destructive cleanup. **Restoration/Cleanup:** validation temp roots close and delete; raw diagnostics retained. **Expected Immutable Evidence:** command matrix, outputs, final Candidate Reference, and scoped diff.
- [ ] 5.3 Complete the local handoff with outcome/non-goals, operating envelope, Candidate/Environment identity, architecture and reuse dispositions, runtime proof, broad-claim ceiling, validation, owner/reviewer state, diagnostics, known non-critical limitations, rollback/restart note, and external-operation state; verify no unfinished accepted task or unsupported universal claim is represented as complete. **Current Rung:** green current candidate. **Next Real Boundary:** a future explicitly accepted consumer-project complexity assessment/refactor. **Blocker/Unblocker:** task 5.2; planning controls remain revisable if evidence requires a semantics-preserving correction. **Observable Proof:** cold-reader handoff traces every accepted requirement to current evidence and clearly separates one-case PMAC evidence from project-neutral workflow behavior. **Owner Authorization:** local reporting only. **Safeguards:** no RC/stable claim unless a separately applicable qualification track earns it; no archive, install, activation, commit, push, deployment, or release. **Restoration/Cleanup:** preserve evidence and leave external projects unchanged. **Expected Immutable Evidence:** final handoff and fresh OpenSpec status.
