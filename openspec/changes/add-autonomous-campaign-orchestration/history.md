# Strategy History

## 2026-08-26 - Keep One Long Grind Root And Rely On Compaction

- **Objective:** Complete multi-day discovered work without adding another durable workflow owner.
- **Approach:** Enable grind on one root, maintain a large todo list, use `session_delivery_context`, compaction summaries, project memory, and one large OpenSpec change until every audit and fix is complete.
- **Evidence:** The completion guard can keep one root moving and preserve bounded redacted evidence, but it does not own a roadmap or survive host/process loss as a source-of-truth workflow database. Project memory is advisory, compaction is bounded prose, and one OpenSpec change cannot safely freeze and checkpoint an unknown number of independently validated remediation outcomes.
- **Outcome:** Rejected.
- **Reason:** Transcript/context would remain the implicit cursor; coverage, writer liveness, dynamic backlog, and restart state could drift or exceed context limits.
- **Do Not Repeat Until:** The runtime provides a durable schema-correlated root workflow database with source-writer leases, project checkpoints, and exact restart reconstruction independent of transcript prose.
- **Evidence-Based Retry Condition:** Maintained installed evidence shows one root can survive process/host restart, retain complete large-scope coverage and dynamic work identities, and resume one source writer without new campaign artifacts or mission composition.

## 2026-08-26 - Put The Entire Audit And Every Fix In One OpenSpec Change

- **Objective:** Avoid campaign/portfolio complexity by using one proposal, design, spec set, and large task list.
- **Approach:** Finish the entire repository audit inside one change, append every finding as another task/spec, implement all fixes, and archive only when the whole repository is clean.
- **Evidence:** OpenSpec is a strong bounded-change owner, but discovered findings have different cohesive outcomes, write owners, proof boundaries, protected effects, and checkpoints. One all-or-nothing change cannot archive verified early outcomes or isolate failures while later findings continue to emerge.
- **Outcome:** Rejected.
- **Reason:** It creates an unbounded context/lifecycle unit, overlapping ownership, stale task/report state, and expensive global invalidation after every correction.
- **Do Not Repeat Until:** The accepted work is known before implementation and shares one cohesive outcome, owner, proof, checkpoint, and rollback boundary.
- **Evidence-Based Retry Condition:** A complete frozen audit demonstrates that all confirmed P0/P1 rows are one inseparable behavior change with one source owner and one validation boundary, rather than a portfolio.

## 2026-08-26 - Let The Roadmap Mission Dynamically Extend Its Queue

- **Objective:** Reuse the existing durable mission without adding a parent campaign controller.
- **Approach:** Allow the mission executor or completion arbiter to read audit/model output, append newly discovered slices, reorder dependencies, and continue after the original queue is exhausted.
- **Evidence:** `unattended-roadmap-orchestration` intentionally requires one immutable 1..100-slice definition, deterministic preflight, no model-derived successor, and one writer/checkpoint chain. Its replay and safety proof depend on those invariants.
- **Outcome:** Rejected.
- **Reason:** Dynamic queue mutation would make model prose lifecycle authority, invalidate mission replay/correlation, and merge discovery policy with source execution.
- **Do Not Repeat Until:** The mission capability is explicitly replaced with a different accepted contract and equivalent fail-closed dynamic-admission/replay evidence exists.
- **Evidence-Based Retry Condition:** A deterministic non-semantic admission mechanism can mutate a live mission queue while preserving exact intent, effects, writer ownership, checkpoints, restart replay, and all current manual mission compatibility; current evidence supports a parent freeze/handoff instead.

## 2026-08-26 - Use The Markdown Audit Report As Campaign State

- **Objective:** Keep campaign artifacts human-readable and avoid a structured ledger/materializer.
- **Approach:** Store file coverage, findings, resolutions, OpenSpec backlog, validation, residual risks, and current phase in one large Markdown document edited by each session.
- **Evidence:** `docs/feedbacks/audit-opencode-kit-2026-06-27.md` contains resolved findings alongside stale residual statements and manually maintained follow-up groups. The existing audit-ledger skill explicitly requires changed-block re-review but provides no executable projection/readback owner.
- **Outcome:** Rejected.
- **Reason:** One mutable narrative cannot reliably provide atomic transitions, stable identities, deterministic totals, stale-row detection, or restart cursor authority.
- **Do Not Repeat Until:** A validated Markdown schema can round-trip every seed fact and derived view without duplicate independently editable state.
- **Evidence-Based Retry Condition:** A maintained parser/materializer proves Markdown is the single normalized seed and rejects all stale/mixed variants with the same clarity and atomicity as structured records; otherwise Markdown remains a derived report.

## 2026-08-26 - Use A Recursive Agent Swarm As The Orchestrator

- **Objective:** Parallelize repository audit, planning, fixes, testing, and review with minimal controller code.
- **Approach:** Let audit agents dispatch more agents, allow writers to work in parallel, and trust summaries returned through the parent transcript to merge findings and decide completion.
- **Evidence:** Current agent contracts prohibit nested orchestration for leaf roles, require active-primary ownership, terminal writer closure, isolated/non-overlapping scopes, and main integration. A recursive swarm cannot durably prove child/process/write liveness after interruption or prevent two discovered fixes from overlapping.
- **Outcome:** Rejected.
- **Reason:** It replaces explicit state and leases with distributed transcript memory and creates unbounded coordination, self-review, and concurrent-writer risk.
- **Do Not Repeat Until:** The runtime exposes durable parent/child identity, isolated write authority, terminal liveness, persisted fan-in, and deterministic conflict handling for recursive execution.
- **Evidence-Based Retry Condition:** Installed evidence proves recursive workers reduce total time while preserving exact campaign state, no overlapping writes, complete coverage, and restart-safe fan-in better than bounded controller-owned read-only partitions.

## 2026-08-26 - Require Owner Approval Before Every Remediation Wave

- **Objective:** Prevent dynamic discovery from silently expanding scope or crossing protected effects.
- **Approach:** Pause after findings freeze and after every re-review so the owner approves work-item severity, grouping, OpenSpec changes, task order, and continuation.
- **Evidence:** The user explicitly selected autonomous wave creation/execution inside a fixed policy. Current authority permits main to update planning controls and dependency closure without owner approval when accepted semantics remain unchanged; protected product/effect choices remain separately owner-bound.
- **Outcome:** Rejected.
- **Reason:** Routine wave approval adds a manual multi-day bottleneck without improving safety for already preauthorized, finding-traceable P0/P1 work.
- **Do Not Repeat Until:** The accepted campaign policy changes or evidence shows deterministic admission plus protected-effect gates cannot prevent a specific unauthorized wave.
- **Evidence-Based Retry Condition:** A reproduced campaign admits a wave outside the accepted outcome/effects despite current freeze, traceability, reconciliation, ownership, and effect checks; then narrow only the failed decision boundary.

## 2026-08-26 - Support Every Host Platform And Playbook In The First Increment

- **Objective:** Make the mechanism universally reusable at initial delivery.
- **Approach:** Implement Windows Scheduled Task, systemd, launchd, multi-repository coordination, arbitrary discover/plan/execute/verify playbooks, and target-specific adapters in one change.
- **Evidence:** The current real host owner and recovery proof are Windows-specific; the user needs reuse across projects, not an unproved claim across every OS and workflow. Only audit-remediation has a concrete severity, coverage, and closure oracle in this discussion.
- **Outcome:** Rejected and narrowed.
- **Reason:** Cross-platform and arbitrary-playbook scope would delay the first complete real path, multiply protected lifecycle surfaces, and produce untested abstractions.
- **Do Not Repeat Until:** The Windows/reference campaign is working and another host or playbook is an accepted current requirement with a real boundary.
- **Evidence-Based Retry Condition:** A second maintained consumer demonstrates the same campaign core with one materially different host or playbook contract and identifies the minimal adapter seam from evidence.

## 2026-08-26 - Add A Separate Campaign Executor That Bypasses Roadmap Mission

- **Objective:** Simplify parent/child coordination by letting the new controller create OpenSpec changes, edit source, validate, archive, and checkpoint directly.
- **Approach:** Copy or reimplement mission process, session, writer, archive, checkpoint, retry, completion-guard, and proof behavior inside the campaign.
- **Evidence:** The mission already owns and has qualified those exact failure-prone boundaries. The architecture review identified duplicate orchestration/writer ownership as the primary risk and recommended frozen-wave input to the existing executor.
- **Outcome:** Rejected.
- **Reason:** It would create two mutation schedulers, two lease/state systems, duplicate archive/proof code, and ambiguous recovery after interruption.
- **Do Not Repeat Until:** The roadmap mission is removed or proven unable to execute a required frozen wave even after a narrow parent-correlation extension.
- **Evidence-Based Retry Condition:** A provider-free real OpenSpec fixture demonstrates an irreducible mission contract mismatch that cannot be solved with parent/work-item correlation and terminal handoff while preserving mission invariants.

## 2026-08-26 - Durable Campaign Above Frozen Missions With Windows Recovery

- **Objective:** Autonomously drive multi-day discovered work across bounded contexts and restarts while preserving scope, owner authority, and one source writer.
- **Approach:** Add one static-envelope/dynamic-portfolio campaign controller, normalized append-only state and ledger, isolated read-only semantic roots, fresh P0/P1 reconciliation and investigation, deterministic frozen-wave admission, existing mission execution, changed-block re-review, derived report, evidence-based convergence, and a separate portable supervisor with a thin protected Windows logon adapter.
- **Evidence:** Current guard, OpenSpec change, mission, audit-ledger, doctor, workstation, and proof owners each provide necessary lower-level contracts but no owner composes the complete lifecycle. User decisions authorize autonomous in-policy waves, Windows auto-resume, P0/P1-only remediation, and material-quality P1. The fresh architecture review confirmed the parent/mission separation, non-authoritative report, explicit unknown/effect gates, source-writer handoff, and active-change serialization as required risk controls.
- **Outcome:** Selected for proposal, delta specifications, design, and tasks.
- **Reason:** It is the smallest complete architecture that meets the accepted multi-day outcome without weakening the mission, foundation recovery, completion guard, protected boundaries, or unrelated work.
- **Do Not Repeat Until:** N/A - selected strategy.
- **Evidence-Based Retry Condition:** Provider-free contracts, real mission handoff, configured semantic proof, or installed Windows recovery disproves a specific selected boundary; revise only that boundary and preserve unaffected evidence rather than returning to a rejected broad mechanism.

## 2026-08-27 - Add A Focused Campaign Pack Beside The Mission Runner

- **Objective:** Establish the provider-free campaign fixture and evaluator contract without duplicating the roadmap capture framework or mixing campaign semantics into a mission-only owner.
- **Approach:** Build the smallest declared `tools/proofs/work-campaign.ts` sibling with reviewed seed records under the existing consumer-outcome fixture tree, create-new raw/evaluation bundles, zero-live replay, and the maintained proof inventory/package surfaces. Reuse the existing stable JSON, immutable evidence, candidate correlation, effect accounting, replay, and cleanup conventions; do not add another process, provider, Git, OpenSpec, or redaction framework.
- **Evidence:** `tools/proofs/roadmap-mission.ts` directly owns `MISSION_SOURCE_PATHS`, mission definitions, disposable mission projects, mission preflight scenarios, and mission-specific replay checks. Campaign definition/report/wave seeds are a distinct evolving responsibility and later add separate state, semantic, mission-composition, and host lanes. Adding those records to the mission runner would couple campaign-only invalidation to established mission replay and worsen its mixed responsibility.
- **Outcome:** Selected `build-minimal` sibling inside the existing proof family; current owner is the declared campaign proof path, and cross-project discovery is not applicable because this is repository-local proof glue over existing kit conventions.
- **Reason:** The focused sibling preserves one discoverable campaign family and one immutable-bundle convention while keeping established roadmap behavior and replay independent.
- **Do Not Repeat Until:** N/A - selected task 1.2 locality decision.
- **Evidence-Based Retry Condition:** A current shared proof primitive emerges whose contract exactly owns campaign seed evaluation and byte-stable replay without importing campaign semantics into mission or consumer behavior owners.

## 2026-08-27 - Build Minimal Campaign Contracts And Reuse Workflow Primitives

- **Objective:** Add the first production campaign definition/record boundary without duplicating a live campaign owner or coupling campaign state to the roadmap mission schema.
- **Approach:** Build the declared minimal `global/bin/work-campaign` owner. Reuse exported roadmap `EffectClass`, `PROTECTED_EFFECTS`, `stableJson`, and `safeId` because their effect taxonomy, canonical serialization, and identifier contracts are identical. Keep campaign-specific exact fields, contained paths, records, errors, and digests in the campaign owner. Use Node `fs`, `path`, and `crypto`; add no package, schema framework, process abstraction, or mission mutation.
- **Evidence:** Current-repository graph/source review found exact mission parser candidates and OpenSpec/complexity path validators, but no campaign definition, adapter, inventory, work-item, wave, closure, or result owner. Mission parsing is mission-shaped and state roots must remain separate. Existing Zod use is isolated to frontmatter; adopting it here would add a second production contract style and adaptation cost without improving the explicit fail-closed boundary. Public ecosystem research stopped because standard-library plus verified current code fully determines this internal schema task. No configured cross-project source/scope was available, so cross-project discovery is `degraded` and no portability claim is based on it.
- **Outcome:** `build-minimal` campaign owner with narrow reuse of identical workflow primitives.
- **Reason:** This has the lowest contract, invalidation, navigation, and proof cost while preserving mission compatibility and one cohesive campaign schema owner.
- **Do Not Repeat Until:** N/A - selected task 2.1 production disposition.
- **Evidence-Based Retry Condition:** A verified current shared workflow-contract module owns the same exact error, path, digest, and record contracts without forcing campaign semantics into mission or OpenSpec owners.

## 2026-08-27 - Extend The Campaign Family With A Separate Ledger Materializer

- **Objective:** Make reviewed campaign seed records sufficient for deterministic inventory indexes, current closure projection, stable Markdown report generation, and drift readback without using report prose or campaign transition state as authority.
- **Approach:** Extend the existing `global/bin/work-campaign` family with one cohesive ledger/report materializer. Reuse the campaign record parsers, stable JSON, digests, and cause-preserving errors; keep an append-only normalized seed chain under the definition-owned evidence path, derive every index/total/report section from its current records, and atomically replace only derived projections and the report.
- **Evidence:** Current-repository graph and source inspection found reusable atomic-write and Markdown-rendering patterns, but no owner combines append-only reviewed seeds, current-record correlation, closure totals, report materialization, and ledger-backed drift detection. `tools/evidence-index.ts` only refreshes retained-file hashes, `tools/project-inventory.ts` renders one unrelated snapshot, and campaign/mission state roots own lifecycle transitions rather than reviewed semantic records. Node filesystem and crypto primitives plus current campaign contracts fully satisfy the bounded local mechanism; no dependency is needed. No configured cross-project source/scope was available, so cross-project discovery is degraded and no portability claim depends on it.
- **Outcome:** Selected `extend` with a minimal `materializer.ts` inside the campaign owner; no shared generic persistence framework and no campaign-state or mission-state reuse.
- **Reason:** This is the lowest-cost contract fit that preserves one reviewed seed owner, separate lifecycle ownership, deterministic regeneration, local failure visibility, and task 2.3's provider-free proof boundary.
- **Do Not Repeat Until:** N/A - selected task 2.3 locality decision.
- **Evidence-Based Retry Condition:** A verified current repository owner exposes the same append-only seed replay, current-record indexing, closure projection, report regeneration, and ledger-backed readback contract without importing campaign semantics into another lifecycle owner.

## 2026-08-27 - Build The Minimal Provider-Free Campaign Controller

- **Requested capability and trigger:** Task 2.4 requires a new top-level campaign controller that composes the proven contracts, state chain, and ledger/report materializer without adding semantic-provider, mission, source-writer, or host authority.
- **Sources reached:** Current campaign contracts/state/materializer/CLI and focused proof family, `global/bin/roadmap-mission/controller.ts`, `global/bin/roadmap-mission/preflight.ts`, `global/bin/portable-process.ts`, the active proposal/design/spec/tasks, Node filesystem/crypto, and installed Git/OpenSpec read-only commands. No portable cross-project peer source is configured, so cross-project discovery is `degraded`; no public ecosystem search is useful because no dependency is required.
- **Material candidates:** Reuse campaign state/materializer and `runPortableCommand`; reject reuse of the roadmap mission controller because it owns OpenSpec/source mutation, archive, checkpoint, and mission-specific recovery; reject extending the CLI monolith with controller policy because the accepted design names a separate campaign owner.
- **Disposition:** `build-minimal` `global/bin/work-campaign/controller.ts`, with the existing CLI as its selected entrypoint and existing component operations retained as explicit low-level proof surfaces.
- **Contract/total-cost reason:** One narrow reviewed fake-semantic phase input and one held campaign-state lease are the smallest seam that proves deterministic inventory, findings freeze, report materialization, frozen-wave admission, status/stop/replay, and a safe pre-mission pause. It adds no package, scheduler, generic engine, semantic scorer, mission clone, or host mechanism.
- **Retry condition:** Do not add semantic roots, mission launch, or Windows behavior until this controller reaches its intended `paused-external` wave boundary in a clean disposable Git/OpenSpec project and invalid, dirty, active-change, and unknown-writer controls remain effect-free.

## 2026-08-27 - Split Controller Readiness And Phase Admission Owners

- **Objective:** Keep the provider-free controller cohesive before sealing task 2.4 evidence.
- **Approach:** Retain lifecycle sequencing and stop/replay behavior in `controller.ts`, move read-only Git/OpenSpec/state readiness into `preflight.ts`, and move the reviewed fake-semantic input plus deterministic wave-admission validation into `phase-input.ts`.
- **Evidence:** The project-native code-quality inventory reported the 868-line controller as a split candidate, and source inspection found three independent change axes: environment readiness, semantic-input admission, and lifecycle orchestration. The focused full-core suite remained green after the split, while `controller.ts` fell below the inventory attention threshold.
- **Outcome:** Selected `extend` of the existing campaign family with two private responsibility modules; no new public entrypoint, package, generic framework, or behavior.
- **Reason:** The split narrows future mission/provider invalidation and test navigation without duplicating campaign state, ledger, process, or policy ownership.
- **Do Not Repeat Until:** N/A - selected task 2.4 locality correction.
- **Evidence-Based Retry Condition:** A future contract proves readiness and phase-input admission always change together with lifecycle sequencing and the separate owners add more adaptation cost than locality.

## 2026-08-27 - Extend Mission Contracts With Parent Correlation And Post-Lease Handoff

- **Requested capability and trigger:** Task 3.1 requires optional campaign/wave/work-item correlation and one bounded mission result without changing the immutable mission queue, source writer, certificate issuer, archive/checkpoint lifecycle, or manual mission behavior.
- **Sources reached:** Current roadmap mission contracts, preflight, controller, state chain, controller result reader, preflight/controller proof lanes, the campaign wave contract, and the active proposal/design/spec/tasks. No portable cross-project peer source is configured, so cross-project discovery is `degraded`; no dependency or public ecosystem search is needed.
- **Material candidates:** Extend the current mission contracts with omitted-when-absent parent/work-item fields; add one private parent-correlation owner for frozen-wave readback, cycle-free wave digesting, and handoff validation; derive bounded result evidence from the existing validated state chain. Reject dynamic queue mutation, campaign-state reads, a second executor/certificate, and importing campaign code into the mission.
- **Disposition:** `extend` the roadmap mission. A parent wave digest hashes the normalized frozen wave with only its mission-digest backlink omitted; the complete mission digest then binds that wave digest. Legacy parsed JSON, digest, checks, and controller reports omit every new field when no parent exists.
- **Readiness finding disposition:** `R-3.1-01` is confirmed. Mission disposition/projection does not prove writer or cleanup closure. The corrected implementation may emit a completed parent handoff only after the controller writer-lease `finally`, a fresh state replay reports `writerStatus=clear`, every slice has one validated completed executor result with non-unknown writer and complete cleanup, the final checkpoint is present, and the validated transition history contains one archive per slice. Non-complete handoffs keep owner condition unknown and cannot close a campaign wave.
- **Contract/total-cost reason:** One optional schema extension, one private validator, and one read-only state-history projection are the smallest contract fit. They preserve the existing serial mission as sole mutation owner and defer campaign launch/consumption to task 3.2.
- **Retry condition:** Do not add campaign launch or consume a handoff until exact/mismatched parent preflight, one provider-free completed parent mission, legacy byte compatibility, and existing mission lanes are green. Do not infer closure from controller status, elapsed time, PID absence, report prose, or campaign state.

## 2026-08-27 - Seal Parent Correlation At Its Private Owner

- **Objective:** Keep task 3.1 local before recapturing its final source identity.
- **Approach:** Move parent handoff parsing from the general mission contract owner into `parent-correlation.ts`, retain state/controller additions as one derived history read and one post-lease attachment, and run a fresh reduction review before final proof.
- **Evidence:** The project-native inventory moved `contracts.ts` below split-candidate; the new correlation owner remains below split-candidate. Fresh reduction row `R-3.1-CQ-01` found the new CLI parser re-export unused, so it was removed without removing the private parser or any oracle. Final preflight/controller captures and offline replays, plus state/runtime/launcher/integrated compatibility lanes, all completed on candidate `task-3-1-final-r1`.
- **Outcome:** Selected `extend` with one private correlation owner and no new public runtime export, state root, certificate issuer, campaign dependency, or proof mechanism.
- **Reason:** This is the smallest locality correction that preserves fail-closed parsing and keeps parent-specific schema evolution away from the legacy contract navigation surface.
- **Do Not Repeat Until:** N/A - selected task 3.1 locality and surface correction.
- **Evidence-Based Retry Condition:** Reconsider only if parent correlation is removed or another independent parent protocol requires a shared public parser with current compatibility evidence.

## 2026-08-27 - Recover Current Task Evidence Identity

- **Foundation Incident:** `FI-CAMP-001` for the exact tuple of schema-v2 strict candidate/environment equality, the task 2.4 campaign source identity, task 3.1 mission identity, and the current evidence index.
- **Reproduction:** Current inventory proved that six earlier checked task rows were stale because their rung-specific candidate/environment values differed from the index identity. Fresh foundation review confirmed that byte-equal task 2.4 source and a green focused suite did not authorize relabeling older raw captures.
- **Correction:** Preserve every historical bundle, select `task-3-1-final-r1` / `node-24.18.1-windows-local-proof-r1` as the aggregate identity, recapture contract-preflight, campaign preflight, state, materializer, and controller boundaries under that identity, replay every supported capture with zero live calls, and bind task 3.1's already matching mission evidence without claiming campaign-to-mission integration.
- **Dependent Sweep:** Rebind the eight current task rows and narrow the broad claim to separately exercised campaign and mission components. Proposal, design, delta spec, ownership, canonical specs, other active changes/claims, and all archives are not dependent and remain unchanged.
- **Current State:** `closed`; `reproductionDisposition=confirmed`. Strict validation and the apply gate pass, while the broad campaign claim intentionally remains `unknown`. Fresh corrected-candidate foundation review `task-3.1-final-r1-corrected-fi-camp-001` returned `no-material-finding` with Effective Model `openai/gpt-5.6-sol`.
- **Do Not Repeat Until:** A new candidate identity or decision-changing evidence creates a distinct current relation.

## 2026-08-27 - Compose One Frozen Wave Through Existing Mission Owners

- **Requested capability and trigger:** Task 3.2 adds the first campaign-to-mission mutation boundary, restart consumption, stop propagation, changed-block marking, and report regeneration. This triggers reuse discovery because it introduces a coordination owner across two existing state machines.
- **Sources reached:** Current campaign controller/state/materializer/phase-input/CLI, roadmap mission controller/state/parent-correlation/CLI and launcher stop behavior, current design/spec/task, focused proof runners, Node filesystem/process primitives, and `runPortableCommand`. No configured portable cross-project peer source exists, so cross-project discovery is `degraded`; public ecosystem search would not improve fit for repository-specific state and evidence contracts.
- **Material candidates:** Reuse the roadmap mission as sole source/OpenSpec/archive/checkpoint/certificate owner; extend campaign transitions and ledger/report owners; reject dynamic mission queues, direct campaign mission-state transitions, a second executor, plugin/PTTY dependence, campaign-held lease during mission execution, and campaign inference from report prose.
- **Disposition:** `build-minimal` one private `work-campaign/mission-handoff.ts` glue owner plus local extensions of existing campaign owners. It deterministically derives the exact mission/wave paths, a canonical wave-admission correlation digest, and the ordinary parent-correlated mission definition; verifies the frozen wave's mission digest; writes create-new/readback-identical campaign evidence; and invokes the existing roadmap mission controller through its portable entrypoint.
- **Lease and restart frame:** Campaign records and flushes `wave-admitted` plus `mission-launch`, then releases its writer lease before mission invocation. `run` intentionally stops after the child mission process reaches a terminal result, leaving campaign state at the durable mission observation boundary. A fresh `resume` derives the current handoff from mission state and consumes it only when correlation, writer, process/session, cleanup, checkpoint, and archive closure are terminal; this makes injected parent restart observable without a second writer.
- **Verification frame:** Completed consumption appends fixed-and-verified work-item versions, marks their source blocks `needs-rereview`, updates the exact wave report row with archive/checkpoint/evidence refs, regenerates and reads back the report, and records `mission-terminal` then `verification`. It pauses at verification because changed-block re-review belongs to later tasks; it does not claim campaign completion or synthesize a successor.
- **Status and stop frame:** Status is read-only and may derive but never consume a mission handoff. Stop records campaign stop intent and, when a mission is active, calls the mission-owned stop-intent primitive with source `campaign`; unknown mission writer/cleanup remains paused-unknown and is never cleared from PID absence.
- **Operating envelope:** Disposable local Git/OpenSpec mutation only, no push/remote/provider/semantic/host operation, one campaign writer outside the mission interval, one mission writer during it, exact contained paths, bounded child timeout/output, cause-preserving failures, terminal fixture cleanup.
- **Observable proof:** One seeded P1 becomes one ordinary propose slice, official apply/archive/checkpoint executes once, campaign process is restarted before consumption, resume reaches verification with fixed item, `needs-rereview` block, exact handoff refs, current report readback, and no concurrent writer. Mismatch, nonterminal handoff, duplicate resume, and stop retain fail-closed/idempotent behavior.
- **Retry condition:** Do not add semantic roots or another wave until this provider-free integrated path and its zero-live replay are terminal green. Do not consume controller status, PID absence, report prose, or a handoff with unknown closure.

## 2026-08-27 - Recover Task 3.2 Foundation Identity

- **Observed strategy:** Append task `3.2` under a new candidate while keeping checked
  tasks `1.1` through `3.1` on `task-3-1-final-r1`.
- **Result:** Rejected. Schema-v2 exact candidate/environment equality would mark the
  aggregate stale; the first task 3.2 capture also lacked a durable environment id and
  omitted reachable roadmap mission state/stop ownership from its source manifest.
- **Retry condition:** One explicit common candidate/environment, exact shared reachable
  source inventory, dependent current-lane recapture, and historical bundle preservation.
- **Selected strategy:** Bind `task-3-2-final-r2` /
  `node-24.18.1-windows-local-proof-r2`; recapture dependent campaign/mission lanes;
  retain historical raws at their original ceilings; rebind every checked task row;
  add an explicit retention exception; keep the broad partitioned claim unknown.
- **Fresh foundation re-review:** Three rows were reported. Main confirmed and corrected
  the two current identity gaps: Bun `v26.3.0` runtime evidence is now a cross-profile
  component lane rather than a Node-24 task artifact, and a direct `28/28` current
  SHA-256 readback closes the source-byte observation gap. The historical-byte row is
  narrowed to the supported embedded-ID/current-digest/non-relabeling ceiling because
  no immutable pre-correction digest authority exists.
- **Terminal result:** `FI-CAMP-002` closed with
  `reproductionDisposition=confirmed`. Any production-source change after the readback
  requires a new candidate and must not reuse or relabel this closure.

## 2026-08-27 - Add One Bounded Read-Only Semantic Root

- **Requested capability and trigger:** Task 4.1 adds the first configured provider/model
  mechanism and a separate semantic-session lifecycle, triggering reuse discovery and
  current OpenCode SDK/schema verification.
- **Sources reached:** installed `@opencode-ai/sdk/v2`, campaign record parsers, roadmap
  mission session executor, reusable proof-client/server helpers, portable process owner,
  current model profile, and Node platform primitives. No configured cross-project peer
  source exists, so cross-project discovery is `degraded`.
- **Disposition:** `build-minimal` a production semantic executor plus thin CLI; reuse SDK
  and campaign parsers, but do not reuse the mission mutation/certificate executor or
  proof-only client owner. The executor connects only to an explicit existing loopback
  runtime, creates one parentless deny-by-default session, accepts one schema-constrained
  result, verifies zero diff/children/questions/permission requests, and deletes it.
- **Configured strategy history:** r1 rejected before invocation due wrong portable-command
  call shape; r2 rejected before session/model due nested agent-list normalization; r3
  reached one model call but rejected OpenCode's built-in `StructuredOutput` control part.
  Each raw bundle was preserved and replayed offline before a causally distinct correction.
  r4 completed with one configured call and r5 replay completed with zero live calls.
- **Reduction disposition:** fresh code-quality review, Effective Model `xai/grok-4.6`,
  returned no safe reduction. The 593-line executor is attention-sized but cohesive; a
  split or shared generic SDK/validator wrapper would add concepts or cross ownership.
- **Retry condition:** do not repeat the configured 4.1 lane unless semantic production
  source, selected runtime/model/profile identity, or a missing observation changes. Do
  not infer semantic effectiveness or playbook completion from the one no-finding result.

## 2026-08-28 - Add Assignment-Owned Schemas And One Playbook Coordinator

- **Profile:** Material.
- **Scope:** Tasks 4.1-4.2 semantic executor and `audit-remediate` orchestration; no
  mission source mutation or task 4.3 completion claim.
- **Mechanism:** Keep one bounded semantic executor as the runtime owner, move each
  assignment result shape into an injected `json_schema`, and add one coordinator that
  permits only disjoint discovery fan-out followed by serialized reconciliation, one
  optional investigation, synthesis, and the existing deterministic phase-input gate.
  Enforce the model-call budget before launch and terminal fan-in through
  `Promise.allSettled`.
- **Evidence:** `npm run test:focused:work-campaign` is green; semantic executor `5/5`;
  playbook `4/4`; controller cases prove fresh reconciliation/investigation and block
  discovery self-reconciliation. Configured failed playbook r1 placed reconciliation
  fields outside `payload`; r2 omitted required discovery partition fields; r3 reached
  all three roots but exposed inventory digest drift from rewriting review status after
  snapshot.
- **Result:** Provider-free task 4.2 behavior is complete. Configured component proof
  needed a causally different immutable-inventory and runtime-surface strategy before it
  could support the playbook admission boundary.
- **Narrower claim:** Schema-valid assignment records and provider-free playbook
  orchestration are proven. A complete configured campaign, mission remediation,
  fixed-and-verified closure, report completion, population closure, and host supervision
  are not.
- **Retry condition:** Never retry by prompt wording alone. Change only an
  assignment-owned schema or fixture identity when the preserved raw bundle identifies
  that exact boundary; preserve each failed bundle and replay it offline.

## 2026-08-28 - Incident FI-CAMP-003: Configured Proof Surface Mutated The Worktree

- **Profile:** Material.
- **Candidate/relation/evidence tuple:** `task-4-1-semantic-configured-r4` and
  `task-4-3-playbook-configured-r6` / configured semantic and playbook proof identity
  versus the repository worktree non-mutation oracle / exact Git porcelain plus preserved
  raw bundles.
- **Observation:** Playbook configured r6 completed discovery, reconciliation, and
  synthesis with zero SDK diff and unchanged `src/main.ts`, but exact Git porcelain
  contained `?? .serena/.gitignore` and `?? .serena/project.yml` after the first prompt.
  The repository-global Serena MCP had mutated the worktree outside the previous oracle.
  Historical task 4.1 r4 therefore proves only unchanged SDK diff plus `src/main.ts`
  bytes, not whole-worktree non-mutation.
- **Protected invariant:** Every configured semantic root is read-only over the whole
  disposable project worktree and uses only the explicitly reviewed runtime surface.
  Untracked MCP/plugin writes are mutation and cannot be ignored, deleted, or
  retroactively relabeled as cleanup.
- **Reproduction disposition:** `confirmed`. Exact post-prompt porcelain and paths made
  the foundation mismatch current and reachable.
- **Smallest correction:** Create a new isolated proof config containing only the
  selected provider/model profile; preserve effective config digest and configured
  MCP/plugin inventories; require empty exact whole-worktree porcelain before and after
  every root; retain built-in provider/auth plugins so configured inference remains
  reachable. Expand `WORK_CAMPAIGN_CONTROLLER_SOURCE_PATHS` to include semantic,
  playbook, schema, proof, profile, package, and test owners, and widen the typed-reference
  grammar consistently to admit contained leading-dot work-campaign evidence paths.
- **Causally distinct attempts:** Isolated semantic r1 disabled default plugins and
  failed before a provider response, so that mechanism was rejected. Isolated playbook
  r2 completed three roots with empty worktree but failed admission because
  `file:.work-campaign/...` was rejected; offline r3 then found unchanged regex copies in
  state/mission handoff. The corrected grammar was swept through every owner and admitted
  the preserved record offline before a new live capture.
- **Corrected evidence:** Common candidate `foundation-fi-camp-003-r5`; environment
  `node-24.18.1-windows-isolated-semantic-r5`. Semantic r5 is complete with one configured
  call; playbook r5 is complete with three configured calls and reaches one
  `wave-admitted` then intentional `paused-external`; configured MCP/plugin inventories
  and exact Git porcelain remain empty; source/session/server/fixture cleanup is terminal.
  Both configured replay evaluations are complete with `liveCalls: 0`. Provider-free
  campaign preflight/state/materializer/controller and mission preflight/controller were
  recaptured on the same expanded source identity; replay invocations report zero live
  calls and recompute their capture evaluations. Provider-free state/materializer/
  controller evaluation fields named `liveCalls` and `processStarts` retain original-
  capture process counts and are not replay-effect oracles. Current direct source readback
  is `43/43`, zero mismatch.
- **Historical evidence rule:** Preserve all prior raw bundles unchanged. Do not relabel
  r4/r6 as successful whole-worktree evidence, delete `.serena` output to manufacture
  cleanliness, or bind a checked task to mixed candidate/environment identity.
- **Corrected-candidate review:** Fresh foundation-integrity review
  `ses_fba962915ffeUW2zI0qp9Dff7M`, Effective Model `openai/gpt-5.6-sol`, found one
  observation-identity contradiction: three provider-free replay evaluation files retain
  capture process counts under both `liveCalls` and `processStarts`, while replay stdout
  reports zero. Main reproduced the mapping in `tools/proofs/work-campaign.ts` and applied
  the reviewer's smallest mitigation: these files support offline recomputation of capture
  evaluation only; replay stdout and the effect-free replay code path support zero replay
  calls. No retained field is used as a zero-live replay oracle.
- **Terminal state:** `closed`; `reproductionDisposition=confirmed`. The common r5 index
  has 11 checked/current rows, no incomplete/stale/unknown/envelope-mismatched rows, 208
  indexed files, no retention overflow, and the broad claim remains `unknown`. Strict
  OpenSpec validation and the apply gate pass. No further configured live attempt is
  authorized by this entry.
- **Claim ceiling:** One isolated configured discovery assignment and one isolated
  configured three-root playbook component can reach frozen-wave admission without
  project mutation and can replay offline. Task 4.3 complete mission remediation/campaign
  completion, multiple waves, population closure, Windows supervision, deployment,
  release, and remote effects remain unproved.
- **Retry condition:** Do not recapture r5 unless production source, proof source,
  selected model profile, isolated config, route, whole-worktree oracle identity, or a
  requirement for replay-evaluation-native effect counts materially changes.

## 2026-08-28 - Complete One Configured Disposable Campaign

- **Profile:** Material. Candidate `task-4-3-configured-r2`; environment
  `node-24.18.1-windows-task-4-3-r2`.
- **Accepted slice:** Task 4.3 only: one configured discovery/reconciliation/synthesis path,
  one mission-owned P1 correction, OpenSpec archive/checkpoint, current changed-block
  re-review, aggregate validation/proof, final challenge, report readback, and campaign
  `terminal-complete`. Another wave, population closure, host supervision, archive of this
  change, and other active changes remain out of this slice.
- **Ownership strategy:** `build-minimal` `verification-input.ts` as the exact contained
  assignment/result parser; extend the existing controller for state-dependent coverage,
  budget, command evidence, report seed, and completion; retain the materializer as closure
  authority and the roadmap mission as sole source writer. Historical wave/partition/
  reconciliation candidates remain immutable ledger evidence while current blocks/items/
  report bind the post-mission candidate.
- **Architecture/readiness correction:** Fresh reviews identified and main corrected mission
  re-consumption after re-review, model terminal authority, impossible pre/post report-digest
  equality, historical/current candidate coupling, partial-finalization recovery, mixed
  phase-input ownership, premature validation completion, historical partition inventory
  coupling, and the post-report transition crash window.
- **Configured strategy r1:** Behavior completed, but raw inspection found stale assignment
  evidence refs to `src/main.ts`. Preserve the bundle; do not use it as current proof. Retry
  condition was a shared assignment-builder correction plus an evaluator assertion over
  current assigned block paths.
- **Configured strategy r2:** Six roots and the full campaign completed with `captureError`
  null. The capture-time evaluator was blocked only because its new ref check read omitted
  capture `context`. Preserve raw and blocked evaluation. A causally different evaluator-only
  correction uses the fixture's explicit block map; full offline replay is complete with all
  checks true and `liveCalls: 0`. No third live attempt is authorized or needed.
- **Current evidence:** Focused tests green; provider-free campaign preflight/state/
  materializer/controller and mission preflight/controller captures and replays complete;
  configured terminal replay complete; direct source readback 44/44 with zero mismatch.
- **Code health:** Fresh reduction review `ses_fba512ab7ffem0hIwQj7RLJ0vb`, Effective Model
  `xai/grok-4.6`, found no safe reduction. Split-or-justify keeps the controller as one state
  authority and the test/proof as linear fixture owners; the verification parser is already
  a separate cohesive owner.
- **Terminal result:** Task 4.3 working inside its disposable configured envelope. Live-attempt
  gate for this lane is clear. Broad claim `autonomous-work-campaign-v1` remains blocked and
  unknown because another wave, population closure, SDET, evidence challenge, and Windows
  supervisor evidence remain absent.
- **Retry condition:** Do not repeat configured task 4.3 unless production/proof/profile/
  isolated-runtime identity changes or a preserved current raw identifies a new missing
  terminal evaluator observation. Use the r2 raw plus zero-call terminal replay as current
  evidence; never relabel r1 or the blocked r2 capture-time evaluator.

## 2026-08-28 - Prove Provider-Free Two-Wave Convergence

- **Profile:** Material. Candidate `task-5-1-provider-free-r1`; environment
  `node-24.18.1-windows-task-5-1-r1`.
- **Accepted slice:** Task 5.1 provider-free production seam and proof only. Configured
  two-wave capture, task checkbox/evidence-index rebind, population closure, host work, and
  later tasks remain outside this recorded increment.
- **Readiness correction:** Fresh challenge `ses_fba375311ffemwK1B334RIwtlx`, Effective Model
  `xai/grok-4.6`, found that the initial two-step frame would allow discovery self-confirmation.
  Main reproduced the gap against `semantic-playbook.ts` and `phase-input.ts`. The corrected
  flow is current re-review discovery candidate, separate non-self/source-correlated
  reconciliation, then synthesis and existing mission handoff.
- **Production strategy:** Extend `verification-input.ts` and the existing controller/CLI
  `verify` owner. Re-review persists only exact current candidate work; reconciliation is a
  distinct producer and confirms one current P0/P1; synthesis covers every and only current
  confirmed P0/P1, validates paths/effects/prior checkpoints/unique wave identity, requires
  explicit mission options, records `wave-admitted` before `mission-launch`, and releases the
  campaign lease before the existing mission source writer starts.
- **Runtime correction:** The first second-wave attempt correctly failed mission preflight
  because the task-3.2 fixture used evidence-only checkpointing and untracked semantic inputs.
  The causally different fixture uses already-authorized disposable local commits and keeps
  semantic records under the ignored campaign evidence root. No production fail-open change
  was made.
- **Current evidence:** `npm run test:focused:work-campaign` is green. Repository capture
  `implementation-evidence/task-5-1-controller-r1` is complete with 44 checks and 66
  proof-owned process starts; `task-5-1-controller-replay-r1` and `-r1b` are complete with
  zero live calls. Negative controls reject next-wave self-reconciliation and missing mission
  options without state advance. Both waves archive/checkpoint once and terminal completion
  retains the P2 report-only row.
- **Code health:** Fresh reduction review `ses_fba192c9bffemE2Ga9H57qUfHM`, Effective Model
  `xai/grok-4.6`, found no safe reduction. Split-or-justify keeps the controller as one state
  authority, the test as one sequential fixture, and the proof as one capture evaluator; the
  verification parser remains the extracted schema owner.
- **Terminal result:** Provider-free task-5.1 lane working and replay-complete. Task 5.1 remains
  unchecked because configured two-wave behavior is unobserved. The campaign broad claim
  remains unknown.
- **Retry condition:** Do not repeat the provider-free r1 capture unless production/proof
  source or evaluator semantics change. The next distinct strategy is to extend the configured
  playbook from six one-wave roots to the exact nine-root two-wave sequence, run provider-free
  playbook tests first, then one bounded configured capture only after preflight is green.

## 2026-08-28 - Correct Configured Two-Wave Report Canonicalization

- **Profile:** Material. Failed candidate `task-5-1-configured-r1`; environment
  `node-24.18.1-windows-task-5-1-r1`.
- **Observed result:** The bounded configured runner completed seven read-only model roots and
  stopped before the second mission with immutable sequence-24 `record digest differs`.
  Server and fixture cleanup were terminal. The preserved capture and
  `task-5-1-configured-replay-r1` reproduce the blocked evaluator with zero live calls.
- **Confirmed cause:** `verifyNextWave` sorted appended report `waveRows` by `waveId`, while the
  report schema canonicalizes every row collection by row `id`. The configured wave IDs made
  those orders differ, so materializer readback rejected the report seed just written by the
  controller. The earlier provider-free fixture used accidentally aligned IDs.
- **Correction:** Sort the appended `waveRows` by row `id`, make the provider-free second-wave
  ID invert wave-ID and row-ID order so the exact recurrence is retained, and update the
  terminal maximum claim to describe two completed waves rather than exclude multi-wave.
- **Current evidence:** The strengthened controller test first reproduced the exact
  sequence-24 digest failure, then passed after the one-line canonicalization correction.
  `npm run test:focused:work-campaign` is green. No second configured call has started.
- **Retry condition:** One new configured r2 capture is permitted only because production and
  proof identities changed after a deterministic provider-free reproducer and correction.
  Never reuse or relabel r1. Run a fresh zero-call preflight first; if r2 fails, preserve and
  replay it before any further governed attempt.

## 2026-08-28 - Seal Task 5.1 Configured Two-Wave Convergence

- **Profile:** Material. Candidate `task-5-1-configured-r2`; environment
  `node-24.18.1-windows-task-5-1-r2`.
- **Runtime proof:** The fresh isolated `quality-independent` capture completed the exact
  nine-root sequence, two serialized mission-owned source corrections, two archive/checkpoint
  rows, current changed-block re-review, validation/proof, final challenge, report readback,
  terminal cleanup, and P2 non-mutation. Capture evaluator is complete with nine model calls;
  `task-5-1-configured-replay-r2` is complete with zero live calls.
- **Failure preservation:** `task-5-1-configured-r1` and its zero-call replay remain immutable
  blocked evidence for the canonical report-row ordering defect. They are not relabeled or
  used as terminal proof.
- **Evidence seal:** Task 5.1 is checked. Schema-v2 aggregate and all 13 checked rows bind r2.
  The compact index is 65,402 bytes, retains 249 files with zero unindexed, and splits the
  historical 113-file foundation lane into two resolver-valid lanes without changing its file
  set. Current/foundation lane resolution, strict OpenSpec validation, and task freshness are
  green; the apply gate remains warning-only for the explicit broad-claim gap.
- **Claim ceiling:** One disposable configured two-wave campaign is proven. Population claim
  `autonomous-work-campaign-v1` remains `unknown` at 0/20; Windows supervision, critical SDET,
  evidence sufficiency, deployment, release, and remote effects remain unobserved.
- **Code health:** Fresh reduction review `ses_fb9f25bc7ffeeN0PvOC0gTH9aa`, Effective Model
  `xai/grok-4.6`, found no safe reduction. Split-or-justify retains `controller.ts` as one
  campaign state authority, the controller test as one dependent sequential fixture, and the
  configured playbook as one capture/evaluator lane; the verification parser remains the
  extracted schema owner. Reverse-ID canonicalization and two-wave/P2/cleanup/replay oracles
  are unique and retained.
- **Retry condition:** Do not recapture configured r2 unless production/proof/profile/runtime
  or evaluator identity materially changes. The next distinct rung is task 5.2 population
  coverage; it must reuse preserved terminal rows and add only missing scenario observations.

## 2026-08-28 - Task 5.2 Implementation Decision Frame

- **Outcome:** Produce one explicit terminal observation row for each of the 20 declared
  `autonomous-work-campaign-v1` members, with provider-free control scenarios and preserved
  configured r2 evidence composed without another model call. Implement missing semantic
  dispositions needed by that pack; keep unsupported host evidence `unknown`.
- **Operating envelope:** Extend current `semantic-playbook.ts`, focused semantic/controller
  tests, and `tools/proofs/work-campaign.ts`. Read existing r2 bundles only. Writes remain
  repository source/tests and create-new disposable/provider-free evidence. No configured
  recapture, consumer target, host operation, remote effect, credential, archive, or release.
- **Non-goals:** Campaign-level aggregate closure/P0-SDET handoff (task 5.3), doctor/supervisor/
  Windows behavior (tasks 6.x), installed composition (task 7.1), claim-index population seal
  (task 7.2), and generic retry/scheduler infrastructure.
- **Non-deferrable invariants:** Discovery cannot self-confirm; only separately confirmed P0/P1
  enters a wave; P2/P3, optional polish, duplicate, and falsified rows never mutate; unknown
  material blocks synthesis; unauthorized protected-effect work becomes `owner-required` while
  separately authorized work may freeze; frozen waves remain immutable; transient/budget/
  cleanup controls never emit completion or start a source writer; every reused observation
  retains candidate/environment/oracle identity and every new bundle replays with zero calls.
- **Implementation slices:** First add a red explicit 20-member population evaluator using the
  member order already declared in `evidence-index.json`. Then extend the semantic playbook with
  definition `allowedEffects`, terminal no-wave completion for report-only/resolved-only work,
  protected-item owner-required routing, and explicit fail-closed control results for immutable,
  transient, budget, and cleanup-unknown assignment outcomes. Add provider-free P0/P3,
  duplicate/falsified, all four investigation outcomes, protected+authorized split, retry-after-
  terminal-failure, and budget/cleanup cases. Finally compose preserved task-5.1 configured
  P1/P2/two-wave/re-review/final-complete evidence and existing controller/state/materializer
  controls into one raw/evaluation/replay pack.
- **Observable proof:** `npm run test:focused:work-campaign` is green; population capture has
  exactly 20 unique member rows with explicit boundary/status/effect/cleanup/evidence identity;
  every non-host task-5.2 member is terminal at its owning boundary; Windows re-entry is
  explicit `unknown`; configured call count is zero; two replays are complete and byte-stable;
  source writer count is zero in every negative/control row.
- **Material residual risks:** This rung does not claim integrated campaign closure for every
  partition, Windows supervision, or population support. Those remain explicit unknowns and
  block the broad claim. Production changes invalidate only semantic-playbook/provider-free
  population lanes; task-5.1 configured raw remains reusable unless its evaluator contract
  changes.
- **Stop line:** Stop before mutation if an accepted semantic disposition requires a product or
  protected decision not fixed by the current spec. Stop before another configured call; a
  missing observation must first be shown impossible from preserved configured and new
  provider-free evidence. Any provider-free failure preserves its raw and must be corrected and
  replayed before population task completion.

### Task 5.2 Readiness Correction

Fresh readiness review `ses_fb9e2846fffek4U61meTPwmTHv`, Effective Model
`xai/grok-4.6`, found two material gaps. Main confirmed both: row count alone did not bind the
named partition oracles, and playbook-only changes could not satisfy production freeze/admission.
The following correction supersedes the narrower envelope and zero-call promise above.

| Claim member | Owning boundary | Expected status | Source writer/effect | Cleanup |
| --- | --- | --- | --- | --- |
| `valid-campaign-definition` | definition preflight | `complete` | zero | not required |
| `invalid-or-unsafe-definition` | definition/preflight | `blocked` immutable input | zero | not required |
| `complete-inventory` | phase input | `complete` | zero | complete |
| `isolated-read-only-discovery` | semantic executor/playbook | `complete` | zero | complete |
| `confirmed-p0` | reconciliation/admission | `complete` eligible | zero before mission | complete |
| `confirmed-material-quality-p1` | configured mission | `complete` | one serialized mission writer | complete |
| `p2-p3-report-only` | reconciliation/report | `complete` report-only | zero | complete |
| `credible-unknown-p0-p1-investigation` | investigation | four terminal subrows | zero | complete/unknown exactly |
| `optional-polish-exclusion` | reconciliation/admission | `complete` report-only/falsified | zero | complete |
| `two-frozen-remediation-waves` | configured mission/controller | `complete` | two serialized mission writers | complete |
| `incomplete-or-failed-mission` | mission observation | blocked/paused, never complete | no replacement writer | terminal or explicit unknown |
| `active-change-conflict` | preflight | `blocked` immutable input | zero | not required |
| `dirty-path-conflict` | preflight | `blocked` immutable input | zero | not required |
| `protected-effect` | playbook/phase admission | `owner-required`; authorized sibling may freeze | unauthorized zero | complete |
| `controller-or-opencode-interruption` | state/executor/playbook | transient/unknown pause | zero replacement | exact terminal/unknown |
| `windows-supervisor-reentry` | later host boundary | `unknown` | unknown | unknown |
| `changed-block-rereview` | controller verification | `complete/current` | mission writer already terminal | complete |
| `report-drift-prevention` | materializer readback | `blocked` | zero | not required |
| `budget-exhaustion` | semantic/control boundary | `paused-budget`, never complete | zero | complete |
| `terminal-completion` | final challenge/controller | `complete` only from current facts | writers terminal | complete |

- **Extra task-5.2 fixture oracles:** duplicate and falsified work-item dispositions are terminal
  no-wave rows; unknown investigation has confirmed/falsified/still-unknown/owner-required
  subrows; immutable input maps to the invalid/unsafe member; transient retry and unknown cleanup
  map to interruption; explicit stop and unknown writer are extra control rows; final challenge
  has both current-complete and overbroad/mismatched-blocked rows. Every row declares candidate,
  environment, boundary, disposition, source-writer/effect count, cleanup, and evidence refs.
- **Corrected production envelope:** Extend `semantic-playbook.ts`, `phase-input.ts`,
  `controller.ts`, the playbook runner context, focused semantic/controller tests, and
  `tools/proofs/work-campaign.ts`. No-wave resolved/report-only input must pause at verification
  without mission admission. Owner-required plus authorized work may admit only the authorized
  wave while preserving the owner blocker. Unknown material admits no wave.
- **Transient retry oracle:** One invocation records the terminal transient result, performs no
  automatic second call, and emits the explicit blocked retry condition. One later bounded
  provider-free invocation may reuse the immutable assignment digest at a new create-only result
  path only after the prior result/cleanup and retry evidence are supplied; invalid/unknown
  cleanup or absent retry evidence rejects the retry. No elapsed-time/PID inference is allowed.
- **Configured invalidation:** r2 remains immutable baseline evidence. If production or configured
  runner source identity changes, it no longer proves the current candidate; after the complete
  provider-free pack and zero-call preflight are green, one fresh bounded configured successor
  capture and offline replay is required. No equivalent r2 recapture is allowed.
- **Corrected proof gate:** Exact member ids and expected fields above, every named task partition
  mapped to a member or extra oracle, privacy scan, zero unindexed files, two zero-call replays,
  focused tests, strict validation, and no unsupported host/population claim. A count-only matrix
  is insufficient.

## 2026-08-28 - Task 5.2 Provider-Free Population And Configured Successor r1

- **Strategy:** Extended the semantic playbook and phase/controller admission owners for no-wave,
  owner-required, protected split, budget, transient, cleanup-unknown, and retry-evidence controls.
  Added an exact 20-member provider-free population evaluator composed with preserved configured
  r2 evidence, then launched one configured successor because current production identity changed.
- **Provider-free evidence:** `task-5-2-population-r1` blocked after its combined state/materializer
  child could not emit both raws; replay `task-5-2-population-replay-r1` reproduced the block with
  zero calls. The causally distinct split-child r2 captured every raw and 19 observed non-host rows
  plus explicit Windows unknown, but its component invocation envelopes were absent; corrected
  offline replay `task-5-2-population-replay-r2` made every behavior check true and blocked only
  the intentionally changed evaluator identity. `task-5-2-population-r3` and replays r3a/r3b are
  complete with 20 rows, `modelCalls: 0`, no provider/source/host effect, and complete cleanup.
- **Configured evidence:** Zero-call `task-5-2-configured-preflight-r1` completed against preserved
  r2 semantic records. `task-5-2-configured-r1` then blocked after seven configured roots because
  `synthesize-negative-divisor-wave` returned an outer structured result missing `schemaVersion`.
  Production preserved `errorClass: invalid-result`; server terminal, fixture removal, and cleanup
  completed. `task-5-2-configured-replay-r1` reproduced the blocked verdict with zero calls.
- **Classification:** The configured failure is a provider result-shape failure at the second-wave
  synthesis root, not a production/admission/state defect. Do not repeat the unchanged request.
- **Retry condition and distinct strategy:** Make the already-required outer
  `{ assignmentId, payload, schemaVersion: 1 }` envelope explicit in that synthesis assignment,
  then require a fresh zero-call installed-runtime preflight before one bounded successor. No
  production semantic/control change or r1 relabel is permitted.

## 2026-08-28 - Seal Task 5.2 Semantic And Control Population

- **Profile and identity:** Material. Candidate `task-5-2-configured-r2`; environment
  `node-24.18.1-windows-task-5-2-configured-r2`.
- **Configured successor:** The distinct outer-envelope prompt plus zero-call preflight unlocked
  one r2 capture. `task-5-2-configured-r2` completed the exact nine-root two-wave campaign with
  nine model calls; `task-5-2-configured-replay-r2` is complete with zero live calls. Historical
  configured r1 remains immutable blocked evidence for the missing outer `schemaVersion` result.
- **Population proof:** `task-5-2-population-r4` composes current semantic/controller/state/
  materializer observations with configured r2 under one candidate/environment identity. It has
  exactly 20 unique terminal observation rows, 19 observed non-host members, explicit Windows
  `unknown`, privacy-safe output, `modelCalls: 0`, no provider/source/host effects, 139 attributed
  local process starts, and complete fixture cleanup. Replays r4a and r4b are complete with zero
  live calls. Provider-free r1/r2 failures and their blocked replays remain preserved; r3 and its
  complete replays remain narrower pre-successor evidence.
- **Behavioral result:** Confirmed authorized P0/P1 is wave-eligible; P2/P3, optional polish,
  duplicate, and falsified rows produce no mutation wave; all four unknown investigations remain
  explicit; unknown material blocks synthesis; protected work remains owner-required while an
  authorized sibling may freeze; no-wave reports persist without mission admission; budget,
  transient, unknown-cleanup, explicit-stop, unknown-writer, report-drift, and overbroad final-
  challenge controls fail closed with zero replacement source writers.
- **Code health:** Fresh reduction review `ses_fb9a4c2cdffefW44Awly04IGmq`, Effective Model
  `xai/grok-4.6`, found no safe reduction. Split-or-justify retains `controller.ts` as one campaign
  state authority, the controller test as one dependent fixture, `work-campaign.ts` as one
  multi-mode campaign evaluator, and the configured playbook as one capture/evaluator owner.
- **Claim ceiling:** Task 5.2 is complete at its owning semantic/control boundaries. Broad claim
  `autonomous-work-campaign-v1` remains `unknown`; the Windows member is not supported, and task
  5.3 aggregate terminal handoff, tasks 6.x host evidence, SDET, evidence challenge, installation,
  deployment, release, and remote effects remain outside this seal.
- **Retry condition:** Do not recapture configured r2 or population r4 unless production/proof/
  profile/runtime/evaluator identity materially changes or a later composition exposes a new
  missing observation. Never relabel configured r1 or provider-free r1/r2 failures.

## 2026-08-28 - Task 5.2 Schema-v2 Seal

- **Identity:** Candidate `task-5-2-configured-r2`; environment
  `node-24.18.1-windows-task-5-2-configured-r2`.
- **Index action:** Rebound the aggregate, all 14 checked-task rows, and the broad-claim identity
  to the task 5.2 configured successor. Added `task-5-2-current-candidate` with all 24 preserved
  task 5.2 files. Compacted 19 unreferenced historical provider-free lanes into
  `historical-campaign` without deleting or relabeling any raw artifact.
- **Retention result:** Compact index 64,938 bytes; 273 retained files / 1,728,151 bytes; 28 lanes;
  largest lane 57 files; zero unindexed files; configured retention exception remains 512 files /
  52,428,800 bytes.
- **Evidence inventory:** Evidence `present`; 14 checked tasks / 14 task rows; zero incomplete,
  mismatched, stale, or unknown task rows; `overLimit: false`. `autonomous-work-campaign-v1` is
  current and remains `unknown (0/20)` because claim observation rows, Windows supervision,
  required critical SDET, and independent broad-claim challenge remain open.
- **Validation:** `npm run test:focused:work-campaign` green; current and compacted lane resolvers
  green; `openspec validate add-autonomous-campaign-orchestration --strict` valid; apply gate exit
  0 with only the expected non-blocking broad-claim warning.
- **Transition:** Task 5.2 is sealed. Do not recapture task 5.2 configured or population bundles.
  Continue offline/provider-free with task 3.3 or task 5.3. Any later configured attempt requires
  an exact observation unavailable from preserved evidence and a distinct bounded strategy.

## 2026-08-28 - Task 5.3 Terminal Handoff Seal

- **Identity:** Candidate `task-5-3-provider-free-r4`; environment
  `node-24.18.1-windows-task-5-3-r4`.
- **Accepted outcome:** Extend the existing campaign contracts, controller, materializer, focused
  tests, and proof evaluator so only current correlated aggregate evidence can issue one exact
  terminal handoff. A confirmed fixed P0 remains durably paused for fresh test-only SDET.
- **Non-goals:** Host supervision, installation, deployment, release, broad population closure,
  self-issued SDET evidence, and protected or remote effects.
- **Non-deferrable invariants:** The campaign writer lease spans report materialization through
  terminal completion; tracked-source and exact validation/proof artifacts are rechecked before
  every complete write; stale readback suppresses handoff; recovery state is durable and caller
  operations remain truthful.
- **Readiness challenge:** `implementation-readiness-reviewer` session
  `ses_fba6a6defe6e04npeNW2wXWaUL` (`xai/grok-4.6`) found no material pre-mutation defect in the
  bounded frame.
- **Protocol challenge PAS-5.3-001:** Confirmed that report-seed/report-materialized recovery could
  bypass the final currentness check and that the lease ended too early. Corrected by sharing the
  lease through terminal transitions, rechecking immediately before `terminal-complete`, and
  suppressing stale handoff on readback. Provider-free r3 and two zero-call replays closed this
  pre-r4 candidate defect.
- **Protocol challenge PAS-5.3-002:** Confirmed that the stale-evidence recovery pause was only
  in-memory and the returned operation was reconstructed as `verify`. Corrected with one durable
  `pause` transition carrying `blocker:terminal-evidence-currentness-required`, exact caller
  operation threading, consistent `resume`/`status`/`replay` reconstruction, and restore-then-resume
  completion. Reviewer session `ses_fb95d84e0ffewPZb0k0SbdcVoK` (`xai/grok-4.6`).
- **Runtime proof:** `npm run test:focused:work-campaign` passed. Controller capture command:
  `node tools/proofs/work-campaign.ts --mode controller --candidate-id task-5-3-provider-free-r4 --environment-id node-24.18.1-windows-task-5-3-r4 --evidence-root <task-5-3-controller-r4>`.
  It completed with 89 local process starts, zero provider/OpenCode/host/source-write effects, and
  all 55 evaluator checks true. Two distinct replay roots completed with `liveCalls: 0`.
- **Reduction challenge:** `code-quality-reviewer` session `ses_fb962acceffeV0djZie8kF5Zo6`
  (`xai/grok-4.6`) found no safe behavior-preserving reduction; the controller remains the sole
  campaign state authority.
- **Preserved history:** r1, r2, r2a-r2c, r3, and r3a-r3b remain immutable narrower evidence and
  are not relabeled as r4. They do not support the current PAS-5.3-002 candidate.
- **Claim ceiling:** `autonomous-work-campaign-v1` remains `unknown (0/20)`; Windows supervision,
  fresh critical SDET, broad-claim challenge, installation, deployment, release, and remote effects
  remain unsupported.
- **Retry condition:** Do not recapture r4 unless production, proof, profile, runtime, or evaluator
  identity materially changes or later composition exposes a distinct missing observation.
- **Schema-v2 seal:** Rebound the aggregate and all 15 checked rows to r4. Preserved every indexed
  raw/replay file, retained explicit task 5.2 and 5.3 current-candidate lanes, and consolidated only
  historical lane metadata into four bounded archive lanes. The compact index is 65,393 bytes with
  six lanes (largest 64 files). Evidence inventory reports 288 retained files / 1,815,522 bytes,
  zero unindexed files, no incomplete/mismatched/stale/unknown task rows, and `overLimit: false`.
- **Seal validation:** Focused OpenSpec gate 23/23 and inventory 10/10 passed; strict OpenSpec
  validation passed; the apply gate exited zero with only the expected broad-claim warning; current
  and compacted schema-v2 lane resolvers passed.

## 2026-08-28 - Reject Fixture-Local Runtime-Graph Expansion For Doctor Readiness

- **Objective:** Make the isolated campaign and unattended doctor gates execute the production
  campaign preflight and roadmap workflow without using repository dependencies implicitly.
- **Approach:** Copy additional production runtime modules into the isolated `OPENCODE_CONFIG_DIR`
  fixture and spawn `work-campaign.ts` from that copied global source.
- **Evidence:** The first run failed on missing `parent-correlation.ts`; after expanding the copied
  runtime list, module loading reached `semantic-executor.ts` and failed because the isolated
  installed source could not resolve `@opencode-ai/sdk`. The unattended workflow similarly failed
  as `Portable process supervisor exited 1`.
- **Outcome:** Rejected as an open-ended fixture-copy strategy.
- **Reason:** Campaign preflight must not load provider dependencies, and every installed
  timeout-bounded command requires `portable-process-supervisor.ts`. Copying more of the ESM graph
  would mask those production runtime-surface defects rather than prove installation readiness.
- **Do Not Repeat Until:** A current production operation genuinely requires another module at the
  exercised boundary and the maintained runtime manifest omits that exact reachable dependency.
- **Evidence-Based Retry Condition:** A fresh isolated installed-source run fails with a named
  missing production module after campaign preflight remains provider-free and the complete
  declared portable runtime surface is present.

## 2026-08-28 - Close Doctor Readiness Through The Installed Production Boundary

- **Selected strategy:** Keep doctor invoking the inspected installed campaign entrypoint, make the
  SDK import lazy at the semantic-call boundary, add `portable-process-supervisor.ts` and
  `semantic-schema.ts` to the maintained runtime surface, and use a schema-valid provider-free
  campaign definition whose effect envelope excludes provider inference.
- **Observed corrections:** The inactive template's zero model-call budget contradicted the
  campaign schema minimum of one. The corrected finite budget grants no provider authority because
  `provider-inference` is absent from `allowedEffects`. The campaign-ready fixture also now names a
  valid empty OpenSpec project so production `openspec list --json` exercises project state rather
  than failing fixture setup.
- **Result:** The campaign-ready/unsafe-supervisor doctor scenario and the independently passing
  unattended-without-campaign scenario both pass through the production doctor entrypoint. Focused
  library (179), init (3), work-campaign, portable-process (9), and installer (30) suites are green.
- **Claim ceiling:** These observations support task 6.1 readiness/bootstrap behavior only. They do
  not support a portable supervisor, Windows registration, host resume, installation, broad claim,
  RC, stable, deployment, release, or remote effect.
- **Retry condition:** Do not return to fixture-local module copying. Reopen only for a reproduced
  installed-source readiness failure on a materially changed runtime manifest or doctor contract.

## 2026-08-28 - Task 6.1 Campaign Readiness Seal

- **Identity:** Candidate `task-6-1-readiness-r1`; environment
  `node-24.18.1-windows-task-6-1-r1`.
- **Runtime proof:** Production doctor passed a complete provider-free disposable campaign, blocked
  only the selected missing supervisor after host recovery was enabled, and left exact Git
  porcelain empty before and after. A separately configured static unattended project passed its
  selected gate while missing campaign configuration remained independently blocked. The
  self-hosted kit correctly exits 2 for `--require campaign` because it intentionally has no
  project campaign definition, without affecting ordinary qualification.
- **Current candidate:** Provider-free controller capture `task-6-1-controller-r1` completed with
  89 local process starts and zero provider/OpenCode/host/source-write effects. Two create-new
  replays completed with `liveCalls: 0`. Because the semantic SDK load boundary materially changed,
  zero-call configured preflight unlocked one bounded successor. `task-6-1-configured-r1` completed
  the preserved nine-root/two-wave behavior with nine configured calls, and its offline replay
  completed with `liveCalls: 0`; no historical configured bundle was relabeled.
- **Validation:** Focused library 179/179, init 3/3, portable-process 9/9, installer 30/30, and all
  focused work-campaign suites passed. Help surfaces are effect-free and document the campaign gate
  and inactive bootstrap behavior.
- **Schema-v2 seal:** Added optional lane-level `pathPrefix` compaction so the index can retain every
  immutable raw without repeated path bytes or a larger bound. Both evidence readers expand the
  same full paths; existing lanes remain compatible. The compact index is 60,467 bytes with 298
  retained files, seven lanes, zero unindexed files, 16/16 current checked rows, and
  `overLimit: false`. Focused evidence resolver, OpenSpec gate, and inventory suites passed; strict
  OpenSpec validation passed; the apply gate exited zero with only the expected broad-claim warning.
- **Claim ceiling:** Task 6.1 is complete at bootstrap/readiness boundaries. Portable supervisor,
  Windows installation/re-entry, broad population closure, fresh critical SDET, RC/stable,
  deployment, release, and remote effects remain unsupported. Claim
  `autonomous-work-campaign-v1` remains `unknown`.
- **Retry condition:** Do not repeat task 6.1 capture or replays unless readiness production,
  proof, runtime-manifest, profile, or evaluator identity materially changes.

## 2026-08-28 - Build A Policy-Free Portable Campaign Supervisor

- **Requested capability and trigger:** Task 6.2 adds a registry-driven long-running process,
  health client, retry/backoff, log retention, and exact status/resume/stop adapter, so reuse
  discovery is required.
- **Sources reached:** Current campaign contracts/controller/state/CLI and task 3.2 interrupted
  handoff proof; one-shot portable process supervisor; roadmap mission child-process owner; Windows
  workstation health, identity, and log patterns; Node `fs`/`spawn`/`fetch`/`crypto`. No explicit
  allowed cross-project peer source/scope exists, so cross-project discovery is `degraded`; no
  package or ecosystem dependency is needed.
- **Material candidates:** Reuse campaign-owned state and resume semantics plus fixed CLI
  entrypoints; reuse Node process/filesystem/network primitives. Reject extending the one-command
  timeout supervisor, adding campaign policy to the workstation god file, accepting registry argv,
  or starting another OpenCode runtime.
- **Disposition:** `build-minimal` one cohesive portable supervisor owner. Registry policy is finite
  and contains only safe ids, canonical roots, contained definition paths, loopback runtime identity,
  and bounded process/log controls. Supervisor reads campaign-owned advice and never interprets
  phase, severity, wave, mission, completion, or evidence prose.
- **Readiness correction:** Fresh review `ses_fb912f8ccffef7flb1Z31gs2Mv`
  (`xai/grok-4.6`) confirmed `OR-CAMP-62-001`: semantic `paused-transient` cannot be resumed by the
  supervisor because it lacks immutable assignment/result/retry evidence, and argv spawn alone is
  not progress. Corrected candidate `task-6-2-inline-frame-r2` defines the happy path as a safe
  runtime/controller interruption after durable mission launch with writer clear and a terminal
  correlated mission handoff. Exact resume must consume the handoff and durably reach verification;
  semantic/provider transient remains suppressed as external input required.
- **Advice contract:** Campaign status owns `resume|suppress|unknown` with enumerated reasons
  `runtime-interruption-ready`, `terminal-evidence-restored`, `explicit-stop`, `owner-protected`,
  `budget`, `definition-or-project-drift`, `writer-or-cleanup-unknown`, `active-operation`,
  `external-input-required`, `complete`, and `not-started`. Only terminal mission handoff or restored
  terminal evidence may advise resume; unclassified facts advise unknown.
- **Corrected re-review:** Fresh review `ses_fb90caac2ffeJvsQQUNTlsRtzV`
  (`xai/grok-4.6`) returned `no-material-finding` after all six readiness attacks.
- **Operating envelope:** Provider-free disposable local projects/processes and registry-local
  supervisor files only; no Windows install/task/registry mutation, provider call, project source
  edit, arbitrary command, secret output, remote effect, emergency kill, deployment, or release.
- **Observable proof:** Production preflight/status/advice/resume consumes one terminal mission
  handoff exactly once and advances state/report to verification; exact fixtures suppress every
  named class; duplicate lease, process failure/backoff, log generations, signal/stop, privacy,
  project-byte stability, cleanup, capture, and zero-live replay are current.
- **Retry condition:** If current campaign facts cannot produce one enumerated advice token, report
  unknown and do not launch. Do not add provider retry or Windows host behavior to task 6.2.

## 2026-08-28 - Task 6.2 Portable Supervisor Seal

- **Identity:** Candidate `task-6-2-portable-supervisor-r1`; environment
  `node-24.18.1-windows-task-6-2-r1`.
- **Runtime proof:** Production `work-campaign-supervisor.ts` consumed one campaign-owned
  `runtime-interruption-ready` handoff through exact sibling `work-campaign.ts resume` and durably
  reached verification. A live duplicate lease was suppressed without clearing ownership; one
  synthetic unexpected child exit observed exactly one 10 ms backoff, re-reconciled campaign
  status, and consumed the handoff once on its bounded successor. Signal and explicit stop paths
  recorded campaign and mission stop intent before ending owned execution.
- **Safety and privacy controls:** Status and unavailable-runtime paths wrote no supervisor runtime
  or project state. Registry drift, path escape, arbitrary `argv`, and unknown fields failed closed.
  The password remained environment-only and absent from registry, report, stdout, stderr, and
  bounded external log generations. The supervisor never started OpenCode, issued campaign policy,
  interpreted lifecycle prose, or retried semantic/provider transient work.
- **Evidence:** `task-6-2-controller-r1` completed with 98 local process starts, zero provider,
  OpenCode, host, and source-write effects, and every supervisor evaluator row true. Independent
  create-new replays `task-6-2-controller-replay-r1a` and `r1b` both completed with
  `liveCalls: 0`.
- **Validation:** Focused work-campaign, library 179/179, installer 30/30, strict OpenSpec, operation
  gate, evidence inventory, and `git diff --check` passed. Fresh code-quality review
  `ses_fb8dbc7c7ffeDfUmDBR4TsSTOV` (`xai/grok-4.6`) identified and main applied only three safe
  public-surface reductions: one dead export, six private-only type exports, and one duplicate
  absolute-path guard. Supervisor ownership remains cohesive at the portable process boundary.
- **Claim ceiling:** Task 6.2 supports only provider-free portable supervisor re-entry over
  disposable local projects and an already-running authenticated loopback runtime. It does not
  support Windows registration/task installation, host re-entry, configured composition, critical
  SDET closure, RC/stable, deployment, release, or remote effects. Claim
  `autonomous-work-campaign-v1` remains `unknown`.
- **Retry condition:** Do not repeat task 6.2 capture or replays unless supervisor production,
  source inventory, proof evaluator, process profile, or runtime identity materially changes.

## 2026-08-28 - Correct The Task 6.3 Windows Ownership Frame

- **Architecture challenge:** Fresh owner review `ses_fb8bf3fd2ffebdbGUnbzdBGkrI`
  (`xai/grok-4.6`) reported three current defects. The proposed nested campaign subroot would be
  recursively deleted by existing workstation rollback without unregistering the campaign task;
  the protected root/credential/ACL/owner-logon protocol had no shared exported contract; and
  `WORK_CAMPAIGN_RUNTIME_FILES` alone was not a runnable installed import closure.
- **Main reproduction:** `opencode-workstation.ts` removes its complete protected root recursively
  and inventories only its two current tasks. Its lifecycle constants and ACL/task policy were
  private. The portable supervisor imports shared portable-process and roadmap-mission owners that
  are outside the campaign-only list. All three findings are confirmed for the proposed frame.
- **Correction:** Use independent protected sibling root
  `C:\ProgramData\OpenCodeWorkCampaignSupervisor`, a small shared workstation-layout contract for
  root/credential/ACL/owner-logon settings, and the complete maintained
  `PORTABLE_WORKFLOW_RUNTIME_FILES` closure preserving `global/` layout. Keep task 6.2 portable
  registry exact and place expected kit source plus source/installed digests in the separate
  protected Windows install manifest. Existing and campaign rollback remain independent and may
  remove only their attributable root/task/material.
- **Operating envelope:** Task 6.3 remains preview/check/repair-plan/rollback-plan and disposable
  fixture materialization only. No live ACL, ProgramData, Scheduled Task, registry, credential,
  process, installation, activation, or rollback mutation is authorized before task 6.4.
- **Retry condition:** Reopen this frame only if a corrected-candidate architecture review finds a
  remaining source/install/runtime identity or cross-rollback contradiction.
