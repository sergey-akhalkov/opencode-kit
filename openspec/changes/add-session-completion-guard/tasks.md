## 1. Freeze Runtime Contract And Baseline

- [x] 1.1 Load the Material change-ready workflow before production mutation; freeze the accepted outcome, protected boundaries, Candidate Reference method, root RC counter, exact OpenCode/SDK/`opencode-pty` compatibility matrix, disposable runtime-proof environment, and external-operation state. Pass condition: the implementation brief names the no-model async gate, fail-closed unknown state, user-stop priority, permission-allow decision, old-reviewer migration order, and unchanged non-goals.
- [x] 1.2 Capture a non-mutating baseline for `opencode --version`, installed/declared plugin and SDK versions, active plugin source paths, current PTY package source, current agent/profile inventory, `npm run validate:strict`, focused session-plugin tests, full `npm test`, and `openspec validate add-session-completion-guard --strict`. Preserve exact commands, exit codes, stdout/stderr, and pre-existing failures before changing production behavior.

## 2. Build Privacy-Safe Completion Evidence

- [x] 2.1 Extend the production `session_delivery_context` projection and plugin metadata to separate human messages, synthetic messages, human question replies, guard interventions, todos, bounded assistant/tool/diff/validation evidence, descendant/background state, strategy refs, and truncation warnings. Do not edit automated tests in this production-author task. Pass condition: a direct disposable DB/tool invocation observably keeps synthetic guard/PTY/task text out of human requirements and requirement signals while preserving redaction.
- [x] 2.2 Add the hidden read-only `session-completion-arbiter` production agent contract and versioned structured verdict schema; route it in every committed model profile without removing the old reviewer yet. Pass condition: `opencode debug agent session-completion-arbiter` shows the intended hidden role, configured model route, guard-supplied session-delivery evidence contract, and denied tool/mutation/orchestration/question capabilities.

## 3. Implement Deterministic Runtime Guard

- [x] 3.1 Align the supported v2 SDK/plugin dependency, pin `opencode-pty`, add the explicit kit-relative PTY bridge and completion-guard entrypoints, and assert one shared manager identity plus required manager/client capabilities. Do not edit automated tests in this production-author task. Pass condition: a disposable OpenCode process loads both extensions from the kit source and both observe one spawned PTY id through the same manager.
- [x] 3.2 Implement the per-root single-flight state machine, audit epoch/CAS correlation, root/child filtering, idle settle/recheck, compaction and guard-turn suppression, privacy-safe diagnostics, and cleanup on root deletion/plugin disposal. Pass condition: duplicate idle/status events for one unchanged root produce one audit epoch, while a changed revision makes the prior result stale and side-effect free.
- [x] 3.3 Implement the PTY lease adapter using tool-call correlation, manager `list/get`, manager callbacks, immediate-exit tombstones, awaited `notifyOnExit` semantics, notification-consumption tracking, and one fallback synthetic exit notification when the normal prompt is absent. Pass condition: active and unknown awaited PTY states produce Waiting with zero arbiter children/model calls, and terminal evidence cannot reopen a lease.
- [x] 3.4 Implement built-in background task/descendant leases and result-consumption tracking. Pass condition: a root idle while a background child is running or its result is unconsumed produces Waiting with zero completion audit, then resumes normal preflight only after the synthetic task result reaches the root.
- [x] 3.5 Implement retained arbiter child creation, exact JSON verdict transport, same-child one-model exponential retry, status/toast transitions, validated plugin-constructed synthetic continuation, root agent/model/variant/tool restoration, and Passed-without-transcript behavior. Pass condition: one incomplete root revision receives exactly one current continuation; one completed revision receives Passed; prose/fenced/malformed/provider-failed verdicts never resume main without a valid current schema.
- [x] 3.6 Implement race-safe `question.asked` escalation, human-reply precedence, guard rejection provenance/correction, exact owner-only preservation, `MessageAbortedError`/interrupt suspension, and non-synthetic Russian/English stop detection with negation handling. Pass condition: concurrent human reply defeats guard rejection, autonomous rejection is not recorded as a human answer, and user interrupt prevents the next idle from restarting work.
- [x] 3.7 Implement strategy fingerprinting, relevant OpenSpec-history discovery, per-session docs fallback, exclusion of journal-only mutations from progress, retry-condition enforcement, and main-owned `troubleshooter` task routing/verification. Pass condition: a failed strategy cannot repeat without qualifying evidence, ambiguous change ownership selects the docs fallback, and a stuck case requires one evidenced troubleshooter route rather than another equivalent attempt.
- [x] 3.8 Apply the accepted simple permission behavior in the guard config hook: merged top-level permission defaults to `allow` with no permission model/classifier, while explicit specialist-agent restrictions remain intact. Pass condition: a disposable root main request proceeds without an ask prompt and a disposable arbiter edit/task/question attempt remains denied.

## 4. Prove The Initial Happy Path

- [x] 4.1 Run and preserve a disposable real OpenCode proof for the deterministic preflight: spawn an awaited PTY, let root become idle, observe Waiting and zero arbiter/model audit, terminate the PTY, observe normal or fallback synthetic exit delivery, and verify exactly one subsequent completion audit. Preserve root/child refs, PTY id/status transitions, timestamps, model-call count, exit status, stdout/stderr, logs, and cleanup evidence under this change's implementation evidence.
- [x] 4.2 Run-observe-correct the representative completion path in the same production-author context: an incomplete accepted task must produce one structured `continue` and one root continuation; the corrected/complete task must produce Passed without a transcript success turn; a user interrupt must suppress continuation. Promote only to MVP when these outcomes reach the actual root-session boundary on the current candidate.

## 5. Complete Scope And Retire The Old Reviewer

- [x] 5.1 Complete all accepted failure paths and operating-envelope behavior not exercised by the initial proof: background child waiting/result consumption, stale verdict, question race, owner-required handoff, audit retry/backoff, restart metadata reconstruction, unknown lease fail-closed behavior, strategy journal fallback, and troubleshooter verification. Keep diagnostics bounded and preserve original causes at owning boundaries.
- [x] 5.2 After current guard proof exists, migrate active global/reusable/template instructions, README catalogs, profiles, agent inventories, validators, contracts, fixtures, installer/config docs, and current tests from `session-delivery-reviewer` to the automatic guard/arbiter contract; delete the active old agent and config/profile keys. Preserve archived OpenSpec evidence and feedback history as historical attribution. This production mutation returns the candidate to development and invalidates affected proof/validation.
- [x] 5.3 Re-run the complete representative root-session proof on the migrated candidate, including programmatic PTY no-model waiting, completion continuation/pass, user pause, question handling, and main permission allow with specialist denies. Preserve a new Candidate Reference and promote to MVP only after the current post-migration candidate is green.

## 6. Fresh Critical SDET

- [x] 6.1 After MVP proof and accepted-scope completion, dispatch one fresh `sdet-quality-engineer` with the original requirements, current Candidate Reference, raw runtime proof, concurrency/lease/question/retry envelope, and explicit production-denied test-only scope. Require `critical-risks-reported | no-critical-risk | blocked`; only SDET may add or modify automated tests, fixtures, simulators, or harnesses for reachable critical incidents.
- [x] 6.2 Main independently reproduces and dispositions every SDET row. If and only if a current reachable critical/non-deferrable defect is confirmed, route the smallest production correction to the production author, regain complete runtime proof, and allow the one fresh corrected-candidate SDET continuation permitted by policy. Park non-critical findings without prolonging testing.

## 7. Validation And Stable Local Handoff

- [x] 7.1 Run the SDET-authored focused guard/context/config/profile tests, existing focused session-plugin and contract suites, `npm run validate:strict`, `npm test`, `npm run openspec:validate`, `openspec validate add-session-completion-guard --strict`, and `git diff --check`. Separate pre-existing baseline failures from candidate regressions; preserve exact command outputs and do not mark this task complete while an applicable current-candidate gate is red.
- [x] 7.2 Run privacy-safe runtime-source and agent diagnostics against a fresh disposable OpenCode process to prove the kit PTY bridge/guard source, shared manager capability, hidden arbiter route, main allow default, specialist denies, absence of active old-reviewer routing, and no duplicate cache-loaded PTY plugin. Do not install or activate the candidate in the owner's continuing OpenCode process as part of qualification.
- [x] 7.3 With accepted scope complete, current proof green, terminal SDET state recorded, applicable validation green, no known reachable critical/non-deferrable defect, and external operations still not performed, freeze the next RC and write the complete local handoff: outcome, changed surfaces, proof, validation, strategy history, known non-critical limitations, rollback, Development-Stage, Stable Candidate, restart requirement, and exact activation state.

## 8. Add The Per-Root Read-Only Audit Monitor

- [x] 8.1 Reopen the qualified RC1 candidate under Material routing and freeze the owner-approved monitor increment: no root fork, no interactive `opencode attach`, no second server, one read-only minimized PowerShell shell monitor per guarded root/runtime, portable default off, machine-local opt-in on, Passed auto-close after 15 seconds, other terminal states remain visible, and monitor failure never affects guard behavior. Preserve the current OpenCode/Node/Bun/Windows baseline; set `Development-Stage: development` and keep the root RC counter at 1. **Current Rung**: preserved RC1 runtime evidence. **Next Real Boundary**: local monitor against a disposable OpenCode server. **Blocker/Unblocker**: none. **Observable Proof**: accepted capsule, baseline commands, current environment identity, no external activation.
- [x] 8.2 Implement the production-only monitor option parser, privacy-safe status projection, one-launch-per-root minimized PowerShell shell launcher, foreground-preserving hidden launch boundary, one-use IPC handoff, and Node console that opens only the root-correlated OpenCode database read-only for persisted root/children metadata. Use the plugin-provided client for guard operations and keep monitor/storage responsibility outside `controller.ts`; do not author automated tests. **Current Rung**: offline production component. **Next Real Boundary**: actual minimized PowerShell shell launch with the production console. **Blocker/Unblocker**: none; owner authorized the visible local window and selected PowerShell after Windows Terminal proved unavailable. **Observable Proof**: provided-client identity, root/database correlation, exact encoded bootstrap/IPC inventory, minimized launch contract, duplicate-launch suppression, root/child render, terminal-delay behavior, and failure-isolated controller state.
- [x] 8.3 Run-observe-correct the monitor at the earliest real boundaries: first attach the production console read-only to disposable persisted root metadata with synthetic transitions, then exercise one real owner-launched TUI root through Waiting with zero audit child and through one completion audit to Passed using the same monitor window. Preserve process/window/database identity, pre/post foreground identity, minimized show-state, privacy-safe root/child/audit refs, state order, model-call count, stdout/stderr/logs, close timing, root cleanup, and proof that monitor launch/read/close failures do not alter the root. Promote only to MVP on the current candidate. **Current Rung**: local integration. **Next Real Boundary**: end-to-end user-visible minimized PowerShell shell workflow. **Blocker/Unblocker**: owner restart after source validation because plugin source is loaded once. **Observable Proof**: one minimized non-activating shell per root, no duplicate on repeated events/retry, correct Waiting/Auditing/Passed rendering, auto-close, unchanged guard side effects, immutable command/output bundle.
- [x] 8.4 Complete the accepted monitor scope after MVP: document states, lifecycle, local opt-in, headless fallback, privacy, restart, manual close, one-window ownership, and coherent rollback; update template/installer/validator surfaces; enable only the owner's machine-local kit config without installing, restarting, or activating the continuing OpenCode process. Re-run the affected current-candidate runtime proof after any production mutation. **Current Rung**: proven monitor happy path. **Next Real Boundary**: fresh-process config/source diagnostics. **Blocker/Unblocker**: none. **Observable Proof**: portable template off, local config on, schema-valid fresh debug config, no provider secrets/ids in argv/output, current Runtime Proof restored.
- [x] 8.5 After current monitor MVP proof and accepted-scope completion, dispatch one fresh test-only `sdet-quality-engineer` with the expanded original requirements, Candidate Reference, raw window/storage proof, process/window lifecycle and privacy envelope, and exact production-denied scope. Require `critical-risks-reported | no-critical-risk | blocked`; main independently reproduces every critical row and follows the one-correction/one-fresh-continuation rule. **Current Rung**: current end-to-end proof. **Next Real Boundary**: SDET-authored critical regression oracles plus project validation. **Blocker/Unblocker**: none after task 8.4. **Observable Proof**: terminal SDET action, Effective Model, critical disposition, test-only diff attribution.
- [ ] 8.6 Run the SDET-authored monitor tests, focused guard/config/installer suites, `npm test`, `npm run validate:strict`, `npm run openspec:validate`, strict target validation, fresh runtime-source/config diagnostics, and `git diff --check`. With no known reachable critical/non-deferrable defect, freeze monotonic RC2 and write the complete stable local handoff including RC1 history, monitor proof, limitations, rollback, restart requirement, exact local-config/activation state, and external operations. **Current Rung**: validated local candidate. **Next Real Boundary**: owner-controlled restart/activation after this change. **Blocker/Unblocker**: separately authorized restart only; not required for local stable handoff. **Observable Proof**: all checks green, no unchecked tasks, `Development-Stage: stable`, `Stable Candidate: RC2`.

## 9. Make Grind Explicit Per Root

- [x] 9.1 Freeze the owner-added control contract: plugin installed but every new root default-off; `/enable-grind` and `/disable-grind` are idempotent, persisted, current-root only; disabled means no audit/model/continuation/fallback/question interception/monitor; the control turn is never audited. **Current Rung**: current owner-TUI behavior and current OpenCode 1.18.15 command/plugin source. **Next Real Boundary**: command hook plus disabled-root idle in a disposable local runtime. **Blocker/Unblocker**: none. **Observable Proof**: accepted command names, SDK hook shape, command loader source, root metadata contract, exact side-effect suppression list.
- [x] 9.2 Implement plugin-owned command registration, persisted `grindEnabled`, disabled state, command-turn suppression, enable/disable transition, active guard-work cancellation, and disabled gates at idle, PTY/task lease, question, continuation, status-monitor, and retry boundaries. Do not modify automated tests in this production-author task. **Current Rung**: offline production component. **Next Real Boundary**: direct controller command/idle replay. **Blocker/Unblocker**: none. **Observable Proof**: default disabled, sibling isolation, idempotence, next-human enable semantics, disable cancellation, no disabled monitor/model/fallback/rejection.
- [x] 9.3 Run-observe-correct a disposable root through ordinary disabled chat with zero guard child/model/window, `/enable-grind` confirmation without self-audit, one enabled deterministic preflight/audit transition, `/disable-grind`, and another ordinary idle with no guard side effect. Preserve command events, root metadata, child/model counts, stdout/stderr/logs, exact source/config identity, and cleanup. **Current Rung**: local integration. **Next Real Boundary**: owner-TUI slash-command workflow after restart. **Blocker/Unblocker**: none for disposable proof; owner restart only for later interactive confirmation. **Observable Proof**: one root `disabled -> enabled -> audited -> disabled`, sibling root remains disabled, no control-turn audit.
- [ ] 9.4 After current opt-in MVP and accepted-scope completion, run the required fresh critical-only SDET, independently disposition its rows, and run focused guard/config/installer tests, full `npm test`, strict repository/OpenSpec validation, runtime command inventory, and `git diff --check`. Re-run affected monitor proof with `/enable-grind` before RC2. **Current Rung**: proven local opt-in behavior. **Next Real Boundary**: SDET and complete validation. **Blocker/Unblocker**: none after 9.3. **Observable Proof**: terminal SDET action, all applicable checks green, updated Candidate Reference and lifecycle evidence.
