## Fidelity Ladder

All slices used one ladder: **Rung 1** deterministic contracts/offline replay; **Rung 2** installed `jscpd` against disposable/local bounded scopes; **Rung 3** fresh same-model OpenCode processes through the real global loader, command, skill, and existing reviewer; **Rung 4** later production reductions in unrelated repositories. Rung 4 remains outside this change.

## 1. Baseline And CLI

- [x] 1.1 Freeze the outcome, active-change ownership, six same-input scenarios, Product Candidate/Runner/Evaluator identities, and pre-mutation global loader baseline; capture six baseline fresh sessions before loaded dedup artifacts change. **Current Rung**: 1/3. **Next Real Boundary**: installed CLI. **Blocker/Unblocker**: none. **Observable Proof**: `implementation-evidence/pre-change-baseline.md`, `baseline-sessions/`, and `baseline-evaluation/`; six exits `0`, source unchanged, sessions/roots removed. **Authorization/Safeguards**: standing bounded synthetic authorization; disposable non-sensitive repositories; edits/remote/external tools denied. **Restoration/Cleanup**: terminal green cleanup in every bundle. **Expected Immutable Evidence**: preserved route/tool/token/time/source/cleanup facts. **Dependency Stop Condition**: satisfied before Product Candidate mutation.
- [x] 1.2 Install npm-global `jscpd@5.0.14`; prove version, package identity, controlled clone detection, bounded repository scan, exclusions, source immutability, and cleanup without a repository dependency or upstream skill. **Current Rung**: 2. **Next Real Boundary**: fresh `/dedup` command. **Blocker/Unblocker**: none. **Observable Proof**: `host-installation.md`, preserved first evaluator-red `cli-proof/`, corrected `cli-proof-attempt-2/`, and current `final-cli-proof/`. **Authorization/Safeguards**: explicit host-install request; pinned npm-global package; `--no-tips --no-colors`; no target writes or `--no-gitignore`. **Restoration/Cleanup**: disposable fixture removed; requested CLI retained. **Expected Immutable Evidence**: version `cpd 5.0.14`, scan exits/stdout/stderr/hashes. **Dependency Stop Condition**: green before instruction implementation.

## 2. Global Workflow And Behavior

- [x] 2.1 Add the lazy global `deduplication-audit` skill, `/dedup` command, all-profile entry, and README catalog entry; preserve scoped read-only candidate semantics, closed classifications/recommendations, semantic enrichment, unique-oracle/coupling gates, existing reviewer reuse, and main-only later production slices. **Current Rung**: 2. **Next Real Boundary**: Rung 3 exact clone. **Blocker/Unblocker**: installed CLI proof green. **Observable Proof**: `final-candidate-preflight/preflight.json` and `final-runtime-proof/exact-clone.bundle.json`; loader exposes skill, command expands, CLI/reviewer run, exact clone/canonical owner/runtime proof reported, source unchanged. **Authorization/Safeguards**: disposable scope; only `jscpd`, read/search, skill, and `code-quality-reviewer`; no production write or exhaustive route. **Restoration/Cleanup**: session/root deletion green. **Expected Immutable Evidence**: current candidate hashes and raw events. **Dependency Stop Condition**: satisfied; exact happy path green.
- [x] 2.2 Evaluate local owner, exact clone, semantically different near clone, unique compatibility oracle, no-match helper, and trivial fix with same profile/input/envelope; correct only proof-fixture/evaluator defects and replay all preserved bundles offline. **Current Rung**: 3. **Next Real Boundary**: fresh SDET. **Blocker/Unblocker**: first corpus identified two exact missing fixture observations; provider-free scenario preflight and matching baseline/candidate overrides unblocked bounded recapture. **Observable Proof**: original expected-red `behavior-evaluation-original-replay/evaluation.json`; final green `behavior-evaluation-final/evaluation.json` with `candidateOraclesPass=true`. **Authorization/Safeguards**: bounded synthetic calls, preserved original bundles, no product mutation, privacy sanitizer, no repeat of unaffected scenarios. **Restoration/Cleanup**: all roots/sessions removed and path evidence sanitized. **Expected Immutable Evidence**: base, corrected overrides, explicit oracle facts, source hashes. **Dependency Stop Condition**: satisfied; accepted scope reached MVP.

## 3. Fresh Critical SDET

- [x] 3.1 Dispatch one fresh test-only `sdet-quality-engineer` after MVP; add the smallest contract/loader/routing test module and aggregator registration, then have main independently reproduce it. **Current Rung**: 3 plus deterministic tests. **Next Real Boundary**: complete validation. **Blocker/Unblocker**: current MVP and closed writers. **Observable Proof**: `critical-sdet.md`; `Action: no-critical-risk`; `node tools/test-contracts.ts` exit `0`, six dedup rows, `OK: contracts tests=62`. **Authorization/Safeguards**: writes limited to `tools/test-contracts-deduplication.ts` and aggregator import/spread; no production/host/provider/remote mutation. **Restoration/Cleanup**: no persistent fixture state. **Expected Immutable Evidence**: SDET identity/model, changed tests, negative in-memory mutations, main replay. **Dependency Stop Condition**: terminal SDET satisfied on first precondition-valid attempt.

## 4. Validation And Handoff

- [x] 4.1 Run focused tests, proof/evaluator replays, `npm run validate:strict`, full `npm test`, `npm run openspec:validate`, `npm run prepush:validate`, and strict validation of this change; reconfirm host CLI, global loader, upstream-skill/new-agent/repository-dependency absence, and frozen exact Runtime Proof. **Current Rung**: 1-3 qualified evidence. **Next Real Boundary**: none inside this change. **Blocker/Unblocker**: terminal SDET; none remaining. **Observable Proof**: `final-validation.md`, `final-cli-proof/`, `final-candidate-preflight/`, and `final-runtime-proof/`; all applicable commands green. The initially planned unsupported `openspec:gate --operation prepush` returned `Unknown OpenSpec operation prepush` and was replaced by the repository-native green `npm run prepush:validate`. **Authorization/Safeguards**: read-only/local/disposable checks, unrelated work preserved, no commit/push. **Restoration/Cleanup**: final proof cleanup green. **Expected Immutable Evidence**: exact command/result summaries and current hashes. **Dependency Stop Condition**: satisfied; RC1 eligible.
- [x] 4.2 Freeze RC1 and complete the local handoff with outcome/non-goals, architecture, installation, evidence topology, Runtime Proof, behavior evaluation, terminal SDET, validation, rollback, residual risks, restart requirement, and external-operation state. **Current Rung**: qualified Rung 3. **Next Real Boundary**: separately requested Rung 4 audit/reduction in another repository. **Blocker/Unblocker**: all current gates green. **Observable Proof**: `stable-handoff.md` records `Development-Stage: stable` and `Stable Candidate: RC1`. **Authorization/Safeguards**: local evidence only; no production refactor, commit, push, release, or uninstall. **Restoration/Cleanup**: requested global CLI retained; no disposable state open. **Expected Immutable Evidence**: final Candidate Reference and evidence paths. **Dependency Stop Condition**: satisfied; change is ready for local handoff, not archive or release.
