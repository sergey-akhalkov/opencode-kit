## 1. Ownership And Current Baseline

- [ ] 1.1 Resolve the planning-only ownership gate before any production mutation: verify `make-team-advice-evidence-triggered` is archived with terminal writers, obtain ownership declarations or exact non-overlap for every ownership-undeclared active change (currently `add-autonomous-kaizen-grind` and `deliver-continuous-reusable-value`), reread every transferred root, update `ownership.json` transfers and `mutationEnabled` only from current facts, and verify `npm run openspec:change-inventory -- --root . --mode ownership` plus the propose/apply ownership checks report no unresolved overlap. **PCR-001 boundary:** no behavior mutation while ownership is unknown.
- [ ] 1.2 Capture the provider-free current baseline for the exact planned owners by running `npm run test:focused:openspec-gate`, `npm run test:focused:consumer-outcome`, and `npm run test:focused:contracts`; inspect status/stdout/stderr and attribute any pre-existing dirty-tree failures before changing production files. **Current rung:** existing structural contracts. **Next boundary:** deterministic protocol and pack leaves.

## 2. Candidate-Separated Record Contract

- [ ] 2.1 Extend the bounded-falsification parser and formatter with optional protocol mode, reconstruction reference, candidate-at-reconstruction state, initial continuity, and corrected-review freshness fields; preserve old records as single-stage only; add positive and malformed/unknown compatibility cases; and verify `npm run test:focused:openspec-gate` passes with exact cause-preserving failures. **Observable result:** a closed pre-authoring-separated record requires a non-`none` reconstruction ref, absent candidate state, and verified initial continuity without claiming semantic readiness.
- [ ] 2.2 Extend operation-gate and contract validation for one pre-authoring/two-call initial challenge, unknown chronology or continuity, fresh corrected-candidate comparison against a current frozen reconstruction, stale-reconstruction unknown, and legacy readback; verify focused gate/contracts tests pass and no historical artifact is rewritten. **Next boundary:** provider-free consumer pack can consume the explicit record facts.

## 3. Answer-Free Consumer Pack

- [ ] 3.1 [automation-dividend] Add the versioned `prospective-consequence-rehearsal` pack contract, reviewed generic PCR-001 seed population including Material-without-plan inline framing and corrected-review freshness/staleness, evaluator-only expected observations, stable ordering, explicit provider/task bounds, and maximum claim; add negative fixtures for actor-visible `representedRisk`, corrected answers, premature file/inline candidate materialization, expected labels, unknown fields, and path escape; verify pack preflight fails before sessions/provider calls with the exact field cause.
- [ ] 3.2 Extend capture to record candidate-absent filesystem and root-prompt state, run stage one with normal tools, record all stage-one prompts/files/tool results, materialize the file candidate or inline decision frame only after successful reconstruction, and resume the exact returned child; preserve task status/stdout/stderr, forbidden-effect sentinels, terminal cleanup, and honest unknown chronology/continuity; verify provider-free fake-adapter cases cover file and inline success, premature candidate creation, stage-one failure, stale/missing identity, stage-two failure, corrected-review freshness/staleness, and cleanup uncertainty.
- [ ] 3.3 Extend provider-free evaluation with exact temporal file/inline candidate absence, full stage-one stream, continuation, reconstruction/comparison, corrected-review, unknown, proportional-routing, task/provider-bound, forbidden-effect, identity, and cleanup oracles; treat intentional extra reviewer calls as pack-specific expected behavior rather than general friction; verify deliberate red bundles fail by arm/scenario/sample/field and green bundles emit only the PCR-001 maximum claim.
- [ ] 3.4 Integrate the new pack into `tools/proofs/consumer-outcome-regression.ts` help, selection, preflight, capture, and evaluate routes; run `npm run proof:consumer-outcome -- --mode preflight --pack prospective-consequence-rehearsal --source-ref working-tree` and `npm run test:focused:consumer-outcome`; verify zero configured calls/effects, stable pack identity, and byte-unchanged general/other focused-pack seeds. **Next real boundary:** installed configured comparison remains blocked until the reviewer candidate exists.

## 4. Reviewer And Routing Candidate

- [ ] 4.1 Extend `implementation-readiness-reviewer` with pre-authoring reconstruction, resumed initial comparison, and fresh corrected-candidate comparison against a frozen current reconstruction; retain evidence-bounded consequence rows, `no-material-finding`, no verdict, and no nested/writer authority; extend exact agent validators and verify focused contracts plus instruction inventory/canonicalization checks pass. **Observable result:** stable role/protocol markers without claiming semantic discovery from source inspection alone.
- [ ] 4.2 Reorder loaded OpenSpec and Material-without-plan authoring so semantic distinction pressure launches reconstruction before first candidate artifact/inline frame/source mutation, then exact `task_id` continuation follows file or frame authoring; preserve one-challenge accounting, single-stage existing candidates, fresh corrected review, direct Ordinary Small, and non-duplicating `behavioral-substitution-qualification`; verify focused contracts/gate/canonicalization checks pass with no new session helper, reviewer, skill, or procedure owner. **Next boundary:** bounded installed OpenCode temporal authoring comparison.

## 5. First Real Behavior Signal

- [ ] 5.1 Run the maintained preflight, then one explicitly bounded matched baseline/candidate PCR-001 authoring capture through the actual installed OpenCode entry point using the same configured model/profile, reviewed inputs, permissions, runtime, and disposable environment; evaluate with `--expectation improvement`; inspect pre-reconstruction filesystem/root-prompt identity, all stage-one model-visible inputs/results, post-reconstruction file and inline-frame candidate creation, source/model/environment identities, exact continuation/fresh corrected-review facts, output, status, stdout/stderr, effects, call bounds, liveness, and cleanup. Keep evidence disposable and report only current-run privacy-safe facts. **Observable proof:** pre-authoring separation and exact initial continuation improve the reviewed file and inline distinction population while stale context stays unknown, Ordinary Small stays direct, and substitution keeps its exact owner.
- [ ] 5.2 Reproduce and correct only accepted-scope failures from task 5.1 using a causally distinct mechanism after two similar non-progressing attempts; rerun the smallest affected provider-free or configured boundary and update tasks/history if evidence changes the route. Do not add risk vocabulary, hardcoded examples, a third generic challenge, or optional polish to force a green result. **Stop condition:** unresolved hard oracle, liveness, cleanup, or source-identity failure keeps the candidate in development and blocks the principle.

## 6. Evidence-Gated Canonical Principle

- [ ] 6.1 Only after task 5.1/5.2 reproduces improvement with every hard oracle green, add one concise project-neutral prospective-consequence statement to the canonical working-philosophy owner and consolidate any overlapping loaded routing instead of duplicating it; verify instruction inventory, exact-duplicate/canonicalization, agent, and focused contract checks pass. **Observable result:** no project/domain example, fixed step count, scoring rule, or new trigger for Ordinary Small.
- [ ] 6.2 Run one final matched PCR-001 configured comparison against the complete loaded candidate after the principle change; verify the improvement and every separation, continuation, unknown, proportional-routing, substitution, safety, privacy, liveness, effect, and cleanup oracle remain green under exact final source/model/environment identities. A regression removes or corrects the principle and returns to the smallest affected proof; it does not start an optimization loop.

## 7. Integration And Handoff

- [ ] 7.1 Run `npm run test:focused:openspec-gate`, `npm run test:focused:consumer-outcome`, `npm run test:focused:contracts`, `npm run instruction:inventory`, `npm run instruction:canonicalize`, `npm run validate:strict`, `openspec validate add-prospective-consequence-rehearsal --strict`, and the portable propose/apply operation gates; inspect exact failures and preserve unrelated dirty work. **Parent integration oracle:** all required leaves pass on the same readable final candidate; leaf evidence alone does not prove integration.
- [ ] 7.2 Confirm all proof-owned sessions/processes/temp roots are terminal or removed, no baseline pointer/other focused pack/remote state was changed, tasks and falsification record match current evidence, and the handoff reports PCR-001 maximum claim, runtime proof, validation, known limitations, loaded-source restart requirement, and external operations. Keep the change in development unless implementation and current real-boundary proof are complete.
