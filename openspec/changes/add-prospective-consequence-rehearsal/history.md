# Strategy History

## PCR-S1: Candidate-visible readiness review

- **Objective**: Detect a locally coherent plan that solves the wrong contextual problem before implementation.
- **Approach**: Supply the original request and candidate together to the existing readiness reviewer and ask it to attack coherent-wrong-outcome and related classes.
- **Evidence**: The current bounded-falsification contract and focused pack provide separate request/candidate fields but the reviewer sees both in one challenge; the pack also supplies `representedRisk`.
- **Outcome**: Insufficient for the reproduced abstraction-loss class.
- **Reason**: Candidate framing can anchor the review, while the supplied risk tests routing of a known issue rather than discovery from raw actors, state, lifecycle, and consumers.
- **Do Not Repeat**: Do not add more risk vocabulary, examples, or checklist items to the same candidate-first prompt and treat that as independent context reconstruction.
- **Retry Condition**: Retry only if new evidence proves a candidate-visible mechanism can establish independent reconstruction through an observable oracle that the current task inputs cannot satisfy.

## PCR-S2: Same-prompt blind phase

- **Objective**: Reconstruct context before evaluating the candidate without changing the reviewer topology.
- **Approach**: Put original evidence and candidate in one prompt but instruct the reviewer to ignore the candidate during an initial section.
- **Evidence**: Candidate bytes remain in the same model context, so tool-call and source evidence cannot establish that the initial reasoning was independent.
- **Outcome**: Rejected before implementation.
- **Reason**: The protocol would be blind only by instruction, not by observable input isolation, and would repeat the candidate-first failure with more formatting.
- **Do Not Repeat**: Do not call a phase candidate-separated when candidate bytes or a mechanism-revealing summary are present anywhere in that phase's model input.
- **Retry Condition**: Retry only if the runtime exposes a verifiable attention/context isolation primitive stronger than separate task inputs.

## PCR-S3: New universal consequence reviewer or skill

- **Objective**: Give prospective consequence analysis a dedicated owner.
- **Approach**: Add a new always-applicable reviewer/skill beside implementation readiness and exact substitution qualification.
- **Evidence**: `implementation-readiness-reviewer` already owns pre-implementation task fit and bounded falsification; `behavioral-substitution-qualification` already owns skip/omit/replacement equivalence.
- **Outcome**: Rejected in favor of extending existing owners.
- **Reason**: A sibling would duplicate routing, authority boundaries, and evidence reconciliation while burdening Ordinary Small work.
- **Do Not Repeat**: Do not add another universal critic, central router, or duplicate substitution procedure for the same semantic boundary.
- **Retry Condition**: Retry only if implementation evidence proves the existing readiness owner cannot express the bounded two-phase contract without mixing unrelated responsibility.

## PCR-S4: Same-worktree candidate-separated resumable review

- **Objective**: Discover current consequential distinctions from raw evidence before candidate framing, then compare the candidate without adding another owner.
- **Approach**: Launch one fresh candidate-free reconstruction brief in the active candidate worktree and resume the exact correlated reviewer child with the candidate; rely on path prohibitions plus first-task input inspection.
- **Evidence**: The first planning review omitted candidate bytes from the brief but project-wide grep emitted candidate-path hits into the stage-one tool/model stream. Readiness finding `PCR-ISO-01` and foundation findings `FI-PCR-001`/`FI-PCR-002` confirmed that disposable input oracles cannot prove the live same-worktree boundary.
- **Outcome**: Superseded before implementation; the candidate's physical-isolation and proof-composition claims were narrowed.
- **Reason**: Prompt/path convention does not prevent normal reviewer tools from reading an existing candidate, so all planned pack tasks could pass while the loaded workflow remained candidate-contaminated.
- **Do Not Repeat**: Do not call a phase physically separated when the current candidate already exists anywhere reachable by its normal tools, and do not compose a disposable first-input oracle into a different live worktree identity.
- **Retry Condition**: Retry only if a technically enforced boundary makes the current candidate absent from every stage-one model-visible input/result and the loaded proof exercises that same identity.

## PCR-S5: Pre-authoring chronological isolation

- **Objective**: Enforce candidate absence without adding a session/workspace owner.
- **Approach**: Launch context reconstruction after original-request foraging but before creating any candidate artifact, inline frame, or source mutation; create the candidate only after reconstruction; resume the exact child for initial comparison; use a fresh corrected-candidate reviewer with the frozen reconstruction when it remains current.
- **Evidence**: Current OpenSpec authoring already has a pre-draft failure-hypothesis step, the existing task adapter supports exact continuation, and candidate nonexistence prevents prompt, file, and tool-result leakage. Explicit-directory proof clients exist but are unnecessary for this first bounded path.
- **Outcome**: Selected as the corrected planning mechanism; implementation/runtime behavior remains unproved.
- **Reason**: This is a same-owner ordering correction with the earliest direct oracle and lower lifecycle/cleanup/context cost than a new isolated-session helper.
- **Do Not Repeat**: Do not retroactively label an existing candidate separated, rebuild context around a corrected candidate, or add isolation infrastructure while chronological absence satisfies the accepted happy path.
- **Retry Condition**: Implement only after ownership transfer; retain only if PCR-001 observes candidate absence across the complete stage-one stream, exact continuation, fresh corrected-review behavior, proportional controls, and cleanup.

## Foundation Recovery FI-PCR-2026-09-01-01

- **Relation**: Claimed same-worktree physical candidate isolation and disposable-pack/live-oracle equivalence versus observed candidate-path leakage into stage-one tool output.
- **State**: observed -> confirmed -> correcting -> swept -> re-reviewed -> closed.
- **Reproduction Evidence**: Initial readiness child reported incidental forbidden-path grep hits; main reproduced prompt-only/path-convention insufficiency; foundation owner confirmed `FI-PCR-001` and `FI-PCR-002` on candidate `working-tree-r1`.
- **Preserved Evidence**: Original request was independently supplied, the brief itself was candidate-free, exact child continuation worked in this run, the challenge remained finite, and Ordinary Small/substitution controls were not invalidated.
- **Correction**: Rebind initial separation to chronological candidate nonexistence and narrow disposable evidence to the identical temporal authoring environment; no archive is rewritten.
- **Corrected-Candidate Review**: Fresh foundation review `ses_fa23ff230ffeCP65AHqPUgaBl4` inspected `working-tree-r2` with effective model `openai/gpt-5.6-sol` and returned `no-material-finding`; runtime effectiveness remains unproved and bounded to future PCR-001 execution.

## PCR-S6: Inline frame after reconstruction

- **Objective**: Apply the selected chronological boundary to Material work that has no OpenSpec tree or prior decision artifact.
- **Approach**: Treat the smallest session-local decision frame as the candidate: reconstruct from original/raw evidence first, create the frame second, and resume the exact reviewer child with that frame; add a dedicated PCR-001 inline sentinel/oracle.
- **Evidence**: Fresh corrected-candidate readiness review `ses_fa2380801ffejOTnkMSJ9CO6Fd` found `PCR-INLINE-01`: the prior delta simultaneously forbade pre-reconstruction frame creation and retained create-frame-then-review behavior, while the pack covered only file candidates.
- **Outcome**: Corrected inside the existing mechanism after the second and final generic challenge; no third generic review is permitted.
- **Reason**: Without the inline ordering and oracle, all file-candidate tasks could pass while newly emerging session-local Material decisions remained candidate-first.
- **Do Not Repeat**: Do not treat a decision frame as neutral context when it already embodies the decision being challenged, and do not infer inline coverage from file-path sentinels.
- **Retry Condition**: Use only focused artifact validation and future PCR-001 runtime proof for this exhausted episode unless a materially changed owner requirement starts a new accepted-outcome episode.

## PCR-S7: Ad hoc direct configured runner

- **Objective**: Observe PCR-001 through installed OpenCode without adding a retained server or session owner.
- **Approach**: Invoke `opencode run` per stage with hand-built XDG paths and inline permissions, parse JSON events, delete recorded sessions, and remove the proof fixture/runtime after each scenario.
- **Evidence**: The first live baseline stopped on a false global-empty session oracle. After narrowing cleanup to recorded proof-owned sessions and preserving diagnostics, repeated one-scenario controls returned installed OpenCode `UnknownError` before any task/tool event. A separate maintained configured delivery-checkpoint capture passed on the same installed `1.18.26`, `openai/gpt-5.6-sol/xhigh` route, and host provider with one request.
- **Outcome**: Rejected after the provider/environment control isolated the defect to PCR's duplicate configured-run setup; no candidate lane ran.
- **Reason**: The runner omitted maintained config materialization, model-catalog seeding, database isolation, and configured proof environment controls, collapsing internal server failures into unhelpful command status.
- **Do Not Repeat**: Do not debug or retry another hand-built installed OpenCode environment while the current repository already owns a passing configured proof setup.
- **Retry Condition**: Retry only if the maintained configured environment is proved incompatible with the PCR temporal/session oracle.

## PCR-S8: Reuse maintained configured proof environment

- **Objective**: Preserve PCR's bounded temporal/session observations while removing the duplicate failing runtime setup.
- **Approach**: Reuse `materializeConfiguredProofConfig`, `seedProofModelsCatalog`, and `configuredProofServerEnvironment` with the existing PCR stage adapter, recorded proof-session cleanup, and create-new temporary evidence lifecycle.
- **Evidence**: Current-source comparison found the same proven helpers already used by configured consumer-outcome captures; the delivery-checkpoint environment control passed and PCR failed before tools under its hand-built environment.
- **Outcome**: Selected for the next one-scenario canary; full PCR-001 remains blocked until that canary passes with readable evidence and terminal cleanup.
- **Reason**: This is `reuse` of a verified current capability with lower implementation, proof, and cleanup cost than a new client or server owner. Cross-project discovery is not applicable because the current maintained owner fits the exact installed-runtime contract.
- **Do Not Repeat**: Do not add a new transport/client abstraction or widen the full pack until the reused environment passes the narrow control.
- **Retry Condition**: If the canary still fails before tools, preserve its diagnostics and switch to the maintained proof-server client rather than modifying prompt semantics or increasing retries.

## PCR-S9: Narrow invalid reliability and hidden-token claim

- **Objective**: Preserve the accepted reconstruct-then-compare outcome after the installed matched canary falsified the pack's broader discovery-superiority oracle.
- **Approach**: Keep all eleven PCR-001 members and capture-owned chronology, continuation, freshness, routing, safety, privacy, liveness, and cleanup oracles; compare candidate semantic output by reviewed containment state and presence rather than exact hidden natural-language identifiers; remove the one-sample `more reliably` claim and leave the canonical principle unchanged.
- **Evidence**: The matched `unlabeled-actor-distinction` canary observed baseline and candidate consequence counts of one each, three semantically related but byte-different identifiers, baseline closed where the fixture predicted unknown, and candidate structural absence/continuation success with an unknown terminal caused by ambiguous episode semantics. Evidence-sufficiency review `ses_f9beb91ebffe2CDLnmE6Ld7J9P` reported `PCR-ES-01` through `PCR-ES-05`: n=1 cannot support reliability, hidden natural-language ids are non-unique, and capture-owned protocol facts are independently valid.
- **Outcome**: Selected as an outcome-preserving evidence correction; no population, safety gate, configured bound, or required full-population proof is reduced.
- **Reason**: Exact hidden-token equality and a candidate-count inequality measured oracle phrasing and a statistically unsupported reliability claim, not the accepted temporal protocol. The original Outcome Capsule remains reconstruct before candidate, resume exactly, report evidence-backed distinctions, preserve unknowns, and keep proportional controls.
- **Do Not Repeat**: Do not tune prompts to force evaluator-only labels, erase the baseline discovery, claim reliability from one sample, or spend the unchanged remaining calls as salvage for a failed oracle.
- **Retry Condition**: Run the full matched population only after provider-free validation of the narrowed oracle; any missing candidate consequence, wrong terminal containment, structural mismatch, forbidden effect, bound breach, privacy leak, or incomplete cleanup still fails closed.

## PCR-S10: Preserve unknown in chronology-only controls

- **Objective**: Keep the full PCR-001 temporal population without claiming a downstream consequence that its reviewed facts do not establish.
- **Approach**: Retain exact inline/file candidate absence, materialization, continuation, task-bound, effect, identity, and cleanup oracles; classify the inline-frame and candidate-materialization chronology controls as `unknown` with no consequence row because their actor-visible facts stop before a consumer of the changed retry or terminal-label decision. Do not change prompts, add risk vocabulary, or tune candidate text toward evaluator labels.
- **Evidence**: The composed installed evaluation passed every structural oracle but both controls returned `downstream-effect-unproved`: the inline reconstruction could not establish that retry eligibility was bound to the caller discriminator, and the delivery reconstruction found no consumer of terminal labels. The prior closed expectations required links absent from the reviewed raw facts.
- **Outcome**: Selected as an evidence-ceiling correction. Existing captures retain diagnostic value but are not relabeled under the corrected pack digest; a fresh bounded matched population is required.
- **Reason**: Honest `unknown` is a non-deferrable outcome of prospective rehearsal. Forcing a consequence through prompt wording or a more leading candidate would test answer shaping rather than evidence-bounded review.
- **Do Not Repeat**: Do not treat a represented distinction alone as a reachable downstream consequence, alter hidden expected labels to match generated prose, or reseal captures against a changed evaluator contract.
- **Retry Condition**: Rerun the full matched installed population only after provider-free preflight and evaluator validation pass for the corrected pack; any chronology, continuation, required-consequence, identity, effect, bound, privacy, liveness, or cleanup miss still fails closed.

## PCR-S11: Explicit single-stage baseline route

- **Objective**: Capture each candidate-first baseline with exactly one fresh single-stage readiness task.
- **Approach**: Supply the already-existing candidate directly in an explicit one-task single-stage instruction and forbid candidate-free reconstruction, task continuation, or reviewer retry. Keep direct and exact-substitution controls on their existing no-readiness-task routes.
- **Evidence**: The corrected-pack full baseline stopped on `state-lifecycle-distinction` with `unexpected-task-repeat`. Its current diagnostic recorded three readiness tasks: a candidate-free reconstruction, a continuation whose brief omitted candidate bytes, and a fresh single-stage fallback. The root prompt had only said to use the applicable route; cleanup was complete, one configured root turn completed, and no forbidden effect was observed.
- **Outcome**: Selected as a proof-runner correction; the failed invocation is final and will not be repeated unchanged.
- **Reason**: Baseline capture owns exact task cardinality and candidate delivery. Leaving route selection implicit let the loaded authoring rules run a chronology protocol that cannot apply to an already-existing baseline candidate.
- **Do Not Repeat**: Do not use a generic route-selection prompt for a bounded baseline or let a missing candidate in an invalid continuation trigger a fallback reviewer.
- **Retry Condition**: Run the affected state/lifecycle baseline as a one-scenario canary after focused provider-free validation; proceed to a fresh full baseline only if it records one task, a valid observation, terminal cleanup, and no forbidden effect.

## PCR-S12: Explicit task-call schema at each reviewer stage

- **Objective**: Ensure each bounded reviewer stage reaches a child identity or fails for a substantive reviewer/runtime reason rather than an avoidable tool-input omission.
- **Approach**: Name the required `description`, `prompt`, and `subagent_type` fields in every fresh task instruction and the exact `task_id` field in continuation instructions without changing reviewer content, call bounds, or failure semantics.
- **Evidence**: The corrected candidate run reached `stale-reconstruction` but its one stage-one `task` event failed runtime schema validation with `Missing key at ["description"]`; child export and task output were empty, candidate creation did not occur, cleanup completed, and no forbidden effect was observed. Earlier scenarios happened to supply the field, so their sealed observations remain attributable.
- **Outcome**: Selected as the smallest Proof Runner correction; the failed stale-reconstruction invocation is final.
- **Reason**: The runtime task schema, not model semantic behavior, rejected the call. Making the existing required fields explicit removes that avoidable ambiguity while preserving the same one-task stage and fail-closed identity oracle.
- **Do Not Repeat**: Do not retry the same stage with only a wording variation that leaves the task schema implicit, and do not fabricate a child identity from the root session.
- **Retry Condition**: After focused provider-free validation, rerun only `stale-reconstruction`; continue only if stage one returns one valid readiness child and terminal cleanup remains complete.

## PCR-S13: Explicit evidence-bounded semantic containment

- **Objective**: Make the finite evaluator accept every honest evidence-bounded outcome without permitting fabricated consequences in controls that explicitly lack downstream evidence.
- **Approach**: Give every scenario one reviewed evaluator-only semantic mode: `required-consequence`, `required-unknown`, `evidence-bounded`, or `not-applicable`. The evidence-bounded mode accepts exactly closed plus one or more consequences and no unknown reasons, or unknown plus no consequence and an explicit reason; all mixed states fail. Keep insufficient evidence, unverified continuation, and stale reconstruction required-unknown.
- **Evidence**: Two matched inline captures with the same actor-visible facts and loaded candidate produced different defensible states: the earlier run stopped at unknown because it could not prove the retry-policy binding, while the corrected-pack run reported a closed `timeout-versus-explicit-rejection` consequence. Every chronology, continuation, task, identity, effect, and cleanup oracle passed in the latter composition; only the exact unknown expectation failed.
- **Outcome**: Selected as the explicit containment contract. The `d82cff` captures remain diagnostic evidence and are not resealed under the new pack digest.
- **Reason**: Requiring either one natural-language judgment for an evidence-ambiguous control turns model variation into a false failure. Accepting only two structurally coherent evidence states preserves fail-closed behavior without scoring or deriving semantics in code.
- **Do Not Repeat**: Do not force either branch through prompt tuning, allow a closed result without a consequence, allow unknown with a consequence or no reason, or weaken the explicit insufficient-evidence control.
- **Retry Condition**: Run provider-free preflight/evaluator negatives, then one fresh matched installed population under the new digest. Stop on any required-consequence/required-unknown, chronology, continuation, identity, effect, bound, privacy, liveness, or cleanup miss.

## PCR-S14: Deterministic TaskTool launch and exact child-session continuation

- **Objective**: Reach a verifiable reviewer child without spending another installed attempt on probabilistic root-authored task arguments.
- **Approach**: Run the maintained configured proof server, submit an SDK `subtask` prompt part with explicit description, prompt, agent, and model fields so installed OpenCode executes `TaskTool`, read back the exact correlated child and route, then send initial comparison directly to that same child session. Fresh baseline and corrected-review launches use new `subtask` parts; cleanup inventories and removes only the resulting proof-owned session tree and server.
- **Evidence**: The first state/lifecycle canary executed the reviewer child but capture searched only the terminal `session.prompt` response and missed the parent task part. After parent-message readback, the second canary returned both arms and exact child continuation, but the root session also continued after the subtask, authored alternate `candidate.md` content, and launched comparison before controller-owned materialization. The sentinel-only absence oracle did not detect that alternate candidate content.
- **Outcome**: Rejected after the bounded canary. The installed subtask transport did not preserve the required controller-owned stage boundary.
- **Reason**: This changes the failing transport mechanism rather than task-schema wording while retaining installed `TaskTool`, the configured reviewer route, exact parent/child evidence, the same continuation context, and the existing capture/evaluator owner.
- **Do Not Repeat**: Do not ask the root model to regenerate the same required task fields, treat direct root-session output as child evidence, mix old CLI-task captures with SDK-subtask captures, or broaden the claim to deterministic model-authored task arguments.
- **Retry Condition**: Reconsider only if the installed runtime exposes an evidenced task-only subtask invocation that cannot continue the parent model before controller-owned candidate materialization. Do not retry the current subtask route or its prompt wording.

## PCR-S15: Directly routed reviewer child with enforced read-only tools

- **Objective**: Preserve installed reviewer execution and exact child continuation while preventing any parent model turn between candidate-free reconstruction and controller-owned candidate materialization.
- **Approach**: Reuse `createRoutedProofSessions` to create a proof-owned parent and reviewer child on the configured route, prompt the child directly with mutation, nested-task, question, shell, and network tools disabled, capture that exact child's messages and route, materialize the candidate only after return, then continue the same child. Fresh single-stage and corrected comparisons receive new routed children. Treat any candidate-path appearance during reconstruction as present regardless of bytes.
- **Evidence**: The rejected S14 canary proved that an SDK subtask may be followed by a parent model turn before capture regains control; that turn created alternate candidate content which passed the prior sentinel-only check. The maintained proof helper already creates correlated parent/child identities without a parent model prompt, and provider-free capture/evaluator controls can detect alternate-content candidate-path materialization.
- **Outcome**: Selected for provider-free validation, then one state/lifecycle canary before any matched full-population capture.
- **Reason**: This removes the parent model from the critical interval, enforces reviewer read-only/non-nesting behavior technically, preserves the installed configured reviewer route and exact continuation identity, and strengthens the absence oracle without changing accepted semantics or the finite population.
- **Do Not Repeat**: Do not represent S14's green row as valid separation, rely only on candidate sentinel equality, resume a different child, or broaden direct-session proof into a claim about model-authored `task` arguments.
- **Retry Condition**: Proceed to the matched full population only if the S15 state/lifecycle canary records candidate-path absence through reconstruction, one exact continuation, valid semantic containment, no forbidden effect, current source/model/runtime identity, bounded calls, and terminal proof-owned cleanup.

## PCR-S16: Proof-owned read classification

- **Objective**: Distinguish a real protected effect from normal read-only inspection of the isolated configured proof source before repeating the S15 canary.
- **Approach**: Keep every outside-fixture path protected except `read`, `glob`, or `grep` within an explicitly supplied proof-owned runtime root. Keep writes there protected, and keep every operation outside both the fixture and explicit proof root protected. Prove these boundaries provider-free before another installed capture.
- **Evidence**: The first S15 baseline completed one directly routed reviewer call and terminal cleanup, with the expected installed model/runtime/source identity. Its only outside-fixture tools were `read` and `glob` under the generated `<runtime-root>\candidate-config` or `<runtime-root>` while resolving the reviewer's referenced contract; no shell, edit, write, network, repository path, credential, destructive, install, or remote tool was observed. The detector nevertheless marked `protected-action` solely because it treated every path outside the fixture as an effect.
- **Outcome**: Selected as a Proof Runner correction; the first S15 baseline lane is diagnostic only and cannot be composed.
- **Reason**: The runtime/config root is created and owned by capture, contains the isolated loaded config, and is deleted during terminal cleanup. Exempting only read-only tools within that exact root preserves the operating envelope while retaining fail-closed behavior for mutation or unowned paths.
- **Do Not Repeat**: Do not globally allow outside-fixture paths, classify a proof-owned write as read-only, compose the rejected baseline lane, or launch the candidate arm after a hard forbidden-effect flag.
- **Retry Condition**: Rerun the state/lifecycle baseline under S16; launch its candidate mate only if the lane records no forbidden effect and terminal cleanup, then apply every S15 canary oracle before the full population.

## PCR-S17: Literal candidate-path leakage closure

- **Objective**: Enforce PCR-001's exact requirement that the file-candidate path, not only its bytes or sentinel, is absent from every stage-one prompt, file, and tool result.
- **Approach**: Replace the reconstruction prompt's literal filename prohibition with representation-neutral candidate-artifact wording. Treat any occurrence of a file candidate's exact relative path in the stage-one projection as candidate visibility in both capture state and evaluation, while retaining the separate pre-reconstruction filesystem check.
- **Evidence**: The matched S15/S16 state/lifecycle candidate canary recorded candidate bytes and sentinel absent, no candidate path in stage-one files, exact continuation, one evidence-backed consequence, no forbidden effect, bounded calls, and terminal cleanup. Direct inspection nevertheless found `candidatePathInStageOnePrompt=true` because the proof prompt said `do not create candidate.md`; the existing sentinel/content oracle returned `candidateStateAtReconstruction=absent` and therefore missed the literal path leak.
- **Outcome**: Selected as an accepted-scope capture/evaluator correction. Both canary lanes remain diagnostic and are not composable with the corrected stage-one projection.
- **Reason**: The proposal and PCR-001 spec explicitly require the exact candidate path to be absent from stage one. Neutral wording preserves the safety instruction without revealing the mechanism, while path-based capture/evaluation prevents another fluent green result from compensating for leakage.
- **Do Not Repeat**: Do not name the candidate filename in reconstruction instructions, use sentinel/content equality as the complete absence oracle, or compose either pre-S17 canary lane.
- **Retry Condition**: After provider-free negatives pass, capture the corrected state/lifecycle baseline/candidate pair as the first matched full-population fragment. Continue to other members only if literal path, bytes, sentinel, chronology, continuation, semantics, effects, bounds, identity, and cleanup all pass.

## PCR-S18: Episode-level corrected-review observation

- **Objective**: Preserve the consequence admitted by the exact initial comparison while proving that one fresh reviewer checks the corrected candidate against the current frozen reconstruction.
- **Approach**: Require a structured semantic observation from the resumed initial comparison even when a corrected comparison follows. Keep that initial observation as the episode-level PCR result; use the corrected response for exact freshness, status, and inspectable report evidence without replacing the initial admitted consequence. Clear the initial observation when reconstruction currency is deliberately false so the stale control remains explicit unknown.
- **Evidence**: The corrected-review configured lane used three bounded calls, exact initial continuation, a distinct fresh corrected child, no forbidden effect, and terminal cleanup. The initial report admitted `R-OWN-PREMATURE-QUEUE-TRANSFER` with the caller-versus-worker cancellation ownership consequence. The corrected report returned `no-material-finding` for the fixed candidate plus residual unknown post-acceptance ownership. Capture overwrote the initial episode consequence with that corrected residual observation, producing required-consequence failure despite correct reviewer behavior.
- **Outcome**: Selected as a capture aggregation correction; the affected corrected-review lane is diagnostic only and must be recaptured.
- **Reason**: The bounded episode challenges the initial candidate and then verifies fresh corrected review. A successful correction should not erase the initial evidence that authorized it, and a fresh no-material-finding should not be forced to reproduce the defect it just confirmed absent.
- **Do Not Repeat**: Do not require the corrected candidate to remain defective, deterministically recover risk semantics from prose, overwrite the initial episode observation, or let an initial result mask a stale frozen reconstruction.
- **Retry Condition**: After provider-free aggregation and stale controls pass, recapture only `corrected-review-freshness`; continue remaining independent fragments only if its initial consequence, fresh corrected identity, bounds, effects, and cleanup all pass.

## PCR-S19: Corrected-scenario candidate identity correlation

- **Objective**: Make the resumed reviewer compare the exact entity described by the frozen raw-evidence statement without supplying a risk label or consequence answer.
- **Approach**: For a scenario with one corrected-candidate step, state at initial comparison that the supplied candidate is the initial candidate named in the frozen raw evidence. Keep reconstruction candidate-free and keep every expected consequence identifier evaluator-only. Permit no further prompt-only retry if the supported consequence remains unknown.
- **Evidence**: The S18 recapture retained the structured initial observation and fresh corrected identity, but its initial report treated “candidate states transfer at queue” and raw evidence “the initial candidate transfers before worker acceptance” as uncorrelated; it therefore marked queue-versus-acceptance identity/order unknown. The prior run correlated the same candidate by its sentinel and admitted the premature-transfer row, demonstrating that the missing fact is entity identity rather than a missing risk term.
- **Outcome**: Selected as the last semantic prompt correction for this scenario; the S18 lane remains diagnostic only.
- **Reason**: The reviewed scenario intends one initial candidate followed by one corrected candidate. Explicit identity is necessary input correlation, not deterministic semantic inference or hidden answer leakage.
- **Do Not Repeat**: Do not add queue/acceptance ordering, expected consequence vocabulary, a hardcoded risk row, or another wording retry after this identity correlation.
- **Retry Condition**: After provider-free prompt-correlation validation, recapture only `corrected-review-freshness`. Continue if its structured initial observation is evidence-backed and every freshness, bound, effect, identity, and cleanup oracle passes; otherwise stop this semantic lane at its exact evidence ceiling.

## PCR-S20: Pre-recapture semantic seed sufficiency checkpoint

- **Objective**: Prevent another full matched population from reaching an ambiguous semantic oracle only after the fixed 11-call baseline cost.
- **Approach**: Preserve PCR-001's eleven-member population, required/evidence-bounded/unknown containment modes, installed boundary, call bound, and maximum claim. Before changing the seed or recapturing, obtain one fresh read-only evidence-sufficiency review of every actor-visible raw-context/candidate/oracle pair. Correct only reproduced evidence gaps in one coherent fixture revision, rerun provider-free leakage/semantic negatives, then start a new matched identity.
- **Evidence**: The S17 full baseline completed all eleven installed rows with terminal cleanup. The ownership candidate fragment then returned honest unknown because “one restart command” did not specify whether it uses coordinator retry's new-id semantics or worker resume's preserved-id semantics; `required-consequence` assumed that missing effect. This is the second semantic ambiguity discovered at the expensive configured boundary after the corrected-review identity ambiguity.
- **Outcome**: Selected delivery route under suppression identity `PCR-CHECKPOINT-SEMANTIC-SEED-R1`; current pre-checkpoint lanes remain diagnostic and cannot be resealed under a changed pack digest.
- **Reason**: One bounded whole-population seed review is the earliest sufficient boundary for the repeated failure class and is cheaper than discovering another ambiguous actor-visible contract after recapture. It changes fixture clarity, not accepted semantics, population, proof scope, or evaluator strictness.
- **Do Not Repeat**: Do not recapture under the current ambiguous ownership candidate, weaken `required-consequence` to force composition, reseal old lanes under a new digest, add hidden labels, or create another checkpoint for unchanged `PCR-CHECKPOINT-SEMANTIC-SEED-R1` evidence.
- **Retry Condition**: Continue to a new matched population only after every current semantic mode is supported by actor-visible evidence or explicitly narrowed through the existing evidence-bounded/required-unknown contract, provider-free leakage controls pass, and the review's material rows are independently dispositioned.

## PCR-S21: Bind the ownership candidate to one evidenced recovery effect

- **Objective**: Close the confirmed ownership seed gap without weakening semantic containment or changing the PCR-001 population.
- **Approach**: Keep the bad candidate's coordinator ownership and one-command design, but explicitly bind every retry and resume to the already evidenced coordinator retry semantics that creates a new import id. Leave the worker resume fact, expected labels, member order, `required-consequence` mode, and maximum claim unchanged.
- **Evidence**: Fresh evidence-sufficiency review `ses_f9a0cf238ffelIYRqzXF0w5C0U` assessed all eleven rows and reported only `PCR-ES-SEED-01`: the prior command did not select either new-id retry or preserved-id resume semantics. Main independently reproduced that gap in the installed ownership fragment, whose exact report stopped at unknown for the same missing effect. The review found every other declared semantic mode supported at its current evidence ceiling.
- **Outcome**: Selected; the fixture digest changes and every earlier configured lane remains diagnostic only.
- **Reason**: The explicit new-id effect makes the candidate's collision with worker resume's preserved import id directly observable from actor-visible evidence, without expected-token leakage, common-sense ordering, or deterministic semantic inference.
- **Do Not Repeat**: Do not downgrade the row to evidence-bounded, add the expected consequence identifier, reseal prior lanes, or revise any independently supported scenario solely for symmetry.
- **Retry Condition**: Run provider-free preflight/evaluator negatives under the new pack digest, then one fresh complete matched baseline/candidate population. Stop on any semantic, chronology, continuation, identity, effect, bound, privacy, liveness, or cleanup miss.

## PCR-S22: Async reviewer submission with terminal message readback

- **Objective**: Preserve the S21 matched population when a valid long-running reviewer turn exceeds the SDK client's response-header timeout.
- **Approach**: Submit each configured PCR prompt through `session.promptAsync`, poll the exact session status and message stream for one new assistant response carrying terminal `time.completed`, `finish`, or `error` metadata, and abort plus verify idle on the PCR-owned timeout. Keep the direct routed reviewer child, prompts, provider count, identity, permissions, call bound, semantic oracles, and cleanup unchanged.
- **Evidence**: Fresh S21 baseline `candidate:pcr-final-s21-r1` stopped at `ownership-recovery-distinction` with `fetch failed` / `UND_ERR_HEADERS_TIMEOUT`, empty task invocations, no candidate arm, and terminal cleanup. The synchronous SDK request's own headers deadline fired before PCR's longer abort deadline, so the response transport rather than candidate semantics owned the observed failure. The first async ownership canary returned one complete terminal response; the following full baseline exposed an idle race where capture accepted a new empty assistant placeholder before terminal message metadata existed, then stopped with `observation-missing-or-invalid` and complete cleanup.
- **Outcome**: Selected as the causally different Proof Runner mechanism before another configured attempt.
- **Reason**: Async submission returns immediately and makes completion observable through the maintained session status/message APIs, while a bounded abort and idle check closes liveness if no terminal response appears.
- **Do Not Repeat**: Do not increase only the outer timeout, treat session idle plus a non-terminal assistant placeholder as completion, retry a failed evidence root, launch the candidate after a missing terminal baseline response, or weaken the semantic, identity, effect, bound, privacy, liveness, or cleanup oracles.
- **Retry Condition**: Run provider-free async success/timeout coverage and the complete PCR preflight first; then recapture the previously failing ownership baseline as one create-new canary. Start one create-new matched S21 baseline/candidate identity only if that canary returns a terminal reviewer response with complete cleanup; stop on any unclosed async session or repeated same-identity transport failure.
