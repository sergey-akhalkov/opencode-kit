# Strategy History

## PCR-S1: Candidate-visible readiness review

- **Objective**: Detect a locally coherent plan that solves the wrong contextual problem before implementation.
- **Approach**: Supply the original request and candidate together to the existing readiness reviewer and ask it to attack coherent-wrong-outcome and related classes.
- **Evidence**: The current bounded-falsification contract and focused pack provide separate request/candidate fields but the reviewer sees both in one challenge; the pack also supplies `representedRisk`.
- **Outcome**: Insufficient for the reproduced abstraction-loss class.
- **Reason**: Candidate framing can anchor the review, while the supplied risk tests routing of a known issue rather than discovery from raw actors, state, lifecycle, and consumers.
- **Do Not Repeat**: Do not add more risk vocabulary, examples, or checklist items to the same candidate-first prompt and treat that as independent context reconstruction.
- **Retry Condition**: Retry only if new evidence proves a candidate-visible mechanism can establish independent reconstruction through an observable oracle that the current task inputs cannot satisfy.

## PCR-S2: Same-prompt blind phase

- **Objective**: Reconstruct context before evaluating the candidate without changing the reviewer topology.
- **Approach**: Put original evidence and candidate in one prompt but instruct the reviewer to ignore the candidate during an initial section.
- **Evidence**: Candidate bytes remain in the same model context, so tool-call and source evidence cannot establish that the initial reasoning was independent.
- **Outcome**: Rejected before implementation.
- **Reason**: The protocol would be blind only by instruction, not by observable input isolation, and would repeat the candidate-first failure with more formatting.
- **Do Not Repeat**: Do not call a phase candidate-separated when candidate bytes or a mechanism-revealing summary are present anywhere in that phase's model input.
- **Retry Condition**: Retry only if the runtime exposes a verifiable attention/context isolation primitive stronger than separate task inputs.

## PCR-S3: New universal consequence reviewer or skill

- **Objective**: Give prospective consequence analysis a dedicated owner.
- **Approach**: Add a new always-applicable reviewer/skill beside implementation readiness and exact substitution qualification.
- **Evidence**: `implementation-readiness-reviewer` already owns pre-implementation task fit and bounded falsification; `behavioral-substitution-qualification` already owns skip/omit/replacement equivalence.
- **Outcome**: Rejected in favor of extending existing owners.
- **Reason**: A sibling would duplicate routing, authority boundaries, and evidence reconciliation while burdening Ordinary Small work.
- **Do Not Repeat**: Do not add another universal critic, central router, or duplicate substitution procedure for the same semantic boundary.
- **Retry Condition**: Retry only if implementation evidence proves the existing readiness owner cannot express the bounded two-phase contract without mixing unrelated responsibility.

## PCR-S4: Same-worktree candidate-separated resumable review

- **Objective**: Discover current consequential distinctions from raw evidence before candidate framing, then compare the candidate without adding another owner.
- **Approach**: Launch one fresh candidate-free reconstruction brief in the active candidate worktree and resume the exact correlated reviewer child with the candidate; rely on path prohibitions plus first-task input inspection.
- **Evidence**: The first planning review omitted candidate bytes from the brief but project-wide grep emitted candidate-path hits into the stage-one tool/model stream. Readiness finding `PCR-ISO-01` and foundation findings `FI-PCR-001`/`FI-PCR-002` confirmed that disposable input oracles cannot prove the live same-worktree boundary.
- **Outcome**: Superseded before implementation; the candidate's physical-isolation and proof-composition claims were narrowed.
- **Reason**: Prompt/path convention does not prevent normal reviewer tools from reading an existing candidate, so all planned pack tasks could pass while the loaded workflow remained candidate-contaminated.
- **Do Not Repeat**: Do not call a phase physically separated when the current candidate already exists anywhere reachable by its normal tools, and do not compose a disposable first-input oracle into a different live worktree identity.
- **Retry Condition**: Retry only if a technically enforced boundary makes the current candidate absent from every stage-one model-visible input/result and the loaded proof exercises that same identity.

## PCR-S5: Pre-authoring chronological isolation

- **Objective**: Enforce candidate absence without adding a session/workspace owner.
- **Approach**: Launch context reconstruction after original-request foraging but before creating any candidate artifact, inline frame, or source mutation; create the candidate only after reconstruction; resume the exact child for initial comparison; use a fresh corrected-candidate reviewer with the frozen reconstruction when it remains current.
- **Evidence**: Current OpenSpec authoring already has a pre-draft failure-hypothesis step, the existing task adapter supports exact continuation, and candidate nonexistence prevents prompt, file, and tool-result leakage. Explicit-directory proof clients exist but are unnecessary for this first bounded path.
- **Outcome**: Selected as the corrected planning mechanism; implementation/runtime behavior remains unproved.
- **Reason**: This is a same-owner ordering correction with the earliest direct oracle and lower lifecycle/cleanup/context cost than a new isolated-session helper.
- **Do Not Repeat**: Do not retroactively label an existing candidate separated, rebuild context around a corrected candidate, or add isolation infrastructure while chronological absence satisfies the accepted happy path.
- **Retry Condition**: Implement only after ownership transfer; retain only if PCR-001 observes candidate absence across the complete stage-one stream, exact continuation, fresh corrected-review behavior, proportional controls, and cleanup.

## Foundation Recovery FI-PCR-2026-09-01-01

- **Relation**: Claimed same-worktree physical candidate isolation and disposable-pack/live-oracle equivalence versus observed candidate-path leakage into stage-one tool output.
- **State**: observed -> confirmed -> correcting -> swept -> re-reviewed -> closed.
- **Reproduction Evidence**: Initial readiness child reported incidental forbidden-path grep hits; main reproduced prompt-only/path-convention insufficiency; foundation owner confirmed `FI-PCR-001` and `FI-PCR-002` on candidate `working-tree-r1`.
- **Preserved Evidence**: Original request was independently supplied, the brief itself was candidate-free, exact child continuation worked in this run, the challenge remained finite, and Ordinary Small/substitution controls were not invalidated.
- **Correction**: Rebind initial separation to chronological candidate nonexistence and narrow disposable evidence to the identical temporal authoring environment; no archive is rewritten.
- **Corrected-Candidate Review**: Fresh foundation review `ses_fa23ff230ffeCP65AHqPUgaBl4` inspected `working-tree-r2` with effective model `openai/gpt-5.6-sol` and returned `no-material-finding`; runtime effectiveness remains unproved and bounded to future PCR-001 execution.

## PCR-S6: Inline frame after reconstruction

- **Objective**: Apply the selected chronological boundary to Material work that has no OpenSpec tree or prior decision artifact.
- **Approach**: Treat the smallest session-local decision frame as the candidate: reconstruct from original/raw evidence first, create the frame second, and resume the exact reviewer child with that frame; add a dedicated PCR-001 inline sentinel/oracle.
- **Evidence**: Fresh corrected-candidate readiness review `ses_fa2380801ffejOTnkMSJ9CO6Fd` found `PCR-INLINE-01`: the prior delta simultaneously forbade pre-reconstruction frame creation and retained create-frame-then-review behavior, while the pack covered only file candidates.
- **Outcome**: Corrected inside the existing mechanism after the second and final generic challenge; no third generic review is permitted.
- **Reason**: Without the inline ordering and oracle, all file-candidate tasks could pass while newly emerging session-local Material decisions remained candidate-first.
- **Do Not Repeat**: Do not treat a decision frame as neutral context when it already embodies the decision being challenged, and do not infer inline coverage from file-path sentinels.
- **Retry Condition**: Use only focused artifact validation and future PCR-001 runtime proof for this exhausted episode unless a materially changed owner requirement starts a new accepted-outcome episode.
