## MODIFIED Requirements

### Requirement: Decision-material work receives one early falsification episode

Change-Ready routing SHALL place the bounded falsification episode at the earliest decision boundary before dependent implementation investment. Decision-material plans, specifications, and architecture decisions SHALL use the pre-implementation readiness owner. When a newly authored Material decision surface has current distinction pressure, main SHALL launch candidate-free reconstruction before creating an OpenSpec candidate or the smallest project-native/session-local decision frame, then create that artifact as the candidate and resume the exact reviewer child for comparison. Material work without distinction pressure MAY first create the smallest frame and use the existing single-stage challenge. `final-candidate-reviewer` SHALL remain optional and concrete-risk-driven. A candidate whose implementation remains inside a current challenged decision surface SHALL NOT receive a duplicate generic post-proof review solely for compliance.

#### Scenario: Material change has a decision-material plan

- **WHEN** the plan receives a current terminal pre-implementation falsification episode and implementation preserves its challenged decision surface
- **THEN** main proceeds through proof and validation without a duplicate generic final review
- **AND** independently triggered exact Practice Owners and critical SDET retain their existing boundaries.

#### Scenario: Material change has no prior decision artifact

- **WHEN** a Material request has no OpenSpec change or other durable decision artifact and raw evidence shows a current decision can collapse consequential distinctions
- **THEN** main supplies the original accepted request and raw evidence to one fresh `implementation-readiness-reviewer` before creating the session-local decision frame
- **AND** only after reconstruction does main create that frame and resume the exact reviewer child with it as the candidate before production mutation.

#### Scenario: Material change has no prior decision artifact and no distinction pressure

- **WHEN** a Material request has no prior decision artifact and current evidence has no prospective-consequence trigger
- **THEN** main may create the smallest decision frame and use the existing single-stage original-request-grounded challenge
- **AND** it does not claim pre-authoring separation.

#### Scenario: Material implementation changes the challenged decision surface

- **WHEN** implementation evidence requires changing a previously challenged outcome, envelope, invariant, proof boundary, user-owned decision, or material-risk decision
- **THEN** main uses the episode's one permitted fresh corrected-candidate comparison against the current frozen reconstruction if still available
- **AND** an exhausted generic budget or stale reconstruction routes any exact remaining defect to focused proof or its registered Practice Owner rather than another generic verdict.

## ADDED Requirements

### Requirement: Prospective rehearsal stays proportional and composes with exact owners

Within the existing early falsification trigger, main SHALL use pre-authoring-separated prospective consequence rehearsal only when current decision evidence has distinction pressure: a representation, identity, occurrence, actor, state, lifecycle, ownership, recovery, or proof-boundary choice can make observably different downstream outcomes appear equivalent. Main SHALL own that semantic trigger and SHALL launch reconstruction before creating the first candidate artifact, inline decision frame, or candidate source mutation. Deterministic tooling SHALL NOT infer distinction pressure from keywords, changed-file count, task count, model output, or a score.

Ordinary Small exact work with one known owner, exact behavior, focused proof, and no such unresolved decision SHALL proceed without the rehearsal. Material work without distinction pressure MAY retain the existing single-stage readiness challenge. A candidate that predates the protocol or lacks a candidate-free reconstruction SHALL remain single-stage and SHALL NOT claim prospective separation. If the exact decision is skip, omit, suppress, cache, replay, emulate, replace, or bypass behavior at an owning boundary, main SHALL route equivalence and effect closure through `behavioral-substitution-qualification`; prospective rehearsal SHALL consume relevant raw path/effect evidence only as part of the existing readiness episode and SHALL NOT create a duplicate substitution workflow or claim.

When pre-authoring reconstruction, initial continuation, corrected-review freshness, or reconstruction currency is unavailable or unverified, only semantic implementation readiness for the dependent decision surface SHALL remain `unknown`; independent accepted work SHALL continue. No reviewer result SHALL authorize mutation, protected action, scope expansion, qualification, or completion. Consequence tracing SHALL stop at a verified contract/oracle, exact owner or protected-decision boundary, or explicit `unknown`.

#### Scenario: Ordinary Small exact work stays direct

- **WHEN** one bounded reversible owner-local change has exact behavior, a representative proof boundary, and no unresolved consequential distinction
- **THEN** main uses the Ordinary Small route without prospective rehearsal
- **AND** normal runtime proof and validation remain required.

#### Scenario: Decision surface can collapse current distinctions

- **WHEN** a Material decision can merge currently evidenced actors, states, occurrences, lifecycle positions, owners, recovery paths, or observable proof outcomes
- **THEN** main completes reconstruction before creating the candidate and resumes the exact child only after authoring
- **AND** dependent mutation waits for a current terminal disposition or remains honestly `unknown`.

#### Scenario: Exact substitution owner applies

- **WHEN** the decision claims equivalence for skipped, omitted, cached, replayed, emulated, replaced, or bypassed behavior
- **THEN** main loads and follows `behavioral-substitution-qualification` as the exact effect-closure owner
- **AND** it does not add a competing consequence-rehearsal skill, baseline/candidate lane, or equivalence record.

#### Scenario: Continuation is unavailable but independent work remains

- **WHEN** reviewer continuity is unverified for one dependent decision surface and another accepted item does not depend on that surface
- **THEN** the affected readiness stays `unknown` while independent work continues
- **AND** no owner question is created solely to approve a different internal review mechanism.

#### Scenario: Always-loaded principle lacks reproduced benefit

- **WHEN** the focused matched pack does not reproduce a candidate improvement while preserving every hard routing and safety oracle
- **THEN** the proposed canonical principle is not added to the always-loaded philosophy
- **AND** the candidate remains in development rather than shipping unproven instruction growth.
