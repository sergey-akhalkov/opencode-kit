## 1. Ownership, Baseline, And Automation Dividend

- [x] 1.1 Inventory the current completion-guard, session-delivery evidence, loaded instruction, OpenSpec apply, roadmap mission, campaign, validator, and proof owners; record current active-change/worktree overlap and exact source/runtime identities without modifying unrelated work. **Current Rung:** source and archived-evidence characterization. **Next Real Boundary:** provider-free frontier fixture through the production parser/controller boundary in task 1.2. **Blocker/Unblocker:** planning artifacts are complete; any overlapping active writer on a required path must be terminal, isolated, transferred, or the affected mutation stays pending while non-overlapping tasks continue. **Observable Proof:** one ownership/evidence record maps every proposed touched responsibility to an existing owner or one justified frontier seam, identifies the current `owner_required`, question, and `maxCycles` behavior, and names baseline/candidate/proof/evaluator/environment identities for `GRIND-TSB-001`. **Authorization:** read-only repository/runtime inspection. **Safeguards:** no source mutation, active-change adoption, installation, activation, protected effect, provider call, or unrelated cleanup. **Restoration/Cleanup:** none beyond closing inspection processes. **Immutable Evidence:** ownership map, source digests, current config values, worktree status, and overlap disposition. **Validation:** proposal/apply operation gates remain readable and ownership refs resolve to current paths.
- [x] 1.2 [automation-dividend] Extend the maintained completion-guard fixture/proof family with a versioned reviewed frontier seed and provider-free materializer/evaluator for valid partial-blocked, all-product-blocked, non-product-waiting, complete, stale, cyclic, malformed, bounded, and migration cases; expose effect-free help and replay. **Current Rung:** reviewed explicit seed data outside helper code. **Next Real Boundary:** the production frontier parser and readiness derivation in task 2.1. **Blocker/Unblocker:** task 1.1 identifies the smallest existing proof owner; semantic product/dependency judgment remains in reviewed fixtures and never moves into helper code. **Observable Proof:** deterministic runs produce stable ordering, exact runnable/gate/decision refs, cause-preserving failures, zero product-semantic scoring, zero provider calls, and no source/worktree effect. **Authorization:** local proof-owned fixture and evidence paths only. **Safeguards:** explicit schemas, bounded bytes/counts, no private prompts, credentials, remote effects, fuzzy scoring, or second general runner. **Restoration/Cleanup:** remove disposable materialization state and retain create-new evidence. **Immutable Evidence:** seed digest, generated frontier bundles, help output, exits/stdout/stderr, stable second pass, and cleanup. **Validation:** focused frontier fixture/materializer tests and provider-free replay.

## 2. Frontier And Completion Protocol

- [x] 2.1 Implement the versioned frontier types, bounds, parser, correlation, reference and cycle validation, deterministic runnable derivation, progress fingerprint, plugin-owned `grind_frontier` tool ingress, atomic optimistic-generation persistence, and conservative legacy reconciliation in the completion guard and session-delivery/arbiter evidence projection. **Current Rung:** provider-free reviewed frontier seeds from task 1.2. **Next Real Boundary:** the real tool handler persists and reads back a schema-valid partial-blocked frontier before controller application in task 2.2. **Blocker/Unblocker:** tasks 1.1-1.2; any ambiguous legacy state must remain reconciliation-required rather than inferred. **Observable Proof:** the handler derives root and latest-human identity from tool context, accepts every valid seed, rejects stale-generation/cyclic/malformed/overflow candidates without replacing the last valid frontier, returns the expected server generation and derived runnable refs, and marks retained schema-v1 verdicts stale for new effects. **Authorization:** repository production source and focused local execution. **Safeguards:** deterministic code validates facts only; no caller-supplied root/human/audit identity, product semantics, permission widening, transcript inference, or unrelated persistence framework. **Restoration/Cleanup:** remove disposable metadata/fixtures; leave unrelated work unchanged. **Immutable Evidence:** exact tool inputs, persisted readback, parser/controller outputs, diagnostics, source diff, exits/stdout/stderr, and cleanup. **Validation:** focused tool/frontier/parser/session-delivery tests plus type/static checks for touched owners.
- [x] 2.2 Implement verdict schema version 2 and controller transitions for `continue`, `product_decision_required`, non-product `waiting`, `allow_stop`, and `user_paused`; enforce `questionAction`/`questionAnswers` legality, empty-runnable legality, selected-item refs, wait kinds, mission-incomplete status, progress-reset execution epochs, and removal of `maxCycles -> owner_required`. **Current Rung:** tool-persisted validated production frontier from task 2.1. **Next Real Boundary:** installed question deferral and continuation in task 2.3, then root proof in task 4.1. **Blocker/Unblocker:** task 2.1; unsupported or stale verdicts produce no ordinary root effect and retain exact retry/reconciliation evidence. **Observable Proof:** a non-empty runnable set deterministically rejects product-decision/wait/stop verdicts; question audits accept only exact `answer`, `defer`, or `present-product-decision` cross-field combinations; an empty product frontier accepts only the exact product decision; non-product gates enter the matching waiting state; complete closure allows stop; and cycle exhaustion never creates owner scope or blind repetition. **Authorization:** repository production source and local effect-free controller execution. **Safeguards:** preserve user pause, protected-action, Live-Attempt Gate, writer-liveness, async lease, correlation, retry, request-budget, and completion-certificate invariants. **Restoration/Cleanup:** close proof-owned controller processes and remove fixtures. **Immutable Evidence:** verdict bundles, state transitions, question actions, selected refs, rejected cross-field diagnostics, cycle/progress facts, exits/stdout/stderr, and diff. **Validation:** focused verdict/controller/restart/status tests and provider-free fixture replay.
- [x] 2.3 Implement race-safe blocker-question deferral as the named `questionAction=defer` audit effect: `continue` with one selected item for a non-empty frontier, or non-product `waiting` with no selected item for an empty frontier. Persist pending/confirmed provenance, use official question rejection, link either a parked product decision or exact non-product gate, continue only after post-rejection idle, preserve human-reply precedence, and retain the existing `questionAction=answer` official reply path. **Current Rung:** component-valid frontier and verdict controller from task 2.2. **Next Real Boundary:** disposable installed OpenCode root invoking real premature product and credential/safety questions in task 4.1. **Blocker/Unblocker:** task 2.2 and supported current question reply/reject capabilities; a missing API remains a capability wait and blocks only the deferral lane. **Observable Proof:** component integration accepts no ambiguous leave-open/reject/reply combination; rejects valid premature product and non-product blocker questions exactly once; continues the selected item when runnable; enters waiting without an open request when only a non-product gate remains; never records a human answer; leaves a true empty-frontier product question open only under `present-product-decision`; preserves autonomous offered-label reply; and gives a concurrent human reply precedence with no stale continuation. **Authorization:** repository production source and disposable local question events only. **Safeguards:** final epoch checks, no invented labels/free text, no synthetic authority, no duplicate rejection/reply, no concurrent root writer, and privacy-safe diagnostics. **Restoration/Cleanup:** resolve/reject every proof question, close sessions/processes, and remove disposable state. **Immutable Evidence:** ordered question/root events, provenance metadata, API results/errors, continuation/wait refs, human-race winner, side-effect counts, cleanup, and diff. **Validation:** focused question/controller/session-delivery race tests.

## 3. Loaded Workflow And Orchestration Alignment

- [x] 3.1 Replace conflicting loaded main, arbiter, Change-Ready, OpenSpec apply, reusable/template, compaction/handoff, and deterministic-contract wording with one canonical task-scoped frontier rule: independent accepted work is mandatory, product questions require an empty runnable set, process controls remain autonomous, and non-product gates remain scoped or waiting. **Current Rung:** production protocol behavior from tasks 2.1-2.3. **Next Real Boundary:** installed loaded primary plus guard scenario in task 4.1. **Blocker/Unblocker:** tasks 2.1-2.3 establish exact runtime vocabulary; remove obsolete global-stop phrases rather than layering overrides. **Observable Proof:** source inventory identifies one canonical complete owner, role-specific mirrors retain only deltas, contract tests reject broad `owner_required`, optional `MAY continue`, and process-budget handoff semantics, and current context-quality limits remain green. **Authorization:** repository instruction/spec/template source only. **Safeguards:** preserve product/protected owner authority, evidence-not-authority, question handoff, local standing authorization boundaries, and no runtime claim from static markers. **Restoration/Cleanup:** no runtime activation; preserve previous source through Git. **Immutable Evidence:** instruction inventory, marker diffs, contract outputs, context metrics, source identity, and no-install state. **Validation:** focused instruction/agent/skill/template/contracts and strict source-schema checks.
- [x] 3.2 Align roadmap mission and autonomous campaign projections, scheduling, result enums, persistence, reports, and restart handling so every declared dependency-valid authorized sibling executes before terminal product-decision handoff, while non-product gates use resumable waiting and blocked siblings remain unresolved. **Current Rung:** controller-valid frontier semantics and loaded routing from tasks 2.1-3.1. **Next Real Boundary:** provider-free mission/campaign composition, followed by installed composed controls in task 4.2. **Blocker/Unblocker:** task 3.1 and exact durable ledger ownership; unknown writer or overlapping paths remain fail-closed and cannot be reordered. **Observable Proof:** protected/product-blocked plus authorized sibling fixtures execute and verify the sibling first; dependent/overlapping siblings do not launch; non-product waits do not become owner-required or complete; resume consumes durable checkpoints exactly once. **Authorization:** repository production source and disposable local mission/campaign fixtures only. **Safeguards:** immutable campaign definition/order, serial writers, checkpoint integrity, no gate clearing by sibling success, no protected/remote effect, and no guard mutation of campaign state. **Restoration/Cleanup:** terminate/close all proof-owned writers, sessions, processes, worktrees, and controllers; cleanup unknown is failure. **Immutable Evidence:** ledger/frontier projections, transition records, writer identities, slice/wave results, checkpoints, exits/stdout/stderr, cleanup, and diff. **Validation:** focused roadmap/campaign controller, semantic, restart, and projection tests.

## 4. Installed Runtime Proof And Accepted Scope

- [ ] 4.1 Run the smallest installed happy path through the actual loaded OpenCode entry point: one premature product question blocks item A, item B remains runnable, the guard defers the question through the official API, item B produces its real marker, and only the then-empty frontier surfaces the product decision. In the same disposable envelope, open a premature credential/safety question with an independent item, prove official rejection and independent continuation, then prove an empty non-product frontier reaches question-free waiting. Pair both with the reproduced circular stop-line/live-attempt process case that must reconcile and continue without a process question. **Current Rung:** green provider-free/component behavior from tasks 1.2-3.1. **Next Real Boundary:** complete reviewed runtime population in task 4.2. **Blocker/Unblocker:** loaded candidate source, exact primary/arbiter routes, supported question APIs, disposable root, and current provider/cost authority; any live failure preserves a complete bundle and reaches terminal provider-free replay before another attempt. **Observable Proof:** exact event order shows product and non-product synthetic deferrals, zero invented answers, independent markers before any product question or waiting state, one product question only after runnable exhaustion, no open non-product request at waiting, autonomous stop-line reconciliation, no protected effect, and terminal liveness/cleanup. **Authorization:** bounded configured-provider calls already permitted for kit validation and disposable local source/runtime effects only. **Safeguards:** create-new evidence, no target project, credentials in evidence, remote/destructive/deploy/release effect, unchanged live retry, or completion overclaim. **Restoration/Cleanup:** disable grind if needed, resolve/reject open questions, terminate provider/OpenCode processes, delete disposable sessions/root/project, and prove no writer remains. **Immutable Evidence:** exact invocation/input, candidate/environment/route identity, root/child/question/tool events, frontier/verdict states, stdout/stderr/logs, effects, bundle hashes, replay, and cleanup. **Validation:** installed evaluator plus provider-free replay for all three scenarios.
- [ ] 4.2 Complete the `GRIND-TSB-001` reviewed scenario population for all-remaining-work product decision, missing credential/capability, technical blocker, safety/protected gate, blocked proof lane with independent route, autonomous offered-label question, stale/cyclic/malformed frontier, unchanged strategy, execution-epoch exhaustion, explicit interruption, roadmap/campaign sibling composition, restart migration, and completed outcome. **Current Rung:** installed happy path from task 4.1. **Next Real Boundary:** fresh critical-only SDET in task 5.1. **Blocker/Unblocker:** task 4.1 and terminal replay for every failed configured capture; evaluator-only corrections replay preserved bundles instead of driving live effects. **Observable Proof:** every scenario reaches its exact continue/product-decision/wait/pause/pass disposition, executes only expected items/effects, preserves every scoped gate, rejects false completion, closes ownership, and supports no claim beyond the proposal maximum. **Authorization:** bounded configured inference and disposable local runtime only. **Safeguards:** matched baseline/candidate identities, no arbitrary permission grant, credential exposure, protected/remote effect, unsafe retry, broad population inference, or mixed-candidate composition. **Restoration/Cleanup:** terminally close every session/process/writer/question and remove disposable projects; retain immutable privacy-safe bundles. **Immutable Evidence:** scenario matrix, baseline/candidate bundles, evaluator/replay results, claim record updates, exact effects, diagnostics, source/environment hashes, and cleanup. **Validation:** focused proof evaluators, composition checks, and current evidence-index schema/readback.

## 5. Critical Risk, Validation, And Handoff

- [ ] 5.1 Dispatch one fresh test-only SDET after current proof and accepted-scope completion to challenge unauthorized/protected execution, human-versus-guard question races, synthetic-answer authority, stale-frontier continuation, false completion/wait classification, infinite or costly repetition, and concurrent writer/sibling overlap; permit only the smallest critical regression oracles in existing focused test owners. **Current Rung:** complete current candidate and runtime population from task 4.2. **Next Real Boundary:** main reproduction and affected candidate proof in task 5.2. **Blocker/Unblocker:** task 4.2, readable current candidate/bundles, closed production writers, and exact test-only write scope. **Observable Proof:** SDET returns `critical-risks-reported | no-critical-risk | blocked` with Effective Model and candidate attribution; any authored test fails against a behaviorally defective fixture and passes against the current candidate. **Authorization:** fresh local test-only specialist and disposable execution. **Safeguards:** no production/instruction/proof/OpenSpec mutation, user question, credential/remote/protected effect, nested orchestration, lifecycle verdict, or non-critical expansion. **Restoration/Cleanup:** close the specialist writer attempt terminally and remove disposable test state before integration. **Immutable Evidence:** specialist identity/model, risk/oracle matrix, exact test-only diff, focused test output, candidate ref, and closure. **Validation:** SDET-authored focused tests and main diff inspection.
- [ ] 5.2 Main independently reproduce, disprove, or show unreachable every SDET row; retain only unique critical test oracles, apply the smallest authorized correction for a confirmed current defect, and regain every invalidated component, installed, composition, and claim-evidence lane before terminal disposition. **Current Rung:** fresh SDET evidence from task 5.1. **Next Real Boundary:** complete project-native validation in task 5.3. **Blocker/Unblocker:** task 5.1; reviewer severity never authorizes mutation, and any production correction returns the candidate to development until affected runtime proof is current. **Observable Proof:** every risk id has evidence-backed main disposition; no plausible reachable authorization, data-integrity, irreversible-action, writer-overlap, or envelope-escape claim remains parked; current candidate and retained tests are green. **Authorization:** main-owned local reproduction and smallest accepted-outcome correction only. **Safeguards:** no optional polish, weakened gate, equivalent SDET rerun, unrelated cleanup, or stale evidence reuse. **Restoration/Cleanup:** close reproducer processes/sessions/writers and remove fixtures; preserve pre/post-correction bundles. **Immutable Evidence:** risk dispositions, reproducer commands/output, causal diff, affected proof/replay, retained-test rationale, SDET terminal history, and cleanup. **Validation:** every invalidated lane from tasks 2.1-4.2 and retained critical tests.
- [ ] 5.3 Run complete applicable project validation, strict OpenSpec validation, proposal/apply operation gates, instruction/config/profile/permission/source diagnostics, completion-guard/roadmap/campaign focused suites, evidence-index readback, `git diff --check`, and final status/diff inspection; prepare a local implementation handoff without installing or activating the candidate. **Current Rung:** terminal critical-risk disposition and current proof from task 5.2. **Next Real Boundary:** separately authorized install/restart and real operator use after implementation completion. **Blocker/Unblocker:** tasks 1.1-5.2; any behavior mutation invalidates affected proof and returns to the relevant task, while unrelated pre-existing validation failures remain explicitly scoped and cannot be hidden. **Observable Proof:** all applicable checks exit zero for one readable candidate, every accepted task and claim row has current evidence, no known confirmed reachable critical/non-deferrable defect remains, and external-operation state is explicit. **Authorization:** local validation and handoff only. **Safeguards:** no install, activation, commit, push, merge, release, deployment, remote mutation, credential disclosure, or false stable claim. **Restoration/Cleanup:** remove validation fixtures, close processes/sessions/writers, retain bounded evidence, and leave unrelated work untouched. **Immutable Evidence:** exact commands, exits/stdout/stderr, diagnostics, candidate/environment refs, proof/SDET/validation summaries, diff/status, cleanup, and known limitations. **Validation:** the exact complete command set above and final OpenSpec status/readback.
