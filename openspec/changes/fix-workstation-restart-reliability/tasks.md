## 1. Effect-free controller contract

- [x] 1.1 Confirm repository `--help`, `preflight`, and `status` still exit `0`, list `restart`/`stop`, and mutate no file, task, or process. **Current Rung:** live log plus repository source. **Next Real Boundary:** `node tools/windows/opencode-workstation.ts --help`. **Blocker/Unblocker:** none. **Observable Proof:** help stdout lists the public modes and the command exit is `0` with unchanged ProgramData hashes. **Authorization/Safeguards:** repository source only. **Restoration/Cleanup:** none. **Immutable Evidence:** command exits and stdout. **Validation:** `node --check tools/windows/opencode-workstation.ts`.

## 2. Targeted stop and honest tray Restart

- [x] 2.1 In the repository controller, stop only validated supervisor, server-root, and listener PIDs without `/T`; treat already-gone validated PIDs as stopped; stop a matching `starting` tree; change `serve()` child termination to that same targeted kill; keep unmatched/drifted identity fail-closed. Update the derived tray helper so one in-flight Restart stays blinking, a second click starts no second controller, a failed Restart stays red with a secret-free balloon naming `controller-errors.log`, and a leftover listener is not treated as success. **Current Rung:** 1.1 help. **Next Real Boundary:** `node --check` plus installed `--help` after the source edit, before ProgramData repair. **Blocker/Unblocker:** none. **Observable Proof:** source no longer issues `taskkill /T` from `stopManagedServer` or `serve` terminate; tray script contains the failure balloon and no silent post-failure `start`. **Authorization/Safeguards:** repository source only. **Restoration/Cleanup:** none until repair. **Immutable Evidence:** reviewable diff and `--check` exit. **Validation:** `node --check tools/windows/opencode-workstation.ts`.

- [ ] 2.2 After a re-verified elevated token, stop the managed server if the current installer requires it, repair the ProgramData controller and tray from the repository source, then invoke Restart once from the live `opencode-server` tray menu. **Current Rung:** 2.1 source. **Next Real Boundary:** actual tray Restart on this workstation. **Blocker/Unblocker:** 2.1; ProgramData repair is owner-authorized host mutation. **Observable Proof:** one tray Restart replaces supervisor/server-root/listener PIDs, lamp returns to green, authenticated `127.0.0.1:4096` is healthy, and `controller-errors.log` has no new `operation: restart` error. **Authorization/Safeguards:** standing local admin; loopback only; no provider/model call; no target-repo write; record pre-repair hashes. **Restoration/Cleanup:** leave one healthy server and green tray, or restore previous protected hashes. **Immutable Evidence:** before/after PIDs, tray-state color, health, log tail, manifest hashes. **Validation:** installed `status`.

## 3. Fail-closed and Desktop surfaces

- [ ] 3.1 Prove Restart/Stop refuse an unmatched listener and report the ownership diagnostic without killing that process. **Current Rung:** 2.2 green tray Restart. **Next Real Boundary:** live unmatched-listener Restart on this host, or the nearest safe local equivalent that still uses the installed controller. **Blocker/Unblocker:** 2.2. **Observable Proof:** controller exits non-zero, unmatched PID remains, lamp or Desktop dialog is secret-free and names the log. **Authorization/Safeguards:** do not bind a substitute listener on `4096` unless it is disposable and restored. **Restoration/Cleanup:** restore one healthy managed server and green tray. **Immutable Evidence:** status/error JSON, process inventory. **Validation:** installed `status`.

- [ ] 3.2 Invoke Desktop `OpenCode Server - Restart.lnk` once against a healthy managed server and confirm the same replacement contract as tray Restart. **Current Rung:** 2.2. **Next Real Boundary:** actual Desktop Restart invoker. **Blocker/Unblocker:** 2.2. **Observable Proof:** new managed PIDs, healthy listener, no invoker dialog, no new restart error. **Authorization/Safeguards:** same local loopback envelope. **Restoration/Cleanup:** leave one healthy server and green tray. **Immutable Evidence:** invoker exit, PIDs, health. **Validation:** installed `status`.

## 4. Qualification

- [ ] 4.1 After current MVP and complete accepted scope, dispatch one fresh critical-only SDET against targeted stop, already-gone success, starting-state Restart, unmatched-listener fail-closed, tray failure signaling, and credential non-disclosure. **Current Rung:** 2.2–3.2 proof. **Next Real Boundary:** fresh SDET. **Blocker/Unblocker:** 2.2. **Observable Proof:** SDET returns `critical-risks-reported | no-critical-risk | blocked` with Candidate Reference and Effective Model. **Authorization/Safeguards:** test-only writes. **Restoration/Cleanup:** restore one healthy server and tray. **Immutable Evidence:** SDET report. **Validation:** main disposition of every row.

- [ ] 4.2 Run `node --check tools/windows/opencode-workstation.ts`, installed `--help`/preflight/status, `npm test`, `npm run validate:strict`, and `npx openspec validate fix-workstation-restart-reliability --strict`. Record known non-critical limits. **Current Rung:** 4.1 terminal SDET. **Next Real Boundary:** project-native validation. **Blocker/Unblocker:** 4.1. **Observable Proof:** named commands exit `0`; no known reachable critical defect. **Authorization/Safeguards:** local validation only. **Restoration/Cleanup:** retain the requested installed workstation. **Immutable Evidence:** command exits/stdout/stderr. **Validation:** those commands.
