## Fidelity Ladder

`current ownership/source inventory -> provider-free Horizon/linkage/receipt schemas ->
fact-only trajectory-context CLI -> loaded core skill/profile readback -> configured linked
archive with no trigger -> configured repeated-touch review and one successor -> remaining
claim partitions -> read-only PMAC diagnostic -> challenge/evidence/strict validation`.
Product Candidate is the Delivery Horizon contract, fact helper, trajectory skill, profile
closure, and archive/propose/apply routing. Proof Runner and Evaluator extend the existing
consumer-outcome fixture/capture/replay family. Environment Identity includes the exact
kit/OpenCode/OpenSpec/Node/Git/model/profile/project/horizon/source digests. No task
authorizes consumer mutation, controller contact, installation, activation, remote action,
deployment, release, credential change, or changed product/protected semantics.

Profile: `Material`. Main owns production, integration, run-observe-correct, semantic
trajectory dispositions, and focused post-proof regression work. No reachable named
critical consequence currently requires independent SDET; a later evidenced critical
authorization, privacy, data-integrity, irreversible-action, or envelope-escape risk would
trigger the existing fresh test-only SDET route before qualification.

## 1. Reconcile Ownership And Extend The Existing Proof Owner

- [ ] 1.1 Re-run OpenSpec status, active-change ownership, repository-candidate, active global-source, profile, and consumer-proof inventories; record terminal, transfer, serialization, or narrowing evidence for every overlapping path and leave all unrecognized work untouched. **Current Rung:** reviewed proposal/design/current source. **Next Real Boundary:** provider-free trajectory fixture preflight. **Blocker/Unblocker:** production mutation of a shared path is blocked while `add-continuous-complexity-management` or another declared writer remains live or unknown; isolated new files and read-only inventory may continue only when their dependencies are unowned. **Observable Proof:** `openspec list --json`, `node tools/openspec-change-inventory.ts --root . --mode ownership`, `node global/bin/repo-candidate-snapshot.ts --root . --summary`, `npm run opencode:sources`, and the current profile/instruction inventory commands exit zero and produce one current ownership/source record. **Authorization:** local read-only inspection plus this change's planning/evidence records. **Safeguards:** no broad staging, restore, archive, adoption, deletion, or mutation of another active change. **Restoration/Cleanup:** none. **Expected Immutable Evidence:** current source/worktree/owner identities and exact unblock conditions.

- [ ] 1.2 [automation-dividend] Extend the maintained consumer-outcome fixture/capture/replay/evaluator family with a `delivery-trajectory` provider-free partition pack, reviewed seed records, schema/readback/drift checks, privacy/effect/cleanup oracles, and one proof-inventory row; do not add a second configured-session runner or semantic classifier. **Current Rung:** current consumer-outcome proof owner and reviewed claim population. **Next Real Boundary:** production Horizon/linkage/receipt schema round-trip in disposable projects. **Blocker/Unblocker:** task 1.1 ownership closure for every touched proof path; if the current runner cannot represent post-archive state and a separately correlated successor without weakening another pack, preserve the mismatch in design/history before adding only the smallest compatible extension. **Observable Proof:** preflight enumerates all `roadmap-delivery-trajectory-v1` members, baseline/candidate inputs are identical except reviewed candidate artifacts, and two provider-free replays are byte-stable with zero model/provider calls. **Authorization:** local fixture/test/evidence writes only. **Safeguards:** deterministic code validates explicit fields and exact fixture expectations only; no forecast, trigger, strategy, authority, or quality inference. **Restoration/Cleanup:** delete disposable projects/processes and retain bounded privacy-safe bundles. **Validation:** focused consumer-outcome preflight/evaluator/replay tests. **Dependency-Chain Stop Condition:** no configured trajectory call until this proof contract and its cleanup oracle are green.

## 2. Build And Prove Explicit Contracts And Fact Context

- [ ] 2.1 Implement versioned project-contained `horizon.json`, explicit proposal `Delivery Horizon` linkage, immutable triggered-review receipt schemas, and schema/readback materialization with contained relative references and cause-preserving errors; key each receipt by horizon id plus stable reviewed decision-context and trigger-evidence digests over ordered refs/content digests, while keeping archive identity and semantic trigger labels as non-key metadata. Verify valid, legacy-unlinked, `none`, missing, duplicate, malformed, contradictory, escaping, unsupported, volatile-archive, and changed-evidence fixtures through production entrypoints. **Current Rung:** provider-free reviewed seed pack. **Next Real Boundary:** operation-gate proposal readback over disposable linked and unlinked changes. **Blocker/Unblocker:** task 1.2 and unowned schema/gate paths from task 1.1; missing or contradictory intent remains invalid/unknown rather than inferred. **Observable Proof:** one valid horizon and linked proposal round-trip exactly; legacy/unlinked proposals remain readable; an unknown horizon or escaping reference exits non-zero before model, archive, Git, or source effects; later archive identity with unchanged decision evidence resolves the same receipt key; changed decision evidence resolves a new key and does not reuse stale disposition. **Authorization:** repository source/tests and disposable local files. **Safeguards:** no mutable horizon status, generated roadmap, no no-trigger receipt, semantic inference, retroactive linkage, or model-owned label in duplicate identity. **Restoration/Cleanup:** remove disposable fixtures. **Validation:** focused contract/materializer/operation-gate tests and `git diff --check` for touched paths.

- [ ] 2.2 Implement `global/bin/delivery-trajectory-context.ts` as one cohesive fact-only core and CLI with explicit `--root`, `--horizon`, `--archive`, `--format`, `--max-archives`, `--max-bytes`, and `--timeout-ms`; use stable bounded regular-file reads, containment, redaction, cancellation, and original-cause diagnostics. **Current Rung:** validated Horizon/linkage/receipt contracts. **Next Real Boundary:** actual CLI over a disposable linked archive window. **Blocker/Unblocker:** task 2.1 and ownership closure for imported utilities; stop if implementation requires extending active-change ownership inventory, Git candidate snapshot, or semantic classification. **Observable Proof:** `--help` and `-h` exit zero outside a repository and state inputs/effects/evidence/cleanup; the CLI emits stable privacy-safe paths/digests/sizes/support states for one linked archive and bounded predecessors without writing. **Authorization:** local source/tests and disposable read-only projects. **Safeguards:** no source payload, absolute private root, untracked contents, provider/model call, Git/OpenSpec mutation, trigger, forecast, N/K, strategy, score, or owner judgment. **Restoration/Cleanup:** remove disposable fixture; no persistent effect. **Validation:** focused helper contract/CLI tests and two identical invocations.

- [ ] 2.3 Complete helper failure and bound behavior for malformed schemas, symlink/path escape, missing/unreadable files, unsupported versions, archive/link mismatch, count/byte/time limits, and cancellation; preserve exact partial/blocked state without silently truncating acceptance-critical context. **Current Rung:** happy-path fact CLI. **Next Real Boundary:** production CLI failure matrix in disposable projects. **Blocker/Unblocker:** task 2.2; any ambiguous clean output blocks skill integration. **Observable Proof:** every reviewed invalid fixture exits non-zero or emits its exact supported partial state and cause, parent/source fallback never occurs, root identity stays redacted, and a positive valid control remains observable. **Authorization:** local disposable files and proof-owned processes only. **Safeguards:** no guessed horizon/archive/source and no swallowed or contextless failure. **Restoration/Cleanup:** terminate proof-owned processes and remove fixtures; cleanup unknown is failure. **Validation:** focused failure/bounds/cancellation/privacy suite. **Dependency-Chain Stop Condition:** do not add loaded semantic routing until valid and invalid production CLI behavior is green.

## 3. Add The Main-Owned Trajectory Skill And Runtime Availability

- [ ] 3.1 Add one thin `roadmap-delivery-trajectory` skill with precise discovery/stay-quiet metadata and a closed output contract for compact signal, five separate cost axes, honest forecast, trigger evidence, one of `continue | measure-next-boundary | replan-outcome-preserving | owner-required | unknown`, duplicate suppression, receipt seed content, and ordinary successor routing. **Current Rung:** proven fact helper and reviewed instruction contract. **Next Real Boundary:** loaded skill discovery and direct provider-free reviewed-case readback. **Blocker/Unblocker:** task 2.3 and terminal ownership for skill/catalog paths; current semantic evidence gaps remain `unknown`. **Observable Proof:** focused instruction fixtures select this skill for named roadmap/phase post-archive trajectory triggers, keep it quiet for ordinary retrospectives/local review/complexity/next-step/audit/campaign work, preserve archive state separately, and reject weaker acceptance or protected semantics as `owner-required`. **Authorization:** kit skill/catalog/test edits only. **Safeguards:** main retains disposition/mutation authority; no new Practice Owner, reviewer, campaign playbook, metric, every-change report, or automatic semantic helper. **Restoration/Cleanup:** fixture removal. **Validation:** skill frontmatter/schema/discovery/output-contract tests and instruction-artifact checks.

- [ ] 3.2 Add the exact skill/helper closure to `core` and `all`, update catalogs/materialization/doctor/readback surfaces, and prove one canonical source identity with on-demand skill content; custom profiles missing either artifact must report capability unavailable without fallback. **Current Rung:** source-valid skill/helper. **Next Real Boundary:** actual generated-profile OpenCode loader. **Blocker/Unblocker:** task 3.1 and terminal/serialized ownership for shared profile/validator paths; no always-loaded route is added before capability availability is proven. **Observable Proof:** source and generated `core`/`all` each resolve the skill/helper exactly once, direct helper help runs from the materialized source, skill content remains on demand, and a custom omission reports the exact missing capability without parent/adjacent-source substitution. **Authorization:** kit profile/catalog/generated-fixture edits and local generated-profile processes. **Safeguards:** no install or activation; no unrelated skill body added to core. **Restoration/Cleanup:** remove generated disposable outputs/processes. **Validation:** runtime-surface materialization/loader, source-identity, profile, doctor, and context-quality checks.

## 4. Integrate OpenSpec Routing And Prove The Smallest Complete Path

- [ ] 4.1 Add the explicit proposal field and compact archive/propose/apply routing to the canonical OpenSpec templates, skills, commands, global authority, and deterministic operation gate: archive completes first; successful linked archive then receives a signal; only the same horizon's later substantial dependent work consumes a current review disposition. **Current Rung:** loaded trajectory capability available in default profiles. **Next Real Boundary:** provider-free complete archive in a disposable linked project. **Blocker/Unblocker:** task 3.2 and terminal/serialized ownership for shared routing/templates/gates; failed archive never emits a success signal. **Observable Proof:** new proposals require a valid horizon or `none - <reason>`; legacy changes remain readable; successful archive reports `archive: archived` independently from `trajectory: not-applicable | none | review-required | unknown`; no trajectory state rewrites archived bytes/tasks/specs or changes archive exit status. **Authorization:** kit instruction/template/gate/test edits plus disposable OpenSpec operations. **Safeguards:** no final-history task, no archive reopen, no global project freeze, no inference of linkage or no-trigger. **Restoration/Cleanup:** remove disposable projects/processes; preserve bounded operation evidence. **Validation:** focused propose/apply/archive/operation-gate contradiction and regression tests.

- [ ] 4.2 Run-observe-correct the first configured happy path in a disposable project: create an explicit horizon and cohesive linked change, complete canonical archive, then observe one compact `trigger: none` signal through the actual installed/configured `core` entrypoint. **Current Rung:** provider-free integrated operation behavior. **Next Real Boundary:** configured successful archive/no-trigger signal. **Blocker/Unblocker:** task 4.1, current matched candidate/environment identity, green offline preflight/replay, and clear proof-owned writer/session/process lanes; a failed evidence-only call preserves its bundle and blocks unchanged repetition until terminal offline replay. **Observable Proof:** archive exits zero and remains terminal, signal names exact horizon/archive and five separate available/unknown axes, no receipt/reviewer/successor/history task appears, archived bytes/tasks/specs remain unchanged, and sessions/processes/fixtures clean up terminally. **Authorization:** minimum non-sensitive configured calls and disposable local Git/OpenSpec effects under standing kit-validation authorization. **Safeguards:** no consumer, remote, install, activation, deployment, release, credential, or protected product effect. **Restoration/Cleanup:** remove proof-owned session/process/fixture and retain privacy-safe immutable bundle. **Validation:** existing consumer-outcome evaluator plus two provider-free replays.

- [ ] 4.3 Run-observe-correct the smallest configured repeated-touch path: two materially similar linked archives expose one shared engineering owner, one current deep review records `replan-outcome-preserving`, and canonical propose authors exactly one bounded same-horizon successor with the earliest leverage proof. **Current Rung:** configured no-trigger archive path. **Next Real Boundary:** configured archive -> review receipt -> ordinary successor path. **Blocker/Unblocker:** task 4.2 and a current trigger tuple; any changed accepted population, proof strength, safety, or protected semantic must stop at `owner-required` rather than create this successor. **Observable Proof:** one receipt correlates horizon/review-context/trigger-evidence digests and separate N/K/automated/irreducible evidence, one successor references it and preserves the outcome/invariants, prior archives stay immutable, a later archive with unchanged decision evidence produces no second review or successor, and changed evidence reopens review only for a distinct material trigger or satisfied prior retry condition. **Authorization:** bounded synthetic configured calls and disposable local planning/OpenSpec writes only. **Safeguards:** no consumer implementation, no sampling, no evidence promotion across members, model-owned trigger label in key identity, or reviewer mutation authority. **Restoration/Cleanup:** all proof-owned writers/sessions/processes terminal; fixture removed and immutable bundle retained. **Validation:** configured evaluator, successor artifact/readback checks, duplicate and changed-evidence replays, and focused operation regressions. **Dependency-Chain Stop Condition:** do not expand claim partitions until both configured happy paths are terminal and green.

## 5. Complete Forecast, Boundary, Failure, And Diagnostic Coverage

- [ ] 5.1 Complete the generic claim partition pack for legacy/unlinked archive, shared-fan-out, forecast outside window, missing window/measurement, external-linear bottleneck, quality-weakening owner boundary, unchanged-trigger suppression, post-archive signal failure, default-core availability, and missing capability; retain one terminal observation or explicit unknown per declared `roadmap-delivery-trajectory-v1` member. **Current Rung:** configured no-trigger and outcome-preserving successor paths. **Next Real Boundary:** complete reviewed generic partition corpus. **Blocker/Unblocker:** tasks 4.2-4.3; each live failure has its own preserved-bundle replay gate and cannot authorize equivalent recapture. **Observable Proof:** calendar output is `unknown` without bounded duration/population evidence; engineering reduction and irreducible `O(N)` external evidence remain separate; quality weakening is `owner-required` without mutation; signal failure leaves archive green/trajectory unknown; missing capability does not fall back; duplicate tuple remains single. **Authorization:** provider-free fixtures and minimum bounded configured calls in disposable projects only. **Safeguards:** no velocity score, universal forecast, aggregate quality proxy, scope reduction, or equivalent verdict-seeking rerun. **Restoration/Cleanup:** all proof-owned sessions/processes/fixtures terminal and removed; raw bundles retained. **Validation:** complete partition evaluator, privacy/effect/cleanup checks, and two provider-free terminal replays.

- [ ] 5.2 Run the separately bounded read-only PMAC diagnostic against the current reviewed Phase 1 evidence and verify it identifies program-as-implementation-unit repetition, shared semantic/mapping owners, automated per-program processing, and irreducible serial per-program controller evidence without mutating PMAC or supplying generic population credit. **Current Rung:** complete disposable generic partition pack. **Next Real Boundary:** real external read-only diagnostic. **Blocker/Unblocker:** task 5.1 plus readable current PMAC evidence and exact no-write/source identity; unavailable or changed evidence leaves only this diagnostic unknown. **Observable Proof:** diagnostic names the evidenced N/K correction and retained external `O(N)` lane, records exact read-only refs/source digest, makes no controller/provider/source write, and is excluded from generic claim-member closure. **Authorization:** read-only local external project access only. **Safeguards:** no PMAC source/test/spec/task mutation, controller contact, credential use, runtime equivalence, population closure, or universal forecast claim. **Restoration/Cleanup:** no external effect; terminate proof-owned process and retain only bounded privacy-safe result. **Validation:** before/after external worktree identity and diagnostic readback.

## 6. Close Evidence, Challenge The Broad Claim, And Validate

- [ ] 6.1 Populate `evidence-index.json` for `roadmap-delivery-trajectory-v1` with current candidate/environment, baseline/candidate/replay/diagnostic lanes, one observation or explicit unknown per declared partition, cleanup/effect states, unresolved observations, and the maximum supported claim; absent or mismatched facts remain `unknown`/`missing` with no supporting reference. **Current Rung:** complete generic corpus plus bounded PMAC diagnostic. **Next Real Boundary:** independent broad-claim challenge. **Blocker/Unblocker:** tasks 5.1-5.2; stale, unreadable, mismatched, cleanup-unknown, or unsupported lanes cannot support a member. **Observable Proof:** schema/readback and operation-gate evidence resolution are green; every claim member maps to current immutable evidence or explicit unknown; the PMAC diagnostic remains separately labeled and host/provider/project limits remain explicit. **Authorization:** local change evidence/document writes only. **Safeguards:** privacy-safe refs, no raw prompts/credentials/private paths, no semantic promotion by deterministic tools. **Restoration/Cleanup:** none. **Validation:** focused evidence-index/operation-gate tests and current replay digests.

- [ ] 6.2 Launch one fresh read-only `evidence-sufficiency-reviewer` for the exact current candidate and `roadmap-delivery-trajectory-v1`; main independently dispositions each coverage/environment/oracle/closure row by correction, falsification, narrowing, or explicit unknown and reruns only invalidated lanes before retaining the challenge. **Current Rung:** schema-valid current claim evidence. **Next Real Boundary:** project-native freeze validation. **Blocker/Unblocker:** task 6.1; a reproduced broad-claim defect requires the smallest correction/narrowing and one fresh changed-candidate challenge, while optional non-critical risk remains parked. **Observable Proof:** challenge identifies candidate/model/environment/raw bundles, covers every declared partition, and the maximum claim does not exceed the exercised installed/disposable/read-only evidence. **Authorization:** one read-only specialist launch. **Safeguards:** no mutation, authority, nested orchestration, user question, lifecycle verdict, or generic confidence rerun. **Restoration/Cleanup:** none. **Validation:** main disposition and challenge refs read back current.

- [ ] 6.3 Run the portable apply gate, strict OpenSpec validation, focused Horizon/helper/trajectory/profile/operation/consumer-proof/evidence suites, source/profile/readiness/context-quality checks, `npm test`, `npm run validate:strict`, proof inventory/readback, and `git diff --check`; inspect exact status/stdout/stderr, candidate/worktree, effects, diagnostics, and cleanup, correct only accepted-outcome defects, and rerun affected lanes. **Current Rung:** current challenged claim/evidence candidate. **Next Real Boundary:** local implementation handoff; install/activation and consumer adoption remain separate. **Blocker/Unblocker:** tasks 6.1-6.2; any non-zero command, stale evidence, unknown cleanup, active overlapping writer, or source/profile/candidate mismatch keeps only the affected lane blocked/unknown. **Observable Proof:** all applicable commands exit zero, replay digests are stable, every runtime/config-time artifact resolves once, no unrecognized file is modified, and handoff records outcome, candidate, proof, architecture, diagnostics, validation, limitations, rollback/cleanup, and external-operation state. **Authorization:** provider-free validation/readback only. **Safeguards:** no archive of this change, commit, push, install, activation, release, deployment, consumer mutation, or unrelated cleanup. **Restoration/Cleanup:** remove only proof-owned disposable outputs; retain bounded evidence. **Validation:** complete command/result matrix and scoped diff review.
