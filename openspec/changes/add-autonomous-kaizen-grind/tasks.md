## 1. Persistence And Cutover Foundation

- [ ] 1.1 Recheck current owner/worktree/runtime identities, validate the mutation-disabled `ownership.json` and its direct archive-before-acquire dependencies on `make-team-advice-evidence-triggered`, `add-prospective-consequence-rehearsal`, and `deliver-continuous-reusable-value`, then run one disposable Node/Bun SQLite write, WAL, lock-timeout, crash-reopen, integrity, and backup spike using the selected owner-local adapter. Current Rung: planning-only ownership plus source-verified runtime pattern. Next Real Boundary: conflict-free ownership readback followed by both installed runtimes committing and reopening the same proof-owned database with expected rows and no surviving process/lock. Blocker/Unblocker: missing or stale ownership, a live overlapping writer, or any runtime mismatch blocks the affected production path; resolve it through exact transfer or the smallest compatible owner-local mechanism, not a silent exclusion. Observable Proof: ownership inventory, exact Node/Bun invocations, versions, database path, transaction/readback, exit status, stderr, process closure, and cleanup. Authorization/Safeguards/Cleanup: planning files and a provider-free local disposable root only, no production store or project mutation, remove only proof-owned files after inspection. Focused validation: ownership inventory, strict selected-change validation, the new compatibility test, and applicable existing session/plugin SQLite tests. Claim row: `KZG-001` environment/runtime compatibility.
- [ ] 1.2 After all three declared predecessors archive with terminal writers, reread and record exact transfers, enable mutation only when the ownership inventory is conflict-free, then extend the Kaizen capture/lifecycle owner with the dedicated D:-root resolver, exact protected config and Grind-registry schemas, SQLite v2 immutable identities/events plus replay-only projections, transactions, migrations, controller/member leases, cursorable status, bounded diagnostics, and Node/Bun adapter; Grind remains a client and capture must append while Grind is disabled. Current Rung: task 1.1 runtime compatibility plus archived-predecessor ownership transfer. Next Real Boundary: direct production module invocation creates, writes, replays, and reopens one disposable v2 lifecycle from both capture and disabled-Grind processes with exact projection and no partial acknowledgement under injected failures. Blocker/Unblocker: task 1.1, archived MTAET/PCR/CRVD predecessors, terminal writers, exact transfer readback, and conflict-free mutation ownership; stop before any production or plugin/profile mutation while one fact is unknown. Observable Proof: exact transfer inventory; exact config/manifest/registry key and range matrix; `dataRoot` and `OPENCODE_KAIZEN_DATA_DIR` both resolve to one inspected absolute lifecycle path with the database directly beneath it and no duplicated `kaizen` segment; activated v2 rejects writable v1 fallback; committed/replayed records, claim conflict, lock/rollback behavior, original-cause diagnostics, and cleanup are observed. Authorization/Safeguards/Cleanup: no provider, host task, installed config, project, or C:/D: production mutation; disposable absolute roots only. Focused validation: ownership inventory, propose/apply ownership checks, store/config/registry tests including exact root identity, serve/attach environment injection, concurrent capture, disabled Grind, deduplication, corruption, 5,000 ms lock timeout, transaction rollback, and privacy output.
- [ ] 1.3 Implement idempotent JSON v1 import, persisted exact-source identity, complete local OpenCode process classification, fold-equivalence comparison, quiescence/cutover state machine, target canary, D:-owned rollback backup, and exact recorded-source cleanup plan without activating it; prove a populated population larger than 25 including duplicates, decisions, diagnostics, checkpoints, resolved and pending signals, concurrent pre-cutover capture, unmatched process, interrupted import, and repeated resume. Current Rung: direct v2 lifecycle. Next Real Boundary: provider-free migration of a disposable exact-schema legacy store to a `D:\OpenCode\data\kaizen`-shaped lifecycle root with complete identity/state equivalence and source preserved until every candidate writer is terminal/isolated and canary succeeds. Blocker/Unblocker: tasks 1.1-1.2 and a qualified process/root liveness source with positive control; any unmatched process blocks. Observable Proof: canonical imported source path/digest, process/root classifications, source/target counts, refs, relationships, folded states, canary, cutover transition, rollback backup, exact-path cleanup, injected-failure state, no split authority, and attributable cleanup. Authorization/Safeguards/Cleanup: no actual production Kaizen deletion or runtime restart; proof-owned source/target/processes only. Focused validation: migration, replay, concurrent-append, unmatched-process, restart, corruption, wrong-default-path, and sibling-preservation tests. Claim row: `KZG-001` migration population.

## 2. Grind Controller And Project Routing

- [ ] 2.1 Implement immutable cycle and non-portfolio execution-record transitions, global lease, high-water member freeze, stable pages of 25, finite default budgets, automatic continuation eligibility, provider-free status, graceful stop, and replay-based crash recovery without project dispatch. Execution records shall contain only controller execution identity, route, retry/gate, Campaign/Mission correlation, optional canonical Beads ID, and execution handoff, with no portfolio-owned fields or authority. Current Rung: migrated disposable store. Next Real Boundary: direct controller entry processes a provider-free synthetic population larger than 25 to complete per-member decision/gate records across multiple invocations while later signals wait for the next cycle. Blocker/Unblocker: task 1.3 and exact config/state identity; no semantic provider or source writer yet. Observable Proof: watermark totals, page/cursor order, execution-record schema and absent portfolio fields, consumed budgets, continuation timestamps, lease exclusion, crash/replay identity, stop suppression, late-signal partition, diagnostics, exit status, and cleanup. Authorization/Safeguards/Cleanup: local disposable data only; deterministic fake semantic result inputs; no model, host task, or project mutation. Focused validation: cycle, execution-record schema, lease, budget, retry, status, stop, restart, and truncation tests.
- [ ] 2.2 Implement the distinct protected Grind project registry without changing campaign-supervisor v1 exact keys; validate privacy-safe project refs and standing project-contained `kaizen-remediate` definition digests, explicit Kaizen provider/local-commit/effect/hook policy, owner classes, non-portfolio execution records, exact cross-owner execution prerequisites, and clean/stale/disabled/unregistered/unknown gates. Add a conditional portfolio-owner check: when the separately enabled Beads bridge owns the selected project, require its canonical Beads ID and copy none of its status/dependency/priority/assignment/duplicate/terminal fields; for every other registration, infer no Beads identity or ownership. Current Rung: provider-free cycle controller. Next Real Boundary: controller routes reviewed synthetic signals to two disposable registered projects and kit owner while exposing no root through the shared store/status and launching no writer. Blocker/Unblocker: task 2.1, current campaign registry owner recheck, and standing definition fixtures; no audit or Beads authority inheritance. Observable Proof: exact registry keys/ranges, canonical root/digest/ref matches, standing definition policy, selected-project canonical Beads correlation, absent Beads correlation on another registration, split-owner execution prerequisites, absence of portfolio fields, registry drift/escape rejection, campaign v1 regression, no disk scan, privacy-safe output, zero repository effects, and cleanup. Authorization/Safeguards/Cleanup: only disposable Git roots and protected registry/portfolio-owner fixtures; no provider, Beads process, or source mutation. Focused validation: both registry schemas, execution-record schema, optional portfolio-owner binding, bounds, path containment, privacy, owner routing, stale config, hook policy, and project isolation tests.
- [ ] 2.3 Implement the production semantic classifier port as one visible read-only managed-server triage root session per cycle, transactional per-member claims, bounded decision/execution-record tools, actionable migrated-decision eligibility, manual-triage claim rejection, exact daily/invocation budget accounting, and no source/OpenSpec/host effects. Current Rung: routed provider-free execution records with fake semantic inputs. Next Real Boundary: one bounded configured-provider cycle session processes more than 25 claimed mixed signals across pages, lists visibly under the kit owner, emits exact decisions/execution-record seeds, includes first-run actionable `project-change`/`kit-candidate` records, and excludes terminal or already completed work. Blocker/Unblocker: tasks 2.1-2.2, current managed server/session identity, finite Kaizen daily/provider authority, and clear member claims. Observable Proof: triage session/title/metadata, claimed refs, manual conflict, decision/execution-record outputs, provider calls and daily reset, zero project Git/OpenSpec/task effects, process/session closure, and cleanup. Authorization/Safeguards/Cleanup: bounded synthetic signals/provider calls, read-only project inspection only, no source writer or remote effect. Focused validation: classifier, claim, status, triage-command, budget, migrated eligibility, and privacy tests.

## 3. Project Campaign And Visible Development

- [ ] 3.1 Extend Work Campaign contracts, preflight, semantic phases, state, report/handoff, and tests with the exact `kaizen-remediate` playbook while preserving `audit-remediate`; consume one frozen campaign input that references the exact non-portfolio execution-record projection through the standing onboarding-owned project definition, keep accepted-outcome/evidence/path/effect/candidate facts in the campaign input rather than `execution_records`, enforce Grind-only host resume, and freeze one correlated Roadmap Mission wave. Current Rung: task 2.3 production decision/execution record with no writer. Next Real Boundary: provider-free campaign preflight and deterministic semantic fixture consume one campaign input referencing one exact project execution record, reject missing/stale definitions and mismatched owner/candidate/Kaizen-effect identities, remain invisible to campaign-supervisor v1 resume, and emit the exact frozen mission/handoff without source mutation. Blocker/Unblocker: tasks 2.2-2.3 plus current Work Campaign owner and clean-tree requirements. Observable Proof: definition/campaign-input/execution-record/Grind-registration/campaign/mission digests, accepted/rejected phase transitions, audit playbook/supervisor regression, Grind-only resume, writer separation, status, result schema, and cleanup. Authorization/Safeguards/Cleanup: disposable project and fake semantic executor only; no real provider, session, host task, or source writer. Focused validation: campaign contracts/controller/playbook/supervisor suites.
- [ ] 3.2 Extend Roadmap Mission with one canonical-root-scoped mutation lease shared across all campaigns/missions, same-managed-server session metadata/visibility, in-flight request ownership, and automation-to-user takeover; prove audit-vs-Kaizen lease exclusion, observation, explicit takeover, submitted human input, abort acknowledgement remaining unknown, write isolation, and correlated return/resume without source mutation. Current Rung: frozen mission handoff. Next Real Boundary: one proof-owned managed OpenCode server lists the created ordinary root session under the exact disposable project directory and both API session facts and visible UI observation identify the same session/store while a competing campaign cannot create a writer. Blocker/Unblocker: task 3.1, available managed-session/request API, current project command/instruction identity, and Nuphus visual observation capability; if UI or in-flight closure identity is unavailable, block rather than accept abort/status heuristics. Observable Proof: project lease, endpoint/data identity, session list/title/metadata, same root session across command fixtures, read-only observation, in-flight abort/isolation, takeover process/request/writer/cleanup transitions, no replacement writer, exact closure, and cleanup. Authorization/Safeguards/Cleanup: proof-owned server/session/project, bounded synthetic calls only when required, no existing OpenCode process termination, no remote effect; remove only proof-owned process/data/screenshots after inspection. Focused validation: mission lease/preflight/executor/stop/launcher tests and one visible-session proof.
- [ ] 3.3 Execute the smallest complete one-project Kaizen happy path through controller, `kaizen-remediate`, Roadmap Mission, visible project session, canonical `opsx-propose` then `opsx-apply`, real-boundary proof, project validation, archive, scoped local commit, campaign handoff, selected portfolio-owner terminal transition, and execution-handoff completion. Current Rung: component owners integrated without source-writing outcome. Next Real Boundary: one disposable registered project receives a representative accepted signal and produces one visible project-local archived implementation commit with terminal writer/cleanup and no remote effect. Blocker/Unblocker: tasks 1.1-3.2, clean project, current provider/budget authority, explicit disposable local-commit authority, and green project adapter; any unknown writer/proof/validation/archive/commit/portfolio-owner fact blocks closure. Observable Proof: exact candidate/runtime/config/registry/run/execution-record/optional-Beads/campaign/mission/session/change refs, absent portfolio fields in Grind, session conversation, runtime output/state, OpenSpec status/tasks/archive, validation, scoped Git diff/commit, selected portfolio-owner terminal result, no-remote canary, status closure, diagnostics, and cleanup. Authorization/Safeguards/Cleanup: one proof-owned project/server/session, finite synthetic provider calls, local commit only, no push/merge/release/deploy; remove only proof-owned runtime roots after preserving required current evidence. Focused validation: affected Kaizen/campaign/mission/OpenSpec tests plus project-native validation. Claim row: first complete `KZG-001` owner-project path.

## 4. Windows Lifecycle And Runtime Exposure

- [ ] 4.1 Add deterministic Grind control entrypoints and thin commands, core/all profile closure, exact config/manifest/registry examples and rejection matrix, protected task host/installer, managed credential-path injection, and provider-free preview/check/repair/enable/disable/run-now/status/rollback while source presence remains inert. Current Rung: direct project happy path with no host scheduling. Next Real Boundary: resolved profile and Windows dry-run fixture materialize exactly one command/controller closure and one `InteractiveToken` highest-run-level 03:00 task repeating every 15 minutes, with StartWhenAvailable, WakeToRun=false, IgnoreNew, PT5H, exact disabled-generation/nextEligibleAt no-op, and no host mutation. Blocker/Unblocker: task 3.3 and current profile/workstation owner recheck; overlapping user changes are preserved and serialized. Observable Proof: loader/profile inventory, exact keys/types/ranges/defaults, config accepted/rejected matrix, task principal/triggers/repetition/settings/action, credential path and ACL plan, protected paths/digests, logs/reload policy, stopping-unknown/pending-restart command matrix, operation effects/rollback preview, no task/process/restart/provider/project effect, and cleanup. Authorization/Safeguards/Cleanup: repository artifacts and disposable Windows fixtures only. Focused validation: runtime-profile, installer, workstation, portable-process, command/instruction, config, and Windows CI-envelope tests.
- [ ] 4.2 Run the protected Windows task lifecycle in a uniquely named proof-owned installation: install, check, run-now, daily/repetition/missed-interactive-logon simulation, ineligible provider-free wake, non-overlap, graceful four-hour budget versus PT5H timeout, managed-runtime unavailable/health recovery, disable while idle/active/unknown, drift/repair, rollback, and process/file/task cleanup. Current Rung: provider-free install plan. Next Real Boundary: actual Task Scheduler invokes only the pinned finite host, reuses the protected runtime credential path in memory, and status observes exact state while no existing task/process/project/data is affected. Blocker/Unblocker: task 4.1, verified effective elevation, proof-owned target/task/PID tree, and complete rollback plan; manual UAC is the only allowed interaction gate. Observable Proof: exact install invocation/status/stdout/stderr, task XML/principal/repetition/settings/action/readback, PIDs and descendants, no-op/eligible wakes, controller result/log generations, non-overlap, timeout unknown state, stop/writer closure, drift behavior, rollback readback, absence-source positive control, and zero attributable remnants. Authorization/Safeguards/Cleanup: local proof-owned host mutation only, no real Kaizen store or user-owned OpenCode process, no provider or remote call. Focused validation: Windows supervisor/installer integration tests and exact host lifecycle proof.

## 5. Migration And Installed Multi-Project Proof

- [ ] 5.1 Run a managed restart/cutover proof with a proof-owned populated C:-shaped v1 store and exact D: target: durable handoff, positively identified runtime stop/start, final import, loaded-plugin root readback, target capture canary, sole-authority publication, rollback backup, exact legacy cleanup, disable, and restoration. Current Rung: isolated migration plus protected task lifecycle. Next Real Boundary: newly started installed plugin writes only D: and status reports exact migrated state while the proven legacy Kaizen source is absent and every unrelated sibling remains byte-identical. Blocker/Unblocker: tasks 1.3 and 4.2, exact managed runtime ownership, current installed global source, and complete backup/readback; ambiguous ownership returns `pending-restart`. Observable Proof: process tree and installed config identity, before/after manifests, record/state equality, D: canary, C: scoped removal, sibling hashes, restart diagnostics, rollback, and cleanup. Authorization/Safeguards/Cleanup: proof-owned runtime/data roots and exact managed lifecycle only; never stop an unproven OpenCode process. Focused validation: migration/install/restart integration tests.
- [ ] 5.2 [automation-dividend] Materialize the reviewed complete `KZG-001` population through one maintained provider-free runner with legacy/v2 stores, registrations, controller/task state, managed-session fixtures, campaign/mission handoffs, Git effects, and failure/recovery cases; then exercise the same population through one bounded installed immediate/repeating cycle: more than 25 pending plus actionable migrated decisions, two disposable project owners, kit owner, late arrivals, duplicate/no-action/unregistered/protected/provider/dirty/manual-claim gates, one visible triage session, one completed project change, one completed kit change, automatic eligible/no-op continuations, project-wide lease exclusion, reviewed local-only hooks, local commits, crash/recovery, in-flight takeover, disable during active ownership, and no remote effect. Current Rung: each real owner boundary proved separately. Next Real Boundary: provider-free runner readback followed by installed end-to-end task/controller/triage/campaign/mission execution closes every captured eligible watermark member to a durable terminal work result or exact gate and retains later signals for the next cycle. Blocker/Unblocker: tasks 2.1-5.1, current finite Kaizen daily/provider authority, clean disposable projects, exact standing definitions/registrations/hook policies, proof-owned task/runtime, and clear live-attempt gate. Observable Proof: stable seed/schema/readback and a second byte-stable provider-free replay with zero configured calls or source effects; complete population/claim accounting, all identity chains, task/process logs, visible triage/development UI sessions, project runtime effects, OpenSpec archives, scoped commits, gate/resume records, writer/cleanup closure, network/remote canary, disable state, and attributable cleanup. Authorization/Safeguards/Cleanup: reviewed seed data, bounded synthetic provider calls only in installed mode, disposable projects/data/task/session/processes, no push/merge/release/deploy, no real backlog or user project mutation. Focused validation: proof-runner schema/replay/drift tests and all affected focused suites followed by project-native validation. Claim row: maximum supported `KZG-001` population/path.

## 6. Critical Risk And Final Validation

- [ ] 6.1 After current installed proof and accepted-scope completion, dispatch one fresh test-only SDET for reachable important signal/source loss, split store authority, uncontrolled repository mutation, takeover/writer-envelope escape, disable late mutation, and destructive rollback scenarios; main shall independently reproduce every row and add only the smallest confirmed critical regression/correction before re-running affected proof. Current Rung: complete `KZG-001` candidate from task 5.2. Next Real Boundary: independent critical tests exercise the same candidate and real safety boundaries without production authorship. Blocker/Unblocker: task 5.2 and terminal writer ownership; missing fresh SDET leaves the named critical evidence gate blocked. Observable Proof: attributed SDET result, exact incident reproducer, main disposition, any corrected candidate identity, affected runtime proof, and no unresolved reachable critical/non-deferrable defect. Authorization/Safeguards/Cleanup: test-only disposable scope, no production code by SDET, no real backlog/project/remote effects. Focused validation: SDET-authored critical tests plus all invalidated focused proofs.
- [ ] 6.2 Complete documentation for enable/disable/status, D:-root ownership, config defaults/reload, registered-project onboarding, visible sessions/takeover, provider/local-commit authority, gates, diagnostics, migration, backup, repair, disable, rollback, and no-remote behavior; verify examples resolve from the installed source and contain no personal path, credential, or activation claim. Current Rung: proved implementation contract. Next Real Boundary: a fresh installed-source readback follows the documented provider-free status/preview/disable path and reaches the stated observation. Blocker/Unblocker: tasks 4.1-6.1 and final command/config shape. Observable Proof: docs/profile/command cross-reference validation, example parsing, installed path readback, and secret/path scan. Authorization/Safeguards/Cleanup: documentation and read-only installed inspection only; no new activation. Focused validation: documentation/instruction/profile validators.
- [ ] 6.3 Run strict OpenSpec validation, complete affected project-native focused suites, full repository validation, Windows non-mutating matrix, installed `KZG-001` runtime proof readback, database integrity, no-remote and worktree-scope checks, writer/process/session/task cleanup, and final current-candidate review of every accepted requirement; keep the change unarchived until all tasks and proof gates are complete. Current Rung: corrected full candidate. Next Real Boundary: canonical `/opsx-archive` only after every task and requirement-linked oracle is green. Blocker/Unblocker: tasks 1.1-6.2, no known reachable critical/non-deferrable defect, and terminal proof ownership. Observable Proof: exact commands/exits/stdout/stderr, candidate/runtime identity, requirement-to-oracle matrix, known non-critical limitations, external-operation state, and clean attributable cleanup. Authorization/Safeguards/Cleanup: no production activation, remote operation, merge, release, deployment, or unrelated worktree cleanup from validation. Focused validation: `openspec validate add-autonomous-kaizen-grind --strict`, applicable focused suites, `npm run validate`, and current installed boundary readback.
