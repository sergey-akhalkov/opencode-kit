# Strategy History

## 2026-08-09 - Current-instruction baseline

- **Objective**: Determine whether the current loaded instructions already sequence a controller-backed emulator roadmap toward early real feedback.
- **Approach**: Run one bounded synthetic roadmap prompt through a fresh `plan` session using `openai/gpt-5.6-sol` with `xhigh`, the active workspace, and current custom global source; prohibit tools and mutations.
- **Evidence**: The response placed a qualification harness before emulator expansion, then a separately authorized bounded controller baseline, one vertical emulator slice, an early comparison campaign, and repeated qualified slices. It explicitly stopped dependent behavior when controller semantics or equivalence were unknown.
- **Outcome**: Strong baseline safety and sequencing behavior for an explicitly framed substitution scenario. It did not establish a durable general fidelity-ladder requirement, per-deferred-boundary unblocker, or deterministic mirror contract across planning surfaces.
- **Reason**: Existing substitution and Runtime Proof policy is strong but cadence is implicit outside clearly described emulator work.
- **Do-not-repeat condition**: Do not rerun a differently worded baseline prompt as a substitute for changing the causal instruction contract.
- **Evidence-based retry condition**: Rerun the exact prompt only in a fresh process after the candidate source changes, using the same model, variant, workspace, and active config identity.

## 2026-08-09 - Candidate roadmap attempt 1

- **Objective**: Prove that the first shift-left candidate preserves baseline safety and makes the general fidelity ladder and deferred-boundary unblocker explicit.
- **Approach**: Run the exact baseline prompt in a fresh OpenCode process with the same model, variant, workspace, and active config after updating canonical and mirrored instruction sources.
- **Evidence**: `implementation-evidence/candidate-roadmap-attempt-1.md` records the complete response and oracle disposition. Early harness, authorized real baseline, independent effect suppression, restoration, equivalence, and dependency-chain stop passed. Explicit ladder, per-slice rung/next boundary, and blocker/unblocker fields failed.
- **Outcome**: Red product-behavior proof. The candidate improved source coverage but did not demonstrate the accepted behavior delta.
- **Reason**: The source told roadmaps what facts to name but did not give a sufficiently explicit rendering contract; the model compressed those facts into phase prose.
- **Do-not-repeat condition**: Do not rerun the same candidate or merely reword the evaluation prompt.
- **Evidence-based retry condition**: Add an exact roadmap output contract requiring the fidelity ladder and per-slice `Current Rung`, `Next Real Boundary`, `Blocker/Unblocker`, and `Observable Proof`, then rerun the unchanged prompt in a fresh process.

## 2026-08-09 - Candidate roadmap attempt 2

- **Objective**: Verify the explicit roadmap rendering contract added after attempt 1.
- **Approach**: Run the unchanged prompt in another fresh process with the same model, variant, workspace, and active config.
- **Evidence**: `implementation-evidence/candidate-roadmap-attempt-2.md` shows earlier and safer sequencing than baseline, including immediate real baseline capture, recovery characterization before implementation, and parser work limited to the chosen slice. The response still omitted exact ladder and per-slice field labels.
- **Outcome**: Behavioral progress, but the exact-output oracle remained red.
- **Reason**: The model treated the field list as semantic planning guidance and compressed it into the user-requested six-step prose format.
- **Do-not-repeat condition**: Do not rerun the same advisory wording or change the evaluation prompt.
- **Evidence-based retry condition**: Make the fields normative within every roadmap item and state that omission makes a plan incomplete. Permit one final unchanged-prompt attempt; if the exact form still fails while semantics stay green, record model-adherence sensitivity rather than repeating external calls.

## 2026-08-09 - Full validation attempt 1

- **Objective**: Freeze the runtime-proven, SDET-tested candidate with complete repository validation.
- **Approach**: Run strict library validation, complete tests, OpenSpec validation, and instruction inventory in parallel after terminal SDET.
- **Evidence**: Strict library and all 10 OpenSpec items passed. Complete tests failed in 14 doctor cases because the new direct `active-authority.ts` marker checks made isolated copied-authority fixtures incomplete before their intended assertions. Focused routing/SDET tests remained green. The initial Node runner also named `test-model-profile.ts`, but diagnostic execution stopped at the library failure, so its causal status remains unknown until the library cause is removed.
- **Outcome**: Validation red; instruction behavior and maintained-surface drift guard remained green.
- **Reason**: The candidate duplicated shift-left enforcement in doctor active-authority inspection even though routing validation already checks all maintained surfaces. This widened an unrelated doctor fixture contract and exceeded the smallest dependency closure.
- **Do-not-repeat condition**: Do not update 14 doctor fixtures to duplicate policy that doctor does not need to own.
- **Evidence-based retry condition**: Remove only the duplicate direct doctor checks, retain routing validator plus SDET negative/fenced-decoy coverage, then rerun complete tests to reveal any independent model-profile result.

## 2026-08-09 - Full validation attempt 2

- **Objective**: Re-run complete validation after narrowing the doctor ownership boundary.
- **Approach**: Run focused contracts, strict library validation, and the complete test command on the corrected candidate.
- **Evidence**: Focused contracts passed with 56 tests; strict validation passed with zero warnings; all 147 library tests passed. The sole remaining failure was `committed presets cover the exact current agent catalog`: the concurrently added untracked `global/agents/session-completion-arbiter.md` appears in the live catalog while the expected baseline intentionally lacks that unrelated in-progress agent.
- **Outcome**: Our affected validator/library lane is green; shared-worktree full-suite result is red from an unrelated concurrent change.
- **Reason**: Full tests inspect the live worktree and cannot attribute the untracked agent to its separate active change.
- **Do-not-repeat condition**: Do not modify, stage, delete, profile-route, or otherwise absorb the unrelated session-completion candidate into this change, and do not rerun the same shared-worktree full suite expecting a different result without that candidate changing.
- **Evidence-based retry condition**: Create a disposable worktree from `HEAD`, apply only this change's tracked diff, install its local dependencies, and run the complete test command there. Treat that as scoped freeze evidence while preserving the shared-worktree failure as an explicit external validation limitation.

## 2026-08-09 - Disposable worktree patch transport

- **Objective**: Reconstruct only this change's tracked candidate in a disposable `HEAD` worktree without copying unrelated live-worktree changes.
- **Approach**: Pipe the exact intended-path Git diff into `git apply` in the disposable worktree.
- **Evidence**: Repeated `rtk git diff | git apply` attempts produced `No valid patches in input` because the wrapper rendered a summary rather than raw patch bytes. Calling the discovered native Git executable directly produced a clean apply; `npm ci --ignore-scripts` then installed four disposable packages.
- **Outcome**: Isolation setup succeeded after switching the patch transport mechanism.
- **Reason**: Token-saving command wrapping is incompatible with consumers that require byte-exact native diff output.
- **Do-not-repeat condition**: Do not pipe `rtk git diff` into patch consumers.
- **Evidence-based retry condition**: For a future raw patch pipeline, use the discovered native Git executable directly or another verified raw-output mode; continue to use `rtk` for human-readable Git inspection.

## 2026-08-09 - Post-proof instruction review correction

- **Objective**: Disposition the fresh read-only instruction review without weakening shift-left cadence or safety.
- **Approach**: Main reproduced each evidence row against operative source and corrected only confirmed ambiguities: Proof Runner versus automated test harness ownership, owner-controlled rung execution, independent effect suppression in compact mirrors, and Material post-MVP SDET gating in the numbered validation loop.
- **Evidence**: `implementation-evidence/instruction-review.md` records reviewer task `ses_01949b628ffeefZO9WJgMTa1od`, Effective Model, all five risk rows, and main disposition. `SL-HARNESS-01` was a concrete outcome defect because pre-MVP proof tooling was simultaneously required and forbidden.
- **Outcome**: Production/instruction candidate mutated after prior proof; affected runtime proof and validation returned to development. Terminal SDET remains attributed to the prior marker-guard candidate and is not repeated because the first no-confirmed-critical result permanently stopped SDET for this root.
- **Reason**: Ownership vocabulary used `harness` for both proof-driving production tooling and automated test-oracle infrastructure.
- **Do-not-repeat condition**: Do not route Proof Runner/capture/evaluator/restoration tooling to SDET and do not run another SDET attempt without the policy's confirmed-critical continuation predicate.
- **Evidence-based retry condition**: Run focused validation, then one fresh same-prompt model proof on the corrected source to confirm harness-first planning stays pre-MVP and production-owned; afterward replay full scoped validation.

## 2026-08-09 - Corrected roadmap proof

- **Objective**: Verify that the post-review candidate keeps the qualification harness in the pre-MVP production proof path without weakening authorization or cadence.
- **Approach**: After green focused contracts and strict validation, run the unchanged baseline prompt in one fresh process with the same model, variant, workspace, and active config.
- **Evidence**: `implementation-evidence/corrected-roadmap.md` records session `ses_0194419ddffeuozMbA8I6URdMC`. It builds the qualification harness before emulator breadth, captures a separately authorized effect-blocked/bounded real baseline immediately afterward, uses evidence to scope parser work, characterizes every dependent slice before implementation, and never routes the harness to SDET.
- **Outcome**: Green corrected Runtime Proof; Development-Stage restored to MVP.
- **Reason**: Explicit Proof Runner versus automated-test-harness ownership removed the contradictory route while preserving the shift-left plan.
- **Do-not-repeat condition**: Do not perform more provider calls for label polish or another equivalent roadmap sample.
- **Evidence-based retry condition**: Another model proof is warranted only after a future production-behavior instruction mutation or a concrete red accepted oracle.
