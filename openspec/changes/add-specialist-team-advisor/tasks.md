## 1. Freeze Ownership And Baseline Inputs

- [ ] 1.1 Refresh `openspec list/status`, current worktree and index state, all five active overlapping changes, and their exact planned/shared paths; validate `ownership.json` and `evidence-index.json`, record a path-level serial or explicit-transfer order, and keep `mutationEnabled=false` until `replace-instruction-limits-with-context-quality` has applied or this change has explicitly rebased and every overlapping writer is terminal or transfers the exact root. **Current Rung:** reviewed planning records. **Next Real Boundary:** provider-free apply operation gate over one conflict-free ownership graph. **Blocker/Unblocker:** the replacement predecessor plus unresolved `global/AGENTS.md`, compaction template, profile, runtime-surface, contract, or consumer-outcome ownership blocks only that shared mutation; applied/rebased replacement and terminal state or exact transfer unblock it. **Observable Proof:** the operation gate, ownership inventory, `npm run instruction:inventory -- --format markdown`, and `npm run instruction:canonicalize -- --check .` report one ordered context-quality contract and no unresolved concurrent writer, `STA-001` remains `unknown` with zero observations, and unrelated worktree changes are preserved. **Validation:** focused ownership/evidence/context-quality parsing plus `node <global-source>/bin/openspec-operation-gate.ts --root <repo> --operation apply --change add-specialist-team-advisor`. **Stop Condition:** do not author runtime behavior on a shared path while its writer identity is unknown or the replacement predecessor is unresolved.

- [ ] 1.2 Verify the installed OpenCode and official plugin/schema/source surfaces provider-free for root-effective `agent` and `skill` listing, custom-tool permission matching, current child-agent attribution, parentless-root resolution, hidden/control-plane filtering inputs, and plugin init/execute failure behavior; preserve exact version/source/API identities and update the smallest design/spec/task text if current facts require an equivalent safer adapter. **Current Rung:** planning assumptions with official API `unknown`. **Next Real Boundary:** disposable OpenCode process exposing an attributable advisor child and a missing-API core-startup control without loading repository candidate source. **Blocker/Unblocker:** inability to prove both caller identity and complete root-effective dispatchable catalog blocks catalog/profile/behavior mutation; an official current read-only route with fail-closed no-entry behavior unblocks it. **Observable Proof:** provider-free preflight records exact API shapes, one positive caller/root control, one non-advisor denial, one missing-API startup control, no prompt/body/raw-session/path disclosure, and terminal cleanup. **Validation:** current schema/source inspection plus a disposable no-provider runtime probe. **Stop Condition:** never substitute profile manifests, copied rosters, or path/keyword inference for a missing live catalog.

- [ ] 1.3 [automation-dividend] After task 1.1 transfers shared proof ownership and task 1.2 proves the runtime adapter, extend `consumer-outcome-regression` with the reviewed `team-advising` pack, nine stable scenario records, root/advisor/specialist/skill identity facts, scenario-specific disposable permissions, multi-turn scenario 7, writer-closure/effect capture, schemas, replay/evaluate modes, and effect-free `--help`/preflight; extend `runtime-surface-loader` only for exact profile/plugin/catalog readback. **Current Rung:** frozen semantics and runtime adapter. **Next Real Boundary:** provider-free pack preflight against unchanged loaded instructions. **Blocker/Unblocker:** shared runner transfer and task 1.2; no configured session until every malformed identity, unsafe root, unexpected child, nonterminal writer, overwrite, or cleanup fixture fails closed. **Observable Proof:** preflight covers all `STA-001` members, deterministic code checks only explicit facts, semantic expectations stay in reviewed scenario data, and help/preflight create no session/provider/product effect. **Validation:** focused runner/schema fixtures and proof inventory readback. **Stop Condition:** do not add another generic runner or semantic scoring/ranking helper.

- [ ] 1.4 Capture the unchanged-source same-model/profile baseline for all nine `STA-001` scenarios through the actual installed parentless root entry point before advisor, catalog, profile, or routing instruction mutation; preserve original goals, catalogs visible to the baseline, task/skill events, outputs, local disposable effects, model/context/latency facts, root/child/process state, and cleanup. **Current Rung:** green provider-free scenario pack. **Next Real Boundary:** actual configured OpenCode baseline in disposable repositories. **Blocker/Unblocker:** task 1.3 and verified configured non-sensitive model route; any unclosed session/child/root or target-worktree effect blocks later behavior mutation. **Observable Proof:** one immutable baseline row per population member, exact source/environment identity, no more than the reviewed call bound, no protected/remote/install/credential effect, and terminal cleanup. **Validation:** provider-free replay/evaluate over the preserved baseline plus privacy and target-sentinel scan. **Stop Condition:** baseline failure is diagnosed and corrected in the proof lane without changing loaded behavior or manufacturing expected routing.

## 2. Build The Isolated Catalog And Advisor Surface

- [ ] 2.1 Implement `global/extensions/specialist-catalog.ts` as one inert-init, execute-local, read-only plugin tool using the verified task-1.2 runtime API; enforce advisor-child and parent-root identity, hashed refs, safe id/class/availability fields, capped sanitized descriptions, hidden/control-plane/self omission, no body/path/session/secret output, stable order, cause-preserving `unknown`, and terminal diagnostics. Add its dedicated unit/contract tests without editing active `global/opencode.json`. **Current Rung:** immutable baseline and verified API. **Next Real Boundary:** disposable plugin loader plus authorized and denied tool calls. **Blocker/Unblocker:** task 1.2 exact API; missing caller/root enforcement keeps this task blocked. **Observable Proof:** ordinary core starts when listing APIs fail; attributable advisor call returns exact safe entries; main and another child receive no entries; private sentinels and absolute paths never appear; plugin source presence alone creates no active tool. **Validation:** focused plugin/schema/redaction/permission tests and exact stdout/stderr inspection. **Stop Condition:** no live global activation, static roster, broad caller fallback, or auto-discovered plugin path.

- [ ] 2.2 Add `specialist-team-advisor` with the exact discovery contract, inherited-primary model classification, dedicated non-reviewer validator branch, wildcard-deny/exact-read-glob-grep-catalog permissions, fresh mission/team-map body, `main-alone | team-recommended | unknown` schema, no nested dispatch, and no feedback/reviewer authority; synchronize governed agent/model catalog tests. **Current Rung:** proven catalog tool in a disposable loader. **Next Real Boundary:** isolated agent discovery, permission readback, and one catalog-only child call. **Blocker/Unblocker:** task 2.1; unresolved custom-tool overlay blocks agent retention. **Observable Proof:** loader discovers exactly one advisor, model route inherits primary, only reviewed tools are callable, generic reviewer rules do not apply, malformed reports fail visibly, and no task/edit/question/skill/network/external effect is possible. **Validation:** focused agent/frontmatter/model-profile/permission tests plus isolated debug readback. **Stop Condition:** do not add a routing skill, readiness mode, second advisor, or broader permission to make validation pass.

- [ ] 2.3 Add the advisor and non-auto-discovered catalog source to `core` and `all`; update the runtime-surface renderer so core emits exactly the materialized catalog-plugin URL while all retains its existing plugin set plus that URL once; synchronize profile manifests, config/template paths, README/catalog maps, inventory diagnostics, and context-quality checks without changing the active installed config. **Current Rung:** proven isolated advisor and catalog source. **Next Real Boundary:** materialized disposable `core` and `all` loaded by actual OpenCode. **Blocker/Unblocker:** tasks 2.1-2.2 and shared profile transfer; any plugin duplication, all-only plugin leakage into core, source collision, canonical-ownership failure, context-quality failure, or loaded-behavior regression blocks routing-instruction mutation. **Observable Proof:** core exposes one advisor and one catalog tool but no notify/session-env/PTY/guard/roadmap plugin added by this change; all exposes one catalog tool and full selected dispatchable catalog; excluded domain agents remain unavailable in core; source presence does not alter the active global runtime; size/token-proxy changes are reported diagnostically. **Validation:** runtime-surface profile tests, loader proof, config schema validation, model/profile inventory, instruction context quality, and current committed-authority/safety boundaries. **Stop Condition:** correct duplicate authority or behavior regressions; do not add a replacement size ceiling or silently omit the default advisor.

## 3. Prove The Smallest Mission-Control Happy Path

- [ ] 3.1 Replace overlapping main-only specialist-selection text with the compact parentless-root mission rule, narrow the Practice Owner zero-trigger sentence to owners only, add the exact main/advisor/owner authority split, add canonical conditional `Team Advice State` fields to `global/AGENTS.md`, synchronize only the runtime compaction prompt markers, and preserve existing safety, SDET, writer, proof, blocker, and owner-handoff kernels. **Current Rung:** isolated loader-visible advisor/catalog surface and frozen baseline. **Next Real Boundary:** actual loaded root behavior for one trivial and one non-trivial mission. **Blocker/Unblocker:** task 2.3 context-quality evidence and shared instruction transfer; a missing or duplicated canonical field, widened advisor authority, context-quality failure, or safety/loaded-behavior regression blocks capture. **Observable Proof:** exact structural markers show one canonical owner, no second routing skill or static roster, zero-trigger Practice Owner semantics remain exact, compaction calls no tools, active installed bytes remain unchanged, and inventory measurements remain diagnostic. **Validation:** instruction inventory/context-quality, agent/profile/compaction contract tests, diff/secret scan, and isolated candidate loader readback. **Stop Condition:** do not add a lifecycle phase, all-agent checklist, passive-main wording, generic reminder, or replacement size ceiling.

- [ ] 3.2 Run the isolated candidate through the actual installed root entry point for `trivial-owner-local-direct` and `non-trivial-single-domain-main-alone`; inspect advisor/catalog events, structured map, main disposition, output, effects, context/latency, session liveness, and cleanup, then correct and recapture only invalidated lanes until both pass. **Current Rung:** complete structural candidate. **Next Real Boundary:** first real loaded behavior signal. **Blocker/Unblocker:** task 3.1 and comparable baseline identities; failed caller/root/catalog attribution returns to task 2.1 rather than prompt tuning. **Observable Proof:** trivial work launches no advisor; non-trivial work launches exactly one advisor after bounded foraging, catalog is privacy-safe/current, `main alone` is accepted without ceremonial children, main completes the representative result, and no forbidden effect or leftover child exists. **Validation:** matched baseline/candidate evaluation and provider-free replay. **Stop Condition:** a faster/smaller candidate cannot compensate for a missed call, extra child, stale catalog, lost outcome, or incomplete cleanup.

- [ ] 3.3 Run `isolated-production-delegation` through the same loaded candidate: advisor returns one dispatch-ready bounded worker package, main validates and dispatches it, the worker writes only its disposable root and returns attributable evidence, main proves terminal writer cessation or isolation, integrates the artifact, and alone runs and inspects the representative command. **Current Rung:** runtime-proven direct and `main alone` routes. **Next Real Boundary:** mission-control delegation with a real local effect. **Blocker/Unblocker:** task 3.2; any unknown writer liveness, unexpected changed path, missing brief invariant, or worker-authored test/proof blocks integration. **Observable Proof:** exact root/advisor/worker graph, one isolated write package, no kit/active-global mutation, terminal worker closure before main proof, expected command output/status/side effects, and complete cleanup. **Validation:** changed-path manifest, target sentinel, raw stdout/stderr, matched scenario oracle, and provider-free replay. **Stop Condition:** main neither trusts the worker report blindly nor repeats the worker implementation solely for confidence.

## 4. Complete The Maintained Routing Population

- [ ] 4.1 Capture and disposition `multi-domain-bounded-team`, `procedural-skill-no-fresh-agent`, `conditional-later-specialist`, `active-profile-capability-unavailable`, and `overstaffing-negative-control` against the same candidate; correct only reproduced accepted-scope defects and recapture affected members. **Current Rung:** runtime-proven delegation happy path. **Next Real Boundary:** complete static-topology `STA-001` partitions. **Blocker/Unblocker:** task 3.3 and exact catalog/profile identity; malformed or unclosed child attempts block the next member. **Observable Proof:** distinct concerns receive only distinct available roles, procedural guidance uses a skill without a fresh child, conditional roles remain dormant, unavailable capabilities are explicit without checkout/static fallback, and broad review fan-out is absent. **Validation:** matched baseline/candidate rows, task/skill identity graph, semantic main disposition, privacy/effect/cleanup oracles, and offline replay. **Stop Condition:** no finding count, agent count, lifecycle stage, or generic quality possibility creates work.

- [ ] 4.2 Capture the multi-turn `mid-task-topology-change` and compaction continuity controls: one initial advisor map, one represented new concurrency/ownership fact, exactly one updated map before crossing that boundary, no reconsultation for ordinary progress or compaction alone, and correct stale-row invalidation after a changed catalog. **Current Rung:** green static-topology population. **Next Real Boundary:** dynamic mission continuation and fresh-session resume. **Blocker/Unblocker:** task 4.1 and current root/candidate/catalog/child identities; tool-less compaction cannot verify availability and must leave that check to resumed main. **Observable Proof:** exact turn/task counts, preserved canonical Team Advice State fields, no full catalog/report replay, terminal prior evidence retained, changed availability invalidates only dependent pending rows, and cleanup is terminal. **Validation:** multi-turn capture, compaction output readback, matched evaluator facts, and provider-free replay. **Stop Condition:** do not turn compaction, a completed package, or unchanged progress into a routing event.

- [ ] 4.3 Replay every preserved baseline/candidate bundle through the terminal evaluator, update `evidence-index.json` with one attributable row per `STA-001` member, perform the semantic smallest-team/main-mission disposition, preserve latency/context/rework facts without inventing timing conclusions, and correct or narrow only evidence-invalidated candidate behavior. **Current Rung:** complete configured-session population. **Next Real Boundary:** frozen reviewable candidate with explicit maximum claim. **Blocker/Unblocker:** tasks 4.1-4.2; any missing member, stale identity, unresolved material observation, unsafe effect, cleanup gap, or evaluator truncation blocks freeze. **Observable Proof:** deterministic replay has zero model/session/product calls and one stable digest; all nine rows have exact source/model/profile/environment and terminal state; maximum claim remains population-bounded. **Validation:** replay/evaluate repetition, claim-evidence schema/readback, privacy scan, and current-candidate gate. **Stop Condition:** evaluator-only changes replay raw bundles and never trigger unchanged configured calls.

## 5. Challenge, Validate, And Hand Off

- [ ] 5.1 Obtain current bounded maintenance/runtime observations from `instruction-artifact-reviewer`, `openspec-architecture-reviewer`, and `execution-safety-reviewer` against the exact frozen candidate for discovery/authority/compaction, responsibility/locality/integration, and plugin/catalog/permission/writer boundaries; main reproduces and dispositions every row without granting review authority or expanding scope. **Current Rung:** frozen runtime-proven population. **Next Real Boundary:** candidate with every triggered practice observation dispositioned. **Blocker/Unblocker:** task 4.3; unavailable owner evidence remains explicit and affects only its reachable invariant. **Observable Proof:** reports identify exact candidate and Effective Model, no owner mutates or dispatches, every plausible non-deferrable row is reproduced/disproved/unreachable, and corrections replay only affected lanes. **Validation:** report schema, task/session closure, main disposition ledger, and current proof after any correction. **Stop Condition:** no optional generic final-review ceremony or repeated unchanged owner call.

- [ ] 5.2 Because the custom catalog changes a reachable authorization/privacy surface, dispatch one fresh test-only `sdet-quality-engineer` after current proof and accepted-scope completion with exact candidate, root-effective catalog runner, private sentinels, non-advisor callers, hidden agents, malformed descriptions, missing APIs, and writer-isolation boundaries; main reproduces every critical row, corrects confirmed defects, and restores affected proof. **Current Rung:** practice-reviewed current candidate. **Next Real Boundary:** terminal independent critical privacy/authorization evidence. **Blocker/Unblocker:** task 5.1 and safe disposable runner; SDET cannot edit production or instructions and an unusable report leaves the risk `unknown`. **Observable Proof:** terminal `critical-risks-reported | no-critical-risk | blocked`, no prompt/body/secret/path/session disclosure, unauthorized callers receive no entries, and any correction has current Runtime Proof. **Validation:** fresh task identity/Effective Model, test-only write manifest, main reproduction ledger, terminal session/process cleanup. **Stop Condition:** no equivalent rerun on unchanged candidate/hypothesis and no non-critical test expansion.

- [ ] 5.3 Obtain one fresh `evidence-sufficiency-reviewer` challenge over the exact `STA-001` record and all preserved lanes; main dispositions the matrix, resolves or narrows every material gap, and records only the strongest supported population/model/profile/environment claim. **Current Rung:** terminal critical-risk disposition. **Next Real Boundary:** honest broad-claim closure. **Blocker/Unblocker:** tasks 4.3 and 5.2; missing or unusable challenge keeps only `STA-001` `blocked` or `unknown` and does not erase exact trustworthy rows. **Observable Proof:** independent challenge identifies exact candidate/model, maximum claim matches evidence, no representative case becomes universal routing correctness, and all report children are terminal. **Validation:** claim schema/readback, challenge/disposition refs, and provider-free replay. **Stop Condition:** no lifecycle or quality claim is inferred from call counts, green structural tests, or reviewer prose.

- [ ] 5.4 Run focused catalog/agent/profile/model/permission/compaction/proof tests, instruction inventory and context-quality checks, isolated core/all loader readback, `npm run validate:strict`, `npm test`, strict selected/all-change OpenSpec validation, apply operation gate, `git diff --check`, and scoped source/diff/secret review; inspect exact exits/stdout/stderr and repair only candidate regressions. **Current Rung:** current claim closure. **Next Real Boundary:** local implementation handoff. **Blocker/Unblocker:** task 5.3 and no unknown writer/child; unrelated existing failures remain separated rather than hidden or weakened. **Observable Proof:** every applicable command exits zero, active installation remains unchanged, source presence did not activate the plugin, candidate/evidence identities agree, no instruction-size ceiling has been restored, and no attributable disposable artifact remains. **Validation:** the listed project-native commands and exact result ledger. **Stop Condition:** validation success does not authorize install, activation, restart, commit, push, release, deployment, or remote mutation.

- [ ] 5.5 Write the local handoff with accepted mission/non-goals, main/advisor/specialist responsibility model, architecture and `build-minimal` disposition, Runtime Proof, catalog/profile/source identities, `STA-001` maximum claim, practice/SDET/challenge dispositions, validation, known non-critical limitations, rollback, cleanup, and `External Operations: not performed`; leave installation/activation/restart to a separate explicitly authorized operation. **Current Rung:** complete validated local candidate. **Next Real Boundary:** separately authorized installation and fresh-session activation, outside this change. **Blocker/Unblocker:** task 5.4 and no known confirmed reachable accepted-outcome or non-deferrable defect. **Observable Proof:** `Outcome: working`, complete evidence refs and limitations, no RC/stable claim, and unchanged active global config/runtime. **Validation:** handoff readback against proposal/spec/tasks and final worktree ownership scan. **Stop Condition:** do not claim universal expert discovery, deployed behavior, active-host behavior, or stable qualification from local implementation evidence.
