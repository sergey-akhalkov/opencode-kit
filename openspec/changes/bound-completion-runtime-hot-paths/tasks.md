## Shared Execution Envelope

Production writes are blocked until active `session-completion-guard` and `unattended-roadmap-orchestration` owners archive or explicitly transfer exact files/requirements. Provider-free fixtures use disposable databases, roots, processes, and fake arbiters; no provider, workstation, or remote effect is required.

## 1. Close Ownership And Capture Baselines

- [x] 1.1 Record exact active owner/dependency/transfer state for guard, session-delivery, scheduler/config, roadmap controller/process, and tests; verify no production edit starts while an overlapping writer is active.
- [x] 1.2 Add baseline fixtures for 100,000 unrelated sessions, continuous status mutation, 33 queued roots, cancellation, and hung child commands; record query plans, selected rows, passes/time, active/queue counts, timeout diagnostics, and cleanup on current source.

## 2. Bound Session Evidence Before Projection

- [x] 2.1 Implement parameterized root/descendant query and capability/index-plan checks without modifying the foreign runtime schema; verify selected output matches existing small fixtures and unrelated database growth does not change selected rows.
- [x] 2.2 Enforce depth/row/omission behavior before message projection; verify completion-relevant omission blocks and non-critical truncation is explicit.

## 3. Bound Guard Convergence And Shared Capacity

- [x] 3.1 Replace status convergence with the eight-pass/two-second envelope and one terminal diagnostic; verify stable state succeeds and injected continuous mutation terminates without spin or duplicate logs.
- [x] 3.2 Add process-wide FIFO scheduler with defaults active=2/queued=32, duplicate suppression, abort removal, and `finally` release; verify 33-root fairness/overflow, revision cancellation, and no third active provider call.

## 4. Bound Roadmap Processes

- [ ] 4.1 Define finite command-class defaults and bounded validation override, then pass explicit timeouts from every production caller; verify no undefined/infinite call remains in source inventory.
- [ ] 4.2 Prove hung inspection, Git, OpenSpec, validation, and cleanup-unknown fixtures terminate only owned process trees, preserve diagnostics, and do not advance mission/checkpoint state.

## 5. Integrated Reliability Proof

- [ ] 5.1 Run provider-free large-DB/concurrency/cancellation/timeout integration and compare baseline/candidate row, query-plan, memory, latency, queue, and cleanup evidence; correct regressions before installed proof.
- [ ] 5.2 Run an installed multi-root guard plus roadmap smoke proof with effect-blocked/fake provider routing; verify bounds, fairness, diagnostics, and cleanup through the loaded entrypoint without changing verdict/product semantics.

## 6. Complete Validation

- [ ] 6.1 Run focused guard/plugin/roadmap tests, performance/reliability proof, `npm test`, `npm run validate:strict`, selected strict validation, and `openspec validate --all`; keep any unrelated active-owner failure explicit.
