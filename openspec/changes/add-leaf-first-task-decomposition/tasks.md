## 1. Acquire Owners And Materialize The Leaf Contract

- [ ] 1.1 After `add-outcome-preserving-delivery-checkpoints` archives with terminal writers, re-run active ownership, canonical-spec, source/runtime identity, scoped diff, and proof-owner inspection; reconcile this change against the archived checkpoint contract, transfer exact overlapping owners, enable one mutation owner, and verify `ownership.json` reports no overlap. **Current Rung:** planning-only successor with `mutationEnabled=false`. **Next Real Boundary:** provider-free reviewed `LFTD-001` scenario materialization in task 1.2. **Blocker/Unblocker:** predecessor archive, terminal writer closure, and explicit owner transfer; no production or proof-owner mutation may start before all three are current. **Observable Proof:** one ownership readback names the exact current source identities, transferred requirements/write roots, no live predecessor writer, and no mixed candidate. **Validation:** ownership inventory/readback, strict selected OpenSpec validation, and scoped status/diff inspection.
- [ ] 1.2 [automation-dividend] Extend the smallest existing instruction/consumer-outcome and completion-frontier proof owners with one reviewed `LFTD-001` seed population covering proactive compound decomposition, recursive independent prerequisites, same-leaf local failure, parent suppression, integration-only failure, independent siblings, cohesive Ordinary Small, grouped mechanical edits, owner/protected gates, compaction, and checkpoint composition; provide effect-free help, materialize/replay, stable explicit fields, and deliberate red controls without semantic scoring. **Current Rung:** reviewed proposal/spec population. **Next Real Boundary:** loaded ordinary-root behavior in task 2.2. **Blocker/Unblocker:** task 1.1 ownership acquisition; semantic expected judgments remain reviewed seed data outside helper code. **Observable Proof:** materialize/replay exit zero, positive and negative rows are stable, defective fixtures fail for the intended oracle, helper calls no provider, no helper infers compoundness or leaf quality, source remains unchanged, and disposable state is removed. **Validation:** focused proof-runner tests, seed/readback schema, malformed/bound controls, stable second pass, and inventory readback.

## 2. Deliver The Ordinary-Root Leaf Happy Path

- [ ] 2.1 Add the compact leaf-first activation and proportional controls to canonical loaded main and exact maintained planning/Material mirrors; require parent dependency suppression, same-leaf direct correction, scoped gates, distinct parent proof, and no process question while preserving existing earliest-real-boundary, checkpoint, blocker, substitution, authority, and Kaizen contracts. **Current Rung:** provider-free scenario pack from task 1.2. **Next Real Boundary:** configured disposable ordinary root in task 2.2. **Blocker/Unblocker:** task 1.2; do not add a skill, agent, planner, hierarchy schema, score, timer, or copied detailed contract. **Observable Proof:** current source has one detailed capability owner and compact role deltas; focused mutation controls reject missing positive triggers, proportional controls, parent stop, scoped-gate language, and no-semantic-inference markers. **Validation:** focused instruction/change-ready contracts, active/mirror parity, context-budget diagnostics, and provider-free scenario replay.
- [ ] 2.2 Through the actual installed/loaded entry point in a disposable non-OpenSpec project, present one accepted parent scenario with two independently observable prerequisites; observe root-to-leaf selection, proof of each leaf before the parent marker, distinct parent proof, zero process question, and no protected effect, then run matched cohesive and same-leaf-failure controls without task-tree ceremony. **Current Rung:** loaded candidate and deterministic contracts from task 2.1. **Next Real Boundary:** OpenSpec task authoring and recursive correction in task 3.1. **Blocker/Unblocker:** exact loaded source/model/environment identity and bounded synthetic provider authority; failed configured capture preserves a complete bundle and reaches terminal provider-free replay before another equivalent attempt. **Observable Proof:** ordered events show leaf A, leaf B, parent only after both, separate evidence refs, direct control execution, unchanged outcome/proof, bounded calls, and terminal process/session cleanup. **Validation:** configured evaluator over raw events, source/environment readback, provider-call bound, worktree effects, and replay equality.

## 3. Preserve OpenSpec Authoring And Apply Continuity

- [ ] 3.1 Extend canonical OpenSpec propose/apply and Material planning so compound tasks are authored as dependency-bearing leaves plus a parent integration task, hidden independent prerequisites add or reopen only the smallest child, same-leaf local defects avoid planning churn, and checked coarse parents reopen when their dependency or integration evidence is incomplete. **Current Rung:** proved ordinary behavior from task 2.2. **Next Real Boundary:** configured disposable OpenSpec propose/apply scenario in task 3.2. **Blocker/Unblocker:** task 2.2 and current post-predecessor OpenSpec owner; task corrections must preserve accepted semantics, history attribution, and scoped evidence invalidation. **Observable Proof:** provider-free fixtures show exact task/dependency deltas for proactive and recursive cases, zero writes for same-leaf correction, separate child/parent evidence requirements, and no unrelated proposal/spec churn. **Validation:** focused OpenSpec operation/task/claim contracts, strict fixture validation, history controls, and provider-free replay.
- [ ] 3.2 Through the loaded entry point in a disposable OpenSpec project, first author one compound parent as two leaves plus dependent integration, then expose a distinct hidden prerequisite during one leaf and observe apply add only that child, block the affected parent, preserve passing evidence, continue from the child without process approval, and keep a same-leaf control unchanged. **Current Rung:** candidate OpenSpec planning/apply source from task 3.1. **Next Real Boundary:** grind projection and compaction continuity in task 4.1. **Blocker/Unblocker:** task 3.1 and bounded configured-provider path; any failed live-dependent capture follows preserved-bundle replay before another equivalent call. **Observable Proof:** exact artifact diffs, task ids/dependencies, event order, separate leaf/parent proof refs, no duplicate history, unchanged accepted scope/proof, zero archive/remote/protected effect, and terminal cleanup. **Validation:** configured evaluator, strict disposable change validation, operation-gate readback, source/status effects, and provider-free replay.

## 4. Enforce Leaf Dependencies In Grind And Compaction

- [ ] 4.1 Extend the current completion arbiter/frontier instruction and fixture owners so a reconciled parent depends on every required leaf, a hidden prerequisite replaces a coarse runnable parent atomically, independent siblings remain runnable, child evidence cannot complete the parent, compaction carries only unresolved leaf/parent facts, and a due delivery checkpoint composes without a duplicate process gate; keep the frontier schema unchanged. **Current Rung:** ordinary and OpenSpec behavior proved in tasks 2.2 and 3.2. **Next Real Boundary:** installed grind scenario in task 4.2. **Blocker/Unblocker:** task 3.2 and current archived frontier/checkpoint contract; deterministic code validates refs, acyclicity, readiness, and correlation only. **Observable Proof:** provider-free rows expose exact child/parent/sibling/gate refs, reject parent-before-child and child-as-parent-proof verdicts, preserve checkpoint suppression identity, and accept existing schema-v1 records. **Validation:** frontier materialize/replay, focused guard/verdict/compaction tests, malformed/stale/cycle controls, and source diff.
- [ ] 4.2 Run the smallest configured grind happy path in a disposable root: two child items gate one parent, one independent sibling remains runnable, a new child is reconciled under the current generation, every eligible leaf and sibling completes before the parent, and the parent obtains its own oracle; verify no product/process question, protected effect, duplicate checkpoint, or schema migration occurs. **Current Rung:** green provider-free frontier behavior from task 4.1. **Next Real Boundary:** complete `LFTD-001` population in task 5.1. **Blocker/Unblocker:** task 4.1, exact primary/arbiter routes, and proof-owned disposable runtime; failed configured capture preserves raw events and reaches terminal offline replay before another equivalent attempt. **Observable Proof:** ordered frontier generations/events show child and sibling markers, parent suppression then eligibility, distinct parent marker, zero invented answer/question, unchanged scoped gates, bounded calls, and terminal liveness/cleanup. **Validation:** configured guard evaluator, provider-free raw replay, session-delivery readback, question/effect counts, and cleanup.

## 5. Close The Finite Claim And Validate

- [ ] 5.1 Complete every `LFTD-001` population member against one readable candidate across at least two unrelated disposable repositories and ordinary/OpenSpec/grind task classes, including proactive split, recursive child, same-leaf, integration-only, mechanical grouping, cohesive direct, owner/protected gate, compaction, checkpoint composition, and independent sibling controls; update the structured claim record with exact observed/missing rows and retain the narrow maximum claim. **Current Rung:** configured happy paths from tasks 2.2, 3.2, and 4.2. **Next Real Boundary:** focused regression decision in task 5.2. **Blocker/Unblocker:** all configured lanes current and every failed configured attempt terminally replayed; no representative row substitutes for another member. **Observable Proof:** each member has current candidate/environment/path/oracle/event/effect/cleanup refs, positive rows preserve leaf/parent ordering, negative rows stay direct, proof never narrows, and unresolved observations remain explicit. **Validation:** population evaluator, evidence-index schema/readback, cross-project isolation, stable replay, privacy/effect scan, and claim-ceiling check.
- [ ] 5.2 After current Runtime Proof, add only the smallest main-owned focused regression for a reproduced accepted-outcome behavior not already distinguished by the scenario pack, or record `none` with exact existing oracle evidence; rerun the affected production boundary after any test change. **Current Rung:** complete current population from task 5.1. **Next Real Boundary:** evidence-sufficiency challenge in task 5.3. **Blocker/Unblocker:** task 5.1; do not add coverage-count, style, optional optimization, or duplicate model-verdict tests. **Observable Proof:** one unique requirement-linked regression fails against the defective fixture and passes current behavior, or the no-gap record maps every realistic candidate regression to an existing oracle. **Validation:** affected focused tests and configured/provider-free boundary replay.
- [ ] 5.3 Freeze the current candidate and obtain one fresh read-only evidence-sufficiency challenge for `LFTD-001` finite population, project-neutral scope, installed ordinary/OpenSpec/grind paths, semantic trigger controls, parent evidence separation, and maximum claim; main narrows or blocks unsupported rows without mutation during the challenge. **Current Rung:** current population and regression disposition from task 5.2. **Next Real Boundary:** complete project validation in task 5.4. **Blocker/Unblocker:** task 5.2, one readable candidate/evidence index, and terminal production writers; missing reviewer evidence blocks only the broad claim and preserves narrower observations. **Observable Proof:** every declared member is current or explicitly missing/unknown, no HMI-specific or representative example is generalized silently, and every reviewer row receives a main disposition. **Validation:** evidence-index/challenge readback, candidate identity match, and zero mutation during review.
- [ ] 5.4 Run focused instruction/OpenSpec/change-ready/compaction/completion tests, provider-free scenario materialize/replay, all configured evaluators, instruction context/budget and runtime-source diagnostics, `npm test`, `npm run validate:strict`, strict selected and all OpenSpec validation, operation gates, evidence-index readback, `git diff --check`, and final scoped status/diff inspection; correct only current-change failures and preserve unrelated dirty files. **Current Rung:** challenged broad claim from task 5.3. **Next Real Boundary:** separately authorized installation/restart and operator use after implementation handoff. **Blocker/Unblocker:** tasks 1.1-5.3 and one current source/evidence identity; behavior mutation invalidates dependent proof. **Observable Proof:** applicable checks exit zero for one readable candidate, accepted tasks and claim rows are current, no known confirmed reachable non-deferrable defect remains, limitations and claim ceiling are explicit, and no planner/agent/schema/score/mandatory-small-task ceremony entered the candidate. **Validation:** the exact command set above and final OpenSpec status/readback.
