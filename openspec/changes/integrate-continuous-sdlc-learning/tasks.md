## 1. Capture A Reproducible Instruction Budget Baseline

- [ ] 1.1 Add `--scope runtime`, `--format json`, `--output <path>`, repeatable `--allow-add <path>`, and stable algorithm-version metadata to `tools/instruction-artifacts-inventory.ts`; runtime scope SHALL include only `global/AGENTS.md`, `global/agents/**/*.md`, `global/skills/**/*.md`, and `instructions/**/*.md`; run `node tools/instruction-artifacts-inventory.ts --scope runtime --format json --allow-add global/skills/continuous-sdlc-learning/SKILL.md` and require a complete per-file manifest plus the exact declared addition rather than top-N output.
- [ ] 1.2 Before editing any runtime instruction artifact, run `node tools/instruction-artifacts-inventory.ts --scope runtime --format json --allow-add global/skills/continuous-sdlc-learning/SKILL.md --output openspec/changes/integrate-continuous-sdlc-learning/implementation-evidence/instruction-runtime-baseline.json`; require baseline Git ref, exact sorted existing paths, per-file/total token proxy, algorithm version, content hash, and exactly one zero-contribution authorized added path.
- [ ] 1.3 Run `npm run instruction:feedback -- --pending`, `npm run instruction:feedback -- --decay-report`, and a deterministic inventory of real `docs/feedbacks` headings/statuses; save privacy-safe outputs under this change's `implementation-evidence/` without changing any feedback status.

## 2. Define The Canonical Prevention Contract

- [ ] 2.1 Update `instructions/leaf-reviewer-agent-contract.md` so its single canonical Prevention Signal section defines Source Event ID, Source Role, qualifying return stages, explicit known/unknown cause/recurrence/replay, scope hint, target, intervention, and privacy-safe evidence references; run `npm run validate` and require exit code 0 before editing role agents.
- [ ] 2.2 Add `global/skills/continuous-sdlc-learning/SKILL.md` with current-correction separation, exact Dream Team v1 mapping, phase-safe fallback, global/local routing, automation-first placement, generalization, curated lifecycle, and replay semantics; run `npm run validate` and require skill/frontmatter validation to pass.
- [ ] 2.3 Replace duplicated feedback/root-cause/helper prose in `global/AGENTS.md`, `global/skills/change-ready-sdlc/SKILL.md`, `global/skills/complain/SKILL.md`, and only role agents requiring a role-specific delta; preserve reviewer contract references and run `npm run validate` after each bounded artifact group.
- [ ] 2.4 Update `tools/contracts/reviewer-binding.ts` and relevant instruction validators to require the canonical reference/fields and reject copied full Prevention Signal blocks; run `npm run validate` and require the contract check to pass.

## 3. Bind The Frozen Dream Team Contract

- [ ] 3.1 Receive and verify the Dream Team Contract Checkpoint containing every wire/tool/qualification-profile version, exact valid/invalid fixture hashes, Dream Team Semantic Candidate Identity, Package Identity, Identity Recipe, local proof, final SDET, and local validation with no unresolved P0/P1; if absent, stop tasks 3.2-3.7 and continue only adapter-independent work.
- [ ] 3.2 Add the kit-owned v1 binding in the on-demand skill for exact tools `dream_team_learning_submit`, `dream_team_learning_status`, and `dream_team_learning_retro`; exact signal/ack/status/result/retro-request versions and fixture hashes; terminal embedded result; and `dream-team.learning-qualification-profile.v1`; do not add tool-name heuristics or copy Dream Team Zod schemas.
- [ ] 3.3 Add `tools/learning-signal-id.ts` with the exact domain-separated length-framed SHA-256 recipe and exhaustive eight-row identity matrix from design; run it twice for reviewer, worker, validation, SDET, delivery, correction-budget, unchanged-candidate, and explicit-friction events and require identical `srcv1-<64 lowercase hex>` output, then prove spaces, Unicode, colons, long ids, cross-row/domain, and component-boundary variants remain valid and distinct while unknown kinds or empty required components return blocked.
- [ ] 3.4 Add the compact conditional qualification-stage rule to `global/agents/sdet-quality-engineer.md`, `instruction-artifact-reviewer.md`, `final-candidate-reviewer.md`, and `session-delivery-reviewer.md`; preserve native direct output, exact agent names, permissions, freshness/test/read-only ownership, `dream_team_*` denial, and SDET immutable pre-pair -> provisional report/session -> orchestrator current-pair recapture/proof/validation -> same-session final handshake; run `npm run validate` and require exit code 0.
- [ ] 3.5 Update `complain` so repository append is allowed only before first candidate mutation or after local handoff; during freeze, proof, SDET, validation, final review, or delivery it SHALL return a privacy-safe `Feedback Candidate` without writing.
- [ ] 3.6 Update `docs/feedbacks/README.md` and reusable project guidance with raw-adapter versus fallback ownership, exact field mapping, every public version, qualification profile, nonblocking acknowledgement, terminal status/result, retro selectors, and phase-safe write rules; run `npm run validate` and require exit code 0.
- [ ] 3.7 Update Change-Ready routing so a qualifying return invokes the learning skill after owner routing is known, preserves current P0/P1 correction, records accepted/duplicate/rejected/unavailable accurately, and never blocks solely on optional binding absence; run `npm run validate` and require exit code 0.

## 4. Establish The Curated Lifecycle Source Of Truth

- [ ] 4.1 Add `docs/feedbacks/curated-prevention-ledger.json` with `opencode-kit.curated-prevention-ledger.v1`, initially containing no synthesized entries, plus the exact strict root/active/duplicate/superseded schemas, still-failing lineage/sourceRef, bounds, conditional fields, sorting, acyclic link-integrity, transition, and 2 MiB size rules from design under `tools/contracts/continuous-learning.ts`.
- [ ] 4.2 Refactor `tools/instruction-feedback-ledger.ts` into a thin CLI over explicit curated-ledger and Markdown-fallback modules under `tools/feedback/`; remove hidden `.opencode/state/instruction-feedback-ledger.json` default ownership while preserving explicit import/report support where migration evidence requires it.
- [ ] 4.3 Implement failure-atomic stable-order updates and allowed `open -> applied -> replayed -> resolved` transitions; `still-failing` SHALL preserve replay and create a new owner-routed record; run `node tools/instruction-feedback-ledger.ts --pending` and require deterministic output from the tracked empty curated ledger.
- [ ] 4.4 Implement one-time Markdown inventory/import reporting that preserves all existing complaints and open statuses without synthesizing root cause, resolution, replay, adapter submission, or curated promotion; compare with task 1.3 and require no missing source entry.

## 5. Enforce Generalization And Non-Growth

- [ ] 5.1 Add `tools/validators/continuous-learning.ts` for exact binding/mapping references, phase-safe fallback, curated-ledger schema/transitions, copied canonical blocks, and forbidden project identifiers in reusable candidates; register one orchestrator call in `tools/validate-library.ts`.
- [ ] 5.2 Add runtime-budget comparison to `tools/instruction-artifacts-inventory.ts` using `--compare <baseline> --fail-on-growth`; compare the union of baseline paths and authorized additions, charge each addition against zero, reject undeclared additions/removals and algorithm mismatch, and report stable per-file/total deltas.
- [ ] 5.3 Run `node tools/instruction-artifacts-inventory.ts --scope runtime --format json --compare openspec/changes/integrate-continuous-sdlc-learning/implementation-evidence/instruction-runtime-baseline.json --fail-on-growth`; require combined runtime token proxy and `global/AGENTS.md` token proxy not to exceed baseline.
- [ ] 5.4 Run `npm run validate:strict`; require canonical placement, exact field mapping, phase-safe fallback, curated state, exact binding, no duplicate full blocks, and instruction-budget checks to pass.

## 6. Prove The Complete Production Happy Path

- [ ] 6.1 In a temporary synthetic project before first mutation, supply the design's complete P1 fixture and mapping to the primary orchestrator through a deterministic accepted Dream Team tool seam; require current production correction plus one accepted opaque learning id without waiting for completion.
- [ ] 6.2 Return terminal status with embedded result through the seam, then submit retro by ids and filter; require exact result validation, duplicate attach, unknown-id blocked, and empty-filter no-action behavior.
- [ ] 6.3 Repeat with the exact binding absent; require one local fallback record before mutation, then freeze the candidate and repeat, requiring a non-mutating `Feedback Candidate` and unchanged Semantic Candidate Identity and Package Identity.
- [ ] 6.4 Exercise reusable, repository, both, investigation, product-only no-action, and validator-first examples through the canonical skill; require global output to omit local paths/commands and local output to preserve only owner-specific detail.
- [ ] 6.5 Apply one curated record through open, applied, replayed, and resolved in a temporary ledger; restart the CLI between transitions, require deterministic persistence, then run still-failing replay and require a new linked record without rewriting history.
- [ ] 6.6 Re-run the runtime budget comparison from task 5.3 and require no growth after the complete production change.

## 7. One Fresh SDET Wave

- [ ] 7.1 Dispatch one fresh `sdet-quality-engineer` with original requirements, complete production candidate, tasks 6.1-6.6 proof, exact production boundary, and test-only write scope limited to `tools/test-*.ts`, test helpers, and dedicated fixtures; production authors SHALL not edit automated tests, fixtures, fakes, snapshots, harnesses, or goldens.
- [ ] 7.2 Require that SDET wave to cover all eight identity matrix rows and collision-safe framing; every mapping field/bound; every exact public/profile version and fixture hash; binding/wrong version/rejection/duplicate/status/result/retro; qualification agent conditional/native compatibility, availability, permissions, envelopes, verdicts, and freshness; phase-safe fallback identities; every curated ledger variant/bound/link/ordering/transition/restart/still-failing case; generalization/project leakage; unrelated safety deletion; authorized/undeclared budget paths and growth; active-config/current-candidate mutation; and unauthorized activation.
- [ ] 7.3 Run every focused command returned by SDET, then `npm test`; require exit code 0 and record justified mock exceptions or blocked real boundaries.
- [ ] 7.4 Review the final SDET report against current candidate identities and post-test Applicable Proof. If it reports a qualifying P0/P1 production defect, route a bounded correction to the production owner, then run a new fresh corrected-candidate SDET and replay affected proof; otherwise record `no qualifying correction`.

## 8. Semantic Instruction Review And Binding Checkpoint

- [ ] 8.1 After final SDET and complete local validation, dispatch a fresh `instruction-artifact-reviewer` over every changed runtime instruction, immutable budget evidence, canonical placement, exact mapping/binding, phase-safe fallback, preserved authority coverage, and removed text; require `clean` or only non-actionable notes and no unrelated safety deletion.
- [ ] 8.2 If semantic instruction review requests a qualifying P0/P1 correction, route it to the production owner, then replay affected proof, a new fresh corrected-candidate SDET, budget comparison, validation, and instruction review; otherwise record `no qualifying correction`.
- [ ] 8.3 Freeze the Kit Binding Checkpoint with the exact Dream Team wire/tool/profile versions and fixture hashes, verified availability of all four kit-owned qualification agents, current kit Semantic Candidate Identity, Package Identity, Identity Recipe, proof, final SDET, instruction review, local validation, and no unresolved P0/P1.

## 9. Cross-Repository Validation And Final Gates

- [ ] 9.1 Run one real cross-repository smoke pinned to the Dream Team Contract Checkpoint and Kit Binding Checkpoint: current P1 correction SHALL remain binding, submit SHALL acknowledge immediately, status SHALL embed terminal result later, and the kit target SHALL retain an isolated proposal/candidate through a machine-owned detached commit without updating source/global primary refs or active config.
- [ ] 9.2 Record both checkpoint identities and the smoke result in each repository's implementation evidence; any candidate or Identity Recipe mutation after smoke SHALL invalidate it and require both checkpoints plus smoke to be recaptured.
- [ ] 9.3 Run `npm run instruction:inventory -- --format markdown`, the scoped comparison from task 5.3, `npm run validate:strict`, and `npm test`; every command SHALL exit 0 and the report SHALL include exact before/after instruction totals.
- [ ] 9.4 Run `npm run openspec:gate -- --operation prearchive --change integrate-continuous-sdlc-learning`; require exit code 0 before archive decision.
- [ ] 9.5 Dispatch one fresh final-candidate reviewer over requirements, complete smoke-bound candidate, proof, final SDET, instruction review, budget evidence, validation, cross-repository smoke, corrections, and residual risks; accept only `approved` or `approved_with_notes` with no actionable notes.
- [ ] 9.6 Run the repository's Material delivery/readiness gate and record accepted output before Change-Ready. Do not integrate, install, activate, update primary/user refs, push, archive, or clean retained candidates as part of this change.
