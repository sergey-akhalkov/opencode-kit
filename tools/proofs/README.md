# Proof Tool Inventory

Reusable production proof runners, capture/evaluator utilities, and cleanup helpers live here. Temporary directories may hold disposable server state and raw evidence bundles, but not the only source copy of a proof tool.

| Tool | Boundary and inputs | Modes and exact invocation | Authorization / external effects | Evidence and cleanup | Known limits |
|---|---|---|---|---|---|
| `session-completion-guard-retry.ts` | OpenCode SDK hidden-agent route and same-child bounded retry transport. Inputs: running local server URL, directory, optional agent name. | `preflight`: `npm run proof:guard-retry -- --mode preflight --server-url http://127.0.0.1:<port>`<br>`live`: `npm run proof:guard-retry -- --mode live --server-url http://127.0.0.1:<port>` | `preflight`: local disposable sessions only. `live`: two configured provider calls and therefore requires the recorded live-attempt/provider gate. | Emits one privacy-safe JSON result. Both modes delete root/child sessions in `finally`; cleanup failure is terminal. Preserve command, server identity/log, exit status, stdout, and stderr as the raw bundle. | Caller owns server startup/shutdown. `live` proves transport/schema behavior, not the complete root idle lifecycle. |
| `session-completion-guard-question.ts` | Production parser and controller question-verdict path. Input: one privacy-safe structured owner-boundary fixture matching the observed wire shape. | `bun tools/proofs/session-completion-guard-question.ts` | Offline only; no provider, server, credential, root prompt, or question-reject effect. | Emits final guard/question state and side-effect counts. Uses in-memory SDK adapters and performs no persistent writes. | Proves parser/application behavior; configured hidden-agent generation remains a separate transport lane. |
| `opencode-permissions.ts` | Installed OpenCode config/plugin loader and compiled permission rules for every resolved agent. Input: current working directory and its real global/project source layering. | `npm run proof:permissions` | Local read-only CLI invocations only; no model/provider calls, sessions, credentials, edits, or remote effects. | Emits one privacy-safe JSON summary with OpenCode version, agent names/count, checked permission kinds, and pass status. Resolved config is parsed in memory and never printed or persisted. | Proves effective permission rules for the current installed OpenCode process and source layering; it does not enable tools disabled separately by an agent's `tools` map. |
| `lib/opencode-proof-client.ts` | Shared OpenCode proof client API. Inputs: SDK client, directory, agent route, session title. | Import `proofClient`, `waitForProofRoute`, `disabledToolMap`, `createRoutedProofSessions`, `requestData`, and `deleteProofSessions`. | No effects until a caller invokes an SDK operation; session helpers create/delete local OpenCode state. | Returns typed route/session facts and preserves SDK errors as causes; cleanup is child then root. | Deliberately does not start servers, select authorization, call models, or evaluate scenario-specific output. |
| `deduplication-audit.ts` | Global dedup command/skill behavior and installed jscpd v5 boundary. Inputs: `quality-independent` profile, six fixed synthetic scenarios, current global source, and disposable repositories. | `preflight`: `node tools/proofs/deduplication-audit.ts --mode preflight --evidence-root <path> --capture-kind baseline\|candidate --candidate-id <id>`<br>`capture`: `node tools/proofs/deduplication-audit.ts --mode capture --evidence-root <path> --capture-kind baseline\|candidate --candidate-id <id> --scenarios all`<br>`cli-proof`: `node tools/proofs/deduplication-audit.ts --mode cli-proof --evidence-root <path>`<br>`evaluate`: `node tools/proofs/deduplication-audit.ts --mode evaluate --evidence-root <path> --baseline-root <path> [--candidate-root <path>]` | `preflight`, `cli-proof`, and `evaluate` are provider-free. Each `capture` scenario makes one configured-provider call under standing kit authorization or a separate gate. Bash is limited to `jscpd`; source edits, external directories, remote tools, arbitrary tasks, and package operations are denied. | Preserves route, tool calls, assistant text, token/time facts when exposed, source hashes, CLI stdout/stderr/exit, and cleanup. Sessions and roots are deleted in `finally`; cleanup failure is terminal. | Clone reports and evaluator facts never prove semantic equivalence. The runner covers synthetic scoped behavior, not a later production refactor or exhaustive audit. |
| `reuse-discovery.ts` | Reuse-registry client and same-model reuse-policy workflows. Inputs: `quality-independent` profile, eight fixed non-sensitive scenarios, current loaded kit source, and disposable registry/producer/consumer roots. | `preflight`: `npm run proof:reuse-discovery -- --mode preflight --evidence-root <path> --capture-kind baseline|candidate --candidate-id <id>`<br>`client-preflight`: `npm run proof:reuse-discovery -- --mode client-preflight --evidence-root <path> --candidate-id <id>`<br>`client-proof`: `npm run proof:reuse-discovery -- --mode client-proof --evidence-root <path> --candidate-id <id>`<br>`capture`: `npm run proof:reuse-discovery -- --mode capture --evidence-root <path> --capture-kind baseline|candidate --candidate-id <id> --scenarios all`<br>`evaluate`: `npm run proof:reuse-discovery -- --mode evaluate --evidence-root <path> --baseline-root <path> [--candidate-root <path>]`<br>`recover`: `npm run proof:reuse-discovery -- --mode recover --evidence-root <path> --abandoned-root <path> --session-id <id>`<br>`sanitize`: `npm run proof:reuse-discovery -- --mode sanitize --evidence-root <path>` | `preflight`, `client-preflight`, `client-proof`, `evaluate`, `recover`, and `sanitize` are provider-free. Each `capture` scenario makes one configured-provider call. Standing kit authorization or a separate provider gate is required. Local effects are limited to disposable Git fixture commits, isolated cache/state, registry/outbox/cache fixture writes, explicit session deletion, privacy restoration, and evidence output; remote/destructive tools and dependency mutation are denied. The existing credential store is read without copying or emitting credential values. | `client-preflight` preserves status/validation diagnostics before broader work. `client-proof` then drives the actual CLI through bootstrap, bounded query, enqueue, sync, and revalidation while capturing command/status/stdout/stderr, before/after hashes, Git state, and cleanup. Model capture additionally records exact prompt, profile route, event/tool/token/cost/time facts when exposed, and session deletion. Interrupted attempts use exact recovery; cleanup failure is terminal. | Uses direct `opencode run` instead of the shared SDK session API because that boundary is a fresh primary loaded instruction/command process. The committed profile is validated, then combined with a final inline proof permission map so later global `permission: allow` cannot broaden tools. The runner derives explicit facts only and never scores prompt quality. |

## Operating Rules

- Run `preflight` before `live` and preserve its JSON route evidence.
- `deduplication-audit.ts` uses direct `opencode run` because the accepted boundary is fresh primary slash-command loading; the shared SDK session helper does not expand that command path.
- Its provider-free `sanitize` mode rewrites only path strings in preserved JSON bundles: `node tools/proofs/deduplication-audit.ts --mode sanitize --evidence-root <path>`.
- Its provider-free `scenario-preflight` mode runs the installed CLI against selected synthetic workspaces before model capture: `node tools/proofs/deduplication-audit.ts --mode scenario-preflight --evidence-root <path> --scenarios id,...`.
- Its `evaluate` mode accepts `--baseline-override-root` and `--candidate-override-root` to compose corrected scenario-only captures without overwriting preserved earlier bundles.
- A live-attempt gate, provider authorization, and evidence/cleanup plan remain mandatory; this inventory does not grant authority.
- Output is privacy-safe and structural. Raw provider text and completion evidence are not printed.
- Before any configured-provider OpenSpec command proof, provider-free preflight the disposable root for the candidate command/skill source, complete `openspec/config.yaml`, the project-native operation-gate adapter and exact argv, a writable contained root when authoring is required, and an evidence/cleanup plan. Stop before inference when any item is absent; command loading alone does not prove the complete project authoring contract.
- Add every new proof runner or shared proof library to this table with its boundary, modes, effects, cleanup, and exact invocation.
