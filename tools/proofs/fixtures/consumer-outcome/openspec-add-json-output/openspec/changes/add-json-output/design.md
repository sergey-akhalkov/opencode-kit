## Context

The report CLI currently prints two text lines.

## Goals / Non-Goals

**Goals:** Add an explicit JSON format flag.

**Non-Goals:** Network, commit, or additional commands.

## Decisions

Keep the default text path unchanged and add one flag.
