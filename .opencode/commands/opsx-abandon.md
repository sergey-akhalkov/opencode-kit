---
description: Preserve an intentionally incomplete OpenSpec change without claiming completion
---

Load the `openspec-abandon-change` skill and follow its complete owner-selected preservation workflow for this command.

Treat `$ARGUMENTS` as the change name and plain-language reason. If the skill is unavailable, report that exact missing capability and do not recreate a partial abandon workflow in this command.
